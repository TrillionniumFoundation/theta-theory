# A2 revision 79

The new principal article is
`papers/A2-v17-boundary-information-coarsening/rigidity_v79.tex`.
The review source is `rigidity_v79.tex`; `tools-v79/build_v79.sh` regenerates the compiled review copy. A pre-publication local build is recorded in `BUILD_AUDIT_V79.json`.

The point-by-point response, proof ledger, source-preservation record and
build instructions are respectively `RESPONSE_TO_REFEREE_V79.md`,
`PROOF_LEDGER_V79.md`, `SOURCE_PRESERVATION_V79.json`, and `REVISION_V79.md`
in that directory.

This is an add-only revision payload based on the controlling v78 review
commit `f9de5e1e471df61f5ac02718d0c7b18b2e7d955e`. It preserves the complete
active proof content of the reviewed principal article and leaves the
periodic companion and all existing branches untouched.

Publication status: the revision source is materialized on the independent remote branch
`revision/a2-v79-robust-clock-quotient-contact-suspension-2026-09-17`,
based directly on the pinned v78 review commit. The source tree is published without
modifying `main` or the separate existing v79 branch. Remote CI is not counted as
success unless its run completes; the committed reproducibility scripts remain the gate.
