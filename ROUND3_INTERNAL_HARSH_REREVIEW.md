# Round-three exact-commit hostile rereview

**Reviewed materialized commit:** `a7461813cf49a9349ee4d5b85f203e507c87255d`  
**Review mode:** fail-closed, theorem-level, independent of LaTeX success  
**Initial verdict:** **additional mathematical repairs required before `main` may move**

The reviewed commit passes the archived-source audit, structural checks, all eleven clean builds, and the undefined-reference scan. Those facts do not discharge the following proof obligations.

## RR3-1 — A2 geometry, temporal UNI, and clock sign

The finite-horizon paragraph treated only the shortest corridor directions, while a complete proof must use the row-spacing formula for every primitive triangular-lattice direction. The one-step expansion constant was also asserted where a uniform finite-iterate expansion is the stable statement.

The temporal UNI paragraph named two inverse branches without first placing them on the stable quotient of an induced magnet. Its lower bound did not exhibit the collision points or the endpoint generating-function derivative. The replacement must use the two oriented triangular-gap return words, eliminate their internal collision variables by the specular critical equations, and compute the terminal momentum difference explicitly. The current/clock local-limit coordinates must also use

`(mean displacement, mean roof) = (partial_xi P, -partial_s P)`

because the twist is `exp(<xi,kappa>-s tau)`.

## RR3-2 — A3 one-big-excursion defect

The claim

`lim_L limsup_N N^{-1} log P(N^{-1} sum r_j 1_{r_j>L} > eps) = -infinity`

is false under a merely exponential return tail. A single return block of length `eps N` has a finite order-`N` cost independent of the cutoff. Consequently an ordinary empirical block measure loses a macroscopic excursion whose collision-time Palm mass is order one. The displayed pure ratio rate therefore misses invariant phases with zero magnet frequency.

The repair must construct the direct collision-time pressure with a defect/recession completion of the inducing scheme. The full collision-path rate is its Legendre dual. On phases of positive magnet frequency it reduces to the induced Abramov ratio; on zero-frequency phases it is the escape-pressure branch. Physical time is then obtained from the bounded finite-horizon roof, so no unbounded renewal truncation is needed.

## RR3-3 — B2 source ball versus full joint LDP

Normal convergence for `||psi|| <= r0` proves only a local exposed family. In the lower-bound proof, a bounded positive collision density `q` requires the source `log q`, whose norm is not controlled by `r0`; approximating arbitrary finite-entropy `q` makes that norm diverge. The upper bound has the same defect because the dual of a small source ball is not the full entropy action.

The repair must establish real-source continuation on every bounded source set by exact block-normalized Liouville composition and a priori Gaussian/moment bounds. It must then prove a balance-preserving regularization lemma for every finite-action pair. Truncation `q -> q_M`, smoothing, and a small reference activity must be accompanied by an explicit correction current solving the weak balance, with action convergence.

## RR3-4 — A4 Ray topology boundary

A right process admits a Ray realization, but strong continuity in the uniform norm on the entire bounded continuous space is not automatic for an arbitrary stationary prepared law. The exact general statement should use the bounded-strict topology of the Ray algebra. Norm-strong `BUC` and the quantitative Lipschitz estimate are available on the exposed spectral phases where the conditional Gibbs kernel is proved continuous.

## RR3-5 — C1 controlled likelihood normalization

The microscopic hard-sphere contact process is deterministic conditional on the initial state and is not a Poisson random measure with predictable compensator `A_{pi^epsilon}`. Therefore

`exp(sum log q - mu int(q-1)dA_pi)`

is not an exact finite-volume likelihood ratio. Predictable piecewise controls must be normalized on each block by the exact conditional log-partition of the full hierarchy. Its limit is the excess Hamiltonian

`H(f,p,log q)-H(f,0,log q) = int q(e^{Delta p}-1)dA_f`.

## RR3-6 — C2 topology and memory derivative

The path-potential dual must be the standard weighted bounded-strict topology, not a custom topology whose Radon-dual assertion is only sketched. The memory compatibility formula must explicitly pair the first derivative of the nonlinear history semigroup with a resolved test in `L^2(nu_Psi)`; the present notation `D E_t(0)[A](B)` is not typed.

## RR3-7 — B1 and B3 null directions

B1's exact-number coefficient needs its span-one lattice gap stated as a consequence of the positive hard-core activity, not only a safe-box heuristic. B3's positive quotient covariance must identify the annihilator with the closed balance-gauge range on the selected weighted spaces; varying one free flight and one collision is the separating argument, but the density of those local variations must be stated.

## Closure protocol

1. Install the six controlling replacements and the B1/B3 completion lemmas.
2. Regenerate all eleven controlling `main.tex` files.
3. Re-run theorem/proof/reference checks and all eleven clean builds.
4. Re-read the exact new commit, with special attention to A2 UNI, A3 recession phases, B2 arbitrary bounded sources, and C1 block-normalized controls.
5. Only after the rereview report has no remaining load-bearing blocker may the status change from `PENDING` to `PASS` and `main` be fast-forwarded.
