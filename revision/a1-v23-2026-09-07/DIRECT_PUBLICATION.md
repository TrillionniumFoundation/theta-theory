# A1 v23 direct publication

The authoritative readable revision is `papers/A1-english-v23/` on branch `revision/a1-english-v23-collision-spine-saturated-proof-2026-09-07`. Start with its `main.tex`, `README.md` and `RESPONSE_TO_REFEREE.md`. Its source tree is `5112f88508f642d181874ccbff2e4c76d1d448c9`.

The initial bootstrap commit `8ec300ca2e015ab8ccff51091644b554fea08846` attempted an Actions materialization. Run `34097210136` failed before any step or runner was assigned. No CI test or build success is claimed. The compressed parts and bootstrap scripts in this directory are retained as historical transport provenance, not as the source of truth and not as instructions for the current readable revision. Do not rerun the obsolete `materialize.py` or `publish.py` against this revision.

The final direct Git data commit includes all 772 source-manifest entries, the eleven active inherited generated TeX inputs, complete revised statements and proofs, the point-by-point response, and actual local execution summaries. Every edited source blob was compared with its locally validated identity. The compact manifest expands against the pinned v22 manifest and verifies the complete source set. The sibling v20, v21 and v22 directories and the controlling v22 report are unchanged.

Local validation completed with 178160 author checks (6939 new), optimization and inherited-proof corruption controls, the pinned v21 referee diagnostic (657 checks), the pinned v22 referee diagnostic (13884 checks), and a three-pass 125-page build with no undefined-reference or overfull warnings. These are finite and source/build checks, not a formal proof certificate or a new independent review.

The compiled PDF is supplied with the accompanying downloadable referee package rather than copied from an earlier Git tree. Its SHA-256 is `2c507678b11293403673439c7d233071488381c58b913b3e01984e4e906e78f6`. It is reproducible using `python build.py` from the new manuscript directory; PDF metadata can change its byte-level hash. The source and displayed mathematical content are the version identified by the manifest.

The workflow has been changed to manual validation of the already readable sources, with read-only repository permissions and artifact output. It neither materializes from the obsolete payload nor writes commits. No successful execution of that replacement workflow is asserted by this publication. Full local logs and individual receipts accompany the downloadable package; repository summaries identify their exact source and receipt hashes.
