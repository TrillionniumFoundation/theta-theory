# CM2 Round244 — Cross-Parent Same-Chart Bulk Quotient Rebuild

## Result

- Reconciled all `9,830` Round220 different-parent coordinate-coincidence candidates.
- Accepted exactly `328` rows with a parent-independent identity embedding in the same global source chart, an exact positive-area common refinement, and equality of the closed ten-field return signature.
- The accepted rows cover `60` exact keys and split as `292` `p`-faces plus `36` `t`-faces.
- Kept all `9,502` cross-chart rows fail-closed: `16` are nonidentity Klein-symmetry images rather than same-point glue, `260` have transformed-face mismatch, and `9,226` have no pinned same-point cross-chart transition.
- No edge was accepted from coordinate coincidence, box touch, key equality, or physical symmetry alone.

## Quotient Delta

- The `328` certified edges connect `292` previously distinct Round243 component pairs.
- Resolved-child bulk components reduce from `8,440` to `8,148`.
- No prior known-connectivity blocks merge; the block count remains `7,388`.
- Seed propagation adds `20` occurrence-to-known-block incidences across `12` exact keys.
- Global incidence advances from `36,180 / 53,968` to `36,200 / 53,968`.
- Remaining unattached occurrences: `17,768`.

## Verification

- Independent verifier status: `PASS_INDEPENDENT_ROUND244`.
- The verifier does not import or execute the Round244 producer.
- It independently reconstructs the full `9,830`-row candidate pool, the `328` accepted physical faces, both DSU quotients, the `20` incidence deltas, all `53,968` occurrence-frontier rows, and all `116` key-frontier rows.
- Hash seeds `244071` and `244929` reproduce byte-identical producer and verifier outputs.

## Strict Boundary

- These edges and incidences are certified known-connectivity lower bounds, not maximal physical-component assignments.
- A Klein symmetry maps physical points but is not same-point connectivity glue.
- The `9,502` rejected cross-chart coordinate candidates must not be recycled without new same-point transition evidence.
- Maximal physical components remain `0 / 53,968`.
- Exhausted exact-key fibres remain `0 / 116`.
- Global source-G exact-key dispositions remain `0 / 224,580`.
- Gate5 remains `10 / 18`; CM2 remains `NO-GO_FOR_CLAIM`.

## Required Next

Return to the retained/event-bearing frontier: map the Round242 `264` owner-shadow transition patches and `2,872` whole-root zero-absence continuations, then process the remaining `5,364` Round239 wall, crossing-time, source-seam, and whole-signature interfaces. Rebuild the quotient and the full occurrence/key frontiers after each physically certified batch.
