# CM2 Round 107 — local two-sided rank-3 cell-germ skeleton

Round107 materializes the first strict two-sided local atlas adjacent to the twelve corrected rank-3 third-tangency faces.  It closes the *local topological object* gap identified in Round106, but it does not pretend that a 24-row lower-bound skeleton is already the final homogeneous operator partition.

## Frozen construction

- Corrected tangent faces: **12**.
- Canonical registered anchors: **12**, selected deterministically by maximal strict rational source-core margin and then port ID.
- Side-labelled local witnesses: **24**.
  - `DESIGNATED_COLLISION_SIDE`, `D > 0`: **12**.
  - `BYPASS_SIDE`, `D < 0`: **12**.
- Complete face trace pairs: **12/12**.
- Positive-dimensional local IFT side germs: **24/24**.
- Complete immutable translated-candidate tables: **24 × 57 = 1,368** candidate rows.
- Canonical witness split depths: depth 4 on **20** rows and depth 5 on **4** rows.
- Faces with a source-grazing endpoint: **8**.

Each row freezes a canonical local witness box, strict discriminant sign, first and second owners, the unique true third owner, the complete immutable candidate tuple, the full root-classification ledger, a trace incidence ID, and locally verified collision-chart component signs.  The generator converts the translated-candidate generator immediately to an immutable tuple; no membership test is allowed to consume the generator as happened before Round98.

The two sides are not assigned the same word.  All twelve bypass rows have a third owner different from the incident tangent owner, so **12/24** provisional ordered-owner tuple keys differ from the incident face key.  Likewise, the bypass collision-chart target differs on **12/24** rows.  This is an explicit correction to the too-strong “same return word on both sides” wording in Round106.

## Exact scope boundary

Round107 freezes a **partial local lower-bound skeleton**, not a completed Gate5 operator atlas:

- partial topological side-germ skeletons: **24**;
- completed smooth homogeneous operator children: **0**;
- face-local maturity inherited only as an incidence label: **4/18**;
- cell-local official field maturity: **0/18**;
- new F5/F6 slots: **0 / 0**;
- complete 18-field blocks: **0**.

The stored `cell_word_key` is deliberately marked provisional.  It hashes the ordered three-owner tuple and is not yet a frozen official Gate5 word key.  The official grammar also requires the source normal chart, retained target lift, and monotone clean-wall record.  No official word-grammar crosswalk is claimed.

The stored prefix/suffix charts are local witness labels.  Round104/105 face-local F1–F4 slot IDs are not copied onto the new cells.  Cell-keyed restrictions and whole-tube suffix-chart enclosures remain pending.

There are three independent reasons why F5/F6 remain illegal here:

1. The official slot key is `(word_key, homogeneous_subbranch_id, roof_level_j, field_name)`, not a tangent-face ID.
2. The collision side approaches a genuine third-collision grazing trace.  It is not one finite homogeneous child; high homogeneity strips accumulate at the trace and require canonical recuts before any adapted-curve inverse-Jacobian or log-distortion composition.
3. The 52 historical `REGISTERED_PHYSICAL_ARC` links have not yet received an immutable-table whole-tube two-sided replay.  The 56 certified gap tubes also need to be exported and split by `D` sign, and the eight exterior rays need transverse endpoint collars.

Thus the universal one-step F5/F6 constants remain pre-characteristic-restriction seeds.  Neither invariant area preservation nor a collision-chart norm is substituted for the adapted unstable one-dimensional inverse Jacobian.

## Independent verification

The Round107 verifier does not import the Round107 producer.  It independently reconstructs the face/anchor/word/chart crosswalk from frozen upstream files and replays every rational witness at **640 bits**, against the producer's **512-bit** output.

- Reconstructed rows: **24/24**.
- Reconstructed trace pairs: **12/12**.
- Complete immutable candidate replays: **24/24**.
- Semantic projection digest: `5cc03304f7a0ea27bda60ea9d8a22a05ba1a18b4ee9ce93cc9e80cbe92f1e1b0`.
- Hostile semantic mutations rejected: **7/7**.
- Strict duplicate/nonfinite/unknown-key JSON attacks rejected: **3/3**.
- `producer_module_imported = false`.

An audit of the recent chain found that the Round102–105 verifiers imported their producers and reran the same `build` routine at higher precision; those checks are reproducible higher-precision replays, not independent implementations.  Round106 likewise compared against the same construction without a 640-bit independent path.  Round107 corrects the verification architecture.  Since none of those rounds promoted the global gate, this wording correction does not require a gate rollback.

## Next lawful assault

1. Replay complete immutable candidate ordering on both `D` sides over all **52** registered arcs, explicitly side-split the **56** gap tubes, and construct transverse collars for the **8** exterior rays.
2. Freeze the true side-specific path records and crosswalk them into the official Gate5 word grammar.
3. Install cell-keyed F1–F4 only after whole-cell domain, homogeneity-child, and chart restrictions are proved.
4. Decompose the collision collars into physical homogeneity children, apply canonical recuts at every collision, then compose F5 multiplicatively and F6 additively.
5. Continue F7–F18 and the complete-block audit.

Global Gate5 remains `10/18`, blocks zero; CM2 remains `NO-GO_FOR_CLAIM`.
