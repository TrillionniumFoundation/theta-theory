# CM2 Round 89 — rank-three projective gap closure frontier

Date: 2026-07-22

## Outcome

The `945` locally physical non-source ports from Round87 collapse to exactly
`56` immutable oriented tangent branches when keyed by source core, second
collision, third candidate, and signed tangency factor.  Every one of the
`449` registered physical-to-physical arcs joins adjacent ports in the strict
projective `q=u_y/(1+u_x)` order; there are no cross-key or skipped-rank arcs.

The complementary interior frontier contains `440` gaps.  Adaptive interval
graph chains certify `432/440` gaps on their whole tubes.  On every accepted
strip:

- one coordinate derivative of the third-tangency equation is strictly
  nonzero and the two dependent-coordinate faces have strict opposite signs;
- the first two collisions retain the immutable owner and outgoing chart;
- the nominated third tangent remains within `tau_max` and precedes every
  competing translated candidate;
- the signed tangency factor remains on the frozen branch.

The remaining eight gaps fail only the interval IFT boundary-sign enclosure at
the current maximum `2,048` strips.  No owner, flight-order, competitor, chart,
or tangency-sign failure occurs there.  They remain unresolved rather than
being joined by sampling.

## Exact projective ledger

```text
physical ports:                         945
live registered arcs:                   496
oriented projective tangent branches:    56
registered physical-physical arcs:      449
registered source endpoints:             47
interior unregistered gaps:              440
whole-tube certified gaps:               432
IFT-envelope residual gaps:                8
exterior branch rays still open:         112
```

Branch source-endpoint histogram:

```text
0 source endpoints: 21 branches
1 source endpoint:  23 branches
2 source endpoints: 12 branches
```

Every branch has even total frozen endpoint parity after its source endpoints
are included.  This parity is an accounting invariant, not a pairing of the
open exterior rays.

## Strict scope

This round removes `432` artificial gaps between registered box unions and
reduces the global continuation problem to eight interior IFT residuals plus
`112` exterior projective rays.  It does not type those exterior rays as
source exits, owner/chart seams, or rank-transition events.  Therefore it does
not create complete physical faces, RN rows, or a Gate5 promotion.

The global gate vector is unchanged:

```text
Gate1 NOT_CERTIFIED; Gate2 0/17; Gate3 NOT_CERTIFIED;
Gate4 1/7; Gate5 10/18 with zero complete blocks;
composite gates 0/5; CM2 NO-GO_FOR_CLAIM.
```

## Next exact frontier

1. Replace the eight residual linear strip chains by interval Newton or a
   projective-parameter reverse-billiard chart.
2. Continue the `112` exterior rays until each reaches a source-core boundary,
   an owner/chart/translation seam, or a genuine rank-transition event.
3. Build the final face quotient only after all physical port degrees are
   closed.
