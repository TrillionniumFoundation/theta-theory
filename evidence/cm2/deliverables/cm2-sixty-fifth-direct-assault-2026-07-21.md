# CM2 Round 65 full direct assault

Date label: 2026-07-21

Strict verdict: **0/5 complete composite gates; Gate 2 official fields 0/17;
Gate 4 landing join 1/7 with fields 1, 4 and 7 partial; Gate-5 maturity
10/18 with zero complete blocks; CM2 NO-GO_FOR_CLAIM.**

## Round result

Round 65 closes no composite gate and does not promote an average, selected
orbit, fixed-time, weak-space or external wrong-interface result.  It makes
ten strict advances in four compatible leaves.

1. The tempting selected-QNL numerical quotient

       (0.091)/(0.092809...)<1

   is proved not to instantiate the combined-Green all-plaque `q_cross`.
   Gauge, registry, quantifier, inequality direction and mathematical role
   differ.  An eight-depth two-plaque completion preserves the selected
   decay while making the second-plaque scalar `T_n=(-1)^n`.

2. For same-representative resonant limits, the complete four-wedge token is
   reduced to three scalar nonvanishing conditions.  With

       Psi=[[ab,au],[lb,lu+(ab)^-1]],

   all four wedges are nonzero exactly when

       l!=0, u!=0, 1+ablu!=0.

   The Round-64 critical matrix has one zero in this complete token; the two
   axis wedges previously used remain nonzero.  Actual same-representative
   scalars and the all-plaque limit are still absent.

3. Gate 3 gains one exact fixed-material calculus

       T_s=Qhat_s Phat_s Rhat_s.

   On retained face/side tags, a strong moving-restriction derivative exists
   exactly when the two-sided weighted trace map into `ell1` is bounded.
   Unit-`L1` triangular functions have trace `2N`, separating the frozen weak
   norm-one restriction from the required strong derivative.

4. A translation/Piola submodel proves that differentiability on every fixed
   `W11` vector does not imply operator-norm differentiability `W11->L1`;
   the normalized remainder tends to one.  One further uniform derivative
   would pay the model remainder.  Independently, stopped mass
   `p_n=1/[n(n+1)]` has finite total mass but divergent depth-weighted
   derivative charge, so physical `MT_DQ` needs another same-law ledger.

5. Gate 2/4's weighted-median theorem is extended to a general
   standard-Borel plaque outer law.  Under a common positive `L1`
   trivialisation,

       P_eta<=delta_eta<=2P_eta,

   and zero distance is exactly almost-everywhere marker equality.  The
   actual collision-SRB trivialisation is not materialized.

6. The all-depth stable problem is compressed to the exact first-failure
   identity

       eta(S_infinity)=eta(S_0)-sum_N eta(F_N)=lim_N eta(S_N).

   The physical 96-depth strip cannot control depth 97, and positive mass at
   every finite depth need not leave positive infinite survivor mass.  The
   actual stable-tree registry remains 0/7.

7. A tagged-BV sufficient bridge needs both the Round-64 zeroth-order
   envelope `W_D in L-infinity` and a weighted transverse-variation sum.
   Smooth two-tag kernels have `W_D=1` but variation `4N`.  A stronger exact
   trace argument rules out a bounded factorisation `L1 -> strong -> BV`
   whose composite is the identity.

8. Gate 5's lineage and cemetery rows are joined exactly.  Live transport
   plus a one-shot arrival split with mass bound `c` exists exactly when

       beta_(j+1)+alpha_(j+1)<=c beta_j,

   with sharp coefficient equal to the joint RN essential supremum.  A
   stationary live law plus positive arrival has coefficient greater than
   one; absorbing cemetery occupancy diverges for `w_Z>1`.

9. One common fibre-constant kernel pays all seven positive sectors exactly
   when `r_j<=1` and every marked ratio obeys its Feynman--Kac inequality.
   Base coefficient `1/3` can coexist with raw coefficient
   `2/3=w^(-1)` and a divergent raw ledger.  The oriented Round-54/Round-61
   positive bridge needs two RN dominations and first requires equality of
   forward/reverse total target masses.  No actual cross-time carrier or
   coefficient is frozen; the eight open Gate-5 rows stay open.

10. The Gate-3 current, Gate-4 tag and Gate-5 sector debts are unified by an
    exact positive path-potential criterion.  The full time-labelled lift
    from `L1(mu)` is bounded exactly when

        H(x)=sum_j w^j P_(0:j)W_j(x)

    belongs to `L-infinity(mu)`, with operator norm `ess sup H`.  A sharp
    separator has `w*kappa=1/2`, one-time moment 3 and all-time actual
    one-vector charge 6, yet `H(n)=2^(n+1)` is unbounded.  Average finiteness
    plus time decay is therefore still not strong assembly.

## Independent audit

The independent Round-65 audit passed without correcting a source leaf.

    new Python syntax including Round-65 common:       9/9
    dependency pin rows across four source leaves:    84/84
    source-leaf SHA artifact rows:                    19/19
    source integrity/replay/reemit:                     4/4
    source hostile semantic mutations:             1148/1148 rejected
    source hostile strict-JSON payloads:              16/16 rejected
    source default entry points:                       8/8 exit 2

The audit's own canonical replay, regeneration and reemit pass; it rejects
`76/76` hostile semantic mutations and `4/4` hostile JSON payloads, and its
two default entry points exit 2.  Across source leaves plus audit, all 24 SHA
artifact rows resolve.

The cross-leaf red team confirms in particular that:

- two nonzero axis wedges are not the complete four-wedge token;
- weak restriction and pointwise Piola differentiation are not strong
  operator differentiation;
- a finite 96-depth tree is not an invariant all-depth product tree;
- `W_D in L-infinity` without variation is not tagged BV;
- owner mass contraction does not pay raw-`Z`, Orlicz, current or tag costs;
- a finite actual all-time vector moment is not an `L1` operator bound; and
- signed equality does not pay an oriented positive common mode.

## Latest technology boundary

The current sources were checked directly.

- `arXiv:2603.19509v3` gives a nonautonomous sequence-space response
  framework, but assumes the strong differentiability and memory loss CM2
  still lacks; its applications are expanding/noisy systems.
- `arXiv:2604.25746v1` treats hyperbolic SRB measures of smooth compact
  boundaryless flows; its singularities are fixed points, not billiard
  collision discontinuities or moving boundaries.
- `arXiv:2510.19573v3` gives function-side weighted-`L-infinity` domination
  and Lyapunov criteria after dominated kernels are supplied; it does not
  construct the missing owner marginals or sector slice.
- `arXiv:2605.07824` is a one-dimensional killing/branching diffusion result,
  not a billiard owner/current theorem.
- The previously audited shrinking-hole, MME-product and transfer-operator
  review results remain wrong-perturbation, wrong-law or review-only for the
  required joins.

No external theorem is promoted.

## Strict final state

    Gate 1: NOT_CERTIFIED
    Gate 2: NOT_CERTIFIED; official fields 0/17
    Gate 3: NOT_CERTIFIED
    Gate 4: NOT_CERTIFIED; landing join 1/7, fields 1,4,7 partial
    Gate 5: NOT_CERTIFIED; maturity 10/18, complete blocks 0
    complete composite gates: 0/5
    CM2: NO-GO_FOR_CLAIM

## Shortest remaining routes

1. **Gate 1:** materialize the actual combined-Green all-plaque registry,
   prove a uniform Holder limit of `T_n`, transport the selected compact-QNL
   loop to the same representative, and compute `a,b,l,u` for the complete
   four-wedge test.

2. **Gate 3:** construct one finite-parameter material atlas `J_s`; prove the
   two-sided weighted trace map, anisotropic `o(s)` Piola remainder, strong
   cemetery/current sums and depth-weighted stopped derivative ledger; then
   install bounded differentiable `Rhat/Phat/Qhat` and `MT_DQ`.

3. **Gate 2/4:** construct a positive all-depth collision-SRB stable base,
   common root trivialisation, source/landing holonomies and commuting
   squares; prove `P_eta=0`, the essential path budget, weighted transverse
   variation and the physical strong/Piola recipient on one keyed registry.

4. **Gate 5:** materialize live, next-live and pre-cemetery arrival laws on
   one immutable cross-time label carrier; compute the joint split RN ratio,
   build finite raw-`Z`/Orlicz sectors, pay all seven sector potentials, and
   construct the oriented Round-54/Round-61 positive bridge before closing
   F5/F6/F10/F11/F14/F15/F17/F18.

The strongest common formulation is one task: on a single immutable actual
path/tree/material registry, prove an essentially bounded positive path
potential containing every sector, tag, current and one-shot-arrival charge.
Until that and the independent twisting/product-tree joins are physical,
the CM2 claim remains a strict no-go.
