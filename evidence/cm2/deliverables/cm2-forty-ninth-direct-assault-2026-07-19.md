# CM2 Round 49 full direct assault

Date: 2026-07-19  
Strict verdict: **0/5 complete gates; CM2 NO-GO**

## Gate 4: incidence-safe long-leaf reduction

For each admissible canonical proper standard family whose leaves carry the
Round-46 density ratio `<2000/1999`, the Round-48 long-leaf reduction can now
be trimmed numerically.  On leaves of adapted length at least

```text
delta_long = 358863/(2883949304*10^90)
```

the retained incidence-safe subfamily has mass strictly above `1999/32000`,
at most 153 pieces per source leaf, Euclidean piece length above
`delta_long/43146`, source incidence rank at most 319 and target rank at most
165.  The target estimate includes the required low-discriminant/exited-region
case split.

The exact remaining source-fraction requirements are

```text
W-diagonal route: 7372800000/10389446755961  (1/1409 safe)
direct C24 route: 2688/893303125              (1/332330 safe)
```

The direct target is easier by the exact factor
`660000000000/2798558021`.  These are conditional thresholds, not actual
crossing weights.  The arbitrary-`R_n` registry is not yet grouped measurably
into the required whole proper families.

## Gate 4: sharp post-cut common-law interface

The exact shortest closed half-contraction block improves from the old safe
1005 to **696**.  Hence, per orientation,

```text
C_sigma(H,k) = H + 221328 + 696*k,
eta = 1/4176.
```

On an abstract single common parent law satisfying
`K_par(y,X)=p(y)`, `Z(G_y)<=C_p*p(y)` and
`Z(H_sigma)<=P*p(y)`, the survivor-supported weight

```text
W_r=max(1,1_Sfw*exp(C_fw/(4176*r)),1_Srev*exp(C_rev/(4176*r)))
```

has the stated common-law `L^r` estimate.  The `r=6` and `r=12` instances
give conditional Hölder interfaces; if the missing physical whole-family
join is supplied, the D1 recovery charge inherits a block exponent `1/12`.
Zero survivors are never normalized.

This is not yet a physical `C_fw/C_rev/q` theorem.  Exact countermodels show
that finite Borel parent mass need not give finite aggregate `Z`, and that an
ambient forward/reverse survivor intersection need not be proper.  The
missing physical fields are the Borel family ID/disintegration, finite
`J=int p/ell`, a grouping-defect tail, a proper same-ID intersection and
strong cemetery domination.

## Latest target-sensitive technology

Climenhaga--Day `arXiv:2604.25881v1`, Proposition 3.19, gives a qualitative
fixed-billiard sufficient-rectangles theorem.  Baladi--Demers
`arXiv:1807.02330v4` separately constructs a positive Cantor rectangle inside
an open target.  The current proof does not establish that the separately
chosen C24 rectangle belongs to the same Cantor cover used by Proposition
3.19.  Even after that bridge, the published argument exposes no numerical
iterate, source-mass fraction, constants uniform on `|s|<=1/400`, or common
forward/reverse clock.  Therefore numerical `H_cover` and actual `beta`
remain absent; the strict lower bound remains zero.

## Gate 5: typed-measure and F17 frontier

For a fixed regular finite record, collision-SRB, occurrence coarea, core
trace and seven-face measures can be stored in one typed direct-sum kernel.
This does not identify their physical measures: D1 is two-dimensional while
the moving face/coarea currents are one-dimensional.  The exact tube model
proves that no finite D1 density coefficient dominates a nonzero face
current, so Round 48's formal subunit F10 ledger remains arithmetic only.

Two exact countermodels close off the current scalar shortcuts:

- `b_n=1/[n(n+1)]`, constant `B=14`, has finite one-step rank moment but
  divergent return-depth face mass;
- with `D_min=150*2^14` and `r=111718729/111718750`,
  `D_min*r=5491198967808/2234375>1`, so the crude F17 suffix product diverges
  even though all polynomial D1 moments are finite.

The measure-correct replacement is a boundary functional
`Z_B=int 2^B/|W| d lambda`.  Its conditional recordwise coefficients are

```text
fixed Z:  276024578258731962000000/95909140889781247
Z_B:      1030000/1999
```

but the owner-aware same-ID trace kernel, insertion-time aggregate `Z_B`
resolvent and face-tower depth moment are still missing.  Gate 5 therefore
stays at **10/18**, with **0** complete 18-field blocks.

## Strict status

```text
Gate 1: NOT CERTIFIED
Gate 2: NOT CERTIFIED
Gate 3: NOT CERTIFIED
Gate 4: NOT CERTIFIED
Gate 5: NOT CERTIFIED (10/18; complete blocks 0)
CM2:    NO-GO
```

## Validation

- syntax: `6/6`;
- duplicate-key-rejecting strict JSON: `3/3`;
- integrity/replay and independent exact arithmetic: PASS;
- substantive hostile mutations: `50/50 + 108/108 + 78/78 = 236/236`;
- cert/verifier default fail-close: `6/6`, each exactly exit 2;
- deterministic manifest regeneration: `3/3`;
- leaf and recursive SHA ledgers: PASS.

The hostile counts are verifier mutation tests, not independent mathematical
proofs.  The main numerical statements were also recomputed independently.

## Next route

1. Materialize the physical Borel whole-family registry: exact family IDs,
   disintegration, finite `J`, grouping-defect tail and the common-law `W_r`
   join.
2. Insert a closure-contained direct-C24 rectangle into the exact sufficient
   cover, then interval-enumerate the finite rectangle rows uniformly over
   `|s|<=1/400` to obtain numerical `H_cover` and actual crossing weight.
3. Combine the two directions on one proper same-ID law to close
   `C_fw/C_rev/q`, followed by strong cemetery.
4. Build the owner-aware trace-`Z_B` kernel, aggregate `Z_B` resolvent and a
   return-depth face-tower moment on the same arbitrary-`R_n` IDs.
5. Prove an anisotropic/dynamic F17 test bound, then close strong F13,
   F14/F15/F18 and finally Gate-3 `MT_DQ`.

Leaf reports:

- `cm2-gate34-round49-incidence-safe-long-leaf-atlas-assault-2026-07-19.md`
- `cm2-gate34-round49-sharp-common-law-recovery-assault-2026-07-19.md`
- `cm2-gate5-round49-typed-measure-f10-f17-frontier-assault-2026-07-19.md`
