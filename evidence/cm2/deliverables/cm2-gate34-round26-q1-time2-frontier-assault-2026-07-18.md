# CM2 Gate-3/4 round-26 Q1-to-time-two strict frontier

Date: 2026-07-18 (Asia/Shanghai)  
Policy: append-only, fail-closed, 384-bit Arb whole-box admission  
Strict verdict: **a positive physical Q2-inner subledger is certified; a
complete R2/Q2 partition, arbitrary-n Rn/Qn, weighted tail and induced strong
operator are not certified.**

## Source carrier and clock

The source is exactly the 2,868 frozen
`SURVIVE_THROUGH_1_INNER` leaves of
`cm2-gate34-full-core-return-adaptive-frontier-manifest-2026-07-18.json`.
Each parent has positive `(t,p)` area at fixed parameter, a positive parameter
interval, and a strict time-one image outside all 24 cores.  The clock is

```text
source C24 state = time 0,
first collision / inherited Q1 test = time 1,
fresh next collision / new core test = time 2.
```

No occurrence germ, midpoint trajectory, or post-core sample is joined into
this carrier.

## Fresh physical propagation

For every source descendant, the producer independently performs the
following operations with 384-bit Arb balls.

1. Rebuild the time-zero point and velocity from the original source chart.
2. Recompute the inherited first target contact, target normal, tangential
   coordinate and physical reflected velocity.
3. Prove one strict dominant-normal chart at time one.
4. Translate the complete retained candidate table for that chart to the
   actual target lift.
5. Compare all possible future roots and admit a second owner only when one
   root is strictly earlier than every retained competitor and is below the
   certified finite-horizon bound.
6. Compute the second target normal and tangential collision coordinate.
7. Test the whole image against all compatible members of the 24-core union.

An unresolved discriminant, root sign, root ordering, chart seam or core face
forces the entire source box into `UNRESOLVED_TIME2_OUTER`.

## Adaptive cover

Unresolved boxes are split in their original `(t,p,s)` coordinates by the
same normalized largest-side rule as the one-step certificate.  Refinement
stops at total binary depth 16.  The final prefix-free family has

```text
frozen Q1 parents:                              2,868
time-two terminal leaves:                     416,994
RETURN_AT_2_INNER:                                  0
SURVIVE_THROUGH_2_INNER:                      114,006
UNRESOLVED_TIME2_OUTER:                       302,988
```

Every admitted Q2 leaf inherits strict exclusion at time one and has a fresh
unique second-collision owner plus strict exclusion from every core at time
two.  Therefore it is a genuine Q2-inner cylinder on the physical collision
section.

The zero R2-inner count is a finite-depth admission result only.  It is not a
claim that the physical exact-time-two return set is empty.

## Exact mass ledger

The parameter-averaged unnormalized base masses are

```text
Q1-inner parent mass:                 123841/80000000
R2-inner admitted mass:                              0
Q2-inner admitted mass:             5257799/5120000000
time-two unresolved outer mass:          106721/204800000
```

They satisfy the exact identity

```text
0 + 5257799/5120000000 + 106721/204800000
  = 123841/80000000.
```

Equivalently, the certified Q2-inner fraction of this frozen Q1-inner carrier
is `5257799/7925824`.  This fraction is not asserted to be uniform under
future survivor conditioning and is not iterated into a tail estimate.

## Typed nonpromotion

The leaf certifies only:

- a prefix-free depth-16 outer/inner cover of the frozen Q1-inner carrier;
- 114,006 strict physical Q2-inner cylinders;
- exact base-mass conservation;
- fresh complete next-owner comparison on every admitted Q2 leaf.

It does not certify:

- exhaustion of the time-two unresolved outer cover;
- a complete R2/Q2 split modulo null boundaries;
- any Rn/Qn branch for `n>2`;
- numerical branchwise unstable Jacobian/distortion or strong `q_n`;
- a survivor-conditioned exponential contraction;
- a q-weighted excursion/cemetery tail;
- an induced strong Lasota--Yorke coefficient;
- Gate 3, Gate 4, Gate 5 or CM2.

## Verification contract

The verifier freezes the certificate and all six dependencies by SHA-256,
rejects duplicate or non-finite JSON, checks the exact result digest and
typed verdict, and independently reruns the full 384-bit producer.  Its
hostile suite mutates 64 semantic scalar fields plus structural and parser
cases.  Normal live execution exits `2` because the complete return partition
and strong gates remain open.

Final strict status:

```text
Q1-to-time-two adaptive frontier:             CERTIFIED
strict Q2-inner atoms:                         114006
strict R2-inner atoms admitted at depth 16:         0
complete R2/Q2 partition:               NOT CERTIFIED
arbitrary-n Rn/Qn:                      NOT CERTIFIED
CM2:                                  NO-GO FOR CLAIM
```
