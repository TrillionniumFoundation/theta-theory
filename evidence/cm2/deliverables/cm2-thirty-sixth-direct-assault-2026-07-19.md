# CM2 thirty-sixth direct assault

Date: 2026-07-19  
Verdict: **all-face parameterized F9 and the aggregate-Z route are certified; all five composite gates remain open**

## Round result

| gate | round-36 advance | exact remaining core gap | status |
|---:|---|---|---|
| 1 | no unsafe promotion | one physical representative with valid twisting/PPE hypotheses | NOT CERTIFIED |
| 2 | no unsafe promotion | invariant stable plaques, physical holonomy, quotient branches and PPE | NOT CERTIFIED |
| 3 | compact regular face germs now have pointwise F10 existence/search | arbitrary-depth dynamic DQ, `MT_DQ` and convergent physical current ledger | NOT CERTIFIED |
| 4 | exact aggregate `Z` recurrence and weighted resolvent; cellwise `D` route safely deferred | physical weighted face forcing `J_n`, complete `C_fw,C_rev,q` and cemetery | NOT CERTIFIED |
| 5 | all five face grammars have finite-rank-path F9; compact-germ F10 frontier | complete global F10, F12/F13, F14--F18 and induced coefficient | NOT CERTIFIED (`7/18`) |

Strict unconditional progress remains `0/5`; CM2 remains
`NO-GO_FOR_CLAIM`.

## 1. Aggregate recovery route selected

On one unnormalised same-ID standard-family representation of each whole
return/survivor level,

```text
Z_(n+1) <= a Z_n + b m_n + J_n,
a=360134800/360493663<1,
b=2*10^90.
```

The exact resolvent is `360493663/358863`.  The explicit weight

```text
w=720628463/720269600>1,
w*a=720628463/720987326<1
```

turns the recurrence into a weighted `l1` estimate whenever the physical
mass and new-face forcing are weighted summable.  This route avoids both
inverse component mass and the cellwise retained-depth factor `2^D`.

An exact correlated-rank countermodel proves that Round 35's one-time rank
tail and additive D1 `L^(6/5)` moment do not logically control arbitrary-time
image-recut products.  The aggregate route is therefore selected as primary;
the cellwise `D` route is deferred, not disproved physically.

## 2. All-face F9 completed parameterwise

All seven one-step collision-boundary kinds now have typed base geometry.
Circle tangencies use the global disk gap `36337/800000`; selected moving
occurrences use flight `>1/10`; integer-corner rays use a uniform length
`>1/2`.  Together with affine coordinate faces and the recurrence

```text
H_j=M(B_j)D_(j-1)^2+L(B_j)H_(j-1),
F9 < (3/2)E_j(K0D_j^2+G0H_j),
```

this supplies a finite F9 value on every finite regular path in all five
physical face grammars.  Gate-5 maturity advances to `7/18`; no global
rank/depth-uniform or collision-SRB `L^p` sum is inferred.

## 3. Compact-germ F10 frontier

Every closure-contained compact rational dyadic subarc of one regular
physical face has a positive analytic parameter radius.  On that germ the
signed coarea density and its first tangential/parameter derivatives are
bounded.  A canonical dyadic-halving plus outward interval-subdivision search
therefore terminates with a finite integer `N_F10(g)`.

This covers regular germs of all five face grammars and preserves the common
restriction, face-instance, parent-W and connected-rank IDs.  It is not a
complete global field: no arbitrary-`R_n` face rows or `N_F10` values are
materialized, no common radius exists, and no weighted sum is proved.

## 4. Exact first remaining equation

The aggregate forcing is

```text
J_n=J_core,n+J_owner,n+J_occurrence,n+J_cemetery,n.
```

The current stack supplies common fw/rev carriers, F8, all-face F9, seed-level
moving F10 and compact-germ F10 existence.  The next actual closure requires
an enumerated or directly integrated all-pullback F10 majorant on the same
physical IDs, followed by return-wide F12, moving-current F13 and a weighted
bound for `J_n`.

Only after that bound can the exact aggregate recurrence yield numerical
`C_fw,C_rev`, final `q`, the strong cemetery, F14--F18 and a new induced
common-space coefficient.

## 5. Independent long poles

Gate 1 still lacks one physical representative satisfying both the Green or
cancellation hypothesis and the twisting/PPE computation.  Gate 2 remains
the deepest structural pole: the positive-area R1 cone-product anchor does
not supply invariant stable plaques, physical holonomy, stable quotient
branches, reverse weights or PPE.  Gates 3--5 cannot substitute for either
structural gate.

## Technology boundary

Round 36 uses no external theorem to bypass a missing physical interface.
The previously audited current literature does not supply this model's
same-ID weighted all-face forcing, strong cemetery, or arbitrary-`R_n`
operator block.  The new advances are derived from the frozen exact analytic
and standard-family stack.

## Validation

All three new suites pass Python syntax, dependency hashes, strict JSON,
independent replay and live fail-close.  They reject

```text
16/16 + 15/15 + 20/20 = 51/51
```

hostile mutations.  Leaf SHA sidecars freeze every report, certificate,
verifier and manifest.

## Next shortest line

1. Materialize or directly dominate the all-pullback compact-germ F10 sum on
   the same arbitrary-`R_n` face IDs; quantify endpoint/cemetery degeneration.
2. Join return-wide F12 and moving-current F13, then prove a weighted physical
   `J_n` bound in the aggregate recurrence.
3. Assemble numerical `C_fw,C_rev,q`, close strong cemetery and materialize
   F14--F18 plus the induced coefficient.
4. Use the completed regular/cemetery ledger to close Gate-3 dynamic DQ and
   `MT_DQ`.
5. Continue the independent Gate-1 representative and Gate-2 stable-quotient
   constructions without transferring hypotheses across carrier types.

Strict unconditional CM2 remains `NO-GO_FOR_CLAIM`, with zero of five
composite gates fully closed.
