# CM2 Gate 4 Round 51 — two-proper-view common law and short-endpoint frontier

Date: 2026-07-20  
Strict verdict: **the Round-50 object-type break is repaired by a physical
two-proper-view measure-transport lemma.  The postproperization fixed-`H`
`W_r` moment now lives on one once-charged proper reference law.  The variable
preproperization clock still has no physical exponential moment, so complete
`C_fw/C_rev/q`, a proper common return, strong cemetery, Gate 4, and CM2 remain
not certified.**

## 1. The repair does not make raw `K_par` proper

Round 50 correctly rejected the following invalid inference:

```text
two different proper pushforwards of K_par
    + pull their predicates back to K_par
    => raw K_par is proper.
```

Properness is geometric and is not changed by merely rewriting an integral.
The correct lemma keeps the two proper geometries separate.

For one Round-50 whole-family ID `y`, let

```text
m_raw(dy,dx)=lambda(dy) K_par(y,dx),
K_par(y,X)=p(y).
```

The half-open owners and the two modulo-null partition identities ensure that
this is one physical charge, not a forward plus a reverse copy.

On the regular component from Round 35, write `H=T_s^n`.  Its regular branch
has a real-analytic inverse.  The two starting views of the same raw point
are

```text
V_fw(x) = the forward starting carrier,
V_rev(x) = I(H(x)) in the reverse starting carrier I(B).
```

Here `I` is billiard time reversal.  If `Dbar(y)` is the common Round-50
minimum-length defect, define

```text
P_fw(y,x)=T_s^(696 Dbar(y))(V_fw(y,x)),
P_rev(y,x)=T_s^(696 Dbar(y))(V_rev(y,x)).
```

In each fibre only finitely many iterates occur.  Across all integer stopping
strata, the stopped-orbit singular preimages form a countable Borel set.  It
is `m_raw`-null by the collision-SRB null theorem together with Round 50's
exact outer disintegration, and belongs to the already frozen singular
cemetery.  No stronger every-fibre null assertion is needed.

Because `Dbar` is integer-valued Borel, on every stratum `{Dbar=d}` the maps
and inverse maps

```text
P_sigma^-1=V_sigma^-1 o T_s^(-696d)
```

are Borel.  Retaining `y` makes the total maps injective; Lusin--Souslin gives
Borel images and Borel inverses.  Define the two view kernels by actual
pushforward:

```text
K_fw^*  =(P_fw)_# K_par,
K_rev^* =(P_rev)_#K_par.
```

Thus each `P_sigma` is a fibrewise measure-space isomorphism modulo null,
both kernels have mass `p(y)`, and outer integration agrees with collision-SRB
invariance.  The natural and image recuts only label half-open components;
they do not change the point map, duplicate mass, or normalize a survivor.

Round 50's exact recurrence now supplies the geometric fact in the correct
place: `K_fw^*` and `K_rev^*` are separately whole proper standard-family
kernels.  No claim is made that raw `K_par` is proper.

## 2. One selected proper common law

Choose the forward proper view as the reference law:

```text
m^*(dy,dz)=lambda(dy) K_fw^*(y,dz).
```

The Borel measure-space isomorphism

```text
Theta_y=P_rev,y o P_fw,y^-1
```

satisfies

```text
(Theta_y)_#K_fw^*(y,.)=K_rev^*(y,.).
```

Transport only the reverse measurable data:

```text
S_rev^*(y,z)=S_rev(y,Theta_y(z)),
C_rev^*(y,z)=C_rev(y,Theta_y(z)).
```

Forward data remain on `K_fw^*`.  Consequently every reverse integral on its
own proper geometry equals the transported integral on `m^*`.  The outer
restriction ID `y` is unchanged, zero survivors remain zero, and no separate
normalization occurs.

This proves the desired physical two-proper-view pullback lemma.  Equivalently,
one may pull both terms back to `m_raw`; the two descriptions give the same
once-charge integral.

## 3. Physical postproperization `W_r` join

Fix one finite return depth, one finite terminal schedule, and one finite
terminal time `H`.  In each proper view Round 49 gives

```text
C_sigma,post=H+221328+696 k_sigma,
A_H=(6/5)^(318+ceil(H/696)),

integral 1_Ssigma exp(C_sigma,post/4176)
    d(lambda K_sigma^*)
  < A_H integral p dlambda.
```

On the selected forward law set, for any integer `r>=1`,

```text
W_r^*=max(
  1,
  1_Sfw^* exp(C_fw^*/(4176r)),
  1_Srev^* exp(C_rev^*/(4176r))
).
```

Pointwise,

```text
(W_r^*)^r
 <=1
   +1_Sfw^* exp(C_fw^*/4176)
   +1_Srev^* exp(C_rev^*/4176).
```

Transporting the second orientation back to its own proper view yields

```text
integral (W_r^*)^r dm^*
 <(1+2A_H) integral p dlambda.
```

This is the literal once-charge fixed-`H` moment that Round 50 could not type.
It uses no properness of raw `K_par`, no equal forward/reverse survivor, no
common shell, and no properness of their intersection.

The certified clock here starts **after** the recordwise properization.  This
scope is essential.

## 4. Exact full-clock condition

The complete orientation clock would be

```text
C_sigma,total=696 Dbar+C_sigma,post.
```

Since `696/4176=1/6`, the same maximum argument gives only the conditional
bound

```text
I_D = integral p(y) exp(Dbar(y)/6) dlambda(y),

integral (Wtilde_r^*)^r dm^*
 < integral p dlambda+2 A_H I_D.
```

A rational sufficient condition is

```text
integral p(y)(6/5)^Dbar(y) dlambda(y)<infinity,
```

because the Round-49 rational comparison gives `exp(1/6)<6/5`.  Neither
moment is currently available on the physical grouped kernel.

## 5. Exact defect formula and sharper conditional tail bridge

Exact integer comparison gives

```text
2^310<C_p<2^311.
```

For the Round-50 safe defect this implies the complete integer formula

```text
Dbar(M)=0,       0<=M<=310,
Dbar(M)=M-309,   M>=311.
```

In particular `Dbar` never takes the value `1`.

Because `M` is finite Borel on a finite law, the strongest unconditional
conclusion is only

```text
mass{M>m}->0,
mass{Dbar>d}->0.
```

This follows from continuity from above and gives no rate or positive
exponential moment.

If a future same-kernel estimate supplies

```text
mass{M>m}<=C_len 2^-m,       m>=0,
```

then exact layer cake, including the skipped value `Dbar=1`, improves the
Round-50 coarse bound to

```text
integral (6/5)^Dbar p
 <=mass_total+(4/5) C_len 2^-310.
```

The coefficient is exact:

```text
[(6/5)^2-1]2^-310
+2^-309[(1/5) sum_(d>=2)(3/5)^d]
=(4/5)2^-310.
```

Therefore the same right side bounds `integral exp(Dbar/6)p` (with `<=`;
strictness is unnecessary).

## 6. Why present fields do not supply the tail

The Round-50 uniform-area countermodel can be sharpened directly at the tail
level.  On disjoint `b` bands of mass `2^-n`, split a unit fibre into
`2^(n^2)` source intervals of length `2^(-n^2)`, take the reverse view and
partition identical to the forward view, and keep fixed incidence rank
`B=14`.

For `n>=19` every such interval is already shorter than `10^-90`, so each
group contains one clipped endpoint cell.  The example therefore does not
hide infinitely many recuts inside one group.  It retains finite total mass,
a standard-Borel countable grouping, positive recordwise lengths, finite
recordwise `J`, and a constant D1 density.

On band `n`,

```text
M=n^2,
Dbar=n^2-309.
```

At `m=n^2-1`,

```text
mass{M>m}>=2^-n,
2^-n / 2^-m = 2^(n^2-n-1)->infinity.
```

Thus the fields currently certified do not logically imply any finite
`C_len`.  Moreover neither

```text
2^-n(6/5)^Dbar
```

nor the weaker required term `2^-n exp(Dbar/6)` tends to zero; for the latter
use `exp(1/6)>1+1/6=7/6`.

This is a logical nonimplication model only.  It does not say that the actual
billiard realizes such endpoint multiplicity, nor does it disprove an actual
physical short-length tail.  The missing physical input must control endpoint
multiplicity/transversality on the same arbitrary-`R_n` grouped kernel.

## 7. Geometric nonpromotion

The same raw point can now be tested against both survivor predicates, so the
joint predicate is Borel.  But the lemma transports integrals, not boundary
geometry.  A transported reverse subset of the forward proper family, and
its intersection with the forward survivor, need not be proper.  Hence this
round does not certify:

- a proper common forward/reverse return;
- numerical `H_cover` or actual positive `beta`;
- complete numerical `C_fw/C_rev`;
- collision time `q`;
- strong trace/current cemetery;
- Gate 4 or CM2.

The Round-50 weak mass cemetery remains valid.

## 8. Mechanical verification

- Python syntax: `2/2` PASS;
- strict duplicate-key and non-finite-constant rejection: PASS;
- exact top-level JSON key set: PASS;
- deterministic certificate replay and dependency SHA checks: PASS;
- independent exact arithmetic, including all `M=0,...,700`: PASS;
- hostile mutations: `104/104` rejected;
- certificate and verifier default modes: both fail closed with exit `2`;
- manifest replay: byte-deterministic;
- leaf SHA manifest: PASS.

Artifacts:

- `cm2_gate34_round51_two_proper_view_common_law_cert.py`
- `cm2_gate34_round51_two_proper_view_common_law_verifier.py`
- `cm2-gate34-round51-two-proper-view-common-law-manifest-2026-07-20.json`
- `cm2-gate34-round51-two-proper-view-common-law-manifest-2026-07-20.sha256`

## 9. Next shortest Gate-4 route

1. Prove endpoint multiplicity/transversality on the same grouped kernel,
   yielding `mass{M>m}<=C_len 2^-m` or directly the defect moment.
2. Insert that moment into the now-certified two-view full-clock formula.
3. Combine it with an actual numerical `H_cover/beta` to form complete
   `C_fw/C_rev`.
4. Separately prove a proper common return (measure transport alone cannot do
   this), then build `q` and the strong cemetery.
