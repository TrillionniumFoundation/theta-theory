# CM2 seventh direct assault: maximal rows and the global Borel current

Date: 2026-07-15  
Global verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; strict composite gates closed = `0/5`**

## Verdict

The seventh assault closes the main geometric remainder left by the sixth:
all rectangular and analytic pieces are now glued into 64 exact maximal
physical event rows on the chart-quotiented source-normal circle.  Every row
has one positive coarea law and one signed hit-minus-miss current.  Their
global sum is a finite Borel measure with an explicit rational TV envelope,
and all 32 `Jx` pairs give exact global signed scalar coarea matching.

This is a major Gate-3 advance, but it is not the full transfer-operator
difference quotient.  The smooth fixed-core derivative, dynamic-`C^1`
boundary tightness and CM2 norm bounds remain absent.  Gates 4–5 now use the
true maximal partition, but numeric forward/reverse costs and controlled
stopped recovery are still missing.

| gate | newly certified | exact remaining obstruction | status |
|---|---|---|---|
| 1. transport/twist | previous exact natural-holonomy obstruction retained | genuinely different transport theorem | **OPEN** |
| 2. physical PPE | previous local branches and scale/mass audit retained | stable full-mass quotient, actual kernel/PPE and normalized amplitudes | **OPEN** |
| 3. global event/DQ | 64 maximal rows; 88 physical boundaries; global finite-Borel current; scalar coarea matching | full transfer DQ convergence, fixed-core derivative and dynamic-`C^1` tightness | **OPEN** |
| 4. same-occurrence `q` | one common `m` and one symbolic `q` expression on every maximal occurrence | numeric `C_fw,C_rev` and controlled stopped recovery | **OPEN** |
| 5. fixed/full transfer | all-maximal-row finite-Borel Kac ledger and exact four-term algebra | physical CM2 typing, prefix/suffix constants and three norm lifts | **OPEN** |

## 1. Gate 1 remains unchanged

The natural canonical stable holonomy still has the exact nonzero asymptotic
coefficient already certified in the fifth assault.  Neither the finite
shadow construction nor current 2026 linear-response results manufacture the
missing convergent canonical transport.  Gate 1 remains open.

## 2. Gate 2 remains unchanged

The stable endpoint/tube scale ratio remains greater than `8.29e65`, and more
than `90.3%` of the common collision-SRB rectangle remains outside the raw
two-branch registry.  The new 2026 small-hole result uses controlled long
standard families adapted to the image of a shrinking hole; it does not
supply this instance's interval-indexed stable saturation, full-mass quotient,
actual reverse weights or stopped-parent amplitude ledger.  Gate 2 remains
open.

## 3. Gate 3 geometric layer is now global

### 3.1 Exact maximal partition

On the 128 active global signed sheets, the exhaustive transition universe is

```text
256 source-grazing candidates,
24 forward u_y=0 candidates,
16 active first-visibility curves,
32 active miss-owner curves,
328 candidates total.
```

All 320 same-sheet candidate pairs are disjoint on the full parameter window;
304 prove directly and 16 require one bisection.  The cyclic order is therefore
constant.  Strict open-band witnesses leave 88 state-changing boundaries:

```text
32 source grazing,
16 first visibility,
32 miss owner,
8 parameter polarity.
```

Their endpoint incidence is `128=2*64`.  Adjacent equal states merge into 64
maximal connected rows with 64 unique chart-free labels and 16 `Jx/Jy`
four-row orbits.

### 3.2 Global order-zero current

On every maximal row,

\[
dm_e=R_S\frac{c_p^2|u_y|}{\ell_T}\,d\theta,
\qquad
J_e(\Phi)=\sigma_e\int\{\Phi(z_T)-\Phi(y_M)\}\,dm_e,
\]

up to the common collision-flux normalization.  Exact global geometry gives

\[
\ell_T^2\ge\frac{36337}{3260000}>\frac1{100},
\qquad
\|J\|_{TV}\le\frac{16128}{5}Z_N^{-1}.
\]

Thus the global event current is a finite signed Borel measure and pairs with
every bounded Borel test.  Source-grazing and polarity endpoint densities
vanish; first/miss endpoints have zero one-dimensional base measure, so no
extra endpoint atoms appear.

The 64 rows form 32 exact `Jx` pairs.  Their positive coefficient laws are
push-forward equal and their polarities are opposite, giving global signed
scalar coarea mass `0`.

### 3.3 Why Gate 3 is not yet marked closed

The new current is the complete singular moving-face term.  A full Gate-3 DQ
still requires a theorem that the actual transfer difference quotient
converges to the sum of:

1. the smooth fixed-core derivative; and
2. this finite event current,

with uniform control on the intended dynamic-`C^1`/CM2 test class.  The Borel
TV estimate alone does not supply that operator-level convergence or norm.

## 4. Gate 4 now uses the true maximal occurrences

The structural charge ledger is no longer tied to rectangular subrows.  It
now contains exactly:

```text
64 maximal physical occurrences,
64 common positive laws m_e,
64 q_e=max(C_fw(e),C_rev(e),2)m_e expressions,
128 singular Kac coordinates,
one shared (+1,-1) mark per occurrence.
```

The forward and reverse views are nonadditive coordinates of the same
incidence.  The Borel change-of-variables identity is global, but no numerical
standard-family/flux costs or controlled stopped-parent recovery envelope has
been proved.  The fat-Cantor obstruction still excludes arbitrary-Borel
recovery.

## 5. Gate 5 finite-Borel layer is global

The exact endpoint adjoint, Kac tower, prefix/suffix pairing and four-term
centered algebra now bind to all 64 maximal rows.  This certifies the global
finite-Borel Kac layer, not its CM2 realization.  Still missing are:

- the homogeneity cut-growth sum;
- dynamic test-pullback constants;
- physical prefix/suffix inverse-Jacobian and distortion sums;
- standard-family, flux-face and dynamic-test CM2 norm lifts.

## 6. Latest-theory audit

The closest new result found is Canestrari's 2026 *Linear response for Sinai
billiards with small holes* (`arXiv:2604.19671v2`).  It proves differentiability
at zero hole size by conditional standard-family relaxation.  Its controlled
hole-image geometry is useful guidance for the next stopped-parent attack,
but it does not instantiate CM2's moving-scatterer event rows, bidirectional
same-occurrence costs or full-mass physical quotient.  The 2026
contracting-fibre response theorem and the 2025 cusp theorem likewise do not
match the present billiard hypotheses.  No honest theorem bypass was found.

## 7. Shortest remaining route

1. Prove the full transfer DQ by combining the smooth fixed-core derivative
   with the now-finite 64-row event current and a dynamic-`C^1` tightness bound.
2. Freeze a controlled interval/cylinder stopped-parent algebra; arbitrary
   Borel restrictions are known to be impossible.
3. Compute the homogeneity cut ledger and rowwise `C_fw,C_rev`, then freeze
   the 64 symbolic `q_e` expressions numerically.
4. Prove bidirectional stopped recovery and export the four Kac terms through
   the three CM2 norm lifts.
5. Independently close Gate 2's stable full-mass quotient and Gate 1's
   alternative transport theorem.

## 8. Evidence

- `cm2-gate3-maximal-rows-global-current-assault-2026-07-15.md`;
- `cm2_gate3_maximal_global_row_registry_cert.py` and manifest;
- `cm2_gate3_global_borel_current_assembly_cert.py` and manifest;
- `cm2_gate45_maximal_row_single_charge_ledger_cert.py` and manifest.

All three verifiers are fail-closed: integrity and mutation self-tests exit
`0`, while live default completion exits `2` until the full DQ/recovery/CM2
layers are proved.  Frozen v51/v52 inputs are not modified.
