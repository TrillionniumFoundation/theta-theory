# CM2 Round 60 full direct assault

Date: 2026-07-20  
Strict verdict: **0/5 complete composite gates; Gate 5 maturity 10/18 with
zero complete 18-field blocks; CM2 NO-GO_FOR_CLAIM**

## Round result

Round 60 closes no composite gate.  It does, however, install three useful
physical or typed sublayers and sharply separates them from the remaining
claims:

1. Gate 4 gains an actual same-measure Radon--Nikodym marker for the common
   landing law and an exact good/bad original-time graph split, while two
   smooth product separators prove that qualitative product geometry cannot
   replace the strict landing boundary inequality.
2. Gate 5 gains the exact Round-50-to-Round-54 owner-token projection, the
   strongest frozen raw-collar domination of the optimal clock, all seven
   suffix predicate definitions, and a conditional positive hybrid ledger
   which treats `A_col` and its complement without reading undefined marks.
3. Gates 1/2/3 gain exact promotion boundaries: a numerical selected-loop
   gauge budget, a depth-97 finite-prefix separator, and a sharp proof that
   the installed `exp(Dbar/6)` clock moment cannot pay the current raw
   `8*162^n` branch-enumeration route.

The three append-only main leaves are Gate 4, Gate 5 and Gate 1/2/3.  A fourth
append-only leaf independently audits all mathematics, measure/operator
typing, frozen dependencies and executables.  The red team required one
pre-freeze Gate-5 correction: its complement hybrid iff is conditional on a
supplied Borel `R` and measurable nonnegative `C_bad`, not an already
constructed physical policy.  All Gate-5 artifacts and hashes were regenerated
before this aggregate was assembled.  No older artifact was edited.

## Gate 4: an actual physical landing RN marker

Let

```text
mu_C=mu_s|C_s
```

be the physical induced collision law, and let `kappa_B` be the frozen
unshifted common landing marginal.  The common source is a positive
once-charged restriction of the original return law, and the induced
first-return map preserves `mu_C`.  Therefore, on the same physical measure,

```text
0<=kappa_B<=mu_C.
```

The actual Borel marker

```text
g_B=d kappa_B/d mu_C,
0<=g_B<=1                       mu_C-a.e.
```

is now certified.  This is a genuine physical upgrade: the common landing is
identified as a marked sublaw of the same collision measure, not merely as a
finite-`Z` abstract family.

If a future theorem supplies an actual physical unstable Rokhlin
disintegration

```text
mu_C=integral mu_u deta(u),
```

then, without singleton normalization,

```text
tilde_kappa_u=g_B mu_u,
m(u)=integral g_B dmu_u,
eta_B=m eta,
kappa_u=(g_B/m(u))mu_u on {m>0}
```

give the exact unnormalised and Bayes conditional formulas.  All identities
remain `eta`-almost-everywhere and conditional-law-almost-sure.  They do not
construct the unstable quotient, a stable projection, marker-component
geometry or two-sided density/log-distortion control.

Consequently field 4 receives a real partial interface, while the seven-field
landing join remains exactly `1/7`: only the tagged branch inverse, field 6,
is complete.  Gate 2's official immutable score remains `0/17`.

## Gate 4: qualitative fields 1--4 cannot pay field 5

Two exact smooth product models isolate the missing quantitative debt.  They
are logical nonimplication models, not claimed billiard realizations.

For the short-plaque model take one rectangle with

```text
C_p=4*10^90*360493663/358863,
L=2/(3C_p).
```

Use full horizontal unstable plaques, vertical stable plaques, identity
return, holonomy Jacobians one and constant density.  Thus the qualitative
forms of fields 1--4 are perfect:

```text
F=1, theta=1, R=1, log distortion=0, full span.
```

Nevertheless

```text
J/h=1/L=3C_p/2>C_p,
D_land=2.
```

For the independent fragmentation model set

```text
H=999/1000,
N=floor(C_p)+1
```

and retain `N` equal positive intervals of total span `H` in a unit plaque.
With the same perfect product/holonomy/density fields,

```text
J/h=N/H in (C_p,2C_p),
D_land=2.
```

Thus short span and fragmentation are independent debts.  Even perfect
qualitative fields 1--4 do not imply the required almost-everywhere inequality

```text
F(u)R(u)<C_p theta(u)L(u).
```

## Gate 4: exact good/bad split, not whole-law properness

On the actual landing registry define

```text
G={h>0 and z_land<C_p},
B={h>0 and z_land>=C_p}.
```

These are Borel and give the exact unnormalised graph split

```text
Gamma_cap=Gamma_G+Gamma_B.
```

The good part has a proper physical same-ID landing family on the original
first-hit graph and retains

```text
T_s^j(source) notin C_s, 1<=j<n,
T_s^n(source) in C_s.
```

It may, however, have zero mass, and physical source properness is not
inferred.  The bad part may have positive ordinary collision-SRB mass and may
be entirely regular.  Finite

```text
integral_B h 2^D_land
```

does not make it null and does not allow it to be relabelled as the
collision-null singular cemetery.  A legal positive bad-defect hybrid still
needs five endpoint-preserving strong/positive interfaces.  Whole-law
properness, later or repeated clocks, physical `q in L^(6/5)` and strong
cemetery remain uncertified.

## Gate 5: exact owner crosswalk, unresolved trace strata

The Round-50 token is

```text
t50=(restriction-id,time-j,physical-event-signature,
     primitive-key,connected-rank-0,side-label),
```

and the Round-54 token is exactly

```text
t54=(t50,word-cell).
```

Forgetting only `word-cell` is an exact Borel projection; all first six
owner/event coordinates and the labelled root carrier are retained.  This
certifies the Round50→54 same-ID crosswalk.  It does not create a
Round25→50 root-ID crosswalk: Round 25 has seven root types and a transverse
isolated-root theorem, but explicitly no curve-by-curve numeric root rows or
Round-50 event/primitive tokens.

After physical corners and simultaneous events have been sent to cemetery,
the regular complement is exhausted by

```text
A_col^c=N_cut union N_acc,
```

where the owner root either coincides with another registered nonphysical
cut or lies in its accumulation closure.  The actual owner-trace mass of
these sets is unknown.  Collision-area nullity cannot be substituted because
the owner/coarea laws are singular.  In particular the actual masses of
source, intermediate, terminal, singular/owner-change and moving-occurrence
complement strata are all `NOT_CERTIFIED`.

Full coverage is therefore not claimed and is not the only possible route.
The pure collar policy assigns infinite cost on `A_col^c`, hence requires
coverage.  A conditional hybrid may instead use

```text
C_hyb
=1_Acol[
    1_{R>=r_K} C_rec w_Z^r_K
   +1_{R<r_K} 2^(K+1)
  ]
 +1_{A_col^c}C_bad.
```

Its exact positive iff is certified only under two supplied hypotheses:

1. the missing code maps have made `R` Borel on `A_col`;
2. `C_bad` is a same-owner-law measurable nonnegative charge on `A_col^c`.

Under those conditions, finiteness is equivalent to finiteness of the long
collar, short/raw-debt and complement-positive integrals separately.  The
registry branch is tested before reading `K/R`; those marks are never used on
`A_col^c`.  Neither the hypotheses nor physical hybrid finiteness is
currently certified.

## Gate 5: optimal clock and suffix typing

Round 59's exact active Abel criterion remains

```text
Mbar<infinity
iff sum_(j in S) w_Z^r_j Fbar_j<infinity,
S={j:r_(j+1)=r_j+1}.
```

No pinned manifest supplies a numerical physical `Fbar_j` envelope or an
explicit usable Orlicz bound.  Round 60 does add the strongest frozen
pointwise comparison:

```text
1<w_Z<2,
r_K=ceil(beta(K+1))<=K+1,
w_Z^r_K<2^(K+1)=ell(K)^(-1).
```

Thus finite raw `Z_col` would pay the optimal clock, but finiteness of raw
`Z_col` is not certified in the frozen ledger.

All seven suffix predicate definitions are now explicit.  Their precise
Borel typing is:

```text
unconditional on the frozen registry:
  L_id, L_word, L_C24, L_horizon                 4/7

conditional on missing common code/evaluation maps:
  L_input, L_operator, L_output                 3/7
```

In particular equality on a countable determining algebra is Borel only
after both `K_word` and `O_s^9148` kernel evaluations are Borel on one common
code registry.  These maps are not frozen.  Hence the seven definitions are
certified, but all-seven Borel typing, their five missing universal values,
physical same-operator equality, `R>=r_K` and Borel `R` itself remain
uncertified; `R` is only a conditional schema.

## Gate 5: positive orientation is not a weighted Jordan anchor

At each fixed insertion time the owner-selected positive ledger certifies

```text
integral 2^B                       <23253221519103/880000,
forward orientation F10 charge    <395304765824751/220000,
reverse orientation F10 charge    <162772550633721/176000,
bidirectional orientation charge  <2395081816467609/880000.
```

The last three satisfy the exact sum identity.  They are real positive
orientation anchors.  They are not, however, the Round-54 Jordan marginals.
Those marginals arise from the Jordan decomposition of the signed physical
flux and currently have only the fixed-marked unweighted mass bound

```text
mu_plus(total)=mu_minus(total)<=8064/5.
```

No dependency pins `abs(lambda_j)` below the same owner-selected `m_occ` law
with the same charge, and `2^B` is explicitly only an envelope, not
`sigma_e`.  Therefore neither fixed-time nor global weighted Jordan
variation/common-mode is certified.  Even a future fixed-time join needs
all-insertion decay before it can pay a `w_Z^j` sum.  Complete positive F10
and strong cemetery stay open.

## Gate 1: exact selected-loop gauge budget, no all-plaque promotion

The selected QNL loop has

```text
E_compact<8568/10^52,
W_min>17344/10^32.
```

If a future immutable same-representative join proves

```text
E_combined<=K_gauge E_compact,
```

then all four selected twisting wedges survive whenever

```text
K_gauge<W_min/E_compact
       =216800000000000000000000/1071
       >2*10^20.
```

This is a certified conditional budget, not a physical value of `K_gauge`.
No frozen row identifies the compact-log and combined half-density gauges.

Two exact registry completions prevent promotion.  The selected compact tail
may retain its exact sum `100/81` while an unconstrained second plaque carries
a harmonic combined-gauge tail.  Independently, an unbounded gauge
amplification can turn the selected compact increments into a divergent
combined sequence.  Hence the all-actual-plaque combined-gauge registry,
same-representative class `H` and physical comparison remain uncertified.

## Gate 2: 96 physical prefixes are not an all-depth quotient

Round 59 certifies zero collision shadows on the actual strip for prefixes
`1,...,96`.  The exact logical completion

```text
g_j=1 for 1<=j<=96,
g_97=0
```

agrees with every frozen row and loses the required graph-transform chart at
the next depth.  It is a nonimplication model, not a statement that the
physical strip fails at depth 97.

An all-depth promotion still needs one immutable package containing actual
stable plaques, every-depth graph transforms, stable projection, two-sided
physical holonomy Jacobians, landing conditional density/fragmentation, the
actual first-return landing join, and strong restriction/assembly.  The
tagged branch inverse supplies only the landing inverse item.  The landing
join remains `1/7`, and the official Gate-2 registry remains `0/17`.

## Gate 3: fixed-depth formula survives, raw enumeration does not integrate

The chosen-target sign audit confirms that, for a distinct competing disk,

```text
a=u dot(C-q),
b=|C-q|^2-R^2>0,
Delta=a^2-b,
```

absence of a root in `(0,tau)` is exactly

```text
a<=0 OR Delta<0
OR [a-tau>=0 AND Delta<=(a-tau)^2].
```

The squaring is guarded by `a-tau>=0`; target choice, outgoing sign,
half-open owners, ties and grazing cemetery remain separate.  The padded
fixed-depth count also remains valid:

```text
161*5+25=830,
variables<=8n+5,
atoms<=830n+12,
degree<=4,
raw words=8*162^n.
```

It is an upper candidate count, not an actual branch lower bound.

Even grant the most optimistic missing type equality `N=Dbar`.  For

```text
P{N=n}=2^(-n-1)
```

the installed clock pays `E exp(N/6)<infinity`, while the expected raw word
upper bound is

```text
sum_n 2^(-n-1)*8*162^n
=4 sum_n 81^n
=infinity.
```

Thus the current enumeration route cannot be paid by the installed moment.
This does not assert divergence of the actual number of nonempty physical
branches.  The CAD degree recurrence still grows as

```text
D_r=2^(4*2^r-1).
```

A legal alternative needs the direct strong route: physical `R_s/Q_s`, a
moving-scatterer directional Piola/current formula, common-domain derivative
operator and `MT_DQ`, all joined to the first-hit/landing carriers.

## Latest-technology audit

The 2026 sources checked by the leaves and independent audit supply useful
local analogies only.  The small-hole Sinai-billiard standard-family theorem
starts from an already regular family and does not construct the pinned
common landing law, moving-scatterer Piola/`MT_DQ`, or a strong space
containing standard pairs.  Current CAD papers do not control growing-depth
inverse-component boundary complexity.  Current inter-sign transport work
does not convert signed cancellation into positive Jordan/common-mode
moments.  No external theorem is promoted into a certificate dependency.

## Independent audit and full validation

The independent audit locks all three main manifests and four-row ledgers,
resolves every leaf artifact row, and independently replays:

- the Gate-4 physical domination/RN chain, Bayes typing, both product
  separators and the good/bad/cemetery guards;
- the Gate-5 token projection, owner-trace frontier, clock comparison,
  `4/7+3/7` suffix typing, conditional hybrid hypotheses and positive-versus-
  Jordan distinction;
- the Gate-1 rational gauge budget and separators, Gate-2 depth-97 guard,
  and Gate-3 root sign, padded atom count, clock separator and exact CAD
  recurrence.

It reports `PASS_NO_CLAIMED_SCOPE_BLOCKER` after one pre-freeze type
correction.  The final four-leaf
validation matrix is:

```text
syntax:                         8/8 PASS
dependency/artifact pins:       32/32 PASS
integrity / replay / reemit:    4/4 / 4/4 / 4/4 PASS
hostile + strict-JSON guards:   596/596 rejected
SHA ledger rows:                16/16 PASS
default cert/verifier:          8/8 exit exactly 2
Round-60 stale/temp files:      0
```

## Strict status after Round 60

```text
Gate 1: NOT_CERTIFIED
Gate 2: NOT_CERTIFIED (0/17 physical fields)
Gate 3: NOT_CERTIFIED
Gate 4: NOT_CERTIFIED (landing join 1/7; field 4 partial)
Gate 5: NOT_CERTIFIED (10/18; complete blocks 0)
complete composite gates: 0/5
CM2: NO-GO_FOR_CLAIM
```

The shortest continuation routes are now:

1. construct the actual physical unstable landing disintegration, stable
   projection/Jacobian, marker fragmentation/span and density bounds; prove
   `FR<C_p theta L` and install strong assembly, or build the five-interface
   positive good/bad landing hybrid without deleting the bad graph;
2. on Gate 5, measure the complement strata in the actual owner-trace law,
   prove either coverage or a finite same-law `C_bad`, supply the active Abel
   tail/raw `Z_col` bound, and freeze the three missing Borel code maps plus
   all five universal suffix values;
3. pin the physical signed-flux Jordan law to the same owner-selected
   positive charge and obtain summable all-insertion decay for variation,
   common mode and cemetery;
4. extend Gate 1 to all actual plaques in the combined gauge, construct Gate
   2's all-depth stable package, and replace Gate 3's raw enumeration by a
   clock-integrable compression or the direct strong `R_s/Q_s`--Piola--
   `MT_DQ` route.

## Frozen Round-60 leaves

- `cm2-gate4-round60-physical-rn-good-bad-assembly-frontier-assault-2026-07-20.md`
- `cm2-gate5-round60-owner-trace-suffix-positive-anchor-frontier-assault-2026-07-20.md`
- `cm2-gate123-round60-combined-gauge-stable-strong-operator-frontier-assault-2026-07-20.md`
- `cm2-round60-independent-core-frontier-audit-2026-07-20.md`.

Their reports, manifests and four-artifact SHA ledgers, together with the
frozen Round-59 aggregate report and recursive ledger, are pinned by
`cm2-sixtieth-direct-assault-manifest-2026-07-20.sha256`.
