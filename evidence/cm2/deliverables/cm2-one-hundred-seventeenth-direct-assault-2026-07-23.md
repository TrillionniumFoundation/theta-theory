# CM2 one-hundred-seventeenth direct assault

Round117 installs an exact symbolic `q=2` countable homogeneity generator on
all 72 Round113 endpoint proof-box regions.

Eight HIT parents receive independent source/actual-third indices and four
tail/central-outer generator families.  Sixty-four BYPASS parents receive a
source index only; `b3` is correctly excluded as a non-collision coordinate,
while the actual G-winner is independently proved central `H0` on every full
parent region.  The central/outer sliver beyond `sin(128^-2)` is retained.

The independent 640-bit verifier rejoins 72/72 parents, recomputes all 64
BYPASS H0 facts, rechecks 112 generator-family templates, six directed
boundaries and eight natural trace rows, and rejects six semantic plus three
strict-JSON attacks.  Both cold replays are byte-identical.

This remains an operator-cell schema, not an actual standard-curve recut.
F1--F6 remain zero, Gate5 stays `10/18`, and CM2 stays `NO-GO_FOR_CLAIM`.
