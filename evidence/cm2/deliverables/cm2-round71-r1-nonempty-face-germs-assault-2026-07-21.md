# CM2 Round 71 — 32 witnessed nonempty R1 face germs

Date: 2026-07-21  
Scope: append-only continuation from Round 70  
Strict verdict: **Thirty-two previously symbolic terminal-core-preimage
families now have actual base-fibre sign-changing witnesses, local connected
face-component IDs, two traces and path-cell incidence edges.  This is a
positive subatlas, not an exhaustion of all 1,152 candidate families.  F10
integers, F14/F15/F17 and complete blocks remain open; CM2 stays
`NO-GO_FOR_CLAIM`.**

## 1. From 1,152 families to actual nonempty germs

The frozen C24 registry supplies 24 source cores, 96 stationary core faces and
1,152 regular time-one pullback candidate families.  Before this round, none
of those candidate families had a nonempty connected component ID.

At `s=0`, the actual Round-69 return graph has 16 reciprocal directed
source/destination core edges.  On each edge, interval-guided search finds two
destination-core faces with a sign-changing source segment: one terminal `t`
face and one terminal `p` face.  The resulting table has exactly

```text
16 positive base-fibre R1 edge cells,
32 distinct candidate family IDs,
32 distinct destination face IDs,
32 local connected rank-zero face germs,
64 one-sided traces.
```

Every witness segment lies in one frozen strict first-hit source core.  Its two
rational endpoints have opposite signs for the destination face level.  The
other destination coordinate remains strictly within the corresponding face:
after 60 bisections, the largest source-segment `l1` length is
`23/23058430092136939520000`, while the inherited depth-one rank-14 map
Lipschitz upper is `2457600`.  Independent 100-digit replay finds the worst
other-coordinate boundary margin divided by the possible Lipschitz drift is
greater than `3.7e11`.  Continuity and regularity therefore give at least one
noncorner crossing, and the implicit-function theorem gives a connected local
analytic germ with inside/outside traces.

## 2. Actual local fields

The common parameterized face registry gives normalized F8 transversality
strictly above `1/5` on every one of the 32 terminal-core pullback germs.

For an affine stationary destination-core face at depth one with rank path
`[14]`, the frozen recurrence has

```text
D_1=E_1=2457600,
H_1=187673440721829888,
K_0=0, G_0=1.
```

Hence each actual germ receives the explicit F9 unit-speed C2 upper

```text
(3/2) E_1 H_1 = 691839371876953699123200,
```

and the finite 32-face sum is bounded by
`22138859900062518371942400`.

The compact-germ F10 terminating-search theorem now applies to 32 actual
germs, but the least integers `N_F10(g)` have not been executed and are not
invented.  Likewise, the exact Piola identity supplies the parameterized F16
transport law, but no numeric F16 cost is promoted without actual F13 rows.

## 3. What this does not prove

- It does not prove the other `1120` candidate families empty.
- It does not identify or subdivide all global connected components.
- It does not assemble shared stationary faces or a complete incidence atlas.
- It does not produce numeric F10/F13 values or a weighted limiting sum.
- It does not close F14, F15, F17, F18 or any complete 18-field block.
- It does not supply the Gates 1--4 stable/material crosswalk.

## 4. Shortest next route

Use the same interval sign/exclusion engine on all `1152` families: certify
the 32 witnessed families' full clipped connected components and reject the
remaining 1120, including corner coincidences.  Then execute the canonical
F10 search on every component, assemble actual F13/F16 costs and only then
attempt the finite weighted face sum.  F14/F15/F17 remain separate operator
problems even after the atlas is complete.
