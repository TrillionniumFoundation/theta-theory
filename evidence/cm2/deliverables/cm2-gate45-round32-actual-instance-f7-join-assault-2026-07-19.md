# CM2 Gate 4/5 round 32: same-ID actual-instance numeric F7 join

Date: 2026-07-19  
Status: **numeric F7 certified on the parameterized Q2 instance registry; Gate 5 remains open**

Round 31 supplied the missing actual parent-W and recut-instance identities.
The frozen Gate-4 componentwise growth theorem already quantifies every short
invariant-cone curve and counts every physical and homogeneity child once,
with

```text
Xi < 900337/901685.
```

The invariant conditional-density ratio is at most `2000/1999`. Their same-ID
join gives every actual parameterized F7 slot the strict one-step coefficient

```text
(2000/1999)*(900337/901685)
  = 360134800/360493663 < 1,
```

with margin `358863/360493663`. The two-step coefficient is
`129697074171040000/129955681063157569 < 1`.

The slot schema is

```text
slot:f7:(branch-rule-id):(parent-W-id):(natural-index-j),
```

over all 228,012 Q2 branch-rule bases and all Borel parameters of the actual
parent curves. The old 285/289 physical-root overledger is not reused as a cut
count; its noncontracting multiplier is irrelevant because the componentwise
growth theorem already performs the physical reachability and single-charge
sum on arbitrary short W.

This advances global Gate-5 maturity from `4/18` to `5/18`. It does not create
the connected nonempty physical face atlas, numerical coarea/one-sided-trace
rows, F14--F18, or a complete 18-field block. Gate 5 and CM2 remain open.
