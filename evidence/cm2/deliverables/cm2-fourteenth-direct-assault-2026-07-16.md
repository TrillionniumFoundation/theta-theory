# CM2 fourteenth full direct assault

Date: 2026-07-16 (Asia/Shanghai)  
Frozen baseline: v51/v52 and the frozen deliverable artifacts of the first
thirteen direct-assault rounds were preserved byte for byte  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; composite
gates fully closed remain `0/5`**

## 1. Executive result

This pass closes a clean finite coding layer at Gate 1, proves a
cancellation-free source-incidence lemma and nearly isolates the
critical-root class at Gate 3, closes the product-kernel joint recovery
moment at Gate 4, and
binds two genuine strong-field seeds to every certified Gate-5 core.  None of
these layers supplies the full-mass quotient, complete physical current or
complete return-word operator demanded by a composite gate.

- Gate 1 now has a strictly transverse selected QNL homoclinic and hence a
  finite faithful SFT coding of a compact clean horseshoe.  Invariant
  stable/unstable frames give a faithful diagonal Butler--Park class-`H`
  representative, but its same-axis twisting wedges vanish exactly.  The
  compact representative with four frozen nonzero wedges still lacks class
  `H`; on the earlier common basic set containing the connector its stable
  canonical tail is now proved to diverge.  No one representative has both
  `H` and twisting.
- Gates 2/5 now bind adapted and Euclidean unstable inverse-Jacobian bounds
  and a log-Jacobian distortion seed on all 24 positive cores.  The global
  Growth estimate remains a pre-characteristic-restriction template: the
  word/chart-owner boundaries have no `Z` table.  Each full key still has
  only `NONEMPTY`, complete 18-field blocks remain zero, and the selected
  QNL loop is only a common-fiber endpoint seed rather than a quotient
  endpoint packet.
- Gate 3 uses an endpoint-scaled cancellation-free factorisation to prove
  `c_p>0` on 24 canonical source-grazing endpoint row-bands, subdivided into
  1,536 parameter cells.  A required audit correction keeps both the old terminal
  record match and all 2,048 deeper source-tagged boxes pending: the old deep
  routine stopped at the former `c_p` test and did not replay their later
  miss/root/owner classification.  One further selective level nevertheless
  leaves only 12 critical-root boxes and 164 untyped graph charts.  The
  terminal frontier still has positive width, so the side-owner current,
  strong DQ, `MT_DQ` and FACE interfaces remain incomplete.
- Gate 4 transports one immutable product-depth record through both
  orientations of the same occurrence.  It closes a numerical
  product-kernel, unnormalised joint `(B,K)` moment before the fixed
  collision-flux normalisation, one registered-cut boundary numerator, and every separately
  predeclared fixed finite restart history.  Native time-zero `Z`, an
  unbounded physical tail on the number of cuts, the strong-operator/test
  payload and final `C_fw,C_rev,q` remain open.

## 2. Gate table

| gate | strongest newly certified layer | exact remaining hard gap | strict status |
|---|---|---|---|
| 1. holonomy/QNL | transverse selected homoclinic; finite faithful clean-horseshoe SFT; diagonal class-`H` representative | one physically relevant representative with both all-plaque `H` and nonzero twisting; full-mass coding and PPE | **OPEN** |
| 2. physical PPE | selected common-fiber loop/four-wedge endpoint seed | stable-saturated product base, quotient `rho`, reverse kernel/weights, branch label, slope identity, same-carrier endpoint maps and PPE | **OPEN** |
| 3. global event/DQ | endpoint-scaled `c_p>0` lemma on 24 canonical source-grazing row-bands / 1,536 parameter cells; 41,344 conditional physical-first charts; 11,678 nonempty arcs; critical class reduced to 12 boxes | terminal-record containment ledger, 151,500 interval-geometry boxes, 2,048 deep source-tagged boxes awaiting downstream replay, 12 critical boxes, 164 untyped charts; complete current, strong DQ, `MT_DQ`, `FACE_2CUT/FACE_TIME` | **OPEN** |
| 4. same-occurrence `q` | shared product occurrence record; joint `(B,K)` recovery moment; fixed finite registered histories | native countable time-zero `Z`, arbitrary/unbounded repeated cuts, strong operator/test matching, complete `C_fw,C_rev,q` | **OPEN** |
| 5. fixed/full transfer | two core-local strong-field seeds on all 24 positive cores; safe Euclidean inverse-Jacobian envelope | maximal homogeneous word registry, characteristic-boundary `Z`, remaining 17 full-key fields, three strong norms, Kac and phase | **OPEN** |

## 3. Gate 1: clean coding and the class-H/twisting separation

The selected symmetric physical homoclinic is replayed through its clean
48-collision half word.  If `(a,b)` is the tangent at the reversibility
midpoint, both components are strictly nonzero and

```text
|det((a,b),(a,-b))| = 2|ab| > 3e54.
```

The Birkhoff--Smale construction therefore gives a compact clean horseshoe
for a return iterate and a finite Markov coding.  Pulling back the actual
derivative gives a faithful physical cocycle; no periodic matrix surrogate
is used.

On that finite SFT, Hölder frames of the invariant `E^u/E^s` lines give an
exact diagonal representative.  Its stable and unstable canonical products
converge with a Hölder modulus, so it lies in Butler--Park class `H`.
Every loop in this representative is diagonal, hence

```text
det(e_u,psi_z e_u)=det(e_s,psi_z e_s)=0.
```

Weak typicality of this representative is therefore refuted.  Conversely,
the compact QNL gauge carrying the four frozen nonzero wedges has not been
shown to lie in `H` on the selected horseshoe.  At the connector a fresh
point-jet enclosure gives

```text
partial_x partial_y (G_B)_2(0) in (-1/20,-1/25).
```

The resulting stable canonical increment grows like a nonzero constant
times `Lambda^n`, proving divergence on the common basic set containing the
connector.  The two canonical families cannot be identified outside the
fiber-bunched regime.  Gate 1 therefore remains open.

## 4. Gates 2/5: physical strong-field seeds, not full word rows

The universal physical branch theorem binds on every one of the 24 compact
positive cores

```text
J_*^-1 < 144000/180337,
|log(J_*(x)/J_*(y))|
  < 15000000000000000000000000 ell_*(x,y)^(1/3).
```

At `ell_*=10^-90` the log variation is below `3/200000`.  Using

```text
(5/27)d ell_E < d ell_* < (141/4)d ell_E
```

also gives the safe, deliberately coarse Euclidean envelope

```text
J_E^-1 < (3807/20)(144000/180337)
       = 27410400/180337.
```

These are genuine one-dimensional unstable-Jacobian/distortion seeds, not
collision-area Jacobians.  They are nevertheless only core-local: the
441,280 candidate keys still lack maximal homogeneous subbranch ids and all
24 certified full keys still have only their `NONEMPTY` field complete.

Before return-word restriction the global estimate is

```text
Xi_1 < 900337/901685 = 1-1348/901685 < 1.
```

It cannot be copied into Gate-5 field 7.  A return-word characteristic
function exposes word-domain, source-chart seam and owner boundaries that
the physical singularity theorem does not charge.  Their multiplicity and
boundary `Z` remain absent, as do fields 14--17 after restriction.

The selected QNL loop matrix and four wedges are now bound as a common-fiber
Gate-2 endpoint seed.  They do not create a quotient inverse-branch label,
the pointwise slope identity, same-carrier maps `X_a,Y_a`, or a denominator
lower bound.  Stable base, `rho`, reverse weights, PPE and all Gate-2
physical fields remain absent.

## 5. Gate 3: endpoint removal and critical-resultant frontier

At a source-grazing endpoint, write the source-to-target vector in the
endpoint frame and let `x` be inward angular distance.  The exact endpoint
identity turns the cancellation-prone numerator into

```text
(A-sigma R_t)(A+sigma R_t),
A-sigma R_t=(cos(x)-1)a_0+q sin(x)B_0.
```

Separate interval signs of the two factors and of the rationalised
denominator prove `c_p>0` on 24 canonical width-`1/1024`
source-grazing endpoint row-bands, subdivided into 1,536 parameter cells.
Their total fixed-s `t`-width across the 24 rows is `3/128`.  This removes
the analytic cancellation in the source-incidence prerequisite.  The frozen
terminal artifact, however, did not export each record's
`(side,t0,t1,v0,v1)` tuple, so a record-to-band containment ledger is still
absent.  Nor does the lemma classify the 2,048 source-tagged boxes in the
deeper replay: that routine stopped at the old `c_p` prerequisite before
evaluating miss discriminants, root gaps, candidate ownership or
criticality.  Those boxes and their width therefore remain pending.

One extra selective level in both normalised coordinates performs 1,722,936
strict audit calls and records

```text
conditionally physical-first graph charts:  41,344
strictly nonempty root arcs:                  11,678
untyped candidate graph charts:                 164
critical-root boxes:                              12
interval-geometry boxes:                    151,500
deep source-tagged boxes pending replay:       2,048
```

The union-aware simultaneous deep terminal fixed-s outer width remains

```text
603/1024.
```

The separate reason-wise envelopes are `595/1024`, `1/1024` and `1/128`.
They are not additive; the replay directly certifies the union-aware
`603/1024` bound.

The critical algebra is now exact.  For `Delta=r^2-w^2`, on
`Delta=Delta_t=0`,

```text
det D_(t,s)(Delta,Delta_t) = -4 r^2 w_s w_tt.
```

The coefficient identity is certified, but `w_s w_tt` has not yet been
proved nonzero on all 12 remaining boxes.  The retained interval-geometry
outer boxes also have parameter-area upper cover `37875/65536`; this is not
a certified positive physical singularity area.  Hence the existing current with
coefficient `partial_s Delta/abs(partial_t Delta)` remains partial and Gate
3 cannot be promoted.

## 6. Gate 4: one shared product record and a joint moment

The query-independent product law uses

```text
w_K=(3/4)4^-K,
j=floor(2^K u_(e,s)(a)).
```

All estimates in this section are for the unnormalised product kernel before
the fixed collision-flux (`Z_N^-1`) normalisation.

The immutable `(occurrence,e,s,K,j,owner,polarity,restriction)` record is
transported through forward and reverse views without additive duplication.
With `A0=301500`, `A1=1005` and `gamma=1/12060`,

```text
sum_K w_K 2^K exp(gamma(R_fw+R_rev)) < 2*3^50.
```

Independence of the declared product mark from the endpoint rank gives

```text
integral 2^(B_s+K) exp(gamma(R_fw+R_rev)) d(m_s tensor w)
 < 31080151660381513756308599682583861157637/12800000.
```

One registered cut also has levelwise boundary numerator, per unit incoming
occurrence mass,

```text
(3/2) C_mesh = 104979995960750899200.
```

For each history length `H` fixed before the later query, the clocked restart
moment is below `(2*3^50)^H`.  There is no physical summable tail on an
unbounded `H`, and materialising the native countable initial partition
still has time-zero `Z=infinity`.  The complete strong-operator/test payload
and Gate-3 current matching are also missing, so these controlled product
subcosts are not renamed `C_fw,C_rev` or final `q`.

## 7. Latest-technology audit

The official arXiv API was checked again on 2026-07-16 after the generic
search endpoint reached its quota.  No applicable new bridge was found.

- `arXiv:2606.10155v1` remains the nearest dispersing-billiard
  transfer-operator review; its characteristic-function restriction
  frontier is precisely the missing word-boundary interface here.
- `arXiv:2604.25881v1` gives the Sinai-billiard MME from one-sided symbolic
  objects, not a collision-SRB stable quotient or the required strong word
  restriction.
- `arXiv:2604.19671v2` treats a shrinking hole for a fixed Sinai billiard,
  not this moving-scatterer branch-record current and recovery contract.
- `arXiv:2604.13401v1` concerns cocycles over hyperbolic automorphisms under
  assumptions that do not produce the singular-billiard quotient or merge
  the class-`H` and twisting representatives above.

No result justifies bypassing the full-mass stable quotient, complete
physical current/DQ, characteristic-boundary table, same-representative
`H`/twisting condition, or the final three-norm/Kac/phase interfaces.

## 8. Exact remaining boundary

```text
Gate 1: one representative with class H + twisting; full-mass coding/PPE
Gate 2: stable product base, rho, reverse kernel, endpoint packet and PPE
Gate 3: interval geometry, 2,048 source descendants pending replay,
        12 critical boxes, 164 chart typings,
        complete current, DQ/MT_DQ and FACE
Gate 4: native/repeated recovery, strong operator/test payload,
        complete C_fw,C_rev,q
Gate 5: maximal word components, characteristic boundary Z,
        complete 18-field blocks (17 remaining full-key fields per
        certified key), three norms, Kac and phase
```

The next direct line is: isolate the 12 critical resultants and audit/resolve
the interval-geometry boxes with cancellation-free miss/root-gap/candidate
coordinates;
construct a single twisting class-`H` representative or prove the stronger
obstruction on the selected horseshoe; build maximal homogeneous word
domains with characteristic-boundary `Z`; then join those rows to the
same-occurrence operator ledger, stable quotient/PPE and the final
three-norm/Kac/phase layer.

## 9. Reproduction and freeze

The four new certificate stacks are replayed from their frozen inputs,
their semantic mutation suites are required to reject every injected
overclaim, and default live execution must exit `2`.  The aggregate
sidecars additionally pin v52, the thirteenth-round sidecar and its frozen
predecessor references.  The append-only `memory/2026-07-16.md` continued to
grow after the twelfth-round snapshot and is deliberately outside the
current immutable deliverable replay; no all-history recursive-SHA claim is
made for that mutable file.

Final validation rejects all semantic mutations in the four new stacks:

```text
Gate 1     18/18
Gates 2/5 18/18
Gate 3     16/16
Gate 4     18/18
total      70/70
```

All four default live verifiers exit `2`.  The two clean full Gate-3
replays finish in `17:34.33` and `16:43.83` with identical manifest result;
the second run is an independent frozen replay after all scope corrections.

Primary evidence:

- `deliverables/cm2-gate1-global-coding-class-h-separation-frontier-assault-2026-07-16.md`;
- `deliverables/cm2-gate25-universal-operator-endpoint-template-frontier-assault-2026-07-16.md`;
- `deliverables/cm2-gate3-endpoint-scaled-resultant-frontier-assault-2026-07-16.md`;
- `deliverables/cm2-gate4-product-same-occurrence-joint-moment-frontier-assault-2026-07-16.md`.
