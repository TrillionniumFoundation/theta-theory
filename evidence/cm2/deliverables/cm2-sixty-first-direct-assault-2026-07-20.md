# CM2 Round 61 full direct assault

Date label: 2026-07-20 (one request batch; final freeze crossed local midnight)  
Strict verdict: **0/5 complete composite gates; Gate 5 maturity 10/18 with
zero complete 18-field blocks; CM2 NO-GO_FOR_CLAIM**

## Round result

Round 61 closes no composite gate and does not promote any missing physical
strong interface.  It does, however, replace three conditional or
combinatorial bottlenecks by exact typed objects:

1. Gate 4 gains an actual marker-weighted physical cone-curve **measure**
   lift and an endpoint-preserving weighted graph-TV defect ledger for the
   positive bad landing law.
2. Gate 5 gains an actual fixed-insertion same-owner-law complement F10
   anchor, unconditional Borel typing of all seven suffix predicates and
   recovery capacity `R`, and an explicit same-law power-Orlicz criterion.
3. Gates 1/2/3 gain exact same-token gauge covariance, the precise
   marker/holonomy RN equation, and an actual all-depth stopped weak-TV
   restriction/assembly which removes the raw `8*162^n` candidate-word cost
   from that weak route.

Two red-team corrections were required before freeze.  First, multiplying a
regular curve law by an arbitrary Borel marker does not leave the regular
standard-family class, so the Gate-4 result is called a cone-curve measure
lift only.  Second, continuous cemetery endpoint/root labels cannot all be
adjoined as atoms to a countable algebra; Gate 5 now uses a countable
separating generator on their standard-Borel product and an injective
subprobability evaluation code.  All affected artifacts were regenerated
before the independent audit.  No older artifact was edited.

## Gate 4: actual marker-weighted cone-curve measure

Round 35 and Round 56 give an exact once-covered physical representation of
the induced collision law by tagged regular unstable-cone curves,

```text
mu_C=integral mu_omega dP(omega).
```

Round 60 supplies the actual common-landing RN marker

```text
kappa_B=g_B mu_C,
0<=g_B<=1.
```

Tonelli therefore gives the exact physical identity

```text
kappa_B=integral (g_B mu_omega)dP(omega).
```

On a tagged source branch with marker `a=d kappa_A/dmu_C`, the marked source
density is

```text
rho_A,marked=a cp/sqrt(17),
```

and its regular image `H=T_s^n|U` has the exact same-arclength pushforward
formula

```text
rho_B,marked(Hx)
 =a(x)[cp(x)/sqrt(17)]/J_ellE H(x).
```

This is a real measure/carrier upgrade.  It is not a dynamically invariant
unstable Rokhlin partition or stable-holonomy product structure.  Arbitrary
Borel `g_B` can have zero lower density, infinitely many support components
and unbounded log/BV oscillation.  The unmarked `mu_omega` are regular
standard-family elements; the marked laws are not claimed to remain so.

Consequently physical landing fields 1 and 4 receive exact partial
interfaces only.  Field 6 remains the sole complete row, and field 7 gains
only the graph-level assembly described next.  The seven-field count stays
exactly `1/7`; Gate 2's official immutable score stays `0/17`.

## Gate 4: exact positive graph-defect ledger

Let `D=D_land` be the frozen Borel landing defect.  Define

```text
X_D^graph
 ={finite signed tagged graph measures nu:
   integral 2^D d|nu|<infinity},

||nu||_X=integral 2^D d|nu|.
```

Multiplication by `2^D` is an isometric order isomorphism to finite signed
measures with total-variation norm, so this is a Banach lattice.  For the
exact bad graph `Gamma_B`, the pinned Round-58 moment becomes the exact norm
identity

```text
||Gamma_B||_X
 =integral_B h(y)2^D_land(y)dlambda(y)<infinity.
```

Both physical endpoint pushforwards contract ordinary TV because `2^D>=1`.
The exact positive identity

```text
Gamma_cap=Gamma_G+iota_B(Gamma_B)
```

retains source, landing, first-hit time, `n/path/ID/owner` and one parent
charge.  It performs no normalization, endpoint replacement, deletion or
renaming of regular bad mass as singular cemetery.  This closes `4/5` rows
of a newly declared **graph-TV defect ledger**.  It is not a Gate score: in
the requested strong-current/cemetery interface only the immutable tag row
is complete, and there is no downstream CM2 theorem accepting the graph
remainder.

A smooth logical separator makes the boundary sharp.  On a short plaque of
length `L=2/(3C_p)`, take

```text
g_N(x)=1/2+(1/4)sin(2*pi*N*x/L).
```

Then `D_land=2` and `h 2^D=2L` are independent of `N`, while

```text
integral |g_N'|=N.
```

Thus the installed scalar moment does not control BV/derivative trace.  The
separator is not asserted to be a billiard realization and does not rule out
every possible anisotropic space.  Fibrewise properness, a physical strong
defect embedding, later/repeated clocks, `q in L^(6/5)` and strong cemetery
remain uncertified.

## Gate 5: actual fixed-insertion complement anchor

Fix one insertion time `j` at the frozen actual base law `s=0`.  Round 52's actual positive owner law
`m_j^own`, pushed by the Round-54 root/collar map `q_j`, gives

```text
nu_j=(q_j)_#m_j^own.
```

The actual complement

```text
nu_j^bad=1_(A_col^c)nu_j,
A_col^c=N_cut disjoint_union N_acc,
```

is therefore a finite same-owner-law Borel submeasure.  Its value and
nullity are unknown; collision-area nullity is not substituted for the
singular owner/coarea trace.

For Round 52's nonnegative forward and reverse F10 costs, define the
cost-weighted pushforwards `xi_j^f,xi_j^r`.  They satisfy

```text
xi_j^f << nu_j,
xi_j^r << nu_j,
```

so their Borel RN densities are actual same-law `L^1` anchors.  The exact
fixed-time arithmetic is

```text
395304765824751/220000
+162772550633721/176000
=2395081816467609/880000.
```

Restriction to `A_col^c` preserves finiteness.  A deterministic labelled
cemetery pushforward retains continuous endpoint/root coordinates, owner
token, word cell and insertion time, preserving the fixed-`j` marked mass
and F10 integral.  This is not a strong trace, a Jordan identification or an
all-time assembly.  The bound has no decay in `j`, so it cannot be summed
against `w_Z^j>1`.

The fixed-`j` hybrid now has a genuinely finite complement term.  Its long
recovery term and short raw-debt term remain unpaid, so neither the full
fixed-`j` policy nor an all-insertion policy is certified finite.

## Gate 5: seven Borel suffix predicates and recovery capacity

The actual collar record space and the labelled collision-plus-cemetery
state are standard Borel.  The cemetery sector is a product of immutable
tokens with continuous endpoint/root label spaces, not a countable atom
list.  A Borel embedding and rational cylinder sets give a countable
separating algebra `{D_m}`.  For standard-Borel subprobabilities,

```text
K -> (K(D_m))_(m>=0)
```

is injective, so equality of every coordinate is equivalent to kernel
equality.  Common input, killed-word/C24 operator and labelled output/next
input codes are therefore Borel; sentinels totalize them without reading
`K` or `R` outside `A_col`.

The exact result is

```text
Borel suffix predicates: 7/7 certified,
universal values:         2 true / 5 open,
Borel R on A_col:         certified,
physical R>=r_K:          not certified.
```

This code schema has the previously declared `|s|<=1/400` parameter typing;
the fixed-`j` F10 finiteness above is only the frozen actual base law.  It
closes typing only and does not prove the five missing physical input,
`C24`, operator, output or horizon values.

## Gate 5: explicit same-law power-Orlicz frontier

On `nu_j|A_col`, put

```text
Y=w_Z^r_K,
q_col=log(2)/(beta log(w_Z))=1/alpha_opt,
Phi(t)=t^q_col.
```

Since `r_K=ceil(beta(K+1))`, pointwise

```text
2^(K+1)<=Phi(Y)<w_Z^q_col 2^(K+1).
```

Hence, on the same owner law,

```text
Z_col<infinity
iff integral Phi(Y)dnu_j<infinity.
```

This is an exact explicit Orlicz criterion, not a finite bound.  No frozen
same-law object supplies finite raw `Z_col`; active Abel, the Orlicz moment,
the long/short suffix debts, weighted Jordan variation/common mode and
strong cemetery remain open.  Gate-5 maturity therefore stays `10/18`, and
the complete block count stays zero.

## Gate 1: exact same-token gauge covariance

For one immutable basepoint and physical loop token, transport the loop and
both physical eigenvectors simultaneously:

```text
Psi^D=D Psi D^-1,
v_i^D=Dv_i.
```

Then exactly

```text
det(Psi^D v_i^D,v_j^D)
 =det(D)det(Psi v_i,v_j).
```

Thus an exact future `SL(2)` same-token cohomology join preserves all four
twisting wedges numerically and needs no separate `10^-29` matrix
comparison.  This removes an artificial downstream numerical obligation; it
does not construct the all-plaque combined class-`H` gauge, the exact
compact-to-combined loop join or the required weighted increment registry.
Gate 1 remains `NOT_CERTIFIED`.

## Gate 2: exact marker/holonomy equation

If a future physical stable holonomy satisfies

```text
h_*mu_u=J_hol mu_v,
```

then the RN chain rule gives

```text
d[h_*(g_u mu_u)]/dmu_v
 =(g_u o h^-1)J_hol.
```

Transporting the same common landing law is therefore equivalent to

```text
g_v=(g_u o h^-1)J_hol             mu_v-a.e.
```

An identity-holonomy separator with equal common mass and mismatched markers
shows that qualitative plaques and `J_hol=1` do not force this equation.
The equation is an exact typed interface, not an installed physical stable
quotient.  All-depth plaques/projection/two-sided holonomy remain absent;
Gate 2 remains `0/17` and `NOT_CERTIFIED`.

## Gate 3: actual stopped weak graph-TV compression

The dependency chain

```text
integer-valued Borel Dbar
+ finite schedule N(d)
+ actual half-open first-root word cells
+ finitely many fixed-depth connected components
```

gives a countable disjoint Borel partition of the actual stopped carrier,
with singular/grazing/tie records retained in explicit cemetery.  Restrict a
finite signed source measure cellwise:

```text
R_stop mu=(1_A mu)_A.
```

Then

```text
sum_A ||1_A mu||_TV=||mu||_TV,
Q_source R_stop mu=mu,
||Q_output nu||_TV<=sum_A||nu_A||_TV.
```

Positive mass is counted exactly once.  Consequently the raw candidate-word
factor `8*162^n` is not needed on this all-depth weak stopped-TV route.

This is not a physical strong operator.  Splitting `1_[0,1)` into `m`
half-open pieces keeps `L^1` mass one but makes the direct-sum zero-extension
`L^1+BV` ratio

```text
(1+2m)/3 -> infinity.
```

Endpoint fragmentation, moving singular faces and current terms must still
be paid.  Physical strong `R_s/Q_s`, directional Piola/current and `MT_DQ`
remain uncertified, so Gate 3 remains `NOT_CERTIFIED`.

## Latest-technology audit

The current official-source audit includes `arXiv:2603.19509v3`,
`2604.25881v1`, `2604.19671v2`, `2605.18110v3`, `2606.10155v1` and
`2606.19621v2`.

- Fixed-table MME total-length growth does not control the number or minimum
  length of marker components and is not the pinned Liouville/SRB landing
  law.
- Small-hole standard-family evolution starts with an already regular
  family and uses survival/conditioned-mass normalization; it does not
  construct the actual marker product or strong assembly.
- Nonautonomous response assumes the uniform strong differentiability and
  memory-loss interfaces that are missing for the moving billiard.
- Current semialgebraic and inter-sign transport results do not supply
  depth-integrated Piola/current or positive Jordan/common-mode moments.

No external theorem is promoted into a physical CM2 certification.

## Independent audit and acceptance

The append-only independent audit replays all exact algebra, RN, Orlicz,
Banach-lattice and stopped-partition rows; checks that `D_land` and `Dbar`
are never conflated; and verifies that no positive regular remainder is
renamed collision-null cemetery.  It records `PASS` after the two pre-freeze
type corrections above, with no remaining claimed-scope blocker or
cross-leaf state contradiction.

Final four-leaf acceptance matrix:

```text
syntax:                         8/8
dependency/artifact pins:       36/36
integrity / replay / reemit:    4/4 / 4/4 / 4/4
hostile + strict-JSON guards:   723/723 rejected
SHA ledger rows:                16/16
default cert/verifier:          8/8 exit 2
Round-61 stale/temp files:      0
```

The strict state is consistent across every leaf:

```text
Gate 1 / Gate 2 / Gate 3 / Gate 4 / Gate 5: NOT_CERTIFIED
Gate 2 immutable fields:                     0/17
Gate 5 maturity / complete blocks:           10/18 / 0
complete composite gates:                    0/5
CM2:                                         NO-GO_FOR_CLAIM
```

## Shortest remaining routes

1. **Gate 4 / Gate 2:** construct the actual invariant unstable/stable
   product registry on the common landing law, prove the marker covariance
   equation and quantitative `F R<C_p theta L`, then install a physical
   strong restriction/assembly.  In parallel, seek a legitimate strong
   recipient for the positive graph defect; the scalar `D_land` moment alone
   cannot supply BV/derivative control.
2. **Gate 5:** estimate `N_cut,N_acc` owner-trace strata and obtain
   deduplicated `w_Z^j`-summable all-time decay; prove the active Abel/raw
   `Z_col`/power-Orlicz bound; establish the five missing suffix values and
   `R>=r_K`; then separately pay weighted Jordan variation, common mode and
   complement cemetery.
3. **Gate 1:** materialize one all-plaque combined determinant-one class-`H`
   gauge and the exact same-token cohomology join.  Exact covariance will
   then transport twisting without a numerical truncation bridge.
4. **Gate 3:** lift the countable stopped partition from weak TV to the
   intended strong graph/current carrier by controlling endpoint
   fragmentation and moving-domain traces, then construct directional Piola
   and `MT_DQ`.

Until those same-law and strong-space joins are paid, CM2 remains a strict
no-go claim.
