# CM2 Round300-E — R248 half-open-owner lower component edges

## Outcome

Round300-E promotes the narrow, one-sided component-connectivity consequence
of the sealed R235/R248 endpoint-sheet partition and the sealed R295-A
two-sided graph-sheet incidences.

The complete result is:

- `111,524` R295-A two-sided graph-sheet binding rows audited;
- `472` bindings contain an R248 single-endpoint graph-sheet subset;
- `12,992` distinct R248 sheet pieces pass every join;
- `12,992` canonical owner-occurrence ↔ Round266-root edges;
- `472` already occur in the Round300-C positive-volume edge set;
- `12,520` are novel against Round300-C.

No canonical edge pair is duplicated.

## Exact owner proof

Every promoted witness independently closes the following chain:

1. the R295-A row and its source R293 row identify the same R291 physical
   witness cell and the same two registry endpoints;
2. the R291 cell is byte-lineage joined to its exact R182 graph leaf;
3. the R235 sheet source and R234 frontier share the R248 sheet's retained
   child, interface, chart, exact `(p,s)` base rectangle, and endpoint factor;
4. the full six-coordinate R234 frontier box is contained in the R182/R291
   leaf box;
5. the R235 active endpoint factor is exactly one factor of the R291
   product-zero predicate;
6. exactly one of the two R295-A endpoint signatures equals the R248
   event-absent owner signature, and its official key also agrees;
7. the R248 sheet and its event-absent owner bulk have the same Round266
   quotient root.

All `12,992` rows pass these checks. There are zero containment, equation,
owner-signature, owner-key, or Round266-root failures.

## Strict one-sided boundary

The edge semantics are:

```text
R248_EVENT_ABSENT_HALF_OPEN_OWNER_TO_ROUND266_ROOT__
COMPONENT_CONNECTIVITY_NOT_OCCURRENCE_IDENTITY
```

Only the unique event-absent owner endpoint is eligible for the later
component DSU. The event-present endpoint gets zero edge credit. Mere
two-sided incidence without the complete R248 owner chain also gets zero
edge credit; consequently the other `111,052` R295-A graph bindings remain
unpromoted.

Round300-E inventories legal edges but does not apply them. Occurrence
identity collapse, component-union application, DSU rank reduction,
quotient, maximality, fibre, and global-disposition credit all remain zero.

## Independent verification and attacks

The independent verifier never imports, executes, or parses the producer.
Before opening any candidate output, it reconstructs the expected witness,
all-edge, novel-edge, and result objects from nine hash-pinned upstream
artifacts. It then requires both semantic equality and byte-for-byte equality
with deterministic canonical JSON/GZIP encodings.

All `36/36` targeted attacks are rejected:

- `32` fully recommitted semantic attacks, including present-side
  substitution, ambiguous-owner forgery, shifted geometry, wrong active
  factor, both mere-incidence and identity overclaims, DSU/quotient/
  maximality/fibre/disposition credit forgery, Round300-C overlap forgery,
  and novel-set forgery;
- `4` duplicate/nonfinite/NUL JSON and malformed-GZIP attacks.

## Artifact commitments

| artifact | SHA-256 |
|---|---|
| producer | `c4cc1e2eafe1f1db2da143da365a56a7907933a2df0d77dd433a16eede6f8dbe` |
| witness ledger | `69f480da55e917b7b75bfe1efa823a9228d9b563f6f3aabd4e2a364c9e50eb9f` |
| all-edge ledger | `4aa7ae76d984d15b345d5d6805ec4f06a47e30a46fff1315f346e81f1f17122f` |
| novel-edge ledger | `a5c37aad322be53b9135fcef429274812639ab456d73cd43c03e64eb0c572501` |
| result file | `193c74535431789ba4fcdad598487916712d6ad1c04f191a083076e4a05f7e3e` |
| embedded result | `58adb4b2ffb70601df69a833deca7106b2cc2d9a06c851b668be7aaae51913b9` |
| verifier | `8f986fc6f30348484ef2bf9cffcb031b7226d2f29ee63a09e138c1489b754b07` |
| attack suite | `3591536d58e3999fe059cd121e28bcbdc14fc795538ca49b5d1c2e88ddbea112` |
| embedded attack suite | `cec41954bd8a41ac600544996027c483dfca7847d5d69582e0caeb0b6e9a013c` |
| verification file | `313008d59a2d67b5ce4b7c8d62cd68a26a855a65c34396b5ae0176dda2096d4a` |
| embedded verification | `8de6e079190b5dcd691636b99170df70962836f66ab1c4fb32437be8ae825417` |

## Required next

Freeze the remaining legal edge channels and then apply the complete
deduplicated edge inventory in one expanded-registry DSU rebuild. Only that
later round may claim actual component unions, rank reduction, or a quotient.
