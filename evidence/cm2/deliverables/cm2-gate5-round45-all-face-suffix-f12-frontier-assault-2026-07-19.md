# CM2 Gate 5 Round-45 all-face suffix F12 frontier assault

Date: 2026-07-19  
Verdict: **F12 is certified on all five physical face grammars for every
finite regular arbitrary-`R_n` suffix rank path.  The terminal empty-suffix
F16 sublayer inherits the physical F13 tail, but nonempty-suffix strong F16
still lacks a joint rank estimate or cancellation.**

## 1. All-face arbitrary-suffix F12

Round 36 supplies unit-speed `C2` parameterizations for all five physical
face grammars, and Round 44 supplies the same-ID suffix two-trace F13 ledger.
For one regular collision of incidence rank `B>=14`, use the frozen derivative
envelope

```text
L(B) = 150 * 2^B.
```

For suffix rank path `(B_1,...,B_k)`, define

```text
D_suffix = product_i L(B_i).
```

The chain rule gives, for every nonempty suffix,

```text
||Phi composed S||_C1(face)
  < (1 + D_suffix) ||Phi||_C1.
```

The empty suffix is the identity and has exact cost `1`.  This closes F12 as
a parameterized field on

1. source-core clipping faces;
2. intermediate core-avoidance preimage faces;
3. terminal core-preimage faces;
4. collision-singularity or owner-change faces;
5. moving-occurrence faces.

No global rank-path moment is required to certify this finite-path F12 field.
Gate-5 maturity advances from `8/18` to `9/18`; complete 18-field operator
blocks remain zero.

## 2. Terminal empty-suffix F16 sublayer

With no suffix, the F12 multiplier is exactly one.  Positivity and the
Round-44 F13 domination therefore give

```text
c_F16,empty <= c_F13,n
             <= (3816937/7800000) c_X,n
             =  (3816937/47112000) c_D1,n.
```

This terminal sublayer inherits the physical `L^(6/5)` moment and the block
tail exponent `1/6`.

## 3. Exact nonempty-suffix obstruction

For a nonempty suffix, a strong F16 estimate contains an insertion-rank factor
and at least one suffix derivative factor.  The available one-time moment
does not imply the resulting product moment.

Indeed, for `m>=4`, let

```text
B = 4m,
P(m) = (127/128) * (1/128)^(m-4).
```

Then the law sums to one and

```text
E[2^(3B/2)] = 127 * 2^18 = 33292288 < infinity,
E[2^(2B)]   = infinity.
```

This is a logical moment countermodel, not a physical billiard counterexample.
It proves that the current `3B/2` moment alone cannot certify a nonempty-suffix
strong F16 norm.

The remaining viable routes are:

1. a joint insertion/suffix rank tail;
2. a lower effective suffix derivative exponent;
3. an exact flux/test cancellation before taking the strong norm.

## 4. Strict frontier

F12 is certified, and terminal empty-suffix F16 is certified only as a
sublayer.  Nonempty-suffix strong F16, cemetery-compatible F16, complete
strong F13, full all-face F10 and F14--F18 remain not certified.  Gate 5 is
therefore `9/18`, not a complete block.

