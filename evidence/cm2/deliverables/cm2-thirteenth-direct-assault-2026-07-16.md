# CM2 thirteenth full direct assault

Date: 2026-07-16 (Asia/Shanghai)  
Frozen baseline: v51/v52 and the first twelve direct-assault rounds were
preserved byte for byte  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; composite
gates fully closed remain `0/5`**

## 1. Executive result

This pass closes substantial local interfaces at Gates 1, 3 and 4 and fills
the missing roof-two chart layer at Gates 2/5.  It does not promote a single
composite gate past the frozen specification.

- Gate 1 now has one immutable physical QNL homoclinic occurrence, exact
  local plaque-entry times, a direct numerical enclosure of its selected
  compact-gauge holonomy loop, and four nonzero QNL eigen-axis twisting
  wedges.  The final tail proof uses `N=260`, includes the fixed
  `lambda^96` enlargement on the stable side, and bounds the two-sided loop
  error by `8.568e-49`.  A faithful global coding, uniform all-plaque
  Hoelder holonomies, Butler--Park class `H`, and physical PPE are still
  absent.
- Gates 2/5 now have all four roof-two transparent-wall charts, all 28
  core-local roof-level chart pairs and all 52 core-local split slots on the
  24 certified positive-mass return cores.  These are compact local charts,
  not maximal homogeneous word blocks or a stable-saturated quotient; the
  number of complete 18-field operator blocks remains zero.
- Gate 3 now has a conditionally physical-first graph registry, a strictly
  nonempty root-arc subfamily, and a genuine partial marked current.  Its
  coefficient records the `t`-side orientation and is
  `partial_s Delta/abs(partial_t Delta)` against the fixed hit-minus-miss
  mark.  The remaining fixed-parameter unresolved width falls from
  `1031/512` to `373/256`, but remains positive.  A complete owner atlas,
  strong-space DQ, branch-record `MT_DQ` and the two FACE interfaces remain
  open.
- Gate 4 now has a strict componentwise global one-step Growth contraction,
  an all-iterate four-phase standard-curve family, adapted distortion and an
  invariant density cone.  It also has explicit `C_p`, `vartheta_p`, recovery
  constants and one native levelwise unnormalised recovery moment.  The
  countably infinite native initial partition has infinite time-zero boundary
  `Z` when materialized as one family, and the same-occurrence forward/reverse
  operator ledgers needed for
  `C_fw,C_rev,q` remain missing.

## 2. Gate table

| gate | strongest newly certified layer | exact remaining hard gap | strict status |
|---|---|---|---|
| 1. holonomy/QNL | one immutable physical homoclinic word; direct selected `psi_z` enclosure; four nonzero local QNL twisting wedges | faithful global physical coding, uniform compatible all-plaque Hoelder holonomies, global class `H`, physical PPE | **OPEN** |
| 2. physical PPE | 24 positive-mass compact return cores; complete core-local roof chart layer including four transparent-wall charts | stable-saturated product base, quotient `rho`, reverse weights/kernel, Gate-2 same-carrier endpoint maps/carrier, native stopping and PPE | **OPEN** |
| 3. global event/DQ | 27,356 conditionally physical-first charts, 7,122 strictly nonempty root arcs, oriented partial current and terminal-cause decomposition | 79,660 interval-geometry boxes, 11,884 critical-root boxes, 1,536 source-incidence boxes, 368 untyped graph charts; complete current, strong DQ, `MT_DQ`, `FACE_2CUT/FACE_TIME` | **OPEN** |
| 4. same-occurrence `q` | `Xi_1<900337/901685`; all-iterate `D_std<30000000`; numeric density cone, `C_p,vartheta_p,A0,A1`; native levelwise moment | full unnormalised/reweighted native contract, repeated-indicator numerator, same-occurrence forward/reverse record ledger, joint `(B,K)` moment and numeric `C_fw,C_rev,q` | **OPEN** |
| 5. fixed/full transfer | every local roof chart slot on the 24 cores is physical; wall-adjacent roof-two chart costs are explicit | maximal homogeneous word domains, remaining 17 full-key fields, three CM2 strong norms, full Kac currents and operator phase transfer | **OPEN** |

## 3. Gate 1: a direct selected holonomy loop and four wedges

The immutable-orbit layer selects the unique actual unstable-graph point
`z_h` with physical itinerary

```text
... QNL | Q5^2 B^4 Q5^2 | QNL ...,
T^96 z_h=I z_h,
F^48 z_h=I z_h,                  F=T^2.
```

Its local plaque-entry times are `N_u=0` and `N_s=48`.  On the compact
logarithmic gauge core the selected typed loop is

```text
psi_z=H_hat^s(z_h,p) H_hat^u(p,z_h):E_p -> E_p.
```

The numerical stack moves `N=260` QNL returns into each tail and performs a
5000-bit correlation-preserving Arb replay.  The whole prefix interval
satisfies

```text
x_prefix-x(z_h centre)
 = -1.1732640510476685...e-67 +/- 4.98e-117,
```

which lies strictly inside the previously frozen `z_h` interval of radius
`1e-50`.  The direct finite loop in the canonical QNL eigenfiber is enclosed
near

```text
[  7.2295747729e2      -1.7344767892e-28 ]
[ -4.9098818223e77      1.1779497863e47 ] .
```

The local increment proof explicitly expands the triangular gauges.  Its
largest scalar product has five nontrivial factors,

```text
t(X) t(Y) c t(x) t(y),
```

so the final finite-product ledger uses degree five, not degree four.  The
derived majorant is about `1.9052e41`, strictly below the declared `1e80`.
For the two conjugated tails the critical powers are

```text
right: lambda^(2n+1),
left:  lambda^(2n+97)=lambda^96 lambda^(2n+1).
```

At `N=260` the right and left exponent sums are respectively below
`7.933e-227` and `1.745e-126`; the resulting entrywise two-sided loop error
is below `8.568e-49`.  The four canonical eigen-axis wedges have magnitudes
bounded away from zero by the displayed intervals; the smallest is centred
at `1.7344767892e-28` with final interval radius about `3.43e-48`.

This is a direct computation of the selected loop.  No periodic-shadow
matrix is substituted for it.  It establishes one local twisting occurrence
but does not manufacture the global coding, one uniform holonomy family on
all coded plaques, class `H`, or PPE.  Gate 1 remains open.

## 4. Gates 2/5: the roof-two transparent-wall layer

The four roof-two keys cross one transparent lattice wall before the target
collision.  In oriented wall coordinates `(z,eta)`, all four compact source
boxes have strict flight, corner-clearance and transversality margins.  The
source-to-wall maps satisfy

```text
|det D_(t,p)(z,eta)| > 3/20,
||D_(t,p)(z,eta)||_infinity < 3,
||D_(z,eta)(t,p)||_infinity < 10.
```

A `2 x 2 x 2` subdivision over each roof-two source yields 32 strict suffix
cells.  On every cell the wall-to-target local collision chart and its
inverse have infinity norm below 20.  Consequently all

```text
20*1 + 4*2 = 28
```

core-local roof-level prefix/suffix pairs and all

```text
20*2 + 4*3 = 52
```

core-local split slots now have physical charts.  A transparent wall is not
a collision or a singularity.  These costs are local chart costs, not
unstable Jacobian, distortion, cut-`Z`, DQ or CM2 strong-operator costs.

Each of the 24 registered full keys still has only its `NONEMPTY` schema
field decided.  Compact central cores are not maximal homogeneous word
domains; complete 18-field physical operator blocks remain zero.  The
two-dimensional cores also remain unsaturated by stable plaques and do not
supply Gate-2 `rho`, reverse weights, a quotient kernel or PPE.

## 5. Gate 3: physical-first graphs, oriented partial current and blockers

The nearest-endpoint two-anchor replay is pointwise identical to the frozen
64-row carrier while substantially reducing interval dependency.  With
half-open `(t,v)` ownership and the final endpoints closed, the final
registry contains

```text
conditionally physical-first graph charts       27,356
strictly nonempty physical-first root arcs         7,122
untyped candidate graph charts                        368
maximum conditional charts on one fixed-s slice       478
maximum nonempty root arcs on one fixed-s slice        144.
```

On each physical chart `partial_t Delta` is strictly signed.  The certificate
records which `t`-side is the hit side and transports graph velocity into the
fixed hit-minus-miss polarity.  Thus the unified coefficient is

```text
partial_s Delta/abs(partial_t Delta),
```

equivalently `-sgn(partial_t Delta) dt/ds`.  Both signs of
`partial_t Delta` occur physically, so this orientation is essential.  The
absolute-value estimate is unchanged:

```text
max fixed-s sum |dt/dv| ceiling = 2938,
||J_s||_TV <= 2*(126/5)*200*2938 = 29615040.
```

Possibly empty charts contribute zero and half-open ownership prevents
dyadic double counting.  The closed `v=1` endpoint is explicitly included
in every terminal, physical, graph and nonempty-root sweep.  The registered
candidate-zero graph family has zero intercept and row-law collar
coefficient `122472/5`; this does not cover terminal positive-width boxes.

The full terminal replay makes `1,209,088` strict audit calls and leaves

```text
terminal boxes                              93,080
maximum boxes on one fixed-s slice           1,492
maximum fixed-s unresolved t-width          373/256
row-law positive intercept                23499/640.
```

The width improves by `285/512` from the preceding `1031/512` frontier, but
is not zero.  The exhaustive terminal causes are

| cause | boxes | parameter area | maximum fixed-s width |
|---|---:|---:|---:|
| interval geometry exception | 79,660 | `19915/16384` | `625/512` |
| candidate zero with `partial_t Delta` containing zero | 11,884 | `2971/16384` | `7/32` |
| source incidence `c_p` not strict | 1,536 | `3/128` | `3/128` |

The three per-cause maxima occur on different parameter slices and are not
added.  The first cause is an interval-geometry failure, not certified
physical singularity mass; the second is the genuine critical-root frontier.
Until these boxes and the 368 untyped graphs are resolved, the partial
current cannot be promoted to a complete physical current or to strong-space
operator DQ, branch-record `MT_DQ`, `FACE_2CUT` or `FACE_TIME`.

## 6. Gate 4: global Growth contraction and numerical recovery layer

For every fixed `|s|<=1/400` and every invariant-cone curve of Euclidean
length at most

```text
delta_1=1/37724355673552103994,
```

there is at most one central homogeneity child globally.  With cutoff
`k0=6121`, all high-strip children of all at most 153 true continuity
components have combined tail below `1/5`.  Therefore

```text
Xi_1(delta_1)
 < 144000/180337 + 1/5
 = 900337/901685
 = 1-1348/901685
 < 1.
```

This is a true componentwise weighted join; it does not charge all 448
conservative candidates as physical children and it is not the final CM2
same-occurrence `q`.

The next stack keeps the exact endpoint-cosine factors in the graph transform
and constructs a four-phase invariant standard-curve family.  Globally over
all iterates,

```text
D_std < 30000000.
```

This supplies adapted log-Jacobian distortion on every phase-typed
homogeneous child and an invariant density cone.  At the deterministic
adapted length `delta_*=10^-90`, the density ratio is at most `2000/1999` and
the numerical linear Growth recurrence has

```text
vartheta_p = 360134800/360493663 < 1,
1-vartheta_p = 358863/360493663,
C_p = 141*10^90*360493663/358863,
A0 = 301500,
A1 = 1005.
```

The physical native prefix antichain, used level by level without
leaf-normalising by `p_a^-1`, then gives at `gamma=1/12060` the finite moment

```text
E[2^K exp(gamma(R_fw+R_rev))] <= 2*3^52.
```

This does not erase the earlier obstruction.  The countably infinite native
initial partition still has infinite time-zero boundary `Z` when materialized
as one family; arbitrary repeated cuts can make the numerator diverge, and
the certificate supplies no
same-occurrence strong-operator forward/reverse record ledger or joint
`(B,K)` moment.  Hence `C_fw,C_rev` and final `q` remain unassigned and Gate 4
remains open.

## 7. Latest-technology audit

The official arXiv API and the current `math.DS` feed were checked again on
2026-07-16.  The nearest recent results still miss a required interface:

- `arXiv:2606.10155` explicitly leaves general loss of memory after
  characteristic-function restriction as an open problem;
- `arXiv:2604.19671v2` treats small holes through standard-family geometry
  that does not supply this moving-scatterer branch-record DQ or full-mass
  stopped quotient;
- `arXiv:2605.05968` and `arXiv:2503.16358v2` assume the Young-tower layer
  that Gate 2 is required to construct;
- `arXiv:2604.13401` concerns hyperbolic automorphisms rather than a global
  singular-billiard coding with the present non-fiber-bunched cocycle;
- `arXiv:2607.11421v2` permits atomic physical stationary measures in a
  locally invertible random setting, warning against the desired stationary
  regularity extrapolation rather than providing it;
- the moving-scatterer reference `arXiv:1210.0011v4` still supplies the
  finite-parameter standard-family input used here, but not the missing
  quotient, repeated-indicator or operator-DQ interfaces.

No newly posted theorem was found that honestly bypasses the remaining
global coding, stable quotient/PPE, full current/DQ, same-occurrence recovery
or three-norm/Kac requirements.

## 8. Verification, frozen evidence and next boundary

All six new verifier stacks pass replay and integrity.  The Gate-3 corrected
materialization was run twice from clean state in `13:51.11` and `13:48.97`;
both runs produced internal digest

```text
1faed7744462e3c16e0072d81520d32d9bf34f4c2904e541641cd044f0aa1164.
```

Semantic mutation totals are

```text
Gate 1 immutable homoclinic:       16/16
Gate 1 numerical tail/twisting:    21/21
Gates 2/5 roof-two wall charts:    12/12
Gate 3 physical-first/current:     16/16
Gate 4 componentwise Growth:       15/15
Gate 4 numerical recovery:         17/17
total:                              97/97.
```

Every live verifier rejects the remaining unsupported interface and exits
`2` by design.  The six stack sidecars, the aggregate Gate manifest, the
direct-assault manifest, v52 and the twelfth-round frozen chain all pass
SHA-256 verification.  No CM2 replay process remains after the final audit.

Primary evidence:

- `deliverables/cm2-gate1-immutable-homoclinic-plaque-entry-frontier-assault-2026-07-16.md`;
- `deliverables/cm2-gate1-numeric-holonomy-tail-twisting-frontier-assault-2026-07-16.md`;
- `deliverables/cm2-gate25-roof-two-wall-chart-frontier-assault-2026-07-16.md`;
- `deliverables/cm2-gate3-physical-first-unresolved-frontier-assault-2026-07-16.md`;
- `deliverables/cm2-gate4-componentwise-global-growth-recovery-frontier-assault-2026-07-16.md`;
- `deliverables/cm2-gate4-numeric-invariant-family-growth-recovery-frontier-assault-2026-07-16.md`.

The next exact attack line is:

```text
faithful global QNL coding and uniform class-H holonomies
 -> endpoint-scaled resolution of Gate-3 interval exceptions
 -> critical-root resultants and complete side-owner current
 -> strong component restriction DQ / branch-record MT_DQ / FACE
 -> same-occurrence forward/reverse ledgers and joint recovery moment
 -> stable quotient/PPE
 -> maximal homogeneous 18-field words, three CM2 norms, Kac and phase.
```

Until those interfaces are constructed and replayed, unconditional CM2 is
strictly fail-closed.
