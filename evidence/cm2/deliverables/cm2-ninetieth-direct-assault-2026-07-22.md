# CM2 ninetieth direct assault

Date: 2026-07-22 (Asia/Shanghai)

Round90 eliminates the last eight internal rank-three projective gaps.  The
key technical change is a centered-Taylor interval face test on a tracked root
chain, followed by exact D4 expansion from one representative to the full
eight-row orbit.

The rank-three continuation frontier is now:

```text
945 locally physical ports
56 oriented projective branches
440/440 interior gaps certified
0 unresolved interior gaps
47 source-boundary branch ends capped
65 exterior projective rays still open
```

The remaining 65 rays, rather than any registered-union gap, are now the sole
rank-three continuation blocker.  They must be typed globally before a final
physical-face quotient can be formed.

Other independent core gates are unchanged: later C24 returns through
`SURVIVE`, the Gate3 all-depth Piola/trace/stopped-`MT_DQ` recipient, and the
F14 arbitrary regular-density prefix/suffix intertwiner remain open.

Strict state: Gate1 `NOT_CERTIFIED`; Gate2 `0/17`; Gate3 `NOT_CERTIFIED`;
Gate4 `1/7`; Gate5 `10/18`, complete blocks 0; composites `0/5`; CM2
`NO-GO_FOR_CLAIM`.
