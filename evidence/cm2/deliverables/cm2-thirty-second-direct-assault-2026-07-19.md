# CM2 thirty-second direct assault

Date: 2026-07-19  
Verdict: **actual-instance F7 and parameterized-face F8 closed; Gate 5 reaches 6/18**

## 1. Round result

The round uses the parent-W identity layer from round 31 to unlock two numeric
same-registry joins that were previously only universal templates.

| gate | round-32 advance | exact remaining core gap | status |
|---|---|---|---|
| 1 | no safe promotion | physical Green rate/exact nonlinear cancellation | NOT CERTIFIED |
| 2 | no safe promotion | all-parent physical quotient/PPE | NOT CERTIFIED |
| 3/4 | depth-four ledger retained | uniform blocker contraction and weighted return tail | NOT CERTIFIED |
| 5 | actual-instance numeric F7; connected-rank and numeric F8 | physical-face F9 C2, all-face F10, F11--F18 | NOT CERTIFIED (`6/18`) |

## 2. Numeric F7

The global componentwise theorem gives `Xi<900337/901685` on every short
invariant-cone curve. Joining it to the invariant density ratio `2000/1999`
and the actual `(branch-rule,parent-W,j)` IDs yields

```text
F7_one_step < 360134800/360493663 < 1,
F7_two_step < 129697074171040000/129955681063157569 < 1.
```

This covers every Borel parameter over all 228,012 Q2 branch-rule bases. The
old 285/289 raw physical-root upper is deliberately not used as a cut count.

## 3. Connected faces and F8

Every parent-W has slope 4. Every active nonvertical physical boundary branch
has slope at most `-25/9`; vertical and horizontal faces are explicit. Thus
each parent-W/carrier-family pair has zero or one root, every nonempty regular
piece has connected rank zero, and the normalized wedge is uniformly greater
than `1/5`. Two one-sided traces are attached; corners and simultaneous roots
remain cemetery events.

This applies to all five physical face grammars and materializes parameterized
numeric F8 slots.

## 4. Exact stopping boundary

The existing `4949` constant controls standard *carrier* C2, not the physical
face C2 atlas required by F9. The corrected `18/5` coarea density and the
`27*2^B`, `52*2^B` log-density costs are attached to the 64 moving occurrence
types, but no common numeric parameter-DQ/coarea bound exists yet for every
regular core-preimage and arbitrary-time face. They cannot be retyped as F9
or complete F10.

Therefore Gate-5 maturity advances exactly from `4/18` through `5/18` to
`6/18`, while complete 18-field blocks remain zero and CM2 remains no-go.

## 5. Next route

1. Derive uniform C2 bounds for all five physical level-set grammars after
   pullback on the `(s,b,k,j)` atlas, separated from carrier curvature.
2. Bound `partial_s F` and its carrier derivative for stationary-core,
   intermediate-core, terminal-core and owner-change faces; combine with the
   new `1/5` transversality to fill F10.
3. Join the endpoint shell rank `B` to the same face IDs so the existing
   `27*2^B` and `52*2^B` series can be summed without losing identity.
4. Only then attempt F11--F18 and the induced strong coefficient.
