# CM2 Round 58 full direct assault

Date: 2026-07-20  
Strict verdict: **0/5 complete composite gates; Gate 5 maturity 10/18 with
zero complete 18-field blocks; CM2 NO-GO_FOR_CLAIM**

## Round result

Round 58 solves the exact same-time representation problem for the Round-57
physical first-return landing, but the answer is an iff rather than a gate
closure.  Within each frozen physical landing registry, maximal connected
positive components attain the smallest boundary functional possible under
every exact positive representation.  The resulting physical
`J_land,min,total` and full dyadic `D_land` moment are finite.  What remains is
the genuinely dynamical strict threshold

```text
J_land,min(y) < C_p h(y)
```

on almost every positive physical landing fibre, or a new cross-fibre
physical Rokhlin redistribution interface.  No further cut, merge or
countable family split inside the same frozen ID and chart can bypass this
threshold.

The four append-only leaves are:

1. the Gate-4 unshifted landing minimal-`Z`, exact properness iff,
   `D_land` and reinduction frontier;
2. the Gate-5 physical owner-ledger, `K/H/J` join and positive-transport
   frontier;
3. the Gate-1/2/3 weighted-projective Dini, projected-shadow, conditional
   landing join and declared-budget CAD frontier;
4. an independent mathematical, type and executable audit of all three
   main leaves.

All physical dynamical claims remain parameterwise for every fixed
`|s|<=1/400`.  No older artifact was edited.

## Gate 4: maximal physical components give the exact minimum

Fix one immutable

```text
(y, physical landing carrier chart, physical restriction ID).
```

After removing the frozen singular, null and cemetery punctures, let `C`
range over the maximal connected positive components of the actual landing
support and define

```text
J_land,min(y) = sum_C kappa_y(C)/ell(C).
```

Every admissible positive standard-pair carrier lies in one such `C` and has
length at most `ell(C)`.  Hence every exact representation pays at least
`kappa_y(C)/ell(C)` on that component.  Restricting the inherited regular
density once to each maximal component attains equality.  Therefore

```text
inf_(exact positive same-coordinate representations) Z
  = J_land,min(y).
```

The quantity is Borel: Round 57 supplies least-rational component owners,
Borel endpoint infima/suprema, Borel masses and lengths, followed by a
monotone countable sum.  Empty fibres use
`h=J_land,min=z_land=0` and are never normalized.

Positive cuts can only shorten carriers; all actual-union coarsenings have
already been taken; true gaps and punctures cannot be crossed; and forgetting
an immutable physical ID changes the kernel contract.  Countably many
positive subfamilies do not help because their aggregate normalized charge
is the outer mass-weighted average.  Thus the exact result is

```text
unshifted same-ID landing proper
iff J_land,min(y) < C_p h(y)
    for almost every positive fibre y.
```

This exhausts same-time positive representation freedom only inside the
fixed `(y, chart, ID)`.  It deliberately does not exclude a future cross-`y`
or cross-chart physical Rokhlin redisintegration built from a genuine
Gate-2 stable-holonomy quotient.

## Gate 4: a new finite physical moment, but not properness

Round 57's global affine landing representation and time reversal imply

```text
J_land,min,total <= Z_3 < infinity.
```

With `z_land=J_land,min/h`, set

```text
D_land=0                                      if z_land<C_p,
D_land=min{d>=1: 2^-d z_land<C_p/2}           otherwise.
```

Minimality gives, on the defect stratum,

```text
2^D_land <= 4 z_land/C_p,

integral h(y) 2^D_land(y) dlambda(y)
 <= H+(4/C_p)J_land,min,total
 < infinity.
```

The corresponding bad-fibre tail is

```text
integral_{z_land>=2^k C_p} h dlambda
 <= J_land,min,total/(2^k C_p) -> 0.
```

The `k=0` mass is not proved zero.  `D_land` is physical unshifted landing
geometry, whereas the pinned `D_cap` is stopped proper-reference geometry.
Appending a positive `696 D_land` clock to time `n` would no longer be the
original first return.

An exact finite separator shows the distinction is real.  On an identity
first-return interval, retain `N=floor(C_p)+1` positive components of total
mass `999/1000`, separated by positive gaps of total mass `1/1000`.  The full
ambient induced law is proper, and a stopped reference reassembler has
`J_cap=2,D_cap=0`, but the physical unshifted landing has

```text
J_land,min=N,
J_land,min/(999/1000)>C_p.
```

Thus ambient properness, high common mass, finite landing `Z`, finite
`J_cap` and even `D_cap=0` do not imply physical time-`n` landing
properness.

Rebasing on the physical landing and extracting all disjoint next first-hit
cylinders does give an exact once-covering next-return graph with finite
aggregate landing `Z`.  From the original source it is the second return,
and its new source and landing may still be unproper.  The identity separator
shows that no finite number of such reinductions forces properness.

## Gate 5: the actual owner ledger is now well typed

On the actual finite Borel owner/root law `nu`, Round 50/54 provide Borel
`A_col`, clearance level `K` and collar length.  Round 56 makes `r_K` a Borel
integer-valued function.  Consequently the physical extended ledger

```text
m_k = nu{a in A_col: K(a)=k},
M_clock = sum_k w_Z^r_k m_k in [0,infinity]
```

exists on the real owner law, with the exact Abel identity

```text
M_clock
 = w_Z^r_0 nu(A_col)
   +sum_{j>=0}(w_Z^r_(j+1)-w_Z^r_j)nu{K>j}.
```

Every finite truncation is finite, but the limit and full `A_col` coverage
remain unproved and logically independent.

The former `D/A/g` shortcut is not physical yet.  Round 53 has one root atom
per parent, integrated over the outer parent law; this is not an
along-collar density.  Spreading that atom uniformly over the artificial
Round-54 collar produces density `ell^-1`, whose aggregate is exactly the
missing raw collar debt

```text
Z_col = sum_k 2^(k+1)m_k.
```

Treating it as a free density would be circular.  A legal route needs an
actual coarsened conditional kernel and a finite joint
`D_b A_b g_b^(-eta/q)` ledger.

The minimal positive suffix carrier must also include

```text
H(a) = number of available compatible 9148-collision blocks,
J(a) = exact Round54/Round42 operator-and-domain alignment bit.
```

The exact canonical policy criterion is

```text
Q_policy
 = integral 1_{J=1,H>=r_K} C_rec w_Z^r_K dnu
   +integral 1_{J=0 or H<r_K} 2^(K+1)dnu
 < infinity.
```

A same-`K`-marginal two-completion example makes the aligned branch finite
and the short/misaligned raw debt divergent.  Hence the `H/J` joint law is a
real missing field, not a proof convenience.

For any nonnegative charge `a` and coupling `pi`, positive transport obeys

```text
integral [a(y+)+a(y-)] dpi
 = integral a dmu+ + integral a dmu-.
```

It is coupling independent.  The certified separator has identical Jordan
marginals and therefore signed `OT=0`, while both positive optimal-clearance
moments and the weighted positive cemetery charge diverge.  Signed
cancellation may still serve the signed BL/F13 route, but it cannot pay
positive F10 or strong cemetery debt without an already finite positive
anchor moment.

Gate 5 therefore remains `10/18`, with zero complete 18-field blocks.

## Gates 1, 2 and 3: exact conditional interfaces

Gate 1 gains a uniform weighted-projective Dini/Cauchy theorem.  If both
critical coordinates share

```text
sup |C_(n+1)^sigma-C_n^sigma| <= eta_n d^beta,
sum_n eta_n < infinity,
```

then they converge uniformly with the explicit Hölder tail
`d^beta sum_{n>=N}eta_n`.  Vanishing increments alone are insufficient.  In
the identical physical QNL fibre and normalized eigenbasis, max-entry loop
error at most `10^-29` preserves all four frozen nonzero twisting wedges.
The physical Dini rows and same-gauge loop enclosure are still absent.

Gate 2 identifies the correct infinite-future input.  For projected bad
shadows `B_j` on a stable-fibre base interval `I`, Borel rows satisfying

```text
Leb(B_j)<=b_j,
sum_j b_j<|I|
```

together with common graph-transform cone, contraction and `C^1`
compactness leave a positive all-depth stable base.  A zero-area transverse
line may project to all of `I`, so collision-area nullity is not a substitute.
The official physical projected-shadow rows remain zero and Gate 2 remains
`0/17`.

Pushing the Gate-4 landing to a long stable quotient gives only a latent
reference lift.  Same-time redistribution on the identical physical graph
is conditional on seven new physical fields: product rectangles; Borel
holonomy/Jacobian bounds; full-span or bounded fragmentation; same-measure
unstable conditional density/distortion; boundary charge below `C_p`; a
branch inverse preserving endpoint/ID/path data; and strong restriction and
assembly.  None is promoted in this round.

Gate 3 gains the computable recurrence

```text
S_(r+1)=2(S_r D_r+1)^2,
D_(r+1)=2D_r^2,
L_r=2S_rD_r+1
```

for formulas separately proved to fit the declared finite-depth encoding
budget.  The physical circular-pilot formula has not been enumerated into
that budget, so no unconditional physical component bound is claimed.  An
exponential `Dbar` moment does not integrate an arbitrary superexponential
CAD majorant, and determinant one does not control directional Piola.  The
strong physical `R_s/Q_s`, Piola suffix bound and `MT_DQ` remain absent.

## Latest-technology audit

Official arXiv metadata and the relevant current sources were rechecked on
2026-07-20, including Demers--Liverani `2606.10155v1`, the sequential
projective-cone paper `2104.06947v3`, Balint--Komalovics `2501.16102v2`, the
latest returned billiard/holonomy and cocycle records, and signed transport
record `2606.19621v2` updated 2026-07-17.  They provide useful surrounding
Growth, holonomy, transfer-operator, tower and signed-transport technology,
but no theorem was found that directly supplies the CM2-specific same-time
physical landing threshold, stable product registry, physical Dini rows,
strong restriction/Piola join, `H/J` operator law or positive cemetery
moment.  No external theorem was promoted into a certificate dependency.

## Independent audit and full validation

The independent audit pins all `15/15` frozen main-leaf artifacts, parses all
`12/12` leaf SHA rows and independently replays the Gate-4 minimal-`Z`
theorem, Borel/zero-fibre policy, `D_land` typing and separator; the Gate-5
Abel identity, density typing, `K/H/J` separator and positive-marginal
transport identity; and the Gate-1/2/3 conditional scope boundaries.  It
reports `PASS`, with no claimed-scope blocker or cross-leaf status collision.

The final four-leaf validation matrix is:

```text
syntax:                    8/8 PASS
dependency/artifact pins: 39/39 PASS
deterministic replay:       4/4 PASS
byte-identical re-emission: 4/4 PASS
hostile mutations:          342/342 rejected
leaf SHA entries:           16/16 PASS
default cert/verifier:       8/8 exit exactly 2
Round-58 temporary/stale artifacts: none
```

The Gate-4, Gate-1/2/3 and independent-audit ledgers use workspace-relative
`deliverables/...` paths; the Gate-5 ledger uses deliverables-local basenames.
Each was checked from its declared path scope.

## Strict status after Round 58

```text
Gate 1: NOT_CERTIFIED
Gate 2: NOT_CERTIFIED (0/17 physical fields)
Gate 3: NOT_CERTIFIED
Gate 4: NOT_CERTIFIED
Gate 5: NOT_CERTIFIED (10/18; complete blocks 0)
complete composite gates: 0/5
CM2: NO-GO_FOR_CLAIM
```

The next shortest primary route is now exact and nonrepresentational:

1. prove `J_land,min(y)<C_p h(y)` almost everywhere on the identical physical
   landing IDs, preferably with a uniform margin; or build and verify all
   seven fields of the cross-fibre physical Rokhlin redistribution;
2. once a proper physical source/landing first-return kernel exists, join
   killed intermediate `C24` avoidance, all later/repeated clocks, physical
   `q` in `L^(6/5)` and the strong cemetery;
3. in parallel, estimate the actual Gate-5 Abel tail and coverage, construct
   the physical `H/J` same-operator joint law, and prove the two positive
   marginal moments;
4. materialize the Gate-1 Dini/same-gauge loop rows, Gate-2 projected shadows
   and stable quotient, and Gate-3 physical formula budget plus
   depth-integrated strong `R_s/Q_s`, Piola and `MT_DQ`.

## Frozen Round-58 leaves

- `cm2-gate4-round58-unshifted-landing-minimal-z-first-hit-frontier-assault-2026-07-20.md`
- `cm2-gate5-round58-owner-ledger-positive-transport-frontier-assault-2026-07-20.md`
- `cm2-gate123-round58-dini-shadow-cad-landing-join-frontier-assault-2026-07-20.md`
- `cm2-round58-independent-core-frontier-audit-2026-07-20.md`

Their recursive SHA ledgers are pinned by
`cm2-fifty-eighth-direct-assault-manifest-2026-07-20.sha256`.
