# CM2 Gate 3/4 direct assault, round 29: all-Q2 time-three anchor registry

Date: 2026-07-18  
Status: **finite time-three registry certified; Gate 3/4/5 and CM2 remain open**

## 1. Result

This append-only leaf performs a fresh 384-bit Arb replay from every one of the
`114,006` certified finite-open `Q2` anchors.  It reconstructs the physical
second outgoing state, searches the complete translated eight-chart candidate
table for the third collision, reconstructs the ordered clean-wall word, and
tests the third collision against the complete 24-core union.

The exact root-box ledger is:

| time-three class | boxes | parameter-averaged coordinate-base mass |
|---|---:|---:|
| strict `R3-inner` | `0` | `0` |
| strict `Q3-inner` | `4,088` | `20441/5120000000` |
| unresolved finite outer | `109,918` | `2618679/2560000000` |
| total certified `Q2` anchors | `114,006` | `5257799/5120000000` |

The three masses add exactly.  Each admitted `Q3` box strictly avoids `C24` at
collision times 1, 2 and 3, has a unique depth-three frozen word-prefix owner,
and is a positive finite-open rectangle on every parameter slice strictly
inside its open `s` interval.  There are `12` distinct admitted depth-three
word prefixes.

The finite count `R3 admitted = 0` has a deliberately narrow meaning: no
unrefined certified `Q2` root box is wholly contained in `R3`.  It does **not**
say that physical `R3` is empty; limiting physical `R3` emptiness and
nonemptiness are both undetermined by this finite ledger.  The round-27 arbitrary-`n` measurable
`R_n/Q_n` and component-existence schema remains the limiting statement.

## 2. Why the finite outer was not recursively split here

The first strict blocker histogram is:

- `104,638`: a third-flight competitor discriminant is not sign-separated on
  the entire root box;
- `5,280`: the inherited time-two outgoing normal chart is not whole-box
  strict (the same representation-seam subledger retained in round 28).

A deterministic probe before freezing compared additional binary depths 2,
4, 6 and 8.  Depth 2 resolved essentially none of the discriminant outer;
depths 4--8 expanded the probe to about 15--93 terminal leaves per Q2 anchor.
Therefore this leaf chooses complete coverage of all 114,006 anchors at root
depth rather than an unaudited partial deep tree.  Every unresolved box and
its exact mass remains explicit.  No observed finite ratio is promoted to a
uniform termination rate, singularity-growth estimate, or return tail.

## 3. Frozen branch payload on the 4,088 admitted Q3 cells

All admitted cells are also whole-box strict in the enlarged central
homogeneity strip `H0(k0=6121)`.  Per cell the registry freezes:

- the inherited two-step homogeneity and branch-rule IDs;
- a third physical homogeneity-child ID;
- a third canonical recut **branch-rule** ID;
- one common forward/reverse smooth Borel restriction ID;
- exact rational coordinate-base mass and a separately typed symbolic
  collision-area mass;
- three-collision invariant area Jacobian `1`, explicitly not identified with
  the one-dimensional unstable Jacobian;
- a universal branch-rule F5 slot with
  `theta^3 = 2985984000000000/5864817765532753`;
- a universal branch-rule F6 slot with log-variation `< 9/200000`;
- the symbolic once-charged frontier
  `(1+C_fw)(1+C_rev)(144000/180337)^3`.

These are `4,088` universal branch-rule slots, not actual standard-curve
instances.  Actual recut-instance count remains `0`; numerical `C_fw`,
`C_rev`, strong `q3`, charged F7 characteristic-`Z`, weighted tail and induced
strong Lasota--Yorke coefficient all remain absent.

## 4. Strong typing and nonpromotion

The following distinctions are enforced in both producer and verifier:

1. finite-open registry components are not maximal physical path-fibre
   components;
2. an admitted whole box is physical, but the unresolved finite outer is not
   declared null;
3. a branch-rule ID is not an actual curve-recut instance ID;
4. coordinate-base mass, collision-area mass, invariant area Jacobian and
   unstable Jacobian are different objects;
5. a common forward/reverse carrier ID does not supply numerical strong norms;
6. the empirical one-step admitted fraction is not an iterable or weighted
   tail estimate.

Consequently the strict verdict remains:

- complete limiting `R3/Q3` component enumeration: `NOT_CERTIFIED`;
- uniform adaptive time-three termination/singularity growth:
  `NOT_CERTIFIED`;
- survivor-conditioned recovery: `NOT_CERTIFIED`;
- strong-`q` excursion/cemetery tail: `NOT_CERTIFIED`;
- induced strong Lasota--Yorke: `NOT_CERTIFIED`;
- Gate 3, Gate 4, Gate 5: `NOT_CERTIFIED`;
- CM2: `NO-GO_FOR_CLAIM`.

## 5. Independent executable checks

Artifacts:

- producer: `cm2_gate34_round29_q2_time3_anchor_registry_cert.py`;
- fail-closed verifier:
  `cm2_gate34_round29_q2_time3_anchor_registry_verifier.py`;
- JSON manifest:
  `cm2-gate34-round29-q2-time3-anchor-registry-manifest-2026-07-18.json`;
- SHA leaf:
  `cm2-gate34-round29-q2-time3-anchor-registry-manifest-2026-07-18.sha256`.

Checks:

```text
integrity                         PASS
fresh 416,994-leaf time2 replay  PASS
fresh 114,006-anchor time3 replay PASS
verdict replay                   PASS
hostile tests                    101/101
live mode                        exit 2 (fail closed)
```

The verifier rebuilds the complete time-two terminal ledger before it accepts
the 114,006 anchors; it does not trust only the frozen counts or digests.

## 6. Shortest next route

1. split the `5,280` representation-seam boxes in a chart-independent physical
   normal coordinate;
2. use targeted dependency-neutral recuts for the `104,638` discriminant
   outers and prove a genuine time-three boundary-tube/termination estimate;
3. instantiate actual parent-`W` recut pieces and attach charged F7 boundaries;
4. obtain numerical same-ID `C_fw/C_rev/q3` and survivor-conditioned recovery;
5. combine that payload with the already certified conditional weighted-tail
   transfer before deriving the induced strong Lasota--Yorke inequality.
