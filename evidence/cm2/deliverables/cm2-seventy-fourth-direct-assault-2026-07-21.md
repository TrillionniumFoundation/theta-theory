# CM2 Seventy-Fourth Direct Assault — 2026-07-21

Round 74 constructs the first actual fixed-`s=0` depth-two/cross-rank layer.

- The legacy three-dimensional Q2 registry is proved incomplete on the fixed
  slice: `804` boxes touch `s=0`, but only `32` contain it in their open
  `s` interval; `772` endpoint-only boxes are correctly rejected.
- A fresh 24-core, two-dimensional, depth-16 Arb tree produces `153,508`
  terminal leaves: `9,112` R1, `2,432` R2, `76,708` Q2, and `65,256` outer.
- The exact normalized area ledger totals `24`; strict R2 area is at least
  `19/512`, while outer area `8157/8192` is retained without null promotion.
- `16` positive R2 boxes certify 8 main plus 8 narrow-corner
  source/destination pairs on 8 W-diagonal source cores.
- `88,252` strict child cells receive same-core Round-73 parent incidence.
- All `153,484` artificial binary split faces cancel with opposite
  orientations across the rank join; physical R2 pullback cancellation is
  not promoted because the complete physical face atlas remains inside the
  retained outer.
- Latest search found arXiv:2602.07718's generalized interval Krawczyk
  surface certification as a plausible next algorithmic tool, not a direct
  billiard theorem.
- Gate 4 remains `1/7`, Gate 5 remains `10/18` with blocks `0`; composite
  gates remain `0/5`; CM2 remains `NO-GO_FOR_CLAIM`.

Primary report:
`deliverables/cm2-round74-s0-depth2-cross-rank-assault-2026-07-21.md`.
