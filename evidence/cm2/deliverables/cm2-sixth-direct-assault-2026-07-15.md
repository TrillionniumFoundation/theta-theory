# CM2 sixth direct assault: complete active event stratification

Date: 2026-07-15 (Asia/Shanghai)  
Execution: direct interactive work only; no CM2 cron or detached scheduler  
Frozen inputs: v51/v52 and their manifests were read but not edited

## Verdict

**Unconditional CM2 remains `NO-GO FOR CLAIM`.**  Under the strict composite
typing used in v52, the five main gates remain **0/5 fully closed**.  The
major advance is inside Gate 3: every active source-grazing, polarity,
first-visibility and miss-owner collar now has an exact analytic
stratification, and the remaining two-dimensional unresolved parameter area
is zero.  The 48 genuine transition curves are retained as DQ faces.

| gate | strongest certified layer after this pass | strict remaining obstruction | verdict |
|---|---|---|---:|
| 1. common magnet / twisting | actual common vertex, four-face full-cross, finite 96-collision shadow and four wedges | natural QNL canonical stable holonomy diverges; no applicable alternative non-FB transport theorem | **OPEN** |
| 2. physical SRB/PPE | two local physical crossings, stopped-energy theorem, exact mass/scale deficit | stable saturation, full-mass quotient, actual tail PPE, endpoints and amplitude registry | **OPEN** |
| 3. global event/DQ | complete active analytic label stratification, zero 2D unresolved area, unique chart-seam owner | maximal connected rows, global coarea/current assembly and arbitrary-test DQ | **OPEN** |
| 4. same-occurrence `q` | all-sheet two-view/common-`m`/single-charge schema; complete active event cuts now available | numeric forward/reverse costs and stopped-parent recovery under one frozen rowwise `q` | **OPEN** |
| 5. full-boundary transfer | finite Kac algebra, bounded-Borel typing, phase gcd one, exact seam ownership | global typed singular currents, physical constants and all CM2 norm lifts | **OPEN** |

## 1. Gate 1: no honest bypass found

The fifth-pass resonance result remains decisive for the frozen natural
codings:

\[
 \lim_{n\to\infty}(H_n^{-1}H_{n+1})_{21}
 =\frac{325}{144}c(z)^2>0.
\]

Thus the faithful canonical stable holonomy does not converge.  The finite
96-collision shadow and its four nonzero wedges remain valid but are not a
QNL canonical holonomy loop.  A current arXiv audit of fiber-bunched and
non-fiber-bunched cocycle results found no theorem that accepts this singular
billiard derivative, bypasses the failed canonical limit and promotes the
finite shadow to the required projective spectral statement.  Gate 1 does
not move in this pass.

## 2. Gate 2: the physical deficit remains structural

The actual 96-word stable leaf and two local crossings remain certified, but
the stable endpoint/tube scale ratio is still greater than `8.29e65`.
Current raw branches cover less than `9.7%` of the common collision-SRB
rectangle, leaving more than `90.3%` unregistered.  No recent standard-family
or small-hole result supplies the missing interval-indexed stable saturation,
record-compatible full-mass quotient, actual reverse weights, stopped tail
PPE and normalized amplitude ledger.  Gate 2 therefore remains unchanged.

## 3. Gate 3: every active collar is now stratified

### 3.1 Source-grazing and polarity

For all 64 active source-target pairs, exact circle separation gives

\[
 |\partial_\theta F|^2\ge\frac{36337}{160000},
 \qquad
 |\partial_zF|^2\ge\frac{36337}{320000}
\]

on `cp=0`.  All 23,376 source-grazing endpoint boxes split into 18,980
uniformly occluded rows and 4,396 outgoing physical half-rows carrying 32
labels.  The `cp=0` boundary is regular, has two-dimensional measure zero and
has zero signed parameter-coarea coefficient.

All 816 original polarity boxes, plus 768 generated by deeper refinement,
split exactly by `u_y<0`, `u_y=0`, `u_y>0`.  First visibility and miss owner
are strict on each parent box; `u_y` is analytic and non-identically zero, so
its zero set has two-dimensional measure zero.

### 3.2 First visibility and miss owner

The complete owner registry has 82,048 target-target descriptors.  The
independent resolver leaves no physical boundary among the 320 former
unresolved descriptors and no physical joint vertex.  Exactly 48 strict
physical curves meet the active cross-colour sheets:

```text
16 earlier first-visibility curves,
32 later miss-switch curves.
```

Depth-thirteen refinement leaves 72,324 ambient residual boxes.  Exact curve
matching gives 59,028 boxes with no physical transition curve, 13,296 boxes
with exactly one, and zero with two or more.  The latter merge into

```text
48 curves x 256 full-window strips = 12,288 strict curve strips.
```

Every boundary-free box has one strict typed state.  Every curve strip
contains its analytic curve strictly and has the correct distinct side
states.  Thus

```text
remaining two-dimensional unresolved parameter area = 0.
```

This is a stratification theorem, not cancellation: all 48 curves remain in
the future DQ ledger.

### 3.3 Chart seams

The exact half-open dominant-chart rule assigns every diagonal tie to `E` or
`W`.  Duplicate traces are identified, not summed.  In the rectangular bulk,
344 seam-touching boxes produce 16 seam-label keys; 96 overlap pairs reduce
the prequotient component count from 64 to 52.  The same ownership rule
applies to every analytic stratum.

## 4. Gates 4–5: downstream frontier moves, gates stay open

The prior all-sheet theorem already gives every completed occurrence two
nonadditive views, one positive coarea law `m` and one pre-recovery charge

\[
 q=\max\{C^{\rm fw},C^{\rm rev},2\}m.
\]

The new active stratification removes the unresolved-collar ambiguity from
the row-cutting stage and supplies a unique chart owner.  It does not provide
the physical numeric costs `C_fw,C_rev`, maximal-row aggregation, recovery
moments or global current norms.  Therefore the 48 analytic transition faces
cannot yet be promoted to a complete arbitrary-test DQ, and the four typed
Kac currents and CM2 norm intertwiners remain absent.

## 5. Shortest remaining route

1. Glue the rectangular and analytic half-rows under the exact seam owner and
   emit the maximal connected event-row registry.
2. Evaluate the physical coarea coefficient, hit/miss trace and signed current
   on every maximal row, including all 48 transition faces.
3. Compute rowwise `C_fw,C_rev`, freeze one `q` per occurrence and prove the
   aggregate or hereditary stopped-parent recovery moment.
4. Assemble arbitrary-test DQ and the four differentiated Kac currents, then
   export distortion, cut, face and test constants through the three CM2 norm
   lifts.
5. Independently complete Gate 2's stable saturation/full-mass quotient and
   Gate 1's genuinely alternative transport theorem; neither follows from the
   Gate 3 advance.

## 6. Evidence

- `cm2-gate3-complete-active-stratification-assault-2026-07-15.md`;
- `cm2_gate3_stratified_collar_closure_cert.py` and manifest;
- `cm2_gate3_first_miss_stratification_cert.py` and manifest;
- `cm2_gate3_chart_seam_quotient_cert.py` and manifest.

All positive certificates exit `0`.  Integrity and mutation self-tests pass.
The live completion modes deliberately exit `2` while maximal rows/global DQ
remain open.  The consolidated SHA manifest and frozen v52 manifest are
revalidated separately.
