# CM2 Round 80 rank-three symmetry quotient and depth-two cell-ledger assault

Date: 2026-07-21

## 1. Strict outcome

Round 80 materially reduces and partially continues the fixed-`s=0` rank-three physical carrier frontier, and it materializes all 164 depth-two cell-label slots plus all 92 two-sided tangency incidences. It does not finish the rank-three face atlas, the exact Q2 cyclic boundary words, the Gate-4 commuting square, or all-rank RN summability.

Strict global state remains Gate 4 `1/7`, Gate 5 `10/18` with complete blocks `0`, complete composite gates `0/5`, and CM2 `NO-GO_FOR_CLAIM`.

## 2. Rank-three connected components

The Round-79 seed incidences live on an exact `64 x 64` grid in each physical core: the two suffix bits after `g32:i:j` are one `t` split and one `p` split.

- source/carrier families: `412`;
- all connected seed components: `1,544`;
- physical third-collision tangency components: `1,536`;
- typed non-face outgoing-chart/geometry components: `8`.

The 408 raw physical source/candidate families therefore contain many disconnected tube components and cannot be treated as 408 curves.

## 3. Exact stratified symmetry quotient

The 24-core registry is not one global D4 set. The 16 cross-obstacle cores are D4-closed, while the eight radial same-obstacle cores contain only the positive-`t` sector and are closed only under diagonal reflection. Round 80 preserves this distinction rather than inventing absent negative-`t` radial cores.

- cross-obstacle D4 orbits: `72`, each of size `8`;
- radial diagonal-C2 orbits: `480`, each of size `2`;
- physical components before quotient: `1,536`;
- exact representative orbits after quotient: `552`;
- exact component-action rows: `6,528`;
- every transformed grid-cell set matches its image component exactly.

## 4. Actual second-owner geometry

All `91,052` physical carrier seed incidences are replayed through the actual time-three geometry.

- distinct second-owner/outgoing-chart branches: `14`;
- mixed-branch grid cells: `0`;
- mixed-branch connected components: `0`;
- branch-homogeneous physical components: `1,536/1,536`.

Thus every representative continuation uses one actual Q2 branch rather than a merged symbolic candidate family.

## 5. Third-collision physical curve continuation

A 384-bit third-collision discriminant Jet is built through the first and second physical collisions. Symmetry representatives are continued as implicit graphs with strict root bracketing and nonzero derivatives.

- representative components tested: `552`;
- complete graph curves certified: `179` representatives;
- symmetry-expanded complete graph curves: `532`;
- strict graph slabs: `2,686`;
- initially difficult representatives: `373`.

The difficult tubes are then decomposed adaptively into strict same-sign open leaves and unique-root slabs.

- adaptive interval box tests: `570,664`;
- additional unique root slabs: `16,996`;
- additional root-bearing representatives: `251`;
- symmetry-expanded additional root-bearing components: `508`;
- representatives proved entirely false tubes: `99`;
- symmetry-expanded false-tube components: `336`;
- representatives retaining unresolved leaves: `274`;
- symmetry-expanded components retaining unresolved leaves: `668`;
- unresolved depth-eight leaves: `85,078`.

A parameterized interval-Newton inclusion was also tested on the residual leaves. It gives no additional promotion, showing that the remaining obstruction is derivative dependency/near-degeneracy in the three-collision expression, not merely an uncentered root interval.

Strictly, Round 80 certifies `532` complete physical graph curves and physical root slabs in another `508` symmetry-expanded components. It does not rename root-bearing partial components as complete faces.

## 6. Complete depth-two cell-label ledger

The complete depth-two quotient has `164` open-cell slots. Round 80 materializes every slot and its rank label.

- R1 cells: `16`;
- R2 cells: `16`;
- Q2 cells: `132`;
- total: `164`.

All 92 time-two tangency curves receive strict two-sided samples:

- two-sided strict Q2 incidences: `92/92`;
- same selected owner on both sides: `88`;
- owner-switching tangency faces: `4`.

This proves that the tangency crosscuts subdivide the Q2 sector and do not create hidden R2 cells. The remaining Gate-4 item is exact: the 132 Q2 slots still need cyclic boundary words and explicit cell-map rows. Therefore the commuting square is not promoted.

## 7. RN frontier

The `64` certified rank-1/2 return-face RN rows remain valid. The rank-three tangency curves are subdivision/homogeneity faces and are not renamed return faces. The finite depth-six registry still has strict `R3=0`, which is not promoted to limiting emptiness.

- certified rank-1/2 return-face RN rows: `64`;
- certified rank-3 return-face RN rows: `0`;
- uniform rank-tail contraction: not certified;
- all-rank RN summability: not certified.

## 8. Audit

- all Round-80 Python sources compile;
- the integrated manifest pins every primary input;
- independent verifier rejects `14/14` hostile semantic mutations;
- strict JSON verifier rejects `4/4` duplicate/non-finite cases;
- all generators, including the heavy adaptive resolver, are cold-replayed byte-identically before sealing.

## 9. Shortest next route

1. replace the residual three-collision expression by centered Taylor-model or slope-matrix Krawczyk forms to reduce derivative dependency;
2. resolve the remaining 668 symmetry-expanded components and connect local root slabs into endpoint/intersection-certified faces;
3. recover exact cyclic boundary words for the 132 Q2 cells from ordered stationary/R1/R2/tangency edge endpoints;
4. materialize the per-cell map rows and test the Gate-4 commuting square;
5. only after actual rank-three return faces exist, attach F8/F9/F10/F13/F16 and branch-RN rows and test a genuine rank-transition tail operator.
