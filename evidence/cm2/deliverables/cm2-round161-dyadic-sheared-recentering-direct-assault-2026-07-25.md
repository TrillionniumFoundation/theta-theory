# CM2 Round161 — direct assault on the second sheared scale jump

Date: 2026-07-25

## Objective

Round160 reached `u=abs(delta_x)/h=100000` with one correlated sheared
macro slab.  Round161 tests whether an exact rational slope recenter can
advance that same connected typed sheet by many additional orders of
magnitude without weakening the complete R1648 or radius-four audits.

The selected stable endpoint is

```text
u_0 = 100000
u_1 = 2500000000000000
width = 2499999999900000h
equivalent exact 4h slabs = 624999999975000.
```

The recentered rational shear is

```text
s = 1403486916994043/500000000000000
  = 2.806973833988086
w = delta_beta/h - s u.
```

Relative to the Round160 shear `2807/1000`, the exact coordinate change on
the inherited `u=100000` face is

```text
w_new = w_old + 13083005957/5000000000.
```

## Why the endpoint is 2.5e15 rather than 3e15

Frontier probes can be made to pass near `3e15`, but the D3 parametric Newton
image then has only about `9h` of lower-face margin.  The selected `2.5e15`
endpoint retains about `73h` in the corresponding diagnostic.  No physical
event was detected between those endpoints; this is a deliberate numerical
robustness reserve.

## Two rejected proof-box designs

The first candidate chain was

```text
C24    [-430,-405]
bridge [-405,-402]
D3     [-402,134111000000].
```

All frontier, graph-derivative, interval-Newton and path-propagation probes
passed, but the full C24 retained-owner audit stopped at collision three.
The generic axis-box owner routine saw the anchor `W[0,0]` discriminant in a
zero-straddling display outer.  Raising Arb precision from 8192 through
16384 bits did not resolve it.  The failure was dependency loss: a direct
8192-bit sheared scalar-affine D3 calculation proved that same discriminant
strictly negative throughout the correlated C24 macro box.

The second attempt added that valid correlated anchor proof.  It allowed the
C24 audit to inspect the other 160 full-radius-four candidates, but exposed a
different mismatch: the three-h-unit bridge was so narrow that the generic
terminal-core classifier returned a strict return cell directly, whereas the
pinned bridge engine expects an unresolved terminal collar and then uses a
correlated monotonicity proof to force `RETURN_AT_3_INNER`.

Neither failure is a typed owner, wall, chart, core or terminal event.  Both
are rejected certificate-box designs.

## Final typed chain

The final common-u slab uses

```text
C24_EVENT             w in [-430,-405]
STRICT_RETURN_BRIDGE  w in [-405,-350]
D3_EVENT              w in [-350,134111000000].
```

The widened 55h bridge simultaneously satisfies:

- collision-three D3 is strictly negative on its upper face;
- the terminal event is strictly positive on its lower face;
- the full terminal interval is the one intended unresolved core collar;
- correlated monotonicity forces the entire bridge to
  `RETURN_AT_3_INNER`;
- the D3 parametric Newton image remains strictly inside its event box.

The two internal faces are exact at `w=-405` and `w=-350`.

## Collision-three correlated anchor adapter

The C24 full audit counts the anchor as one of all 161 radius-four candidates
at collision three, but decides it with the stronger correlated proof:

1. propagate the depth-two sheared scalar-affine D3 model at 8192 bits;
2. prove `partial D3/partial w > 0` on the whole C24 macro box;
3. prove D3 strictly negative at the upper C24 face `w=-405`;
4. conclude D3 is strictly negative throughout the C24 box;
5. audit the other 160 candidates with the pinned complete-owner and
   winner-gap logic.

The retained and full-radius-four candidate universes are both serialized,
the nonanchor ambiguous count is required to be zero, and the adapter is
required to restore every patched runtime function.  The total remains 161
candidate tests for that collision; the proof does not drop the anchor from
the census.

## Exact inherited face

At `u=100000`, the prior Round160 physical beta faces are

```text
C24    [280280,280295]
bridge [280295,280690]
D3     [280690,280710].
```

The final Round161 faces are

```text
C24
  [1401336916994043/5000000000,
   1401461916994043/5000000000]
bridge
  [1401461916994043/5000000000,
   1401736916994043/5000000000]
D3
  [1401736916994043/5000000000,
   670556403486916994043/5000000000].
```

Their strict intersections are respectively

```text
C24
  [280280,1401461916994043/5000000000]
bridge
  [280295,1401736916994043/5000000000]
D3
  [280690,280710].
```

All have positive width, so the remote macro slab attaches to the already
certified connected sheet in every typed family.

## Strict scope

Even a successful full audit of this scale jump does not make its upper
endpoint terminal and does not exhaust the lifted tail.  The geometrically
forced E-to-S source-chart face remains at scale
`u approximately 2^4295.086903`, and it is a nonterminal chart gluing.
Before reaching it, every named R1648 margin must still be propagated.

The final D02 obligation remains a four-chart compact angular
branch-and-bound with zero unresolved exterior leaves.  Until that is done,
D02 stays blocked, D03 stays unauthorized, Gate5 stays `10/18`, global
complete blocks stay zero and CM2 stays `NO-GO_FOR_CLAIM`.
