# CM2 one hundred thirty-fifth direct assault

Date: 2026-07-24

Strict verdict: **the exact-fibre rank-three construction is now certified on
the full positive-Borel collar of half-width `2^-17`, with all recut geometry,
four physical subcollars, F17 guards, and new wide-family ID constructors
closed. This is a local robustness theorem only. Gate5 and CM2 do not move.**

## Main widening

Round129/130 worked on a nondegenerate but tiny `2^-512` collar. Round135
certifies

```text
lambda in [5/131072, 7/131072].
```

This has center `3/65536`, half-width `2^-17`, and lies strictly inside the
frozen Round113 parent. The next dyadic half-width `2^-16` lands exactly on
both parent faces, so it cannot be promoted using the same frozen
parent/operator-cell contract.

The root graph is monotone, and its `b` image has positive certified length.
The proof uses positive normalized derivative cells and integral envelopes,
not subtraction of nearly coincident adapted endpoints.

## Materialized geometry

The complete finite evidence census is:

```text
root partition endpoints:          5
derivative convergence rows:       9
normalized derivative cells:     192
input recut roots:                23
stage-three recut roots:          192
common children:                  24
merged cuts:                     215
output fragments:                216
physical subcollar audits:         4
F17 guards:                       72
field transport templates:        18
total:                           970
```

The monotone `V_i` envelopes prove `6<V1(1)<7`,
`17<V2(1)<18`, and `192<V3(1)<193`. They produce 24 positive common
children and 216 positive stage-three fragments. All recut guards and merged
orders are strict.

## Physical and current boundary

The four closed physical subcollars cover the full wide collar. Each replays
4056 typed occurrence checks:

```text
total checks:                    16224
physical incidences:                0
residual physical faces:            0
physical F10:                        0
```

All 72 F17 rows remain strict. Stagewise actual generator bounds stay below
`8`, `2`, and `0`, while the authoritative F17 bounds remain
`4915200/2457600/2457600`.

The empty physical family does not become positive F10. The exhaustive
half-open output partition gives only restricted relative cemetery zero; it
does not install ambient pre-regularization cemetery, all-time owner
cemetery, or positive cemetery payment.

## Wide local 18-field transport

The 18 transport rows use new constructors keyed by the wide family root and
canonical exact-`lambda` key. For each exact fibre they preserve 120 base
keys, 2160 field slots, 120 local level blocks, 24 local child packets, and
local `18/18` maturity.

These 18 rows are finite family templates. They are not actual slots for
uncountably many fibres and cannot be counted as global complete blocks.
Likewise, the structural F18 transport does not prove Wiener invertibility,
aperiodicity, or Kac closure.

## Independent verification

The independent verifier does not import or execute the Round135 producer.
It reconstructs the entire certificate result and all 970 evidence rows from
frozen upstreams, re-closes every row and group hash, and verifies 970 unique
primary evidence IDs.

Formal verification is `PASS`:

```text
re-signed semantic mutations rejected: 59/59
strict-JSON attacks rejected:          18/18
dual hash-seed byte replay:             identical
hostile I/O cases:                      fail-closed
```

## Frozen global state

```text
wide every-exact-fibre local maturity: 18/18
global complete 18-field blocks:           0
Gate5 global maturity:                 10/18
Gate5:                          NOT_CERTIFIED
CM2:                         NO-GO_FOR_CLAIM
```

The next honest route must leave this single frozen parent/operator-cell
setting and supply global return-word/arbitrary-depth coverage together with
positive physical F10 and the unpaid ambient cemetery/owner recovery
contracts. Round135 does not claim those steps.

