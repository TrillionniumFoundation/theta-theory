# CM2 Round 93 — first full source-chart event isolation

Date: 2026-07-22 (Asia/Shanghai)

## Result

All 65 exterior rank-three rays closed by Rounds 91–92 at their frozen-core
boundaries have now been continued diagnostically to their first boundary of
the full dominant-coordinate source chart.

The terminal event census is exact and stable under 640-bit recomputation:

```text
source chart seam |t|=1/sqrt(2): 28
source grazing / inverse loss:    37
untyped:                             0
```

Each endpoint is isolated by 112 rational bisections with a strict full-chart
interior state on the inner endpoint and a strict seam or source-inverse loss
on the outer endpoint.  Along every open candidate ray, 128 interior point
states were independently replayed through the first owner, second owner, and
third physical tangency classifier.  All 8,320 point states pass.

## Strict boundary

This round does not claim a whole-open-interval continuation theorem.  The
direct adaptive interval replay suffers repeated-variable inflation near the
physical endpoints; the intervals between the 128 strict point probes still
need centered source-coordinate Taylor coverage.  The 28 chart-seam endpoints
also still need to be transferred into their adjacent dominant-coordinate
charts.

Therefore no complete physical-face quotient, RN row, Gate 5 field, or global
gate is promoted.

## Verification

- producer precision: 512 bits;
- independent replay precision: 640 bits;
- endpoint rows: 65/65;
- strict physical point probes: 8,320/8,320;
- hostile semantic mutations rejected: 3/3;
- strict JSON duplicate/nonfinite rejection: pass.

## Next lawful route

1. cover each interval between consecutive point probes with centered Taylor
   source-coordinate boxes;
2. transfer and reclassify the 28 seam endpoints in the adjacent source chart;
3. terminate the 37 grazing rays and pair the transferred seam branches;
4. rebuild the complete rank-three physical-face quotient and only then attach
   RN/Gate-5 rows.

