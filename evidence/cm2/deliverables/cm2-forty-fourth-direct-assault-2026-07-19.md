# CM2 forty-fourth direct assault

Date: 2026-07-19  
Verdict: **F13 closes at the global regular-density Borel layer and Gate 5
advances to `8/18`; the proper-family C24 hit block remains nonnumerical, so
no composite gate is yet closed.**

## Round result

| gate | Round-44 advance | exact remaining core gap | status |
|---:|---|---|---|
| 1 | no unsafe transfer | physical Green/cancellation representative and same-representative twisting/PPE | NOT CERTIFIED |
| 2 | no cross-carrier promotion | invariant stable plaques, physical holonomy, quotient branches and PPE | NOT CERTIFIED |
| 3 | arbitrary-`R_n` suffix-pushed two-trace TV ledger and D1-dominated physical tail | common moving branch-record convergence, dynamic `MT_DQ` and cemetery | NOT CERTIFIED |
| 4 | exact bump-mixing and C24-magnet numerical interfaces; SYZ constant audit | numerical proper-family C24 minorization, then collision-time `C_fw/C_rev/q` and cemetery | NOT CERTIFIED |
| 5 | all-five-face regular-density Borel F13, physical `L^(6/5)` trace charge and `1/6` block tail | F12, strong F13/F16, full F10, cemetery and F14--F18 | NOT CERTIFIED (`8/18`) |

Strict unconditional progress remains `0/5`; CM2 remains
`NO-GO_FOR_CLAIM`.

## 1. Gate 4: the target-sensitive gap is isolated

The numerical clocks

```text
C24 killed Growth: n_*=9148,
standard-family recovery: R(D)<=301500+1005D
```

control boundary complexity/properness, not fixed-target incidence.  An exact
scalar countermodel proves that properness plus Growth alone cannot imply a
C24 hit.

The existing bump gives

```text
mu_s(g)>21/55859375, ||g||_C1<2724,
epsilon_hit=21/111718750.
```

Thus a numerical proper-family mixing pair `C_SF,theta_SF` would give the
explicit block

```text
H_SF=max(0,1+ceil(log(2724*C_SF/epsilon_hit)
                  /(-log(theta_SF)))).
```

The alternative SYZ magnet route would give

```text
q=(1-zeta/2)^(1/Delta)<1.
```

Official `arXiv:1210.0011v4` supplies the architecture but neither numerical
`zeta,Delta` nor a proof that one stable-saturated magnet lies inside C24.
This route therefore remains fail-closed.

## 2. Gate 5: arbitrary-suffix F13

In collision area coordinates, positive trace pushforward preserves mass.
The complete 64-row occurrence current and the 96 C24 edges therefore remain
two positive Borel traces after every regular suffix.

The exact C24 boundary calculation gives

```text
normalized total C24 edge measure <469439/1950000,
two-trace core flux <(469439/975000)c_X,j.
```

The occurrence current gives

```text
two-trace occurrence TV <=(63/8000)c_X,j.
```

Consequently

```text
c_F13,n <=(3816937/7800000)c_X,n
         <(1/2)c_X,n=(25/302)c_D1,n.
```

This closes the all-five-face regular-density Borel F13 field and imports the
same-ID physical `L^(6/5)` moment plus block exponent `1/6`.  Gate-5 maturity
advances from `7/18` to `8/18`; complete 18-field blocks remain zero.

The promotion is deliberately limited.  No C1 suffix pullback, strong
standard-family F16 norm, cemetery-compatible flux ledger or dynamic
branch-record convergence is inferred from TV contraction.

## 3. Latest-technology audit

The official arXiv API reports

```text
1210.0011v4 / 2104.06947v3 / 2604.19671v2 / 2606.10155v1.
```

No newer matched theorem supplies numerical proper-family C24 mixing, a
C24-contained coupling magnet, or the missing strong arbitrary-path
F12/F16/cemetery layer.  No shape-mismatched result is imported.

## 4. Validation

Both suites pass syntax, strict JSON, pinned dependencies and independent
replay.  Their fail-closed verifiers reject

```text
28/28 + 38/38 = 66/66
```

hostile mutations.  Certificate/verifier default modes remain fail-closed;
leaf and recursive SHA chains freeze the complete Round-44 output.

## Next shortest line

1. Build a direct numerical covering/minorization on every long canonical
   parent-`W` family, including a numerical post-C24-cut return to the same
   proper class.
2. Convert that block to collision-time `C_fw/C_rev/q`, then close the strong
   cemetery independently.
3. Prove F12 C1 trace pullback on the same suffix IDs; join the new F13 payload
   to a standard-family/strong F16 flux cost.
4. Close full all-face F10, F14--F18 and the induced coefficient; return the
   dominated current ledger to Gate-3 dynamic `MT_DQ`.
5. Continue Gate 1 and Gate 2 as independent structural poles.

Strict unconditional CM2 remains `NO-GO_FOR_CLAIM`, with zero of five
composite gates fully closed and Gate-5 maturity at `8/18`.

