# CM2 Seventy-Sixth Direct Assault — 2026-07-21

Round 76 deepens the fixed-parameter residual tree and closes all requested
numeric fields on the physical R2 registry.

- The actual 24-core tree is refined from depth 16 to 20, yielding `670,724`
  leaves: 45,080 R1, 23,184 R2, 339,388 Q2, and 263,072 outer.
- Exact outer area contracts from `8157/8192` to `8221/32768`, a ratio
  `8221/32628 < 1/3`; strict R2 area grows from `19/512` to `5501/65536`.
- All 23,184 strict R2 leaves land in exactly the 16 Round-75 pair labels;
  the eight narrow-corner branches now contribute 48 strict leaves, while
  new strict pair labels remain `0`.
- A 384-bit second-order T² Jet in `(t,p,s)` gives numeric F8/F9/F10/F13/F16
  rows on all `32/32` R2 faces: common F8 `1/2`, F9 sum `32`, F10 sum `32`,
  and total F13/F16 cost each `<5354421251/250000000000`.
- The first finite cross-rank geometric-depth sum uses weights `1/2` on R1
  and `1/4` on R2: F10 is `248`; F13/F16 are each
  `<5354437251/1000000000000`.
- Strict boundary: depth-20 outer remains `8221/32768`; no new strict pair at
  finite depth does not exclude a hidden open component. The 40 residual
  cells and official physical path-law weighted sum are not promoted.
- Cold residual/numeric reemit, `23184/23184` R2 replay, `32/32` numeric rows,
  hostile semantic `256/256`, strict JSON `4/4`, and SHA checks pass.
- Gate 4 remains `1/7`; Gate 5 remains `10/18` with blocks `0`; composite
  gates remain `0/5`; CM2 remains `NO-GO_FOR_CLAIM`.

Primary report:
`deliverables/cm2-round76-residual-numeric-assault-2026-07-21.md`.
