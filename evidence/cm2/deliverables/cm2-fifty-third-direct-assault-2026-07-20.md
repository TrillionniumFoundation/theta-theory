# CM2 Round 53 full direct assault

Date: 2026-07-20  
Strict verdict: **0/5 complete gates; CM2 NO-GO**

## Gate 4: one numerical SYZ clock component

The proof-level coupling schedule is no longer represented by one opaque
`Delta`.  In the Euclidean `Z` convention used by the SYZ proper-family
argument, the frozen numerical Growth recurrence gives

```text
a=360134800/360493663,
a^696<1/2,
C_p^E<2^317,
n_p<=696*317=220632.
```

The remaining schedule is reduced to five typed numerical rows

```text
zeta0>0,
S,R in Z_{>=0},
C>=1,
0<lambda<1.
```

If

```text
L_rec=min{L>=0:2*C*lambda^L<=zeta0*(1-zeta0)},
```

then the exact source scheduling algebra gives the safe upper

```text
D=3*S+2*R+220632+L_rec >= Delta.
```

Together with Round 52's `C_bump,outer<=3807/10` and
`Lambda^(-1/2)<9/10`, this gives integer-only conditional builders for
`H_bump` and `H_out`.  The five physical rows remain nonnumerical, so neither
clock has an actual integer value and no field is promoted.

## Gate 4: fixed-map relative crossing thickness

For each fixed parameter `sigma`, Round 50 supplies one finite but
nonnumerical `N_sigma` on the Euclidean-selected long-curve class

```text
|V|>=delta_rect,
delta_rect=119621/(41476958890128*10^90).
```

Strict crossing is open, and the selected curve class is compact.  After the
audit correction, the relative thickness is measured in the **same adapted
line element** as the conditional density ratio:

```text
eta_sigma^*
=inf_V sup_U ell_*(U)/ell_*(V)>0.
```

The fixed-map conclusion is qualitative.  On every retained piece,

```text
beta_sigma>(1999/2000)*eta_sigma^*,
```

and, after the retained-family factor,

```text
h_sigma>(3996001/64000000)*eta_sigma^*.
```

The two correctly typed benchmarks give the same exact condition:

```text
eta_sigma^*>=43008/14285703575
```

would imply both

```text
beta_sigma>2688/893303125,
h_sigma>21/111718750.
```

No numerical `eta_sigma^*`, branch derivative row, singularity clearance or
parameter-persistence modulus is available.  Pointwise fixed-map positivity
therefore does not give a uniform numerical `H_cover/beta`; the strict
inferable uniform numerical lower remains zero.  Whole-family C24 hit mass
is still not renamed as a registered Cantor crossing.

## Gate 4: exact cell-Z bridge to the defect moment

On the Round-52 once-charged cell law define

```text
J_pair=integral sum_c p_c*(ell_fw,c^(-1)+ell_rev,c^(-1)) dlambda,
M_c=ceil(log2(1/min(ell_fw,c,ell_rev,c)))_+.
```

For every integer `m>=0`, the strict event identity gives

```text
{M_c>m}={min(ell_fw,c,ell_rev,c)<2^(-m)},
T_m<2^(-m)*J_pair.
```

The pure-rational estimate

```text
exp(1/6)<241/204<13/11
```

and the exact skip-one layer cake then give the following rational conditional bridge

```text
I_D<nu(X)+[35/(99*2^309)]*J_pair.
```

The coefficient includes the Round-52 jump at `M=311`; there is no dyadic
rounding or two-view double charge.  Physical `J_pair<infinity` is not yet
proved, so physical `I_D` remains open.

## Gate 4/5: the aggregate-Z route now has three explicit joins

Round 36's unnormalised recurrence has the exact resolvent

```text
Z_(n+1)<=a*Z_n+b*m_n+F_n,
a=360134800/360493663,
b=2*10^90,
1/(1-a)=360493663/358863.
```

It yields `sum Z_n<infinity` if both `sum m_n` and `sum F_n` are finite.
The audit found that Round 28's

```text
S_n=mu_s(Q_n)/mu_s(C_s)
```

is normalized, while Round 36's `m_n` is the unnormalised mass of a complete
same-ID survivor/return-level standard family.  Their ID, level shift and
normalization have not been joined.  Thus the exponential `S_n` tail does
not currently certify `sum m_n`.

The physical route to `J_pair` now has exactly three fail-close joins:

1. identify or quantitatively dominate the Round-36 `m_n` by the same-ID
   Round-28 survivor mass, with the level shift and normalization explicit;
2. prove the all-insertion-time face forcing `sum F_n<infinity`, including
   cemetery;
3. prove a same-law terminal-extraction inequality from aggregate `Z` and
   terminal face injection to `J_pair`.

Round 52's fixed-insertion owner `Z_B` is not the terminal cell numerator and
has no insertion-time decay.  Only after all three joins would the resolvent
close `J_pair` and hence `I_D`.

## Gate 4: independent common-refinement numerator

The common terminal survivor of relative mass `>249/250` needs a second
quantity.  On one future physical Borel common-refinement atlas define

```text
J_cap(y)=J_cap,fw(y)+J_cap,rev(y),
z_cap(y)=J_cap(y)/h(y).
```

With the strict properness boundary,

```text
D_cap=0                                  if z_cap<C_p,
D_cap=min{d>=1:2^(-d)z_cap<C_p/2}       if z_cap>=C_p.
```

In particular `z_cap=C_p` gives `D_cap=2`, not zero.  Conditional on
`J_cap,total<infinity`, the two orientation-specific pushforwards become
proper after the synchronized clock `696*D_cap`, and

```text
integral h*exp(D_cap/6)
 <= H+(2197/1331)*(4/C_p)^(1/4)*H^(3/4)*J_cap,total^(1/4).
```

This supplies a proper two-view reference carrier and the moment of this one
extra properisation clock only.  It is not one physical first-return kernel,
does not prove intermediate avoidance or later recovery clocks, and does not
construct physical `q` or strong cemetery.  The physical common-refinement
atlas and `J_cap` bound remain missing.

## Gate 5: transverse owner trace and the exact tower frontier

At `s=0`, one fixed insertion time `j` and registered `j<n`, the owner law
selects one transverse root on each unstable parent.  It is a trace law, not
an unstable proper-standard-family density.  A foliated-square separator has
trace mass one on a transverse face and proper-family mass zero there, so the
Round-42 collision-volume Growth recurrence cannot be transferred by finite
positive domination.

A unit-loss conditional bridge would need positive maps `E,Tr` satisfying

```text
Tr composed E = Id                     on owner trace laws,
K_trace = Tr composed K_proper composed E
                                        on killed-block survivors,
norm(E),norm(Tr)<=1,
```

with `E` landing in the proper-family class and all owner IDs preserved.
Only under these hypotheses would `kappa_trace<=rho` follow.  No such bridge
is installed; the recovery identity explicitly excludes the vacuous zero
maps.

Trace-nullity alone is also insufficient.  The nested model

```text
Q_p=[0,rho^p]x[0,1/(p+1)]
```

has a trace-null limiting set but trace survivor mass `1/(p+1)` and face
charge `2^14/(p+1)`, so its weighted tower diverges.

On one common same-ID trace law, the exact conditional Holder region is

```text
w*delta^(1-1/q)<1.
```

For `delta=rho` at the frozen Round-42 weight,

```text
q_*=2.0008601538040746333039402493451877... .
```

The certified raw `4^(-b)` tail has critical moment exactly `q=2`; an exact
saturating law proves that no `q>q_*` moment follows.  A shell extremizer also
shows divergence at the equality boundary of the Holder criterion.  The
weaker conditional `q=3/2` weight

```text
w_face=(1+rho^(-1/3))/2
```

would work only after a same-ID trace contraction at rate `rho` is actually
proved.  No face-tower field is promoted.

## Gate 5 and Gate 3: graph-current F17 frontier

For the physical source representation

```text
T_(K,B)(phi)=integral K dot grad(phi) dm+B(phi),
```

the Lipschitz-dual source injection is now explicit:

```text
||T_(K,B)||_(Lip*)<=||K||_L1+TV(B).
```

The source bulk/trace constant and the regular boundary-trace Piola
multiplier are one.  The bulk suffix multiplier is not controlled by area
preservation: for

```text
S_L=diag(L,L^(-1)),
K=e_1,
```

the source bulk cost is `1/L` and the target cost is one, forcing any common
multiplier to be at least `L`.  Physical F17 still needs a billiard-specific
vector-current anisotropic graph space with a directional Piola or
quotient/coboundary estimate.

This source typing is compatible with the fixed Gate-3 free graph-current
carrier of `41,508` slots, but the physical `Q_s/R_s` lift/quotient, moving
branch IDs, observable inclusion, suffix bound and cemetery are absent.
Gate-3 `MT_DQ` is not promoted.  Gate 5 remains **10/18**, with **0** complete
18-field blocks.

## Latest-technology audit

The official versions rechecked on 2026-07-20 remain

```text
1210.0011v4 / 2104.06947v3 / 2604.19671v2 /
2604.25881v1 / 2606.10155v1 / 1807.02330v4.
```

Round 53 additionally audited `2502.07765v2`.  Its complex-cone/sequential
mechanism still imports existential `N_F(delta)`, `epsilon` and cone-diameter
data.  The checked sources do not supply the five numerical magnet rows, a
parameter-uniform adapted C24 crossing atlas, the missing survivor-mass
normalization join, the two physical `J` bounds, a transverse owner-trace
contraction, or a vector-current F17 suffix constant.

## Strict status

```text
Gate 1: NOT CERTIFIED
Gate 2: NOT CERTIFIED
Gate 3: NOT CERTIFIED
Gate 4: NOT CERTIFIED
Gate 5: NOT CERTIFIED (10/18; complete blocks 0)
CM2:    NO-GO
```

## Validation

- Python syntax: `6/6 PASS`;
- strict JSON, dependency pins, integrity and deterministic replay: PASS;
- independent exact arithmetic, measure/index typing and literature-scope
  audits: PASS;
- hostile manifest mutations:
  `104/104 + 172/172 + 109/109 = 385/385` rejected;
- certificate/verifier default modes: `6/6`, each exactly exit `2`;
- deterministic manifest regeneration: `3/3` byte-identical;
- three leaf SHA ledgers and the recursive Round-53 ledger: PASS.

Three independent read-only audits found the adapted-line-element join, the
normalized-to-unnormalized survivor-mass join, and the trace
extension/recovery intertwining conditions.  All required corrections were
incorporated before the final replay.  The mutation suites are fail-close
regression tests, not substitutes for the mathematical proofs.

## Next route

1. Fill the five numerical magnet rows `zeta0,S,R,C,lambda`; the exact
   interface then outputs numerical `Delta,H_bump,H_out`.
2. Interval-enumerate adapted-compatible target widths, clean branches,
   derivative/distortion bounds and parameter-persistence intervals until
   `eta_sigma^*>=43008/14285703575` uniformly.
3. Prove the same-ID Round-28-to-Round-36 mass normalization, the all-time
   face forcing, and terminal extraction; the aggregate resolvent then
   closes `J_pair` and `I_D`.
4. Independently construct the common-refinement atlas, bound `J_cap`, and
   prove the physical first-return/intermediate-avoidance and every remaining
   recovery-clock moment before defining `q` and strong cemetery.
5. Prove direct transverse trace contraction or a genuine `E/Tr` bridge;
   combine it with owner grazing sparsity beyond `q_*`, signed cancellation,
   or the conditional weak-weight route.
6. Build the physical vector-current anisotropic F17 space and suffix bound;
   then finish all-face F10, strong F13, F14/F15/F17/F18 and return to Gate-3
   `MT_DQ`.

Gate 1 and Gate 2 remain independent structural poles.

Leaf reports:

- `cm2-gate34-round53-effective-clock-relative-atlas-frontier-assault-2026-07-20.md`;
- `cm2-gate34-round53-fractional-z-common-return-frontier-assault-2026-07-20.md`;
- `cm2-gate5-round53-trace-standard-family-graph-f17-frontier-assault-2026-07-20.md`.
