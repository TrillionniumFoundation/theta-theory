# CM2 Round267: Source-G lower-stratum terminal lineage

## Certified result

Round267 closes the bookkeeping lineage of every one of the `62,696`
Round174 lower-dimensional strata against the pinned Round179 normal forms
and the frozen Round266 frontiers.

- `6,120` rows are strictly absent on every descendant;
- `56,576` rows reach one canonical terminal support;
- only `1,148` supports have an explicit physical-existence certificate;
- the other `55,428` remain nominal `if present` supports and receive no
  existence, occurrence, glue, or exact-key credit;
- the Round266 quotient and all frontiers remain byte-identical:
  components `63,224`, expanded occurrences `126,468`, exact keys `116`,
  valid virtual nodes `133,284`, and explicit members `259,752`.

The certificate result SHA256 is
`deaee59586be2f324fbedfe7a97d8ee54b7962f818a3d12ff1d4935d881b0c35`;
the complete certificate SHA256 is
`66879215e0250a4a462fea9837c24bccf49a936ce3c5e66da2707e1a66fafc8f`.

## Independent verification and reproducibility

The independent verifier reconstructs all `62,696` terminal-lineage rows and
all `582,844` Round266-to-Round267 quotient/frontier identity rows from pinned
upstream inputs before opening the candidate.  It does not import or execute
the producer.  Complete result equality passes as `PASS_INDEPENDENT_ROUND267`.
All `16/16` result-level-resigned semantic attacks are rejected.

Producer seeds `267071` and `267929` are output-inert and yield the same
result and complete certificate bytes.  The verifier output SHA256 is
`4588918351f064906515bc96dee29f424c44750abb5a2a480fd159d2c82cf1e9`.

## Scope and strict non-promotion

Terminal lineage is not by itself physical existence or an exact-key
occurrence.  Round267 therefore awards zero new physical glue, component
union, maximality, exact-key-fibre, global-disposition, or Jx/Jy same-point
credit.  Gate5 remains `10/18`, D02 remains blocked, and CM2 remains
`NO-GO_FOR_CLAIM`.

The next gate is the Round182 descendant audit: materialize the `184,452`
geometrically closed collar leaves with side-specific return data, exhaust
the `152` positive-area true source-seam owner/shadow candidates, and
reverse-rechart all `728 + 152 = 880` positive-volume chart guards.  Only
after those channels close can the `63,224` components be considered for
maximality and the `116` exact-key fibres for exhaustion.
