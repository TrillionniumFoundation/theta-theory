# CM2 Round 65 independent core-frontier audit

Date label: 2026-07-21

Audit verdict: **PASS.  The four Round-65 leaves are mutually consistent,
their executable claims replay, and no abstract interface, average moment,
selected-orbit number, wrong-law theorem or signed cancellation is promoted
to an unpaid actual CM2 row.  No composite gate closes.**

## 1. Frozen inputs and acceptance

The audit reads the already frozen Gate-1/3, Gate-2/4, Gate-5 and cross-gate
positive-potential manifests.  It authors none of those source leaves and
modifies no old artifact.

```text
new Python syntax including Round-65 common:       9/9
dependency pins across four leaves:               84/84
leaf SHA artifact rows:                           19/19
leaf audit/replay and deterministic reemit:         4/4
hostile semantic mutations:                    1148/1148 rejected
hostile strict-JSON payloads:                     16/16 rejected
default leaf entry points:                         8/8 exit 2
```

The independent audit has its own canonical regeneration, hostile semantic,
strict-JSON, source replay, SHA and fail-closed checks.

## 2. Gate 1/3 audit

### 2.1 Selected QNL numbers do not instantiate `q_cross`

The display

```text
(91/1000)/(9281/100000)=9100/9281<1
```

is arithmetically correct and semantically irrelevant to the Round-64
all-plaque ratio.  The numerator and denominator live in the selected
compact-log QNL tail, whereas `q_cross` needs a combined-Green all-plaque
Holder defect rate divided by a uniform lower diagonal-product bound.  The
gauge, registry, quantifier, inequality direction and role differ.  The
eight-depth second-plaque completion preserves selected decay while making
`T_n=(-1)^n`; the audit replays all eight rows.

### 2.2 The complete four-wedge token

For

```text
Psi=[[ab,au],[lb,lu+(ab)^-1]],
```

the four oriented wedges are all nonzero exactly when

```text
l!=0,  u!=0,  1+ablu!=0.
```

The Round-64 critical matrix `[[1,1],[-1,0]]` has the two previously reported
axis wedges nonzero but has one zero in the complete four-wedge token.  This
is a refinement, not a contradiction.  The nearby `l=-1/2` replay has four
nonzero wedges and determinant one.  The actual same-representative scalars
remain absent.

### 2.3 Strong current and Piola

The material factorisation

```text
T_s=Qhat_s Phat_s Rhat_s
```

and its three-term derivative are exact interfaces.  A moving restriction
on retained side/face tags has a bounded strong derivative exactly when the
two-sided weighted trace map into `ell1` is bounded.  Unit-`L1` triangular
functions have boundary trace `2N`, so weak norm-one restriction does not
pay this row.

The hat-translation replay independently shows that pointwise
differentiability on every `W11` vector does not give operator-norm
differentiability `W11->L1`; the normalized remainder tends to one.  A
uniform one-more-derivative estimate would suffice, but no physical
anisotropic remainder is frozen.  Finally, stopped mass one with
`p_n=1/[n(n+1)]` has divergent depth-weighted derivative charge.  Physical
`R_s/Q_s`, Piola and `MT_DQ` remain open.

## 3. Gate 2/4 audit

The finite/countable weighted-tree result is correctly extended to a
standard-Borel plaque outer law.  Under a common positive `L1`
trivialisation, a measurable lower median realizes `delta_eta`, with

```text
P_eta<=delta_eta<=2P_eta,
delta_eta=0 iff P_eta=0 iff pulled markers agree a.e.
```

The finite replay gives `P_eta=19/80` and `delta_eta=2/5`.

The all-depth first-failure identity is exact:

```text
eta(S_infinity)=eta(S_0)-sum_N eta(F_N)=lim_N eta(S_N).
```

The frozen physical strip stops at depth 96.  A depth-97 empty continuation
is compatible with every pinned prefix, and `S_N=[0,2^-N]` shows that every
finite depth may have positive mass while the infinite survivor has mass
zero.  The actual stable-tree registry remains `0/7`.

For a tagged BV lift, `W_D in L-infinity` controls the weighted zeroth-order
term but not transverse variation.  The sufficient bridge also needs the
weighted variation sum `A_1`; the two-tag smooth kernel has `W_D=1` while
variation is `4N`.  A stronger exact-trace argument rules out any bounded
factorisation `L1 -> B_strong -> BV` whose composite is the identity.  No
physical Piola/strong assembly is inferred.

## 4. Gate 5 audit

The live plus one-shot-arrival theorem is an exact extension of the
Round-64 lineage criterion:

```text
beta_(j+1)+alpha_(j+1)<=c beta_j,
c_*=||d(beta_(j+1)+alpha_(j+1))/d beta_j||_infinity.
```

The three replay coefficients are `1/2`, `3/2` and infinity.  Thus stationary
live mass plus a positive arrival cannot be hidden in a sub-Markov row.

For fibre-constant marks, one physical label-preserving kernel pays all seven
positive sectors exactly when

```text
r_j<=1,
r_j h_(j+1)^s<=kappa_(j,s)h_j^s  for every sector s.
```

The charge-marginal version is only an exact sector-specific transport
criterion; it does not create one common physical kernel.  The one-label
separator has base coefficient `1/3` and finite weighted base mass, but raw
coefficient `2/3=w^-1` and a constant divergent raw ledger.  Base lineage
decay therefore does not pay raw-`Z` or Orlicz.

The leaky cemetery identity is correct for `wq<1`, while absorbing occupancy
diverges for every nonzero arrival because `w>1`.  The oriented positive
bridge correctly needs two RN dominations.  Before any exact Round-54/61
crosswalk, equal positive source masses force the necessary scalar test
`xi^f(X)=xi^r(X)`; that equality is not frozen.  Signed equality alone does
not pay the common mode.

The remaining global rows are exactly

```text
F5, F6, F10, F11, F14, F15, F17, F18.
```

No actual field is promoted; Gate-5 maturity stays `10/18`, blocks zero.

## 5. Cross-gate positive-potential audit

For an initial law `mu`, path kernels `P_(0:j)`, tag kernels `K_j` and typed
positive costs `g_j`, put

```text
W_j=K_j g_j,
H=sum_j w^j P_(0:j)W_j.
```

The positive direct-sum lift from `L1(mu)` is bounded exactly when
`H in L-infinity(mu)`, and its norm is `ess sup H`.  The upper bound follows
from total variation; equality on nonnegative inputs proves necessity.

The exact separator has `mu{n}=3*4^-n`, `w=3/2`, path survival `3^-j` and
tag weight `2^n`.  It simultaneously has

```text
w*kappa=1/2,
one-time actual moment=3,
all-time actual one-vector charge=6,
H(n)=2^(n+1) unbounded.
```

Hence even a valid Gate-5 mass contraction and a finite actual all-time
moment do not pay a Gate-3/4 strong operator.  Conversely, a bounded tag at
each time with no decay has infinite potential.  Both factors are
independent.

## 6. External technology red team

The audit agrees with the leaves' direct source checks.

- `arXiv:2603.19509v3` assumes strong differentiability and rapid memory loss;
  its verified examples are expanding/noisy systems, not moving billiards.
- `arXiv:2604.25746v1` treats compact boundaryless `C^(1+beta)` flows whose
  singularities are fixed points, not collision discontinuities.
- `arXiv:2510.19573v3` supplies function-side weighted-`L-infinity`
  dominated-kernel criteria after domination/Lyapunov hypotheses are given;
  it does not construct CM2 owner marginals.
- `arXiv:2605.07824` is a killing/branching diffusion result, the wrong
  process and recipient.

No external theorem is promoted.

## 7. Strict audited state

```text
Gate 1: NOT_CERTIFIED
Gate 2: NOT_CERTIFIED; official fields 0/17
Gate 3: NOT_CERTIFIED
Gate 4: NOT_CERTIFIED; landing join 1/7, fields 1/4/7 partial
Gate 5: NOT_CERTIFIED; maturity 10/18, complete blocks 0
complete composite gates: 0/5
CM2: NO-GO_FOR_CLAIM
```

The shortest joint route is now explicit: construct one immutable actual
path/tree/material registry, then prove the all-plaque combined-Green limit,
the two-sided strong trace and Piola remainder, the all-depth collision-SRB
stable trivialisation, the live/arrival RN split and the `L-infinity`
positive path potential for every sector.  None of those actual joins can be
replaced by its present average or fixed-time shadow.
