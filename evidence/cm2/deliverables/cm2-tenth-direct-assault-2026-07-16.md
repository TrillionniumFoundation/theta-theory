# CM2 tenth full direct assault

Date: 2026-07-16 (Asia/Shanghai)  
Frozen baseline: v51/v52 and the first nine assault rounds were read only  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; composite
gates remain `0/5`**

## 1. Executive result

This pass closes the complete common finite-parameter endpoint/core mesh and
the uniform one-time stopped-parent recovery layer on `|s|<=1/400`.  It also
completes the limiting face--product centered cluster and its auxiliary
two-time clock algebra, and supplies the first numeric physical Borel
prefix/suffix and Kac norm envelope.

No composite gate closes.  The finite-`s` recovery theorem has uniform but
non-evaluated constants and applies to one fixed-`s`, one-time stopped atom,
not a native stopping antichain or hereditary repeated restrictions.  The
moving dyadic endpoints are not registered in the iterated Gate-3 common DQ
atlas.  The physical return-wide standard-family, flux-face and dynamic-test
intertwiners also remain absent.

## 2. Gate table

| gate | strongest certified layer after this pass | exact remaining hard gap | strict status |
|---|---|---|---|
| 1. holonomy/QNL | unchanged exact natural-holonomy resonance; latest bypass audit negative | physical same-carrier holonomy or an applicable alternative/cohomological construction | **OPEN** |
| 2. physical PPE | unchanged weak bridges and finite benchmarks | full-mass stable quotient, `rho`, reverse kernel, native stopping, endpoint typing and PPE | **OPEN** |
| 3. global event/DQ | limiting face--product centered cluster; finite-`s` auxiliary recovery clock and clock-only two-time summability | common iterated moving atlas, branch-record `MT_DQ`, physical `FACE_2CUT/FACE_TIME` | **OPEN** |
| 4. same-occurrence `q` | full-window moving endpoint/core mesh, explicit initial `C_mesh`, uniform fixed-`s` one-time recovery | numeric theorem constants and propagated `C_fw,C_rev,q`; native antichain and repeated-cut recovery | **OPEN** |
| 5. fixed/full transfer | actual Borel TV/L-infinity prefix/suffix contractions, height-nine Kac bound and singular output envelope | return-word registry, three CM2 intertwiners, full four-term Kac and operator phase transfer | **OPEN** |

## 3. Gate 4: complete finite-`s` one-time recovery layer

All 128 moving endpoint incidences over `|s|<=1/400` have one common inward
collar of width `1/48`.  A 384-bit Arb replay certifies strict inward
derivative bounds

```text
source grazing  9/10,   parameter polarity  1/6,
earlier hit      1/16,  later miss switch   1/32.
```

The endpoint cover has 20,104 leaves and maximum parameter depth 5.  The 64
moving cores have 110,336 leaves and maximum angular depth 6; all five
relevant incidence scales are strictly larger than `2^-20`.  This yields the
uniform rank tail

```text
sum_e m_(e,s){B_s>b} <= (4839232/3125) 4^-b,  b>=20,
C_fw^(geom)(s,a), C_rev^(geom)(s,a) <= 204*2^B_s(a).
```

The common mesh rule

```text
delta_b=2^-ceil(3(b+1)/2)
```

has the exact one-endpoint boundary series
`18522046487/524288` and gives the explicit initial bound

```text
C_mesh=69986663973833932800,
Z_(s,fw)(K,j), Z_(s,rev)(K,j) <= C_mesh*2^K.
```

The 35,024-box horizon cover was replayed with squared penetration slack
strictly larger than `2^-19`.  It places the entire table path in one explicit
Stenlund--Young--Zhang class with horizon `(3,1/512)`, minimum-flight
parameter `1/25`, and curvature interval `[25/9,25/4]`.

Applying Stenlund--Young--Zhang, `arXiv:1210.0011v4`, Lemmas 12 and 16, to
the constant configuration sequence `K_s,K_s,...` gives constants
`C_p>1`, `0<vartheta_p<1`, uniform in `s` and orientation.  With

```text
A0=ceil(log(C_mesh)/|log(vartheta_p)|)+1,
A1=ceil(log(2)/|log(vartheta_p)|),
```

the two oriented clocks satisfy

```text
R_fw+R_rev <= 2A0+2A1 K,
E[2^K exp(gamma(R_fw+R_rev))] < infinity
for 0<gamma<log(2)/(2A1).
```

This is uniform one-time recovery for the same fixed-`s` restricted measure
and `(s,K,j)` record in both orientations.  The theorem constants have not
been numerically evaluated, the moving dyadic endpoints are not part of the
Gate-3 common DQ atlas, and neither native stopping nor repeated-indicator
recovery is claimed.

## 4. Gate 3: centered cluster and two-time frontier

The corrected limiting 64-row graph current is completed with its actual
marginals to the exact face--product centered cluster.  Before the common
normalizer `Z_N^-1`,

```text
||J_V||_TV <= 16128/5,
||P_V||_(product) <= 32256/5,
typed cluster norm <= 48384/5.
```

Both cluster marginals vanish exactly, and the product block pairs to zero
against two individually centered endpoint tests.  After the full `2^K`
parent-normalization charge, the auxiliary level mass is exactly

```text
a_K=(3/4)2^-K,  sum_K a_K=3/2.
```

Combining the finite-`s` recovery moment with any properly matched recovered
coupling rate `c_mix` gives the clock-only two-time rate

```text
delta=c_mix/(gamma+c_mix),
kappa=gamma*c_mix/(gamma+c_mix),
C exp(-kappa max(m,n)),
sum_(m,n>=0) exp(-kappa max(m,n))=(1+e^-kappa)/(1-e^-kappa)^2.
```

Exact countermodels still prevent promotion: depth-one strong DQ plus the
formal telescope does not control an iterated source outside the strong
space; weak current convergence does not give moving-boundary tightness; and
fixed-pair `FACE_2CUT` tails do not imply a summable product-time array.
Therefore branch-record `MT_DQ`, physical `FACE_2CUT/FACE_TIME`, and Gate 3
remain open.

## 5. Gate 5: physical Borel prefix/Kac envelope

On the actual finite-signed-measure/bounded-Borel-test pair, every physical
prefix, suffix and endpoint return operator has TV/L-infinity constant one.
Because `1<=r_N<=9`, the Kac test sum and normalized phase lift have norm at
most nine.  Binding these constants to the corrected current gives

```text
global event-current TV                    <= 16128/5,
two singular Kac coordinates in l1(TV)     <= 32256/5,
worst height-nine phase lift               <= 290304/5.
```

This is an actual Borel norm lift.  It does not control regular density,
standard-family boundary cost, flux-face cuts or inverse dynamic-test
pullback.  Exact split-curve and `H(x)=x^2` models show that TV contraction
cannot imply those stronger bounds.  The full four-term physical Kac output
and all three CM2 norm lifts remain open.

## 6. Gates 1--2 and latest theorem audit

No applicable bypass was found through 2026-07-16.

- Gate 1 retains the exact QNL canonical stable increment
  `(H_n^-1 H_(n+1))_21 -> (325/144)c(z)^2>0`; both natural symbolic codings
  remain outside the required fiber-bunched regime.
- Gate 2's registered strips cover less than `9.7%` of physical mass, leaving
  more than `90.3%` without the required quotient/kernel registration.
- New toral-Anosov rigidity, random-matrix regularity, random-Anosov, Sinai
  billiard MME, Axiom-A and stationary-Hoelder results all require hypotheses
  absent from this singular full-mass quotient problem.  In particular,
  `arXiv:2607.11421` exhibits atomic physical stationary measures for a class
  of locally invertible random systems, warning against extending global
  bi-Lipschitz stationary-regularity statements to the present physical
  branch system.

The useful bridge in this pass is instead the older but directly applicable
uniform Growth/Z-recovery theorem of Stenlund--Young--Zhang.  It advances the
finite-`s` one-time layer but does not supply the missing characteristic-
function, branch-record or full-mass interfaces.

## 7. Exact next target

1. register the moving dyadic endpoints in one iterated common atlas and
   prove fixed-time branch-record `MT_DQ`;
2. replace the auxiliary product policy by a native stopping antichain and
   prove hereditary recovery after repeated restrictions;
3. extract numerical `C_p,vartheta_p`, or a numerical instance-specific
   Growth Lemma, and finish propagated `C_fw,C_rev,q`;
4. build the complete return-word operator registry and its three CM2
   intertwiners;
5. close physical `FACE_2CUT/FACE_TIME`, full Kac and phase transfer.

## 8. Validation status

The three new Gate-3/4/5 certificate stacks compile, replay, pass structural
integrity and mutation tests, and fail closed live with exit 2.  Their JSON
manifests and SHA sidecars bind the exact dependencies.  Seven current
Gate-1/2 obstruction verifiers and their SHA chains were also replayed; all
remain fail closed.  Frozen v52 was independently cold-compiled and its SHA
manifest remains unchanged.

Detailed reports:

- `cm2-gate45-finite-s-common-mesh-recovery-assault-2026-07-16.md`;
- `cm2-gate3-branch-record-face-2cut-assault-2026-07-16.md`;
- `cm2-gate5-physical-prefix-kac-norm-frontier-assault-2026-07-16.md`.
