# CM2 Gates 2/5: universal operator and selected-endpoint template assault

Date: 2026-07-16 (Asia/Shanghai)  
Frozen inputs: the thirteenth-round Gate-1 selected loop, 24 physical return
cores, roof-two wall charts, componentwise Growth theorem, numeric invariant
standard-family theorem and Gate-2/5 schemas were read only  
Strict verdict: **two genuine strong-field seeds are now bound on every one
of the 24 positive cores and the selected common-fiber loop/wedge packet is
bound to the Gate-2 frontier; return-word characteristic restriction, the
stable quotient, complete 18-field blocks, Gate 2 and Gate 5 remain
`NOT_CERTIFIED`**

## 1. Exact advance

The preceding stacks certified three layers with deliberately different
types:

1. at least 24 nonempty positive collision-SRB return keys, with one compact
   central homogeneous core on each;
2. physical charts on all 28 core-local roof-level pairs and all 52
   core-local split slots;
3. all-iterate standard-curve distortion and a componentwise one-step
   Growth contraction on every physical collision branch.

This assault performs the previously missing type join.  On every one of the
24 compact cores it binds the physical adapted unstable inverse-Jacobian seed

```text
central child:        J_*^-1 < 144000/180337,
high strip rank k:    J_*^-1 < 4/k^2,
universal envelope:   J_*^-1 < 144000/180337,
```

and the physical log-Jacobian distortion seed

```text
|log(J_*(x)/J_*(y))|
 < 15000000000000000000000000 ell_*(x,y)^(1/3).
```

At the canonical adapted length `delta_*=10^-90`, the last display is
strictly below

```text
3/200000.
```

The frozen metric comparison

```text
(5/27) d ell_E < d ell_* < (141/4) d ell_E
```

also converts the universal adapted inverse bound to the safe Euclidean
envelope

```text
J_E^-1 < (3807/20)(144000/180337)
       = 27410400/180337.
```

These are the correct one-dimensional unstable-Jacobian types in both the
adapted and physical Euclidean metrics.  They are not the old collision-area
Jacobian identity and are not local chart norms.  Thus the 24 cores gain two
genuine strong-field seeds corresponding to Gate-5 schema fields 5 and 6.

## 2. Why these are seeds rather than completed full-key fields

The immutable Gate-5 slot is indexed by

```text
(word key, homogeneous subbranch id, roof level j, field name).
```

The 441,280-key envelope still has no complete physical homogeneity
subbranch table.  A compact central core is only a strict subdomain of one
word key, not its maximal homogeneous component.  Consequently the universal
estimates can be evaluated on every actual physical child and on all 24
certified cores, but there is no immutable full-key row universe on which to
mark fields 5 and 6 complete.

The exact status remains

```text
completed full-key fields on each of 24 keys:       1 (NONEMPTY),
new core-local strong-field seeds on each core:      2,
complete 18-field physical operator blocks:          0.
```

## 3. The characteristic-restriction boundary

Before return-word restriction, every canonical curve has Euclidean length

```text
< (27/5) 10^-90
< 1/37724355673552103994 = delta_1.
```

The componentwise theorem therefore gives

```text
central multiplicity <=1,
high-strip tail       <1/5,
Xi_1                  <900337/901685
                       =1-1348/901685<1.
```

This does **not** complete Gate-5 field 7.  The `Xi_1` theorem charges true
physical singularity/homogeneity cuts.  Restricting the operator by a
return-word characteristic function additionally exposes word-domain,
source-chart seam and owner boundaries.  Their component multiplicity and
boundary `Z` have not been bounded on the 441,280-key registry.  Copying the
pre-restriction `Xi_1` into every word row would therefore count the wrong
boundary family.

For the same reason the new invariant density cone and standard-family
recurrence do not automatically fill fields 14--17 after word restriction.
This is the precise characteristic-function interface highlighted by the
current dispersing-billiard transfer-operator review, not a missing scalar
constant.

## 4. Selected common-fiber endpoint seed

The selected immutable physical QNL homoclinic occurrence now supplies a
direct matrix

```text
psi_z:E_p -> E_p
```

and four nonzero QNL eigen-axis wedges, all in the same canonical fiber.  The
matrix is computed directly from the selected orbit; no periodic-shadow
identification is used.  This is the first genuine common-fiber numerical
loop/twisting packet available to the Gate-2 endpoint frontier.

It still does not fill Gate-2 fields 9--12.  The selected occurrence has not
been assigned to an onto stable-quotient inverse branch `a`; the physical
pointwise endpoint-slope identity, same-carrier endpoint maps `X_a,Y_a` and
the endpoint denominator lower bound remain absent.  In particular,

```text
selected loop matrix seed:                 CERTIFIED,
selected four-wedge twisting seed:         CERTIFIED,
transported quotient-branch M_a field:     NOT CERTIFIED,
same-carrier endpoint packet:              NOT CERTIFIED.
```

## 5. Stable-quotient nonpromotion

None of the new joins constructs the first Gate-2 object

```text
Lambda_A = a stable-saturated physical product base.
```

The full collision/path key refinement remains invertible and hence has a
Dirac physical reverse conditional.  Universal collision-branch estimates
do not create stable projection `pi^s`, quotient density `rho`, reverse
weights `p_a`, native cylinders or parentwise amplitudes.  The existing
`>0.903` unsaturated common-rectangle complement is unchanged.

## 6. Latest-technology audit

The official arXiv API was checked again on 2026-07-16 after the generic web
search endpoint hit its quota.  No newer applicable bridge appeared.

- `arXiv:2606.10155v1` remains the nearest transfer-operator review; its
  characteristic-function restriction frontier is exactly the missing
  word-boundary step above.
- `arXiv:2604.25881v1` constructs the unique Sinai-billiard MME from
  one-sided symbolic objects, not the collision-SRB stable quotient or the
  present strong word restrictions.
- `arXiv:2604.19671v2` treats a shrinking hole for a fixed Sinai billiard;
  its standard-family hole geometry does not provide this wordwise boundary
  registry.
- `arXiv:2604.13401v1` concerns cocycles over hyperbolic automorphisms and
  does not supply a singular-billiard stable quotient or endpoint identity.

Thus no current theorem justifies promoting either template to a completed
word/quotient field.

## 7. Exact remaining boundary

```text
24 CORE-LOCAL ADAPTED INVERSE-JACOBIAN SEEDS:       CERTIFIED
24 CORE-LOCAL LOG-JACOBIAN DISTORTION SEEDS:        CERTIFIED
PRE-RESTRICTION GLOBAL GROWTH TEMPLATE:             CERTIFIED
SELECTED COMMON-FIBER LOOP/FOUR-WEDGE SEED:         CERTIFIED

MAXIMAL HOMOGENEOUS WORD DOMAINS:                   NOT CERTIFIED
RETURN-WORD CHARACTERISTIC BOUNDARY Z:              NOT CERTIFIED
COMPLETE 18-FIELD OPERATOR BLOCK COUNT:              0
STABLE-SATURATED PRODUCT BASE / rho / p_a:           NOT CERTIFIED
PHYSICAL ENDPOINT-SLOPE IDENTITY / X_a,Y_a:          NOT CERTIFIED
THREE CM2 STRONG-NORM LIFTS / KAC / PHASE:           NOT CERTIFIED
GATE 2:                                               NOT CERTIFIED
GATE 5:                                               NOT CERTIFIED
```

The next constructive step is to build the maximal homogeneous word-domain
and characteristic-boundary component table, while independently continuing
an interval-indexed physical stable plaque family through the common magnet.

## 8. Reproduction

```bash
PY=/tmp/cm2-flint-venv/bin/python

PYTHONPATH=deliverables $PY -m py_compile \
  deliverables/cm2_gate25_universal_operator_endpoint_template_frontier_cert.py \
  deliverables/cm2_gate25_universal_operator_endpoint_template_frontier_verifier.py

PYTHONPATH=deliverables $PY \
  deliverables/cm2_gate25_universal_operator_endpoint_template_frontier_verifier.py \
  --replay --integrity-only

PYTHONPATH=deliverables $PY \
  deliverables/cm2_gate25_universal_operator_endpoint_template_frontier_verifier.py \
  --self-test

# Expected exit 2: Gate 2 and Gate 5 remain fail-closed.
PYTHONPATH=deliverables $PY \
  deliverables/cm2_gate25_universal_operator_endpoint_template_frontier_verifier.py
```
