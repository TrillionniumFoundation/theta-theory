# CM2 thirty-seventh direct assault

Date: 2026-07-19  
Verdict: **the base forcing has been type-corrected and two exact escape
frontiers are frozen; all five composite gates remain open**

## Round result

| gate | round-37 advance | exact remaining core gap | status |
|---:|---|---|---|
| 1 | latest literature rechecked; no unsafe promotion | one physical representative with valid cancellation/Green hypothesis and same-representative twisting/PPE | NOT CERTIFIED |
| 2 | no cross-carrier hypothesis transfer | invariant stable plaques, physical holonomy, quotient branches and PPE | NOT CERTIFIED |
| 3 | dynamic-current typing is now separated from base fixed-`s` Growth | arbitrary-depth F13 current, DQ/`MT_DQ`, cemetery and convergent physical current ledger | NOT CERTIFIED |
| 4 | round-36 `J_n` typing corrected; exact delayed-block thresholds `697/5694` certified conditionally | full-`Q_N` strong characteristic `C_N` with `C_N a^N<1`, then `C_fw,C_rev,q` and cemetery | NOT CERTIFIED |
| 5 | exact pointwise-to-global F10 obstruction and sufficient physical interfaces frozen | global physical F10 `L^1/L^p`, return-wide F12/F13 and F14--F18 | NOT CERTIFIED (`7/18`) |

Strict unconditional progress remains `0/5`; CM2 remains
`NO-GO_FOR_CLAIM`.

## 1. Aggregate forcing type correction

The frozen closed-map coefficient

```text
a=360134800/360493663
```

already includes continuity/collision-singularity, first-owner and
homogeneity cuts.  Moving occurrences are F10/F13/F16 parameter currents,
and cemetery is a separate absorbing strong charge.  The fixed-parameter
base survivor forcing is the repeated C24-complement restriction, not the
round-36 type-mixed four-term expression.

The abstract recurrence remains valid as bookkeeping, but no physical
mass-additive `J_C24` is certified.

## 2. Why one-core contraction does not transfer

The one retained interval coefficient is

```text
(2000/1999)a
 =720269600000/720626832337<1.
```

One core interval can leave two complement components, whose safe coefficient
is

```text
(4000/1999)a
 =1440539200000/720626832337>1.
```

An exact constant-density affine-cut model has ideal local `F8=1,F9=F10=0`
but doubles the boundary functional from one to two while its mass tends to
zero.  Therefore local face regularity cannot imply a uniform mass-additive
base forcing.

## 3. Exact delayed-characteristic escape route

If one can restrict only after a whole closed block and prove a full-survivor
multiplier `C_N`, then the inherited coefficient is `C_N a^N`.  Exact rational
replay gives:

```text
C_N=4000/1999:   first possible block N=697,
C_N=580000/1999: first possible block N=5694.
```

At those thresholds the strict simple uppers are respectively
`49973/50000` and `24983/25000`, with explicit rational exponential weights.
This proves scalar feasibility.  It does not install the physical theorem:
the first constant is one-step/two-component, the second is one finite key
fibre, while the full arbitrary-depth path universe has size
`24*441280^n` and no uniform or subexponential `C_N`.

The shortest Gate-4 target is now exact:

```text
produce one physical full-Q_N characteristic multiplier C_N
on the same unnormalised strong carrier with C_N a^N<1.
```

## 4. Global F10 cannot follow from compactness

The analytic affine countermodel

```text
m_k=2^-k,
rho_k=2^k-1/2,
N_F10(k)=2^k
```

has total face mass one, finite F10 on every germ, and perfect F8/F9, yet
`sum_k m_k N_F10(k)=infinity`.  Thus the round-36 pointwise compact-germ
search does not imply a global F10 field.

The next required physical scalar is a uniform bound, an `L^p` estimate with
`p>1`, or a summable endpoint/rank/margin shell for `N_F10`.  The D1
one-time additive rank tail does not dominate arbitrary rank-path derivative
products or vanishing analytic margins.  F12/F13 and F14--F18 therefore
remain downstream.

## 5. Strong cemetery remains independent

The closed-map continuity decomposition already accounts for regular
singularity/owner cuts in `a`, but this does not make the absorbing strong
cemetery zero.  The existing Borel stopping ledger is nonquantitative, and
collision-SRB-null boundary mass does not control boundary `Z` or trace
charge.  No cemetery term was silently absorbed or promoted this round.

## 6. Latest technology boundary

Official arXiv metadata was rechecked on 2026-07-19:

- `2604.19671v2`, *Linear response for Sinai billiards with small holes*;
- `2606.10155v1`, *Recent Progress in the Application of Transfer Operators
  to Dispersing Billiards*;
- `2104.06947v3`, *Projective Cones for Sequential Dispersing Billiards*.

The small-hole paper treats a boundary hole whose limiting image is a full
angle vertical line and states that the method relies significantly on hole
geometry and foliations by long standard pairs; circular phase-space holes
and regimes forcing very small standard pairs remain out of reach.  The 2026
review says small regular holes can preserve cones and sparse holes can be
handled with intervening mixing, but also identifies concentration after
characteristic restrictions on atypical trajectories as a major open
problem.  None supplies a C24-specific full-survivor strong multiplier,
arbitrary-`R_n` F10 tail, or strong cemetery.

Official sources:

- https://arxiv.org/abs/2604.19671
- https://arxiv.org/abs/2606.10155
- https://arxiv.org/abs/2104.06947

No external theorem is promoted.

## 7. Validation

All three suites pass Python syntax, frozen dependency hashes, strict JSON,
independent exact replay and live fail-close.  They reject

```text
16/16 + 17/17 + 15/15 = 48/48
```

hostile mutations.  All six default live modes exit `2`.  Leaf and recursive
SHA chains freeze every report, certificate, verifier and manifest.

## Next shortest line

1. Attack the full-`Q_N` characteristic union directly: prove a physical
   uniform/subexponential `C_N`, or a native no-hidden-cut dwell block, with
   the concrete target `C_N a^N<1` (`N=697` or `5694` for the two frozen
   candidate constants).
2. In parallel, derive a physical analytic-margin/rank shell for `N_F10` and
   prove its global `L^1` or `L^p` face sum on the arbitrary-`R_n` same IDs.
3. Join return-wide F12 and moving-current F13; then assemble the separate
   strong cemetery and F14--F18.
4. Use the resulting regular/current/cemetery ledger to close Gate-3 dynamic
   DQ and `MT_DQ`, then assemble numerical `C_fw,C_rev,q`.
5. Continue Gate 1's same-representative cancellation/twisting route and Gate
   2's invariant stable plaque/holonomy/quotient/PPE construction as
   independent structural poles.

Strict unconditional CM2 remains `NO-GO_FOR_CLAIM`, with zero of five
composite gates fully closed and Gate-5 maturity unchanged at `7/18`.
