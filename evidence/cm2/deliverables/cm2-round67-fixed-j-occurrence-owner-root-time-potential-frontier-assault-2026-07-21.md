# CM2 Round 67 cross-gate assault: actual fixed-time root and direct time potential

Date label: 2026-07-21

Strict verdict: **one graph-supported physical subroot is now certified: at
every fixed insertion time, the moving-occurrence positive/current seed and
the owner/collar record are deterministic views of the same restricted
`m_occ` law.  This does not produce an all-time finite strong root, an actual
return/product/Green join, or a composite gate.  CM2 remains
`NO-GO_FOR_CLAIM`.**

## 1. The fixed-time physical subroot already present in the frozen chain

Let `m_occ` be the finite standard-Borel positive endpoint-coarea occurrence
law.  At a fixed insertion time `j`, the frozen owner construction is

```text
m_j^own
 =sum_a 1_(E_(j,a)^owner intersect R_(j,a)^reg) m_occ,
```

on the disjoint source-marked record union.  The owner/root map `q_j`
retains, without deduplication,

```text
restriction-id, time-j, physical-event-signature, primitive-key,
connected-rank-0, side-label, word-cell, endpoint/root coordinates.
```

Round 60 gives the exact Round-50-to-Round-54 token projection; Round 61
constructs `nu_j=(q_j)_#m_j^own` and the positive forward/reverse occurrence
cost pushforwards on that same restriction.  Therefore

```text
Omega_j={(a,x): x in E_(j,a)^owner intersect R_(j,a)^reg},
M_j=m_j^own,
```

with its identity occurrence view and deterministic owner view `q_j`, is a
graph-supported physical common root.  It is not an abstract coupling and
does not identify anonymous equal marginals.  The positive cost densities
are the RN versions

```text
C_j^f=d(q_j#(c_f M_j))/dnu_j,
C_j^r=d(q_j#(c_r M_j))/dnu_j.
```

Their fixed-`j` integrals inherit the frozen finite bounds.  This certifies
an actual occurrence/current-to-owner/collar registry edge at fixed time.
It does not certify equality with the Round-54 Jordan pair or any common-mode
identity.

## 2. Countable time carrier versus a strong all-time law

The tagged disjoint union

```text
Omega_time = disjoint_union_(j>=0) {j} x Omega_j
```

is standard Borel and retains time exactly.  That is a carrier theorem, not
a finite weighted-law theorem.  Write `r_j=dm_j^own/dm_occ`; on the declared
disjoint owner construction it is an indicator version.  For any measurable
typed positive mark `g_j`, define on the raw occurrence root

```text
H_g(x)=sum_(j>=0) w^j r_j(x)g_j(x),        w>1.
```

The time-labelled positive lift from `L1(m_occ)` into the weighted `ell1`
sum is bounded exactly when

```text
H_g in L-infinity(m_occ),
operator norm = ||H_g||_infinity.
```

This is the Round-65 positive-potential theorem specialized to the now
physical occurrence/owner root.  Fixed-time integrability of every
`r_jg_j` is not its conclusion.

For the maximal terminal-eligibility envelope

```text
r_j^elig(x)=1_{j<n(x)},       g_j=1,
```

the potential is exact:

```text
H_elig(x)=sum_(j=0)^(n(x)-1)w^j
         =(w^n(x)-1)/(w-1).
```

Thus `H_elig` is essentially bounded if and only if terminal depth `n` is
essentially bounded.  Actual owner indicators satisfy only
`r_j<=r_j^elig`; the eligibility obstruction does not by itself prove that
the smaller actual owner potential diverges.  The actual pointwise owner
pattern and the actual essential bound of `n` are not frozen.

## 3. Finite weighted mass is still not a strong root

Take `X={1,2,...}` with

```text
m{n}=3*4^(-n),       w=3/2.
```

First activate each point at exactly one time, `r_j(n)=1_{j=n-1}`, with
unit charge.  Then

```text
H(n)=w^(n-1) -> infinity,
sum_j w^j integral r_j dm = 6/5 < infinity.
```

Even disjoint time slices and a finite complete weighted vector charge do
not give an `L1` operator bound.

For the full eligibility envelope `r_j^elig(n)=1_{j<n}`,

```text
H_elig(n)=2[(3/2)^n-1] -> infinity,
integral H_elig dm = 8/5 < infinity.
```

This isolates the exact missing quantifier: an exponential terminal-depth
moment is an average statement, whereas strong assembly requires a
pointwise essential envelope or a genuine pointwise attenuation.

## 4. Minimal spanning registry after the new subroot

The existing physical namespaces can now be organized as a junction-tree
target rather than an all-pairs wish list.

```text
[moving occurrence + fixed-j owner/collar]     CERTIFIED SUBROOT
                 |
          owner-to-return graph                NOT CERTIFIED
                 |
          return-to-material atlas             NOT CERTIFIED
                 |
          return-to-stable product             NOT CERTIFIED
                 |
          stable plaque-to-Green/QNL gauge      NOT CERTIFIED
```

The first box is one node because its graph-supported common root was just
proved.  Four explicit physical crosswalks remain:

1. attach owner records to the exact first-return `R_n` path and its
   half-open component/recut owner;
2. attach that path to the all-cell/all-depth material atlas;
3. attach it to the actual collision-SRB stable product root; and
4. attach stable plaques to the combined-Green/QNL representative.

The weak branch inverse and arbitrary Rokhlin disintegration of the exact
return graph do not instantiate items 2--4.  The Round-66 stable-tree ledger
is still `0/7`, and the Gate-1 common registry is still `0/8`.

## 5. Latest technology boundary

Fresh official records were checked directly.

- `arXiv:2604.18929v3` develops transfer operators, SRB measures and product
  formulas for smooth Axiom-A diffeomorphisms through Markov partitions.  It
  assumes smooth hyperbolicity and has no billiard collision singularity,
  moving boundary or immutable occurrence/return crosswalk.
- `arXiv:2602.19435v2` certifies spectral approximations after a compactness
  or Doeblin--Fortet--Lasota--Yorke approximation bound is already supplied.
  It does not construct CM2's missing physical space, bound or registry.
- `arXiv:2601.10539v2` proves smooth martingale observables and generalized
  Feynman--Kac formulas for Hörmander Itô diffusions with boundary stopping.
  This is the wrong stochastic process and recipient.
- The current searches returned no newer same-law moving-scatterer response
  or collision-SRB stable-product theorem beyond the already audited
  sequential-CLT, small-hole and review results.

No external theorem is promoted.

## 6. Strict frontier

```text
actual fixed-j occurrence/owner graph subroot:        CERTIFIED
countable time-labelled Borel carrier:                CERTIFIED
direct H_g L-infinity iff on that root:               CERTIFIED_EXACT
eligibility geometric-potential criterion:           CERTIFIED_EXACT
finite-average versus strong-root separators:         CERTIFIED_EXACT
all-time actual owner/sector H_g in L-infinity:       NOT_CERTIFIED
owner-to-return/material/product/Green crosswalks:     NOT_CERTIFIED

Gate 1: NOT_CERTIFIED
Gate 2: NOT_CERTIFIED; official fields 0/17
Gate 3: NOT_CERTIFIED
Gate 4: NOT_CERTIFIED; landing join 1/7
Gate 5: NOT_CERTIFIED; maturity 10/18, blocks 0
complete composite gates: 0/5
CM2: NO-GO_FOR_CLAIM
```

The shortest common route has therefore advanced by one genuine edge.  The
next lawful step is to attach the fixed-time occurrence/owner subroot to the
exact return path, and to prove the pointwise all-time potential on that same
root rather than summing separately normalized fixed-time laws.
