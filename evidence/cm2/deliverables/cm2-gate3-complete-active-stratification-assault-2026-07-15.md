# CM2 Gate 3 complete active-sheet stratification assault

Date: 2026-07-15 (Asia/Shanghai)  
Arithmetic: exact rational combinatorics and 192-bit Arb  
Verdict: **all active first/miss/polarity/endpoint collars are stratified;
maximal rows and global DQ remain open**

## 1. What is closed

The predecessor left exact ambient parameter area

\[
 \frac{5263}{51200}
\]

inside source-grazing, first-visibility, miss-owner and coarea-polarity
interval collars.  Those boxes mixed genuine analytic boundaries with
interval dependency.  This pass keeps every genuine boundary and proves a
strict label on every complementary side.  The resulting active-sheet
atlas has

```text
remaining two-dimensional unresolved parameter area = 0.
```

This is not a claim that the boundary currents vanish.  Forty-eight physical
first/miss transition curves are retained as DQ faces, and the source-grazing
and polarity zero sets remain explicit strata.

## 2. Source-grazing endpoint closure

There are 64 distinct active source-target pairs.  Exact minimisation over
`|s|<=1/400` gives the uniform squared circle-separation margin

\[
 \frac{36337}{160000}>0.
\]

On `cp=0`, the common-tangent normal form satisfies

\[
 |\partial_\theta F|^2
 =|C|^2-(r_{\rm source}+\sigma r_{\rm target})^2,
 \qquad |d\theta/dz|^2\ge\frac12.
\]

Hence every active source-grazing zero set is regular, with the uniform
squared `z`-derivative bound `36337/320000`.

All 23,376 predecessor endpoint boxes are then classified without discarding
their boundary:

| endpoint stratum | boxes | ambient box area |
|---|---:|---:|
| outgoing physical half-row | 4,396 | `1099/102400` |
| outgoing side uniformly occluded | 18,980 | `949/20480` |

The 4,396 physical half-rows carry 32 strict event labels in eight exact
`Jx/Jy` four-orbits.  Their incoming side is non-outgoing.  The boundary
`cp=0` has two-dimensional parameter measure zero, and its signed parameter
coarea coefficient is exactly zero because the coefficient contains the
factor `cp`.

## 3. Coarea-polarity closure

The 816 depth-nine polarity boxes already have strict first visibility and a
strict miss owner on the whole box.  Their only ambiguity is the sign of
`u_y`.  On every box, a strict point witness proves that the analytic
function `u_y` is not identically zero.  Therefore

```text
u_y < 0 | u_y = 0 | u_y > 0
```

is an exact sign stratification; the zero set has two-dimensional measure
zero.  The two open signs carry the two correct coarea polarities.  The same
argument resolves all 768 new polarity boxes generated by the later
depth-thirteen refinement.

## 4. Exact first/miss transition universe

The owner/Voronoi registry contains 82,048 target-target common-tangent
descriptors.  Its strict whole-window pass has 48 physical earlier-occlusion
descriptors and 48 physical later-miss-switch descriptors.  The independent
forward-time resolver proves all 320 formerly unresolved descriptors
physically empty, with no physical joint vertex.

Only 48 of the 96 strict physical descriptors meet the active cross-colour
sheets:

```text
16 physical earlier first-visibility curves,
32 physical later miss-switch curves.
```

Pairwise disjoint target circles imply that equal first roots cannot occur.
Consequently first visibility can change only on the earlier curves, while
the strict next target can change only on the later curves.

## 5. Closing every residual first/miss box

Depth-thirteen refinement leaves 72,324 first/miss boxes with ambient area

\[
 \frac{18081}{1638400}.
\]

The exhaustive curve match gives:

| class | boxes |
|---|---:|
| meets no physical transition curve | 59,028 |
| meets exactly one physical transition curve | 13,296 |
| meets two or more physical transition curves | 0 |

Every boundary-free box has one strict midpoint first/miss state.  Since no
physical transition curve meets its connected box, that typed state is
constant there.  This statement is deliberately narrow: first-visibility
boxes record only visible/occluded status, not a miss owner or polarity;
miss-owner boxes record only the strict miss owner.  The separately certified
polarity stratification supplies the coarea sign.

The 13,296 one-curve boxes merge into 12,288 curve strips:

```text
48 analytic curves x 256 exact s intervals = 12,288 strips.
```

Of these, 11,280 strips use one source box and 1,008 merge two boxes meeting
at a rational `z` grid line.  Every analytic curve lies strictly inside its
merged `z` envelope on the complete `s` interval.  The two outer faces have
strict, different typed states:

- an earlier curve has one strictly occluded side and one physical-first side;
- a later curve has two physical sides with different strict miss owners.

The 48 curves cover the full parameter window in 256 adjacent strips each.
No physical transition is discarded or replaced by a numerical midpoint.

## 6. Exact chart-seam quotient

The four dominant charts overlap only at diagonal normals.  A half-open
ownership rule assigns every diagonal tie to `E` or `W`; `N` or `S` excludes
the tie.  Paired representations have the same physical normal and the same
absolute chart Jacobian, so their trace is identified rather than added.

On the 11,812 rectangular physical bulk boxes:

```text
344 boxes touch a seam,
16 complete physical seam-label keys,
12 two-chart duplicate keys,
4 one-chart owner-only keys,
96 positive-s overlap pairs,
64 prequotient components -> 52 postquotient components.
```

The ownership rule applies equally to analytic source-grazing, polarity and
first/miss strata.  The explicit count `52` concerns only the predecessor's
rectangular bulk; the complete stratified maximal-component registry has not
yet been assembled.

## 7. Precise remaining boundary

This pass removes the former unresolved-collar and chart-duplicate
obstructions.  It does **not** yet certify:

1. the maximal connected component registry after gluing every rectangular
   and analytic half-row;
2. the complete physical coarea coefficient/current row for each maximal
   component;
3. arbitrary-test DQ convergence and fixed-core/boundary tightness;
4. numeric forward/reverse recovery costs and one frozen `q` on each row;
5. stopped-parent recovery, four global Kac currents or any CM2 norm lift.

Accordingly Gate 3, Gate 4, Gate 5 and unconditional CM2 remain open.

## 8. Reproduction

```bash
PYTHONPATH=deliverables /tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_gate3_stratified_collar_closure_verifier.py \
  --replay --integrity-only

PYTHONPATH=deliverables /tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_gate3_first_miss_stratification_verifier.py \
  --replay --integrity-only

PYTHONPATH=deliverables /tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_gate3_chart_seam_quotient_verifier.py \
  --replay --integrity-only
```

Each verifier's live default mode deliberately exits `2` until maximal rows,
global DQ and scalar matching are certified.
