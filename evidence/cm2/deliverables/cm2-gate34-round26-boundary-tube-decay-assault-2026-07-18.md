# CM2 Gate 3/4 round 26 — fair-dyadic boundary-tube decay

Date: 2026-07-18  
Status: append-only strict leaf; no aggregate, root, or prior artifact modified

## Frozen conclusion

This leaf closes the quantitative **one-step unresolved-boundary** problem on
the frozen 24-core carrier.  It does two separate jobs and keeps their logical
strength distinct.

1. It freshly replays the round-25 `26,876 -> 53,752` scheduled child
   generation with the original 384-bit whole-box Arb classifier.
2. It replaces the non-iterable natural-interval heuristic by a
   dependency-neutral midpoint-plus-analytic-radius classifier and proves a
   uniform physical boundary-tube estimate.

For every fair absolute binary depth `d>=24`, put

```text
h_d = 2^(-floor(d/3)).
```

If `U_d` is the residual unresolved fair frontier, then its
parameter-averaged collision base mass satisfies

```text
M_base(U_d)
  <= B(h_d)
   = (60489/15625) h_d + (26208/25) h_d^2
   < 8 h_d.
```

Consequently

```text
M_base(U_d) = O(2^(-d/3)),
M_base(U_(24+3j)) < 2^(-5-j),
mu_collision(U_d) < (50/39) h_d.
```

The bounds are uniform for every `|s|<=1/400`, including parameter
endpoints.  Hence the limiting one-step `R1/Q1` partition exists modulo a
collision-null boundary set.  A finite complete raw branch ledger is still
not materialized.

## Fresh replay of the frozen next generation

The producer independently reconstructs every unresolved parent from the
33,960-row upstream registry, verifies that its scheduled fair next split is
the `t` split, generates both children, and freshly replays the full strict
Arb trichotomy.

| class | child count | parameter-averaged base mass |
|---|---:|---:|
| `RETURN_AT_1_INNER` | 4,088 | `5953/1024000000` |
| `SURVIVE_THROUGH_1_INNER` | 2,048 | `17319/1024000000` |
| `UNRESOLVED_OUTER` | 47,616 | `38701/256000000` |

The depths are `{13: 13,632, 16: 40,120}`.  All 26,876 parent identities,
both child paths, exact rational source boxes, complete classification
witness digests, output Arb enclosures, and exact masses enter the frozen raw
child digest.  Every pair has equal child masses summing exactly to its
parent, and globally

```text
sum child masses = 44519/256000000.
```

The newly terminal children have count 6,136 and base mass
`2909/128000000`.  The finite one-generation residual ratio is

```text
38701/44519 < 7/8.
```

This `7/8` is explicitly marked **non-iterable**: it is a fact about this one
fresh generation, not a survivor-conditioned hazard or recovery theorem.

## Regularity and local-diffeomorphism audit

All 24 source-core branches replay their complete first owner.  On every
whole parent core,

```text
0 < tau < 3,
|p_target| < 1/5.
```

Thus the selected collision is uniformly nongrazing.  The one-collision
billiard map is a reversible local diffeomorphism and preserves
`R_obstacle dtheta dp`; its absolute invariant-area Jacobian is `1`.

The parameter dependence is harmless here: source and target centers are
affine in `s`, so the derivative of their displacement with respect to `s`
has norm at most `1`.

## Dependency-neutral radius budget

Natural interval evaluation was deliberately **not** used to prove decay.
It can retain dependency overestimation even when the true image is separated
from every core face.

Instead, evaluate the exact dyadic midpoint with Arb precision increased
until each output scalar ball has radius at most `h_d/100`, and add analytic
mean-value radii.  The frozen source-root widths are at most

```text
Delta t = 1/100,  Delta p = 1/25,  Delta s = 1/200.
```

Using `|dtheta/dt|<3/2`, `|du/dp|<2`, center distance `<4`, target radius
`>=4/25`, and `|p_target|<1/5`, the output derivatives obey

```text
|partial p'/partial(t,p,s)| < (44,50,7),
|partial t'/partial(t,p,s)| < (48,55,8).
```

The exact center-to-box radius coefficients are therefore

```text
p': 99/80,
t': 34/25.
```

After reserving `h_d/100` for the midpoint Arb ball, the certified radii are

```text
eta_p = (5/4) h_d,
eta_t = (7/5) h_d.
```

These radii decrease with the actual mathematical box diameter, so Arb
dependency blow-up no longer enters the decay argument.

## Physical boundary tube and chart seams

The 24 physical destination rectangles have 48 `t` faces and 48 `p` faces.
There is no additional physical seam face.  The E/W/N/S dominant-cell seam
is only an atlas artifact: use the signed major-half-circle coordinate and
absorb any seam-side overlap into the adjacent `t`-upper-face tube.

At `d>=24`, `h_d<=1/256`; the twice-expanded tube stays in

```text
|t| <= 91/128,
dtheta/dt < 3/2,
|major normal component| > 7/10.
```

Hence the chart sign is uniformly fixed and no seam ambiguity survives.

Let the sums over all 24 destination rectangles be weighted by the radius of
their collision obstacle.  Exact enumeration gives

```text
sum R                         = 156/25,
sum R * (p width)             = 546/3125,
sum R * (t width)             = 39/625.
```

For one rectangle, the two `t` faces contribute at most
`12 R eta_t (w_p+4 eta_p)` and the two `p` faces contribute at most
`12 R eta_p (w_t+4 eta_t)`.  Summing gives exactly

```text
B(h) = (60489/15625) h + (26208/25) h^2.
```

For `h<=1/256`, its effective coefficient is

```text
995787/125000 < 8.
```

At each fixed parameter, reversibility and invariant-area preservation map
the unresolved source set injectively into this target boundary tube.  The
source base density is no larger than the invariant collision density.
Integrating the uniform parameter measure proves the displayed mass bound.
Because the global collision volume is `>156/25`, the normalized estimate is
`mu(U_d)<(50/39)h_d`.

## Exact advancement and nonpromotion

Newly certified:

- all 53,752 scheduled children freshly classified with exact mass
  conservation;
- a non-iterable finite-layer residual contraction `<7/8`;
- fair-dyadic unresolved outer mass `O(2^(-d/3))`;
- collision-null preimage of the complete physical C24 boundary;
- limiting one-step `R1/Q1` partition modulo collision-null fibres.

Still not certified:

- finite-depth exhaustion of every unresolved box;
- a finite complete raw one-step branch ledger;
- arbitrary-`n` physical `R_n/Q_n` materialization;
- survivor-conditioned recovery or a `q_n`-weighted return tail;
- complete forward/reverse restriction IDs and F10--F18 loads;
- an induced strong Lasota--Yorke coefficient;
- Gates 3, 4, 5, or CM2.

The strict global status remains `CM2 NO-GO FOR CLAIM`.

## Frozen artifacts and replay

- `cm2_gate34_round26_boundary_tube_decay_cert.py`
- `cm2_gate34_round26_boundary_tube_decay_verifier.py`
- `cm2-gate34-round26-boundary-tube-decay-manifest-2026-07-18.json`
- `cm2-gate34-round26-boundary-tube-decay-manifest-2026-07-18.sha256`

```bash
PYTHONPATH=deliverables .venv-neurips/bin/python \
  deliverables/cm2_gate34_round26_boundary_tube_decay_verifier.py \
  --integrity-only

PYTHONPATH=deliverables .venv-neurips/bin/python \
  deliverables/cm2_gate34_round26_boundary_tube_decay_verifier.py \
  --replay

PYTHONPATH=deliverables .venv-neurips/bin/python \
  deliverables/cm2_gate34_round26_boundary_tube_decay_verifier.py \
  --self-test
```

Integrity, independent replay, and `75/75` hostile tests pass.  Default live
mode exits `2` because arbitrary-`n` return branches and the strong gates
remain open.
