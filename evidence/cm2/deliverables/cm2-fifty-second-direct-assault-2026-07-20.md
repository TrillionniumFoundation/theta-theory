# CM2 Round 52 full direct assault

Date: 2026-07-20  
Strict verdict: **0/5 complete gates; CM2 NO-GO**

## Gate 4: proof-level outer rate and terminal C24 survivor

Reopening the proof of the uniform memory-loss theorem in
Stenlund--Young--Zhang (`arXiv:1210.0011v4`) exposes more numerical data than
the former black-box `C,theta` interface.  At Lipschitz exponent one, the
frozen pilot value `hat_c=20/3807` gives

```text
C_bump,outer <= 3807/10.
```

The proof line immediately before the final display contains the safe
hyperbolic term `Lambda^(-n/2)`.  With
`Lambda=180337/144000`, exact arithmetic gives

```text
Lambda^(-1/2) < 9/10.
```

The complete safe rate is therefore

```text
max((1-tilde_zeta/2)^(1/(2 Delta)),9/10).
```

Only a numerical lower bound for the compatible magnet fraction
`tilde_zeta` and an upper bound for the coupling-gap integer `Delta` remain
hidden.  Given `tilde_zeta>=zeta0>0` and `Delta<=D`, the certificate supplies
an exact integer-only search for `m` and the safe clock `H_bump=2Dm`.
Neither scalar is presently published numerically, so `H_bump` remains
existential.

Independently, a single Lipschitz outer majorant for all 24 C24 boxes now has

```text
mu_s(supp g_out) < 87603/125000000 < 1/1000,
||g_out||_infinity+Lip_1(g_out) < 7251.
```

At one common finite but nonnumerical terminal time, each normalized proper
view has C24 hit fraction below `1/500`.  On the once-charged two-view parent
law this gives a same-ID common terminal-nonhit **relative fraction**

```text
>249/250,
```

or absolute mass `>249p/250` on a parent of mass `p`.  This is a one-time
terminal predicate, not intermediate avoidance, a proper restriction, or a
proper same-ID return.  The distinct per-long-leaf crossing problem
`H_cover/beta` is also unchanged: whole-family C24 mass does not certify a
registered Cantor crossing fraction.

## Gate 4: exact defect threshold and corrected return-clock frontier

The terminal-survivor lower bound forces both Round-48 dyadic shells to zero:

```text
k_fw=k_rev=0,
C_fw,post=C_rev,post=H_joint+221328,
p<(500/499)h_sigma.
```

This eliminates the former marginal-shell obstruction and transfers the
Round-51 survivor-supported estimate to a parent-charged **ambient two-view
max-clock envelope**.  It does not yet construct the physical first-return
charge `q`.

The remaining variable preclock is controlled by

```text
I_D = integral p(y)*exp(Dbar(y)/6) dlambda(y),
Dbar(M)=0       for M<=310,
Dbar(M)=M-309   for M>=311.
```

For `T_m=nu{M>m}`, the exact layer-cake calculation gives the sharp pure
exponential threshold

```text
T_m <= C*b^m  suffices iff b<exp(-1/6),
alpha_c=1/(6 log 2)=0.2404491734814939... .
```

The fully rational quarter-block interface

```text
T_m <= C_quarter*2^(-floor(m/4))
```

already implies

```text
I_D <= nu(X)
       +11723/54477219746384227185197056*C_quarter.
```

Critical symbolic and rational atomic laws prove sharp nonimplication at the
threshold.  The physical quarter-block tail is not yet available.

Cell-level singleton reindexing is Borel, once-charged, and removes the old
group-min poisoning.  It still does not close `I_D`: a typed model with band
mass `2^-n`, `2^(n^2)` cells, leaf density `rho_j=1`, and one fixed finite D1
density `d_D1=D0` has finite D1 cell-mass moment but divergent outer counting
density and divergent defect moment.  The missing interface is therefore a
physical cell-packing/counting-density tail, not merely the existing D1
moment.

Large common mass also does not supply boundary geometry.  A two-view Borel
isomorphism with marginal survivors `999/1000` and common mass `499/500` can
fragment the pullback into intervals `A_k` separated by positive gaps.  Full
support of every positive regular density forces distinct connected carriers,
and each nonnull `A_k` contributes at least one unit to the boundary
numerator; hence every positive regular representation has `J=infinity`.
Thus a physical common-refinement boundary-`Z` estimate remains necessary.

If the physical `I_D` is later proved, it closes the total ambient max-clock
envelope and its absolutely continuous Borel-exhaustion tail.  It does **not**
by itself close physical `q` or strong cemetery.  Those still require a
proper same-ID first-return carrier, every additional recovery-clock moment,
and separately typed singular/current cemetery control.  Numerical
`C_fw,C_rev,q` also still require numerical clocks.

## Gate 5: fixed-insertion owner-tail transfer at `s=0`

Gate-5 statements remain restricted to the base parameter `s=0`.  For each
fixed insertion time `j`, over the registered arbitrary-`R_n` owner range
`j<n`, positivity and owner deletion transfer the raw occurrence rank tail
to the disjoint source-marked owner law with constant one:

```text
sum_a m_(j,a)^owner{B>b}
 <= (9158592/6875)*4^(-b),              b>=14,

integral 2^B
 < 23253221519103/880000,

bidirectional fixed-time F10 owner charge
 < 2395081816467609/880000.
```

Each finite regular suffix preserves record total mass and the retained
source-rank mark.  Because different records use different suffix maps, no
domination on one common unmarked target Borel set is claimed.  The bound is
uniform in `j` but has no decay in `j`, so summing it over insertion times is
illegal and no complete F10 field is promoted.

## Gate 5: exact face-tower and F17 obstructions

The nested-tube model

```text
Q_p=[0,rho^p]x[0,1],
Gamma={0}x[0,1]
```

has collision volume `mu(Q_p)=rho^p` but trace mass
`H^1(Gamma intersect Q_p)=1`.  A fixed rank `B=14` therefore gives constant
owner charge at every tower level even while all ordinary collision-volume
forcing is summable.  The present hypotheses allow `kappa_B>=1`, whereas the
Round-50 fixed-weight resolvent requires

```text
kappa_B < 2rho/(1+rho) < 1.
```

The exact first missing physical theorem is trace-nullity of the
collision-null never-return/cemetery set, or a quantitative trace-survivor
contraction.

Even if trace mass were granted to decay like `rho^p`, every Holder bridge
with available `q<=2` has

```text
kappa_q>=sqrt(rho)>2rho/(1+rho).
```

Thus the frozen weight needs a genuine `q>2` owner moment or sharper
cancellation/recurrence.  A weaker `w_face` route could use the certified
`q=3/2` moment only after trace-survivor decay itself is proved.

Finally, stable-curve scalar test norms do not control the transverse
Eulerian divergence current.  On

```text
U_L=[0,1/L]x[0,1], phi_L(u,s)=L*u, K=e_1,
```

the stable-curve scalar norm is one and source mass is `1/L`, while
`integral K dot grad(phi_L)=1`.  A physical F17 space must therefore be a
vector-current anisotropic graph space with explicit bulk-current and trace
injections, physical observable pairing, and a controlled suffix constant.
No complete F10, physical F17, strong F13, F14/F15/F18, or aggregate
face-tower field is promoted.  Gate 5 remains **10/18**, with **0** complete
18-field blocks.

## Latest-technology audit

The official source versions were rechecked on 2026-07-20 and remain
`1210.0011v4`, `2104.06947v3`, `2604.19671v2`, `2604.25881v1`,
`2606.10155v1`, and `1807.02330v4`.  The official
`1210.0011v4` archive and its `Moving_final.tex` member were hash-scoped
separately.  The coupling proof exposes the numerical outer prefactor and
safe hyperbolic rate used above, but not numerical `tilde_zeta,Delta`.
The sufficient-rectangle and anisotropic-space sources do not directly give
the missing C24 crossing atlas, trace-survivor contraction, physical
cell-packing tail, common-refinement boundary geometry, or typed
vector-current F17 injection.

## Strict status

```text
Gate 1: NOT CERTIFIED
Gate 2: NOT CERTIFIED
Gate 3: NOT CERTIFIED
Gate 4: NOT CERTIFIED
Gate 5: NOT CERTIFIED (10/18; complete blocks 0)
CM2:    NO-GO
```

## Validation

- Python syntax: `6/6 PASS`;
- strict JSON, dependency pins, integrity and deterministic replay: PASS;
- independent exact arithmetic and literature-scope audits: PASS;
- hostile manifest mutations:
  `98/98 + 101/101 + 104/104 = 303/303` rejected;
- certificate/verifier default modes: `6/6`, each exactly exit `2`;
- deterministic manifest regeneration: `3/3` byte-identical;
- three leaf SHA ledgers and the recursive Round-52 ledger: PASS.

The mutation suites are fail-close regression tests, not substitutes for the
mathematical proofs.  Two independent read-only audits checked the official
source formula, exact fractions, measure/index scopes, owner-time range,
common-return typing, countermodels, and every nonpromotion boundary.  Their
required corrections were incorporated before final replay.

## Next route

1. Compute a compatible numerical lower `tilde_zeta` and upper `Delta`; the
   exact interface then produces numerical `H_bump` and `H_out`.
2. Independently complete the interval-certified long-leaf Cantor atlas to
   obtain the distinct numerical `H_cover/beta` crossing interface.
3. Prove the physical quarter-block defect tail, or an equivalent
   cell-packing/counting-density estimate, on the same once-charged kernel to
   close `I_D`.
4. Put the two terminal predicates and the two-view map into one physical
   curve metric, bound common-refinement boundary `Z`, properise the common
   same-ID subkernel, and prove every additional recovery-clock moment.  Only
   then construct physical `C_fw/C_rev/q` and strong cemetery.
5. For Gate 5, prove trace-nullity or quantitative trace-survivor contraction;
   combine it with either a `q>2` owner moment at the frozen weight, a sharper
   signed recurrence, or the explicitly weaker `w_face` resolvent.
6. Construct the vector-current anisotropic graph space and its physical
   constants; then finish all-face F10, strong F13, F14/F15/F17/F18 and return
   to Gate-3 `MT_DQ`.

Gate 1 and Gate 2 remain independent structural poles.

Leaf reports:

- `cm2-gate34-round52-outer-rate-numeric-frontier-assault-2026-07-20.md`;
- `cm2-gate34-round52-defect-threshold-same-id-return-frontier-assault-2026-07-20.md`;
- `cm2-gate5-round52-owner-tail-tower-anisotropic-frontier-assault-2026-07-20.md`.
