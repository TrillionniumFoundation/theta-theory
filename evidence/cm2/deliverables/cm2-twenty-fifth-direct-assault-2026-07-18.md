# CM2 twenty-fifth direct assault: sparse C24 returns, adaptive R1 geometry, and local strong blocks

Date: 2026-07-18 (Asia/Shanghai)  
Mode: append-only, fail-closed, exact rational joins and strict Arb replay  
Parent root: `cm2-twenty-fourth-direct-assault-manifest-2026-07-18.sha256`  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; no composite
gate is fully closed (`0/5`).  This round proves a uniform unweighted
exponential return tail for the full 24-core union, materializes the first
positive-area two-dimensional R1/Q1 inner atoms, installs candidate-local
Gate-5 fields F1--F9 on all 4,216 certified R1 atoms, and anchors one exact
positive-area Gate-2 cone-product tile.  The unresolved outer cover, physical
stable product, weighted strong tail, F10--F18 and induced strong operator
remain open.**

## 1. Executive result

| Gate | New strict progress | Remaining decisive blocker | Status |
|---|---|---|---|
| 1 | one compact twisting gauge has `q>999/1000`, an exact determinant-one common frame, local canonical holonomies, one immutable loop and four nonzero wedges | the same representative is refuted as all-plaque class `H` on the connector basic set | **OPEN (`0/1`)** |
| 2 | one actual R1 atom contains a positive-area affine cone-product tile with 7/7 candidate layers | invariant stable plaques, physical `Lambda_A`, `I_A`, `pi^s`, holonomy Jacobian and compatible return strips | **OPEN (`0/17`)** |
| 3 | C24 `O1/O1'/O2`; a uniform unweighted exponential return tail; 4,216 strict R1 and 2,868 strict Q1 inner atoms | complete physical R_n/Q_n partition, residual-boundary exhaustion and common fw/rev strong restriction | **OPEN** |
| 4 | measurable induced `L1` payload plus uniform exponential collision-time return tail | branchwise strong `q_n`, weighted excursion/cemetery tail and induced strong Lasota--Yorke coefficient | **OPEN** |
| 5 | 4,216 R1 candidates carry candidate-local F1--F9 packets (`9/18`) | F10--F18, global face assembly, complete R1 and arbitrary-n blocks, three return-wide norms | **OPEN (global `4/18`)** |

## 2. Gate 1: the common-frame gap is closed only for a refuted candidate

On the same compact QNL gauge

```text
G=U_v L_u=[[1+uv,v],[u,1]],
|u|,|v|<10^-18, |uv|<10^-36,
q=1/(1+uv)>999/1000,
```

the round freezes an exact determinant-one biprojective half-density/common
frame.  This gauge already carries the selected local canonical `Hs/Hu`, the
immutable physical loop and four nonzero twisting wedges.  The join is no
longer a cross-representative splice.

The decisive obstruction is also in this same representative.  On the
frozen connector basic set,

```text
(K_n)_21 ~ -Lambda*c*d*Lambda^n,
```

so the canonical stable increment is unbounded.  The candidate therefore
has five of six local interface layers, but its all-plaque Butler--Park class
`H` layer is refuted.  Shrinking the amplitude or proving only `q>0` cannot
repair the exponent mismatch.  Gate 1 remains open.

## 3. C24 open-hole geometry and exponential return tail

For every fixed `|s|<=1/400`, the collision-SRB core mass now satisfies the
uniform exact interval

```text
147/550000 < mu_s(C24) < 29021/75000000 < 1/2500.
```

The 24 rectangular cores satisfy the Demers--Liverani sparse-hole geometry
on the common collision section:

```text
O1 and O1' complexity P0:              49,
O2 constant Ct:                       1493,
C24 stable diameter:                 <1/20.
```

An explicit global `C1` bump supported inside one axial core has

```text
||g||_C1 <2724,
mu_s(g)>21/55859375,
epsilon_hit=21/111718750.
```

Cone recovery and loss of memory give one common theorem-supplied integer
`N_open>=1` such that

```text
P_{mu_C24,s}(tau_C24^+>n)
 < (550000/147)
   (111718729/111718750)^floor(n/N_open).
```

Thus the full core union has a uniform **unweighted exponential collision-
time return tail** and a positive exponential moment.  The theorem does not
numerically evaluate `N_open`, and this statement is not a branchwise CM2
strong-`q` tail.

## 4. Full-dimensional adaptive R1/Q1 frontier

The complete 24-core source carrier, including the parameter interval, was
split by a strict 384-bit Arb trichotomy.  The frozen cover has

```text
total adaptive leaves:                         33,960,
RETURN_AT_1_INNER:                              4,216,
SURVIVE_THROUGH_1_INNER:                        2,868,
UNRESOLVED_OUTER:                              26,876,
step-one singular atoms on parent branches:         0.
```

Every leaf has positive two-dimensional `(t,p)` source area at fixed
parameter, and the leaf union covers `C24 x [-1/400,1/400]` modulo shared
owned faces.  The exact parameter-averaged base masses are

```text
return inner:      6473/256000000,
survivor inner:  123841/80000000,
unresolved outer: 44519/256000000,
total:                   273/156250.
```

The return atoms use 16 source cores and hit 16 destination cores.  On every
one of the 32 open parameter slabs, the normalized time-one return
probability is strictly larger than `1/160`.  This is an unconditional
time-zero atom, not a survivor-conditioned minorization that may be iterated
without recovery.

The residual frontier now has a frozen fair next-generation plan:

```text
26,876 unresolved parents -> 53,752 children,
total residual base mass   = 44519/256000000 on both generations.
```

All current next splits are in `t`; later fair splits cycle through `p` and
`s` by the largest normalized side rule.  The children remain unclassified
until a fresh whole-box Arb replay.  Fairness and mass conservation do not
prove finite exhaustion or a numerical residual-mass decay rate.

## 5. Gate 5: candidate-local F1--F9 on every strict R1 atom

The 4,216 strict R1 atoms first receive exact candidate-local F1--F6 packets:

```text
F1 domain and half-open ownership,
F2 one-row H0-to-H0 homogeneity table,
F3/F4 inherited prefix and suffix charts,
F5 adapted unstable inverse Jacobian <144000/180337,
F6 canonical log variation <3/200000.
```

There are `4216*6=25296` immutable F1--F6 slots.  The collision-area
Jacobian one is explicitly not used as the unstable F5 Jacobian.

The companion face leaf installs F7--F9.  At fixed `s`, one R1 atom is a
single `(t,p)` rectangle.  Both coordinates are monotone on a canonical
unstable graph, so its intersection is empty or one interval.  Hence

```text
Z_*(1_A F) <= (2000/1999) Z_*(F),
(2000/1999)*(360134800/360493663)
 =720269600000/720626832337<1.
```

This is an unnormalized **per-atom** contraction, not a bound for the union
of all 4,216 atoms or for conditioning on a tiny atom.  The four fixed-fibre
phase faces are coordinate lines.  The cone `25/9<V<29` yields

```text
F8 normalized transversality:             >1/30,
F9 individual coordinate-face C2 seminorm:    0.
```

Thus the companion contributes 12,648 F7--F9 slots, and the exact local join
is `9/18` on every certified R1 atom.  Since these atoms do not yet form the
complete true R1 partition and no arbitrary-n ledger exists, the new packets
add zero global Gate-5 credit:

```text
candidate-local R1 maturity:                        9/18,
complete 18-field R1 blocks:                            0,
global Gate-5 maturity:                     4/18 unchanged,
first missing candidate-local field: F10 coarea regularity.
```

## 6. Gate 2: one exact cone-product anchor, still no stable product

Inside the first frozen R1 row, the round installs the exact tile

```text
t=-4479/6400+u+v,
p=-31/1600+(3/2)u-(3/2)v,
|u|,|v|<=1/25600.
```

It lies strictly inside the source atom for every parameter in its slab, has
`|(dt dp)/(du dv)|=3`, and has normalized collision-SRB mass strictly above

```text
189/187432960000.
```

The `u` and `v` directions have

```text
35/12 < |dphi/dr| < 625/147,
```

so they lie strictly in the frozen unstable and stable cones.  The affine
projection `pi_aff(u,v)=(u,0)` has an exact positive area disintegration
with

```text
27/320000 < rho_aff(u) < 37827/320000000.
```

These seven layers are genuine cone/area facts on one physical R1 atom, but
the affine negative-cone leaves are not certified invariant stable
manifolds.  `pi_aff` is not the physical stable holonomy projection, and
`rho_aff` is not its conditional SRB Jacobian.  The invariant stable-product
layer count is therefore zero and all 17 official Gate-2 fields remain
false.

## 7. Induced measurable payload versus the missing strong payload

The full core union now has, modulo its singular null set:

```text
complete Borel first-return levels A_{s,n}, B_{s,n},
m_{s,n}=mu_s(A_{s,n})=mu_s(B_{s,n}),
sum_n m_{s,n}=mu_s(C24),
singular and nonreturning mass zero,
common Borel fw/rev restriction schema,
induced Perron L1 norm one.
```

Together with the new open-hole leaf, the measurable return time has a
uniform exponential tail.  This still does not produce the missing strong
payload.  In particular, the following interfaces remain unclosed:

1. a complete connected physical R_n/Q_n refinement with strict stepwise
   collision, homogeneity and hidden-recut margins;
2. numeric component masses, unstable Jacobians, distortion sums and
   additive strong charges `q_n` on that same registry;
3. common forward/reverse strong carriers and F10--F18;
4. a weighted excursion/cemetery tail `sum_{tau>n}q_tau<=C rho^n`;
5. an induced common-space Lasota--Yorke coefficient and the three
   return-wide CM2 norm intertwiners.

The old coefficient `b_core` belongs to an open restriction and is not
silently reused as an induced coefficient.

## 8. Technology check

The official arXiv API was rechecked through 2026-07-18.  The closest
available sources remain:

- arXiv:`2604.19671v2`, linear response for a fixed Sinai billiard with a
  special small boundary hole;
- arXiv:`2606.10155v1`, the transfer-operator review and its sparse-hole cone
  interfaces;
- arXiv:`2607.11180v2`, exponent-s dynamical Borel--Cantelli/waiting-time
  estimates for shrinking targets.

They support the sparse unweighted route used here, but none supplies the
CM2-specific physical stable quotient, arbitrary-n connected branch table,
F10--F18, or a branchwise weighted strong return tail for these moving phase
rectangles.  No later applicable billiard theorem appeared in the current
arXiv query.

## 9. Verification and frozen chain

This round freezes eight new leaf chains.  Independent replays give

```text
Gate-1 common frame hostile tests:                  45/45,
induced strong-payload frontier:                    76/76,
C24 open-hole geometry:                             70/70,
C24 sparse hit gap:                                 74/74,
full-core adaptive frontier:                        32/32,
Gate-2 product-base anchor:                         67/67,
Gate-5 R1 F1--F6 join:                              75/75,
Gate-5 adaptive-face F7--F9:                        91/91,
round-25 total:                                   530/530,
all-history total through round 25:             1416/1416.
```

Every leaf passes integrity, independent raw replay and self-test.  Every
default live verifier exits `2`, preserving the strict nonpromotion status.
All leaf SHA manifests, the v52 manifest, the round-24 parent root, the
round-25 aggregate and the new recursive root pass `sha256sum -c`.

Frozen aggregate:

```text
deliverables/cm2-twenty-fifth-gates12345-manifest-2026-07-18.sha256
```

## 10. Strict final boundary and next shortest path

```text
COMPOSITE GATES FULLY CLOSED:                         0/5
UNCONDITIONAL CM2:                       NO-GO FOR CLAIM

C24 UNWEIGHTED EXPONENTIAL RETURN TAIL:         CERTIFIED
FULL-DIMENSIONAL STRICT R1 INNER ATOMS:       4216 CERTIFIED
R1 CANDIDATE-LOCAL GATE-5 FIELDS:                  9/18
GATE-2 INVARIANT STABLE-PRODUCT FIELDS:            0/17
COMPLETE 18-FIELD OPERATOR BLOCKS:                     0
```

The next highest-leverage execution order is:

1. replay the frozen 53,752-child generation and seek a quantitative
   boundary-tube decay for the residual outer mass;
2. iterate the same strict machinery on Q1 survivors to materialize physical
   R_n/Q_n branches, with numeric mass/Jacobian/distortion and common fw/rev
   IDs attached at creation time;
3. fill F10 coarea regularity and the F11--F17 norm costs on those branches,
   then assemble F18;
4. use recovered-cone domination to turn the certified unweighted tail into
   the required additive strong-`q` tail;
5. derive the induced strong Lasota--Yorke coefficient independently, while
   attacking Gate 2 through an actual invariant stable-plaque registry and
   Gate 1 through a connector-compatible twisting gauge.

