# CM2 Round 50 full direct assault

Date: 2026-07-19  
Strict verdict: **0/5 complete gates; CM2 NO-GO**

## Gate 4: fixed-parameter enlarged-cover target bridge

The Round-49 target-membership gap is closed at the strongest qualitative
level supported by the official sources.  For each fixed parameter `s`, write

```text
T_s = A_s^-1 hat_T_s A_s,
m_s = inf_x sigma_min(DA_s(x)) > 0,
hat_delta_s = m_s*delta_rect > 0,
delta_rect = 119621/(41476958890128*10^90).
```

Baladi--Demers supplies a positive physical Cantor rectangle `hat_R_*` whose
solid hull lies in the physical direct-C24 target.  The proof does not claim
that `hat_R_*` itself satisfies the published cover's global `0.9` condition.
Instead, leafwise differentiation and absolute continuity select a separate
positive uniformly-dense subset `hat_P_* subset hat_R_*`.  The original
finite proper-crossing source subcover is unchanged; adjoining `hat_R_*`
enlarges the countable target registry, and finite-pair Liouville mixing plus
the crossing lemma gives a finite but nonnumerical `N_s`.  Pullback by
`A_s^-1` gives the target hit on the corresponding admissible `u`-curve
class for `T_s`.

This is a fixed-map derived lemma, not a numerical or parameter-uniform
application.  The direct-C24 quantitative requirement remains

```text
zeta_direct >= 2688/893303125,
1/332330 > 2688/893303125 > 1/332331,
(1999/32000)*(2688/893303125)=21/111718750.
```

The cited argument only gives a nonempty crossing subcurve.  It supplies no
source-mass fraction, numerical conjugacy factor, rectangle table, mixing
time, transverse width, distortion conversion or parameter-persistence
modulus.  Therefore the actual numerical `H_cover` is absent and the strict
inferable lower bound for `beta` remains **zero**.

## Gate 4: physical whole-family grouping and exact type frontier

For every fixed `|s|<=1/400` and finite return depth `n`, the physical
arbitrary-`R_n` registry is now grouped under the standard-Borel ID

```text
rn-whole-family:
  (s,component-id,b,source-interval-rank,incidence-rank-path).
```

The group contains the complete positive source interval, all deterministic
natural short cells and all deterministic image recuts.  Internal endpoints
use an oriented half-open owner.  On a fixed finite rank path,

```text
D_path = product_i(150*2^B_i) < infinity
```

uniformly bounds the branch derivative; bounded source length therefore
gives finite image length and finitely many recuts.  Lusin--Novikov handles
the finite-fibre Borel projection.  After pulling image recuts back, the two
partitions satisfy, modulo the frozen null outer boundary,

```text
sum_(k,j) K_fw(y,k,j;A)  = K_par(y,A),
sum_(k,j) K_rev(y,k,j;A) = K_par(y,A).
```

Thus the physical outer disintegration and once-charge property are now
installed.  Both boundary numerators are finite recordwise, and with

```text
M(y)=ceil(log2(1/min_j(ell_fw,ell_rev)))_+
```

one has `max(J_fw,J_rev)<=2^M p(y)`.

The exact `696`-step half-contraction gives a Borel defect `Dbar(M)` such that
after `696*Dbar(M)` orientation-specific closed evolution, both alternative
whole families are proper.  This does **not** install the literal Round-49
common proper-parent law: the raw `K_par` fibre need not be proper, and
pulling two different proper pushforwards back to the raw charge preserves
mass but not geometric `Z`-properness.  A two-proper-view pullback lemma or
one explicitly selected common proper pushforward kernel is still missing.

Even after that type repair, the total clock needs

```text
integral p(y)*exp(Dbar(y)/6) dlambda(y) < infinity,
```

for which `integral p(6/5)^Dbar<infinity` is a rational sufficient input.  A
same-kernel tail `mass{M>m}<=C_len*2^-m` would give the exact layer-cake bound
`integral p(6/5)^M<=mass_total+C_len/2`, but no current physical artifact
proves this tail.  An exact standard-Borel partition countermodel shows that
Round-35's D1 `L^(6/5)` rank moment alone cannot imply the inverse-length
moment.  Finite-rank/minimum-length exhaustion gives only a weak `L1`
cemetery; strong trace/current cemetery remains open.

## Gate 5: owner-aware boundary `Z_B` and F17 frontier

The five physical face grammars are now assembled into a global
standard-Borel owner-deduplicated, positive marked/total-variation boundary
kernel schema.  Owner minimisation is performed only within one physical
event signature; different events are never merged, while corners and
simultaneous events go to cemetery first.  The resulting `Z_B^owner` is a
well-defined extended-valued Borel functional.  It is finite on every fixed
regular record, but finiteness after summing all depths is not claimed.

The available positive homogeneity majorant has the sharp softened-weight
threshold

```text
M_theta < infinity  iff  0<=theta<1/2.
```

At the physical F10 weight `theta=1`, every high-strip majorant term is above
`2448`; the existing positive ledger therefore diverges.  This proves only
that the available positive upper bound fails, not that the signed physical
current diverges.  Face-specific cancellation/sparsity or a different
anisotropic trace norm is required.

With

```text
rho=(111718729/111718750)^9148,
w_Z=(1+rho^-1)/2,
```

a future recurrence

```text
b_(p+1)<=kappa_B*b_p+A_B*z_p+C_B*m_p
```

has the exact weighted resolvent provided

```text
kappa_B < 2*rho/(1+rho),
0.99914 < 2*rho/(1+rho) < 0.99915
```

(approximately `0.999140216`).  No such finite full-weight `kappa_B` is
currently certified, and collision-time conversion still needs numerical
`N_open`.

For one regular suffix `S`, the branch-adapted norm
`||phi||_(dyn,S)=||phi o S||_C1` gives an exact dual pushforward isometry
`C_dyn=1`.  It lies below the formal quarter and unit thresholds, but the
norm depends on the suffix.  The exact area-preserving diagonal family
`diag(L,L^-1)` keeps Piola flux invariant while making a common physical C1
pullback grow like `L`.  Hence no branch-uniform physical CM2 test embedding,
complete F17, full F10 or strong F13 follows.  Gate 5 remains **10/18**, with
**0** complete 18-field blocks.

## Latest technology audit

Official sources were rechecked.  The relevant current versions remain
`2604.25881v1`, `2604.19671v2`, `2606.10155v1`, `2104.06947v3` and
`1807.02330v4`.  The official source archives for `2604.25881v1` and
`1807.02330v4` were hash-pinned and their relevant proof lines were audited.
They support the fixed-parameter qualitative adjunction above, but publish no
effective constants sufficient for numerical `H_cover/beta`, no common
proper-law repair, and no full-weight owner-`Z_B` contraction or common F17
test space.

## Strict status

```text
Gate 1: NOT CERTIFIED
Gate 2: NOT CERTIFIED
Gate 3: NOT CERTIFIED
Gate 4: NOT CERTIFIED
Gate 5: NOT CERTIFIED (10/18; complete blocks 0)
CM2:    NO-GO
```

## Validation

- syntax: `6/6`;
- duplicate-key/non-finite-constant rejecting strict JSON with exact top-level
  schemas: `3/3`;
- verifier integrity/replay and independent exact arithmetic: PASS;
- hostile manifest mutations: `72/72 + 84/84 + 82/82 = 238/238` rejected;
- cert/verifier default fail-close: `6/6`, each exactly exit `2`;
- deterministic manifest regeneration: `3/3` byte-identical;
- three leaf SHA ledgers and the recursive assault ledger: PASS.

The hostile counts are verifier mutation tests, not independent mathematical
proofs.  The literature quantifiers, exact fractions and object-type joins
were separately audited.  That audit caught and repaired the overstrong
target-density wording and rejected the attempted raw-parent/common-proper-law
promotion before this final replay.

## Next route

1. Materialise the enlarged-cover physical rows: numerical conjugacy factor,
   target rectangle, finite proper-crossing source subcover, effective mixing
   constants, transverse width and distortion.  Prove interval persistence
   over `|s|<=1/400` to obtain a real uniform `H_cover` and source fraction.
2. Prove a Borel two-proper-view pullback lemma, or choose one common proper
   pushforward kernel and transport the other orientation's survivor
   predicate to it.
3. On that same kernel, certify `mass{M>m}<=C_len*2^-m` or directly the
   `exp(Dbar/6)` moment.  Then join the preclock, actual `H_cover/beta` and the
   Round-49 `W_r` theorem to build physical `C_fw/C_rev`.
4. Close a proper same-ID forward/reverse intersection, collision time `q`
   and strong cemetery.
5. For Gate 5, replace the divergent positive `theta=1` strip sum by a
   face-specific cancellation/sparse-incidence theorem and prove
   `kappa_B<2rho/(1+rho)`; use the resolvent to obtain the face-tower depth
   moment.
6. Construct one common billiard anisotropic/dynamic test space with the
   displayed embedding thresholds, then close full F10/F17, strong F13,
   F14/F15/F18 and Gate-3 `MT_DQ`.

Gate 1 and Gate 2 remain independent structural poles throughout.

Leaf reports:

- `cm2-gate34-round50-same-cover-target-adjunction-assault-2026-07-19.md`
- `cm2-gate34-round50-physical-whole-family-grouping-assault-2026-07-19.md`
- `cm2-gate5-round50-owner-boundary-zb-f17-frontier-assault-2026-07-19.md`
