# CM2 Gate 3/4 round 27 — composed-flight time-two boundary tube

Date: 2026-07-18  
Status: append-only strict leaf; no aggregate, root, or prior artifact modified

## Frozen conclusion

This leaf upgrades the round-26 finite `Q1 -> time 2` outer cover to a
quantitative limiting theorem on the **complete physical Q1 carrier**.

For fair absolute binary depth `d>=60`, put

```text
h_d = 2^(-floor(d/3)).
```

If `U2_d` is the unresolved composed-flight frontier, then, uniformly for
`|s|<=1/400` including both parameter endpoints,

```text
M_base(U2_d) < 160 sqrt(h_d),
mu_collision(U2_d) < 26 sqrt(h_d).
```

Consequently

```text
M_base(U2_d) = O(2^(-d/6)),
M_base(U2_(60+6j)) < 2^(-2-j),
mu_collision(U2_(60+6j)) < 2^(-5-j).
```

Together with the round-26 limiting physical `R1/Q1` partition, this proves
that the full physical `R2/Q2` split exists modulo a collision-null set.
It does not materialize a finite complete branch ledger.

## Why the depth-16 zero is not a physical emptiness claim

The frozen round-26 finite replay remains unchanged:

```text
Q1 parents:                         2,868
time-two leaves:                  416,994
strict R2 admitted at depth 16:         0
strict Q2 admitted:              114,006
unresolved outer:                302,988
```

The new theorem removes the limiting unresolved boundary mass.  It does not
turn the finite-depth admission count `0` into a claim that the exact physical
R2 set is empty, and it does not invent R2 atoms absent from the frozen replay.

## Complete second-owner singularity audit

The eight physical source charts contain `448` retained candidate entries
with multiplicity:

| source -> target | entries |
|---|---:|
| G -> G | 140 |
| G -> W | 88 |
| W -> G | 104 |
| W -> W | 116 |

Their exact radius weight is

```text
sum R_source R_target = 20108/625.
```

At an exact dominant-chart seam the theorem uses the union of the two
adjacent complete candidate tables.  Thus the seam is an atlas artifact, not
a promoted physical singularity.

The global boundary separation between distinct obstacle lifts is strictly
larger than `9/50`:

```text
G-G gap = 7/25,
W-W gap = 17/25,
G-W center distance > 7/10,
G-W boundary gap > 7/10 - 13/25 = 9/50.
```

Therefore, away from candidate tangency, a future root cannot cross zero and
near roots belonging to distinct obstacles cannot tie: their contact points
would otherwise violate the `9/50` boundary gap.  The global physical
horizon theorem supplies an actual owner before time `3`; the composed
classifier does not need a separate interval test of `selected_root<3`.

## Dependency-neutral composed derivative budget

The round-26 first-flight theorem supplies, on every physical source core,

```text
partial p1 / partial(t,p,s)       < (44,50,7),
partial theta1 / partial(t,p,s)   < (48,55,8),
|p1| < 1/5.
```

Using `d(arcsin p1)/dp1<21/20`, the reflected time-one direction has
derivative bounds `(95,108,16)`.  The time-one contact has bounds
`(18,20,4)`.  Exact enumeration of all retained candidates gives center
distance squared at most `13`, hence candidate-center to contact distance is
strictly below `4`.

For every second candidate impact coordinate `p2*`, this gives

```text
partial p2* / partial(t,p,s) < (2488,2825,432).
```

On a fair depth-d source box the exact center-to-box radius coefficient is

```text
2488/200 + 2825/50 + 432/400 = 3501/50,
```

and after the adaptive Arb midpoint reserve it is strictly below `71 h_d`.

The line projection has radius coefficient `4481/400`.  Since
`sqrt(142)<12` and every target radius is at most `9/25`, the square-root
modulus for a near root is below `6 sqrt(h_d)`.  At `d>=60`, two such radii
sum to less than the `9/50` obstacle gap, so root sign and root order are
strictly decided outside the tangency tube.

The second collision normal obeys the global chord estimate

```text
radius(n2) < 18 sqrt(h_d),
radius(p2) < (1/10) sqrt(h_d).
```

No natural interval dependency estimate is used for the decay theorem.

## Candidate-tangency tube

An unresolved discriminant box lies in

```text
abs(abs(p_candidate)-1) <= 142 h_d.
```

At `d>=60` this is smaller than `1/16`.  If `D` is the distance from the
current contact to a distinct candidate center, then

```text
D >= R_target + 9/50,
|d(cross(u,D))/d(angle)| > 1/4
```

throughout this band, for both radii `4/25` and `9/25`.  Integrating in the
four source angular cells, using `pi/2<11/7`, gives

```text
tangency mass
  < (3539008/4375)(142 h_d)
  = (502539136/4375) h_d
  < 113 sqrt(h_d).
```

This bound covers all candidate tangencies, including candidates that are
not the physical next owner.  No empirical depth-16 histogram is iterated.

## Time-two C24 face tube

The exact radius-weighted C24 widths remain

```text
sum R * p_width = 546/3125,
sum R * t_width = 39/625,
sum R           = 156/25.
```

With `eta_t=18 sqrt(h_d)` and `eta_p=(1/10)sqrt(h_d)`, the physical face
tube has

```text
B2(r) = (23634/625) r + (134784/125) r^2,
r = sqrt(h_d).
```

At `r<=1/1024`, the expanded chart stays where `dtheta/dt<3/2`, and

```text
B2(r) < (194337/5000) r < 39 r.
```

The time-two dominant-chart seam adds no physical component: any unresolved
seam overlap is already contained in the adjacent diagonal-core `t`-face
tubes.  Reversibility and preservation of `R dtheta dp` transfer the target
face-tube bound back to the source Q1 carrier.

Adding the tangency and core-face bounds and keeping rational slack yields
the displayed `160 sqrt(h_d)` base-mass estimate.  Since the total collision
volume is greater than `156/25`, its normalized coefficient is

```text
160 / (156/25) = 1000/39 < 26.
```

## Exact advancement and nonpromotion

Newly certified:

- a dependency-neutral composed-flight boundary-tube rate
  `O(2^(-d/6))`;
- complete handling of second discriminants, root signs, root ordering,
  chart seams, and physical C24 faces;
- collision-null limiting composed singularity/face preimage;
- the complete physical limiting `R2/Q2` partition modulo collision-null.

Still not certified:

- a finite complete R2/Q2 branch ledger;
- any claim that the physical R2 set is empty;
- arbitrary-n `Rn/Qn` materialization;
- branchwise numerical mass/Jacobian/distortion and strong `q_n` loads;
- survivor-conditioned recovery or a strong-q weighted tail;
- an induced strong Lasota-Yorke coefficient;
- Gate 3, Gate 4, Gate 5, or CM2.

The strict global status remains `CM2 NO-GO FOR CLAIM`.

## Frozen artifacts and replay

- `cm2_gate34_round27_time2_boundary_tube_cert.py`
- `cm2_gate34_round27_time2_boundary_tube_verifier.py`
- `cm2-gate34-round27-time2-boundary-tube-manifest-2026-07-18.json`
- `cm2-gate34-round27-time2-boundary-tube-manifest-2026-07-18.sha256`

```bash
PYTHONPATH=deliverables .venv-neurips/bin/python \
  deliverables/cm2_gate34_round27_time2_boundary_tube_verifier.py \
  --integrity-only

PYTHONPATH=deliverables .venv-neurips/bin/python \
  deliverables/cm2_gate34_round27_time2_boundary_tube_verifier.py \
  --replay

PYTHONPATH=deliverables .venv-neurips/bin/python \
  deliverables/cm2_gate34_round27_time2_boundary_tube_verifier.py \
  --self-test
```

Integrity, independent rational audit, producer replay, and `92/92` hostile
tests pass.  Default live mode exits `2` because arbitrary-n branches and the
strong gates remain open.
