# CM2 twenty-sixth direct assault: limiting R1/Q1, fresh time-two cylinders, and F11/F12

Date: 2026-07-18 (Asia/Shanghai)  
Mode: append-only, fail-closed, exact rational ledgers and 384-bit Arb replay  
Parent root: `cm2-twenty-fifth-direct-assault-manifest-2026-07-18.sha256`  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; no composite
gate is fully closed (`0/5`).  This round closes the one-step physical
`R1/Q1` partition modulo a collision-null boundary, freshly replays all
53,752 planned children, materializes 114,006 strict `Q2` inner cylinders on
the frozen `Q1` inner carrier, and raises the candidate-local Gate-5 maturity
on every certified `R1` atom from `9/18` to `11/18`.  It does not certify a
complete `R2/Q2` partition, an arbitrary-`n` branch ledger, a strong weighted
tail, or an induced strong Lasota--Yorke coefficient.**

## 1. Executive result

| Gate | New strict progress | Remaining decisive blocker | Status |
|---|---|---|---|
| 1 | round-25 exact common frame and `q>999/1000` remain frozen | the same representative still fails connector class `H` | **OPEN (`0/1`)** |
| 2 | the positive-area affine cone-product anchor remains valid | invariant stable plaques and the 17-field physical stable-product interface | **OPEN (`0/17`)** |
| 3 | limiting physical step-one `R1/Q1` modulo collision-null; 114,006 strict `Q2` inner leaves | complete arbitrary-`n` `R_n/Q_n`, hidden-recut margins, and common fw/rev strong restrictions | **OPEN** |
| 4 | the uniform unweighted exponential return tail remains valid; a second-step survivor subledger is now physical | numeric strong `q_n`, weighted excursion/cemetery tail, and induced strong coefficient | **OPEN** |
| 5 | candidate-local F11/F12 added on all 4,216 R1 atoms, giving `11/18` | F10, F13--F18, complete R1 and arbitrary-`n` operator blocks | **OPEN (global `4/18`)** |

## 2. Fresh replay of the frozen 53,752 children

The round-25 split plan contained 26,876 unresolved parents and exactly two
fair children per parent.  Every child has now been rebuilt from its parent
source box and freshly classified by the whole-box 384-bit Arb trichotomy:

```text
children:                                  53,752
RETURN_AT_1_INNER:                          4,088
SURVIVE_THROUGH_1_INNER:                    2,048
UNRESOLVED_OUTER:                          47,616
```

Their parameter-averaged base masses are

```text
new return inner:                 5953/1024000000
new survivor inner:              17319/1024000000
residual unresolved:              38701/256000000
parent unresolved total:          44519/256000000
```

The identity is exact.  Child paths are precisely `w0,w1`, their interiors
are disjoint, their union is the parent modulo the shared owned face, the
first-collision owner proof is inherited only by subset restriction, and
mass is conserved separately for every parent.

The observed single-generation ratio satisfies

```text
38701/44519 < 7/8,
5818/44519  > 1/8.
```

This ratio is explicitly typed as **noniterable**.  It is not used as a
uniform survivor contraction and is not promoted to a weighted tail.

## 3. Dependency-neutral boundary-tube decay and limiting step-one partition

Natural interval evaluation near a core face suffers dependency blow-up and
cannot by itself prove residual decay.  The independent boundary leaf instead
uses midpoint values plus analytic Lipschitz radii.  On the complete frozen
one-step carrier it proves the strict derivative budgets

```text
output p' partials in (t,p,s):       < (44,50,7),
target-chart t partials in (t,p,s):  < (48,55,8).
```

Let

```text
h_d = 2^(-floor(d/3)).
```

For every fair frontier of absolute binary depth `d>=24`, the unresolved set
is contained in explicit outer tubes around the 48 physical `t` faces and 48
physical `p` faces of the 24 cores.  Chart seams are atlas artifacts and are
absorbed into this physical cover.  Exact integration gives

```text
M_base(U_d) <= B(h_d) < 8 h_d,
mu_collision(U_d) < (50/39) h_d,
M_base(U_(24+3j)) < 2^(-5-j).
```

Hence `M_base(U_d)=O(2^(-d/3))`.  Core-boundary preimages have collision
measure zero, so the limiting physical one-step `R1/Q1` partition exists
modulo a collision-null set.  This is stronger than finite-depth numerical
exhaustion, but it remains a **one-step** statement: no arbitrary-time
singularity-growth or survivor-conditioned recovery constant is inferred.

## 4. Strict propagation of the frozen Q1 inner carrier to time two

The 2,868 round-25 `SURVIVE_THROUGH_1_INNER` parents were propagated from
their original `(t,p,s)` source boxes.  The certificate does not reuse a
float midpoint image.  On every admitted descendant it:

1. recomputes the first collision and physical outgoing state with 384-bit
   Arb;
2. identifies a strict target chart;
3. translates and compares the complete retained eight-chart candidate
   table to prove a unique second-collision owner;
4. tests the second image against all 24 cores;
5. bisects every unresolved source box fairly through total depth 16.

The resulting prefix-free cover contains

```text
time-two terminal leaves:                    416,994
RETURN_AT_2_INNER:                                 0
SURVIVE_THROUGH_2_INNER:                     114,006
UNRESOLVED_TIME2_OUTER:                      302,988
```

The exact base-mass ledger is

```text
frozen Q1-inner source mass:             123841/80000000
strict Q2-inner mass:               5257799/5120000000
time-two unresolved mass:               106721/204800000
R2-inner mass certified at depth 16:                    0
```

Thus the admitted `Q2` mass is exactly `5257799/7925824` of the frozen Q1
inner mass, and the three typed masses sum exactly to the parent mass.  The
zero admitted `R2` count means only that no whole descendant box through
this finite depth was proved strictly inside a core.  It is **not** a proof
that the physical `R2` set is empty.  The time-two leaf therefore freezes a
large physical `Q2` inner subledger plus an exact unresolved outer mass, not
a complete `R2/Q2` partition.

## 5. Gate 5: F11 and F12 advance, while F10 and F13 are localized

Every one of the 4,216 strict R1 atoms receives two further candidate-local
slots:

```text
F11 dynamic C^alpha pullback bound:       <158,
F12 four fixed-fibre phase faces,
    C1 sum-norm bound:                    <159.
```

There are 4,216 slots of each type, or 8,432 new slots.  The exact atom and
homogeneous-subbranch IDs join the pre-existing F1--F9 packets, so the local
maturity becomes

```text
candidate-local maturity per R1 atom:       11/18,
complete 18-field R1 blocks:                    0,
global Gate-5 maturity:             4/18 unchanged.
```

F10 is now the first precisely localized missing field.  The coarea bounds
on the 64 physical occurrence faces live on physical hit/miss carriers.  The
4,216 rectangular R1 atoms have 16,864 artificial coordinate-face
incidences.  The required

```text
occurrence_id -> atom_id -> physical face_id
```

join has cardinality zero.  A zero-speed artificial coordinate face is not
silently relabeled as a physical coarea face.

F13 is also still absent.  Among the frozen guards, 352 incidences touch
`s0`, 176 own `s0`, and none contains `s0` in relative interior.  A half-open
ownership convention is not a DQ atlas, and the physical hit/miss trace to
R1-face join remains empty.  F14--F18 therefore retain zero slots.

## 6. What changed in the proof frontier

The decisive new fact is not merely that one more finite split happened.
The one-step unresolved boundary has a uniform dependency-neutral tube bound
and therefore disappears in collision measure at the fair limit.  This
closes the physical `R1/Q1` measurable partition interface that remained open
after round 25.

The next obstruction moves one full level deeper.  To turn the current
time-two subledger into arbitrary `R_n/Q_n`, the proof must combine:

1. stepwise complete next-owner comparisons on survivor boxes;
2. new singular/core boundary-tube estimates after every composed flight;
3. survivor-conditioned admissibility and recovery constants uniform in
   time and parameter;
4. numeric branch mass, unstable Jacobian, distortion and additive strong
   `q_n` on the same immutable IDs;
5. common forward/reverse strong restrictions and physical F10/F13 faces.

The round-25 unweighted exponential tail remains valid, but it cannot replace
these branchwise strong interfaces.

## 7. Verification and chain policy

The three independently produced round-26 leaves were audited a second time
without modifying them.  Their SHA chains, integrity checks, independent
replays, self-tests and live fail-closed exits all agree.  The fourth
time-two leaf uses the same policy: frozen dependency hashes, strict JSON
parsing, full producer replay and hostile mutation tests.

```text
round-26 hostile tests:              297/297
historical cumulative hostile tests: 1713/1713
live verifier exit code:                    2
```

All new leaves are append-only.  The round-25 root and every older frozen
artifact remain unchanged.

## 8. Technology check

The official arXiv API was rechecked for recent dispersing-billiard,
small-hole, transfer-operator and escape-tail work.  The closest applicable
items remain arXiv:`2604.19671v2` and arXiv:`2606.10155v1`.  They support the
sparse unweighted open-system route already used in round 25, but no current
theorem directly supplies this pilot's composed physical branch registry,
strong `q_n` weights, common forward/reverse restrictions, or physical
F10/F13 face joins.  No external result is used to bypass those missing
interfaces.

## 9. Strict verdict and shortest next route

Strict status after round 26:

```text
CM2:                                      NO-GO FOR CLAIM
fully closed composite gates:                         0/5
Gate 2 official fields:                              0/17
Gate 5 global maturity:                              4/18
Gate 5 candidate-local R1 maturity:                 11/18
complete arbitrary-n R_n/Q_n partition:     NOT CERTIFIED
strong q-weighted return tail:               NOT CERTIFIED
induced strong Lasota-Yorke coefficient:      NOT CERTIFIED
```

The highest-leverage next route is:

1. replace the finite-depth time-two outer cover by a composed-flight
   boundary-tube theorem and close the limiting `R2/Q2` split;
2. iterate that construction with explicit singularity-growth and
   survivor-conditioned recovery constants to obtain arbitrary `R_n/Q_n`;
3. attach mass, Jacobian, distortion, fw/rev IDs and strong `q_n` when each
   branch is created, rather than joining them afterward;
4. build physical occurrence-to-Rn-face joins for F10 and DQ trace atlases
   for F13, then derive F14--F18;
5. prove the weighted excursion/cemetery tail and only then derive the
   induced strong Lasota--Yorke coefficient on the common two-view space.

Until those interfaces exist on one immutable physical registry, the CM2
claim remains fail-closed.
