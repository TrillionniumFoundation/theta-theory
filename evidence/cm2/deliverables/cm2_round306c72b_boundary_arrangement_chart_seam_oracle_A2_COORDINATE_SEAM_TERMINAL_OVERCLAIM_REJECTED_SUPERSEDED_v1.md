# Rejected / superseded C72b A2 stage

The interrupted `c72b-build-a2.v1` stage has no result object and is not
consumable.  Its draft row semantics treated the compact outgoing-chart seam
`H1=0` as a cemetery/source-grazing terminal without a frozen authority rule.
That is not established: the seam is ordinarily coordinate glue, not a
physical terminal.  The stage was terminated and must never be completed,
renamed, overwritten, or used for credit.  A successor must either prove the
physical cross-chart downstream route on the graph or leave the graph stratum
explicitly unresolved / hand it to a fully sealed next decider.
