# CM2 Round240 — Remote Sheet-to-Interface Strict Corridor Incidence

## Result

- Closed all `12/12` Round230 rows classified as `FULL_SIGNATURE_REMOTE_WITHOUT_EXACT_POSITIVE_AREA_FACE_OVERLAP`.
- Each row has a deterministic depth-2 `t`-lane inside the retained root.
- Every lane has strict positive 3D coordinate volume.
- Every lane has strict positive 3D overlap with its original Round208 region carrying direct Round211-sheet incidence.
- Every lane meets the resolved sibling on an exact positive-area `s`-interface patch.
- Interval recomputation certifies the same complete 10-field return signature on the entire lane.
- The strict `F`, `HPLUS`, and `HMINUS` signs equal the originating Round208 region signs.

## Incidence Delta

- New occurrence-to-known-block incidences: `12`.
- Distinct reached Round225 known blocks: `12`.
- Distinct exact keys touched: `8`.
- Pre-Round240 incidence: `35,896 / 53,968`.
- Post-Round240 incidence: `35,908 / 53,968`.
- Remaining unattached occurrences: `18,060`.

## Remaining P2 Frontier

- `8,500` Round239 interfaces have no Round225 block reference.
- Candidate-key multiplicity: `6,268` one-key, `2,216` two-key, `16` three-key.
- These interfaces require new event-bearing retained/common-refinement materialization.
- Exact-key equality alone remains forbidden as physical glue.

## Verification

- Independent verifier status: `PASS_INDEPENDENT_ROUND240`.
- The verifier does not import or execute the Round240 producer.
- Two hash seeds reproduce byte-identical producer and verifier output.

## Strict Boundary

- Known-block membership assignment credit: `0`.
- Physical-component credit: `0`.
- Maximal-component credit: `0`.
- Global exact-key fibre credit: `0`.
- Gate5 remains `10/18`.
- CM2 remains `NO-GO_FOR_CLAIM`.
