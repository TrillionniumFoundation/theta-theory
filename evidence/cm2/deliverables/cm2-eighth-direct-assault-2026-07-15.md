# CM2 eighth full direct assault

Date: 2026-07-15 (Asia/Shanghai)  
Frozen baseline: v51/v52 unchanged  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; composite
gates remain `0/5`**

## 1. Executive result

This pass found and corrected a real collision-coordinate scale error in the
seventh-round current.  With `p=sin(phi)`, the invariant collision law is
`dr dp`; therefore the correct 64-row positive law is

\[
 dm_e=R_{\rm source}\frac{c_p|u_y|}{\ell_T}\,d\theta,
\]

not the superseded formula with `cp^2`.  The old numerical TV bound
`16128/5 Z_N^-1` remains valid, all 32 `Jx` scalar pairs remain exact, and
the complete Gate-4/5 structural layer has been rebound to the corrected
measure.

The corrected finite event atlas now closes the complete **depth-one**
fixed-gauge transfer difference quotient for `C^1` source densities against
`C^{1,alpha}` static tests.  A separate cumulative-mass dyadic algebra
removes the fat-Cantor restriction obstruction inside a declared stopped
policy, with exact normalization cost `2^K` and endpoint cemetery mass
`2^(1-L)`.  Every source row is stable-oriented, every miss image is
unstable-oriented, and all raw slopes are strictly bounded by `29`.

These are substantial geometric/operator advances, but they do not supply
the iterated `MT_DQ`, full `C_fw,C_rev`, recovery moment or CM2 norm lifts.

## 2. Gate table

| gate | strongest certified layer after this pass | exact remaining hard gap | strict status |
|---|---|---|---|
| 1. holonomy/QNL | unchanged exact natural-holonomy obstruction and pilot QNL data | physical same-carrier holonomy transport | **OPEN** |
| 2. physical PPE | unchanged stopped-parent bridge and finite benchmarks | full-mass physical quotient/kernel, spatial Frostman and normalized amplitude | **OPEN** |
| 3. global event/DQ | corrected 64-row current; smooth-core derivative; complete depth-one fixed-gauge DQ; regular-radical tightness; scalar matching | iterated dynamic-`C^1` branch records and full three-space `MT_DQ` | **OPEN** |
| 4. same-occurrence `q` | corrected common `m_e`; dyadic stopped algebra; all-row orientation; slope subcharge `29m_e` | full homogeneity-weighted `C_fw,C_rev` and stopped recovery moment | **OPEN** |
| 5. fixed/full transfer | corrected 128-coordinate finite-Borel Kac layer and four-term algebra | physical prefix/suffix constants and three CM2 norm lifts | **OPEN** |

## 3. New exact certificates

### 3.1 Corrected depth-one DQ

The certificate replays all 64 maximal rows and proves:

```text
corrected current-row digest
  5c03da290697ac25b814848c5aee50b22387866f9d72303a60649466cad896bd

uniform |partial_phi Delta| lower bound
  4/125

regular radical collar
  O(rho^1/2)

complete depth-one quotient
  (P_s-P_0)h/s -> D_0 h in (C^{1,alpha})*
```

The limiting defect is exactly the finite smooth-core derivative plus the
corrected hit-minus-miss current.  Source-grazing and polarity endpoints
have zero density; first/miss endpoints have zero one-dimensional base mass;
chart/lift duplicates cancel before absolute values.

### 3.2 Controlled stopped algebra

For the normalized cumulative mass coordinate `u_e`, depth-`K` atoms have
exact mass `2^-K m_e(row)`.  Every admitted restriction is a finite union of
intervals, has normalization cost at most `2^K`, and transports through both
occurrence views with the same atom record.

Trimming `2^-L` at each endpoint leaves exact core mass
`1-2^(1-L)` and exact cemetery mass `2^(1-L)`.  At `K=8,L=4`, each row has
224 core atoms, so all 64 rows have 14,336 core atoms and normalization cost
at most 256.

### 3.3 Global slope envelope

The exact formulas are

\[
 \frac{d\varphi_-}{dr_-}
 =-\kappa_S-\frac{c_{p,S}}{\ell_T},
 \qquad
 \frac{d\varphi_+}{dr_+}
 =\kappa_M+\frac{c_{p,M}}{t_M-\ell_T}.
\]

Global disk separation gives

\[
 t_M-\ell_T>\frac{36337}{800000},
\]

and hence

\[
 -\frac{65}{4}<\frac{d\varphi_-}{dr_-}< -\frac{25}{9},
 \qquad
 \frac{25}{9}<\frac{d\varphi_+}{dr_+}
 <\frac{4108425}{145348}<29.
\]

Thus `q_e^slope=29m_e` is one certified numeric subcharge per occurrence.
It is not the final standard-family/recovery charge.

### 3.4 Corrected Kac rebind

The new ledger has, on the corrected law:

```text
64 corrected common m_e laws;
64 symbolic q_e=max(C_fw,C_rev,2)m_e expressions;
64 numeric slope-only q_e^slope=29m_e subcharges;
64 controlled mass coordinates;
128 singular Kac coordinates;
64 shared (+1,-1) marks;
32 exact Jx scalar pairs.
```

The finite-Borel endpoint adjoint, Kac tower, prefix/suffix pairing and
four-term centred algebra replay unchanged after the scale correction.

## 4. Latest technology check

Fresh arXiv API queries on 2026-07-15 found no newer theorem closing the
remaining moving-scatterer recovery or iterated-DQ layer.  The closest result
remains `arXiv:2604.19671v2`, *Linear response for Sinai billiards with small
holes*.  Its hole-image standard-family geometry does not supply the present
all-row `C_fw,C_rev`, the dyadic depth/recovery moment, or the three CM2 norm
intertwiners.

## 5. Exact next target

The next attack is now narrower:

1. refine every depth-`K`, trim-`L` atom by physical homogeneity rank;
2. compute log-density, curvature, inverse-chart and test-pullback costs;
3. prove
   `E_q[2^K exp(gamma(R_fw+R_rev))]<infinity`;
4. promote depth-one DQ to fixed-time branch-record `MT_DQ` and then a
   summable two-time CM2 majorant;
5. export the corrected Kac layer through physical prefix/suffix and all
   three norm lifts.

The detailed proof and exact remaining boundary are in
`cm2-gate3-depth-one-dq-controlled-stopped-assault-2026-07-15.md`.

## 6. Validation status

All four new certificate/verifier pairs:

- compile under Python 3;
- pass integrity-only and mutation self-tests;
- replay their complete dependency-bound results;
- fail closed with live default exit `2` while the remaining physical layers
  are absent.

The eighth-round SHA chain binds the new manifests, reports and the unchanged
seventh-round aggregate manifest.
