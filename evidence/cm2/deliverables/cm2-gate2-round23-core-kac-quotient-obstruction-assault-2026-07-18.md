# CM2 Gate-2 round-23 core/Kac quotient obstruction

Date: 2026-07-18  
Status: append-only strict leaf; no prior artifact modified.

## Verdict

Round 23 adds valuable physical precursors but no genuine stable-quotient
field. Gate 2 remains exactly `0/17` and `NOT_CERTIFIED`.

The audit binds the following frozen facts:

- 24 positive compact collision cores with collision-SRB mass strictly above
  `147/550000`;
- 128 charged first-core entrance cylinders reaching 14 cores;
- four local source-current boxes, three with finite first returns;
- 64 corrected occurrence laws and 128 singular Kac coordinates;
- exact finite-Borel four-term Kac algebra.

Their carrier types remain different. The cores are compact collision
subcores, not a stable-saturated product base. The 128 cylinders are entrance
words from selected germs, not a full source partition or quotient inverse
branches. The four local boxes are fixed-parameter source curves, not
two-dimensional stable-saturated return strips. Borel Kac coordinates are
not parentwise quotient amplitudes.

## Exact 17-field audit

Every Gate-2 field remains false after the round-23 join. The first missing
object is still

```text
stable_saturated_product_base_Lambda_A
```

and the first reverse-kernel object is still

```text
stable_holonomy_projection_pi_s.
```

The producer records a precursor and a nonpromotion reason for each field.
Important type guards include:

- destination core ID is not a stable projection fibre;
- `hit/miss`, `fw/rev`, and the two Kac coordinates are independent axes,
  none being the Gate-2 inverse-branch axis;
- collision-SRB core mass is not a quotient density `rho`;
- first-core stopping time is not inverse-cylinder scale stopping;
- a censored `tau_C>2018` local survivor is not an overshoot cemetery;
- the occurrence `q` charge is not a parentwise normalized PPE amplitude.

No fake cross-product or positional zip was materialized.

## Logical countermodel

An exact 128-state identity model carries total positive mass one, 64 owners,
two parameter tags per owner, 14 destination labels, and the signed Kac mark
`(+1,-1)` with global sum zero. Nevertheless every reverse conditional is a
Dirac mass:

```text
reverse support size = 1,
sum of squared reverse weights = 1,
two-copy diagonal pair-energy coefficient = 1.
```

Thus positive core mass, complete finite labels, and exact Kac centering do
not logically imply a stable quotient or PPE. This is a type-nonimplication
countermodel, not a CM2 dynamics counterexample.

## Shortest new physical input

The next honest object is one positive-SRB product rectangle `Lambda_A` with:

1. an interval-indexed invariant stable plaque family;
2. a reference unstable interval `I_A`;
3. a physical stable projection `pi_s: Lambda_A -> I_A`;
4. the conditional SRB stable-holonomy Jacobian with explicit bounds.

Only after those four records exist can connected stable-saturated return
strips, onto inverse branches `h_a`, quotient density `rho`, and reverse
weights `p_a` be constructed on one common branch registry.

## Gate-1 route check

The parallel Gate-1 probe found no geometry-only promotion. A physical
full-cross and its reversible inherited strip are already frozen. The latest
Gate-1 obstruction has moved to the coupled half-density/common-frame
equations, a uniform big-cell lower bound, and twisting in that same
representative. Existing periodic-orbit and cone data do not supply those
objects.

## Reproduction

With `PYTHONPATH=deliverables` and `.venv-neurips/bin/python`:

```text
--integrity-only  REPLAY_AND_INTEGRITY: PASS
--replay          REPLAY_AND_INTEGRITY: PASS
--self-test       SELF_TEST: PASS (52/52 mutations rejected)
live mode         exits 2
```

Strict final status:

```text
ROUND23 CORE/KAC GATE2 PRECURSOR AUDIT: CERTIFIED
GATE2 FIELD PROMOTION AFTER ROUND23:    0/17
CORE/KAC -> PPE NONIMPLICATION:         CERTIFIED
PHYSICAL STABLE QUOTIENT/PPE:           NOT_CERTIFIED
GATE 2:                                 NOT_CERTIFIED
```
