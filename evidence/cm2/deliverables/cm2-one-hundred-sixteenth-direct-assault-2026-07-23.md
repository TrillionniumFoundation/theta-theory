# CM2 one-hundred-sixteenth direct assault

Round116 constructs separate positive-width HIT and BYPASS implicit root
sheets on every frozen rank-three interior tangent strip.

The 52 registered-arc links and 56 immutable gap links contribute 67,584
tangent strips.  Complete first/second/third candidate replay certifies all
67,584 strips with no residual, yielding 135,168 regular open sheets at
`0<c3,b3<=2^-64`.  Every link has one constant official three-key path run.

An independent 640-bit verifier reconstructs all links and strips without
importing the producer, rechecks every stored dyadic margin and official path,
and rejects 12 semantic plus three strict-JSON attacks.  Producer and verifier
cold replays are byte-identical.

This is an interior-link certificate only.  The common `D=0` edge remains
singular; endpoint collars, cross-link registered-port stitching, whole-face
reach, actual standard-curve recuts, and Gate5 F1--F6 child fields remain open.
Gate5 stays `10/18`; CM2 stays `NO-GO_FOR_CLAIM`.
