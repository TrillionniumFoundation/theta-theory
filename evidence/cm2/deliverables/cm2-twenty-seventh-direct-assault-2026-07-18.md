# CM2 twenty-seventh direct assault: limiting R2/Q2, arbitrary-n path schema, empty R1 physical-face joins, and typed Q2 payload

Date: 2026-07-18 (Asia/Shanghai)  
Mode: append-only, fail-closed, exact rational ledgers and 384-bit Arb replay  
Parent root: `cm2-twenty-sixth-direct-assault-manifest-2026-07-18.sha256`  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; no composite
gate is fully closed (`0/5`).  This round closes the limiting physical
time-two `R2/Q2` split modulo a collision-null boundary, creates a
parameterwise arbitrary-finite-depth C24 candidate-path and canonical
regular-component existence schema, proves that all physical F10/F13 face
families on the 4,216 strict R1-inner atoms are empty, and attaches exact
typed mass/restriction/area-Jacobian/symbolic-charge payloads to all 114,006
finite Q2-inner atoms.  It does not enumerate the nonempty arbitrary-n
components, prove a strong-q weighted tail, materialize F14--F18, or derive
an induced strong Lasota--Yorke coefficient.**

## 1. Executive result

| Gate | New strict progress | Remaining decisive blocker | Status |
|---|---|---|---|
| 1 | round-25 exact common frame and `q>999/1000` remain frozen | the same representative still fails connector class `H` | **OPEN (`0/1`)** |
| 2 | the positive-area affine cone-product anchor remains valid | invariant stable plaques and the 17-field physical stable-product interface | **OPEN (`0/17`)** |
| 3 | complete limiting physical `R2/Q2` modulo null; arbitrary-n C24 candidate-path coverage and regular-component existence schema | explicit nonempty component enumeration, uniform composed singularity-growth and numeric branch payloads | **OPEN** |
| 4 | all 114,006 finite Q2 atoms now carry exact typed masses, common fw/rev restriction IDs, invariant-area Jacobian and symbolic once-charged `q2` | numerical common-carrier `C_fw/C_rev`, strong `q_n`, weighted tail and induced strong coefficient | **OPEN** |
| 5 | F10/F13 certified-empty local families raise every strict R1 atom to candidate-local `13/18` | limiting-R1 physical face atlas, F14--F18 and complete operator blocks | **OPEN (global `4/18`)** |

## 2. Composed-flight boundary tube and limiting R2/Q2

Let

```text
h_d = 2^(-floor(d/3)).
```

For every fair absolute binary depth `d>=60`, the complete unresolved
time-two composed-flight frontier satisfies, uniformly for the full parameter
window,

```text
M_base(U2_d) < 160 sqrt(h_d),
mu_collision(U2_d) < 26 sqrt(h_d).
```

Thus `M_base(U2_d)=O(2^(-d/6))`, and the unresolved composed singular/core-face
boundary has collision mass zero in the fair limit.

The proof audits all 448 retained second-collision candidates, the second
discriminants and tangencies, root sign/order changes, the strict obstacle
boundary gap `>9/50`, the time-one adjacent-chart union, the time-two C24
faces, and square-root normal continuity.  It uses midpoint values plus
analytic radii, not an iterated empirical residual ratio or a dependency-
inflated natural interval.

Together with the round-26 limiting physical `R1/Q1` split, this certifies
the complete limiting physical `R2/Q2` partition modulo collision-null sets.
The old finite replay remains exactly

```text
time-two leaves:                 416,994
strict Q2 admitted:              114,006
unresolved at depth <=16:         302,988
strict R2 admitted at depth 16:         0.
```

The final zero is still only a finite-depth admission count.  No claim that
the physical R2 set is empty is made, and a finite complete raw R2/Q2 branch
ledger is still absent.

## 3. Arbitrary-n C24 path and component schema

The physical 24-core registry is now directly joined to the frozen global
regular collision-key partition.  Its alphabet has size

```text
|K| = 441280,
```

and every regular collision step has exactly one key owner under the frozen
chart tie/cemetery policy.  Hence at every finite depth `n` the source-tagged
candidate universe is

```text
c24-path:(source_core_id,n,k_1,...,k_n),
candidate count = 24 * 441280^n,
```

with empty fibres retained.  These fibres cover C24 modulo the standard
grazing/corner/homogeneity and core-face collision-null cemetery.

For

```text
Q_n = C_s intersection intersection_{j=1}^n T_s^(-j)(C_s^c),
R_n = C_s intersection intersection_{j=1}^{n-1} T_s^(-j)(C_s^c)
      intersection T_s^(-n)(C_s),
```

the exact telescope, Kac partition and symbolic physical mass identities are
now installed on this common path grammar.  The frozen unweighted tail reads

```text
mu_s(Q_n)/mu_s(C_s)
  < (550000/147)
    * (111718729/111718750)^floor(n/N_open),
```

where the common theorem-supplied `N_open>=1` remains nonnumeric.

At fixed parameter and finite `n`, every regular path fibre is relatively
open and has at most countably many connected components.  A fixed bijective
natural-number enumeration of rational dyadic basis elements gives each
component a rigorous canonical least-index existence label; the natural
numbers are used as a well-order.  This repair was independently audited and
the verifier explicitly freezes the rank and ID rule.

This is a nonconstructive existence and mass-bookkeeping schema.  It does not
enumerate the nonempty path words or components and does not attach numeric
component masses, margins, unstable Jacobians, distortion, common strong
fw/rev carriers, or strong `q_n`.

## 4. Exact empty physical F10/F13 families on strict R1 atoms

The round-26 F10/F13 frontier is resolved on the 4,216 strict R1-inner atoms
themselves.  All 64 physical occurrence carriers are grazing first-event
faces with `|p_target|=1`; every R1 atom lies on a strict compact core branch
with incoming and outgoing `|p|<3/10`.

The complete `4,216*64=269,824` pair audit is

```text
different collision-section component:                 133,568
same component but different strict first target:       106,520
same target, grazing |p|=1 versus core |p|<3/10:          29,736
                                                        -------
total disjoint pairs:                                   269,824.
```

Therefore each strict R1 atom receives one certified-empty F10 physical
coarea family and one certified-empty F13 moving-current/trace family:

```text
new F10 slots:                         4,216
new F13 slots:                         4,216
candidate-local R1 maturity:           13/18.
```

The empty-sum F10 cost and empty F13 current are legitimate only on these
strict inner restrictions.  The 16,864 artificial coordinate faces are not
retyped as physical occurrence faces.  The limiting R1 set still has
singular and destination-core preimage boundaries, so the global physical
face atlas, F14--F18, complete blocks and global Gate 5 remain open.

## 5. Typed payload on all 114,006 finite Q2 atoms

The complete round-26 384-bit time-two recursion is replayed and every strict
Q2-inner atom receives separately typed payload slots:

1. exact rational coordinate-base mass;
2. exact fixed-fibre and parameter-averaged collision-area mass formulas;
3. one immutable two-collision restriction ID shared by the forward and
   reverse Borel/smooth views;
4. per-collision and composed invariant collision-area Jacobian `1`;
5. one symbolic once-charged expression
   `q2=max(C_fw,C_rev,2)*m2` on that same restriction and mass.

The coordinate-base total remains exactly

```text
5257799/5120000000.
```

It is not renamed an exact collision-SRB mass.  For a source box
`[t0,t1]x[p0,p1]`, the physical fixed-fibre mass is kept in its exact form

```text
R_source * (p1-p0) * (asin(t1)-asin(t0)).
```

The two forward/reverse views are nonadditive and charge this one mass only
once.  Collision-area preservation is not promoted to either the adaptive
`(t,p)` Jacobian or the one-dimensional unstable Jacobian.

The universal strong estimates remain conditional templates.  Only after
physical homogeneity refinement and canonical recuts at both steps would

```text
two-step inverse unstable factor
  < (144000/180337)^2
  = 20736000000/32521433569,

two-step canonical-recut log variation < 3/100000.
```

The Q2 rows currently contain no homogeneity-child or canonical-recut IDs,
so F5/F6 remain zero on these unsplit atoms.  Numerical `C_fw`, `C_rev`,
strong `q2`, F14--F18, a weighted tail and an induced strong coefficient all
remain unmaterialized.

## 6. Verification and independent audit

The four round-27 leaves use strict JSON parsing, frozen dependency hashes,
full producer replay, independent result reconstruction, hostile mutations,
and fail-closed live exits.  The arbitrary-n component-label leaf received a
separate mathematical audit: its initial lexicographic-minimum convention was
rejected because lexical order is not a well-order, replaced by a fixed
natural-number basis enumeration, re-frozen, and then re-audited successfully.

```text
round-27 hostile tests:               332/332
historical cumulative hostile tests: 2045/2045
live verifier exit code:                    2
```

All four new leaf SHA chains, the round-26 recursive parent, the v52 chain,
the round-27 aggregate and the new recursive root pass SHA-256 verification.
All older frozen artifacts remain unchanged.

## 7. Technology check

The source of arXiv:`2604.19671v2` was rechecked, including its invariance of
standard families and conditional exponential-mixing theorem for Sinai
billiards with small holes.  Its proof relies significantly on a boundary-
position hole whose images can be foliated by long standard pairs; the paper
explicitly notes that even a circular phase-space hole is out of reach.
C24 is instead a parameter-uniform union of phase-space rectangles.  The
paper supplies a useful survivor-recovery blueprint but not the missing C24
component enumeration, numerical strong carriers, weighted tail, or induced
strong Lasota--Yorke constants.  The broader review arXiv:`2606.10155v1`
likewise does not replace these physical interfaces.

References:

- <https://arxiv.org/abs/2604.19671v2>
- <https://arxiv.org/abs/2606.10155v1>

## 8. Strict verdict and shortest next route

Strict status after round 27:

```text
CM2:                                           NO-GO FOR CLAIM
fully closed composite gates:                              0/5
Gate 2 official fields:                                   0/17
Gate 5 global maturity:                                   4/18
Gate 5 candidate-local R1 maturity:                      13/18
limiting physical R2/Q2 modulo null:                 CERTIFIED
arbitrary-n measurable path/component schema:        CERTIFIED
nonempty arbitrary-n component enumeration:      NOT MATERIALIZED
numeric strong q-weighted return tail:             NOT CERTIFIED
induced strong Lasota-Yorke coefficient:            NOT CERTIFIED
```

The highest-leverage next route is:

1. turn the parameterwise arbitrary-n existence schema into an adaptive
   nonempty-component registry, with composed singularity-growth bounds and
   limiting-face ownership carried on immutable IDs;
2. add physical homogeneity-child IDs and canonical recuts when each branch
   is created, thereby making the conditional F5/F6 templates numerical;
3. build the limiting Rn physical coarea/DQ face atlas and materialize
   F14--F18 on the same common fw/rev restriction registry;
4. prove survivor-conditioned recovery constants `C_fw/C_rev`, assemble
   numerical strong `q_n`, and derive the weighted excursion/cemetery tail;
5. only then sum the return branches and prove the induced strong
   Lasota--Yorke coefficient on the common two-view strong space.

Until those interfaces exist on one immutable physical registry, the CM2
claim remains fail-closed.
