# CM2 Gate 5 Round-50 owner-boundary-`Z_B` / F17 frontier assault

Date: 2026-07-19  
Strict verdict: **the global standard-Borel owner-deduplication layer is now
closed as an extended-valued marked boundary-`Z_B` kernel.  The certified
positive homogeneity ledger cannot make it finite: its full `2^B` majorant
diverges, with a sharp softened-weight threshold `theta<1/2`.  The exact
branch-adapted dynamic-test transport has multiplier one, but it is not one
common physical CM2 test norm.  No complete F10/F17 field is added; Gate-5
maturity remains `10/18`, the complete block count remains `0`, and CM2
remains `NO-GO`.**

## 1. Global owner-aware marked kernel

The arbitrary-`R_n` path universe is countable in depth/path labels and its
actual parent-curve registry is standard Borel.  Each of the five physical
face grammars has a Borel F13 payload, and the parameterized F8 theorem gives
zero or one transverse connected-rank-zero root on one parent `W`.

Use the candidate token

```text
(restriction-id,time-j,physical-event-signature,primitive-key,
 connected-rank-0,side-label).
```

Within one physical-event signature, order primitive keys and set

```text
E_i^owner = E_i \ union_(h<i) E_h.
```

This is a Borel construction.  The owner sets are pairwise disjoint and
cover all regular active representations.  Different physical events are
never merged.  Corners and simultaneous physical events are sent to
cemetery before minimization; artificial chart/homogeneity faces are
assembled before absolute values and excluded.

Consequently

```text
K_owner(y,A)=sum_i K_i(y,A intersect E_i^owner)
```

is a global standard-Borel sigma-finite marked trace-kernel schema, and

```text
Z_B^owner(G)
 = integral sum_(owned roots xi on W)
     2^B_inc(xi)/length_*(W) dlambda(W)
```

is an extended-valued Borel functional.  This genuinely removes the global
owner-deduplication schema gap without pretending that the value is finite.
No finite nonempty-component enumeration is needed for the Borel minimum.

## 2. The full positive `Z_B` majorant fails sharply

The certified one-step Growth ledger has at most 153 true components and,
for each sign and high homogeneity index `k>=6121`, inverse expansion

```text
< 4/k^2.
```

The physical strip geometry is

```text
1/[2(k+1)^2] < c_target < 1/k^2.
```

Thus the new target rank obeys

```text
B_target <= Bbar(k)=ceil(log2(2(k+1)^2)).
```

The available positive rank-weighted high-strip majorant is therefore

```text
M_theta
 = sum_(j<=153) sum_(sign=+,-) sum_(k>=6121)
     (4/k^2) 2^(theta Bbar(k)).
```

Because

```text
2(k+1)^2 <= 2^Bbar(k) < 4(k+1)^2,
```

comparison with `sum k^(2theta-2)` gives the exact threshold

```text
M_theta < infinity  iff  0<=theta<1/2.
```

At the required F10 exponent `theta=1`, each majorant term is strictly above

```text
2448*(k+1)^2/k^2 > 2448,
```

so the supplied positive majorant is infinite.  Even the single central
child is unusable under the full worst-case rank relaxation:

```text
c_target^-1 < 2*6121^2 = 74933282,
B_target <= 27,
(2000/1999)*(144000/180337)*2^27
 = 38654705664000000/360493663 > 10^8.
```

This does **not** assert that the actual signed physical current diverges.
It proves that the existing positive expansion/multiplicity ledger cannot
produce a finite full-`Z_B` recurrence.  Face-specific cancellation or
sparsity, or a genuinely different anisotropic trace norm, is necessary.
A softened `theta<1/2` norm cannot pay the physical `2^B` F10 amplitude.

## 3. Exact conditional aggregate resolvent

Let

```text
rho=(111718729/111718750)^9148,
w_Z=(1+rho^-1)/2.
```

Round 42 already supplies the weighted aggregate sums for ordinary `Z` and
mass.  If a future owner-boundary recurrence has the form

```text
b_(p+1) <= kappa_B b_p + A_B z_p + C_B m_p,
```

then summation is exact:

```text
sum_(p>=0) w_Z^p b_p
 <= [b_0
     +w_Z A_B sum w_Z^p z_p
     +w_Z C_B sum w_Z^p m_p]
    /(1-w_Z kappa_B).
```

The sharp threshold is

```text
kappa_B < w_Z^-1 = 2rho/(1+rho),
99914/100000 < 2rho/(1+rho) < 99915/100000.
```

Numerically the threshold is about `0.999140216`.  The current full-weight
positive majorant is infinite, so no `kappa_B` is installed.

If `L=9148*N_open` were numeric and `b_p` denoted total face charge in block
`p`, then

```text
eta=log(w_Z)/L
```

would yield a collision-time exponential face-tower moment.  `N_open` is
still nonnumeric, so even the conditional collision-time exponent cannot be
reported numerically.

## 4. Exact dynamic-test isometry, and why it is not F17

For one finite regular area-preserving suffix `S:U->V`, define the
branch-adapted test norm

```text
||phi||_(dyn,S)=||phi composed S||_C1(U).
```

Then every source distribution `T` satisfies the exact dual identity

```text
||S_*T||_(dyn,S)^* = ||T||_(C1(U))^*.
```

Thus the algebraic branch-adapted suffix multiplier is exactly `C_dyn=1`.
It is below both Round-49 targets

```text
C_dyn < 7961063/7800000  -> preserve the source quarter bound;
C_dyn < 43295063/7800000 -> preserve a subunit bound.
```

But the norm depends on `S`.  A common candidate

```text
||phi||_common = sup_S ||phi composed S||_C1
```

does not contain the desired physical C1 observables under the available
assumptions.  The exact area-preserving family

```text
A_L=diag(L,L^-1),   phi(y)=y_1
```

has Piola normal-flux multiplier one while

```text
||phi composed A_L||_C1 >= L -> infinity.
```

Hence area preservation, Piola cancellation and branchwise transport do not
provide the required branch-uniform physical test embedding.

The precise F17 first missing interface is a billiard-specific common
anisotropic/dynamic test space `T_dyn` satisfying

```text
sup_suffix ||phi composed S_suffix||_source
 <= C_dyn ||phi||_T_dyn
```

with `C_dyn<43295063/7800000`, preferably the sharper quarter threshold
`7961063/7800000`.

## 5. Strict status and next route

Certified this round:

```text
global owner-aware extended-valued boundary-Z_B kernel schema: CERTIFIED
sharp available-majorant exponent threshold theta=1/2:        CERTIFIED
conditional aggregate-Z_B resolvent and kappa threshold:      CERTIFIED
branch-dependent dynamic-test isometry C_dyn=1:               CERTIFIED
```

Not certified:

```text
global owner-Z_B finiteness:                    NOT CERTIFIED
same-ID full-Z_B one-step recurrence:           NOT CERTIFIED
unconditional aggregate-Z_B resolvent:          NOT CERTIFIED
return-depth face-tower moment:                 NOT CERTIFIED
branch-uniform physical dynamic-test embedding: NOT CERTIFIED
complete all-face F10 / dynamic F17:            NOT CERTIFIED
strong F13 / cemetery / F14/F15/F18:            NOT CERTIFIED
Gate-5 maturity:                                10/18
complete 18-field blocks:                       0
complete composite gates:                       0/5
CM2:                                             NO-GO
```

The shortest valid continuation is now more specific:

1. replace the divergent positive `2^B` strip sum by an owner/face-specific
   signed cancellation or sparse-incidence theorem and obtain
   `kappa_B<2rho/(1+rho)`;
2. use the displayed resolvent to close the block-index face-tower moment,
   then supply numeric `N_open` for collision time;
3. build one common billiard anisotropic test norm with the displayed F17
   embedding threshold;
4. only then promote complete F10/F17 and strong F13/cemetery.

## 6. Validation

The suite replays every dependency hash and all exact fractions.  It checks
the five-face/two-side owner rows, the rank-shell terms, the sharp resolvent
threshold and the diagonal dynamic-test countermodel.  Results:

```text
syntax:                 2/2 PASS
deterministic replay:   PASS
hostile mutations:      82/82 rejected
default cert/verifier:  fail closed with exit 2
strict JSON:            duplicate keys rejected
Gate-5 maturity:        10/18
CM2:                    NO-GO_FOR_CLAIM
```
