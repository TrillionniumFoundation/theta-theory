# CM2 thirty-eighth direct assault

Date: 2026-07-19  
Verdict: **one genuine scheduled strong-tail theorem and one exact F10
exponent frontier are certified; all five composite gates remain open**

## Round result

| gate | round-38 advance | exact remaining core gap | status |
|---:|---|---|---|
| 1 | official versions rechecked; no unsafe theorem transfer | one physical representative with valid cancellation/Green hypothesis and same-representative twisting/PPE | NOT CERTIFIED |
| 2 | no cross-carrier promotion | invariant stable plaques, physical holonomy, quotient branches and PPE | NOT CERTIFIED |
| 3 | scheduled C24 survivor now has a projective-order strong tail | all-collision arbitrary-depth DQ/`MT_DQ`, moving currents and cemetery on canonical same IDs | NOT CERTIFIED |
| 4 | sparse killed blocks have explicit-rate strong decay in the Demers--Liverani order norm | cone-to-canonical-`Z` bridge or full-`Q_N C_N a^N<1`, then numerical `C_fw,C_rev,q` and cemetery | NOT CERTIFIED |
| 5 | exact margin-tail/F10-blow-up criterion `rp<alpha` frozen | physical arbitrary-`R_n` exponents, global F10, return-wide F12/F13 and F14--F18 | NOT CERTIFIED (`7/18`) |

Strict unconditional progress remains `0/5`; CM2 remains
`NO-GO_FOR_CLAIM`.

## 1. First scheduled strong survivor tail

The existing recovered-cone hit gap gives

```text
r=111718729/111718750<1
```

per sparse C24 opening block.  Proposition 8.7 returns normalized outputs to
a strict projective subcone.  Proposition 6.13 gives finite Hilbert diameter,
and Definition 8.14/Lemma 8.15 supply the order norm.  Their combination
proves, uniformly over the compact parameter window,

```text
||K_s^k f||_*<K_cone*r^k*mass(f), k>=1,
K_cone=exp(Delta_open)<infinity.
```

With

```text
w=223437479/223437458,
w*r=223437479/223437500,
1-w*r=21/223437500,
```

the weighted scheduled strong Green sum is finite with safe multiplier
`K_cone*223437500/21`.

This removes the earlier blanket statement that the sparse theorem supplies
only a weak mass tail: it supplies a strong tail in its native projective
order norm once combined with the explicit hit gap.

## 2. Why Gate 4 still does not close

The new norm is not the standard-family boundary Growth `Z`.  The same unit
square density can be disintegrated into full horizontal leaves with `Z=1`
or into `k` equal pieces per leaf with `Z=k`, without changing the density,
mass, projective cone element, or order norm.  Therefore a uniform
cone-to-canonical-carrier comparison is indispensable.

The round-35 parent-`W` registry fixes canonical arbitrary-`R_n` IDs, but the
scheduled cone outputs are not joined to that registry.  Nor may the frozen
all-collision first-return scheme be silently replaced by a sampled return
map; a Kac/inducing equivalence must transport DQ, currents, and cemetery.

The two admissible shortest Gate-4 routes are now:

1. prove a uniform comparison from scheduled projective order norm to the
   canonical same-ID standard-family `Z`; or
2. prove a physical full-`Q_N` characteristic multiplier with
   `C_N a^N<1` directly.

## 3. Exact F10 exponent budget

For analytic margin `eta`, a physical tail

```text
mu{eta<=t}<=C_eta*t^alpha
```

and F10 blow-up

```text
N_F10<=A_eta*eta^(-r)
```

give `N_F10 in L^p` exactly under the strict shell condition

```text
r*p<alpha.
```

The condition cannot be relaxed from these two inputs alone.  An exact
probability model has a linear margin tail (`alpha=1`), pointwise finite
inverse-margin F10 (`r=1`), and divergent `L^1` sum at the critical exponent.

Round 26's available physical tube law is linear in the actual width `h`:

```text
mu(U_d)<(50/39)h_d,
h_d=2^(-floor(d/3)).
```

Thus its physical width exponent is `alpha=1`, not `1/3`.  But it is only a
one-step parameter-averaged theorem, and no physical F10 blow-up exponent
`r` has been derived.  For any desired `p>1`, even an optimistic `alpha=1`
requires `r<1/p<1`.  This is the next exact Gate-5 scalar target.

## 4. Downstream operator frontier

No field is promoted merely from the new scalar criteria:

- global arbitrary-`R_n` F10 remains absent;
- return-wide F12 and moving-current F13 remain absent;
- strong cemetery is still an independent absorbing charge;
- F14--F18 and the induced coefficient remain absent;
- Gate-3 dynamic DQ/`MT_DQ` remains downstream of the same physical current
  and cemetery ledger.

Gate-5 maturity therefore stays `7/18`, with zero complete 18-field blocks.

## 5. Latest technology boundary

Official arXiv metadata was rechecked on 2026-07-19:

- `2104.06947v3`, *Projective Cones for Sequential Dispersing Billiards*;
- `2604.19671v2`, *Linear response for Sinai billiards with small holes*;
- `2606.10155v1`, *Recent Progress in the Application of Transfer Operators
  to Dispersing Billiards*.

Round 38 uses the finite-diameter and order-norm mechanism of
`2104.06947v3` on the already certified sparse C24 theorem interface.  The
other two sources still do not supply a canonical C24 full-survivor
standard-family multiplier, arbitrary-`R_n` F10 exponent ledger, or strong
cemetery.  No theorem is transferred beyond its certified geometry and norm.

Official sources:

- https://arxiv.org/abs/2104.06947
- https://arxiv.org/abs/2604.19671
- https://arxiv.org/abs/2606.10155

## 6. Validation

Both suites pass Python syntax, frozen dependency hashes, strict JSON,
independent exact replay and live fail-close.  They reject

```text
19/19 + 20/20 = 39/39
```

hostile mutations.  Both certificate defaults and both verifier defaults
exit `2`; integrity and replay modes pass.  Leaf and recursive SHA chains
freeze every report, certificate, verifier and manifest.

## Next shortest line

1. Build the explicit scheduled-cone-to-canonical-parent-`W` comparison and
   test whether its multiplier preserves the exact weighted rate; in
   parallel continue the direct full-`Q_N C_N a^N<1` route.
2. Derive the physical F10 blow-up exponent `r` from coarea denominators and
   analytic margins, then prove arbitrary-`R_n` margin exponent `alpha` on
   the same face IDs with one `p>=1` satisfying `r*p<alpha`.
3. Join global F10 to return-wide F12 and moving-current F13, then assemble
   the separate strong cemetery and F14--F18.
4. Feed that ledger into Gate-3 dynamic DQ/`MT_DQ` and numerical
   `C_fw,C_rev,q`.
5. Continue Gate 1's same-representative cancellation/twisting construction
   and Gate 2's invariant stable-plaque/holonomy/quotient/PPE construction as
   independent structural poles.

Strict unconditional CM2 remains `NO-GO_FOR_CLAIM`, with zero of five
composite gates fully closed and Gate-5 maturity unchanged at `7/18`.
