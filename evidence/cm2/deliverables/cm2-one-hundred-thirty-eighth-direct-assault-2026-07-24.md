# CM2 one hundred thirty-eighth direct assault

Date: 2026-07-24

Strict verdict: **two positive-width first-return cylinders are now certified
on opposite sides of one common implicit slope-four D=0 parent leaf. The
primitive-free Round132 event type and global insertion time `j=3` are
crosswalked. The global restriction/component/owner chain remains open, so
Gate5 and CM2 do not move.**

## Two-sided return advance

At fixed `p_star=-1587/1638400`, Round138 isolates one D=0 root and defines

```text
b_star = asin(p_star) - (16/25) asin(t_star).
```

On that same leaf it isolates

```text
D = -(4/25)^2 2^-128
D = +(4/25)^2 2^-128
```

and proves `BYPASS < D0 < HIT`. The along-leaf derivative is strictly
positive, so the side labels have a certified orientation.

Each side moves `2^-1800` inward, fixes a unique nearest `2^-4096` dyadic
`p` center, and encloses the exact graph in a `2^-2400` half-width rational
square. Both squares have positive area `2^-4798`.

## Complete physical replays

```text
BYPASS first return depth:      298
HIT first return depth:         349
combined collision rows:        647
strict preterminal nonreturns:   645
strict terminal returns:           2
```

Every collision performs both a retained 55/57-candidate search and a full
161-candidate radius-four search. The winner is identical in both accounts.
All official words, homogeneity labels, incidence ranks, and terminal C24
cores are rebuilt.

The BYPASS path is entirely central with rank 14. The HIT path has one
`H4294967295` collision and two rank-65 incidence rows.

## Primitive-free event time

The translated physical label

```text
["G", "W[0,-1]", 1, "W[0,-2]", -1]
```

selects exactly one Round132 candidate. No primitive key, restriction, or
root coordinate is adopted.

The explicit local collision timeline gives

```text
j = 2 prefix collisions + 1 Round132 face-local offset = 3.
```

The suffix alignment offsets `{hit:1, miss:0}` remain separate from `j`.

## Remaining hard gate

The two certified objects are local return word-cells, not Round35 short
cells. Still missing are:

```text
global Round35 restriction
connected-component and least dyadic-basis ranks
global short cell and image recut
D0-adjacent fixed full-word/no-extra-cut t54 theorem
Round50 owner and pi54 projection
Round67 Omega_j and q_j
```

On the plus side, approach to D=0 crosses infinitely many homogeneity strips
and may have `N_acc` or `d=0`. On the minus side, the required D0-adjacent
fixed full-word/no-extra-cut theorem is also absent. The current squares
therefore cannot be renamed Round54 `t54` cells.

## Independent verification

The verifier does not import or execute the Round138 producer and does not
read temporary spike data. Full 12288- and 16384-bit reconstructions are
identical.

```text
formal verification:                   PASS
re-signed semantic mutations:         81/81 rejected
strict-JSON attacks:                  19/19 rejected
path-safety self-tests:               13/13 rejected
dual hash-seed artifacts:             byte-identical
process-level hostile I/O:            12/12 fail-closed
```

## Frozen global state

```text
local slope-four parent roots:            1
local positive-width return word-cells:   2
global restrictions/components:           0
global short cells/image recuts:          0
owners/t54/Omega_j/q_j:                    0
global complete 18-field blocks:          0
Gate5 global maturity:                10/18
Gate5:                         NOT_CERTIFIED
CM2:                        NO-GO_FOR_CLAIM
```
