# CM2 Round 67 Gate 1/3 — actual-root transfer and uniform material/Piola frontier

Date: 2026-07-21  
Scope: append-only Gate-1/Gate-3 assault from the frozen Round-66 aggregate  
Strict verdict: **Round 67 identifies the maximal actual common root that the
frozen Gate-1 leaves really share and writes the exact relative transfer over
that root.  It also proves, by a sharp completion separator, that the frozen
selected constants cannot determine the renormalised endpoint defects or the
four target wedges.  For Gate 3 it replaces the homogeneous stopped-depth
majorant by the exact nonautonomous path ledger, gives the weighted direct-sum
and three-factor uniform-Piola criteria, and installs the exact side-tagged
two-sided trace/current norm.  The required keyed scalar transfer, all-cell
material radius, uniform anisotropic remainders, physical trace/current and
`MT_DQ` bounds are still absent.  Gate 1 and Gate 3 remain `NOT_CERTIFIED`,
complete composite gates remain `0/5`, and CM2 remains
`NO-GO_FOR_CLAIM`.**

## 1. Frozen roots and append-only discipline

This leaf directly pins the Round-66 aggregate and independent audit, the
Round-66 Gate-1/3 leaf, the faithful clean-SFT/coding leaf, the
variable-diagonal Green leaf, the 5000-bit selected QNL computation, the
actual circular-pilot formula, and the moving-trace/dyadic-clock interfaces.
No old artifact is modified.

The frozen records distinguish three levels which must not be conflated:

```text
same physical clean SFT/orbit/cocycle root:             available
exact pointwise formula for a relative transfer:        available below
same-key scalar table and renormalised endpoint limits: absent
```

## 2. Gate 1 — maximal actual-root crosswalk

### 2.1 What is genuinely common

The global-coding leaf provides one finite clean SFT

```text
(Sigma_A,sigma,pi),     A_log(x)=E_0(sigma x)^(-1)D_(pi x)G E_0(x),
```

containing the physical QNL periodic token `p` and the selected physical
homoclinic token `z_h`.  On this root the invariant-frame change `E` gives

```text
A_diag(x)=E(sigma x)^(-1) A_log(x) E(x).
```

The compact logarithmic QNL gauge is a physical section gauge and therefore
restricts to the same clean root; call its pulled-back transfer `Q`.  The
variable-diagonal construction defines the two Green functions on this same
SFT and their pointwise product

```text
D(x)=U_(v(x)) L_(u(x)).
```

The nonlinear cross term remains open, so this statement does not assert
that the combined representative is in class `H`.  It only identifies the
common physical carrier on which the gauges are functions.

Thus the two representatives have the exact forms

```text
A_q(x)=Q(sigma x)^(-1) A_log(x) Q(x),
A_c(x)=(E D)(sigma x)^(-1) A_log(x) (E D)(x).
```

and the oriented relative transfer is forced algebraically:

```text
C(x)=Q(x)^(-1) E(x) D(x),
A_c(x)=C(sigma x)^(-1) A_q(x) C(x).                 (2.1)
```

Equation (2.1) is stronger than comparing the surface syntax of two
upper/lower shears: it is an exact same-root factorisation through the
physical derivative cocycle.  It also exposes the remaining data gap.  The
frozen manifests do not tabulate `Q,E,u,v` under one immutable
`(code,orbit-time,plaque-side)` key.  Hence they do not determine a numerical
matrix `C(x)` even at all selected tail points, much less its all-plaque
Hölder constants.

The exact common-root audit is therefore:

```text
same Sigma_A / p / z_h tokens:                         CERTIFIED
same physical derivative cocycle A_log:                CERTIFIED
formal graph-supported transfer (2.1):                 CERTIFIED_EXACT
same-key Q,E,u,v scalar rows:                           NOT_CERTIFIED
numerical C and C^(-1) bounds on all plaques:           NOT_CERTIFIED
```

These are subrows, not an official Gate-1 promotion.

### 2.2 Frozen quantities do not determine endpoint defects

For a stable pair the transfer debt is

```text
Delta_n^s
 =A_q^n(y)^(-1)[C(sigma^n y)C(sigma^n x)^(-1)-I]A_q^n(x),
```

with the backward analogue on unstable plaques.  Root identity and the value
`C(p)` do not determine these sequences.

Take the exact logical class-`H` model `A=diag(2,1/2)` and a stable orbit
with distance `2^-n` from `p`.  All three transfers below are Lipschitz on
the orbit closure, lie in `SL(2)`, and equal `I` at `p` and on an independently
declared selected orbit:

```text
C_0(sigma^n y)=I,
C_+(sigma^n y)=I+4^-n E_21,
C_osc(sigma^n y)=I+(-1)^n 4^-n E_21.
```

Yet conjugation gives respectively

```text
Delta_n^s=0,       Delta_n^s=E_21,
Delta_n^s=(-1)^n E_21.                               (2.2)
```

The first completion has zero debt, the second a nonzero limit, and the
third no limit.  They agree on every selected-orbit value and determinant
row.  This is a logical completion separator, not a billiard realization.
It proves that no computation from the frozen `0.091`, `0.092809...`,
depth-260 error, determinant one, or common root alone can recover the
actual endpoint limits.

### 2.3 Consequence for the four wedges

If both endpoint limits exist, the target canonical loop is

```text
Psi_c=C(p)^(-1)(H_q^s+L_s)(H_q^u+L_u)C(p).           (2.3)
```

Simultaneous transport preserves the four nonzero wedges only after either

```text
H_q^s L_u+L_s H_q^u+L_s L_u=0
```

is certified or the changed loop in (2.3) is recomputed in the transported
axes.  The completion separator (2.2) changes these limits without touching
the selected frozen values.  Therefore the actual four compact-QNL wedges
cannot be promoted to the combined Green family from the frozen quantities.

The shortest lawful Gate-1 payload is now a same-key export of `Q,E,u,v`
on every selected tail point and every local plaque, followed by direct
forward/backward `Delta_n` bounds and a replay of (2.3).  No further
surface-gauge comparison is needed.

## 3. Gate 3 — all-cell window and weighted strong assembly

### 3.1 A common material window is a uniform infimum

For every regular branch/cell/depth token `k`, let `r_k>0` be the supremal
admissible connected parameter radius on which all incidence, grazing, tie,
chart-face and flight margins remain strict and the implicit collision branch
remains `C^2`.
The countable regular atlas has a nontrivial common material window exactly
when

```text
r_* = inf_k r_k > 0.                                  (3.1)
```

The Round-66 compact interior of one depth-96 word certifies one positive
`r_K`; it supplies no lower bound for (3.1).  The sharp family `r_k=1/k`
has a valid finite window for every branch and every finite collection, but
no common nonzero window.  Clock, grazing, topology-change and cemetery
records cannot be discarded to manufacture `r_*`.

### 3.2 Weighted direct sums

Let

```text
X=direct_sum_1 a_k X_k,      Y=direct_sum_1 b_k Y_k,
J_s=direct_sum_k J_(s,k).
```

Block norm algebra gives the exact identities

```text
||J_s||_(X->Y)=sup_k (b_k/a_k)||J_(s,k)||,

||(J_s-J_0-sG)/s||
 =sup_k (b_k/a_k)||J_(s,k)-J_(0,k)-sG_k||/|s|.       (3.2)
```

Thus weights do not turn pointwise differentiability into a global theorem;
they change the missing uniform supremum by the precise ratio `b_k/a_k`.

### 3.3 Uniform `Rhat/Phat/Qhat` composition

On any one strong material scale assume, uniformly in the immutable block
key,

```text
F_s=F_0+sF_1+s e_F(s),       F in {Rhat,Phat,Qhat},
||F_0||<=A,  ||F_1+e_F(s)||<=B,
sup_k||e_(F,k)(s)|| ->0.
```

Then

```text
D(QPR)=Q_1P_0R_0+Q_0P_1R_0+Q_0P_0R_1
```

and direct expansion yields the uniform remainder bound

```text
||[Q_sP_sR_s-Q_0P_0R_0-sD(QPR)]/s||
 <=A^2 sum_F ||e_F(s)|| +3|s|AB^2+|s|^2B^3.          (3.3)
```

The weighted version uses the supremum in (3.2) at every typed leg.  Formula
(3.3) is a sufficient strong assembly bridge.  It is not a converse for the
individual factors: `R_s=I+sS`, `P_s=I`,
`Q_s=(I+sS)^(-1)` makes the product identically `I` while factor debts may
remain relevant.  In CM2 the factors are separately consumed by restriction,
material transport and assembly, so an algebraic cancellation cannot pay
their typed rows.

### 3.4 Exact side-tagged two-sided trace/current row

Let `e` range over face, clock, grazing, birth/death and one-shot cemetery
tags.  Give the two sides distinct immutable tags and define

```text
Gamma_v x
 =(w_e v_e U_e^- Gamma_e^- x,
   w_e v_e U_e^+ Gamma_e^+ x)_e
```

in the `ell1` direct sum of landing/current spaces.  Because side tags are
disjoint,

```text
||Gamma_v x||
 =sum_e w_e |v_e|
   (||U_e^-Gamma_e^-x||+||U_e^+Gamma_e^+x||).         (3.4)
```

Hence the complete boundary-current derivative is bounded if and only if
the supremum of the right side over `||x||<=1` is finite.  Forgetting the
side tags may create accidental signed cancellation and is not the physical
strong current.  The frozen weak graph-TV norm one does not imply (3.4).

## 4. Gate 3 — exact nonautonomous stopped `MT_DQ` ledger

For a path of length `n`, write

```text
T_[0,n)=T_(n-1)...T_0,       ||T_i||<=M_i,
||DT_i||<=L_i.
```

The product rule gives

```text
||D T_[0,n)||
 <= A_n := sum_(r=0)^(n-1) L_r product_(i!=r) M_i.   (4.1)
```

For stopped weights `p_n`, `sum_n p_n A_n<infinity` is the sharp positive
scalar/disjoint-depth criterion and the sharp general operator majorant.  It
strictly refines `L P'(M)` and reduces to it when `M_i=M`, `L_i=L`.

Two separators show why neither a uniform subunit norm nor base contraction
alone is the right actual statement.

1. Put `L_i=1`, and put `M_i=1` when `i+1` is a power of two and `M_i=1/2`
   otherwise.  Then `sup_i M_i=1`, but

   ```text
   A_n <=4 n^2 2^-n.
   ```

   Hence `p_n=1/[n(n+1)]` pays the all-depth derivative ledger.  A physical
   pathwise product estimate can therefore succeed without one uniform
   `M<1`.

2. Put every `M_i=1/2` but `L_i=2^(i+1)(i+1)`.  The last summand in (4.1)
   gives `A_n>=2n`, so the same stopped law produces a harmonic divergence.
   Strong base contraction does not pay uncontrolled material derivatives.

Clock/face/cemetery currents add their positive tagged ledger from (3.4);
they do not cancel (4.1).  The exact remaining physical row is therefore

```text
sum_n p_n [A_n+B_n^trace+B_n^clock+B_n^cem] < infinity
```

on one all-depth material registry and one strong recipient.  None of the
frozen physical files supplies its `M_i,L_i` or trace arrays.

## 5. Latest same-type technology boundary

The official `arXiv:2502.07765v2` was rechecked.  It treats
`N_F`-admissible sequential dispersing billiards with fixed scatterer
arclengths, a common collision phase space and common SRB reference measure,
under uniform flight/curvature/`C^3` bounds.  It proves projective-cone
contraction and a sequential CLT.  It does **not** differentiate the maps in
a material parameter, construct a common moving incidence atlas, control
two-sided face currents, or prove operator-norm Piola/`MT_DQ` remainders.
It is useful same-model statistical technology but not the missing
same-recipient derivative theorem.  No external theorem is promoted.

## 6. Strict Round-67 Gate-1/3 state

```text
actual clean-root/orbit/cocycle crosswalk:             CERTIFIED
formal relative transfer C=Q^-1 E D:                  CERTIFIED_EXACT
same-key scalar transfer/endpoint defects:             NOT_CERTIFIED
combined actual loop and four transported wedges:      NOT_CERTIFIED
Gate 1:                                                 NOT_CERTIFIED

one compact depth-96 material window:                  CERTIFIED_LOCAL
all-cell common material radius r_*>0:                 NOT_CERTIFIED
weighted direct-sum/Piola/trace criteria:               CERTIFIED_EXACT
nonautonomous stopped derivative criterion:             CERTIFIED_EXACT
physical all-depth trace/Piola/MT_DQ:                   NOT_CERTIFIED
Gate 3:                                                 NOT_CERTIFIED

complete composite gates:                              0/5
CM2:                                                    NO-GO_FOR_CLAIM
```

## 7. Executable evidence

- `cm2_gate13_round67_actual_root_transfer_uniform_piola_frontier_cert.py`;
- `cm2_gate13_round67_actual_root_transfer_uniform_piola_frontier_verifier.py`;
- `cm2-gate13-round67-actual-root-transfer-uniform-piola-frontier-manifest-2026-07-21.json`;
- `cm2-gate13-round67-actual-root-transfer-uniform-piola-frontier-manifest-2026-07-21.sha256`.

The producer and independent verifier pin every frozen input, replay the
same-root factor algebra, the endpoint-completion separator, weighted block
norms, uniform product remainder and nonautonomous derivative ledgers,
require canonical strict JSON, reject hostile semantic/parser mutations,
regenerate byte-for-byte, and fail closed with default exit code `2`.
