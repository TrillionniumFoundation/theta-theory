# CM2 Eighty-Seventh Direct Assault — 2026-07-22

## Scope

Round 87 attacks four quantified frontiers left by Rounds 85–86: landing
preimages that straddle the frozen C24 leaf partition, the 3,212 non-source
rank-three union-boundary ports, the 24 unmatched positive and 24 unmatched
negative finite-root material rows, and the short-fibre normalization gap in
the candidate-local F14 construction.  Every result is kept at its actual
local or finite-root scope.

## C24 landing-pullback event arrangement

The 2,328 boundary-straddling return parents are cut twice.  At each node the
producer propagates all exact rational `t`, `p`, and `s` bisections with Arb
and uses the smallest normalized landing-image diameter only as a deterministic
efficiency heuristic.  Correctness depends solely on the exact child cover
and strict target containment, not on selector optimality.

The complete frozen target-boundary equations are

```text
landing_t(t,p,s) = target_t_boundary
landing_p(t,p,s) = target_p_boundary
s                = target_s_boundary
```

The bounded result is

```text
input boundary parents                         2328
strictly contained SURVIVE children            1636
residual children                              6952
residual volume ratio                     1793/2298
Round86 residual volume ratio             1991/2298
parents with at least one contained child       828
parents retaining a residual                    1952
```

Every one of the 6,952 residual rows is independently replayed at 768 bits
and rigorously overlaps at least one frozen target-boundary equation.  No
floating-point endpoint test remains in this claim.  Exact rational child
mass equals parent mass.  No `RETURN -> RETURN` edge is created, so this is an
event-arrangement frontier rather than recurrence or a stable plaque.

## Rank-three local port continuation

All 3,212 registered non-source ports lie on algebraic discriminant graphs
that cross their artificial box-union boundaries.  Each graph is isolated by
128 exact dyadic root steps and extended into a positive-width outward slab.
On that slab the producer proves nonzero coordinate derivatives, opposite
parameter-face signs, strict source-core margin greater than `1/100000`, the
same first-two-collision branch and outgoing chart, and a complete translated
candidate ordering.

The local physical typing is

```text
physical next tangency, locally continuing              945
algebraic tangency strictly behind second collision      980
algebraic tangency strictly after tau_max                432
algebraic tangency occluded by an earlier third owner    855
unresolved                                                 0
locally physical next-tangency endpoints                   0
```

The 2,267 algebraic rows in the middle three classes are not points of the
local physical third-tangency locus.  They are not relabelled as physical
arcs.  The 945 locally physical rows are certified non-endpoints, but they are
not globally paired.

The complete non-target ledger contains 86,315 candidate equations:
81,417 have no real intersection, 1,481 lie behind, and 3,417 have a strict
future near root.  The future rows split into 1,908 before and 1,509 after the
nominated tangent flight.  A separate 1536-bit adversarial replay reproduces
the full 3,212-row and 86,315-equation census.  The final artifact pins all 15
loaded CM2 executable dependencies and the C24 physical-core theorem.

This is a local continuation theorem only.  It proves neither a 1,606-gap
pairing nor a complete physical-face quotient.

## Gate 3 finite-root seam obstruction

Exact repartitioning cannot improve the current `152/176` two-sided material
coverage.  Subdivision preserves each immutable directed-branch union, while
all 48 unmatched rows meet the opposite-side union only in zero-area sets:

```text
edge-seam rows                                  40
corner-only rows                                 8
edge contacts                                   64
corner contacts                                 48
edge normals: t / p                         40 / 24
tangential widths: 1/800 / 1/3200           40 / 24
```

All 48 boxes independently replay as `RETURN_AT_1_INNER` to their frozen
destination.  The minimum lawful enlargement is a positive normal collar for
each edge row and a positive-area two-coordinate neighborhood for each corner
row, followed by fresh branch-key, owner, flight-order, and destination
proofs.  Gate 3 is not promoted.

## Gate 5 F14 weighted-BV frontier

For the 152 current finite-root packets, collision coordinates

```text
r=R asin(t),  phi=asin(p),  v=phi-4r
```

give the exact normalized disintegration

```text
h_v=cos(phi)/sqrt(17)
m(v)=integral_Iv cos(4r+v) dr
M=(r1-r0)(p1-p0)
dLambda(v)=m(v)dv/M
rho_v=h_v/m(v)
```

Both `Lambda` and every positive-mass conditional density have mass one.  The
304 zero-length support endpoints are `Lambda`-null, and in internal-fibre BV

```text
m(v) ||rho_v||_BV = ||h_v||_BV.
```

The resulting packet synthesis cost is below 4,300 in the precisely stated
`direct_sum L1(Lambda;BV_internal)` space.  This is a certified weighted-BV
lemma, not an official F14 slot.

The immutable integration audit crosswalks all 152 packet and F10 keys but
finds zero registered F14 slots.  The accepted frozen strong norm contains
the dynamic `Reg_alpha` recovery term; no intertwiner from the internal-fibre
BV control to that fixed recipient is certified.  Therefore F14 remains open,
candidate-local maturity remains `13/18`, global Gate 5 remains `10/18`, and
complete blocks remain zero.

## Verification hardening

The first F14 draft was rejected because it scaled the displayed recipient
incorrectly and lacked immutable-slot and accepted-recipient binding.  The
final version corrects the probability measure, pins 14 upstream artifacts,
and explicitly records zero installed F14 slots.  The first pullback draft's
floating endpoint checks were replaced with Arb/rational interval overlap.
The port certificate was narrowed from an all-physical reading to the exact
945/2,267 physical/nonphysical distinction and gained closed executable and
physical-core theorem pins.

All final producers use 512-bit Arb and all formal verifiers use 768 bits.
The independent port audit additionally replays the full ledger at 1536 bits.
Configured semantic, pin, structural, and strict-JSON attacks are rejected,
and the frozen producers and verifiers cold replay byte-for-byte.

## Strict state

```text
Gate 1                         NOT_CERTIFIED
Gate 2                         0/17
Gate 3                         NOT_CERTIFIED
Gate 4                         1/7
Gate 5                         10/18, complete blocks 0
complete composite gates       0/5
CM2                             NO-GO_FOR_CLAIM
```

## Next shortest route

1. Isolate and cut the 6,952 residual `landing_t`, `landing_p`, and `s`
   pullback-event surfaces directly, then search the resulting variable-return
   graph for a nonempty recurrent same-key subroot.
2. Globally continue the 945 locally physical tangent branches through owner,
   chart, translation, and rank-transition events; discard or separately type
   the 2,267 locally nonphysical algebraic branches before forming a quotient.
3. Enlarge the 24+24 Gate-3 seam rows by the certified minimal collars and
   rerun strict owner/flight/destination tests.
4. Bind the 152 weighted-BV packets to an immutable F14 slot by proving the
   fixed `Reg_alpha` recovered-strong intertwiner; a bespoke rescaled norm is
   not sufficient.

No external theorem is promoted in this round.

