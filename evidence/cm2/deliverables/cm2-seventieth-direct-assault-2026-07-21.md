# CM2 Round 70 full direct assault

Date label: 2026-07-21

Strict verdict: **0/5 complete composite gates; Gate 2 official fields 0/17;
Gate 4 landing join 1/7 with fields 1, 4 and 7 partial; Gate 5 maturity
10/18 with zero complete blocks; CM2 remains `NO-GO_FOR_CLAIM`.**

## Round result

Round 70 follows the corrected Round-69 typed-incidence route.  It does not
identify a physical face with a path-cell interior point.  Instead it
materializes two genuine physical boundary pullbacks incident to one already
certified positive connected path component.

1. **Actual selected carrier.**  The carrier is the positive connected
   depth-one component
   `7359148da1c43a255f2238d9c831b8638f2c9885035034a5792699c968b2f3b5`
   for `G:E -> W[0,0]`, with predicate `|p0|<3/10` and `|p1|<3/10`.

2. **First nonempty time-qualified face components.**  On
   `t in [13/20,2/3]` and `s in [-1/10000,1/10000]`, strict monotonicity
   `partial_p T < -13277/76000` and opposite boundary signs produce unique
   connected analytic graphs `p1=+3/10` and `p1=-3/10`.  Both have `time_j=1`,
   connected rank zero, a typed incidence edge to the selected path cell, and
   one inside plus one outside trace.  The realized packet has two faces and
   four distinct one-sided traces.

3. **Explicit Gate-5 local fields.**  Each face receives actual instance data:
   F8 transversality lower `13277/76000`, F9 graph-C2 upper `3000`, F10 integer
   C1 upper `6031`, and F13 current variation below `1/5`.  Each one-sided trace
   mass is below `19/60`; the two-face current is below `2/5`, and all four
   traces total below `19/15`.

4. **Gate impact.**  Gate 4 now has two concrete time-one physical-face/path-cell
   incidence edges, but no all-depth stable-product or commuting-square family.
   Gate 5's instantiated finite-return nonempty compact face rows rise from zero
   to two, but the limiting-R1 atlas, weighted global F9/F10/F13 sums, F14--F18,
   and complete 18-field blocks remain absent.  Gates 1 and 3 still lack the
   same-key `Q,E,u,v` material/stable crosswalk.

## Exact verification

- Producer replay: two faces, four traces, F10 upper `6031`.
- Independent replay: `2/2` nonempty faces, `2/2` time-one incidence edges,
  `4/4` one-sided traces, and the F8/F9/F10/F13 arithmetic packet.
- Deterministic manifest audit: pass.
- Hostile semantic mutation rejection: `150/150`.
- Strict JSON rejection: `4/4`.
- Byte-identical re-emission: pass.

## Frozen artifacts

- `deliverables/cm2-round70-selected-nonempty-face-incidence-all-gate-assault-2026-07-21.md`
- `deliverables/cm2-round70-selected-nonempty-face-incidence-all-gate-manifest-2026-07-21.json`
- `deliverables/cm2_round70_selected_nonempty_face_incidence_all_gate_cert.py`
- `deliverables/cm2_round70_selected_nonempty_face_incidence_all_gate_verifier.py`

## Shortest next route

Enumerate an exhaustive finite limiting-R1 physical face atlas using this
typed face grammar, attach actual F8/F9/F10/F13 values to every nonempty
component, and prove rank/path-weighted global sums.  Then materialize F14--F17
operator costs and separately crosswalk the path cell to the stable/material
representative required by Gates 1--4.  Two selected compact faces alone do not
support any global gate promotion.
