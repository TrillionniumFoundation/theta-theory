# CM2 Round147 Gate5 strict re-audit and upgrade frontier

Date: 2026-07-24

## Formal result

The global Gate5 ledger remains **10/18**, with **0** complete global
18-field blocks and **0** new field credits.

Strictly satisfied global fields:

`F1,F2,F3,F4,F7,F8,F9,F12,F13,F16`

Strictly blocked global fields:

`F5,F6,F10,F11,F14,F15,F17,F18`

Round130 supplies prospective/local-v1 18/18 evidence separately on every
exact fibre of its parameterized local family.  That result is useful
construction evidence for the eight open fields, but it is not complete
global-registry coverage and is not the Round144 identifier-migration
namespace.  It therefore earns no global field credit.

## Full sealed-repository census

Round147 independently hashed and searched 2,205 top-level sealed
`.json/.py/.md/.sha256` files through Round144, totaling 760,135,902 bytes.
Round145+, Round147 itself, dot-temporaries, and concurrently ambiguous
`one-hundred-forty-*` prose assault names were excluded.  The formal
Round139--144 source/certificate/verifier/verification bytes were pinned
directly, so the prose-name exclusion removes no controlling semantic input.

The only namespace-shaped concrete `rn-restriction:<64hex>` found was
Round70's
`rn-restriction:8bc002c75e6ffb86a82f75a2543c3bb4dbd43feb881e3f4619d49095486918c8`.
Round147 replayed its payload exactly as:

`(component_id, t_interval, s_interval, physical_word)`.

It is not a historical Round35 restriction: it omits the historical component
ID, source parent-W ID, source interval rank, incidence-rank path, short-cell
index, and image-recut rank required by the frozen Round35 schema.

The strict census is:

- historical Round35 parent-W records: 0
- historical schema-compatible Round35 restrictions: 0
- materialized Round50 owner keys: 0
- complete Round50 same-event candidate fibres: 0
- Round132-to-Round50 active regular crosswalk rows: 0
- materialized owner-bound Round54 t54 tokens: 0
- Round54 same-root word/side crosswalk rows: 0
- materialized Round67 owned Omega_j records: 0
- materialized Round67 q_j outputs: 0
- Round67 owner-to-path crosswalk rows: 0

Round60 does certify the seven-field `t50 -> t54` Borel projection schema:
`pi_50` drops only `word-cell`.  This is a genuine reusable map definition,
but its actual owner-bound record domain is empty in the pinned chain.
Round67 likewise certifies the abstract Omega_j root and deterministic Borel
q_j formula, not recordwise serializations.

## Historical and versioned frontier

Round142 proves the historical Round27 enumeration is underdetermined: nine
executable enumeration fields are absent, and two compatible fixed
bijections yield different least ranks.  Historical identity guessing is
therefore forbidden.

Round144's superseding Round137-v1 DAG is preserved unchanged:

- `D00,D01` are ready.
- The first exact blocker is `D02`, a complete two-generator centered-jet
  outer atlas with exhausted physical-event frontier.
- `D05` and `D08` await future exact leaf-corridor/image-recut certificates.
- All component, parent-W, restriction, owner, t54, Omega_j, and q_j v1
  identifiers remain null.

Round147 reserves two atomic future slots for Round145 and Round146.  Their
bytes are excluded from the Round147 sealed dependency snapshot and no bytes
from either round are pinned or admitted as certificate dependencies.  A later
superseding audit may bind a slot only when source, certificate, verifier,
verification, and verification-result hashes become non-null together; the
verification must pass the Round144 guards before any DAG or Gate5 row is
reconsidered.

## Verification

The independent verifier replayed all byte pins, the 760 MB sealed search
snapshot, the open-eight ledger, the 18 field rows, the Round70 false-positive
payload, the Round142 underdetermination status, and the Round144 null-input
frontier.

It passed 205 semantic checks, rejected 16 re-signed semantic mutations, and
rejected 10 hostile process/I/O inputs.  `PYTHONHASHSEED=0` and
`PYTHONHASHSEED=91` cold producer replays were mutually and canonically
byte-identical.

Strict conclusion: Gate5 remains **NOT CERTIFIED** and CM2 remains
**NO-GO FOR CLAIM**.
