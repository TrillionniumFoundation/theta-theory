# CM2 Round 51 full direct assault

Date: 2026-07-20  
Strict verdict: **0/5 complete gates; CM2 NO-GO**

## Gate 4: uniform gauge and qualitative uniform C24 hit

For the actual fixed-radius horizontal-translation pilot, the physical
collision sections share the same intrinsic `(r,phi)` arclength/outgoing-angle
coordinates.  The project conjugacy is therefore the label identity:

```text
A_s(r,phi)=(r,phi),
DA_s=I,
m_s=inf sigma_min(DA_s)=1,
hat_delta_s=delta_rect.
```

This removes the nonuniform conjugacy factor left open in Round 50.  A single
parameter-uniform open target was also placed strictly inside C24:

```text
G:E,
t in (11/1000,19/1000),
p in (-3/2000,3/2000),
mu_s(G:E) > 189/143000000
            = (225/32)*(21/111718750).
```

The standard-family object type was joined to the uniform memory-loss theorem
of Stenlund--Young--Zhang (`arXiv:1210.0011v4`).  The project density bound,
adapted/Euclidean length comparison and inverse contraction give

```text
|Delta log rho| < 2*10^27*(93/100)^s(x,y),
```

while the common invariant collision law has relative density one.  The
smooth-density embedding and the compact moving-configuration theorem then
give one finite time `H_bump`, common to every `|s|<=1/400` and every
admissible canonical proper family, such that

```text
(T_s^H_bump)_* G(C24) > 21/111718750.
```

This is the first parameter-uniform C24 hit theorem on the actual pilot, but
`H_bump` is existential rather than numerical.  The published constants
`C,theta`, effective Cantor rows, transverse widths and branch-Jacobian
conversion are not explicit.  Consequently no collision-time value can be
inserted into `C_fw/C_rev/q`; numerical `H_cover`, numerical beta and Gate 4
remain unclosed.

## Gate 4: two proper views on one once-charged law

The Round-50 raw-parent type gap is repaired without asserting that raw
`K_par` is proper.  On the raw once-charged law define the two Borel regular
views and their stopped properisations

```text
V_fw,
V_rev=I o H,
P_sigma=T_s^(696*Dbar) o V_sigma.
```

On the integer `Dbar` strata these maps are Borel injections modulo the frozen
null boundaries.  Lusin--Souslin supplies the inverse, and

```text
Theta=P_rev o P_fw^-1
```

pushes the selected forward proper reference law exactly to the reverse
proper view.  Thus both Round-49 per-view survivor clocks live on one
once-charged proper reference law.  For each fixed `|s|<=1/400`, finite
return depth, fixed finite terminal schedule and fixed finite terminal time
`H`, and integer `r>=1`, the physical postproperisation join is now
unconditional at that stated scope:

```text
integral (W_r^*)^r dm^* < (1+2*A_H)*integral p dlambda.
```

It does not create a common survivor shell or a proper forward/reverse
intersection.  The complete clock additionally requires

```text
I_D = integral p(y)*exp(Dbar(y)/6) dlambda(y) < infinity.
```

The exact defect is

```text
Dbar(M)=0       for 0<=M<=310,
Dbar(M)=M-309   for M>=311,
```

and skips the value one.  A same-kernel short-end tail
`mass{M>m}<=C_len*2^-m` would imply the needed moment by an exact layer-cake
bound, but current finite-depth and D1 fields provide only tail convergence.
An explicit Borel band countermodel proves that this convergence alone does
not imply the exponential moment.  Hence `C_fw/C_rev/q` and strong cemetery
remain open for a precisely identified physical reason.

## Gate 5: face-local full-rank seed and common dynamic envelope at `s=0`

Every certification in this section is restricted to the base parameter
`s=0`, exactly as in the Gate-5 leaf certificate.  No parameter-uniform
promotion is made.

The five face grammars were separated at the one-step insertion ledger.  Four
face classes have zero or rank-free forcing there; only moving occurrence
needs the full `2^B` weight.  The Round-39 coarea tail can therefore be used at
the physical exponent `theta=1` on its own raw occurrence measure:

```text
integral 2^B dm_occ < 23253221519103/880000.
```

This closes the former seed-exponent concern.  It is deliberately not a full
F10 theorem: the raw endpoint/coarea law has not yet been transported to the
owner-deduplicated arbitrary-`R_n` leaf law.  Owner-once and scalar paired
cancellation do not repair that measure mismatch; an exact signed-current
countermodel shows that finite scalar mass still need not control the `C1`
dual norm.

One common algebraic suffix test envelope is now installed:

```text
||phi||_env = sup_(S in S_reg) ||phi o S||_C1,
C_dyn=1.
```

Every regular suffix pullback and dual pushforward has norm at most one.  The
explicit maps

```text
A_L=diag(L,L^-1),
U_L=[0,L^-1]x[0,1],
V_L=[0,1]x[0,L^-1]
```

show why this algebraic result is not yet physical F17: area preservation
keeps the normal Piola flux exact while a physical full-gradient pullback can
grow like `L`.  A billiard-specific anisotropic test space with a finite
physical CM2 embedding remains necessary.  No physical one-step `kappa_B`,
aggregate `Z_B` resolvent, face-tower moment, full F10, F17 or strong F13 is
promoted.  Gate 5 remains **10/18**, with **0** complete 18-field blocks.

## Latest-technology audit

The official sources used in the active routes were rechecked at versions
`1210.0011v4`, `2104.06947v3`, `2604.19671v2`, `2604.25881v1`,
`2606.10155v1` and `1807.02330v4`.  The uniform memory-loss theorem is strong
enough for the new common finite `H_bump`, but its proof does not publish the
effective constants needed to turn that time into an integer.  The newer
sufficient-rectangle, Growth/small-hole and anisotropic-space results likewise
do not provide the missing numerical hit time, same-kernel short-end moment,
owner-law transfer or physical full-gradient F17 embedding.

## Strict status

```text
Gate 1: NOT CERTIFIED
Gate 2: NOT CERTIFIED
Gate 3: NOT CERTIFIED
Gate 4: NOT CERTIFIED
Gate 5: NOT CERTIFIED (10/18; complete blocks 0)
CM2:    NO-GO
```

## Validation

- syntax: `6/6`;
- duplicate-key and non-finite-constant rejecting strict JSON: `3/3`;
- verifier integrity/replay and independent exact arithmetic: PASS;
- hostile manifest mutations: `90/90 + 104/104 + 100/100 = 294/294`
  rejected;
- cert/verifier default fail-close: `6/6`, each exactly exit `2`;
- deterministic manifest regeneration: `3/3` byte-identical;
- three leaf SHA ledgers: PASS.

The hostile counts are fail-close mutation tests, not independent mathematical
proofs.  Separate read-only audits checked the literature quantifiers,
coordinate gauge, object-level two-view transport, exact fractions, measure
types and all nonpromotion boundaries.

## Next route

1. Effectivise the uniform memory-loss application by exposing numerical
   coupling/magnet constants, producing a numerical whole-family `H_bump`.
   This does not produce the distinct per-leaf crossing clock `H_cover`.
2. Independently build an interval-certified common Cantor-crossing atlas
   (or a new typed conversion theorem) to obtain numerical `H_cover` and the
   actual crossing fraction `beta`.
3. On the grouped physical kernel, prove the same-measure short-end tail
   `mass{M>m}<=C_len*2^-m` or directly the `exp(Dbar/6)` moment.
4. Join that moment with the appropriate numerical clock and the
   two-proper-view law to construct the corresponding `C_fw/C_rev` moment.
   Prove a proper same-ID forward/reverse return separately as a geometric
   theorem; it does not follow from the moment or measure transport.  Only
   then construct collision-time `q` and strong cemetery.
5. For Gate 5 at `s=0`, transport the raw `4^-b` moving-occurrence tail to the
   arbitrary-`R_n` owner law (or prove paired summable image separation),
   derive physical `kappa_B<2rho/(1+rho)` and close the face-tower resolvent.
6. Construct one billiard anisotropic test space with a finite physical CM2
   inclusion and full-gradient current pairing; then close dynamic F17,
   strong F13, full F10, F14/F15/F18 and Gate-3 `MT_DQ`.

Gate 1 and Gate 2 remain independent structural poles throughout.

Leaf reports:

- `cm2-gate34-round51-uniform-gauge-bump-frontier-assault-2026-07-20.md`
- `cm2-gate34-round51-two-proper-view-common-law-assault-2026-07-20.md`
- `cm2-gate5-round51-face-sparse-zb-dynamic-envelope-frontier-assault-2026-07-20.md`
