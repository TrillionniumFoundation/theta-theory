# CM2 Round 69 full direct assault

Date label: 2026-07-21

Strict verdict: **0/5 complete composite gates; Gate 2 official fields 0/17;
Gate 4 landing join 1/7 with fields 1, 4 and 7 partial; Gate 5 maturity
10/18 with zero complete blocks; CM2 remains `NO-GO_FOR_CLAIM`.**

## Round result

Round 69 pins Round 68 and reopens the proposed common-root route against the
original physical ledgers rather than another abstract model.

1. **Positive base-parameter return root.**  The frozen half-open ownership
   rule selects exactly 176 of the 352 return boxes touching `s=0`.  Their
   disjoint source rectangles have fixed-`s` collision mass strictly between
   `191/8000000` and `267591/8000000000`.  They form 16 directed reciprocal
   core edges in eight unordered pairs; every atom has one strict first owner,
   lands inside its declared destination core, has collision-area inverse
   Jacobian one and zero log area distortion.
2. **Direct-equality route rejected by physical support.**  The 64 moving
   first-event occurrence faces are disjoint from all 24 C24 core interiors:
   `1536/1536` core/face pairs and `269824/269824` strict-inner-atom/face pairs
   are exhausted with zero intersections.  Hence the selected root has
   `11264/11264` disjoint raw occurrence pairs.  Restricting to a fixed owner
   cannot create source support.
3. **Correct registry type.**  The missing global object is a typed incidence
   graph between path cells, time-qualified physical pullback faces and their
   one-sided traces.  A face is incident to a path-cell boundary; it is not an
   interior path point.  R1-inner moving-occurrence incidence is certified
   empty, while arbitrary-`R_n` nonempty pullback components and connected
   ranks remain unmaterialized.
4. **Gate 1/3.**  The actual base root exports nine carrier fields, but no
   same-key `Q,E,u,v`, stable-plaque side, all-cell material radius, uniform
   Piola remainder, two-sided physical trace/current or stopped `MT_DQ` row.
5. **Gate 2/4.**  The 176-atom depth-one graph sharpens Gate-4 field 4 at the
   base parameter.  Stable plaques, holonomies, all-depth commuting squares,
   marker saturation, path budget and the strong recipient remain absent.
6. **Gate 5.**  All 176 atoms inherit one actual same-root candidate-local
   F1--F13 packet, including 176 empty F10 and 176 empty F13 slots.  This is
   local maturity `13/18`, not a complete global block; F14--F18 and all seven
   missing positive-potential sectors stay open.

## Audit

The new producer and independent verifier compile.  Canonical audit,
independent replay and byte-identical re-emission pass.  The replay checks all
4,216 return atoms, the exact 176/176 half-open base ownership split, the mass
ledger, reciprocal core graph and `11264/11264` selected occurrence
disjointness.  The hostile suite rejects `192/192` semantic mutations and
`4/4` strict-JSON attacks.  Default producer and verifier entry points both
exit `2`.

## Technology boundary

A same-day arXiv API search finds no dispersing-billiard stable-holonomy or
time-dependent-billiard transfer-operator result that supplies the missing
typed incidence or moving-boundary operator estimates.  The sequential results
`2502.07765v2` and `2104.06947v3` do not close those rows.  No external theorem
is promoted.

## Shortest remaining route

Materialize one nonempty time-qualified pullback of a frozen physical carrier
on an actual `R_n/Q_n` path component, assign its canonical connected rank and
two side traces, and certify the incidence edge.  Then attach owner/current
data to the face node and stable/material/operator data to the path node.
Direct equality between event faces and return-core interiors is permanently
retired.
