# CM2 Round 56 Gate-4 terminal `J_pair` / Kac-mesh closure

Date: 2026-07-20  
Strict status: **the physical adapted-metric terminal numerator `J_pair` and
the Round-53 defect moment `I_D` are now finite.  `J_cap`, a physical proper
same-ID first-return kernel, intermediate avoidance, later recovery clocks,
physical `q`, the strong cemetery, Gate 4, and CM2 remain not certified.**

## 1. Frozen scope

This append-only leaf pins twelve earlier manifests: the Round-27 regular
first-return path schema, the collision-SRB Kac baseline, the Round-41/42
hereditary C24 Growth theorem, the maximal-word inner-core characteristic
ledger and its 24-core strong multiplier, the two Round-35 carrier/rank
ledgers, the Round-50 whole-family disintegration, the Round-53
`J_pair -> I_D` bridge, and both Round-55 Gate-4 leaves.  No old file is
modified.

All statements are parameterwise and uniform for every fixed
`|s|<=1/400`.  The hereditary replay is for the stationary physical map
`T_s`; no arbitrary moving-map sequence is asserted.

## 2. Exact slope-four paired reverse replay

Equality of represented measures is not enough: two standard-family
representations of `mu_s|C_s` can cross transversely, while `Z` depends on
the leaf representation.  We therefore start from the exact Round-35 kernel

```text
Psi_s(b,r)=(r,phi=4r+b),  p=sin(4r+b),
rho_leaf=cp/sqrt(17).
```

The base construction never uses a full-grazing collision-section parent.
The 24 physical cores are compact central-homogeneity cores.  Use finitely
many half-open collision-chart envelopes covering only those cores, retain
only slope-four parents whose closure stays inside `|p|<3/10` and meets
`C_s`, then chop at the fixed adapted `delta_open` scale.  On every retained
parent

```text
cp=sqrt(1-p^2)>sqrt(91)/10>19/20,
|d_r log cp|=4|p|/cp<24/19.
```

Together with `ell_E<=(27/5)ell_*` and `delta_open<10^-90`, the strict log
oscillation is below `x=648/(95*10^90)<1/2000`; hence
`exp(x)<1/(1-x)<2000/1999`.  Thus the conditional density lies in the
registered ratio class.  Finite safe chart and `b` ranges plus the fixed
positive chop give finite coarea boundary `Z`.  Applying the certified
24-core characteristic, whose strong standard-family norm is at most
`2000/1999`, gives

```text
G_A0=1_(C_s)G_slope,safe=mu_s|C_s,
Z(G_A0)<infinity.                                      (2.1)
```

This is an unnormalised exact Round-35 slope-four initial family.  Replay the
stationary `C_s`-killed Round-41/42 theorem from `G_A0`.  Its first-return
terminal law

```text
E_fw,total=disjoint_union_(n>=1)E_(fw,n)
```

has finite total `Z`.  Every proof atom is

```text
B'=T_s^n(A') subset B=T_s^n(A),
```

where `A'` is a subinterval of the exact Round-35 slope-four
`A subset R_n` leaf.  Singularity, homogeneity, core-boundary and canonical
Growth cuts only split analytic branch intervals; they never transversely
redisintegrate.  The induced first-return map preserves normalized
`mu_C_s`; restoring the common scalar `mu_s(C_s)` gives

```text
E_fw,total=(T_C_s)_#(mu_s|C_s)=mu_s|C_s
```

exactly once.

Set `G_IB=I_#E_fw,total`.  It is exactly `mu_s|I(C_s)`, is carried on the
tagged exact `I(B') subset I(B)` subcurves, and has finite `Z`.  For the
retained tag `B=T_s^n(A)`, time reversal gives

```text
T_s^k(I(B))=I(T_s^(n-k)(A)),  0<=k<=n.                (2.2)
```

Because `A subset R_n` has no intermediate `C_s` hit, (2.2) says that
`I(B)` first returns to `I(C_s)` at exactly time `n`, at `I(A)`.  Replay the
stationary `I(C_s)`-killed hereditary theorem from `G_IB`, keeping the same
path tag.  Every paired reverse terminal proof curve is an
`I(A'') subset I(A)` subcurve and their total `Z` is finite.  Finally define

```text
G_src,coarse=I_#E_rev,paired,total.
```

Then every proof carrier `A''` is a subinterval of the exact Round-35
slope-four `A` leaf, and

```text
G_src,coarse represents mu_s|C_s exactly once,
Z_src,coarse=Z(E_rev,paired,total)<infinity.           (2.3)
```

This paired construction uses only the safe finite-`Z` slope-four base, the
hereditary terminal theorem, induced invariance and time reversal.  The
later `B_max`/source-short ledger is not used to establish (2.3), so there is
no circularity and no same-measure leaf shortcut.

Forward and reverse remain two length views of one physical mass.  Proof-only
canonical chop labels are allowed below, but forgetting those labels recovers
each Round-35/50 physical cell once.  If a physical interval is the union of
proof intervals, its `p/ell` is a length-weighted average of their
`p_j/ell_j` and is therefore at most their sum.  Thus a finite proof
refinement bounds the physical numerator without duplicating mass.

## 3. Freeze the path rule at `B_max`

On a regular rank path put

```text
B_i(x)=B_inc(T_s^(i-1)x),  1<=i<=n,
B_max=max_i B_i,
delta_B=2^-ceil(3(B+1)/2).
```

The physical source rank-density mesh is one oriented half-open Euclidean
carrier grid at `delta_(B_max)`.  This is a valid refinement of the source
rank mesh because `B_max>=B_1`; the other ranks remain in the immutable path
code.  Exact parity arithmetic gives

```text
delta_B^-2 <= 16*2^(3B),
delta_(B_max)^-1
 <= sum_(i=1)^n delta_(B_i)^-1
 <= 4 sum_(i=1)^n 2^(3B_i/2).                       (3.1)
```

This resolves the Round-55 path-maximum/additive-union ambiguity without a
rank product.

## 4. Kac occupation pays the full cells

Write `R_n=C_s intersect {tau_C_s^+=n}`.  For `x in R_n`, the occupied tower
levels are

```text
x,T_s x,...,T_s^(n-1)x,
```

and the matching ranks are `B_1,...,B_n`.  In particular `R_1` has one
level, not zero levels.

The sets `T_s^i R_n`, `0<=i<n`, are pairwise disjoint modulo the frozen null
cemetery.  Indeed, invertibility plus an equality between two such levels
would force an earlier hit of `C_s` unless the tower labels agree.  Therefore
the general first-return occupation identity gives, for nonnegative `f`,

```text
sum_(n>=1) integral_(R_n) sum_(i=1)^n f(T_s^(i-1)x)dmu_s
 = integral_(saturation(C_s)) f dmu_s
 <= integral_X f dmu_s.                              (4.1)
```

No ergodicity or full-saturation equality is used.  Taking
`f=2^(3B_inc/2)`, the Round-35 physical moment and (3.1) yield

```text
sum_(n>=1) integral_(R_n) delta_(B_max)^-1 dmu_s
 < 4*(134217735/64)
 = 134217735/16.                                    (4.2)
```

This is a mass-weighted grid charge.  It is not multiplied by a separate
coarse-`Z` random variable.

## 5. The additive finite-`Z` source ledger

The exact physical source law also contains the fixed adapted `10^-90`
source recut.  To bind it without assuming grid alignment, first apply a
proof-only adapted `delta_open` chop to every coarse parent in (2.3).
Round 42 pins one fixed positive symbolic `delta_open<10^-90`.  Full cells
and at most two clipped cells give

```text
Z_src,pre,*
 <= mu_s(C_s)/delta_open
    +(4000/1999) Z_src,coarse
 < infinity.                                         (5.1)
```

Now intersect these proof parents with the Euclidean `B_max` grid.  The
standard-family length is adapted and Round 42 gives

```text
ell_E <= (27/5)ell_*,
ell_* >= (5/27)ell_E.
```

Hence all full Euclidean grid cells contribute at most

```text
(27/5) sum p/delta_(B_max)
 < (27/5)*(134217735/16)
 = 724775769/16.                                    (5.2)
```

There are at most two clipped endpoint cells in each proof parent.  Since
the invariant adapted-density ratio is at most `2000/1999`, those cells cost
at most `(4000/1999)` times the parent boundary term.  Thus

```text
Z_src,rank,*
 < 724775769/16
   +(4000/1999) Z_src,pre,*
 < infinity.                                         (5.3)
```

Finally common-refine with the frozen adapted `10^-90` source grid.  Every
rank-grid proof cell lies inside a `delta_open` proof parent, so its adapted
length is strictly below `10^-90`; it meets at most two half-open fixed-grid
cells.  The Round-55 refinement lemma gives

```text
Z_src,short,*
 <=(4000/1999) Z_src,rank,*
 < infinity.                                         (5.4)
```

Equations (5.1)--(5.4) close the issue that separate `L1` bounds on mesh
multiplicity and coarse `Z` would not close: the two charges enter
additively at the single-parent level before integration.

## 6. Third hereditary replay from the exact source-short law

The proof-refined `G_src,short` is still exactly the same positive measure
`mu_s|C_s`, with no survival normalization.  It is a controlled canonical
initial family:

- every cell is a positive interval restriction of an unstable parent;
- the invariant cone, curvature and density-ratio class are unchanged;
- its total adapted `Z` is finite by (5.4);
- it is dominated by the same admissible base-cone source;
- its survivor masses are the same physical `mu_s(Q_n)` as before.

The Round-41/42 hereditary every-collision C24-killed Growth theorem therefore
applies to this static representation.  This is the third hereditary use in
this leaf: exact slope-four forward, paired reverse, then source-short
forward.  Repeating the Round-55 finite-residue argument gives

```text
Z_img,short,*
 = sum_(n>=1) Z(forward terminal descendants of G_src,short)
 < infinity.                                         (6.1)
```

These proof descendants refine the physical images `B=T_s^n(A)`.  Positive
coarsening therefore gives

```text
Z_image,physical,short <= Z_img,short,*.
```

## 7. Fixed image recut closes `J_pair`

Round 55 proves that every connected image parent has adapted length below
`2397`, so its deterministic adapted `10^-90` image recut has at most

```text
N_img=2397*10^90+1                                  (7.1)
```

cells.  Common-refine this physical image grid with the proof terminal
chops.  Each proof target parent meets at most `N_img` image cells, and their
pullbacks split the matching proof source parent into the same number.
Apply the `R*N` refinement lemma with `R=2000/1999` in the adapted metric on
both views.  Physical cells are coarsenings of the proof cells, hence

```text
J_pair,*
 <= (2000/1999)(2397*10^90+1)
    (Z_src,short,*+Z_img,short,*)
 < infinity.                                         (7.2)
```

Here

```text
ell_fw,c  = ell_*(A_c),
ell_rev,c = ell_*(I(B_c))=ell_*(B_c),
```

and `p_c` is charged once.  Equation (7.2) is the physical Round-35/50
same-ID cell law, not merely a recordwise or abstract refinement statement.

## 8. Exact defect consequence and strict frontier

The Round-53 same-measure bridge now applies:

```text
I_D < nu(X)+[35/(99*2^309)]J_pair < infinity.        (8.1)
```

Certified by this leaf:

```text
B_max natural-mesh path rule:                 CERTIFIED
physical Kac-tower inverse-mesh charge:       CERTIFIED
exact Round35 slope-4 source-leaf inclusion:  CERTIFIED_BY_PAIRED_REVERSE_REPLAY
adapted metric / once-charge terminal join:   CERTIFIED
exact source 10^-90 finite-Z refinement:      CERTIFIED
hereditary replay on refined source law:      CERTIFIED
physical terminal refinement -> J_pair:       CERTIFIED
physical J_pair:                              CERTIFIED_FINITE
physical I_D:                                 CERTIFIED_FINITE
```

Still open:

```text
global/recordwise J_cap and D_cap,
physical proper same-ID first-return kernel,
intermediate C24 avoidance after properisation,
later and repeated recovery-clock moments,
physical collision-time q,
strong singular/current cemetery,
full numeric C_fw,C_rev,q,
Gate 4 and CM2.
```

Thus the strict verdict remains

```text
Gate 4: NOT_CERTIFIED
complete composite gates: 0/5
CM2: NO-GO_FOR_CLAIM
```

## 9. Executable evidence

Artifacts:

- `cm2_gate34_round56_terminal_j_pair_kac_mesh_closure_cert.py`;
- `cm2_gate34_round56_terminal_j_pair_kac_mesh_closure_verifier.py`;
- `cm2-gate34-round56-terminal-j-pair-kac-mesh-closure-manifest-2026-07-20.json`;
- `cm2-gate34-round56-terminal-j-pair-kac-mesh-closure-manifest-2026-07-20.sha256`.

The verifier independently reconstructs the parity rows, tower indexing,
the exact constants `134217735/16` and `724775769/16`, the source
`delta_open/B_max/10^-90` three-stage ledger, the image multiplier and the
Round-53 coefficient.  It checks strict JSON, dependency hashes, AST
nonpromotion literals, deterministic replay and byte-identical re-emission.

Validation targets:

```text
syntax:                         2/2 PASS
dependency hashes:             12/12 PASS
positive audit:                PASS
hostile mutations:             55/55 REJECTED
deterministic re-emission:      byte-identical PASS
certificate/verifier defaults: both exit exactly 2
SHA ledger:                     PASS
```
