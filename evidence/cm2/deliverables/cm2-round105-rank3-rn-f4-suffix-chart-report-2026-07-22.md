# CM2 Round 105 — rank-three RN/Gate5 F4 suffix charts

Round105 installs official immutable F4 `homogeneous_suffix_chart` slots on all twelve corrected rank-three faces.

For each face, both terminal q intervals are recovered strictly: source-cap ends use the bisection-certified source-core exit root, while grazing ends use the frozen grazing parameter bracket.  On the resulting whole-face q interval, the outgoing tangent direction

`u=((1-q^2)/(1+q^2), 2q/(1+q^2))`

has two nonzero component signs.  Hence the full third-tangency face lies in one accepted target open-semicircle collision chart.

- New immutable F4 slots: **12**.
- Rank-three face-local maturity: **4/18**.
- Complete 18-field blocks: **0**.
- First missing field: **F5 `inverse_Jacobian_bound`**.

The 640-bit independent replay reproduces every whole-face q enclosure and suffix chart and rejects 3/3 semantic mutations.  Global Gate5 remains `10/18`, blocks zero; CM2 remains `NO-GO_FOR_CLAIM`.

