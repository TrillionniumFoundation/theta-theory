# CM2 Round 63 Gate 1/3 — sharp gauge budget and incidence-reduced stopped-current frontier

Date: 2026-07-21  
Scope: append-only Gate-1/Gate-3 assault from the frozen Round-62 aggregate  
Strict verdict: **Round 63 does not promote Gate 1 or Gate 3.  It removes an
unnecessary Hölder sub-obligation from the Gate-1 third-gauge interface,
gives the sharp quantitative plaque-tempered threshold, and replaces the raw
stopped-component endpoint debt by an incidence-reduced physical-face
ledger.  The base-parameter regular physical part of that ledger joins the
already certified same-ID F13 Borel charge, but moving stopping-clock jumps,
cemetery traces, the anisotropic bulk Piola bound and physical strong
assembly remain unpaid.  Gate 1 and Gate 3 remain `NOT_CERTIFIED`, complete
composite gates remain `0/5`, and CM2 remains `NO-GO_FOR_CLAIM`.**

## 1. Frozen recursive inputs

This leaf pins the Round-62 aggregate report and recursive ledger, the
Round-62 Gate-1/3 leaf and independent audit, and the exact older leaves used
for the new joins.  No frozen artifact is modified.  The decisive imported
facts are:

```text
Round62 aggregate recursive ledger:                         PASS
compact QNL gauge selected twisting wedges:                CERTIFIED
compact QNL gauge connector class H:                       REFUTED
faithful diagonal representative class H:                  CERTIFIED
faithful diagonal same-axis twisting:                      REFUTED
actual stopped weak graph-TV R/Q:                          NORM-ONE
actual stopped cells: {Dbar=d} x word x maximal component
chart/lift duplicate trace quotient:                       CERTIFIED
proof-only dyadic refinement edges are physical faces:     FALSE
all-five regular physical F13 Borel payload:                CERTIFIED
same-ID F13 L^(6/5) moment and weighted tail:               CERTIFIED
physical strong F13/R/Q/Piola/MT_DQ:                        NOT_CERTIFIED
```

The imported Round-62 replay is not weakened.  Its generic mismatched-face
separator remains correct, but it applies only to genuinely unmatched
physical faces.  It must not be charged once per proof subdivision.

## 2. Gate 1 — the sharp plaque-tempered budget

### 2.1 Exact transport and the redundant approximant-modulus row

Use the frozen convention

```text
B(x)=C(fx)^(-1) A(x) C(x).
```

For a local stable pair `x,y`, put

```text
Delta_n^s(x,y)
 =A^n(y)^(-1)
  [C(f^n y)C(f^n x)^(-1)-I]
  A^n(x).                                             (2.1)
```

The finite identity is

```text
H_B^s(x,y;n)
 =C(y)^(-1)[H_A^s(x,y;n)+Delta_n^s(x,y)]C(x).         (2.2)
```

There is an identical backward formula for `Delta_n^u`.  Suppose on the
actual plaque registry that:

1. `A` already has canonical stable/unstable families `H_A^s,H_A^u` with
   one uniform Hölder exponent and constant;
2. `C,C^(-1)` are uniformly bounded and Hölder on that registry; and
3. `Delta_n^s,Delta_n^u -> 0` uniformly.

Passing to the limit in (2.2) gives the explicit formula

```text
H_B^s(x,y)=C(y)^(-1)H_A^s(x,y)C(x),                  (2.3)
H_B^u(x,y)=C(y)^(-1)H_A^u(x,y)C(x).                  (2.4)
```

The right sides are Hölder by the ordinary product estimate.  Therefore a
separate equi-Hölder modulus for every finite approximant is **not** an
independent Gate-1 debt once the three rows above are installed.  It is one
possible proof of the limit regularity, but it is stronger than necessary.
Round 63 accordingly compresses the third-gauge interface to:

```text
one all-plaque Hölder SL(2) transfer C with bounded inverse
+ uniform forward/backward renormalized-defect decay
+ the immutable periodic/homoclinic token.
```

At the periodic base `p`, both eigenaxes and the loop are transported by
`C(p)^(-1)`.  Each wedge is multiplied by `det(C(p)^(-1))`; for `C in SL(2)`
the four selected numerical wedges are unchanged.

### 2.2 Quantitative sufficient threshold

Assume on every local stable plaque

```text
d(f^n x,f^n y) <= L_s lambda_s^n d(x,y),
||A^n(y)^(-1)|| ||A^n(x)|| <= M_s kappa_s^n,
||C(z)-C(z')|| <= H_C d(z,z')^alpha,
||C(z)^(-1)|| <= K_C.
```

Then (2.1) has the exact uniform majorant

```text
||Delta_n^s(x,y)||
 <= M_s H_C K_C L_s^alpha
    (kappa_s lambda_s^alpha)^n d(x,y)^alpha.          (2.5)
```

The backward analogue uses `kappa_u,lambda_u`.  Hence the two strict
inequalities

```text
theta_s=kappa_s lambda_s^alpha < 1,
theta_u=kappa_u lambda_u^alpha < 1                    (2.6)
```

are a directly checkable sufficient interface.  They are not asserted for
the physical third gauge.

The threshold is sharp in the logical `SL(2)` model.  Take

```text
A=diag(a,a^(-1)),
C(f^n y)C(f^n x)^(-1)=I+lambda^n E_21.
```

Then

```text
A^(-n)[lambda^n E_21]A^n=(a^2 lambda)^n E_21.        (2.7)
```

The executable replays three rational regimes:

```text
a=3/2, lambda=1/4: theta=9/16 <1, defect ->0;
a=2,   lambda=1/4: theta=1,      defect stays E_21;
a=2,   lambda=1/2: theta=2,      defect diverges.
```

Thus non-strict critical control is insufficient.  Determinant one,
same-token equality and Hölder regularity alone still do not pay (2.6).

### 2.3 Physical Gate-1 boundary

The only currently materialized transfer to the compact twisting gauge is
already tied to the frozen connector/shadow divergence.  The local disjoint
jet repairs show that those two finite jets are not a universal no-go, but
they do not give an all-plaque transfer satisfying (2.6) or direct uniform
defect decay.  The physical ledger is therefore:

```text
exact endpoint-conjugacy Hölder inheritance:              CERTIFIED
separate finite-approximant equi-Hölder row:               NOT_INDEPENDENTLY_REQUIRED_UNDER_ROWS_1-3
theta<1 sufficient threshold, sharp in diagonal model:     CERTIFIED_SUFFICIENT_SHARP_IN_MODEL
critical theta=1 implication:                              CERTIFIED_FALSE
actual all-plaque third transfer and two-sided defects:     NOT_CERTIFIED
same physical representative class H plus twisting:        NOT_CERTIFIED
Gate 1:                                                     NOT_CERTIFIED
```

## 3. Gate 3 — incidence reduction before total variation

### 3.1 Oriented face formula

Let the actual stopped regular cells carry oriented moving branch currents.
At a shared codimension-one face `e` between cells `i` and `j`, the two
Reynolds boundary contributions assemble to

```text
B_e=v_e[(Y_i)_*(rho_i trace_e)
        -(Y_j)_*(rho_j trace_e)].                      (3.1)
```

Here `v_e` is the signed face speed in one common source coordinate, `rho`
is the source density trace, and `Y` includes the physical landing and every
immutable tag retained by the target carrier.  The face cancels before
absolute values exactly when the quotient identifies both sides and all of
the following agree:

```text
same regular physical branch/formula,
same source density trace,
same signed face velocity,
same physical landing trace,
same retained tag (or the same exact quotient tag).
```

Half-open ownership is only a mass owner.  It neither proves the trace
equalities nor cancels a physical current.

### 3.2 Actual stopped-cell cut taxonomy

The Round-61 cells are

```text
A_(d,w,c)={Dbar=d} intersect {physical word w}
           intersect {maximal connected component c}.       (3.2)
```

This definition has an important consequence: CAD boxes used to prove a
component, and the dyadic boxes used to certify a root, are not additional
cells in (3.2).  Their internal edges never enter the incidence chain.  The
frozen cancellation-free leaf explicitly calls its refinement edges
bookkeeping, not physical faces.

After quotienting the actual chain, the cuts have the following strict
types.

| cut class | current treatment | frozen support |
|---|---|---|
| proof-only CAD/dyadic subdivision | absent from the chain | actual cells use maximal components; refinement edges are bookkeeping |
| duplicate source-chart seam | exact cancellation before TV | identical physical trace and opposite orientation |
| duplicate Euclidean lift seam | exact cancellation before TV | identical torus trace |
| internal chart/homogeneity cut with matching quotient traces | exact cancellation before TV | frozen F13 assembly rule |
| deterministic recut inside one unchanged regular branch | cancels only after the finite-`s` trace/velocity match is checked | measure partition identity alone is insufficient |
| source-core clipping in fixed common source coordinates | zero parameter current | F13 grammar 1 |
| intermediate/terminal C24 preimage | retain physical two-trace current | F13 grammars 2--3 |
| collision singularity/owner change | retain physical current | F13 grammar 4 |
| moving occurrence | retain physical current | F13 grammar 5 |
| true grazing/tie/outer endpoint/cemetery | retain unless a null/trace theorem applies | strong cemetery open |
| change of `Dbar`/stopping schedule | retain clock-jump current | not one of the five F13 grammars |

Consequently the Round-62 raw component-count separator is not an automatic
debt of the actual partition.  It remains a sharp separator for arbitrarily
many **genuinely mismatched** faces.

### 3.3 Same-ID F13 join on the regular base layer

For one fixed common `R_n` restriction ID, time `j`, physical face kind,
carrier seed and side label, the Round-44 slot token is exactly the data
retained by the regular part of (3.2), with `d,w,c` acting only as further
restrictions.  Incidence reduction therefore maps every nonzero regular
physical face of the five grammars to the already certified F13 Borel
two-trace payload.

At the base parameter `s=0`, this gives

```text
E_regular,physical <= c_F13,n,
c_F13,n <= (3816937/7800000)c_X,n
           < (1/2)c_X,n=(25/302)c_D1,n.               (3.3)
```

The pinned F13 `L^(6/5)` moment and block exponent `1/6` therefore pay the
incidence-reduced **regular Borel TV** face ledger on the same IDs.  This is
not a strong trace pullback, a finite-`s` derivative, or a cemetery bound.

### 3.4 The stopping-clock jump is a distinct sixth debt

The safe defect clock is

```text
Dbar(M)=0                         if 2^M<=C_p,
Dbar(M)=min{d>=1:2^(M-d)<C_p/2}   otherwise,
R0=696 Dbar.                                           (3.4)
```

The exact rational replay gives

```text
M=310 -> Dbar=0,
M=311 -> Dbar=2,
M=312 -> Dbar=3,
...
```

so the first stopping-time change is `0 -> 1392` collisions and later
thresholds change by `696`.  If a finite-`s` threshold face moves with speed
`v_e`, its current is

```text
B_e^clock
 =v_e rho_e[(Y_d)_*delta_e-(Y_(d'))_*delta_e],        (3.5)
```

where `Y_d` and `Y_(d')` are different stopped outputs and the time label is
immutable.  It cancels only if those **tagged** outputs coincide; half-open
ownership of the threshold does not help.  Neither the five-face F13 ledger
nor the base Borel stopped kernel supplies the threshold trace, its speed,
or the sum of (3.5).

Thus the exact reduced strong ledger is

```text
E_stop^red = E_regular,physical + E_clock + E_cemetery,      (3.6)
```

after all proven artificial incidences have been removed.  The first term
is paid only in the base regular Borel layer.  The latter two terms and the
moving strong version of the first remain open.

### 3.5 Bulk Piola and strict Gate-3 boundary

Incidence reduction changes no bulk derivative.  Round 62's exact
regular-branch formula and determinant-one separator remain decisive: an
anisotropic moving-billiard bound for the bulk Piola series is still needed.
Accordingly:

```text
proof-CAD/dyadic fragmentation charge:                   REMOVED
chart/lift exact duplicate incidence:                    CERTIFIED_ZERO
regular five-grammar base Borel face join to F13:        CERTIFIED
moving clock-jump current formula:                       CERTIFIED_INTERFACE
moving finite-s E_clock/E_cemetery summability:          NOT_CERTIFIED
anisotropic bulk directional-Piola summability:          NOT_CERTIFIED
physical strong R_s/Q_s and MT_DQ:                       NOT_CERTIFIED
Gate 3:                                                   NOT_CERTIFIED
```

## 4. Latest official-technology audit

The 2026-07-21 official searches for moving dispersing billiards,
shape/Piola derivatives, anisotropic transfer spaces and cocycle holonomy
were rechecked against the Round-62 audit.  No newer theorem supplies either
remaining physical interface.

- `arXiv:2603.19509v3` assumes common strong spaces, strong
  differentiability and memory loss.
- `arXiv:2604.19671v2` begins with already regular standard families for a
  fixed billiard with a changing hole.
- `arXiv:2606.10155v1` is an anisotropic billiard-space survey, not an
  all-depth moving-domain trace/Piola theorem.
- Butler--Park `arXiv:1909.11548v2` defines the canonical class-H limits; it
  does not supply a non-fibre-bunched third gauge.

No external theorem is promoted.

## 5. Strict continuation

```text
Gate 1:                  NOT_CERTIFIED
Gate 3:                  NOT_CERTIFIED
complete composite gates: 0/5
CM2:                     NO-GO_FOR_CLAIM
```

The shortest next routes are:

1. **Gate 1:** materialize one actual all-plaque `SL(2)` transfer and prove
   either the two strict budgets (2.6) or direct uniform defects.  No extra
   approximant-Hölder row is needed after that.
2. **Gate 3:** construct one finite-`s` atlas for the incidence-reduced
   physical chain, prove the clock-threshold/cemetery trace sums in (3.6),
   and install an anisotropic bulk directional-Piola bound.  Only then can
   the stopped `R_s/Q_s` telescope be called physical `MT_DQ`.

## 6. Executable evidence

- `cm2_gate13_round63_incidence_reduced_current_sharp_gauge_frontier_cert.py`;
- `cm2_gate13_round63_incidence_reduced_current_sharp_gauge_frontier_verifier.py`;
- `cm2-gate13-round63-incidence-reduced-current-sharp-gauge-frontier-manifest-2026-07-21.json`;
- `cm2-gate13-round63-incidence-reduced-current-sharp-gauge-frontier-manifest-2026-07-21.sha256`.

The producer and independent verifier pin the recursive artifacts, replay
the exact sharp-threshold rows, the incidence cancellation, the rational
safe-clock jumps and the F13 ratios, require canonical JSON, reject hostile
semantic/JSON mutations, regenerate the manifest byte-for-byte, and fail
closed with default exit `2`.
