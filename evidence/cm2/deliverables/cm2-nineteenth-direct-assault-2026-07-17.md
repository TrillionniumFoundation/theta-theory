# CM2 nineteenth full direct assault

Date: 2026-07-17 (Asia/Shanghai)  
Frozen baseline: the first eighteen rounds remain read-only; this pass is
append-only  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; fully closed
composite gates remain `0/5`.  Gate 1 is reduced to a common-plaque frame
transport problem and the naive small-amplitude IFT promotion is rejected.
The recovery line makes a physical structural advance: all 64 occurrences
now have immutable recovery carriers, positive-width event patches, and one
certified two-sided dyadic phase-space shell with common regular suffix
charts.  Parameter-DQ identification, first 24-core entrance, no-recut dwell
and common strong-space typing remain open.**

## 1. Executive result

- **Gate 1:** the coupled half-density equations are exactly wedge equations
  for the two columns of one determinant-one frame.  On every nondegenerate
  plaque, all pair equations are equivalent to one common `2x2` transport
  with determinant equal to the diagonal ratio.  This exposes the correct
  unknowns but does not solve their stable/unstable compatibility.  A plain
  small-amplitude IFT is not promoted because no compatible bounded right
  inverse is installed and critical exponent loss is amplitude-independent.
- **Gates 3/4, boundary recovery:** all 64 hit/miss pairs now receive 128
  oriented trace IDs, 64 shared event-base signatures, 64 physical recovery
  carrier IDs and exact boundary collision offsets `hit/miss=1/0`.  At the
  singular limit, hit and miss meet on the same regular collision graph over
  the certified miss owner.
- **Gates 3/4, positive-width recovery:** every carrier contains a full
  `(z,s)` square of half-width `1/1024` with immutable physical label and one
  regular suffix chart.  A further phase-angle assault constructs 64
  two-sided dyadic shells, or 128 oriented tubes.  The hit tube first hits the
  tangent target and then the miss owner; the miss tube hits that owner
  directly.  Both successor states are regular and share one suffix chart.
- **Gate 2:** unchanged at `0/17`.  Recovery carrier IDs live on collision
  event rows and still do not supply a stable-saturated quotient base,
  `pi^s`, `rho`, reverse weights, native stopping or PPE.
- **Gate 5:** unchanged at selected maturity `2/18`, complete physical blocks
  `0`.  No homogeneity table, three-norm intertwiner or physical Kac term is
  promoted in this pass.

## 2. Gate table

| gate | strongest nineteenth-round layer | exact remaining core gap | status |
|---|---|---|---|
| 1 | common-plaque transport lemma for both half-density column systems; naive amplitude-only IFT rejected | construct one shared determinant-one frame with `q>=q_*` and selected twisting | **OPEN** |
| 2 | collision-event recovery IDs now physical and immutable | stable-saturated base, `pi^s`, `rho`, `p_a`, endpoint/stopping/PPE packet; `0/17` | **OPEN** |
| 3 | 64 recovery carriers, 64 event patches, 128 transverse phase tubes, common regular suffix charts | identify the actual parameter-DQ transverse coordinate, prove all-scale convergence and install strong `DQ/MT_DQ/FACE` maps | **OPEN** |
| 4 | two sides physically recover to one regular suffix chart on one dyadic shell | first 24-core/cemetery destination, arbitrary-scale shell registry, `2018/12108` no-hidden-recut itinerary and common strong norm | **OPEN** |
| 5 | eighteenth selected fields `1,7` on 28 roof slots retained | complete homogeneity tables, fields `2–6,8–18`, three norms and physical four-term Kac | **OPEN** |

## 3. Gate 1: common-plaque transport

For

```text
a=q^(-1/2)(1,u)^T,
b=q^(-1/2)(v,1)^T,
q=1-uv,
```

the critical coordinates are

```text
c_s(x,y)=det(a(x),a(y)),
c_u(x,y)=det(b(y),b(x)).
```

The stable half-density equation is therefore

```text
det(a(sigma x),a(sigma y))
 = r(x) det(a(x),a(y)).
```

If two columns on one plaque are noncollinear, they and their images define a
unique matrix `M_s`.  All other image columns are forced because their
coordinates in the anchor basis are wedge ratios; the common factor `r`
cancels.  Hence

```text
a(sigma z)=M_s a(z),
det(M_s)=r
```

for the entire plaque.  The time-reversed statement holds for `b` on
unstable plaques.  Exact rational replay verifies the lemma on four vectors
and rejects a perturbed image that violates common transport.

This reduction replaces an unstructured all-pair nonlinear solve by the
following precise compatibility target:

```text
future-plaque transports M_s,
past-plaque transports M_u,
one shared determinant-one frame [a,b],
uniform q lower bound,
selected twisting in that same frame.
```

### 3.1 IFT nonpromotion

The half-density multiplier has zero first derivative at amplitude zero but
is generically nontrivial at quadratic order.  The exact sample gives

```text
q_shift_product-q_source_product
 = -5 epsilon^2+13 epsilon^4
 = -487/10000 at epsilon=1/10.
```

After multiplication by `du`, the critical residual is cubic.  Cubic size
alone is insufficient: the exact exponent model

```text
r=1/16, theta=1/2,
normalized residual = epsilon^3(theta/r)^n
```

has `theta/r=8`, so every nonzero amplitude eventually grows.  This is a
fail-closed warning, not an impossibility theorem for every physical gauge.
A valid IFT still needs a right inverse preserving common-plaque transport
and bounded in the actual anisotropic norm.  Neither is present.

## 4. Boundary recovery carrier registry

For one event row the source ray is tangent to `T` at time `ell_T` and hits
the later miss owner `M` at `ell_M>ell_T`.  Tangent reflection is the identity,
so at the singular boundary

```text
hit:  source -> T(grazing) -> M,
miss: source -------------> M.
```

Both traces arrive at `M` with the same incoming state and therefore share
the same regular outgoing collision graph.  The exact collision offsets to
that graph are `1/0`.

The full immutable registry contains

```text
maximal occurrence rows:                  64,
oriented hit/miss trace seeds:           128,
shared event-base signatures:             64,
common boundary recovery carriers:        64,
Jx reflection carrier pairs:              32.
```

At every selected rational witness, 384-bit Arb replay proves

```text
source cos(phi) > 3/20,
source |p|      < 99/100,
ell_M-ell_T     > 31/100,
ell_M           < 8/5,
miss |p|        < 97/100,
miss cos(phi)   > 6/25.
```

Thus the former blanket statement “no recovery carrier or transport time” is
no longer accurate.  The strict replacement is: boundary carrier IDs,
shared base restrictions and `1/0` collision offsets are materialized, while
open-shell native transport time and first core destination are not.

## 5. Positive-width event patches

Every carrier contains the uniform square

```text
z in [z_* - 1/1024, z_* + 1/1024],
s in [    - 1/1024,     + 1/1024].
```

The complete endpoint classifier verifies one target, side, miss owner and
coarea polarity on each entire box.  Every box maps to one regular suffix
collision chart with

```text
source cos(phi) > 1/10,
source |p|      < 199/200,
ell_M-ell_T     > 1/5,
ell_M           < 2,
miss |p|        < 199/200,
miss cos(phi)   > 1/10.
```

There are 64 positive-width event patches.  Their labelled occurrence-sheet
coordinate area is `1/4096`; this is not collision-SRB mass.

## 6. Dyadic transverse phase-space shells

The next replay rotates the tangent outgoing velocity by

```text
1/65536 <= |alpha| <= 1/32768.
```

The hit sign is `epsilon`, the miss sign is `-epsilon`.  Complete local
first-hit comparison uses both obstacle species on a `9x9` lattice window
centered at the current collision lift, excluding the current obstacle.
Every owner flight is strictly below `2`, so the radius-4 lattice window is
complete for the certified itinerary.

All 64 rows pass after three base refinements:

```text
half-width 1/1024: 36,
half-width 1/2048: 24,
half-width 1/4096:  4.
```

The physical itineraries are

```text
hit tube:  source -> tangent target -> fixed miss owner,
miss tube: source ------------------> fixed miss owner.
```

Both successor tubes are regular, have `cos(phi)>1/20`, `|p|<999/1000`, and
occupy the same suffix chart per occurrence.  They are not claimed to be the
same state.  The output is

```text
dyadic transverse recovery shells:       64,
oriented phase-space tubes:              128,
common regular suffix charts:             64.
```

This is a phase-angle shell.  It has not been identified with the actual
moving-parameter transverse coordinate of `DQ`, and one slab is not an
all-scale differentiability family.

## 7. Technology audit

- The general web-search endpoint reached its external weekly quota; no
  conclusion is based on that failure.
- Direct arXiv API replay on 2026-07-17 found no theorem that constructs the
  required non-fibre-bunched common half-density frame.
- Butler--Park `arXiv:1909.11548` remains the class-`H` definition context and
  assumes convergence/Hölder regularity of canonical holonomies.
- Kalinin--Sadovskaya `arXiv:2604.13401`, Theorem 1.3, requires conjugate
  periodic data, Hölder control of the periodic conjugacy at a periodic point
  and sufficiently narrow periodic spectrum.  Those assumptions do not
  solve the present common-frame compatibility.

No external theorem is used beyond its checked hypotheses.

## 8. Strict final boundary

```text
COMPOSITE GATES FULLY CLOSED:                         0/5
UNCONDITIONAL CM2:                                    NO-GO FOR CLAIM

COMMON-PLAQUE TRANSPORT REDUCTION:                    CERTIFIED
PLAIN SMALL-AMPLITUDE IFT PROMOTION:                  REJECTED
BOUNDARY RECOVERY CARRIERS:                            64 CERTIFIED
ORIENTED BOUNDARY TRACE SEEDS:                        128 CERTIFIED
UNIFORM POSITIVE-WIDTH EVENT PATCHES:                  64 CERTIFIED
DYADIC TRANSVERSE PHASE-SPACE SHELLS:                  64 CERTIFIED
ORIENTED TRANSVERSE PHASE-SPACE TUBES:                128 CERTIFIED
COMMON REGULAR SUFFIX CHARTS:                          64 CERTIFIED

COUPLED HALF-DENSITY FRAME PLUS TWISTING:             NOT CERTIFIED
STABLE QUOTIENT / PHYSICAL PPE:                       NOT CERTIFIED
PARAMETER-DQ TRANSVERSE IDENTIFICATION:               NOT CERTIFIED
ALL-SCALE STRONG DQ / MT_DQ / FACE:                   NOT CERTIFIED
FIRST 24-CORE OR CEMETERY DESTINATION:                NOT CERTIFIED
NATIVE 2018/12108 NO-RECUT DWELL:                     NOT CERTIFIED
COMPLETE 18 FIELDS / THREE NORMS / PHYSICAL KAC:      NOT CERTIFIED
```

## 9. Next direct attacks

1. **Parameter-DQ shell identification:** compute the exact Jacobian from
   moving parameter displacement to the signed angular transverse coordinate
   on all 64 patches; extend the one dyadic slab to a nested all-scale shell
   family and match its coarea normalization to `FACE_TIME`.
2. **First-core propagation:** jointly propagate the two regular suffix tubes
   with adaptive Arb subdivision until their first 24-core entrance; if a
   uniform entrance fails, enlarge the contracting-core registry or assign a
   typed cemetery with an explicit payload.
3. **Native no-recut schedule:** on each transported tube, certify no hidden
   singular cut during the `2018` shell dwell and `12108` raw field-7 dwell,
   then attach the existing scalar Green constants to physical carrier IDs.
4. **Gate 1 common frame:** solve the future/past common-transport frame
   compatibility directly, establish `q>=q_*`, and recompute selected
   twisting in that same representative.
5. **Operator closure:** bind the transported registry to strong
   `DQ/MT_DQ/FACE`, complete fields `2–6,8–18`, the three norm intertwiners,
   stable quotient/PPE and the four physical Kac terms.

The highest-leverage line is now the parameter-DQ/all-scale identification
followed by first-core propagation.  The former converts the certified
phase-space tubes into the actual derivative shell; the latter unlocks the
existing sparse-dwell contraction for Gates 3, 4 and 5.

## 10. Reproduction

Four new fail-closed verifiers support replay/integrity, hostile self-tests
and live mode.  New hostile mutations rejected: `76/76`; together with the
eighteenth frozen cumulative total, the frontier total is `196/196`.  Every
new replay passes and every live verifier deliberately exits `2` because the
composite gates remain open.  Individual and aggregate SHA-256 sidecars
freeze the complete nineteenth-round chain.
