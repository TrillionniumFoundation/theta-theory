# CM2 Round252 — Wall s/t-Face Patch Saturation

## Result

- Exhaustively reconciled all `22,264` remaining same-chart, same-target wall contacts across `1,304` root pairs.
- Certified `22,016` strict two-sided physical patches at 256-bit interval precision.
- Accepted all `22,008` s-face contacts and exactly `8 / 256` cross-root t-face contacts.
- Kept the other `248` t-face contacts fail-closed because no common strict two-sided return-signature corridor exists through dyadic depth four.
- No contact was accepted from coordinate touch, exact-key equality, or signature intersection alone.

## Quotient Delta

- Added `22,016` certified physical adjacency edges.
- Exactly `16,296` edges reduce quotient rank; `5,720` are independently certified redundant physical edges.
- The mixed-sheet quotient reduces from `82,036` to `65,740` components.
- Cumulative physical adjacency edges increase from `62,452` to `84,468` over `133,684` virtual-stratum nodes.
- The final quotient has `440` seeded and `65,300` unseeded components.

## Exact Geometry

- Exact common patch-area sum: `83,224,099 / 4,194,304,000`.
- Exact left and right corridor-volume sums: `830,661 / 33,554,432,000` each.
- Every accepted patch has a closed equal ten-field return signature, an identity transform in one global source chart, a positive-area common face, and positive-volume corridors on both sides.
- Every rejected t-face row has zero physical-glue credit and an explicit fail-closed disposition.

## Fibre Refresh

- Rebuilt all `116 / 116` exact-key quotient-fibre inventories.
- Every quotient component remains memberwise exact-key pure.
- No exact-key fibre is globally exhausted.
- Occurrence incidence remains `36,200 / 53,968`; the unresolved occurrence deficit remains `17,768`.

## Verification

- Independent verifier status: `PASS_INDEPENDENT_ROUND252`.
- The verifier does not import or execute the producer.
- It independently reconstructs all `22,264` candidates, reruns every interval patch and rejection, rebuilds the quotient, and refreshes all `116` fibre inventories.
- Hash seeds `252071` and `252929` reproduce byte-identical producer and verifier outputs.

## Strict Boundary

- Round252 grants additional physical known-connectivity and refreshed quotient-fibre inventory credit, not maximal-component credit.
- The `65,740` quotient components remain lower bounds on physical components; maximality is unproved.
- Maximal physical components remain `0 / 53,968`; exhausted exact-key fibres remain `0 / 116`; global dispositions remain `0 / 224,580`.
- Gate5 remains `10 / 18`; CM2 remains `NO-GO_FOR_CLAIM`.

## Required Next

Close the `17,768` Round179/Round204/Round208 occurrence deficits against the saturated key-pure quotient, beginning with the `16,444` Round179 resolved-child occurrences, before any maximality or global-fibre promotion.
