# CM2 Round 77 direct-assault summary

- Replayed all `263072` frozen depth-20 outer leaves and partitioned them into `19` exact first-failure reason rows.
- Resolved the partition to `65` finite interval carrier tubes: `16` time-one destination faces, `40` time-two candidate discriminants, `8` time-two destination faces, and `1` outgoing seam/geometry tube.
- The tube complement contains exactly `407652` already strict leaves: R1 `45080`, R2 `23184`, Q2 `339388`.
- Applied the actual tagged branch RN law `0<=g_B<=1` to all `64` physical R1/R2 faces.  Finite actual-law bounds are F9 `22138859900062518371942432`, F10 `512`, and F13/F16 each `<5354429251/250000000000`.
- Did not promote the interval tubes to genuine codimension-one carriers, did not invent 64 exact RN density values, and did not infer an all-rank sum.
- Strict state remains Gate 4 `1/7`, Gate 5 `10/18` with blocks `0`, complete composite gates `0/5`, CM2 `NO-GO_FOR_CLAIM`.

Primary report: `deliverables/cm2-round77-boundary-atlas-rn-weight-assault-2026-07-21.md`.
