# CM2 Round 60 Gate 1/2/3 — combined gauge, all-depth stable join and strong-operator frontier

Date: 2026-07-20  
Scope: append-only structural assault from the frozen Round-59 aggregate and
Gate-1/2/3 leaf  
Strict verdict: **Round 60 does not promote Gate 1, Gate 2 or Gate 3.  It
turns each attempted promotion into an exact, typed boundary.  Gate 1 gains
an explicit compact-to-combined twisting budget but no physical gauge
comparison or all-plaque Dini registry.  Gate 2 gains a sharp finite-prefix
separator and a minimal all-depth stable/landing join, but no new physical
stable field.  Gate 3 re-audits the chosen-target first-root sign and the
`830n+12` atom budget, and proves that even under the optimistic identity
between clock and formula depth the installed `exp(Dbar/6)` moment cannot pay
the raw `8*162^n` enumeration route.  Strong `R_s/Q_s`, directional Piola and
`MT_DQ` remain absent.  Gates 1/2/3 are `NOT_CERTIFIED`, composite gates are
`0/5`, and CM2 remains `NO-GO_FOR_CLAIM`.**

## 1. Frozen baseline and hash discipline

This leaf pins, without editing, the Round-59 aggregate report and recursive
ledger and the Round-59 Gate-1/2/3 manifest and ledger:

```text
Round59 aggregate report SHA256
  ec0623ec2fd6138c74d3707fd1c6f5e385b98187dcbd616cadee3b19e39dd5ef

Round59 aggregate recursive-ledger SHA256
  c013b5aa22db44308c7aa4cbe2b0800da14f709bcc80250464478b047cfc0712

Round59 Gate123 manifest SHA256
  16a2562868df58b2e14bf672d2d0d11735581016ff50e10f5a6d2c1e660d3466

Round59 Gate123 SHA-ledger SHA256
  985910708fe715d0c891cd6a288b37a5e1b88530db858a712e7e82e80cbb4b71
```

The frozen physical rows are exactly:

```text
Gate 1 selected QNL compact-log germ/loop Dini:       CERTIFIED_LOCAL
Gate 2 collision shadows on actual prefixes 1..96:   96/96 CERTIFIED
Gate 2 physical seven-field landing join:             1/7
Gate 3 physical fixed-depth branch formula:           CERTIFIED
physical variables / atoms / degree:                  8n+5 / 830n+12 / 4
raw fixed-depth word universe:                         8*162^n
```

None of those rows was an all-plaque combined-gauge theorem, an all-depth
stable quotient or a strong moving-scatterer operator theorem.

## 2. Gate 1 — the exact compact-to-combined boundary

### 2.1 The selected compact-gauge budget is large but conditional

Round 59 pins the strict rational enclosures

```text
E_compact < 8568/10^52,
W_min     > 17344/10^32,
```

where `E_compact` is the two-sided selected loop entry error and `W_min` is
the smallest selected twisting wedge in the same frozen compact-log fibre
and basis.  Suppose a genuine immutable representative join identifies that
loop with a loop in the combined half-density gauge and proves

```text
E_combined <= K_gauge E_compact.
```

Then all four selected wedges remain strict whenever

```text
K_gauge < W_min/E_compact
        = 216800000000000000000000/1071
        > 2*10^20.                                      (2.1)
```

Equation (2.1) is a newly explicit **conditional gauge-comparison budget**.
It is not a physical value of `K_gauge`: no frozen row identifies the
compact-log gauge with the combined half-density gauge or bounds their
comparison on the selected representative.

### 2.2 Selected-to-all-plaque is exactly nonimplicative

The frozen selected tail can be kept byte-for-byte while completing the
unconstrained plaque registry in incompatible ways.  On the logical
two-plaque registry `{p_star,p_bad}`, set

```text
delta_n^compact(p_star)=(n+1)10^-n,
delta_n^combined(p_bad)=1/(n+1),       n>=0.
```

The selected sum is exactly `100/81`, while the bad-plaque harmonic sum
diverges.  Thus one selected physical germ, even with preserved selected
twisting, does not determine an all-plaque weighted Dini row.

There is an independent gauge separator on the selected token itself.  If
no uniform comparison is supplied, an unbounded formal amplification

```text
A_n=10^n,
delta_n^combined=A_n delta_n^compact=n+1
```

turns the certified compact-gauge series into a divergent combined-gauge
series.  These are exact logical registry completions, not claimed billiard
plaques or a construction of class `H`.

### 2.3 Minimal all-plaque/combined-gauge interface

The shortest legal Gate-1 promotion now has five typed rows:

1. a standard-Borel registry of **all actual** stable and unstable plaque
   pairs, with immutable endpoint/chart/representative IDs;
2. combined half-density increments on that registry, in one common gauge;
3. a physical summable envelope for the required weighted increments, not
   merely a selected-orbit or pointwise limit;
4. an immutable same-representative `H` join from the selected compact loop
   to the combined-gauge loop and a finite `K_gauge` satisfying (2.1);
5. the already pinned selected twisting wedges transported through that
   same representative join.

Rows 1--4 are not certified.  Consequently

```text
Gate 1 all-plaque combined-gauge Dini:                NOT_CERTIFIED
same representative class H plus twisting:            NOT_CERTIFIED
Gate 1:                                                NOT_CERTIFIED
```

## 3. Gate 2 — a 96-prefix strip is not an all-depth stable quotient

### 3.1 Sharp depth-97 separator

The actual Round-59 strip certifies only the collision-shadow statements at
depths `1,...,96`.  The logical depth ledger

```text
g_j=1 for 1<=j<=96,
g_97=0
```

agrees with every frozen finite-prefix row and loses the required
graph-transform chart at the next depth.  Therefore

```text
96 finite collision-shadow zeros
  does not imply
all-depth graph-transform survival.                    (3.1)
```

This is a finite-prefix nonimplication model, not an assertion that the
actual billiard strip fails at collision 97.  It also separates collision
regularity from stable-chart survival: the full bad shadow additionally
records loss of the required graph-transform chart.

### 3.2 Minimal all-depth stable package

An all-depth promotion needs one immutable physical package containing:

1. an actual stable-plaque registry on a stable-saturated Borel base;
2. graph transforms at every depth, with common charts and quantitative
   contraction/distortion and a controlled all-depth bad shadow;
3. a Borel stable projection to the target unstable plaques;
4. stable holonomies with two-sided Radon--Nikodym Jacobian `J_hol` for the
   pinned collision Liouville/SRB law;
5. target conditional densities and fragmentation/span rows in the same
   adapted arclength used by the Gate-4 boundary charge;
6. an immutable join to the **actual Round-57/59 first-return landing law**,
   retaining landing point, `n/path/ID/half-open owner` and once charge;
7. strong restriction and assembly on the intended physical carrier space.

The exact first-return graph already supplies item 6's tagged branch inverse,
but it does not supply items 1--5 or 7.  Weak same-graph Rokhlin
reconditioning is measure-level reassembly, not stable holonomy or a strong
assembly map.

The seven-field landing audit therefore remains

```text
field 6 branch inverse:                                CERTIFIED
fields 1--3:                                           NOT_CERTIFIED
field 4: existing curve densities, required field      NOT_CERTIFIED
field 5 J_land,min<C_p h a.e.:                         NOT_CERTIFIED
field 7 strong restriction/assembly:                   NOT_CERTIFIED
physical join:                                         1/7
official immutable Gate-2 fields:                      0/17
Gate 2:                                                NOT_CERTIFIED
```

## 4. Gate 3 — strict formula re-audit and the clock barrier

### 4.1 Chosen target and first-root sign

For each distinct competing disk put

```text
a=u dot(C-q),
b=|C-q|^2-R^2>0,
Delta=a^2-b,
```

on the physical domain `|u|=1`, `tau>0`.  The current source disk is handled
by its outgoing-convexity sign and is not treated as a distinct competitor.
The earliest competing ray root, when it exists, is
`a-sqrt(Delta)`.  There is no competing root in `(0,tau)` exactly when

```text
a<=0
OR Delta<0
OR [a-tau>=0 AND Delta<=(a-tau)^2].                   (4.1)
```

The final square is legal only after `a-tau>=0`.  The certificate and an
independent verifier replay (4.1) on a rational sign grid and check it
against the complementary earliest-root predicate.  Chosen contact,
`0<tau<3`, outgoing signs, half-open owners, target ties and grazing cemetery
remain separate rows; (4.1) is never applied to silently choose the target.

### 4.2 Atom budget remains a padded upper bound

At each collision there are `162` target slots: one chosen slot and `161`
nonchosen slots.  The declared encoding deliberately assigns five atomic
rows to every nonchosen slot:

```text
161*5=805 competitor rows,
at most 25 flight/contact/normal/reflection/source/owner/tie/cemetery rows,
total <=830 rows per step,
plus 12 initial rows.
```

On a nonempty physical branch one nonchosen slot is the current source and
uses only the simpler outgoing row, so the five-row allocation is an
overcount, not an undercount.  Together with the eight auxiliary variables
per collision, the Round-59 bound remains valid for **each fixed depth**:

```text
variables <=8n+5,
atoms     <=830n+12,
degree    <=4.
```

No uniform-in-depth atlas, cell enumeration, inverse-component-length bound
or strong restriction norm follows from this audit.

### 4.3 The installed clock cannot pay even raw enumeration

Give the counting route its most favourable missing type identification,
namely set formula depth `N=Dbar`.  On the exact logical clock law

```text
P{N=n}=2^(-n-1),       n>=0,
```

one has

```text
E exp(N/6)
 =1/[2(1-exp(1/6)/2)]<infinity,
```

because `exp(1/6)<2`.  But the expectation of the raw candidate-word bound
is

```text
sum_n 2^(-n-1) * 8*162^n
 =4 sum_n 81^n
 =infinity.                                             (4.2)
```

Thus the installed `exp(Dbar/6)` moment does not imply integrability of the
current raw-enumeration upper bound.  Equation (4.2) does **not** prove that
the actual physical number of nonempty branches has infinite expectation:
`8*162^n` is an upper candidate count.  It proves that this counting route
cannot be paid without a much sharper compression or a stronger clock
moment.

The CAD majorant is still larger.  Its degree recurrence has the exact
solution

```text
D_0=8,
D_(r+1)=2D_r^2,
D_r=2^(4*2^r-1).
```

This confirms the fixed-depth/depth-integrated type boundary; it is not an
actual cell lower bound.

### 4.4 Direct strong route

Bypassing global CAD counting would require, on one pinned pair of physical
anisotropic spaces:

1. uniformly bounded physical restriction maps `R_s` and quotient/assembly
   maps `Q_s`, with immutable chart/owner/cemetery IDs;
2. a moving-scatterer directional Piola/current formula including boundary,
   recut and singular terms, not only smooth interior Jacobians;
3. a common-domain derivative operator and the `MT_DQ` estimate against the
   required moving test class;
4. convergence and commutation rows that join these maps to the actual
   first-hit/landing carriers without renormalisation.

None is currently certified.

## 5. Latest official-technology type audit

The official arXiv API/HTML were checked on 2026-07-20.

### `arXiv:2604.19671v2`, *Linear response for Sinai billiards with small holes*

- Proposition 4.4 gives invariance of regular standard families under the
  hole conditional evolution; Theorem 4.7 gives coupling/convergence against
  `C^1` observables; Lemma 5.1 turns the one-step image of a vertical boundary
  line into a uniformly regular standard family.
- This is a useful local analogue for a single boundary current and a
  `C^1`-dual evolution.
- Its parameter is hole size for a fixed billiard map.  It does not give the
  moving-scatterer directional Piola terms, `MT_DQ`, or the pinned CM2
  restriction/quotient maps.
- The paper explicitly states that no known general piecewise-hyperbolic
  Banach spaces contain standard pairs.  Its standard-family result therefore
  cannot be relabelled as the missing strong Banach-space join.

### New real-algebraic/CAD sources

- `arXiv:2605.18110v3` computes sample points in connected components defined
  by one real inequation under generic smoothness assumptions.  It does not
  control this multi-atom quantified billiard formula, inverse component
  lengths, boundary charge or depth integration.
- `arXiv:2607.02400v1` gives fixed-dimensional semialgebraic algorithms for
  cutting polytopal measures.  It supplies neither moving billiard branch
  geometry nor strong transfer-operator maps.

No external theorem is imported into a CM2 certification.

## 6. Strict Round-60 ledger

```text
Gate 1 compact-to-combined twisting budget:            CERTIFIED_CONDITIONAL
Gate 1 selected-to-all-plaque implication:             CERTIFIED_FALSE_BY_SEPARATOR
Gate 1 physical all-plaque combined-gauge registry:    NOT_CERTIFIED
Gate 1 same representative H plus twisting:            NOT_CERTIFIED

Gate 2 96-prefix-to-all-depth implication:              CERTIFIED_FALSE_BY_SEPARATOR
Gate 2 physical all-depth graph transforms:             NOT_CERTIFIED
Gate 2 stable base/projection/two-sided J_hol:           NOT_CERTIFIED
Gate 2 actual landing strong join:                      1/7; NOT_CERTIFIED
Gate 2 official immutable fields:                       0/17

Gate 3 chosen-target first-root sign re-audit:           CERTIFIED
Gate 3 atom/degree/fixed-depth scope re-audit:           CERTIFIED
Gate 3 exp(Dbar/6)-to-raw-enumeration implication:       CERTIFIED_FALSE_BY_SEPARATOR
Gate 3 depth-integrated physical complexity:            NOT_CERTIFIED
Gate 3 physical strong R_s/Q_s:                         NOT_CERTIFIED
Gate 3 directional Piola / MT_DQ:                       NOT_CERTIFIED

Gate 1 / Gate 2 / Gate 3:                              NOT_CERTIFIED
complete composite gates:                              0/5
CM2:                                                   NO-GO_FOR_CLAIM
```

## 7. Shortest continuation

1. **Gate 1:** construct the all-actual-plaque combined-gauge increment
   registry and a physical compact-to-combined representative comparison
   satisfying (2.1).
2. **Gate 2:** construct an all-depth stable graph-transform base with
   two-sided physical `J_hol`, then join its target conditionals and
   fragmentation rows to the actual first-return landing graph; the tagged
   inverse need not be rebuilt.
3. **Gate 3:** compress actual nonempty physical branch complexity below the
   installed clock rate, or implement the four-row direct strong route of
   Section 4.4.

## 8. Executable evidence

- `cm2_gate123_round60_combined_gauge_stable_strong_operator_frontier_cert.py`;
- `cm2_gate123_round60_combined_gauge_stable_strong_operator_frontier_verifier.py`;
- `cm2-gate123-round60-combined-gauge-stable-strong-operator-frontier-manifest-2026-07-20.json`;
- `cm2-gate123-round60-combined-gauge-stable-strong-operator-frontier-manifest-2026-07-20.sha256`.

The producer and independent verifier pin all four frozen dependencies,
replay the rational gauge budget, both logical separators, the depth-97
prefix guard, the chosen-target sign grid, the atom budget, the geometric
clock separator and the exact CAD degree recurrence.  They enforce strict
JSON, deterministic byte-identical re-emission, hostile mutation rejection
and fail-closed default execution.  Positive audit modes exit `0`; default
execution exits `2` because no composite gate is closed.
