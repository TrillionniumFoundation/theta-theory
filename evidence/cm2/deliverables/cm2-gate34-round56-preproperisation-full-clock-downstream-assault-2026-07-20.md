# CM2 Round 56 Gate-4 downstream preproperisation/full-clock closure

Date: 2026-07-20  
Strict status: **the physical preproperisation moment, the full ambient
two-view max-clock exponential moment, its parent-charged `L^(6/5)` envelope,
and its absolutely-continuous Borel-exhaustion tail are now certified
qualitatively finite.  This is not physical `q`: `J_cap`, a proper same-ID
first-return kernel, intermediate `C24` avoidance, later recovery clocks and
the strong cemetery remain open.  Gate 4 and CM2 remain not certified.**

## 1. Frozen inputs

This append-only leaf pins four manifests by exact SHA-256:

```text
Round51 two-proper-view common law:
028c5a8f59a6efffa9df93cfba841f3844d244988dc235038183eef222d21abc

Round52 defect/terminal-survivor frontier:
7ddecb544fa5a2b243882eaf2028159c22f8437fc3fbb49fda4eb487ab181798

Round53 J_pair -> I_D / J_cap frontier:
2c6ab1c0b3b89000e6d96d04a60d7c662855ae1f24c1d9f43405df04732d7e58

Round56 physical terminal J_pair/Kac-mesh closure:
7c9d089219ef00234b7e7bdafaea706bd4c91a46990d6f14d0837ea360406414
```

The Round56 upstream leaf passed a separate strict mathematical audit after
repairing the representation-dependent leaf-inclusion gap with an exact
slope-four paired reverse replay.  Its frozen conclusion is

```text
physical J_pair < infinity,
physical I_D < infinity,
```

on the same once-charged cell law used by the Round52/53 defect bridge.  No
old artifact is edited here.

## 2. Why the substitution is not circular

Round51 constructs, record by record,

```text
P_fw(y,x)=T_s^(696 Dbar(y))(V_fw(y,x)),
P_rev(y,x)=T_s^(696 Dbar(y))(V_rev(y,x)).
```

Every record has finite Borel `Dbar`.  This pointwise finiteness is enough to
construct both stopped Borel maps, their two proper pushforward kernels and
the selected forward proper reference law

```text
m_star(dy,dz)=lambda(dy) K_fw_star(y,dz),
```

with reverse data transported by `Theta_y`.  No global exponential moment
of `Dbar` is used in that construction.

Round52 then performs the once-charged cell retyping, chooses one uniform
finite terminal time `H_joint`, and proves

```text
k_fw=k_rev=0,
C_fw,post=C_rev,post=H_joint+221328.
```

These geometric and terminal-mass statements also do not assume `I_D`.

Independently, the Round55/56 hereditary/Kac/refinement route proves
`J_pair<infinity` and then the same-measure estimate

```text
I_D
 < nu(X)+[35/(99*2^309)] J_pair
 < infinity.
```

That proof does not invoke the Round51 full-clock conclusion.  The logical
order is therefore

```text
recordwise Dbar and stopped maps
 -> two proper views / selected once-charged reference law
 -> cell retyping, finite H_joint, zero shells
 -> independent J_pair and I_D closure
 -> substitution into the pre-existing conditional full-clock formula.
```

The fact that later manifest files pin earlier typing manifests is provenance,
not a hypothesis cycle.

## 3. Exact actual promotion

The total orientation clock is

```text
C_sigma,total=696 Dbar+H_joint+221328.
```

Its preproperisation exponential factor is exactly

```text
exp((696 Dbar)/4176)=exp(Dbar/6).
```

Thus the Round56 theorem is precisely the missing moment, not merely a
stronger-looking quantity on another law:

```text
I_D=integral p(y) exp(Dbar(y)/6) dlambda(y)<infinity.
```

Put

```text
A_Hjoint=(6/5)^(318+ceil(H_joint/696))<infinity
```

and, for every integer `r>=1`, use the Round51 selected reference law to
define

```text
Wtilde_r_star=max(
  1,
  1_Sfw_star exp(C_fw,total/(4176r)),
  1_Srev_star exp(C_rev,total/(4176r))
).
```

The former conditional inequality is now actual:

```text
integral Wtilde_r_star^r dm_star
 < nu(X)+2 A_Hjoint I_D
 < nu(X)+2 A_Hjoint(
      nu(X)+[35/(99*2^309)]J_pair
   )
 < infinity.
```

Consequently the following fields are upgraded:

```text
physical preproperisation exp(Dbar/6) moment:      CERTIFIED_FINITE
full ambient two-view max-clock exponential moment:CERTIFIED_QUALITATIVELY_FINITE
parent-charged total ambient max envelope L6/5:    CERTIFIED_QUALITATIVELY_FINITE
ambient AC Borel-exhaustion tail:                  CERTIFIED_VANISHING
```

The conclusion is qualitative rather than numerical: `H_joint` is one
uniform finite theorem-supplied constant but is not materialized, and the
Round56 `J_pair` proof supplies finiteness without a closed numerical upper
bound.

For any increasing Borel exhaustion `E_j` whose union has full base-law
measure, integrability gives

```text
ambient-envelope-mass(E_j^c) -> 0.
```

This is the absolutely-continuous/weak mass tail only.

## 4. Exact nonpromotion

The Round51 two-proper-view measure isomorphism and selected common reference
law are **not** remaining blockers for the ambient preclock: they were already
certified before `I_D` and now accept the moment directly.

They do not, however, make the transported common terminal survivor a proper
standard family.  The independent Round52 interval-translation separator has
two proper marginal views and common mass `499/500`, but infinite common
boundary numerator.  Therefore the new finite `J_pair` does not silently
become

```text
J_cap,total=integral J_cap(y) dlambda(y)<infinity.
```

The following remain independent and not certified:

1. recordwise/global `J_cap` and the associated `D_cap` moment;
2. a proper common terminal two-view carrier;
3. one physical proper same-ID first-return kernel;
4. avoidance of `C24` at every intermediate collision after properisation;
5. every later or repeated recovery-clock moment not contained in `Dbar`;
6. the Round28 physical collision-time charge `q` and its `L^(6/5)` moment;
7. the strong singular/current cemetery;
8. full numerical `C_fw,C_rev,q`.

The terminal Round52 event controls only the single terminal collision.  It
does not provide item 4.  Likewise an integrable absolutely-continuous
ambient envelope does not control a collision-null never-return set carrying
nonzero trace/current mass; hence the AC exhaustion tail is not the strong
cemetery.

The strict global boundary is unchanged:

```text
Gate 4:                  NOT_CERTIFIED
complete composite gates:0/5
CM2:                     NO-GO_FOR_CLAIM
```

## 5. Executable evidence

Artifacts:

- `cm2_gate34_round56_preproperisation_full_clock_downstream_cert.py`;
- `cm2_gate34_round56_preproperisation_full_clock_downstream_verifier.py`;
- `cm2-gate34-round56-preproperisation-full-clock-downstream-manifest-2026-07-20.json`;
- `cm2-gate34-round56-preproperisation-full-clock-downstream-manifest-2026-07-20.sha256`.

Validation:

```text
Python syntax:                    2/2 PASS
dependency SHA validation:       4/4 PASS
independent arithmetic/replay:   PASS
hostile mutations:               37/37 REJECTED
deterministic re-emission:        byte-identical PASS
certificate/verifier defaults:   both exit exactly 2
strict q / Gate4 / CM2:          NOT_CERTIFIED / NOT_CERTIFIED / NO-GO
```
