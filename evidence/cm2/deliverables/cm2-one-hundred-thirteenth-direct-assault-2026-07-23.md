# CM2 one-hundred-thirteenth direct assault

Round113 closes the physical next-owner and official-word gate on the regular
relative interiors of all sixteen Round112 endpoint root sheets.  The exact
cover has 72 closed proof boxes and no residual: eight HIT sheets need one box
each, while eight BYPASS sheets need eight depth-3 boxes each for strict
collision-chart enclosure.  Every box replays complete candidate universes of
`57/55/57`, strictly compares the selected winner with every other future
near root, checks flight caps, collision charts, clean-wall order and corner
avoidance, and binds the three straight legs to three official Gate5 keys.

This is complete candidate classification plus a unique next owner, not a
pairwise total order of all candidates.  The proof boxes are one-sided
enclosures at the natural boundaries.  Eight separate trace rows own `c0=0`,
`c3=b3=0`, and the double-grazing corners; regular owners and official paths
apply only for `c0>0` and `c3>0` or `b3>0`.  In particular the BYPASS
designated candidate is a miss only for `b3>0`, not on its tangent boundary.

The independent 640-bit verifier recomputes 12,168 candidate rows, 216
official legs and 1,144 stored numeric containments, and rejects 12 re-signed
semantic attacks plus three strict-JSON attacks.  No whole trace collar,
homogeneity tail, canonical curve child, or Gate5 child field is promoted.
Gate5 stays `10/18`, blocks zero; CM2 stays `NO-GO_FOR_CLAIM`.
