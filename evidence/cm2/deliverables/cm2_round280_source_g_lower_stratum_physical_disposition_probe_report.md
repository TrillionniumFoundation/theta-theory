# CM2 Round280: lower-stratum physical-disposition probe

## Zero-credit result

Round280 audits every one of the `55,428` Round267 nominal supports against
the frozen Round179/Round182/Round204/Round208 evidence and keeps later
Round268/Round275/Round279 records strictly non-dispositive.

The exact four-way census is:

- `38,440` `CERTIFIED_PHYSICAL_SUPPORT_EXISTS`;
- `15,712` `CERTIFIED_SUPPORT_ABSENT`;
- `396` `FORMAL_PROVENANCE_ONLY__NO_PHYSICAL_DECISION`;
- `880` `UNRESOLVED__SOURCE_T0_HALF_OPEN_OWNER_NOT_MATERIALIZED`.

The physical rows consist of `21,392` outgoing zero-set supports, `16,936`
wall-factor-union supports, and `112` transverse pair intersections.  The
absence rows consist of `8,768` outgoing supports, `6,728` wall supports,
and `216` pair candidates.

The `396` provenance-only rows are exactly the Round179 fully replaced
origins for which Round182 deliberately built no retained collar.  Missing
collar rows are not treated as absence.  Their remaining local dependency is
an exact predicate-sign replay over the exhaustive Round179 resolved-child
cover.

The `880` unresolved rows have an exact source factor zero only on a `t=0`
boundary.  Of these, `176` rows form `88` exact same-chart,
opposite-`t`-side pairs; `704` remain one-sided.  They require a dedicated
same-point half-open source-sheet owner/absence ledger.  A final DSU cannot
decide local physical existence, so the number of rows whose local decision
depends on the final DSU is exactly zero.

## Evidence partition

The complete evidence partition is:

- `38,244` explicit Round182 full/clipped 2D graph-sheet supports;
- `15,492` exhaustive Round182 all-empty closed collars;
- `112` Round182 strict-interior interval-Newton transverse lines;
- `216` Round182 strict same-sign empty pair candidates;
- `32` Round204 explicit source/target 2D sheet lineages;
- `52` Round208 residual outgoing graph-sheet supports;
- `4` Round208 exhaustive residual outgoing all-empty supports;
- `396` Round179-full-replacement / Round182-no-collar provenance rows;
- `176 + 704` exact Round179 source-`t=0` boundary tails.

Round268's `152` positive seam patches reference no nominal-support row ID.
Round275 has `128` guard rows on `80` overlapping nominal parents, but no
direct support-row join and zero occurrence/component credit.  All `332,016`
Round279 candidate atoms join `53,824` nominal collar occurrence IDs, but
their frozen occurrence credit is zero; atom presence is therefore not used
as lower-stratum existence.

## Replay and structural audit

Producer seeds `280071` and `280929` generated byte-identical ledger and
result files.  The deterministic gzip ledger passes `gzip -t`.  An
independent structural audit reopened all `55,428` rows and verified:

- unique Round280 row IDs;
- every row's own SHA256 closure;
- complete row, row-ID and row-hash digests;
- the exact four-way census;
- all occurrence/component/maximality/fibre/global-disposition/JxJy credits
  are zero on every row.

Frozen identities:

- producer SHA256:
  `f62e4270c51f063183216f299c09006f0e5884cef912a0df5dbce4c019c31173`;
- ledger file SHA256:
  `0f939334f9ca65c535be7123b8f9370b0833f84743b6a094c92ffcc1087dec1b`;
- ledger rows SHA256:
  `85c3ab069b8e99088caca082d52bbc4c2fb2919567eb9e113c876c70334bc0a7`;
- ledger row IDs SHA256:
  `a98460c5e9dc45e817f0027709d1cc1ca3979fa2a5743b63a4675fe7d78a1337`;
- ledger row hashes SHA256:
  `64d5f631b548b3f7afdcd9e5b9fa7de78b61143e78e7f5b6662a2f8a528b62f1`;
- result object SHA256:
  `30310231391c0fb3369a9d9c1364801bd0daa4ce6fb355b96be4cd3497e824ea`;
- result file SHA256:
  `3147925b1f49ae9743055c7dc2e3194d7b0a385994c67097f3cb88e91bf0d484`.

## Strict nonpromotion

This probe classifies local physical support only.  It does not materialize
an expanded occurrence or add a component edge.  The frozen quotient remains
`63,224`, expanded occurrences remain `126,468`, maximality remains
`0/63,224`, exact-key fibres remain `0/116`, and global dispositions remain
`0/224,580`.  Jx/Jy same-point glue remains zero, Gate5 remains `10/18`,
D02 remains `BLOCKED`, and CM2 remains `NO-GO_FOR_CLAIM`.
