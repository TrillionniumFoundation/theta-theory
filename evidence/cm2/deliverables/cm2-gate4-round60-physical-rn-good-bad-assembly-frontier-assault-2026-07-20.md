# CM2 Round 60 Gate 4 — physical RN marker / good-bad / assembly frontier

Date: 2026-07-20  
Strict status: **the actual common landing law now has a certified physical
Radon--Nikodym marker and an exact same-measure conditional formula on any
future supplied physical unstable disintegration.  The original-time graph
also splits exactly into a possibly-zero proper good **landing** subkernel
and an unpaid bad subkernel while retaining the original killed first-hit
predicate; physical source properness is not inferred.  Two
smooth product separators prove that even perfect qualitative fields 1--4 do
not imply the strict boundary threshold.  No actual physical product
rectangle, stable holonomy, quantitative marker geometry, whole-law
properness or strong assembly has been installed.  The seven-field join stays
`1/7`, Gate 4 stays `NOT_CERTIFIED`, and CM2 stays `NO-GO_FOR_CLAIM`.**

## 1. Frozen baseline and type discipline

This leaf is append-only.  It pins the Round-59 Gate-4 manifest at

```text
e7305e0b72eed28bc68b115e1201fc02e2b66d3cc95906fc6d1efc5e9463a4f5
```

and its SHA ledger at

```text
967421bc83b283510cdb25013f2e723e5761337e56471fad64a9ef74b35536c0.
```

The frozen conclusion is:

```text
exact tagged first-return branch inverse:       CERTIFIED
weak same-graph Rokhlin reconditioning:          CERTIFIED
actual seven-field physical landing join:        1/7
fibrewise J_land,min<C_p h:                       NOT CERTIFIED
proper physical same-ID first-return kernel:      NOT CERTIFIED
```

Every conditional statement below keeps the Round-59 measure typing.  If
`eta` is a quotient law and `Gamma_u` a conditional graph law, statements are
made for `eta`-almost every `u` and `Gamma_u`-almost surely.  There is never a
division by `eta({u})`; the quotient may be nonatomic.  Values chosen on a
quotient-null fibre have no endpoint, tag or normalization meaning.

The numerical properness constant remains

```text
C_p=4*10^90*360493663/358863.
```

## 2. Actual common landing RN marker

Let `mu_C=mu_s|C_s` be the physical collision-SRB law on the induced section,
and let `kappa_B` be the unshifted common landing marginal of the frozen raw
first-return graph.

The source common law is an exact positive restriction of the original
`R_n` law.  The induced first-return map preserves `mu_C` exactly once.  A
positive pushforward preserves domination, so

```text
0 <= kappa_B <= mu_C.                                 (2.1)
```

Consequently there is an actual Borel marker

```text
g_B=d kappa_B/d mu_C,
0<=g_B<=1                  mu_C-a.e.                   (2.2)
```

This is stronger than merely knowing that the landing has finite `Z`: it
identifies the common landing as a marked sublaw of the same physical
collision measure.  It does not yet identify a physical unstable quotient.

Now suppose a future theorem supplies an actual unstable Rokhlin
disintegration

```text
mu_C = integral mu_u deta(u)                           (2.3)
```

on the physical landing points.  Define without normalizing fibres

```text
m(u)=integral g_B dmu_u,
tilde_kappa_u=g_B mu_u.                                (2.4)
```

Then the exact same-measure formula is

```text
kappa_B=integral tilde_kappa_u deta(u).                (2.5)
```

Equivalently, on `{m>0}` put

```text
deta_B=m deta,
kappa_u=(g_B/m(u))mu_u.
```

Then

```text
kappa_B=integral kappa_u deta_B(u).                    (2.6)
```

If `dmu_u=rho_u ds` on an actual unstable plaque, the unnormalised common
conditional has the exact density

```text
d tilde_kappa_u/ds=g_B rho_u.                          (2.7)
```

Equations (2.2)--(2.7) are the strongest actual materialisation found for
field 4.  They certify the physical RN marker and the Bayes/Rokhlin formula,
but supply no physical quotient, no support-component bound for `g_B`, and no
two-sided or log-distortion bound for `g_B rho_u`.

The tagged branch inverse pulls (2.5) back to the same source.  The physical
source, landing, `n`, path, immutable ID and half-open owner identities then
hold `Gamma_u`-almost surely for `eta_B`-almost every positive fibre.  No
landing point is replaced by a stable projection.

## 3. Full fields 1--4 materialisation audit

The actual audit is:

| # | Physical field | Round-60 actual state |
|---:|---|---|
| 1 | countable physical product rectangles covering the common landing | `NOT_CERTIFIED` |
| 2 | immutable stable projection and two-sided Borel holonomy Jacobian | `NOT_CERTIFIED` |
| 3 | full span or quantitative common fragmentation | `NOT_CERTIFIED` |
| 4 | same-measure unstable conditionals with density/log distortion | `PARTIAL`: actual RN marker and exact conditional formula only |
| 5 | physical boundary charge strictly below `C_p` | `NOT_CERTIFIED` |
| 6 | tagged Borel branch inverse retaining `n/path/ID/owner` | `CERTIFIED` in Round 59 |
| 7 | strong restriction and assembly | `NOT_CERTIFIED`; conditional standard-family category only |

Thus the actual complete count stays `1/7`.  The field-4 formula is a real
typed upgrade but not a completed field.  Gate 2's official immutable score
also remains `0/17`.

The two closest frozen candidates still do not join:

- Round 25 constructs one genuine positive-area physical return tile, but its
  affine fibres and projection are cone candidates, not invariant local
  stable plaques or physical stable holonomy.
- Round 49 has `F=153`, density ratio `<2000/1999` and long-leaf geometry only
  conditionally on one admissible source proper family.  It is not the actual
  common time-`n` landing law.

The missing actual object is therefore not a numerical multiplication of
those two leaves.  It is a new physical landing disintegration of (2.1).

## 4. Perfect fields 1--4 still do not pay field 5

Two exact smooth product models isolate the quantitative debt.  They are
logical nonimplication models, not claims that the billiard realizes the
geometry.

### 4.1 Short full-plaque separator

Take one Euclidean rectangle

```text
X=[0,L]x[0,1],
L=2/(3C_p),
```

with horizontal unstable plaques, vertical stable plaques and identity first
return at time one.  Use the vertical stable projection, whose forward and
reverse RN Jacobians are both one.  On every unstable plaque retain the full
plaque with constant conditional density `1/L`.

Thus the strongest qualitative form of fields 1--4 holds:

```text
one product rectangle,
F=1,
theta=1,
R=1,
log distortion=0,
full span.
```

Nevertheless

```text
J/h=1/L=3C_p/2>C_p,
F R/(theta L)=3C_p/2,
D_land=2.                                             (4.1)
```

The original identity return is finite-`Z` but improper.

### 4.2 Long but fragmented separator

Let

```text
H=999/1000,
N=floor(C_p)+1.
```

In one unit plaque retain `N` equal intervals of total length `H`, separated
by true positive gaps.  Use the same perfect product rectangle, stable
projection, Jacobian one and constant retained density.  Now

```text
L=1,
F=N,
theta=H,
R=1,
J/h=N/H in (C_p,2C_p),
D_land=2.                                             (4.2)
```

Equations (4.1) and (4.2) prove that plaque span and marker fragmentation are
independent debts.  Neither absolute continuity, perfect holonomy nor zero
density distortion supplies

```text
F(u)R(u)<C_p theta(u)L(u)                             (4.3)
```

almost everywhere.  Field 5 cannot be inferred from qualitative fields 1--4.

## 5. Exact good/bad original-time split

On the frozen physical landing registry define

```text
G={y:h(y)>0 and z_land(y)<C_p},
B={y:h(y)>0 and z_land(y)>=C_p}.                       (5.1)
```

Both sets are Borel.  Restricting the exact raw graph gives

```text
Gamma_cap=Gamma_G+Gamma_B                              (5.2)
```

with no normalization or duplicate charge.

By the Round-58 iff, `Gamma_G` has a proper physical same-ID **landing
family** on the exact first-return graph at the original time `n`.  This does
not prove that its physical source family is proper and therefore does not by
itself produce the full proper return kernel.  It retains exactly

```text
T_s^j(source) notin C_s, 1<=j<n,
T_s^n(source) in C_s.                                  (5.3)
```

Thus original-path killed `C24` avoidance is already available on the good
landing subgraph.  No recovery collision is appended, and the endpoints,
path and owner remain unchanged.

Two guards are decisive:

```text
mass(G)>0: NOT CERTIFIED,
mass(B)=0: NOT CERTIFIED.                              (5.4)
```

The pinned finite dyadic moment restricts to

```text
integral_B h 2^D_land<infinity,                        (5.5)
```

but (5.5) is not a nullity theorem.  In the short full-plaque separator of
Section 4, `G` is empty, `B` has full mass, `D_land=2`, and the moment is
exactly `4`.

### Why the bad part is not a free cemetery

`Gamma_B` can carry positive ordinary collision-SRB mass and can be entirely
regular.  The frozen singular cemetery is collision-null.  Relabelling
`Gamma_B` as cemetery would delete a positive part of the exact graph; in the
short-plaque separator it would delete the whole identity graph.

A legal positive-defect hybrid would need all five of the following new
interfaces:

1. a positive, endpoint-preserving strong embedding `E_B Gamma_B`;
2. retention of `n/path/ID/owner` and one parent charge without fibre
   normalization;
3. a physical norm estimate such as
   `||E_B Gamma_B||_cem<=C integral_B h2^D_land`, or a stronger same-law
   `q/current` estimate;
4. an exact identity
   `Full=Good+iota_cem(E_B Gamma_B)`, never deletion and never replacement by
   the stopped reference `D_cap` endpoints;
5. a downstream CM2 theorem that accepts this positive remainder with the
   required summability or vanishing conclusion.

None is installed.  The hybrid is therefore a precise conditional route, not
a closure of the full physical common landing.

## 6. Field-7 literature audit: arXiv 2604.19671v2

The official PDF of Giovanni Canestrari, *Linear response for Sinai
billiards with small holes*, arXiv `2604.19671v2`, was checked with SHA-256

```text
fc00f45a8ec6513763665bf8fa34569a95ca5d6d6b9fa5d90b7e1089fabd9004.
```

It gives a useful but conditional standard-family subinterface:

- Definition 4.2 starts with an already regular `(omega,B,D)` standard family,
  including an a priori boundary bound `Z<=B`.
- Proposition 4.4 preserves that category under normalized small-hole
  conditional evolution, producing qualitative constants
  `(omega_2,B_2,D_2)`.
- Section 6.2 canonically cuts into connected homogeneous components and
  proves equality of the represented evolved measures.

It does not close field 7 here:

- the small-hole surviving law and the one-step image of a vertical hole
  line are not `kappa_B`;
- the theorem assumes the regular entry family whose common-marker boundary
  is the present missing object;
- `B_2` is qualitative and no inequality `B_2<C_p` or (4.3) is proved;
- Lemma 6.17 divides by surviving mass, and Lemma 6.29's restriction bound
  divides by the selected conditioned mass, so arbitrarily thin markers do
  not receive a uniform free restriction bound;
- the paper's introduction explicitly states that no known Banach spaces for
  general piecewise hyperbolic systems contain standard pairs.  Its
  `C^1`-test coupling is not the requested strong operator assembly.

The legitimate promotion is only:

```text
if an exact initial regular physical common carrier is independently supplied,
then canonical standard-family evolution/reassembly is available.
```

That is a conditional field-7 category subinterface.  It is not an actual
Round-60 field.

## 7. Downstream join after a future whole-law closure

If a future proof establishes the full original-time landing proper on the
same graph measure, then the killed intermediate predicate is automatic from
the already frozen `R_n` tag:

```text
[product_(j=1)^(n-1) 1_(C_s^c)(T_s^j x)]
*1_(C_s)(T_s^n x)=1.                                  (7.1)
```

Under a physical Rokhlin reconditioning, (7.1) holds `Gamma_u`-almost surely
for `eta`-almost every `u`.  This closes the *conditional join* from whole-law
original-time properness to killed intermediate `C24` avoidance without an
additional clock.

It does not close the remaining downstream interfaces:

- every later or repeated recovery-clock moment is still separate;
- physical `q in L^(6/5)` still needs same-ID numerical `C_fw/C_rev` and a
  survivor-conditioned or global physical first-return charge envelope;
- an absolutely-continuous moment does not control the singular/current
  strong cemetery.

## 8. Strict frontier

```text
actual physical landing RN marker:                    CERTIFIED
same-measure Bayes formula on a supplied physical
unstable quotient/disintegration:                     CERTIFIED_CONDITIONAL
fields 1--4 actual materialisation:                   fields 1--3 absent; field 4 partial
seven-field physical landing join:                    1/7 actual complete
fibrewise J_land,min<C_p h a.e.:                      NOT_CERTIFIED
good original-time proper landing subkernel:          CERTIFIED_POSSIBLY_ZERO
physical source properness on that subkernel:         NOT_CERTIFIED
good subkernel positive mass:                         NOT_CERTIFIED
bad landing mass zero:                                NOT_CERTIFIED
positive bad-defect cemetery hybrid:                  CONDITIONAL; FIVE INTERFACES MISSING
physical proper same-ID first-return landing/kernel:  NOT_CERTIFIED
original R_n intermediate C24 avoidance:              CERTIFIED (pinned)
whole-proper-graph killed join:                        CERTIFIED_CONDITIONAL
later/repeated recovery-clock moments:                NOT_CERTIFIED
physical q in L^(6/5):                                NOT_CERTIFIED
strong singular/current cemetery:                     NOT_CERTIFIED
Gate 4:                                               NOT_CERTIFIED
complete composite gates:                            0/5
CM2:                                                  NO-GO_FOR_CLAIM
```

## 9. Executable evidence

- `cm2_gate4_round60_physical_rn_good_bad_assembly_frontier_cert.py`;
- `cm2_gate4_round60_physical_rn_good_bad_assembly_frontier_verifier.py`;
- `cm2-gate4-round60-physical-rn-good-bad-assembly-frontier-manifest-2026-07-20.json`;
- `cm2-gate4-round60-physical-rn-good-bad-assembly-frontier-manifest-2026-07-20.sha256`.

The suite pins the Round-59 report, manifest and SHA ledgers plus the exact
transitive physical dependencies.  It independently replays both rational
product separators, the strict dyadic boundary, RN/good-bad/zero-fibre type
guards, the seven-field count and the no-promotion frontier.  It also checks
strict JSON, deterministic regeneration and byte-identical re-emission,
rejects hostile semantic mutations, and requires both default entry points to
exit exactly `2`.
