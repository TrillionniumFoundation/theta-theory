# CM2 Gate 2: collision-key to stable-quotient/PPE frontier assault

Date: 2026-07-16 (Asia/Shanghai)  
Model: centred rational two-disk torus pilot  
Frozen inputs: the tenth-round baseline, 441,280-key Gate-5 envelope,
post-full-cross return base, stable-saturation gap, full-mass tail, pair-energy
and actual-plaque manifests were read only

## Verdict

Gate 2 remains **`NOT_CERTIFIED`**.  The new Gate-5 key envelope gives one
strict positive upgrade and one exact no-promotion theorem.

1. The positive-SRB rectangle `S_A` now has a countable, full-measure
   **two-dimensional first-return path-key schema**: every regular collision
   step has one of 441,280 immutable keys, so every almost-sure Poincare
   return has a finite word over this alphabet.
2. This label refinement does not create the required reverse randomness.
   The two-dimensional first-return map is invertible modulo zero; after any
   finite or countable refinement of its graph, the physical reverse
   conditional remains one Dirac mass.  Its diagonal two-copy Riesz
   coefficient is exactly one.

Thus Borel path coverage is now full, but the stable-saturated product base,
stable projection, quotient density, non-invertible reverse weights, endpoint
typing and native stopping remain absent.  The raw stable-quotient physical
registration gap remains

```text
registered two-strip fraction < 0.097,
unregistered complement       > 0.903.
```

No TV/Borel or label-cardinality statement is promoted to PPE.

## 1. New full-measure two-dimensional path-key schema

The Gate-5 envelope supplies

```text
K = 441280
```

candidate keys for every regular solid-collision step.  A key records its
source normal chart, retained target lift and monotone transparent-wall
record.  Its immutable digest is

```text
841cb96798c9bd41e1440c8b2cdd93af5d80f2f64d693f2aa00175a440045ab9.
```

Let `T` be the collision map and let `S_A` be the already certified
positive-SRB source rectangle.  At collision depth `n`, declare the
candidate path universe `K^n` and select by the Borel predicate

```text
x in S_A,
T^j x notin S_A for 1<=j<n,
T^n x in S_A,
every collision step has the recorded Gate-5 key.
```

Empty path fibres remain in the common universe.  The first counts are

| collision depth | candidate path keys |
|---:|---:|
| 1 | 441280 |
| 2 | 194728038400 |
| 3 | 85929588785152000 |
| 4 | 37919008939111874560000 |
| 9 | 634494994712579641465820849774241469431808000000000 |

The disjoint union over `n>=1` is countable.  The singular cemetery is the
countable union of grazing/corner preimages and has collision-SRB measure
zero.  Poincare recurrence on `S_A` then gives

```text
mu_{S_A}{finite first-return path with a key word}=1.
```

The path grammar digest is

```text
fa64b3d66dfe1b9416419396176f41c796c3da7bd4d32d173c04cb24ba42de6c.
```

This upgrades the previous two-dimensional return alphabet from a purely
existential component formula to an executable finite-alphabet path schema.
It still does not decide which candidate paths are nonempty, cut their
homogeneous connected components, or prove that any image is a full stable
quotient branch.

## 2. Exact mass typing

Three different notions of coverage must not be identified.

| layer | exact status |
|---|---|
| regular one-collision Gate-5 key coverage | full collision-SRB measure, modulo the standard singular set |
| two-dimensional first-return path-key coverage on `S_A` | full normalized `mu|S_A` measure |
| stable-quotient branch probability mass | undefined before a stable quotient exists |
| endpoint-typed PPE probability mass | undefined before the endpoint registry exists |

The Gate-5 physical homogeneous operator block count is still zero.  The
full-measure key envelope therefore does not add stable-saturated physical
mass to the two local strips.  Their independent scale audit remains

```text
two raw strips       < 0.097 of the candidate common rectangle,
unregistered part   > 0.903.
```

The stable-quotient mass is recorded as **undefined**, not silently replaced
by zero or by the full Borel path mass.

## 3. Exact theorem: countable key refinement preserves the Dirac kernel

Let `F:S->S` be any invertible first-return map modulo zero and let `c(x)` be
any finite or countable label attached to the graph edge `x->F(x)`.  Given a
target `y`, the physical past is uniquely

```text
(F^-1 y,c(F^-1 y)).
```

Therefore the labelled reverse conditional is

```text
K_rev(y,.)=delta_(F^-1 y,c(F^-1 y)).
```

The certificate replays this on the exact 97-state permutation

```text
F(i)=37i+11 mod 97
```

while declaring the full 441,280-label universe.  Every target still has
support size one and reverse-weight square sum one.

For the truncated Riesz energy

```text
V_r(u,v)=max(r,|pi(u)-pi(v)|)^(-alpha),   alpha=1/20,
```

two conditionally independent copies started at the same target select the
same predecessor.  Hence

```text
(K_rev tensor K_rev)V_r(u,u)=r^-alpha=V_r(u,u).
```

No uniform inequality with coefficient `kappa<1` can hold.  This remains true
for the full Gate-5 path label, not only for a one-step label.

Randomizing by deliberately forgetting the physical state is not a stable
quotient: it supplies no stable holonomy, no physical endpoint carrier and no
cocycle transport compatible with the forgotten coordinate.

## 4. Cardinality/full-coverage countermodel

Split `[0,1]` into 441,280 equal Borel labels and let the map be the identity.
Every label has mass

```text
1/441280
```

and the registered Borel mass is exactly one.  Nevertheless the reverse
kernel is `delta_x`, the projective stationary law from one point is atomic,
and there is no stable-saturated product base, quotient density, native
stopping or PPE.

This proves that neither the number of keys nor their full Borel coverage can
replace the missing quotient geometry.

## 5. Stable quotient and reverse-kernel frontier

The positive precursors remain useful:

- `S_A` is a positive-SRB two-dimensional return base;
- an actual unstable reference **curve candidate** lies inside it;
- two local physical strips exist in a common rectangle;
- the abstract reverse-weight formula is exact once a quotient exists;
- the pair-energy theorem is ready once a physical kernel exists.

The first missing object is still a stable-saturated product set

```text
Lambda_A subset S_A
```

with a physical projection to a reference unstable interval

```text
pi^s:Lambda_A -> I_A.
```

Only then can connected return strips induce onto inverse branches

```text
h_a:I_A -> I_a
```

and define

```text
p_a(x)=rho(h_a(x)) |h_a'(x)| / rho(x).
```

The 441,280 keys describe collision records.  They do not assert stable
saturation, full-image after quotienting, or the holonomy Jacobian which
enters `rho`.

## 6. Endpoint typing frontier

The Gate-5 keys supply source-chart and target-lift labels, but none of the
following Gate-2 fields on a common quotient branch:

```text
transported M_a in one trivialisation,
same-carrier endpoint maps X_a,Y_a,
denominator lower bound for the projective slope,
nonzero endpoint wedge,
forward/reverse entrance and exit contexts.
```

The first missing endpoint object is the transported branch matrix `M_a` on
the same labels as `rho,h_a,p_a`.  A source/target key without a nonempty
homogeneous branch and derivative block cannot type the physical endpoint
state.

## 7. Native stopping frontier

The abstract prefix-antichain algebra remains certified.  A physical native
stopping rule additionally needs, on the actual inverse branches,

```text
diam(I_w), diam(I_parent(w)),
two-sided comparison to the requested scale,
an overshoot cemetery,
full-mass continuation after every stopped parent.
```

The first missing stopping object is the inverse-cylinder diameter registry.
The candidate path length `n` alone does not give diameter or prevent a first
child from overshooting below `sigma^2`.  On the current two-dimensional
Dirac reverse tree there is only one physical predecessor, so a label-prefix
stop cannot provide projective regularization.

## 8. Immutable 17-field completion interface

All downstream objects must share one physical branch label.  The frozen
interface now lists 17 required fields:

1. stable-saturated product base `Lambda_A`;
2. reference unstable interval `I_A`;
3. stable projection `pi^s`;
4. stable-holonomy conditional SRB Jacobian;
5. connected first-return strips;
6. onto quotient inverse branches `h_a`;
7. quotient density `rho` with upper/lower bounds;
8. physical reverse weights `p_a`;
9. transported projective matrix `M_a`;
10. same-carrier endpoint maps `X,Y`;
11. endpoint denominator lower bound;
12. nonzero endpoint wedge;
13. inverse-cylinder diameter registry;
14. native-scale prefix antichain;
15. overshoot cemetery and two-sided scale control;
16. off-diagonal projective near-collision bound;
17. parentwise normalized amplitude moment.

Their schema digest is

```text
72cdcd5a9da5056d90243419b6c6c0b2533ed4fe34d90beda71a49308803c2e8.
```

All 17 physical completion flags remain false.  This is a typed frontier,
not a claim that the fields are impossible.

## 9. Latest technology audit

The official arXiv API was queried on 2026-07-16; after the successful latest
billiard query it rate-limited repeated refinements.  No applicable new
stable-quotient/PPE theorem was found.

- Lima--Obata--Poletti, arXiv:2405.04676v2, identify the collision-SRB
  geometric potential and a countable Markov coding.  This does not identify
  a q-anchored one-state full-image quotient, `rho,h_a,p_a`, endpoint carriers
  or native stopped parents.
- Demers--Liverani, arXiv:2606.10155v1, organize anisotropic/cone methods for
  dispersing billiards.  Their review does not turn an arbitrary Borel label
  refinement into a stable quotient; its characteristic-function restriction
  frontier is directly relevant to stopped parents.
- Climenhaga--Day, arXiv:2604.25881v1, construct the unique measure of maximal
  entropy from one-sided symbolic objects.  That is not the collision-SRB
  q-anchored quotient or its endpoint/PPE kernel.
- Canestrari, arXiv:2604.19671v2, treats a fixed Sinai billiard with a
  shrinking boundary hole, not the physical reverse tree of moving collision
  words.
- Goverse--Kleptsyn, arXiv:2607.11421, construct a locally invertible random
  system with an **atomic physical stationary measure**.  It is a direct
  warning that noninvertibility or a large random alphabet alone does not
  imply Hölder/Frostman regularity.

Classical Young-tower existence for finite-horizon billiards gives an
existential hyperbolic inducing scheme.  It does not by itself bind the
declared q-anchored strips, cocycle matrices, endpoints, amplitudes and native
stopping to one executable branch registry.  No honest automatic bridge was
found.

## 10. Exact remaining Gate-2 boundary

```text
FULL-MASS 2D FIRST-RETURN PATH-KEY SCHEMA:          CERTIFIED
EXACT KEY-REFINED 2D REVERSE KERNEL:                DIRAC
PAIR-ENERGY CONTRACTION OF THAT DIRAC KERNEL:       IMPOSSIBLE

STABLE-SATURATED YOUNG RECTANGLE:                   NOT CERTIFIED
ONE-STATE FULL-BRANCH STABLE QUOTIENT:              NOT CERTIFIED
QUOTIENT DENSITY rho:                               NOT CERTIFIED
PHYSICAL REVERSE WEIGHTS p_a:                       NOT CERTIFIED
SAME-CARRIER ENDPOINT TYPING:                       NOT CERTIFIED
NATIVE STOPPING ANTICHAIN:                          NOT CERTIFIED
FULL COUNTABLE PAIR-ENERGY DRIFT:                   NOT CERTIFIED
PHYSICAL PPE:                                       NOT CERTIFIED
GATE 2:                                             NOT CERTIFIED
```

## 11. Reproduction

```bash
python -m py_compile \
  deliverables/cm2_gate2_collision_key_stable_quotient_frontier_cert.py \
  deliverables/cm2_gate2_collision_key_stable_quotient_frontier_verifier.py

python deliverables/cm2_gate2_collision_key_stable_quotient_frontier_verifier.py \
  --replay --integrity-only
python deliverables/cm2_gate2_collision_key_stable_quotient_frontier_verifier.py \
  --self-test

# Expected exit 2: physical stable quotient/PPE remains open.
python deliverables/cm2_gate2_collision_key_stable_quotient_frontier_verifier.py

sha256sum -c \
  deliverables/cm2-gate2-collision-key-stable-quotient-frontier-manifest-2026-07-16.sha256
```

Replay/integrity exits zero, nine independent mutations are rejected, and
live mode exits two by design.
