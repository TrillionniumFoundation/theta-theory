# CM2 forty-eighth direct assault

Date: 2026-07-19  
Verdict: **Round 48 closes the fixed-parameter Borel survivor-kernel schema,
sharpens the post-cut clock theorem, reduces numeric C24 covering to one
target-sensitive long-leaf atlas, and completes numeric base seeds for all
seven one-step F10 boundary kinds.  No composite gate is yet closed.**

## Round result

| gate | Round-48 advance | exact remaining core gap | status |
|---:|---|---|---|
| 1 | no unsafe promotion | physical Green/cancellation representative and same-representative twisting/PPE | NOT CERTIFIED |
| 2 | no cross-carrier promotion | invariant stable plaques, physical holonomy, quotient branches and PPE | NOT CERTIFIED |
| 3 | Borel finite-schedule survivor subkernels sharpen current typing | bulk dynamic-test `MT_DQ`, branch convergence and cemetery | NOT CERTIFIED |
| 4 | fixed-`s` Borel survivor kernel; corrected shell moment; long-leaf cover reduction | target-sensitive atlas, numeric `H_cover/beta`, proper same-ID joint return, `q`, cemetery | NOT CERTIFIED |
| 5 | all seven one-step regular F10 base seeds; exact subunit formal ledger | same-measure arbitrary-`R_n` F10, strong F13/F17, F14/F15/F18, cemetery | NOT CERTIFIED (`10/18`) |

Strict unconditional progress remains `0/5`; CM2 remains
`NO-GO_FOR_CLAIM`.

## 1. Gate 4: measurable kernels and a single geometric bottleneck

For every fixed parameter and finite depth, the arbitrary-`R_n` component
registry now gives Borel parent, forward-survivor and reverse-survivor
subkernels on the same restriction IDs.  Forward and reverse survivors are
not asserted equal, and zero survivor mass is never normalised.

Conditionally on a Borel grouping into whole proper families, the terminal
time `H` is included in the corrected clock

```text
C_sigma=H+319590+1005*k_sigma.
```

The exact survivor-weighted moment at `eta=1/6030` loses no extra `5/2`
factor.  A same-ID common intersection also has an ambient two-clock moment at
`eta=1/12060`, but the ambient clocks recover the two larger survivor
families; their intersection need not be proper.  Hence this is not yet a
joint return or `q`.

The covering problem is reduced sharply.  With

```text
delta_long=358863/(2883949304*10^90),
```

at least half the mass of every normalised proper family lies on longer
leaves.  It is sufficient to construct a common finite-time W-diagonal atlas
giving source fraction

```text
zeta_rect >= 460800/5197322039
```

on every such leaf.  Existing materials do not materialise this atlas, so
actual `H_cover` and `beta_Wdiag` remain null and the strict beta lower bound
remains zero.

The eight stationary W-diagonal cores have SRB union mass `>7/89375`, above
the conditional beta threshold, but stationary two-dimensional mass cannot be
substituted for arbitrary proper-family crossing weight.

## 2. Gate 5: all seven base seeds, still three unmatched measures

The full one-step seven-kind inventory contains 6888 candidate slots and 2604
nonzero moving slots.  Only cross-colour tangencies and W-source integer
corners move.  Their certified regular-germ costs are

```text
A_tan = 516607461656000000/47978559724753 < 10768,
A_corner < 40.
```

The other five kinds, same-colour tangencies and G-source corners have zero
parameter current at the base gauge.

The worst-source collision-boundary ratio is about `0.228275`.  Together with
the existing occurrence and C24 core targets, the formal exact ledger is

```text
13571096530812446357/14837273637760415744
 ~= 0.914662 < 1.
```

This does not earn full F10 credit: endpoint coarea, collision SRB and core
trace have not yet been joined on one arbitrary-`R_n` measurable kernel with
return-depth integrability.  Gate-5 maturity therefore stays `10/18`; complete
blocks remain zero.

Piola still removes only normal-flux multiplication.  The nonempty-suffix bulk
term pays `D_suffix*c_X+c_F13`, so strong F13 needs an F17 dynamic-test bound
or a joint insertion/suffix rank tail.

## 3. Latest-technology audit

Official versions `1210.0011v4`, `2104.06947v3`, `2604.19671v2` and
`2606.10155v1` remain unchanged.  The newly checked official
`2604.25881v1` supplies a qualitative sufficient-rectangles mechanism for
finite-horizon Sinai billiards.  Its common time and crossing weight are
obtained non-effectively and its reference rectangle is not the selected
W-diagonal C24 target, so it cannot be imported as numeric `H_cover/beta`.

## 4. Validation

Both Round-48 suites use pinned same-directory dependencies,
duplicate-key-rejecting JSON, independent exact rational replay,
deterministic manifests and fail-closed default modes.  Final validation:

```text
syntax:                   4/4
strict JSON:              2/2
integrity/replay:         4/4
hostile mutations:        64/64 + 59/59 = 123/123
default fail-close:       4/4 at exit 2
deterministic manifests:  2/2
leaf SHA manifests:       PASS
recursive SHA manifest:   PASS
```

## Next shortest line

1. Materialise a target-sensitive sufficient-rectangle atlas on all leaves of
   adapted length at least `delta_long`, with one common `H_cover`, source
   weights and artificial-child de-duplication; derive actual
   `beta_Wdiag>=230400/5197322039`.
2. Install the whole-proper-family grouping over the fixed-`s` Borel leaf
   kernel and construct a proper common fw/rev survivor return; then assemble
   numerical `C_fw/C_rev/q` and strong cemetery.
3. Build one common arbitrary-`R_n` disintegration for occurrence coarea,
   collision-SRB D1 and core traces, and prove return-depth integrability; this
   converts the subunit formal ledger into full F10.
4. Close the bulk Duhamel suffix through F17/dynamic tests or a joint rank
   tail, then F14/F15/F17/F18 and Gate-3 `MT_DQ`.
5. Continue Gates 1 and 2 independently as the two structural long poles.

Strict unconditional CM2 remains `NO-GO_FOR_CLAIM`, with zero of five
composite gates fully closed and Gate-5 maturity at `10/18`.

