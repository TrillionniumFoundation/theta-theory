# CM2 Round 59 full direct assault

Date: 2026-07-20  
Strict verdict: **0/5 complete composite gates; Gate 5 maturity 10/18 with
zero complete 18-field blocks; CM2 NO-GO_FOR_CLAIM**

## Round result

Round 59 does not close a composite gate.  It removes the remaining
aggregate shortcut around the Gate-4 landing threshold, certifies the exact
tagged branch inverse of the physical first-return graph, unifies Gate 5
coverage and optimal-clock integrability into one extended Abel ledger, and
for the first time instantiates the declared Gate-3 CAD budget with the
actual circular-pilot fixed-depth collision formula.

The three append-only attack leaves are:

1. Gate 4 cross-fibre Rokhlin / landing-threshold frontier;
2. Gate 5 unified clearance, seven-bit operator join and positive Jordan
   anchors;
3. Gate 1/2/3 physical selected-germ Dini, finite collision shadows and
   fixed-depth formula budget.

A fourth append-only leaf independently audits the mathematics, typing,
dependencies and executables.  No older artifact was edited.

## Gate 4: the aggregate route is exactly exhausted

Round 58 reduced unshifted same-ID landing properness to

```text
J_land,min(y) < C_p h(y)
```

for almost every positive physical landing fibre.  Round 59 proves that no
available global affine or moment estimate can replace this pointwise
inequality.

Let `H=999/1000` and `N=floor(C_p)+1`.  A good outer fibre has one retained
interval and `J_good=1`, while a bad fibre has `N` equal retained intervals,
the same total mass `H`, and

```text
J_bad=N,
z_bad=N/H in (C_p,2 C_p),
D_bad=2.
```

Giving the bad fibre weight `epsilon_m=1/(mN)>0` yields

```text
J_total = 1+1/m-1/(mN),
H_total = 999/1000,
integral h 2^D = (999/1000)(1+3/(mN)).
```

Thus `J_total/H_total<C_p` and the full dyadic moment can be arbitrarily
close to its minimum while a positive-mass bad fibre remains improper.  The
same model has an ambient proper identity return and a reference reassembler
with `J_cap=2,D_cap=0`.  This is an exact logical standard-family separator,
not a claim that the billiard realizes the fragmentation.  It rules out all
aggregate-to-fibre, moment-only and same-ID recutting shortcuts.

## Gate 4: one of seven cross-fibre fields is now physical

The Round-57 raw first-return law is carried by countably many tagged regular
branches recording

```text
(n, R_n path, source/parent/restriction ID, recut owner).
```

On each branch `T_s^n` is injective.  Lusin--Souslin therefore gives a Borel
tagged image and the exact inverse

```text
(tag,landing) -> (tag,T_s^(-n)(landing)).
```

It preserves first-hit time, path, immutable ID and half-open ownership.
For a Borel map into a standard-Borel quotient, put
`eta=(q o pr_landing)_#Gamma_cap`.  Rokhlin disintegration gives a probability
kernel `Gamma_u`, determined only `eta`-almost everywhere, with
`Gamma_cap(A)=integral Gamma_u(A)deta(u)`.  This is not division by singleton
mass `eta({u})`.  Graph, endpoint and tag identities hold
`Gamma_u`-almost surely for `eta`-almost every `u`; representatives on an
`eta`-null quotient set are arbitrary.  Reassembly preserves the graph
measure and its once-charge at measure level, with no pointwise
deletion/duplication claim.  This is weak same-graph Rokhlin reconditioning,
not a physical unstable-product family or a strong-space assembly theorem.

The seven-field Gate-2-to-Gate-4 interface is therefore exactly `1/7`:
only the tagged branch inverse is certified.  This does not promote any of
Gate 2's official `17` fields, which remain `0/17`.

On an actual target unstable plaque, if the common conditional is
`f 1_E ds`, `E` has at most `F` components and retained span at least
`theta L`, while `0<d_-<=f<=d_+<=R d_-`, then

```text
z_land <= F R/(theta L).
```

Hence the exact remaining numerical bridge is

```text
F R < C_p theta L
```

almost everywhere, together with physical product/holonomy fields and
strong restriction/assembly.  The arithmetic test
`F=153,R=2000/1999,theta=1/2,L=1/12800` is far inside the margin, but those
constants do not yet belong to the actual common landing law.

## Gate 5: coverage and the optimal clock are one exact ledger

On the actual finite Borel owner/root law, define the extended level

```text
Kbar=K on A_col,
Kbar=infinity on A_col^c,
Fbar_j=nu{Kbar>j}.
```

For `a_k=w_Z^r_k`, finite truncation and monotone convergence give

```text
M_N = a_0 nu(total)
      +sum_(j=0)^(N-1)(a_(j+1)-a_j)Fbar_j,

Mbar = integral a_Kbar dnu.
```

Consequently

```text
Mbar<infinity
iff nu(A_col^c)=0 and integral_(A_col)w_Z^r_K dnu<infinity.
```

Since `r_(j+1)-r_j` is zero or one, with
`S={j:r_(j+1)=r_j+1}`, this is exactly

```text
Mbar = a_0 nu(total)
       +(w_Z-1)sum_(j in S)w_Z^r_j Fbar_j.
```

The critical family

```text
Fbar_j=c/[w_Z^r_j (j+1)^p]
```

has a finite clock moment exactly for `p>1`; `p=1` is harmonic divergence.
There is also a full-coverage example with `B=14` and every polynomial
`K`-moment finite but divergent optimal-clock moment.  For one fixed owner
law the equivalent de la Vallee-Poussin Orlicz criterion is certified, but
no physical tail or uniform Orlicz bound is available.

## Gate 5: the remaining joins are now minimal and positive

Round 25 does not close `A_col` coverage.  Its finite isolated physical-root
registry is not pinned to the Round-50/54 immutable owner ID, and `d_other`
also sees homogeneity, singularity, owner, hole and chart boundaries.  The
exact complement is

```text
A_col^c={d_other=0}=N_coinc union N_acc,
nu(A_col^c)=lim_(m->infinity)nu{d_other<2^(-m)}.
```

Compact nongrazing, locally finite, exactly same-ID roots enter `A_col`
pointwise; their owner-trace mass and the grazing accumulation stratum are
not controlled.

The Round-54-to-Round-42 suffix interface decomposes into seven Boolean
joins: ID, word, input domain, all intermediate `C24` killed bits, operator,
output-to-next-input, and horizon.  Only ID and word are currently frozen.
On `A_col`, the minimal future recovery capacity is the number of consecutive
fully joined blocks; its physical law is not certified.  The exact policy
assigns infinite cost on `A_col^c`, and only uses `K` and `R` on `A_col`, so
finite policy cost itself forces full coverage.

For positive measures `mu+` and `mu-`, with signed difference `J` and common
mode `lambda=mu+ wedge mu-`, the exact identity

```text
mu+ + mu- = |J| + 2 lambda
```

shows that positive F10/cemetery charge requires both the weighted Jordan
variation and the weighted common-mode anchor.  Signed BL/OT cancellation
controls neither.  Even one finite positive anchor plus finite transport
cost is insufficient without a pointwise charge/transport inequality.
Neither physical anchor is presently finite, so Gate 5 remains `10/18` with
zero complete blocks.

## Gate 1 and Gate 2: local physical rows, no global promotion

The frozen selected QNL homoclinic germ has an actual summable compact-log
tail:

```text
rho<1/10,
q^2<1/100,
sum_(m>=0)(m+1)(1/10)^m=100/81,
sum_(m>=0)(m+1)(1/100)^m=10000/9801.
```

Its depth-260 entry error is below `8.568e-49<1e-29`, preserving all four
selected twisting wedges in the same fibre and basis.  This is one selected
germ/loop, not all-plaque weighted-projective Dini and not the missing
combined half-density gauge.  Gate 1 remains open.

The frozen positive-width actual 96-word source strip avoids every recorded
collision singularity at each of its 96 prefixes, so its finite-prefix
collision-shadow rows are `96/96` zero.  It is not an invariant stable fibre,
does not supply graph-transform charts at every future depth, and leaves the
official projected all-depth stable-shadow registry empty.  Gate 2 remains
`0/17`.

## Gate 3: actual fixed-depth formulas now fit the CAD budget

For every fixed depth `n` and `|s|<=1/400`, the circular pilot can be encoded
with the complete conservative target alphabet

```text
2 obstacle types * 9 * 9 torus lifts = 162.
```

Writing `q_j,u_j` for position and unit velocity, every collision adds eight
scalar auxiliaries.  For a distinct competing disk set

```text
a=u dot(C-q),
b=|C-q|^2-R^2,
Delta=a^2-b.
```

On `|u|=1,b>0`, absence of a root in `(0,tau)` is exactly

```text
a<=0 OR Delta<0
OR [a-tau>=0 AND Delta<=(a-tau)^2].
```

The current source disk uses the outgoing convexity sign; half-open owners,
grazing cemetery and tie rows handle boundary strata.  An explicit padded
count gives

```text
variables                 <= 8n+5,
atomic polynomial rows    <= 830n+12,
degree                    <= 4,
raw branch words           = 8*162^n.
```

These fit the Round-58 declared budget
`(400n+20,2000n+100,8)`, so the actual physical fixed-depth formula now
instantiates the explicit CAD recurrence.  This is the strongest new
Gate-3 interface in the round.

It still does not give an actual cell enumeration, a depth-integrable
complexity bound, inverse-length/boundary control, strong `R_s/Q_s`,
directional Piola or `MT_DQ`.  In particular the fixed-depth CAD majorant is
far too large to be paid by the existing `integral exp(Dbar/6)<infinity`.

## Latest-technology audit

Current official sources were checked through 2026-07-20.  In particular,
arXiv `2604.25881v1` supplies local product structure and symbolic stable
holonomy for its constructed measure of maximal entropy, not for the pinned
Liouville/SRB common landing law; it supplies no physical RN constants,
fragmentation, boundary `Z` or strong assembly.  Its total component-length
estimate does not control component count or inverse lengths.  Recent
CAD/o-minimal work (`2603.25380`, `2604.06144`, `2601.09548`,
`2605.04718`), dispersing-billiard transfer-operator surveys and current
signed-transport work likewise do not instantiate the missing CM2-specific
physical joins.  No external theorem was promoted into a certificate
dependency.

## Independent audit and full validation

The independent audit pins all `6/6` main manifests and recursive ledgers,
parses all `12/12` main-leaf artifact SHA rows, and independently replays:

- the Gate-4 two-fibre arithmetic, `D_bad=2`, product bridge and corrected
  `eta`-a.e. Rokhlin scope;
- the Gate-5 Abel identity, active count, critical `p>1` threshold, Orlicz
  scope, extended policy typing and Jordan identity;
- the Gate-1/2 local-only boundary and Gate-3 identity
  `161*5+25=830`, `330` exact rational root cases, word universe and
  fixed-depth-only scope.

It reports `PASS_NO_CLAIMED_SCOPE_BLOCKER`.  The final four-leaf validation
matrix is:

```text
syntax:                    8/8 PASS
dependency/artifact pins: 37/37 PASS
integrity:                  4/4 PASS
deterministic replay:       4/4 PASS
byte-identical re-emission: 4/4 PASS
hostile mutations:          331/331 rejected
leaf SHA entries:           16/16 PASS
default cert/verifier:       8/8 exit exactly 2
Round-59 temporary/stale artifacts: none
```

## Strict status after Round 59

```text
Gate 1: NOT_CERTIFIED
Gate 2: NOT_CERTIFIED (0/17 physical fields)
Gate 3: NOT_CERTIFIED
Gate 4: NOT_CERTIFIED
Gate 5: NOT_CERTIFIED (10/18; complete blocks 0)
complete composite gates: 0/5
CM2: NO-GO_FOR_CLAIM
```

The next shortest routes are now:

1. materialize Gate-4 product rectangles, stable projection/Jacobian,
   fragmentation and same-measure conditional density on the actual common
   landing law; prove `FR<C_p theta L`; then install strong assembly;
2. use the resulting proper original-time first-return kernel to join killed
   intermediate `C24` avoidance, later/repeated clocks, physical
   `q in L^(6/5)` and strong cemetery;
3. on the Gate-5 owner law, prove `A_col` trace coverage and the active Abel
   tail (or one physical Orlicz bound), then freeze the five missing suffix
   bits and both positive anchors;
4. extend the Gate-1 selected-germ tail to all plaques and the combined
   gauge, construct Gate 2's all-depth stable quotient, and replace Gate 3's
   fixed-depth CAD majorant by a depth-integrable strong operator/Piola
   interface.

## Frozen Round-59 leaves

- `cm2-gate4-round59-cross-fibre-rokhlin-threshold-frontier-assault-2026-07-20.md`
- `cm2-gate5-round59-unified-clearance-join-jordan-frontier-assault-2026-07-20.md`
- `cm2-gate123-round59-physical-formula-shadow-dini-frontier-assault-2026-07-20.md`
- `cm2-round59-independent-core-frontier-audit-2026-07-20.md`.

Their recursive SHA ledgers are pinned by
`cm2-fifty-ninth-direct-assault-manifest-2026-07-20.sha256`.
