# CM2 twenty-fourth direct assault: source-clock ledgers, Kac baseline, and induced-operator boundary

Date: 2026-07-18 (Asia/Shanghai)  
Mode: append-only, fail-closed, exact canonical joins and 2048-bit Arb replay  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; no composite
gate is fully closed (`0/5`).  This round upgrades all 128 charged entrance
cylinders to source-relative first-event ledgers, closes the aggregate
collision-SRB Kac mass/first-moment baseline on the full 24-core union, and
freezes why neither the open-core coefficient nor the new core/Kac data can
be promoted to the missing induced strong operator or Gate-2 PPE.**

## 1. Executive result

| Gate | New strict progress | Remaining core blocker | Status |
|---|---|---|---|
| 1 | no promotion; the prior physical full-cross/reversible strip remains | coupled half-density/common frame, uniform big-cell `q>0`, twisting in the same representative | **OPEN** |
| 2 | exact 17-field nonpromotion audit and core/Kac-to-PPE countermodel | `Lambda_A`, invariant stable plaques, `I_A`, `pi^s`, conditional stable-holonomy SRB Jacobian | **OPEN (`0/17`)** |
| 3 | 128 source-relative first-core cylinders; 128 exact charged Borel domains and first-event ledgers | full-dimensional physical source partition and common fw/rev strong restriction | **OPEN** |
| 4 | aggregate collision-SRB return-mass identity, finite first moment, measurable induced `L1` map | materialized `R_n/Q_n`, `q`-weighted exponential tail, induced strong coefficient | **OPEN** |
| 5 | exact proof that fields 5/6 are only seeds and `b_core` is not an induced coefficient | 14 missing fields, three norms, physical Kac typing and complete operator blocks | **OPEN (`4/18`)** |

## 2. Source-relative first stopping

The twenty-third-round clock began at the regular suffix collision.  This
round also classifies the source collision and, on every hit side, the
near-grazing intermediate collision against all 24 frozen cores.

```text
shared source states strictly outside C24:                 64/64,
hit pre-suffix states strictly outside C24:                64/64,
source-relative first-core cylinders:                    128/128,
strict preterminal outside-core states:                      948,
strict terminal unique-core states:                          128,
total classified states:                                   1076,
ambiguous classifications:                                    0,
maximum source-relative first-core time:                       22.
```

The hit enclosure contains the singular `r=0` boundary trace, but the
regularity claim is restricted to the open positive side `r>0`.  Strict
outside-core separation on the larger closed enclosure covers that open
side without declaring the boundary trace regular.

The oriented time histogram is

```text
3:22, 4:32, 5:18, 6:8, 8:8, 9:12, 10:8,
11:4, 17:2, 18:2, 20:4, 21:6, 22:2.
```

## 3. Exact charged-domain first-event ledgers

An initial raw-interface audit found a real domain-typing problem.  Only the
two original occurrences, four parameter sides, were strictly contained in
the old occurrence-matched all-scale germs.  The other 124 charged cylinders
were valid Arb trajectory atoms but could not be inserted into those old
ledgers merely by matching occurrence IDs.

The resolution is append-only: register each charged cylinder as its own
canonical Borel `(z,h)` domain at `s=0`, rather than pretending it equals an
old germ.  All 128 domains now have an exact domain-specific ledger and one
nonempty source-first-core slot equal to the whole domain.

```text
exact charged Borel domains / ledgers:                    128/128,
old selected-germ ledgers reused:                               0,
prior occurrence-only mismatches resolved by new domains: 124/124,
unique nonempty destination-core slots:                       128,
empty finite-prefix core slots:                             25696,
empty finite-prefix singular slots:                          1076,
total finite-prefix slots:                                  26900.
```

The materialization stops at first core entrance.  It creates no post-stop
slot and no nonreturning-cemetery slot.  At a fixed parameter the domains
remain one-dimensional collision-source curves, hence their finite union has
zero two-dimensional collision-SRB mass.  This entrance ledger does not
cover a positive-area core rectangle or define a post-core return operator.

## 4. Full-core collision-SRB Kac baseline

For each fixed `|s|<=1/400`, let `C_s` be the union of the 24 physical cores
and let `mu_C_s` be normalized collision-SRB restricted to `C_s`.  The frozen
mass lower bound is

```text
mu_s(C_s) > 147/550000.
```

Poincare recurrence and the general, nonergodic Kac formula now give the
first unconditional full-core return statements:

```text
mu_C_s{tau_C_s^+<infinity} = 1,
sum_(n>=1) mu_s(C_s intersection {tau_C_s^+=n}) = mu_s(C_s),
integral_C_s tau_C_s^+ dmu_s = mu_s(saturation_T_s(C_s)) <= 1,
E_mu_C_s[tau_C_s^+] < 550000/147.
```

Therefore

```text
sum_(n>=0) mu_C_s{tau_C_s^+>n} < 550000/147,
mu_C_s{tau_C_s^+>n} < 550000/[147(n+1)].
```

The pointwise bound is still capped by one at `n=2018`; at `n=12108` it is

```text
mu_C_s{tau_C_s^+>12108} < 550000/1780023
                         = 0.30898477....
```

The measurable induced map is defined `mu_C_s`-almost everywhere, preserves
`mu_C_s`, and its Perron operator is positive with `L1(mu_C_s)` norm one.
This is an aggregate measure-theoretic induced baseline.  It does not list
connected return branches, collision words, `m_n/q_n`, Jacobians,
distortion, or strong-space costs.

Thus two statements must remain distinct:

- aggregate collision-SRB mass conservation and a finite unweighted first
  moment are now certified by recurrence/Kac;
- materialized branchwise mass conservation and a `q`-weighted exponential
  tail remain uncertified.

## 5. Why the open-core coefficient does not transfer

The frozen coefficient

```text
b_core=720269600000/720626832337
```

belongs to the open operator `O=L M_C`.  The desired objects have signatures

```text
R_n=M_C L(M_Cc L)^(n-1)M_C,
Q_n=(M_Cc L)^n M_C.
```

An exact two-state signature countermodel has `L` swap the core and exterior.
It gives

```text
||O^2||_1=0,              ||R_2||_1=1.
```

Hence even a perfect power estimate for `O` does not formally imply the same
estimate for `R_n`.  This is a logical operator countermodel, not a CM2
dynamics counterexample.  The four local CM2 boxes make the semantic gap
physical: they all leave `C24` at post-core time one, while their positive
returns are `545,649,1531,>2018`.  Repeated open powers clip precisely the
excursions that an induced operator must retain.

The machine ledger freezes 13 still-missing induced interfaces: a complete
collision-SRB source partition; raw `R_n/Q_n` rows; all complement guards;
hidden-recut margins; `m_n`; unstable Jacobians; accumulated distortion; the
four strong operator costs; a common fw/rev restriction and carriers;
physical four-term Kac typing; singular/survivor masses; a weighted `q` tail;
and the resulting common-space contraction coefficient.

Gate-5 maturity therefore remains exactly

```text
completed selected fields:          F1,F3,F4,F7 = 4/18,
field-5/6 component-local seeds:                 24,
field-5/6 completed roof-level slots:              0,
complete 18-field operator blocks:                 0.
```

## 6. Gate-2 type boundary

The new core, entrance, return and Kac records do not create a stable
quotient.  An exact field-by-field audit keeps all 17 Gate-2 completion flags
false.  In particular:

- a destination core ID is not a stable projection fibre;
- `hit/miss`, `fw/rev`, and the two singular Kac coordinates are not the
  inverse-branch axis;
- positive core mass is not a quotient density `rho`;
- first-core time is not inverse-cylinder scale stopping;
- a censored `tau_C>2018` curve is not overshoot cemetery.

An exact 128-state identity countermodel carries positive mass one, 64
centered `(+1,-1)` Kac pairs and 14 destination labels, but every reverse
conditional remains Dirac and its pair-energy coefficient is one.  Thus
core mass plus exact Kac centering does not logically imply PPE.

The shortest valid Gate-2 input remains

```text
Lambda_A + interval-indexed invariant stable plaques
+ I_A + pi^s + conditional stable-holonomy SRB Jacobian.
```

Only then can saturated return strips, `h_a`, `rho` and `p_a` share one
physical branch registry.

## 7. Current technology check

Official arXiv pages and the relevant theorem statements were checked through
2026-07-18.

- Demers--Wright--Young, arXiv:`0904.1135`, proves escape results for
  sufficiently small physical/table-boundary holes using a hole-respecting
  mixing tower.  The 24 bounded `(t,p)` phase rectangles are not one of its
  declared hole classes, and no such tower is frozen here.
- Demers--Liverani, arXiv:`2104.06947v3`, and the 2026 review
  arXiv:`2606.10155v1` require small-hole/sparse-hole interfaces such as
  uniform `O1/O2`, stable-diameter smallness relative to cone constants, or
  a sparse mixing block.  Those constants are not bound to `C_s`.
- Canestrari, arXiv:`2604.19671v2`, exploits the special geometry of a small
  vertical boundary hole and an initial regular standard family; it does not
  cover these general phase rectangles.
- Liu, arXiv:`2406.13460`, and Bajovic--Petkovic,
  arXiv:`2607.11180v2`, give recurrence/waiting-time results for shrinking
  targets.  They do not supply a fixed-core branchwise `q` envelope.
- Duvall, arXiv:`2606.27488v1`, treats survivor-conditioned renewal for open
  intermittent interval maps through a killed induced operator, not the
  missing billiard operator interface.

No external result is promoted beyond the standard Poincare--Kac theorem
whose hypotheses are already bound to the frozen invariant collision
probability and positive Borel core union.

The smallest theorem-match package that could upgrade Kac to an exponential
unweighted survivor tail is now explicit:

```text
uniform C_s-specific O1'/O2 stable-curve hole geometry,
a uniform core-mass upper bound (<=1/2 for the sparse route),
either a quantitative small-hole threshold or a sparse block n_*,
one compact-s family of cone/admissibility/recovery constants,
and a stationary mass-loss/eigenvalue bound strictly below one.
```

If those yield

```text
mu_s(intersection_(j=0)^(n-1) T_s^(-j) C_s^c) <= A rho^n,
```

then the existing core-mass lower bound immediately gives

```text
P_mu_C_s(tau_C_s^+>n) < (550000/147) A rho^n.
```

That would still be unweighted; the branchwise strong `q` tail would require
the additional distortion and operator-cost payload.

## 8. Strict final boundary

```text
COMPOSITE GATES FULLY CLOSED:                              0/5
UNCONDITIONAL CM2:                                         NO-GO FOR CLAIM

SOURCE-RELATIVE FIRST-CORE CYLINDERS:                  128/128 CERTIFIED
EXACT CHARGED-DOMAIN FIRST-EVENT LEDGERS:              128/128 CERTIFIED
FINITE-PREFIX EMPTY CORE / SINGULAR SLOTS:        25696 / 1076 CERTIFIED
AGGREGATE COLLISION-SRB FIRST-RETURN MASS IDENTITY:          CERTIFIED
NORMALIZED FIRST-RETURN FIRST MOMENT:                <550000/147 CERTIFIED
MEASURABLE INDUCED L1 OPERATOR BASELINE:                     CERTIFIED
OPEN-POWER -> INDUCED-COEFFICIENT NONIMPLICATION:            CERTIFIED

FULL 2D BRANCH-MATERIALIZED R_n/Q_n PARTITION:          NOT CERTIFIED
q-WEIGHTED EXPONENTIAL EXCURSION/CEMETERY TAIL:         NOT CERTIFIED
INDUCED COMMON STRONG LASOTA--YORKE COEFFICIENT:         NOT CERTIFIED
COMMON FW/REV STRONG RESTRICTION AND CARRIERS:           NOT CERTIFIED
PHYSICAL FOUR-TERM KAC / THREE NORMS:                    NOT CERTIFIED
GATE-2 STABLE QUOTIENT / PPE FIELDS:                            0/17
GATE-5 SELECTED FIELD MATURITY:                                  4/18
COMPLETE 18-FIELD OPERATOR BLOCKS:                                  0
```

## 9. Fail-closed validation

The six new verifier suites reject

```text
51/51 + 64/64 + 60/60 + 55/55 + 57/57 + 52/52
= 339/339
```

new hostile mutations.  Together with the previous `547/547`, the cumulative
count is `886/886`.  Every explicit replay and integrity mode passes; every
live mode exits `2`.  Frozen v52, all six leaf SHA files, the recursive
Gate-2/3/4/5 chain, and the direct-assault root pass at final freeze.

## 10. Next direct attacks

1. **Certify the 24-core open-hole geometry.**  Prove uniform `O1'/O2`, a
   core-mass upper bound, and either quantitative small-hole admissibility or
   a sparse recovery block.  This is now the shortest route from the Kac
   first moment to an unweighted exponential survivor tail.
2. **Materialize the full-dimensional return partition.**  Adaptively split
   each two-dimensional core rectangle in `(t,p)`, record every owner/root and
   core-separation margin, and terminate in a return atom, singular outer
   cover, or survivor parent.  The 128 charged entrance ledgers are not a
   substitute for this post-core partition.
3. **Attach physical operator payloads.**  For every raw return atom bind
   collision-SRB mass `m_n`, Jacobian/inverse, accumulated distortion,
   regular/standard/flux/dynamic costs, common fw/rev restrictions and strong
   envelope `q_n`.
4. **Derive the induced coefficient from complement propagation.**  Prove
   the branchwise weighted tail and the new common-space Lasota--Yorke
   inequality; never reuse `b_core` as though open clipping followed an
   excursion.
5. **Close the long structural poles.**  Build the Gate-2 stable-saturated
   base and quotient/PPE, complete Gate-5 fields `2,5--18`, and return to the
   Gate-1 coupled common frame and twisting.

The highest-leverage next line is now a paired attack: **uniform 24-core
open-hole admissibility for an exponential physical survivor tail, together
with adaptive full-dimensional `R_n/Q_n` branch materialization.**
