# CM2 seventeenth full direct assault

Date: 2026-07-17 (Asia/Shanghai)  
Frozen baseline: v51/v52 and the first sixteen rounds remain read-only; this
pass is append-only  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; fully closed
composite gates remain `0/5`.  Five missing interfaces were nevertheless
sharpened or partially closed with replayable certificates.**

## 1. Executive result

This pass follows the requested chain from all-plaque class H through
characteristic boundaries, graph-current DQ/FACE carriers, recovery, stable
quotient/PPE, 18 fields, three norms and Kac/phase.

- **Gate 1:** the two variable-diagonal groupoid equations are solved
  separately on every stable and unstable plaque by exact Green-series
  shears.  Each one-sided representative is class H.  Combining both shears
  creates an explicit noncommutative cross term; its uniformly renormalised
  Hölder limit and twisting in that same representative remain open.
- **Gates 2/5 characteristic layer:** the finite characteristic-boundary
  theorem now covers all `441280` keys and every maximal connected component,
  not only 24 seeds.  Field 7 has an all-key formula, but its worst
  restriction/step coefficient is greater than one.
- **Gate 3:** the 64 initial face rows and 41,444 future graph charts embed in
  one fixed 41,508-slot free graph-current carrier.  The first two-step DQ has
  exactly two additive face insertions and positive TV outer
  `2590777728/5`; no delta-product term occurs.  The physical anisotropic
  lift/quotient and moving-test convergence remain missing.
- **Gate 4:** the fixed 24-core contraction now yields an actual unbounded
  transport-delay Green kernel, including exact first moment and a rational
  exponential moment.  Unbounded newly selected cuts require a geometric
  count tail `rho<8/15` for the mass-weighted shell, or
  `rho<1999/580000` for raw field 7.  No physical such tail or transported
  occurrence-to-core incidence is certified.
- **Gates 2/5 closure layer:** class H is proved not to imply stable quotient
  or PPE by an exact invertible Dirac-kernel countermodel.  Gate 2 remains
  `0/17` physical fields.  Gate 5 has one all-key formula field out of 18,
  two additional 24-core local seeds, and zero complete operator blocks.  The
  direct-standard-`N` route needs no separate phase tower, but the three
  return-wide norm intertwiners and physical Kac typing remain absent.

## 2. Gate table

| gate | strongest new certified layer | exact remaining core gap | status |
|---|---|---|---|
| 1 | stable and unstable variable-diagonal equations solved separately; both one-sided representatives class H on all plaques | one combined representative: renormalised shear cross term, class H and selected twisting simultaneously | **OPEN** |
| 2 | all-key component characteristic formula; exact class-H-to-PPE nonimplication | stable-saturated base, `pi^s`, `rho`, `p_a`, endpoint/stopping/energy packet; `0/17` fields | **OPEN** |
| 3 | fixed 41,508-slot free current carrier; exact two-cut additive algebra; TV `2590777728/5` | physical `B2->B0` lift/quotient, strong depth-two DQ, `MT_DQ`, `FACE_2CUT/FACE_TIME` | **OPEN** |
| 4 | fixed-core unbounded-delay Green kernel and exact new-cut tail thresholds | graph-occurrence/core transport incidence, cemetery payload, native count tail, `C_fw,C_rev,q` | **OPEN** |
| 5 | all-key field-7 formula (`1/18` at schema level); direct-`N` phase route selected | homogeneous branch ids, complete 18-field blocks, three norm intertwiners, physical four-term Kac output | **OPEN** |

## 3. Gate 1: all-plaque class H

After scalar Sinai normalisation of the diagonal entries, let
`r=a_s/a_u` depend on the future and satisfy `sup|r|<1`.  For a Hölder
future marker `xi`, the exact stable Green series

```text
u(x)=sum_{k>=1} r^(k)(sigma^-k x) xi(sigma^-k x)
```

satisfies

```text
u(sigma x)=r(x)(u(x)+xi(x)),
u(sigma^n y)-u(sigma^n x)=r^(n)(x)(u(y)-u(x)).
```

The lower shear `L_u=I+uE_21` therefore cancels the critical stable
conjugation on every stable plaque and has Hölder canonical stable and
unstable holonomies.  Time reversal gives the corresponding upper shear
`U_v=I+vE_12` on every unstable plaque.

For `D=U_vL_u`, however,

```text
D_y D_x^-1
 =[[1+v_y du, dv-v_y du v_x],
   [du,       1-du v_x]].
```

The stable critical coordinate is exact, while the unstable coordinate has
the cross term `-v_y du v_x`.  Reversing the order moves the symmetric cross
term to the stable side.  The two linear groupoid equations are thus solved
separately, but the combined nonlinear third gauge is not certified.

## 4. All keys and maximal components

The complete key count is

```text
G-source: 4*57*985 = 224580,
W-source: 4*55*985 = 216700,
total:                   441280.
```

Every key fibre and every maximal component has relative boundary inside the
same exhaustive physical branch union.  Each active branch crosses a
canonical unstable curve at most once, and simultaneous roots are charged
once.  Therefore

```text
G-source: at most 289 roots and 290 intervals,
W-source: at most 285 roots and 286 intervals.
```

Uniformly for `|s|<=1/400`,

```text
Z_*(1_{D_w(s)}F) <=(580000/1999)Z_*(F)  on G keys,
Z_*(1_{D_w(s)}F) <=(572000/1999)Z_*(F)  on W keys,
```

and the same holds for each maximal component.  The worst physical-step
coefficient is

```text
208878184000000/720626832337>1,
```

so this is a finite field-7 formula, not a Growth contraction.

## 5. Common graph-current carrier

The fixed slot set is

```text
Omega_<=2=Omega_initial disjoint-union Omega_future,
|Omega_<=2|=64+41444=41508.
```

Pulling every coefficient measure to `I=[0,1]` gives a fixed free Banach
triple with `W^{1,1}`, `BV` and measure slots.  Static graph assembly is
bounded by the slotwise `l1` TV norm.  The exact finite-difference identity

```text
Delta_s(P^2)=Delta_s(P)P_0+P_s Delta_s(P)
```

has two additive single-face insertions.  Adding the frozen outers gives

```text
16128/5+518152320=2590777728/5.
```

This free carrier is not the physical Demers--Zhang triple.  The missing
maps are a uniformly bounded physical restriction/lift into the carrier and
a physical anisotropic quotient/assembly out of it, intertwined on all
branch records and all 18 fields.

## 6. Recovery ledger and unbounded counts

For the fixed core,

```text
b=720269600000/720626832337,
sum b^k=720626832337/357232337,
sum k b^k
 =519045600276638055200000/127614942598481569.
```

Since `b^2018<1/2`, choosing `gamma=1/4036` and using
`exp(1/2)<5/3` gives

```text
sum exp(gamma k)b^k   <=20180,
sum k exp(gamma k)b^k<=244339440.
```

Thus any summable sequence of positive injections into the fixed core has a
complete unbounded-delay convolution ledger.  This does not prove that an
arbitrary sequence of new cuts is summable.  If the number `H` of new cuts
has `P(H>=h)<=C rho^h`, the shell moment is finite under `rho<8/15`; raw
field 7 requires `rho<1999/580000`.  These are now exact targets for the
missing native stopping theorem.

## 7. Stable quotient, 18 fields and Kac/phase

The constant class-H cocycle `diag(2,1/2)` over the invertible permutation
`i ->37i+11 mod 97` has identity holonomies but a Dirac reverse kernel.  Its
diagonal two-copy Riesz coefficient is one.  Hence class H cannot replace
the physical stable quotient or imply PPE.

The current maturity ledger is

```text
Gate 2 physical quotient/PPE fields:       0/17,
Gate 5 all-key formula fields:             1/18 (field 7),
Gate 5 24-core local seeds:                fields 5 and 6,
complete physical homogeneous 18-field blocks: 0.
```

The Borel four-term Kac algebra is exact, singular event-current TV is at
most `16128/5`, and the height-nine singular phase lift is at most
`290304/5`.  The standard-`N` component gcd is one.  The preferred direct
route therefore needs no extra phase tower.  Full Kac still requires
physical typing of all four terms through complete word blocks and all three
return-wide norm intertwiners.

## 8. Strict final boundary

```text
COMPOSITE GATES FULLY CLOSED:                         0/5
UNCONDITIONAL CM2:                                    NO-GO FOR CLAIM

SEPARATE ALL-PLAQUE CLASS-H GAUGES:                   CERTIFIED
ALL-KEY/ALL-COMPONENT CHARACTERISTIC FIELD-7 FORMULA: CERTIFIED
COMMON FREE TWO-CUT GRAPH-CURRENT CARRIER:             CERTIFIED
FIXED-CORE UNBOUNDED-DELAY GREEN KERNEL:               CERTIFIED
DIRECT-STANDARD-N PHASE ROUTE CHOICE:                  CERTIFIED

COMBINED CLASS-H + TWISTING REPRESENTATIVE:            NOT CERTIFIED
STABLE QUOTIENT / PHYSICAL PPE:                        NOT CERTIFIED
PHYSICAL STRONG DQ / MT_DQ / FACE:                     NOT CERTIFIED
NATIVE UNBOUNDED NEW-CUT RECOVERY:                     NOT CERTIFIED
COMPLETE 18 FIELDS / THREE NORMS / PHYSICAL KAC:       NOT CERTIFIED
```

The shortest honest next attacks are now uniquely typed:

1. prove the renormalised nonlinear shear cross-term limit on all plaques;
2. construct an interval-indexed stable-saturated return base and `pi^s`;
3. materialise homogeneous branch ids and the physical carrier
   lift/quotient in all 18 fields;
4. build the occurrence-to-core transport incidence and a native cut-count
   tail below `8/15`;
5. propagate the three CM2 norm intertwiners and type all four Kac terms on
   the direct-standard-`N` route.

## 9. Reproduction

All five new fail-closed verifiers support replay/integrity, hostile
self-tests and a live mode which deliberately exits `2` while the composite
gate remains open.  The individual `.sha256` files and the seventeenth
aggregate manifests freeze every certificate, verifier, report and JSON
manifest.
