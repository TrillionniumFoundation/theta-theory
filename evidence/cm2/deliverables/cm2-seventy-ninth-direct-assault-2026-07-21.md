# CM2 Round 79 complete depth-two quotient and rank-three carrier assault

Date: 2026-07-21

## 1. Strict outcome

Round 79 closes the complete fixed-`s=0` depth-two **physical boundary quotient** generated by all Round-77 failure carriers, but it does not close the cellwise commuting square or the all-rank RN sum.

The strict global state remains:

- Gate 4 landing join: `1/7`;
- Gate 5 maturity: `10/18`, complete blocks `0`;
- complete composite gates: `0/5`;
- CM2: `NO-GO_FOR_CLAIM`.

## 2. All 176 pending tangency pairs are excluded

Round 78 left 176 same-source tangency pairs with overlapping axis-aligned bounding boxes. Round 79 processes every one of them with 384-bit Arb interval subdivision and a two-equation Krawczyk branch when needed.

- same-source tangency pairs: `244`;
- strictly bbox-disjoint pairs: `68`;
- interval-processed overlap pairs: `176`;
- certified disjoint after interval subdivision: `176/176`;
- certified intersections: `0`;
- unresolved pairs: `0`;
- interval box tests: `19,632`;
- maximum binary depth: `14`.

Thus the bbox overlaps were interval overestimation, not physical intersections.

## 3. Mixed R1/R2/tangency intersections and endpoints

All same-source mixed pairs involving a tangency curve are also discharged:

- tangency/R1 pairs: `160`;
- tangency/R2 pairs: `64`;
- total mixed pairs: `224`;
- bbox-disjoint mixed pairs: `64`;
- interval-processed mixed pairs: `160`;
- certified disjoint after subdivision: `160/160`;
- certified intersections: `0`;
- unresolved pairs: `0`;
- interval box tests: `5,136`;
- maximum binary depth: `10`.

The `184` tangency endpoints are separately certified as new stationary-side vertices. The audit performs `224` same-side tangency endpoint separation tests and `320` tangency-endpoint/R1 level-function tests. No tangency endpoint lies on an old R1/R2 vertex or on another tangency endpoint.

## 4. Complete fixed-s=0 depth-two physical boundary quotient

Starting from the Round-75 R1/R2 subquotient `(V,E,F)=(208,256,72)`, insert the `92` pairwise-disjoint proper tangency crosscuts with `184` new stationary endpoints.

- new vertices: `184`;
- new stationary segments: `184`;
- new pullback edges: `92`;
- new two-cells: `92`;
- complete boundary quotient: `(V,E,F)=(392,532,164)`;
- Euler identity: `392-532+164=24` across the 24 physical core rectangles;
- physical boundary curves: `32 R1 + 92 tangency + 32 R2 = 156`;
- oriented internal physical trace pairs: `156`;
- duplicate trace cost before total variation: exactly `0`.

This is a complete quotient for the physical boundary-carrier set. It is not yet the Gate-4 commuting square: the `164` open cells have not been explicitly enumerated with a cellwise `R1/R2/Q2` label and source/destination incidence row.

## 5. Fixed-s=0 rank-three depth-six registry

The actual time-three adaptive registry is deepened from two to six extra binary levels.

- terminal incidences: `484,812`;
- strict Q3 cells: `200,408`;
- finite strict R3 cells: `0`;
- unresolved cells: `284,404`;
- strict Q3 normalized area: `211971/16384`;
- unresolved normalized area: `71101/16384`;
- depth-two unresolved area: `667/64`;
- four-level refinement ratio: `(71101/16384)/(667/64)=71101/170752 < 1/2`.

The ratio is only contraction of the **certification remainder under spatial refinement**. It is not a physical rank-survival contraction and is not used as an RN tail estimate. Finite `R3=0` is not promoted to limiting emptiness.

## 6. Actual rank-three physical carrier seeds

Every finite depth-two time-three unresolved seed is replayed with geometry and assigned to an explicit carrier identity.

- unresolved seed incidences: `92,364`;
- source/carrier families: `412`;
- physical third-collision discriminant families: `408`;
- third-collision tangency seed incidences: `91,052`;
- typed second-outgoing chart/geometry families: `4`.

This is the first actual fixed-`s=0` rank-three physical **carrier seed registry**. It is not yet a physical face-row atlas: connected-component extraction, generalized interval-Krawczyk continuation, endpoint/intersection certification, and rank-3 numeric F8/F9/F10/F13/F16 rows remain open.

## 7. RN frontier

The existing `64` actual RN-dominated face rows at ranks 1 and 2 remain valid. Round 79 does not manufacture rank-3 RN rows from carrier seeds.

- certified rank-1/2 RN face rows: `64`;
- certified rank-3 RN face rows: `0`;
- uniform physical rank-tail contraction: not certified;
- all-rank RN summability: not certified.

The depth-refinement ratio `71101/170752` cannot substitute for rank-tail contraction because it measures interval-certification resolution, not path-law mass or face charge.

## 8. Audit

- all Round-79 Python sources compile;
- integrated manifest pins all primary inputs;
- independent audit rejects `12/12` hostile semantic mutations;
- strict JSON audit rejects `4/4` duplicate/non-finite cases;
- all lightweight generators replay byte-identically;
- the 16-worker depth-six generator and the rank-three carrier extractor are cold-replayed byte-identically before final sealing.

## 9. Shortest next route

1. quotient the 408 raw `(source core, third candidate)` families by lattice/dihedral symmetry;
2. extract connected grid components within each reduced carrier orbit;
3. run generalized interval-Krawczyk continuation for the third-collision discriminant curves;
4. certify endpoints, pair intersections, and open-cell labels;
5. attach rank-3 F8/F9/F10/F13/F16 and actual branch-RN incidence rows;
6. compare rank-1/2/3 physical face charge by a genuine rank transition operator before attempting any uniform tail claim.
