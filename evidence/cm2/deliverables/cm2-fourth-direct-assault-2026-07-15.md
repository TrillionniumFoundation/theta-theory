# CM2 fourth direct assault: two local branches, corrected event ledger, and phase audit

Date: 2026-07-15 (Asia/Shanghai)  
Execution: direct interactive work only; no CM2 cron or detached scheduler  
Frozen inputs: v51/v52 and their manifests were read but not edited

## Verdict

**Unconditional CM2 remains `NO-GO FOR CLAIM`.**  Under strict theorem
typing, the five composite gates remain **0/5 fully closed**.  The result is
not merely a restatement of the previous boundary: Gate 1's literal geometry
is certified, Gate 2 now contains two actual local physical branches in one
rectangle, Gate 3's entire suspect endpoint family has been resolved, and the
actual component phase graphs have gcd one.  None of these local or algebraic
closures supplies the still-missing full-mass quotient, global physical event
rows, stopped recovery, or Banach-norm transfer.

| gate | strongest certified layer | strict remaining obstruction | verdict |
|---|---|---|---:|
| 1. common magnet / twisting | true invariant-graph common vertex, four-face full-cross, 96-collision closed shadow, four nonzero same-orbit wedges | canonical actual-cocycle holonomy loop or another applicable non-fiber-bunched typicality theorem | **OPEN** |
| 2. physical SRB/PPE | direct stopped energy theorem and two disjoint local physical full-cross branches in one common rectangle | stable saturation/common holonomy, full-mass countable quotient, actual weights, tail PPE, endpoints and amplitude registry | **OPEN** |
| 3. global event/DQ | all eight atlases; pair/triple exclusion; owner reduction; 320 suspect descriptors all empty; one immutable four-row orbit | maximal connected rows over all 288 sheets, duplicate quotient, global DQ and scalar matching | **OPEN** |
| 4. same-occurrence `q` | one physical grouped incidence; universal completed-row two-view/common-`m`/single-charge schema | complete physical row registry and stopped-parent bidirectional recovery | **OPEN** |
| 5. full-boundary transfer | exact finite Kac algebra, scalar roof cancellation, actual component phase graphs strongly connected with gcd one | complete operator word registry, return-boundary DQ, typed singular currents, physical norm constants and two CM2 norm lifts | **OPEN** |

## 1. Gate 1: literal geometry certified, strict typicality input open

A 500-bit correlation-preserving Krawczyk inclusion certifies a transverse
intersection of the actual `W^u_loc(QNL)` and `W^s_loc(connector)` graphs,
with

```text
det DF = 6.82e14 +/- 7.16e11 > 0.
```

The complete 24-collision word and all first-hit margins pass.  A Taylor
model then certifies the four-face full-cross and the reversed inherited
strip.  Separately, a 900-bit interval-Newton solve closes a 96-collision
finite shadow orbit with positive connector dwell and certifies four
same-orbit wedges

```text
-4.0146568962e28,
 1.4538820778e54,
 1108.5816546,
-4.0146568962e28.
```

This does not yet produce the canonical Bonatti--Viana/Park--Piraino
holonomy loop over the QNL periodic fibre.  The natural derivative cocycle
fails the Park--Piraino fiber-bunching inequality: the QNL spectral condition
number is `>123.1589`, while the relevant natural bounds are `<4` and `<2`.
The finite shadow point is a different periodic orbit, and a tiny positive
gauge change can make the raw common-coordinate wedge exactly zero.  Thus
the literal common vertex/full-cross and finite shadow twisting are
certified, but the strict common-magnet typicality input remains fail-closed.

Evidence:

- `cm2-gate1-true-graph-krawczyk-assault-2026-07-15.md`;
- `cm2-gate1-full-cross-transport-assault-2026-07-15.md`;
- `cm2-gate1-closed-shadow-twisting-assault-2026-07-15.md`;
- `cm2-gate1-bv-holonomy-typing-obstruction-2026-07-15.md`.

## 2. Gate 2: two actual local branches, not yet a physical quotient

### 2.1 Abstract stopped-parent layer

For the pilot projective maps with arbitrary place/history-dependent weights
in `[1/5,4/5]`, a direct two-copy truncated-Riesz drift gives

```text
alpha = 1/20,
kappa = (17/25) 169^(1/20) < 1,
```

and hence

```text
nu_N(I) <= (1-kappa)^(-1/2)|I|^(1/40) + kappa^(N/2).
```

This avoids TV-Doeblin and iid Markovisation.  A scaled-Hölder relative
bound also closes the normalized-amplitude algebra on a clean component.
These are conditional analytic tools, not yet the actual full-mass kernel.

### 2.2 Common rectangle and first local branch

In the QNL Perron chart, the QNL fixed point is `(0,0)` and the 96-word
fixed point is `(a_*,a_*)`, where

```text
a_*=-8.2937621943296019634...e-15.
```

The common rectangle of half-width `4.2e-15` contains both.  A short
two-collision QNL strip full-crosses it with strict first-hit, stable-entry
and cone margins.  The earlier affine full-height 96-word proposals are
rigorously impossible in both natural Perron charts: their forced mixed
corners have negative discriminants at collisions 70 and 74.  This only
ruled out those affine routes, not the actual curved plaque.

### 2.3 Actual stable-plaque continuation and second branch

The reversible root was refined to a 2400-bit interval-Newton box of radius
`10^-600`.  Starting on the certified local unstable plaque, two complete
96-word collisionwise Taylor replays with a rigorous intermediate rebox give
opposite global-`u` face signs.  Invariance and reversibility therefore
certify the actual `W^s(z_*)` crossing of the common face `v=0` at

```text
u_cross = a_* + 1.007246119561962...e-42,
uncertainty < 8.53e-172.
```

A positive-width source strip around that crossing,

```text
(r_u,r_v)=(8e-68,1e-80),
```

keeps all 96 declared first hits.  Its unstable face images satisfy

```text
negative face < -1.68718e-14,
positive face >  2.84304e-16,
```

and its stable image is

```text
-8.2937621943e-15 +/- 4.74e-38.
```

Uniform forward and inverse cone images are below `4.74e-24` and
`7.42e-28` for cone slope `1e-20`.  The strip is disjoint from the short-QNL
strip.  Therefore **two local physical full-cross branches in the same
common rectangle are certified**.

The new strip has stable half-height only `1e-80`; stable saturation and a
common invariant holonomy have not been certified.  Consequently there are
not yet two onto inverse branches on a quotient interval.  A full-mass
countable return partition, density `rho`, physical reverse weights,
off-diagonal tail energy, stopping antichain, same-carrier endpoints and the
exhaustive amplitude registry all remain open.

Evidence:

- `cm2-gate2-wasserstein-energy-assault-2026-07-15.md`;
- `cm2-gate2-two-branch-common-rectangle-assault-2026-07-15.md`;
- `cm2-gate2-actual-stable-plaque-continuation-2026-07-15.md`.

## 3. Gate 3: mandatory correction and exact endpoint frontier

The global atlas contains 288 signed tangency sheets: 128 active cross-colour
sheets and 160 parameter-inactive same-colour sheets.  Their active raw
endpoint ledger has 36,992 descriptors.  The full owner construction first
examines 82,048 target--target descriptors over all sheets.

The previous endpoint pass omitted the physical forward-time condition
`ell_other>0`.  That omission invalidated the earlier claim of sixteen
physical joint vertices.  The corrected audit proves that all sixteen have
`ell_other<0`; they are algebraic common-line roots behind the source, not
forward billiard events.  The claim is withdrawn.

An independent resolver replays the whole original suspect family:

| strict empty witness | descriptors |
|---|---:|
| third target strictly before the distinguished target | 164 |
| distinguished target behind the source | 60 |
| earlier partner behind the source | 52 |
| common tangent does not meet the outgoing source | 32 |
| later partner is not the next target | 12 |
| **total** | **320** |

Thus

```text
EMPTY=320, physical=0, unresolved=0.
```

The same certificate isolates all twenty legacy transition roots by exact
Sturm counts: sixteen nonphysical third-tangent roots and four nonphysical
`tau=3` roots.  It also records 1,480 true dominant-cell phase boxes and
eight nonphysical chart-seam duplicates.

One independent positive layer survives the correction.  A compact
connected physical subrow on `G:E` and its `Jx/Jy` orbit are certified over
the full parameter window.  The four constant miss traces are
`G[1,1]`, `G[-1,1]`, `G[1,-1]`, `G[-1,-1]`, with polarity pattern
`[+1,-1,+1,-1]`.  This yields scalar cancellation for a
symmetry-invariant constant test only; it is not arbitrary-test DQ.

The global blocker is now structural rather than numerical: quotient every
physical duplicate endpoint, enumerate maximal connected visible components
of all 288 sheets, freeze miss/polarity-constant rows, and assemble global
DQ plus scalar matching.

Evidence:

- `cm2-gate3-owner-voronoi-event-registry-assault-2026-07-15.md`;
- `cm2-gate3-endpoint-descriptor-resolver-assault-2026-07-15.md`;
- `cm2-gate3-component-orbit-dedup-assault-2026-07-15.md`.

## 4. Gate 4 and Gate 5: exact algebra, local charging, actual phase gcd

Gate 4's global raw-sheet schema exactly matches Gate 3's 288-sheet universe.
For every future completed physical row it constructs two nonadditive views
of the same occurrence, a common coarea law `m`, and one charge

```text
q=max(C_fw,C_rev,2)m.
```

One positive-width incidence

```text
G[0,0] -> tangent W[0,0] -> G[1,1]
```

and its symmetry orbit instantiate this schema.  The exact finite
prefix/suffix/Kac algebra combines its two singular coordinates with shared
mark `(+1,-1)`.  The global reflection involution proves scalar roof-mass
cancellation, but an exact two-point current counterexample shows that
scalar cancellation gives neither arbitrary-test cancellation nor a current
norm bound.

The actual phase audit supplies physical weight-one and weight-two cycles.
The height-two `M*,W` support graph and the standard-`N` `G,W` spanning graph
are both strongly connected and have weighted cycle gcd

```text
gcd(1,2)=1.
```

This closes the component-level period obstruction.  It does not give the
complete return-word/operator matrix or invertibility of the operator-valued
`D(z)`.  Moreover, the preferred direct-standard-`N` route does not use a
separate phase tower; the phase result is mainly relevant to the optional
fixed-to-full common-refinement route.

Still missing are the immutable all-row physical ledger, stopped-parent
recovery, return-boundary DQ/source matching, homogeneous distortion and
cut-growth sums, face/test constants, four fully typed singular Kac currents,
operator Wiener aperiodicity for the optional route, and both CM2 norm lifts.

Evidence:

- `cm2-gate4-all-sheet-single-charge-schema-assault-2026-07-15.md`;
- `cm2-gate45-algebraic-typing-frontier-2026-07-15.md`;
- `cm2-gate5-actual-phase-graph-assault-2026-07-15.md`;
- `cm2-gate5-prefix-suffix-norm-assault-2026-07-15.md`.

## 5. Latest-technology audit

The exact Park--Piraino definition was rechecked from arXiv:2007.02349.
The available theorem still requires the canonical homoclinic holonomy
typing and does not promote a raw finite product at another periodic point.
The 2026 parameterisation/radii-polynomial papers provide useful validated
continuation frameworks; their ideas informed the collisionwise Taylor
rebox used in Gate 2, but they do not supply this billiard's physical
quotient or event table.  Canestrari's perturbative source/recovery results
and Demers--Liverani's norm-equivalence result remain templates or regular
layers; none substitutes for the missing moving-scatterer row and singular
source ledgers.

## 6. Shortest remaining route

1. Stable-saturate the two certified local Gate-2 strips in one invariant
   rectangle, then extend to a full-mass countable return partition with
   actual conditional density, weights and tail PPE.
2. Enumerate maximal physical components of all 288 Gate-3 sheets, quotient
   duplicates and freeze a complete event/DQ registry.
3. Instantiate the Gate-4 two-view/common-`m`/single-charge theorem on every
   row using a controlled interval/cylinder stopped algebra or physical PPE.
4. Export the complete return prefix/suffix distortion, cut, coarea and test
   constants into Gate 5; type all four singular currents and certify the two
   CM2 norm intertwiners.
5. Supply a theorem-valid actual-cocycle twisting/holonomy input for Gate 1,
   then independently audit the assembled unconditional theorem.

## 7. Verification boundary

All positive certificates in this assault exit zero.  Fail-closed live
verifiers for Gates 2--5 exit two by design while their required physical
fields remain absent.  New SHA-256 manifests and the frozen v51/v52 manifests
are checked after consolidation.  No CM2 cron was used and no result in this
report promotes a local branch, scalar identity or component graph to an
unconditional CM2 theorem.
