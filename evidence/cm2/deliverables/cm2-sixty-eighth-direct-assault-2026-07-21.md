# CM2 Round 68 full direct assault

Date label: 2026-07-21

Strict verdict: **0/5 complete composite gates; Gate 2 official fields 0/17;
Gate 4 landing join 1/7 with fields 1, 4 and 7 partial; Gate 5 maturity
10/18 with zero complete blocks; CM2 remains `NO-GO_FOR_CLAIM`.**

## Round result

Round 68 pins the complete Round-67 aggregate and performs one integrated,
append-only assault on the common-root join and all four quantitative gate
families.

1. It certifies the exact twelve-field recordwise criterion needed to join the
   two Round-67 physical subroots and gives a four-atom exact separator showing
   that equal marginals do not imply a graph-supported join.  Actual joined
   rows remain `0/12`.
2. It certifies the exact product perturbation ledger, a finite-selection versus
   uniform-convergence separator, and a five-row uniform material/Piola/
   trace/current/`MT_DQ` sufficient contract.  Actual contract rows remain
   `0/5`.
3. It replays the square-summable and harmonic hazard products in exact
   arithmetic and separates bounded per-level variation from finite total
   weighted variation.  Actual same-key full-hazard rows remain zero.
4. It certifies the sectorwise positive-resolvent criterion for Gate 5 and the
   sharp weighted attenuation threshold at charge weight `3/2`.  None of the
   seven missing actual sectors is closed.
5. A same-day arXiv API search finds no moving-billiard response theorem that
   supplies the missing physical crosswalk or moving-boundary estimates.  No
   external theorem is promoted.

## Audit

The three new Python files compile.  The independent verifier passes canonical
audit and byte-identical re-emission, replays 798 hazard identities and 300
resolvent terms, rejects `306/306` hostile semantic mutations, and rejects
`4/4` hostile strict-JSON payloads.  Producer and verifier default entry points
both return exit code `2`, preserving the strict partial-only verdict.

## Shortest remaining route

The next positive move is no longer another abstract coupling.  It is to emit
the twelve composite-key fields from both existing physical subroots, prove
recordwise equality on one half-open return graph, and only then attach the
Gate-1/3 all-cell data, Gate-2/4 hazards/variation, and Gate-5 terminal kernel
to that exact root.
