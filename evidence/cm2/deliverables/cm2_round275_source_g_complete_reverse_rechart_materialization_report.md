# CM2 Round275: complete reverse-rechart materialization

## Certified result

Round275 closes the complete `880`-guard reverse-rechart universe and
materializes `13,788` connected adjacent-chart regions:

- `5,288` strict whole-signature cover regions;
- `8,500` regions from the 56 exact outgoing/X=0/Y=0 arrangements;
- `3,488` regular graph crossing cells;
- `1,524` exact zero-set absence cells;
- remaining fail-closed guards/cells: `0 / 0`.

The result contains `880` closed guard conservation rows.  Every strict region
is re-evaluated on its entire rational outer box.  Every arrangement region is
backed by strict interval derivatives, an exact extremal witness, and a full
10-field return signature.  Outgoing crossings change only
`outgoing_cell/target_chart`; X/Y crossings change only the official exact-key
fields, ordered wall events, roof, and signed wall word.

Certificate result SHA256:
`9c33f2e8799b777cb14694c2917bd4c4cfec1b98a6f168227e1fec96a4495bd6`.

Complete certificate SHA256:
`e18935169614fc8b62ead3be7f60b383396ea1d2c52e8151457241f49e770386`.

## Independent verification

The verifier reconstructs the 880 input guards, adjacent rational enclosures,
deterministic cover, activity equations, strict derivatives, extrema,
signatures, row IDs, and guard conservation without trusting producer census
claims.  It returns `PASS_INDEPENDENT_ROUND275`; all `5/5` mutations are
rejected.  Seeds `275071` and `275929` produce byte-identical certificates.

## Strict non-promotion

Round275 grants zero expanded-occurrence, component-edge, maximality, fibre,
global-disposition, or Jx/Jy same-point glue credit.  The strict baseline
remains quotient `63,224`, expanded occurrences `126,468`, maximality
`0/63,224`, fibres `0/116`, dispositions `0/224,580`, Gate5 `10/18`, D02
blocked, and CM2 `NO-GO_FOR_CLAIM`.

Next: use the now-frozen reverse-rechart region universe to incident-bind the
`152` Round268 true-seam patches, then recompute the DSU.  Jx/Jy remains
forbidden.  Only the resulting final frontier may enter component maximality,
116-fibre exhaustion, and the 224,580 global dispositions.
