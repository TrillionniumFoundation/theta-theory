# CM2 forty-third direct assault

Date: 2026-07-19  
Verdict: **the nonnumerical `N_open` source is now fully audited, and the
Eulerian route gains a same-ID arbitrary-path summable insertion ledger; no
composite gate is yet closed.**

## Round result

| gate | Round-43 advance | exact remaining core gap | status |
|---:|---|---|---|
| 1 | no unsafe transfer from shared work | physical Green/cancellation representative and same-representative twisting/PPE | NOT CERTIFIED |
| 2 | no cross-carrier promotion | invariant stable plaques, physical holonomy, quotient branches and PPE | NOT CERTIFIED |
| 3 | arbitrary-finite-path Duhamel current identity and insertion charge | suffix-propagated two traces, dynamic `MT_DQ`, strong current norm and cemetery | NOT CERTIFIED |
| 4 | exact conditional `N_open` formula and complete effectivity audit | numerical proper-family C24 hit minorization, collision-time `C_fw/C_rev/q`, cemetery | NOT CERTIFIED |
| 5 | rank-one Eulerian generator, physical `L^(3/2)` bound and D1-dominated `L^(6/5)` insertion tail | complete F13 traces, F16, all-face F10, F12 and F14--F18 | NOT CERTIFIED (`7/18`) |

Strict unconditional progress remains `0/5`; CM2 remains
`NO-GO_FOR_CLAIM`.

## 1. Gate 4 route correction

The published `arXiv:2104.06947v3` large-hole proof explicitly leaves the
dependence of its recovery block on `delta` non-explicit.  The exact frozen
conditional schedule is

```text
n_star=max(NF',nbar_delta),
N_return=J n_star,
m_mix=max(0,1+ceil(log(2724 C_mix/epsilon_hit)/(-log vartheta_mix))),
N_open=max(N_return,nbar_delta+m_mix).
```

Nine numerical theorem inputs are absent.  The local numerical clocks
`n_*=9148` and `R(D)<=301500+1005D` control Growth/properness, not fixed-target
hit mass, so neither can be substituted for `N_open`.

The new shortest Gate-4 target is a direct numerical C24 minorization on the
canonical proper-family class.  It would combine with the 9,148-step killed
Growth block and bypass the non-effective projective chain.

## 2. Gate 3/5 current advance

In area coordinates the one-step Eulerian generator obeys

```text
|X|_1<=25*2^B,
integral |X|^(3/2)dmu_s<16777216875/64.
```

The exact Duhamel expansion makes the arbitrary-path insertion charge
additive:

```text
c_X,n=25 sum_i 2^B_i=(25/151)c_D1,n.
```

It therefore inherits Round 35's same-ID physical `L^(6/5)` moment and
exponential block tail.  This removes the false need for an
`integral 2^(3B)dmu` bound at the insertion level.

The result is deliberately not promoted to F13/F16: suffix propagation of
both traces and the all-face normal trace/operator norm are still absent.

## 3. Latest-technology audit

The official arXiv API still reports

```text
2104.06947v3 / 2604.19671v2 / 2606.10155v1.
```

No newer theorem supplies the missing numerical large-hole constants or the
arbitrary-depth physical two-trace/F16 assembly.  No shape-mismatched result
is imported.

## 4. Validation

Both suites pass syntax, strict JSON, pinned dependencies and independent
replay.  They reject

```text
22/22 + 31/31 = 53/53
```

hostile mutations.  All four certificate/verifier default modes exit `2`;
leaf and recursive SHA chains freeze the complete Round-43 output.

## Next shortest line

1. Prove a numerical C24 hit minorization on recovered canonical proper
   families and combine it with the 9,148-step Growth block.
2. Convert that block to collision-time `C_fw/C_rev/q`; close strong cemetery
   independently.
3. Materialize suffix-pushed plus/minus trace IDs and `C1` trace norms for
   every Duhamel insertion on arbitrary `R_n` homogeneous levels.
4. Join the all-five-face F10/F12/F13/F16 ledger, then F14--F18 and the induced
   coefficient; return it to Gate-3 dynamic `MT_DQ`.
5. Continue Gate 1 and Gate 2 as independent structural poles.

Strict unconditional CM2 remains `NO-GO_FOR_CLAIM`, with zero of five
composite gates fully closed and Gate-5 maturity unchanged at `7/18`.
