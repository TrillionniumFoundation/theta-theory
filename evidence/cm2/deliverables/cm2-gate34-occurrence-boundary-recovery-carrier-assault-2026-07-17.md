# CM2 Gates 3/4: occurrence boundary recovery carriers

Date: 2026-07-17 (Asia/Shanghai)  
Frozen inputs: the 64 maximal physical occurrence rows, the global signed
event current, the 24 physical return cores, and the sparse-dwell ledger  
Strict verdict: **every hit/miss pair now has immutable oriented trace IDs, a
shared event-base signature, and one analytic singular-limit recovery carrier
on the regular miss collision.  The hit trace reaches it after one boundary
collision and the miss trace after zero.  Open-shell transport, first 24-core
entrance, no-recut dwell and common strong-space bounds remain uncertified;
Gates 3 and 4 stay open.**

## 1. Exact boundary join

Fix one maximal occurrence row.  Its source ray is tangent to target `T` at
flight time `ell_T`, and the certified later miss owner `M` is hit at
`ell_M>ell_T`.  At exact tangency the normal velocity is zero, so specular
reflection is the identity.  Therefore

```text
hit boundary trace:  source -> T(grazing) -> M,
miss boundary trace: source -------------> M,
```

and both arrive at the same point with the same incoming velocity.  After
the regular reflection at `M`, they define one common outgoing collision
state.  Thus the boundary collision offsets are exactly

```text
hit:  1,
miss: 0.
```

This identity is analytic over each open occurrence row.  It does not claim
that two open neighborhoods on opposite sides of the singularity remain
identical or uniformly close.

## 2. Full immutable registry

The certificate regenerates all 64 maximal rows and assigns

```text
64 shared event-base signatures,
128 oriented hit/miss trace seed IDs,
64 common boundary recovery-carrier IDs,
32 exact Jx-reflection carrier pairs.
```

The physical type histogram is

```text
G -> W(graze) -> G: 36,
G -> W(graze) -> W:  8,
W -> G(graze) -> G:  8,
W -> G(graze) -> W: 12.
```

Both grazing signs occur 32 times.  The two traces of each occurrence carry
the same immutable pulled-back event-base restriction.

## 3. Arb witness margins

At the certified rational witness of every row, 384-bit Arb replay proves

```text
source cos(phi) > 3/20,
source |p|      < 99/100,
ell_M-ell_T     > 31/100,
ell_M           < 8/5,
miss |p|        < 97/100,
miss cos(phi)   > 6/25.
```

It also verifies the tangent reflection identity, equality of the two
continuation points, strict regularity of the common miss collision, and
unit-speed specular reflection.  These are uniform bounds on the 64 selected
witnesses, not uniform margins on every point of every open row.

## 4. What this resolves

The eighteenth audit listed four absent incidence fields.  Their boundary
status is now

```text
recovery carrier ID:                  MATERIALIZED (64 singular-limit graphs)
transport offset:                     MATERIALIZED (hit/miss = 1/0)
shared pulled-back event restriction: MATERIALIZED
first 24-core destination:            NOT CERTIFIED
```

The carrier is physically stronger than a label join: it is the actual
regular collision graph obtained by continuing the tangent orbit to its
certified miss owner.

## 5. Strict nonpromotion

The map through `T` is singular in the open-shell derivative even though its
boundary state continuation is exact.  Consequently none of the following
is promoted:

```text
bounded hit-side pushforward through grazing,
uniform full-row homogeneity margin,
open-shell recovery neighborhoods,
first entrance into one of the 24 contracting cores,
2018/12108 collision steps without hidden recut,
common strong-space restriction or cemetery payload.
```

Hence the abstract sparse-dwell contraction is still not a native physical
recovery theorem.

## 6. Strict boundary

```text
BOUNDARY RECOVERY CARRIERS:                 64 CERTIFIED
ORIENTED HIT/MISS TRACE SEEDS:             128 CERTIFIED
BOUNDARY HIT/MISS COLLISION OFFSETS:       1/0 CERTIFIED
SHARED EVENT-BASE RESTRICTIONS:            64 CERTIFIED
FIRST 24-CORE DESTINATIONS:                0 CERTIFIED
OPEN-SHELL NATIVE RECOVERY:                NOT CERTIFIED
PHYSICAL STRONG DQ / MT_DQ / FACE:         NOT CERTIFIED
GATE 3:                                    NOT CERTIFIED
GATE 4:                                    NOT CERTIFIED
```

The next highest-leverage step is an adaptive two-sided tube around each
common carrier: propagate the hit and miss neighborhoods to their first
regular homogeneous chart, then continue them jointly until a 24-core or a
typed cemetery is reached.
