# CM2 Eighty-Fifth Direct Assault — 2026-07-22

## Scope

Round 85 begins from the Round-84 closure of all registered
different-candidate intersections.  It rebuilds the same-candidate registered
zero-set topology without trusting rectangle connectivity, tests the complete
one-step C24 return atlas for recurrence, installs the largest actual
two-sided material subregistry currently supported at `s=0`, and audits the
same physical keys against the missing Gate-1 and Gate-5 fields.

## Registered elementary arcs and ports

The producer exactly deduplicates all 34,652 rational patch boxes, cancels
internal sides of each rectilinear union, and classifies every exposed segment
with 512-bit Arb.  Both coordinate partial derivatives are strictly nonzero on
all 34,652 boxes.  Therefore the registered zero set is regular and cannot
contain a closed component inside a box union: such a component would have a
coordinate extremum at which the corresponding partial derivative vanished.

The complete census is

```text
frozen Round-82 box-connectivity rows             1672
exact union-boundary segments                    45016
boundary interval tests                          45712
unique transverse ports                           4216
source-boundary ports                             1004
non-source registered-union ports                 3212
registered elementary arcs                        2108
single-arc frozen rows                             1568
multi-arc frozen rows                               104
unresolved boundary segments                          0
```

All 1,004 source ports are in bijection with the frozen Round-82 source
endpoint ledger.  The 104 multi-arc rows contain 540 arcs rather than 104;
hence the correction from 1,672 box aggregates to 2,108 registered elementary
arcs is `540-104=436`.  Every multi-arc row is a single Round-81 centered
Taylor patch and none was produced by the 108 Round-82 cross-tube joins.

An independent 768-bit verifier uses a separate event-sweep boundary
construction, reproduces all counts, and rejects the configured semantic,
ledger-pin, and strict-JSON attacks.  This remains a registered-union census:
the 3,212 non-source ports are not yet paired or promoted to complete physical
faces.

The production outward-cell lookup then classifies all 3,212 non-source ports
under one closed-schema definition.  Every transverse root lies in a unique
physical `64 x 64` parameter atom, all 3,156 required grid-cut signs are
strict, and all projective tangent coordinates use the regular chart
`q=u_y/(1+u_x)`.  The exact frontier is

```text
NO_UNRESOLVED_SEED_REGISTERED                    2696
SECOND_OUTGOING_CHART_OR_GEOMETRY_BLOCKER_ONLY    516
candidate seed rows                                 0
positive transverse factor                       1606
negative transverse factor                       1606
```

This strict replay supersedes the earlier unpersisted diagnostic partition
`2588/428/196`, which used a different outward lookup and is explicitly marked
as unreproduced in the certificate.  Neither the two equal factor-sign counts
nor seed absence pairs the ports or turns them into physical endpoints.

## Gate 1: current-root transfer obstruction

The Round-67 relative transfer equation is

```text
A_c(x)=C(sigma x)^-1 A_q(x) C(x),    C=Q^-1 E D.
```

A same-root row therefore requires both `x in R` and `sigma(x) in R`.  The
Round-84 escape certificate proves, on the 176-atom Round-69 owner root, that

```text
R intersect sigma^-1(R) = empty,
```

with 1,360 destination-owner comparisons and uniform `p`-coordinate gap
greater than `1/100`.  Thus the number of nonempty same-key `Q,E,u,v` transfer
rows installable on this root is zero.  This is a physical-root obstruction,
not an abstract completion argument.  Gate 1 remains unpromoted until the
root is reselected or enlarged.

## Full one-step C24 return incidence

The recurrence test was expanded from the 176 base-owner atoms to every one
of the 4,216 frozen `RETURN_AT_1_INNER` atoms.  Each rational source box was
replayed at 512 bits.  Every landing enclosure lies strictly inside its
declared destination C24 core, but its `p` interval lies strictly outside the
entire hull of all one-step return-source atoms in that core:

```text
return atoms                                      4216
landing below destination return-source hull      2108
landing above destination return-source hull      2108
uniform coordinate gap                          >1/200
landing-to-return-source incidence edges              0
cyclic or self-loop SCCs                              0
recurrent nodes                                       0
```

The 768-bit replay agrees.  Consequently the frozen one-step return atlas has
no nonempty recurrent same-key subroot.  This does not exclude recurrence
through `SURVIVE_THROUGH_1_INNER`, a later return time, or a different C24
registry.  Graph recurrence would in any case be only a prerequisite, not an
invariant stable plaque.

The follow-on crosswalk exhausts all 33,960 frozen full-core leaves:

```text
RETURN_AT_1_INNER leaves                    4216
SURVIVE_THROUGH_1_INNER leaves              2868
UNRESOLVED_OUTER leaves                    26876
RETURN-landing interval-possible edges     10384
  to SURVIVE                               10352
  to UNRESOLVED                               32
  to RETURN                                    0
whole landings contained in one survivor    1888
boundary-straddling landings                 2328
```

All 4,216 landings are covered: 4,208 have only survivor candidates, while
eight also touch an unresolved leaf.  The reached targets comprise 176 unique
survivor atoms and 32 unique unresolved atoms.  The latter remain unresolved
at 512 and 768 bits.  These are conservative interval-possible edges, not
certified nonempty orbit edges.  The frozen atlas contains no later-time or
variable-return key, so it still cannot form a recurrent SCC; the next finite
frontier is the 32-box classifier refinement plus preimage-guided refinement
of the 2,328 boundary-straddling landings.

## Gate 3: finite-root two-sided material windows

The 352 `RETURN_AT_1_INNER` boxes touching `s=0` split exactly into 176
positive-side owner boxes and 176 negative-side boxes on 16 directed physical
core edges.  All 2,504 same-edge positive/negative comparisons have the exact
rational partition

```text
exact boxes                         104
partial positive-area intersections 64
edge-only contacts                  376
corner-only contacts                320
disjoint                           1640
```

Choosing the largest positive-area opposite-side match for each owner row
produces 104 exact and 48 partial matches.  Hence 152/176 owner rows have an
actual two-sided finite-root material window with uniform `L-infinity` radius
`1/6400`.  The remaining 24 positive and 24 negative boxes have no
positive-area opposite-side counterpart on the same directed branch; edge or
corner adjacency supplies radius zero.

This is genuine local material data, but it is not a complete 176-row cover,
an all-cell/all-depth radius, a uniform Piola remainder, or a two-sided strong
trace/current theorem.  Gate 3 remains unpromoted.

## Gate 5: same-key field frontier

The 152 material-window atom keys crosswalk to actual Round-27 empty-face
slots:

```text
F10 coarea-density regular cost = 0 slots          152
F13 current = 0 / trace-pair count = 0 slots       152
F14 same-key slots                                   0
F15 same-key slots                                   0
F16 same-key slots                                   0
F17 same-key slots                                   0
F18 same-key slots                                   0
```

The Round-73 `4/125000000` F16 bound is a base-fibre aggregate with no
`atom_id` or candidate-packet key and is not attached.  The Round-67 terminal
exponential moment remains uncertified.  Thus these 152 candidate packets
remain locally `13/18`, no complete block is created, and global Gate 5 stays
`10/18` with zero complete blocks.

The first attempted F14 crosswalk is also strictly excluded.  The older
Round-67 affine collision-SRB formula root has

```text
s in [-3/1600,-1/640],
```

whereas every new material window has

```text
s in [-1/6400,1/6400].
```

All 152 comparisons are disjoint with uniform parameter gap `9/6400`, and
the older formula artifact exports no atom or candidate-packet keys.  Its
`q,a,Z,rho_v,lambda,J` rows therefore cannot be relabelled as F14 data on the
new root.  A valid F14 construction must derive those quantities directly on
the 152 current keys and still provide the recovered strong operator cost.

That direct numerical construction succeeds on all 152 keys.  With local
coordinates `u=t`, `v=p` and

```text
Phi(u,v)=(R asin(u),v),
q=a=R/sqrt(1-u^2),
Z=R(asin(t1)-asin(t0)),
rho_v=1/Z,
lambda=J=1,
```

all 304 positive/negative half-slab replays have real collision geometry,
inherit a strict first owner, and land strictly inside the declared
destination core.  The uniform certified bounds are

```text
1-t^2 >= 51/100
4/25 <= q=a < 13/25
1/20000 <= Z < 1/6000
6000 < rho_v <= 20000
lambda=J=1
```

Thus the same-key numerical collision-density inputs for F14 are complete on
152/152 windows.  Official F14 still requires the fibres to be typed as
physical standard/stable families and a recovered strong Banach recipient;
those operator rows are not inferred from finite scalar bounds.

The follow-on typed construction first proves that the tempting `p=constant`
fibres are invalid: their slope `d phi/d r=0` is strictly outside the frozen
unstable cone

```text
25/9 < d phi/d r < 4108425/145348 < 29.
```

On every one of the 152 current keys, the central cone-aligned curve

```text
phi(r)=4(r-r_c)+phi_c
```

is instead a genuine finite-root standard pair.  It has slope 4, zero second
derivative, strictly monotone `p`, endpoint margin greater than `3/10000`, and
length between `1/4000` and `7/10000`.  Owner and homogeneity data are inherited
strictly from the material window.  Along this pair,

```text
rho=cos(phi)/(Z sqrt(17)),    Z=(p_right-p_left)/4,
1400 < rho < 5010,
Lip(log rho) < 1/40,
TV(rho) < 1/15,
BV probability norm < 16/15.
```

This gives a minimal local recipient
`X_central = direct-sum_{i=1}^{152} BV(W_i)` and synthesis cost below `16/15`.
It still does not close full-packet F14.  The parallel label
`v=phi-4r` produces fibres of arbitrarily small length near rectangle corners;
without a transverse weight and short-fibre recovery, the normalized density
has no uniform upper bound.  F14 therefore remains uninstalled and the local
maturity stays `13/18`.

## Strict state

```text
Gate 1                         NOT_CERTIFIED
Gate 2                         0/17
Gate 3                         NOT_CERTIFIED
Gate 4                         1/7
Gate 5                         10/18, complete blocks 0
complete composite gates       0/5
CM2                             NO-GO_FOR_CLAIM
```

## Next shortest route

1. Continue the 2,696 no-seed and 516 typed-blocker ports through chart, owner,
   translation, radicand, flight-order, and genuine rank-transition events.
   Do not install `3212/2` as a gap count.
2. Extend the C24 root through survivor atoms and variable return times; the
   frozen one-step return atlas has no recurrent node.  Align the next finite
   partition with landing pullback boundaries rather than blindly increasing
   dyadic depth.
3. Refine or repartition the 24 unmatched material seams and add a transverse
   packet measure plus short-fibre recovery to the 152 typed central standard
   pairs before claiming F14.
4. Only after all non-source ports and typed endpoints close, rebuild the
   physical face quotient and generate rank-three numeric/RN rows.

No external theorem is promoted in this round.
