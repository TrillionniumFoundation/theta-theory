# CM2 eleventh full direct assault

Date: 2026-07-16 (Asia/Shanghai)  
Frozen baseline: v51/v52 and the first ten assault rounds were preserved  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; composite
gates fully closed remain `0/5`**

## 1. Executive result

This pass produces strict new layers on every gate without promoting any
missing interface.

- Gate 1's exact `325/144` QNL resonance is no longer an absolute local
  Hoelder-cohomology obstruction: an explicit two-axis logarithmic gauge
  restores both selected local canonical tails.  A global faithful coding,
  all-pairs holonomies, class `H` and physical projective conclusion are still
  absent.
- Gate 2 now has a full-measure two-dimensional first-return path-key schema,
  but exact invertibility shows that every deterministic key refinement keeps
  the physical reverse conditional Dirac and cannot create PPE contraction.
- Gate 3 now has one common finite-parameter mass-coordinate atlas for every
  finite stopped-depth cutoff and a full-measure countable physical
  depth-two atlas at `s=0`.  Finite-`s` future-singularity continuation and
  strong/operator DQ remain open.
- Gate 4 now has a genuinely native full-mass prefix antichain, an exact
  inverse-mass normalization divergence theorem, and the first numerical
  adapted-metric/cut-sum leaves.  Global Growth/recovery constants and
  hereditary repeated-cut recovery remain open.
- Gate 5 now has an immutable 441,280-key return-word envelope and all
  3,286,976 symbolic level factorizations.  The physical homogeneity table,
  18 quantitative operator fields, three strong norm lifts, full Kac output
  and operator phase remain open.

## 2. Gate table

| gate | strongest newly certified layer | exact remaining hard gap | strict status |
|---|---|---|---|
| 1. holonomy/QNL | local stable and unstable QNL tails converge after one explicit `C^{1,alpha}`, `alpha<1`, logarithmic gauge | global faithful coding, compatible gauge on overlaps, all-pairs Hoelder holonomies, typed QNL loop and projective spectral/PPE conclusion | **OPEN** |
| 2. physical PPE | full-measure countable 2D first-return path-key schema; exact Dirac no-promotion theorem | stable-saturated product base, quotient `rho`, reverse branch kernel, same-carrier endpoints, native stopping and PPE | **OPEN** |
| 3. global event/DQ | all finite moving dyadic cutoffs on one common mass atlas; full-measure countable `s=0` physical depth-two atlas | finite-`s` future singular atlas, uniform dynamic boundary control, strong-source invariance, branch-record `MT_DQ`, `FACE_2CUT/FACE_TIME` | **OPEN** |
| 4. same-occurrence `q` | native full-mass prefix antichain; exact normalization no-go; numerical adapted expansion and single-true-branch cut sum | global weighted true-singularity sum, distortion and Growth constants; finite charged native/repeated recovery and propagated `C_fw,C_rev,q` | **OPEN** |
| 5. fixed/full transfer | 441,280 candidate return keys and 3,286,976 symbolic prefix/suffix level pairs | exact nonempty homogeneous blocks, 18 operator fields, three CM2 lifts, full Kac and operator phase | **OPEN** |

## 3. Gate 1: the local resonant obstruction is repaired

The exact QNL cubic replay gives

```text
F_1,xxy/lambda=325/72,
F_2,xyy/mu=-325/72,
lambda*mu=1.
```

For the stable critical truncation

```text
M_s(y)=diag(lambda,mu)+g_s y^2 E_21,
g_s/mu=-325/144,
```

set

```text
t(y)=k y^2 log|y|,
k=-325/(144 log(mu)),
B_s(y)=I+t(y)E_21.
```

The exact identity

```text
lambda t(mu y)-mu t(y)=g_s y^2
```

makes the transformed quadratic truncation constant.  The reversible
unstable coefficient `g_u/lambda=325/144` is removed by
`B_u(x)=I+k x^2 log|x|E_12`, with the same `k`.  The product `B_uB_s` has
determinant one.  It is `C^1` and `C^{1,alpha}` for every `alpha<1`, but not
`C^2`.

The dependency-reinforced replay directly binds the frozen two-sided stable
asymptotic

```text
y_n/mu^n -> c(z) != 0.
```

After cancellation, the conjugated increment envelopes are

```text
critical 21: O(n mu^n),
diagonal:    O(n mu^(2n)),
decaying 12: O(mu^(4n)),
```

and are absolutely summable.  The symmetric inverse-branch calculation gives
the unstable tail.  The old natural-gauge divergence remains valid; the new
certificate changes the boundary by proving that it can be repaired locally
with a controlled logarithmic regularity loss.

No finite global Markov coding or common chart-overlap gauge was constructed,
and no all-pairs canonical holonomy family or Butler--Park class-`H` cocycle
was certified.  Periodic eigenvalue ratios also keep the two frozen natural
Park--Piraino codings outside fiber bunching.

## 4. Gates 2 and 5: full Borel labels are not a physical quotient

The exact Gate-5 crossing grammar has 985 monotone transparent-wall patterns.
Combining them with 448 retained source-chart/target-lift pairs gives

```text
448*985 = 441280 candidate return-word keys.
```

The key digest is

```text
841cb96798c9bd41e1440c8b2cdd93af5d80f2f64d693f2aa00175a440045ab9.
```

All roof levels are registered symbolically:

```text
sum_w r(w)     = 3286976 prefix/suffix level pairs,
sum_w (r(w)+1) = 3728256 boundary split positions.
```

Empty key fibres are retained.  Thus this is a full collision-SRB regular
key envelope modulo the usual singular set, not an exact nonempty
homogeneous operator registry.  All 18 required physical fields, beginning
with domain nonemptiness and the homogeneity partition, remain absent.

On the positive-SRB rectangle `S_A`, Poincare recurrence and the per-collision
key envelope give a countable full-measure first-return path schema.  At
depth `n` its candidate universe has `441280^n` words and grammar digest

```text
fa64b3d66dfe1b9416419396176f41c796c3da7bd4d32d173c04cb24ba42de6c.
```

This does not create reverse randomness.  For an invertible first-return map
`F` and any deterministic finite or countable graph label `c`, the reverse
conditional is exactly

```text
K_rev(y,.)=delta_(F^-1 y,c(F^-1 y)).
```

Two conditional copies therefore have diagonal Riesz coefficient one, so no
uniform pair-energy contraction with `kappa<1` follows.  A 97-state exact
permutation replay and a full-mass 441,280-label identity model guard this
no-promotion boundary.

The 2D Borel path mass is one modulo zero.  Stable-quotient branch mass and
endpoint-PPE mass are **undefined** before the quotient/endpoint registry
exists; they are neither zero nor one.  The two raw local strips still cover
less than `9.7%`, leaving more than `90.3%` of the candidate physical
rectangle unregistered for the stable quotient.  All 17 Gate-2 physical
completion fields remain false.

## 5. Gate 3: common stopped atlas and genuine depth two

For `r=tau*s`, the cumulative row-mass coordinate puts every finite stopped
cutoff on the fixed carrier

```text
disjoint_union_e ([0,1]_tau x [0,1]_u),
dm_(e,r)=M_e(r)du,
u_endpoint=j/2^K.
```

At depth two this gives 256 fixed components.  At every finite cutoff `L`
there are `64*2^L` components, and the exact graph-current collar is

```text
|J_s|([S_L]_rho)
 <= min(16128/5,(32256/5)(2^L+1)rho).
```

This closes the moving stopped-endpoint/common-depth-one-carrier blocker.  It
does not register preimages of future grazing or owner singularities.

Separately, a 384-bit physical `s=0` replay carries all 64 miss traces through
one more specular collision.  The explicit depth-10 outer registry contains

```text
185068 certified immutable box components,
128399/131072 = 97.96066284179688% normalized box coverage,
42768 unresolved outer-cover boxes.
```

The outer-cover complement is not physical singular mass.  All 4,704
occurrence/candidate next-tangency discriminants have a strict nonzero
dyadic witness and hence are non-identically-zero real-analytic functions.
Their zero sets are discrete on each open row, finite on compact trims and
at most countable overall.  Because distinct periodic disks are disjoint,
strict positive roots cannot exchange ownership without such a tangency.
Therefore the actual `s=0` second singular set has row length and corrected
coarea mass zero, and its complement is a full-measure countable
component-indexed depth-two atlas.

This is still weaker than the finite common v52 atlas: endpoint accumulation,
finite-`s` root continuation, uniform dynamic boundary-`Z`, componentwise
strong-source invariance, operator-norm DQ and dynamic tests remain open.

## 6. Gate 4: native stopping obstruction and numerical Growth leaves

The physical cumulative-mass coordinate now carries a genuinely native,
query-independent full-mass prefix antichain.  After `K` leading `00` bit
pairs, a nonzero pair `q` and `K` payload bits give

```text
S_K=[4^(-(K+1)),4^-K),
m(S_K)=(3/4)4^-K,
D_K=3K+2,
number of leaves at level K=3*2^K.
```

Both orientations use the same physical occurrence and restriction.  This
replaces the auxiliary product record algebra, but its charged normalization
moment diverges:

```text
m(S_K)2^D_K=3*2^K,
sum_(K=0)^N m(S_K)2^D_K=3(2^(N+1)-1).
```

More generally, any countably infinite positive-mass native leaf partition
with leaf cost `p_a^-1` satisfies

```text
E[p_A^-1]=sum_a p_a p_a^-1=sum_a 1=infinity.
```

One open indicator can already have infinite normalized boundary `Z`, and
repeated first/last-quarter cuts give `Z_h=4^h`.  Only fixed-`H`, explicitly
registered cuts separated by recovery have a conditional restart theorem.

The numerical fixed-table leaves nevertheless advance substantially.  The
adapted billiard metric gives

```text
Lambda > 180337/144000,
inverse contraction < 144000/180337,
C_metric=141/4, C_cone=30,
c_hat=20/3807, L_0<68,
C_s=1296803367/80000.
```

For one true continuity branch, the two homogeneity tails and central piece
obey, with `k0=41`,

```text
Xi_1^(one true branch)
 <144000/180337+1/5
 =900337/901685
 =1-1348/901685 <1.
```

This is not the global Growth coefficient.  The complete weighted
true-singularity multiplicity, an executable `delta_n`/complexity recurrence,
all-branch `D2T_s` and log-Jacobian distortion constants remain missing.
Consequently numerical `C_gr,vartheta_gr,C_p,vartheta_p,A0,A1` and the
propagated `C_fw,C_rev,q` are still not certified.

## 7. Latest theorem audit

The official arXiv audit through 2026-07-16 found no theorem that supplies the
remaining interfaces.

- Butler--Park `arXiv:1909.11548v2` accepts non-fiber-bunched cocycles only
  after global canonical holonomies and their Hoelder control are proved; the
  new logarithmic gauge closes only the local QNL tails.
- Demers--Liverani `arXiv:2606.10155v1` does not provide a general
  characteristic-function-stable wordwise recovery theorem; its stated
  frontier matches the repeated-cut obstruction.
- Canestrari `arXiv:2604.19671v2` supplies the adapted metric and qualitative
  Growth framework used here, but imports the still-nonnumeric global
  singularity, small-curve and distortion leaves.
- Climenhaga--Day `arXiv:2604.25881v1` and Lima--Obata--Poletti
  `arXiv:2405.04676v2` give symbolic/coding information, not the declared
  collision-SRB one-state quotient and its endpoint/PPE kernel.
- Goverse--Kleptsyn `arXiv:2607.11421` gives an atomic physical stationary
  measure in a locally invertible random setting, warning that a large
  alphabet or noninvertibility alone does not yield the needed regularity.

## 8. Exact next target

1. extend the root-isolated `s=0` depth-two atlas to a finite-`s` common atlas
   with uniform dynamic boundary collars, then prove componentwise strong
   invariance and branch-record operator DQ;
2. compute the full true-singularity weighted expansion sum and all-branch
   distortion ledger, then extract numerical Growth/recovery constants;
3. replace leafwise inverse-mass native normalization by a proved
   unnormalized/reweighted-family recovery theorem, or a finite antichain
   with quantitatively charged cemetery;
4. build the stable-saturated one-state quotient and bind `rho,h_a,p_a`,
   matrices, endpoints, stopping and amplitudes to one branch registry;
5. decide nonempty homogeneous return-word blocks and populate the 18
   operator fields, then close the three CM2 lifts, `FACE_TIME`, Kac and
   operator phase.

## 9. Validation status

The six new certificate stacks for Gates 1, 2, 3, Gate-4 native stopping,
Gate-4 numerical leaves and Gate 5 compile and replay from frozen inputs.
Their integrity and mutation tests reject respectively 9, 9, 9, 9, 8 and 9
mutation classes.  Every live default remains fail-closed with exit `2`.
All JSON manifests and SHA sidecars bind their direct dependencies; the Gate-1
stack additionally binds the bottom exact QNL certificate directly.  Frozen
v52 is unchanged and its SHA chain remains valid.

Detailed reports:

- `cm2-gate1-resonant-log-gauge-frontier-assault-2026-07-16.md`;
- `cm2-gate2-collision-key-stable-quotient-frontier-assault-2026-07-16.md`;
- `cm2-gate3-iterated-common-atlas-mt-dq-assault-2026-07-16.md`;
- `cm2-gate4-native-stopping-repeated-recovery-assault-2026-07-16.md`;
- `cm2-gate4-numeric-growth-leaves-assault-2026-07-16.md`;
- `cm2-gate5-return-word-three-norm-frontier-assault-2026-07-16.md`.
