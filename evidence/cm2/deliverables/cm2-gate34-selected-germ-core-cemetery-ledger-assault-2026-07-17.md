# CM2 Gates 3/4: selected-germ core/cemetery stopping ledger

Date: 2026-07-17 (Asia/Shanghai)  
Frozen inputs: 64 selected actual-parameter germs, 128 oriented branches,
the frozen 24-core, and the exact obstruction to global core absorption  
Strict verdict: **all 128 branches now have immutable, total, typed Borel
first-core/singular/nonreturning stopping ledgers.  The ledger is a measurable
partition theorem, not a positive core-hit fraction or quantitative cemetery
tail, so Gate 4 remains open.**

## 1. Total measurable stopping interface

Adjoin an absorbing singular symbol `dagger` to the compactified collision
section and extend the billiard map by sending its singular set to `dagger`.
The extended map is Borel.  For every oriented branch define

```text
tau_C = first entrance time into the finite union of 24 compact cores,
singular cemetery = first entrance into dagger before a core,
nonreturning cemetery = tau_C=infinity without an earlier dagger hit.
```

Finite-time core-hit and singular sets are Borel; the nonreturning class is a
countable intersection of Borel sets.  The three classes are pairwise
disjoint and exhaustive.  Every finite germ measure therefore has an exact
core-hit/singular/nonreturning decomposition.

The immutable registry contains

```text
64 selected germs,
64 hit branches,
64 miss branches,
128 oriented stopping ledgers,
24 admissible destination-core IDs.
```

The ledger IDs type the stopping problem.  They do not claim that a branch
has a finite first-core value.

## 2. Horizon-eight schema

The materialized finite prefix contains

```text
27648 potential core-hit cylinder slots,
1152 potential singular-cemetery slots,
128 unresolved tail slots,
28928 total slots.
```

Slots may be empty.  The prefix certifies a partition schema, not nonempty
physical cylinders or a tail bound.

## 3. Conditional core attachment

If a branch is later proved to hit a core and its native no-hidden-recut
itinerary is audited, the frozen abstract core packet may be attached:

```text
shell dwell:       2018 steps,
raw field-7 dwell: 12108 steps,
b_core^2018 < 3/8.
```

This conditional typing does not install the common strong recovery operator.

## 4. Strict frontier

```text
TYPED BOREL STOPPING LEDGERS:                     128 CERTIFIED
CORE/SINGULAR/NONRETURNING DECOMPOSITION:         CERTIFIED
HORIZON-8 PARTITION SLOT SCHEMA:                  28928 CERTIFIED

POINTWISE FIRST-CORE DESTINATIONS ON ALL BRANCHES: NOT CERTIFIED
SELECTED-GERM POSITIVE CORE-HIT FRACTION:          NOT CERTIFIED
QUANTITATIVE CEMETERY PAYLOAD:                     NOT CERTIFIED
NATIVE 2018/12108 NO-RECUT DWELL:                  NOT CERTIFIED
COMMON STRONG-SPACE RECOVERY OPERATOR:              NOT CERTIFIED
GATE 3 / GATE 4:                                   NOT CERTIFIED
```

The fail-closed verifier rejects `26/26` hostile mutations.  Replay and
integrity pass; live mode exits `2`.
