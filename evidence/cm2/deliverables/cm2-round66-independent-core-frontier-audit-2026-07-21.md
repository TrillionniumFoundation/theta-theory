# CM2 Round 66 independent core-frontier audit

Date label: 2026-07-21

Audit verdict: **PASS.**  The four frozen Round-66 leaves replay on their
declared types and are mutually consistent.  The new exact interfaces make
the missing common physical object sharper, but do not materialize it.  No
composite gate closes and CM2 remains `NO-GO_FOR_CLAIM`.

## 1. Frozen inputs and executable acceptance

This audit is append-only.  It reads the frozen Gate-1/3, Gate-2/4, Gate-5
and immutable-registry leaves, authors none of them, and modifies no previous
artifact.  It directly pins the Round-65 aggregate/audit, every Round-66 leaf
report/manifest/SHA ledger, and the shared Round-66 helper.

```text
new source Python syntax including Round-66 common:   9/9
dependency pins across the four source leaves:      134/134
source-leaf SHA rows:                                 19/19
audit/replay/self-test/reemit source bundles:          4/4
hostile semantic mutations:                         980/980 rejected
hostile strict-JSON payloads:                        16/16 rejected
default source cert/verifier entry points:             8/8 exit 2
```

The independent audit adds its own canonical regeneration, strict JSON,
hostile semantic mutations, independent formula replay, SHA ledger and
fail-closed default entry point.

## 2. Gate 1/3 audit

The exact relative shear between the selected-QNL and combined-Green gauges
is

```text
C=G_q^-1 G_c=L_-u_q U_(v_c-v_q)L_u_c.
```

It is the identity exactly when `v_c-v_q=0` and `u_c=u_q`.  The nontrivial
rational replay gives `[[4/3,1/2],[0,3/4]]`, determinant one.  Thus matching
triangular syntax is not a same-representative crosswalk.

On a genuinely common registry the finite tail identity yields the exact
criterion: the transformed cocycle has all-plaque class-H holonomies exactly
when the forward and backward defects have uniform Holder limits.  Those
limits need not be zero.  The selected loop changes by

```text
H_A^s L_u + L_s H_A^u + L_s L_u.
```

The nearby replay has correction `[[0,2],[-3/2,-1/2]]`.  None of the eight
required actual common-registry rows is frozen, so these exact criteria do
not close Gate 1.

For Gate 3, the positive-width 96-collision word gives only a local compact
interior material atlas.  It does not give one all-depth common `s` window.
For a direct sum, the normalized Piola remainder is the branch supremum;
each fixed branch may converge while the supremum remains one.  The audit
replays all six rows.  The stopped derivative ledger is governed by
`L P'(M)`: it is finite for `M<1`, while the frozen law
`p_n=1/[n(n+1)]` diverges at `M=1`.  The physical strong trace, uniform
anisotropic remainder and `MT_DQ` remain absent.

## 3. Gate 2/4 audit

In an actual product chart the collision-SRB density, arclength derivative
and stable-holonomy RN derivative obey

```text
rho_s=q/(Z(s)a),
lambda_st=a(u,t)/a(u,s),
J_st(hx) rho_t(hx)=rho_s(x)/lambda_st(x).
```

The rational replay gives `rho_s=1`, `rho_t=2/3`,
`lambda_st=3/2`, `J_st=1`.  This is an exact conditional formula, not an
actual all-depth chart.

Equal owner and plaque marginals do not determine immutable-key support:
the aligned coupling has compatible mass one, while the switched coupling
has compatible mass zero.  Likewise, perfect product geometry and equal
landing mass admit both a zero-marker completion and a varying-marker
completion with

```text
P_eta=5/64,  delta_eta=1/8.
```

The 96 frozen collision shadows contain no full graph-transform
first-failure row, so their sharp actual all-depth survivor lower bound is
zero.  A future same-root bound
`eta(F_(96+k))<=A sigma^k` would give a positive lower bound; the replay
`A=1/4`, `sigma=1/2` yields `3/4`.

Zero marker defect still does not control transverse variation: the five
smooth same-marker replays have variation `1,2,17,257,4096`.  A fixed
material BV multiplier bridge is valid under its three weighted bounds, but
the physical Piola coefficients, uniform remainder and anisotropic
recipient are not frozen.  The actual product-tree interface stays `0/7`.

## 4. Gate 5 audit

The immutable terminal-record crosswalk correctly retains time and separates
overlap, terminal departure, owner-label switch and owner birth.  The
nested eligibility sets do not imply nested owner sets, and cross-time owner
deduplication is illegal.

On the full terminal-source-owner label, a positive birth is singular for
the live RN coefficient.  In the six-atom replay the source, overlap, birth
and next totals are `21,14,7,21`; birth-free full-label coefficient is one,
while including birth gives infinity.  Therefore this overlap identity
cannot produce a coefficient below `w_Z^-1`.

The seven typed sectors have birth-free coefficients

```text
1/2, 2, 2, 1, 2, 1, 1,
```

and positive target birth makes every corresponding full-label coefficient
infinite.  Same-slice `raw <= Orlicz < C raw` creates neither a cross-time
drift coefficient nor uniform truncation; the twelve raw marks grow from
`2` to `4096`.  Switch departure is a one-shot arrival, absorbing occupancy
diverges for `w_Z>1`, and pre-regularization cemetery remains unmaterialized.

For the oriented positive bridge, the two displayed strict upper bounds are
not actual totals and their inequality proves no forward/reverse mass
inequality.  The physical carrier, positive source mass, common mode and two
RN rows remain open.  The eight unpaid fields remain exactly
`F5,F6,F10,F11,F14,F15,F17,F18`; maturity stays `10/18`, blocks zero.

## 5. Immutable-registry audit

For a finite standard-Borel junction tree with the running-intersection
property, exact separator-marginal agreement is necessary and sufficient
for an abstract global coupling.  Rooting the tree and attaching regular
conditionals constructs one.  The valid replay on cliques `AB,BC,BD` has
global atoms `0001` and `1110`.

The cyclic parity completion is the sharp guard.  The pair laws force
`A=B`, `B=C`, `A!=C`; all singleton marginals are uniform, but none of the
eight global atoms survives.  Removing any one constraint restores exactly
two atoms.  Pairwise overlap matching on a cycle therefore does not create a
global registry.

Even an abstract global coupling is not yet the physical CM2 root.  The join
must be graph-supported on pinned immutable IDs or pinned Borel crosswalks
for path/time/owner/branch, stable plaques, material faces, live arrivals and
oriented sector pairs.  That actual graph-supported root is not frozen.

Finally, exact registry gluing is independent of strong assembly.  With
`nu_n=3*4^-n`, every fragment can use the identical immutable key `n`; the
global join is exact and the actual moment is three, but `H(n)=2^n` is
unbounded.  The two sufficient lines remain: construct the graph-supported
registry and prove same-root pointwise bounds whose weighted constants are
summable.  Neither actual line is certified.

## 6. Technology boundary

The audited searches remain type-safe.  Recent sequential dispersing-
billiard CLTs do not supply parameter derivatives, Piola remainders or the
marked product tree; smooth Anosov response and closed-manifold local-product
results omit collision discontinuities; killed-chain results assume a
different process and hypotheses.  No external theorem is promoted.

## 7. Strict audited state

```text
Gate 1: NOT_CERTIFIED
Gate 2: NOT_CERTIFIED; official fields 0/17
Gate 3: NOT_CERTIFIED
Gate 4: NOT_CERTIFIED; landing join 1/7, fields 1/4/7 partial
Gate 5: NOT_CERTIFIED; maturity 10/18, complete blocks 0
complete composite gates: 0/5
CM2: NO-GO_FOR_CLAIM
```

The shortest remaining route has two independent lines on the same physical
root: first materialize the graph-supported immutable junction registry;
then prove all-plaque/all-depth geometry together with the uniform strong
trace, Piola, sector-drift and positive-potential bounds.  Neither a finite
prefix, compatible marginals, finite average moment nor local smooth atlas
can replace those lines.
