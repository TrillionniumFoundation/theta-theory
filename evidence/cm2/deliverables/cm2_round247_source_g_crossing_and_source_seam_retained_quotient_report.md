# CM2 Round247 — Crossing-Time and Source-Seam Retained Quotient

## Result

- Attached all `240` crossing-time whole-signature roots and all `264` source-chart-seam roots.
- Every crossing-time root uses its full retained box as a strict positive-volume corridor.
- Every source-seam root uses an exact physical-side corridor satisfying `2t²−1<0`; `232` close at dyadic depth `1` and `32` at depth `2`.
- Coordinate-guard volume and source-chart seam volume receive no physical bulk credit.
- All `504` roots have an exact positive-area `t`-face contact with the resolved sibling carrying the same ten-field signature.

## Quotient Delta

- New virtual retained strata: `504`.
- New positive-area physical edges: `504`.
- Newly touched Round244 components: `304`; overlap with the Round246 touched set: `0`.
- Cumulative touched components: `4,280 / 8,148`.
- Cumulative virtual strata and physical edges: `6,388` each.
- New occurrence-to-known-block incidences: `0`; occurrence incidence remains `36,200 / 53,968`.
- Remaining retained-interface frontier: exactly `2,640` wall finite-key roots.

## Verification

- Independent verifier status: `PASS_INDEPENDENT_ROUND247`.
- The verifier does not import or execute the producer.
- It independently reconstructs every physical-side corridor, exact face, node, edge, cumulative component row, and unchanged frontier commitment.
- Hash seeds `247071` and `247929` reproduce byte-identical producer and verifier outputs.

## Strict Boundary

- Virtual retained-stratum incidence is not occurrence incidence.
- Mixed components are known-connectivity lower bounds, not maximal physical components.
- Maximal physical components remain `0 / 53,968`; exhausted exact-key fibres remain `0 / 116`.
- Gate5 remains `10 / 18`; CM2 remains `NO-GO_FOR_CLAIM`.

## Required Next

Materialize the final `2,640` wall finite-key retained partitions, including every positive-volume endpoint/count-transition branch and each half-open wall sheet, then rebuild the mixed quotient.
