# CM2 fifteenth full direct assault

Date: 2026-07-16 (Asia/Shanghai)  
Frozen baseline: v51/v52 and the first fourteen direct-assault rounds were
preserved; only new fifteenth-round artifacts were added or audit-corrected  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; composite
gates fully closed remain `0/5`**

## 1. Executive result

This pass converts the Gate-1 class-`H`/twisting separation into a concrete
same-gauge obstruction, removes both the endpoint-source and critical-
resultant positive-width classes from Gate 3, materializes a positive seeded
maximal-word registry with a global cross-chart characteristic theorem, and
adds both weak-Borel and core-local strong payloads to the Gate-4 frontier.
None of these advances supplies the missing full-mass quotient, complete
physical current, or full-key operator ledger.

- **Gate 1.**  The exact non-fiber-bunched cohomology-defect identity is now
  frozen.  A 5000-bit full-word mixed-jet replay at the 96-collision periodic
  shadow gives a stable-direction lower-left coefficient

  ```text
  -3.894455937406274188...e-13 +/- 2.91e-94,
  ```

  which strictly excludes zero.  Hence the existing compact twisting gauge
  has a divergent stable canonical tail on any clean horseshoe that contains
  this shadow and a nontrivial local-stable tail accumulating on it.  The
  isolated periodic orbit is outside that negative statement, membership in
  the predecessor's particular existential horseshoe is not certified, and
  a third gauge remains possible.
- **Gates 2/5.**  Each of the 24 positive keys now has an immutable seeded
  implicit maximal component and a frozen 394/402-predicate boundary
  over-ledger.  A separate physical cross-chart audit orders all twelve core
  normal arcs on each source obstacle and proves global source-`r` gap
  `>1/625>delta_1`.  Thus one short curve meets at most one rational core,
  with at most two artificial endpoints, and the unnormalised characteristic
  multiplier is at most `2000/1999`.  Maximal-boundary root order, full-key
  characteristic `Z`, stable quotient/PPE and the remaining 17 full-key
  fields are still absent; complete 18-field blocks remain zero.
- **Gate 3.**  All 2,048 deep endpoint-source boxes are proved contained in
  their canonical scaled-`c_p` cells and become immutable after replaying the
  downstream miss/root/owner path.  The twelve coarse critical boxes contain
  444 conservative candidate tasks; 3,196 selective interval calls split
  them into 932 strict-`Delta` exclusions and 888 strict-`Delta_t` graph
  leaves, with zero unresolved leaves and no fold.  The only positive-width
  terminal class now consists of 151,500 interval-geometry boxes, with
  fixed-`s` outer width `595/1024`.  The 888 new graphs are still untyped, so
  the complete current, DQ/`MT_DQ` and FACE layers do not follow.
- **Gate 4.**  The same-occurrence product record now carries a certified
  weak `M_b`--`B_b` source/test payload.  Its height-nine two-coordinate Kac
  coefficient is 36 and is already dominated by `C_mesh`.  On the 24 inner
  cores, the standard-family strong source multiplier is at most `2000/1999`
  and the pre-step open Growth coefficient is

  ```text
  b_core = 720269600000/720626832337 < 1.
  ```

  The half-life block is 2018 and explicit open-family constants are
  numerical.  But no frozen map joins the 24 return-core IDs to the 64
  singular occurrence IDs, and corner clips make the normalized shape-cost
  supremum infinite.  The existing `K`-only recovery clock therefore cannot
  be reused; complete `C_fw,C_rev,q` remain open.

## 2. Gate table

| gate | strongest newly certified layer | exact remaining hard gap | status |
|---|---|---|---|
| 1. holonomy/QNL | exact cohomology defect; compact-gauge shadow tail divergence on a nontrivial shadow horseshoe | a third faithful representative with both all-plaque class `H` and twisting; full-mass coding/PPE | **OPEN** |
| 2. physical PPE | 24 positive seeded implicit maximal components; core-local endpoint/characteristic packets | stable-saturated base, quotient `rho`, reverse weights/kernel, same-carrier endpoint maps and PPE | **OPEN** |
| 3. global event/DQ | all source descendants immutable; all twelve critical boxes resolved; width `595/1024` | 151,500 interval boxes, 1,052 untyped graphs, complete current, strong DQ, `MT_DQ`, `FACE_2CUT/FACE_TIME` | **OPEN** |
| 4. same-occurrence `q` | weak-Borel product payload; 24-core strong multiplier/open Growth; abstract terminal product arithmetic | core-to-occurrence incidence, retained-fraction shell, uniform pre-recovery clock, native/repeated recovery, complete `C_fw,C_rev,q` | **OPEN** |
| 5. fixed/full transfer | implicit maximal positive word components; core-local fields 5--7 and 14--15 seeds | full-key boundary root order/`Z`, 17 remaining fields, three norms, complete Kac/phase | **OPEN** |

## 3. Gate 1: exact defect and the periodic-shadow obstruction

For

```text
B(x)=D(fx)^(-1) A(x)D(x),
```

the finite stable comparison is exactly

```text
H_B^s(x,y;n)
 =D(y)^(-1) A^n(y)^(-1)
  [D(f^n y)D(f^n x)^(-1)] A^n(x)D(x).
```

The bracket tends to the identity under Hölder continuity, but the
non-fiber-bunched conjugation by `A^n` can amplify it.  Therefore a formal
Hölder cohomology statement does not transport canonical holonomies.  If all
stable and unstable defects vanished, the compact loop would be base-fiber
conjugate to the diagonal class-`H` loop and its same-axis wedges would still
vanish, contradicting the four frozen nonzero compact-gauge wedges.

The second-order physical replay uses the refined shadow root with radius
`1e-800` and the full word `Q^10 B^4 Q^10` (96 solid collisions).  In compact
gauge eigen-coordinates, the stable mixed coefficient above is strictly
nonzero, while the return has unstable multiplier `>1e53`.  Stable comparison
increments grow like

```text
-Lambda*c_* d Lambda^n
```

for a nontrivial local-stable tail.  Thus the existing compact gauge is not
class `H` on a clean horseshoe containing both the shadow and such a tail.
This is a no-go for that gauge, not for every possible resonant transfer.

## 4. Gates 2/5: maximal positive seeds and characteristic restriction

For every fixed `|s|<=1/400`, each positive rational core selects the
connected component containing it of the exact return-word, source-chart,
target-chart, intermediate transparent-wall and central-homogeneity
predicate.  This gives 24 distinct seeded implicit maximal component IDs,
20 of roof one and four of roof two, with collision-SRB mass

```text
>147/550000.
```

Every possible component boundary family is frozen: each of the twelve
`G`-source rows has 402 predicates and each of the twelve `W`-source rows has
394.  Their intersections with arbitrary short curves are not yet root-
isolated or ordered.

The smaller rational cores admit a separate complete characteristic
calculation.  The audit checks all cross-chart and within-chart cyclic gaps,
not merely three bands inside one normal chart:

```text
normal-angle gap >1/100,
physical source-r gap >(4/25)(1/100)=1/625>delta_1.
```

Therefore a canonical short curve retains at most one interval and gains at
most two endpoints.  With density ratio `2000/1999`,

```text
Z_*(1_core F) <= (2000/1999) Z_*(F),
(2000/1999)*(360134800/360493663)
 =720269600000/720626832337 <1.
```

These are unnormalised core-local fields 7, 14 and 15 seeds.  They do not
decide the full maximal-component boundary, fields 16--17, the stable
quotient endpoint packet, or a complete 18-field word block.

## 5. Gate 3: source and critical classes removed

The old 1,536 source records and deep 2,048 source records now carry complete
half-open coordinates.  Every deep record lies in one canonical width-
`1/1024` endpoint band and one parent parameter cell; replay with the
separately certified scaled-`c_p` sign changes all 2,048 records to immutable
owner components.  No graph or positive-width terminal remains in this
class, so its exact simultaneous fixed-`s` width `1/128` is removed.

The twelve remaining critical coarse boxes have 444 possible candidates.
Selective refinement records

```text
audit calls:                     3,196
strict Delta exclusions:           932
strict Delta_t graph leaves:        888
regular-fold leaves:                  0
unresolved leaves:                    0.
```

Hence no simultaneous `(Delta,Delta_t)=0` remains on those boxes, and the
twelve positive-width terminals are removed.  The 888 new graph leaves are
only conservative candidate graphs; they are not assigned a unique physical
first-hit owner.  The updated atlas has

```text
conditionally physical-first charts:    41,344
strictly nonempty physical root arcs:     11,678
untyped graph charts:                      1,052
partial marked-current TV upper:       518152320.
```

The current TV bound is unchanged because the 888 new leaves are untyped.
The remaining 151,500 interval-geometry boxes have positive-width cover
`37875/65536` in the normalized parameter rectangle and uniform fixed-`s`
outer width `595/1024`; neither number is a positive physical singularity
measure.  Complete current/DQ/`MT_DQ`/FACE remains open.

## 6. Gate 4: weak payload and core-local strong bridge

On finite signed Borel measures and bounded Borel tests, every physical
prefix/suffix has operator multiplier one and the height-nine tower has
multiplier at most nine.  The corrected event current satisfies

```text
||J_e||_TV <=2m_e,
two-coordinate l1(TV) <=4m_e,
height-nine pair <=36m_e.
```

Since `C_mesh=69986663973833932800>36`, the existing product maximum already
dominates this weak payload with no forward/reverse double charge.  This is
not a finite-`s` physical-current/`MT_DQ` identification and it supplies none
of the three CM2 strong norms.

For a positive measured short curve with mass `M`, regularity mark `Reg` and
length `L`, use the core-local source cost

```text
M(1+Reg+1/L).
```

Restriction to the sole retained core interval preserves `Reg` and yields
operator norm at most `2000/1999`.  Composing with the physical step gives
the contracting coefficient `b_core` above, with

```text
b_core^2018 <1/2.
```

The abstract post-recovery terminal composition has cost at most
`(2000/1999)C_prod`; its integral bound before the fixed flux normalizer is

```text
1087598065258783059015245434544676138185728521412801591795461
/6707530956800.
```

This arithmetic cannot be installed in the physical same-occurrence ledger:
the certified 24-core-to-64-occurrence mapping count is zero, and a corner
clip can have retained length tending to zero, so its normalized shape cost
has infinite supremum.  A new retained-fraction/depth shell is required.

## 7. Latest-technology audit

The official arXiv `math.DS` API was rechecked through the 2026-07-15 update
batch.  No new theorem closes a missing CM2 interface.

- `arXiv:2607.13785v1` uses analytic planar ODE/Dulac flow boxes, a finite
  equilibrium/sector alphabet, labelled one-hit branches and one-dimensional
  divider sorting.  Its Proposition 4/8 and Theorem 10 are useful strategy
  analogies, but the paper explicitly does not give finite-component control
  for arbitrary transported finite-smooth faces.  It supplies no numerical
  regular-value or owner theorem for the present billiard
  `(Delta,Delta_t)` boxes and was not used to remove any box.
- `arXiv:2607.13530v1` concerns SRB measures for smooth
  `C^{1+Dini}` diffeomorphisms with dominated splittings; it does not cover
  singular billiard branches, the required stable quotient/PPE, or moving-
  boundary DQ.

The closest earlier billiard results likewise do not replace the full-mass
stable quotient, characteristic word-boundary table, finite-`s` physical
current/`MT_DQ`, same-occurrence retained-fraction recovery, or the final
three-norm/Kac/phase interfaces.

## 8. Exact remaining boundary

```text
Gate 1: third/same representative with class H + twisting;
        full-mass singular coding and physical PPE
Gate 2: stable product base, rho, reverse kernel/weights,
        same-carrier endpoint packet and PPE
Gate 3: 151,500 interval boxes; owner-type 1,052 graphs;
        complete current, DQ/MT_DQ, FACE_2CUT/FACE_TIME
Gate 4: 24-core ->64-occurrence incidence; retained-fraction/depth shell;
        native/unbounded repeated recovery; complete C_fw,C_rev,q
Gate 5: maximal-boundary root order and characteristic Z;
        17 remaining full-key fields, three norms, Kac and phase
```

The next executable line is: resolve the 151,500 interval boxes in
cancellation-free miss/root-gap/owner coordinates and type all 1,052 graph
charts; assemble the common branch-record current/`MT_DQ`/FACE ledger;
simultaneously build a 24-core-to-64-occurrence incidence table with a
retained-fraction/depth shell, then return to the stable quotient/PPE and
full-key 18-field/Kac/phase closure.

## 9. Reproduction and freeze

Five new certificate stacks were independently audited.  All pass
`py_compile`, replay/integrity and SHA verification; every default live
verifier exits `2`.  Their semantic mutation suites reject

```text
Gate 1 same-representative obstruction       20/20
Gates 2/5 maximal-word characteristic        26/26
Gate 3 deep source/resultant                  19/19
Gate 4 weak-Borel product payload             16/16
Gate 4 inner-core strong/product bridge       17/17
total                                         98/98
```

The frozen fourteenth-round 384-bit Gate-3 full replay performed 1,722,936
strict calls; this pass's targeted critical refinement adds 3,196 selective
calls.  The final targeted replay
reconstructs the complete source and critical ledgers from the frozen
aggregate rather than silently changing precision.  No residual CM2 process
remains.

Primary evidence:

- `deliverables/cm2-gate1-same-representative-resonant-shadow-frontier-assault-2026-07-16.md`;
- `deliverables/cm2-gate25-maximal-word-characteristic-frontier-assault-2026-07-16.md`;
- `deliverables/cm2-gate3-deep-scaled-resultant-frontier-assault-2026-07-16.md`;
- `deliverables/cm2-gate4-product-borel-payload-frontier-assault-2026-07-16.md`;
- `deliverables/cm2-gate4-inner-core-strong-product-bridge-frontier-assault-2026-07-16.md`.

The append-only `memory/2026-07-16.md` remains outside the immutable
deliverable replay.  No recursive all-history hash claim is made for that
mutable file.
