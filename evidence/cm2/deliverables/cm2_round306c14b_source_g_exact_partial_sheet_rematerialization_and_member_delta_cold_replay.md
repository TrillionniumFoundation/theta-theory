# C14b scrubbed cold replay

Two ordinary seeds and one scrubbed `env -i` replay produced byte-identical exact-sheet-identity, outer-envelope-disposition, and result files. The scrubbed replay took 165.86 seconds with peak RSS 219,512 KiB.

C14b constructs 4,432 exact partial-sheet identities, their graph-sheet set equalities, and 4,432 canonical representation definitions. It retains the 4,432 old outer-envelope members and representations and disposes only their use as graph-sheet set equalities. Registry admission, official-key/base-root assignment, old-family disposition, component/edge inheritance, representation pullback, normalized support, and all downstream credit remain zero.
