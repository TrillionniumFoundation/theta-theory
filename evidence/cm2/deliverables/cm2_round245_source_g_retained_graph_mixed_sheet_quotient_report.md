# CM2 Round245 — Retained Graph Mixed-Sheet Quotient

## Result

- Mapped all `3,136` post-Round240 outgoing-graph retained interfaces into the physical mixed-sheet quotient.
- The `2,872` whole-root zero-absence cases each contribute one positive-volume retained bulk stratum and one exact positive-area resolved/retained contact.
- The `264` graph cases each contribute owner bulk, shadow bulk, and one half-open transition-sheet stratum.
- Every graph case has two independently interval-certified positive-volume side corridors and three physical quotient edges.
- The `252` graph cases on a `t` interface use their exact Round242 graph base as the resolved-side contact.
- The `12` graph cases on a `p` interface use explicit half-width dyadic three-dimensional corridors; their total exact coordinate volume is `177/13107200000`.

## Quotient Delta

- New virtual retained/sheet strata: `3,664`.
- New positive-area physical quotient edges: `3,664`.
- Touched Round244 resolved components: `2,512 / 8,148`.
- Touched seeded components: `108`; inherited virtual-stratum known-block incidences: `216`.
- Mixed-sheet component count remains `8,148`; no prior occurrence components merge.
- New occurrence-to-known-block incidences: `0`.
- Authoritative occurrence incidence remains `36,200 / 53,968`; `17,768` occurrences remain unattached.
- Remaining Round239 retained interfaces: `5,364`.

## Verification

- Independent verifier status: `PASS_INDEPENDENT_ROUND245`.
- The verifier does not import or execute the Round245 producer.
- It independently reconstructs all `3,136` roots, `3,664` virtual nodes, `3,664` physical edges, and `8,148` mixed components.
- It independently re-evaluates all `528` owner/shadow bulk corridors and all `12` `p`-interface contact corridors with the frozen 256-bit interval kernel.
- Hash seeds `245071` and `245929` reproduce byte-identical producer and verifier outputs.

## Strict Boundary

- Virtual retained-stratum incidence is not occurrence incidence.
- The new mixed-sheet components are known-connectivity lower bounds, not maximal physical components.
- No edge is issued from exact-key equality alone.
- Maximal physical components remain `0 / 53,968`.
- Exhausted exact-key fibres remain `0 / 116`.
- Global source-G exact-key dispositions remain `0 / 224,580`.
- Gate5 remains `10 / 18`; CM2 remains `NO-GO_FOR_CLAIM`.

## Required Next

Process the remaining `5,364` Round239 whole-signature, wall, crossing-time, and source-seam retained interfaces with the same positive-area or positive-volume contact standard, then rebuild the mixed quotient and the occurrence/key frontiers.
