# CM2 Gates 3/4: uniform boundary-carrier patches

Date: 2026-07-17 (Asia/Shanghai)  
Frozen input: the 64 singular-limit boundary recovery carriers  
Strict verdict: **every carrier now contains an explicit positive-width
`(z,s)` square of half-width `1/1024`, with one immutable event label and one
regular common suffix collision chart.  These are event-sheet patches, not
two-sided transverse recovery shells or 24-core entrances; Gates 3 and 4
remain open.**

## 1. Uniform positive-width expansion

Around the rational witness of every maximal occurrence row take

```text
z in [z_* - 1/1024, z_* + 1/1024],
s in [    - 1/1024,     + 1/1024].
```

The complete endpoint classifier certifies on all 64 boxes that

```text
the target and grazing side are fixed,
the later miss owner is fixed,
the coarea polarity is fixed,
the box stays strictly inside its source chart and parameter window.
```

Each labelled event-sheet patch has coordinate area `1/262144`; in the
disjoint occurrence-sheet coproduct the 64 patches have total coordinate
area `1/4096`.  This is not collision-SRB mass.

## 2. Regular common suffix charts

On every full patch the tangent continuation reaches the fixed miss owner
with the uniform Arb bounds

```text
source cos(phi) > 1/10,
source |p|      < 199/200,
ell_M-ell_T     > 1/5,
ell_M           < 2,
miss |p|        < 199/200,
miss cos(phi)   > 1/10.
```

The target normal stays in one dominant-coordinate chart over the entire
patch.  The 64 suffix charts split as

```text
G:E/G:N/G:S/G:W = 10/12/12/10,
W:E/W:N/W:S/W:W =  4/ 6/ 6/ 4.
```

Thus every boundary carrier now has a positive-width regular collision-chart
seed, not merely a point witness.

## 3. Remaining physical gap

The patch lies inside the event singularity sheet.  A native recovery theorem
still needs a transverse coordinate resolving the hit and miss sides, a
bounded pushforward through the grazing layer, and joint propagation of both
resulting tubes to

```text
the first 24-core destination, or
a typed contracting cemetery.
```

Only then can the `2018/12108` no-recut dwell ledgers be installed on the
physical operator.

## 4. Strict boundary

```text
UNIFORM POSITIVE-WIDTH EVENT PATCHES:       64 CERTIFIED
REGULAR COMMON SUFFIX CHARTS:               64 CERTIFIED
UNIFORM PATCH HOMOGENEITY MARGINS:          CERTIFIED
TWO-SIDED TRANSVERSE OPEN SHELL:            NOT CERTIFIED
FIRST 24-CORE DESTINATION:                  NOT CERTIFIED
NATIVE NO-RECUT DWELL:                      NOT CERTIFIED
COMMON STRONG-SPACE RESTRICTION:            NOT CERTIFIED
GATE 3:                                     NOT CERTIFIED
GATE 4:                                     NOT CERTIFIED
```
