# CM2 twenty-third direct assault: first stopping, local return tail, and operator-slot incidence

Date: 2026-07-18 (Asia/Shanghai)  
Mode: append-only, fail-closed, exact canonical joins and 2048/8192-bit Arb replay  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; no composite
gate is fully closed (`0/5`).  This round nevertheless upgrades all 128
charged cylinders to genuine suffix-relative first-core stopping cylinders,
materializes three local post-core first returns plus one strict
`tau_C^+>2018` survivor, and joins all 64 occurrence owners through 14
destination cores to the existing field-1/3/4/7 component objects.**

## 1. Executive result

| Gate | New certified progress | Remaining strict blocker | Status |
|---|---|---|---|
| 1 | no promotion; previous half-density/common-transport frontier retained | common physical frame, uniform `q>=q_*`, same-representative twisting | **OPEN** |
| 2 | no promotion | stable-saturated rectangle, quotient branches, native stopping and PPE (`0/17`) | **OPEN** |
| 3 | global first-owner audit on all 64 source boxes; 128 suffix-relative first-core stopping cylinders; exact occurrence/core/component incidence | normalized common-strong `DQ/MT_DQ/FACE`, full recovery carriers and common restriction | **OPEN** |
| 4 | three local whole-box first returns at `545,649,1531`; one `tau_C^+>2018` survivor; exact four-box labelled tail | full-core induced operator, collision-SRB excursion/cemetery tail, new strong coefficient | **OPEN** |
| 5 | 14 destination components bound to existing fields `1,3,4,7`; parameter/recovery/Kac axes separated | fields `2,5--18`, complete tables/blocks, three norms and complete physical Kac | **OPEN** |

## 2. All-occurrence first stopping

The exact charged source boxes are re-audited against the global collision
candidate universe:

```text
physical immutable-row labels:                     64/64,
hit global tangent / suffix owners:                 64/64,
miss global suffix owners:                          64/64,
hit/miss common suffix chart:                       64/64.
```

With the regular suffix state declared to be time zero, every state in every
displayed entrance word is strictly classified against the complete frozen
24-core union:

```text
first-stopping cylinders:                         128/128,
strict preterminal outside-core states:               756,
strict terminal inside-one-core states:               128,
ambiguous classifications:                              0,
maximum first-core time:                                20,
distinct destination cores:                             14.
```

The stopping-time histogram is

```text
2:44, 3:20, 4:16, 7:16, 8:8, 9:8, 16:4, 19:8, 20:4.
```

This certifies the old finite words as first stopping words on their labelled
positive cylinders.  It does not cover the complete germ domains or produce
a normalized physical mass.

## 3. Local post-core return and truncated tail

Every one of the `8072` post-core states on the four materialized `2^-8000`
boxes is replayed at 8192 bits and classified against all 24 cores.

```text
occ:c5... | hit:   first return 545, another hit 1604,
occ:c5... | miss:  first return 1531,
occ:f2... | hit:   no return through 2018,
occ:f2... | miss:  first return 649.
```

Hence, on the equal labelled coordinate volumes of these four source-current
boxes only,

```text
P_label(tau_C^+>n) = 1      for 0<=n<=544,
                      3/4    for 545<=n<=648,
                      1/2    for 649<=n<=1530,
                      1/4    for 1531<=n<=2018.
```

The returning labelled volume is `3*2^-15998`; the survivor volume is
`2^-15998`; singular cemetery through this finite horizon is zero.  These
objects are `(z,h)` source-current boxes and, for fixed `h`, only
one-dimensional source curves.  They are not full collision-core rectangles,
so no induced transfer operator is promoted.  The survivor is not declared
nonreturning cemetery.

## 4. Occurrence-to-operator-slot incidence

The exact canonical join is now materialized:

```text
64 occurrence owners / 128 parameter sides,
14 destination cores,
14 selected roof-one maximal components at j=0,
14 field-1 component entities,
14 field-3 prefix-chart slots,
14 field-4 suffix-chart slots,
14 field-7 cut-growth slots.
```

Field 1 correctly uses the selected homogeneous component-row ID and has no
fabricated `slot:` ID.  The join binds existing fields only; Gate-5 maturity
remains `4/18`, with zero complete homogeneity tables and zero complete
18-field blocks.

The schema also prevents a dangerous cardinality collision.  The 128
hit/miss parameter-side records, the two fw/rev recovery views per occurrence,
and the 128 singular Kac coordinate outputs are different axes.  They are not
positionally zipped or additively cross-charged.  Each occurrence receives
exactly one

```text
q_e=max(C_fw(e),C_rev(e),2)*m_e
```

charge; fw/rev are alternative views, and the two singular Kac coordinates
share the same occurrence measure and mark `[+1,-1]`.

## 5. Technology check

The current literature was checked without importing an external theorem.
Canestrari, *Linear response for Sinai billiards with small holes*,
arXiv:`2604.19671v2` (2026), proves a small-hole response result by
standard-family/coupling methods and emphasizes two relevant limitations:
the geometry must permit useful long standard-pair foliations, and a general
Banach-space differentiability route for piecewise hyperbolic systems is not
currently available in that setting.  Demers--Liverani,
arXiv:`2606.10155v1`, remains a useful transfer-operator review.

These papers support investigating survivor-conditioned standard-family
recovery and mass loss, but neither supplies the missing CM2 physical
source/core partition, common restriction, weighted excursion tail, or
strong coefficient.  No literature statement is promoted into the proof.

## 6. Strict final boundary

```text
COMPOSITE GATES FULLY CLOSED:                         0/5
UNCONDITIONAL CM2:                                    NO-GO FOR CLAIM

GLOBAL SOURCE OWNER AUDITS:                         64/64 CERTIFIED
SUFFIX-RELATIVE FIRST-CORE STOPPING CYLINDERS:     128/128 CERTIFIED
LOCAL FIRST-RETURN CURRENT CYLINDERS:                   3 CERTIFIED
LOCAL tau_C^+>2018 SURVIVOR BOXES:                      1 CERTIFIED
LOCAL LABELLED RETURN FRACTION BY 2018:               3/4 CERTIFIED
OCCURRENCE/CORE/COMPONENT/F1-F3-F4-F7 JOIN:       64/14 CERTIFIED
PARAMETER/RECOVERY/KAC AXES DISTINCT:             CERTIFIED TYPED

WHOLE-GERM / FULL-CORE FIRST-RETURN PARTITION:     NOT CERTIFIED
COLLISION-SRB EXCURSION/CEMETERY TAIL:             NOT CERTIFIED
FULL 24-CORE INDUCED OPERATOR:                     NOT CERTIFIED
NEW INDUCED STRONG LASOTA--YORKE COEFFICIENT:      NOT CERTIFIED
COMMON TWO-VIEW STRONG RESTRICTION:                NOT CERTIFIED
PHYSICAL FW/REV RECOVERY CARRIERS:                 NOT CERTIFIED
COMPLETE PHYSICAL KAC / THREE NORMS:               NOT CERTIFIED
SELECTED FIELD MATURITY:                                      4/18
COMPLETE KEY HOMOGENEITY TABLES:                                 0
COMPLETE 18-FIELD OPERATOR BLOCKS:                               0
```

## 7. Fail-closed validation

All three new verifier suites pass replay and integrity.  Their self-tests
reject

```text
38/38 + 43/43 + 46/46 = 127/127
```

new hostile mutations.  With the previous `420/420`, the cumulative count is
`547/547`.  Every live verifier exits `2`, as required by the unchanged gate
verdict.  Individual leaf SHA-256 manifests and the recursive Gate-3/4/5
aggregate chain pass.

## 8. Next direct attacks

1. **Materialize the full first-return partition.**  For
   `R_n=M_C L(M_{C^c}L)^{n-1}M_C` and
   `Q_n=(M_{C^c}L)^nM_C`, record complete raw branches, candidate universes,
   owner/root gaps, preterminal core-separation margins, Jacobian/distortion,
   mass and strong `q` envelopes, and singular/survivor outer covers.
2. **Build a physical excursion tail.**  Pull the collision law onto a
   complete source/core partition, prove
   `source mass = return + singular cemetery + survivor`, and seek a weighted
   tail `sum_{tau>n} q_tau <= C rho^n` with the first/exponential moments
   needed by DQ.  The local `3/4` labelled fraction is not this input.
3. **Derive a new induced strong coefficient.**  Establish the common
   two-view restriction and one Banach/standard-family space for return,
   recovery, `DQ`, `MT_DQ` and `FACE`; do not reuse `b_core` along excursions.
4. **Finish the physical type joins.**  Materialize fw/rev recovery carriers
   on one restriction, then the two remaining Kac terms, three norm
   intertwiners and Gate-5 fields `2,5--18` without duplicating the occurrence
   charge.
5. **Return to the structural long poles.**  Close the Gate-1 common frame and
   twisting, then the Gate-2 stable quotient/PPE; neither is implied by the
   new finite incidence ledgers.

The highest-leverage next line is the **complete physical source/core
first-return partition with a weighted excursion tail**.  The present round
removes the finite stopping and incidence ambiguities needed to state that
problem correctly, while keeping the full induced-operator claim closed.
