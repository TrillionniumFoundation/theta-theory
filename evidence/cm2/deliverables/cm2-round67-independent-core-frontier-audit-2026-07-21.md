# CM2 Round 67 independent core-frontier audit (2026-07-21)

## Verdict

**PASS — the four Round 67 source leaves are internally consistent, reproducible, and correctly retain `NO-GO_FOR_CLAIM`.** No source leaf or older frozen artifact was modified by this audit.

The strict state remains:

- Gate 1: `NOT_CERTIFIED`.
- Gate 2: `NOT_CERTIFIED`, official fields `0/17`.
- Gate 3: `NOT_CERTIFIED`.
- Gate 4: `NOT_CERTIFIED`, landing join `1/7`, fields `1/4/7` partial.
- Gate 5: `NOT_CERTIFIED`, maturity `10/18`, complete blocks `0`.
- Complete composite gates: `0/5`.
- CM2: `NO-GO_FOR_CLAIM`.

## Frozen inputs and acceptance

The audit pins the Round 66 aggregate report/ledger/audit, the four Round 67 leaf reports/manifests/sidecars, and `cm2_round67_common.py`: `18/18` direct pins.

Independent source acceptance passed:

- Python syntax, including the common helper: `9/9`.
- Source dependency pins: `101/101`.
- Source SHA artifact rows: `19/19`.
- Source audit/replay/self-test/byte-identical reemit bundles: `4/4`.
- Source hostile-semantic mutations: `1045/1045` rejected.
- Source hostile strict-JSON payloads: `16/16` rejected.
- Source default producer/verifier entry points: `8/8` exit `2`.

The independent audit bundle additionally rejects `96/96` hostile-semantic mutations and `4/4` hostile strict-JSON payloads; its SHA sidecar has `5/5` rows and deterministic reemit is byte-identical.

## Gate 1/3 audit

The actual clean symbolic root and exact formal transfer `C=Q^{-1}ED` replay correctly. The rational replay has determinant one. This is a genuine advance in typing: the physical derivative cocycle and root tokens coexist on the clean root.

It does not supply the same-key all-plaque `Q,E,u,v` rows. The three endpoint completions share the selected-orbit data but have renormalized defects `0`, `E_21`, and `(-1)^nE_21`; hence selected constants still do not determine the actual endpoint limits, transported loop, or four nonzero wedges.

For Gate 3, the exact criteria for a positive common material window, a weighted direct sum, a three-factor Piola remainder, a two-sided side-tagged trace/current, and the nonautonomous stopped `MT_DQ` ledger all replay. The finite rows pass (`6/6`, `5/5`, `3/3`, `6/6` respectively). The actual all-depth window, uniform direct-sum remainder, strong factors, strong trace/current, and positive stopped ledger remain unmaterialized.

## Gate 2/4 audit

The new `R1` construction is a genuine same-`(s,u,v)` graph-supported owner/collision/landing/material pre-registry with exact affine collision-SRB chart. Its determinant is `-3`, squared singular values are `2,9/2`, and the density/holonomy compatibility identity replays exactly.

Its scope is local and one-step. It is not an invariant stable product, it carries no actual stable marker, and it is not crosswalked to the distinct Round 15/59 positive stable crossing. Thus the actual stable tree remains exactly `0/7`, with only rows `2/4/7` partial.

The conditional-hazard identity is sharp. A summable model has survivor limit `1/2`; `h_n=1/(n+1)` has limit `0`. Existing frozen data still give `96` zero collision-shadow rows but `0` full graph/registry hazard rows, so the strict actual survivor lower bound stays `0`. Zero marker defect still does not pay weighted transverse BV, and the physical all-depth strong/Piola recipient remains open.

## Gate 5 audit

The direct positive lift criterion is exact:

`A:L^1(mu) -> positive typed direct sum` is bounded iff the conditional path potential `H` belongs to `L^infinity(mu)`, with norm `||H||_infinity`.

For unit live charge, this becomes

`ess sup_x E_x[w_Z^N] < infinity`.

The geometric replay with conditional tail parameter `1/3` and `w_Z=3/2` has full-label survivor RN density `1`, but `w_Z r=1/2`, `E[w_Z^N]=2`, and `H_live=2`. This is a lawful attenuation mechanism and does not reuse owner-set nesting as a contraction.

The one-shot lost-departure identity also replays:

`H_departure = x_0 + (w_Z-1) H_live`.

It shows why unweighted telescoping and absorbing cemetery occupancy are insufficient. The seven typed potentials, raw-Z/Orlicz charge, pre-regularization cemetery, and oriented total/two-RN/common-mode rows still need to coexist on one actual root. Fields `F5/F6/F10/F11/F14/F15/F17/F18` remain open; no Gate 5 field is promoted.

## Fixed-j physical root and cross-leaf consistency

The occurrence leaf certifies a real graph-supported fixed-`j` physical subroot: the identity occurrence view and deterministic owner view live over `M_j=m_j^own`, with time and eight immutable key groups retained. This is not merely a same-marginal coupling.

The countable disjoint union over time is standard Borel, but no finite weighted physical law or strong all-time operator follows. On the raw occurrence root, boundedness is exactly `H_g in L^infinity(m_occ)`. The two exact separators have finite total weighted charges `6/5` and `8/5` while their pointwise potentials are unbounded.

There is no contradiction with the Gate 5 conditional-tail model:

- deterministic eligibility `r_j=1_{j<n(x)}` has `H=(w^n-1)/(w-1)` and is bounded iff `n` is essentially bounded;
- a conditional random terminal time can instead satisfy `ess sup_x E_x[w^N]<infinity` even when its full-label survivor RN density is one.

These are different kernels. The deterministic eligibility envelope also only bounds the actual owner indicator from above, so divergence of the envelope does not prove divergence of the actual owner potential.

The spanning registry has one certified physical node and four open crosswalks: owner-to-return graph, return-to-all-depth material atlas, return-to-collision-SRB stable product, and stable plaque-to-combined-Green/QNL representative. A global graph-supported CM2 root is therefore still not certified.

## Technology boundary

The checked smooth Axiom-A product, spectral-approximation, diffusion Feynman–Kac, and sequential-billiard results either assume the needed compactness/drift/product structure or use a different smooth/process interface. None constructs the missing singular moving-billiard immutable registry together with the all-depth strong recipient. No external theorem was promoted.

## Shortest next route

The shortest remaining route is to extend the genuine fixed-`j` and `R1` physical subroots into one immutable all-depth root, then on that same root prove:

1. all-plaque Green endpoint limits and nonzero four-wedge transport;
2. a positive all-depth material window, uniform Piola remainder, and side-tagged strong trace/current;
3. summable full graph/registry hazards, zero stable marker defect, and weighted transverse BV;
4. a conditional charged terminal moment and all seven sector potentials in `L^infinity`, including pre-cemetery and oriented common mode.

Until those rows coexist, the only strict conclusion is `NO-GO_FOR_CLAIM`.
