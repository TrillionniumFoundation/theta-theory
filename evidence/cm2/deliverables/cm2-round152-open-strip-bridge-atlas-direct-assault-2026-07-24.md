# CM2 Round152 direct hostile assault

Date: 2026-07-24

Independent verification status: **PASS**.

The verifier reconstructs every bridge cell and rejects ten fully re-signed
semantic mutations attempting to:

- change a strict-return bridge to survive;
- delete one bridge cell;
- erase one exact x-face bridge adjacency;
- disable the physical-anchor D3 exclusion;
- reverse the terminal-event beta derivative;
- invent an interior competing event surface;
- close Round144 D02;
- authorize the D03 negative oracle;
- promote global Gate5 to `18/18`; or
- change CM2 to `GO_FOR_CLAIM`.

It also rejects four strict parser attacks: duplicate JSON keys, a UTF-8
BOM, a top-level array and `NaN`.

```text
re-signed semantic attacks rejected       10/10
strict JSON/encoding attacks rejected       4/4
full R1648 bridges reconstructed              21
radius-four candidate tests replayed   5,571,888
typed atlas boxes                              63
untyped interior event cells                    0
D02                                      BLOCKED
global Gate5                               10/18
CM2                             NO-GO_FOR_CLAIM
```
