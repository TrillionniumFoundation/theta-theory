# CM2 fortieth direct assault

Date: 2026-07-19  
Verdict: **one Round-39 Gate-4 interface claim is corrected, the native C24
Growth target and arbitrary-`R_n` F10 mixed-jet recurrence are now exact;
all five composite gates remain open**

## Round result

| gate | round-40 advance | exact remaining core gap | status |
|---:|---|---|---|
| 1 | no unsafe transfer from the shared Gate-3/4/5 work | one physical representative with valid cancellation/Green hypothesis and same-representative twisting/PPE | NOT CERTIFIED |
| 2 | no cross-carrier promotion | invariant stable plaques, physical holonomy, quotient branches and PPE | NOT CERTIFIED |
| 3 | the Round-39 no-trace obstruction is retained only for moving face currents | arbitrary-depth dynamic DQ/`MT_DQ`, moving-current source propagation and cemetery | NOT CERTIFIED |
| 4 | canonical positive-family initialization is no longer falsely blocked by C0 trace; the C24 native Growth target is exact | C24-specific expansion/fragmentation block with `gamma_C24<1`, then aggregate `Z`, numeric `C_fw,C_rev,q` | NOT CERTIFIED |
| 5 | exact arbitrary-path `D/H/P/Q/S` mixed-jet recurrence and compact-germ installation interface | physical `U/V/W`, all-face parameter jets, joint margin/path moment, F12/F13 and cemetery | NOT CERTIFIED (`7/18`) |

Strict unconditional progress remains `0/5`; CM2 remains
`NO-GO_FOR_CLAIM`.

## 1. Round-39 Gate-4 correction

The Round-39 concentration model remains a valid proof that stable-test
control does not bound a pointwise transverse trace.  It is relevant to
moving inverse-branch face currents.  It does **not** prove that positive
standard-family initialization requires such a C0 trace.

The dependency chain already has controlled `s=0` stopped-atom families with

```text
Z_fw(K,j),Z_rev(K,j)<=C_mesh*2^K
```

on the same restricted measure and depth record.  Therefore the actual first
Gate-4 gap is hereditary open Growth after repeated C24 characteristic cuts,
not positive-measure disintegration.

No old artifact is rewritten.  Round 40 supersedes only the overtyped
necessity claim; Round 39's nonpromotion, rate budgets and moving-current
trace obstruction stay valid.

## 2. C24 native Growth is now the primary route

The exact target is

```text
Z(hat F_C24^((p+1)n_*)G)
 <= gamma_C24 Z(hat F_C24^(p n_*)G)+Z0_C24 mass(G),
gamma_C24<1.
```

C24 already satisfies the sparse-opening geometry interface

```text
P0=49, Ct=1493, 96 physical boundary edges,
```

but the existing crude global one-step coefficient

```text
137751561/901685
```

is larger than one.  The missing proof must reproduce expansion beating
fragmentation on every surviving standard curve and control conditioning.

An exact tail-only boundary criterion is now frozen: a same-law normalized
carrier-length tail `t^alpha` gives a finite inverse-length moment when
`alpha>1`; `alpha=1` is insufficient by an explicit dyadic countermodel.
This also explains why Round 26's linear parameter-averaged tube law cannot
alone close canonical boundary `Z`.

## 3. Latest technology audit

Official versions remain

```text
2104.06947v3 / 2604.19671v2 / 2606.10155v1.
```

Canestrari `2604.19671v2`, Proposition 4.4 and Lemma 6.14, supply the right
architecture: regular conditional leaky families and a block Growth
recurrence with coefficient below one.  The theorem's boundary-strip hole is
not the 96-edge C24 phase hole, so it cannot be imported without a new
C24-specific expansion/fragmentation match.  The direct official pages were
checked; the general search endpoint was temporarily quota-limited.

## 4. Arbitrary-Rn F10 chain rule is complete at the algebraic level

For the path map `X_i`, Round 40 freezes

```text
D_i <= A_iD_(i-1),
H_i <= B_iD_(i-1)^2+A_iH_(i-1),
P_i <= U_i+A_iP_(i-1),
Q_i <= V_iD_(i-1)+B_iP_(i-1)D_(i-1)+A_iQ_(i-1),
S_i <= W_i+2V_iP_(i-1)+B_iP_(i-1)^2+A_iS_(i-1).
```

The spatial `A_i,B_i` are known from F9.  The physical rank-indexed mixed
jets

```text
U_i=||partial_sT_i||,
V_i=||D_xpartial_sT_i||,
W_i=||partial_s^2T_i||
```

are not.  This is the exact reason the Round-39 seed exponents
`alpha=2,r=1,p=3/2` do not automatically propagate through arbitrary
pullbacks.

Every fixed compact analytic germ remains constructively searchable, but
global F10 additionally needs all-face base parameter jets, a joint
spacetime margin atlas and a same-law nonlinear path moment.  Gate-5 maturity
therefore remains `7/18`.

## 5. Validation

Both suites pass Python syntax, frozen dependency hashes, strict JSON,
independent exact replay and live fail-close.  They reject

```text
23/23 + 24/24 = 47/47
```

hostile mutations.  Both certificate defaults and both verifier defaults
exit `2`; replay modes pass.  Leaf and recursive SHA chains freeze every
report, certificate, verifier and manifest.

## Next shortest line

1. Reproduce the `2604.19671v2` Growth-Lemma mechanism for the 96-edge C24
   phase hole on the frozen same-ID parent-`W` registry; certify a fixed-`s`
   block `gamma_C24<1` and finite `Z0_C24`.
2. Derive physical one-step mixed jets `U(B),V(B),W(B)` on the existing
   incidence-rank branch IDs and construct a joint spacetime persistence /
   denominator-margin atlas.
3. Prove a same-law path `L^p` moment for the full `D/H/P/Q/S` recurrence,
   then install arbitrary-`R_n` all-face F10 and return-wide F12/F13.
4. Close strong cemetery, F14--F18 and the induced coefficient; feed the
   common ledger into Gate-3 dynamic DQ/`MT_DQ` and numeric Gate-4 charges.
5. Continue Gate 1's same-representative cancellation/twisting and Gate 2's
   plaque/holonomy/quotient/PPE constructions as independent structural
   poles.

Strict unconditional CM2 remains `NO-GO_FOR_CLAIM`, with zero of five
composite gates fully closed and Gate-5 maturity unchanged at `7/18`.
