# CM2 Round 67 full direct assault

Date label: 2026-07-21

Strict verdict: **0/5 complete composite gates; Gate 2 official fields 0/17;
Gate 4 landing join 1/7 with fields 1, 4 and 7 partial; Gate 5 maturity
10/18 with zero complete blocks; CM2 remains `NO-GO_FOR_CLAIM`.**

## Round result

Round 67 freezes four compatible source leaves and one independent aggregate
audit.  For the first time it obtains two graph-supported physical subroots
rather than only abstract same-law couplings.  They are different roots and
have not been crosswalked, so the round records the genuine local promotions
without treating them as one global CM2 registry.

1. **A physical fixed-time occurrence/owner subroot.**  For each insertion
   time `j`, restrict the endpoint-coarea occurrence measure to

       Omega_j={(a,x):x in E_(j,a)^owner intersect R_(j,a)^reg}.

   The identity occurrence view and the deterministic owner map `q_j` are
   graph-supported views of the same restricted measure `M_j=m_j^own`.
   They retain restriction ID, time, physical event signature, primitive
   key, rank-zero component, side, word cell and endpoint/root coordinates.
   This certifies one actual occurrence/current-to-owner/collar registry
   edge at fixed time.  It does not identify the Round-54 Jordan pair or
   provide a finite all-time strong root.

2. **A positive physical owner/collision/landing/material pre-registry.**
   On the frozen Round-25 `RETURN_AT_1_INNER` tile, the same positive
   `(s,u,v)` root now deterministically carries its first-collision owner,
   source collision coordinate, time-one landing, affine cone-product
   coordinate and persistent material tag.  Its law is, up to normalization,

       dLambda_0=3 R_G/[sqrt(1-t(u,v)^2)] ds du dv.

   This is a genuine graph-supported physical join.  It is only a
   pre-registry: its affine cone fibres have not been proved invariant stable
   plaques, and no frozen immutable crosswalk identifies it with either the
   Round-15/59 depth-96 stable-plaque root or the common landing marker.

3. **Exact keyed collision-SRB formulas on that pre-registry.**  With
   `Phi(u,v)=(R_G asin(t),p)`, the actual pullback gives

       q=3 R_G/sqrt(1-t^2),
       a=sqrt(R_G^2/(1-t^2)+9/4),
       rho_v=q/(Z(v)a),
       lambda_(v,w)=a(u,w)/a(u,v),
       J_(v,w)=q(u,v)Z(w)/[q(u,w)Z(v)].

   Hence `J rho_w=rho_v/lambda` on the keyed affine chart, and its candidate
   metric factor is strictly below `101/100`.  Stable meaning remains
   conditional on an unpaid graph-transform upgrade, so this is not a Gate-2
   field promotion.

4. **Actual clean-root Gate-1 transfer.**  The clean SFT, physical cocycle,
   compact QNL representative and combined-Green functions do share one
   maximal actual carrier.  Their exact relative transfer is

       C(x)=Q(x)^(-1) E(x) D(x),
       A_c(x)=C(sigma x)^(-1) A_q(x) C(x).

   Frozen manifests do not export `Q,E,u,v` under one immutable
   `(code,time,plaque-side)` key.  Three `SL(2)` Lipschitz completions agreeing
   at the periodic and selected records yield renormalized endpoint defects
   respectively `0`, `E_21` and `(-1)^n E_21`.  Consequently the selected
   `0.091/0.092809...`, depth-260 and determinant-one rows still do not
   determine the all-plaque endpoint limits, corrected loop or four wedges.

5. **Exact full first-failure frontier.**  For same-key survivors `S_N` and
   conditional hazards

       h_N=eta(S_(N-1)\S_N)/eta(S_(N-1)),

   one has `eta(S_N)=eta(S_0) product_(n<=N)(1-h_n)`.  Positive all-depth
   survivor mass is equivalent to every `h_n<1` and `sum_n h_n<infinity`.
   The examples `h_n=1/(n+1)^2` and `h_n=1/(n+1)` give limits `1/2` and `0`.
   The 96 frozen rows eliminate only collision-shadow failures, not
   graph-transform/domain or immutable-key failures.  Therefore the number
   of actual full hazard rows is still zero and the certified survivor lower
   bound remains zero.

6. **Gate-3 uniform assembly criteria.**  A countable material atlas has a
   common parameter window exactly when `inf_k r_k>0`.  For weighted direct
   sums,

       ||J_s||=sup_k (b_k/a_k)||J_(s,k)||,

   and the global derivative requires the same weighted supremum of the
   block remainders to tend to zero.  The three-factor
   `Qhat_s Phat_s Rhat_s` expansion gives an explicit uniform product
   remainder, while the physical two-sided current is the positive sum over
   distinct face/clock/grazing/birth/cemetery side tags.  Weak graph-TV and
   per-vector differentiability do not pay this norm.

7. **Nonautonomous stopped derivative and fixed-material BV criteria.**  For
   a length-`n` path with `||T_i||<=M_i`, `||DT_i||<=L_i`, the exact positive
   majorant is

       A_n=sum_(r<n) L_r product_(i!=r) M_i.

   The stopped `MT_DQ` row is `sum_n p_n A_n<infinity`, together with the
   separately tagged trace/clock/cemetery ledgers.  This may hold even when
   `sup_i M_i=1`, and may fail with all `M_i=1/2` if derivative spikes are
   unpaid.  On a fixed material interval, the tagged-BV multiplier and its
   moving Piola derivative satisfy the sharp equivalences

       M_q:BV -> ell^1_a(BV) bounded
         iff sum_r a_r||q_r||_BV<infinity,
       M_(q(s)) operator-norm differentiable
         iff sum_r a_r||R_r(s)||_BV=o(|s|).

   These are exact recipients, but the global physical coefficients and
   anisotropic intertwining are absent.

8. **Direct all-time positive potential.**  On a graph-supported terminal
   record kernel `Q_x`, retain every immutable label and time coordinate and
   put

       H(x)=E_x[sum_j w_Z^j sum_s g_j^s].

   The positive lift from unweighted `L1` into the seven-sector weighted
   direct sum is bounded exactly when `H` belongs to `L-infinity`; its norm is
   `||H||_infinity`.  This is the exact route after Round 66 ruled out using
   terminal owner-set nesting to manufacture a one-step RN coefficient below
   one.

9. **Conditional terminal killing lawfully supplies attenuation.**  For
   unit live charge and terminal lifetime `N`,

       H_live=(E_x[w_Z^N]-1)/(w_Z-1).

   Thus `ess sup_x E_x[w_Z^N]<infinity` is necessary and sufficient.  A
   conditional geometric lifetime may satisfy `w_Z r<1` even though the
   full-label density of every surviving record is exactly one.  This does
   not contradict the fixed-time occurrence-root calculation: deterministic
   eligibility with unbounded `n(x)` has unbounded recordwise potential,
   whereas a terminal kernel may average `w_Z^N` uniformly in the source.
   The actual charged terminal law has not yet been frozen.

10. **One-shot departure and seven-sector debt.**  For decreasing live mass
    `x_j` and one-shot lost arrivals, exact summation gives

        H_departure=x_0+(w_Z-1)H_live.

    Hence bounded live potential pays bounded one-shot departure marks.
    Bare telescoping and absorbing cemetery occupancy do not.  Active-clock,
    raw-`Z`, Orlicz, complement, variation, common-mode and one-shot cemetery
    each require their own positive potential on the same root.  The actual
    raw-`Z`/Orlicz slice, pre-regularization cemetery, conditional charged
    terminal tail and Round-54/Round-61 total-mass/two-RN/common-mode bridge
    remain absent.  Gate-5 fields F5/F6/F10/F11/F14/F15/F17/F18 stay open.

## Independent audit

The independent Round-67 audit passed without modifying or correcting a
source leaf.

    new Python syntax including Round-67 common:        9/9
    dependency pin rows across four source leaves:    101/101
    source-leaf SHA artifact rows:                      19/19
    source audit/replay/self-test/reemit:                 4/4
    source hostile semantic mutations:               1045/1045 rejected
    source hostile strict-JSON payloads:                16/16 rejected
    source default entry points:                          8/8 exit 2

The audit's own canonical replay, regeneration and byte-identical reemit pass.
It rejects `96/96` hostile semantic mutations and `4/4` hostile JSON payloads;
its five SHA rows resolve and both default entry points exit `2`.
The cross-leaf replay distinguishes the two new physical subroots, confirms
that neither is the missing global stable registry, and verifies that
deterministic terminal eligibility and conditional random killing are
different lawful quantifiers rather than contradictory claims.

## Latest technology boundary

Official sources were checked directly on 2026-07-21.

- `arXiv:2502.07765v2` proves a CLT with error bounds for sequential systems,
  including sequential dispersing billiards, through projective cones.  It
  does not provide moving-boundary material derivatives, face currents or an
  operator-norm Piola/`MT_DQ` theorem.
- `arXiv:2104.06947v3` supplies projective-cone memory loss for sequential
  dispersing billiards after standard-family inputs have been constructed;
  it does not create the immutable stable-product/marker registry.
- `arXiv:2604.18929v3` develops SRB/product formulas for smooth Axiom-A
  diffeomorphisms via Markov partitions.  Its smooth boundaryless hypotheses
  do not cover moving billiard collision singularities.
- `arXiv:2602.19435v2` certifies spectral approximations after compactness or
  Doeblin-Fortet-Lasota-Yorke approximation bounds are supplied.  It does not
  construct CM2's physical recipient or bound.
- `arXiv:2601.10539v2` treats generalized Feynman-Kac formulas for Hormander
  Ito diffusions with stopping.  It is a different process and cannot supply
  the missing collision-SRB kernel.
- The current search found no newer same-law moving-scatterer response or
  invariant collision-SRB stable-product theorem that closes these rows.

No external theorem is promoted.

## Strict final state

    Gate 1: NOT_CERTIFIED
    Gate 2: NOT_CERTIFIED; official fields 0/17
    Gate 3: NOT_CERTIFIED
    Gate 4: NOT_CERTIFIED; landing join 1/7, fields 1,4,7 partial
    Gate 5: NOT_CERTIFIED; maturity 10/18, complete blocks 0
    complete composite gates: 0/5
    CM2: NO-GO_FOR_CLAIM

## Shortest remaining routes

1. **Join the two physical subroots to the exact return graph.**  Emit pinned
   Borel maps from fixed-time occurrence/owner records and from the Round-25
   `(s,u,v)` pre-registry to one half-open return-path namespace.  Equality of
   marginal laws is not a substitute.

2. **Gate 1:** export `Q,E,u,v` under the same
   `(code,time,plaque-side)` keys; compute the forward/backward renormalized
   defects on every plaque and replay the corrected loop and four wedges.

3. **Gate 3:** prove a positive uniform all-cell material radius, the
   weighted direct-sum Piola remainder, side-tagged two-sided trace/current
   bound and the nonautonomous stopped derivative ledger on that return root.

4. **Gate 2/4:** upgrade the actual affine chart to invariant stable plaques
   or crosswalk it to the 96-word stable root; then prove summable full
   conditional hazards, marker saturation, strict path budget, weighted
   transverse variation and the physical strong recipient.

5. **Gate 5:** materialize the conditional charged terminal kernel on the
   same root and prove all seven `L-infinity` potentials, including births,
   raw-`Z`/Orlicz, pre-cemetery and the oriented common mode.  Do not return to
   terminal nesting as a contraction argument.

Until these rows coexist on one graph-supported actual root, the CM2 claim
remains a strict no-go.
