# CM2 Round300-F — R245 half-open-owner sheet attachments

## Outcome

Round300-F closes the exact one-sided component-edge consequence of the
sealed R245 half-open transition-sheet family.

The complete result is:

- `264` R245 half-open transition sheets audited;
- `264` exact R291 graph leaves selected from the complete `111,524`-cell
  graph witness inventory;
- `264` unique half-open owner occurrences selected;
- `264` canonical post-Round266-root ↔ owner-occurrence component-edge
  witnesses;
- `264` opposite shadow occurrences retained as incidence-only;
- zero unresolved sheets and zero duplicated canonical endpoint pairs.

The exact positive two-dimensional sheet-area sum is
`1609/26214400`.  The owner/shadow outgoing-cell pairs split evenly as
`E|N = 66`, `E|S = 66`, `N|W = 66`, and `S|W = 66`.

## Exact lineage and owner proof

Every promoted row independently closes the following chain:

1. the R245 sheet is reopened through its R242 existence witness and exact
   R182 graph leaf;
2. retained child, chart, predicate equation, and outgoing normal form agree
   exactly throughout the R179/R182/R220/R233/R242/R245 lineage;
3. the R242 witness box is contained in the R182 leaf box and its `t`
   interval is exactly equal;
4. the exact R291 leaf is joined through its R293 two-sided binding to the
   two R294 registry occurrences;
5. the two exact full signatures recover the R242 owner and shadow R279
   atoms without ambiguity;
6. only the half-open owner signature is selected;
7. the selected owner is joined to the exact post-Round266 quotient root.

All `264/264` sheets pass.  The resulting ledger endpoint contract is:

```text
[post_Round266_quotient_component_id, Round294_owner_occurrence_id]
```

The ledger is a legal edge inventory for a later DSU rebuild.  It does not
apply any union.

## Fail-closed Round300-A cross-check

The `128` explicit graph-zero pairs isolated by Round300-A were cross-joined
against the R245 family by both binding identity and canonical endpoint pair.
The exact intersection is `0/128`.

Therefore Round300-F claims no credit for those pairs.  They remain
unresolved here, and neither endpoint of any Round300-A explicit pair is
silently promoted.  In particular, the numerical proximity between the two
frontiers is not used as evidence.  Their separate R246/R247 lineage must be
audited independently.

## Strict nonpromotion

Round300-F grants exactly one formal half-open-owner component-edge witness
per selected sheet.  It grants:

- zero shadow-edge credit;
- zero occurrence-identity or alias-collapse credit;
- zero component-union application or DSU-rank reduction credit;
- zero quotient, maximality, fibre, or global-disposition credit;
- zero complete-component-frontier credit.

## Independent verification and attacks

The independent verifier never imports, executes, parses, or tokenizes the
producer.  It treats the producer only as inert hash-pinned bytes.  Before
opening either candidate artifact, it reconstructs the complete expected
ledger and result from the fourteen pinned upstream inputs.

It reopens all `111,524` R291 graph witness cells, all `431,208` R294
registry rows, and all `133,284` R266 virtual-frontier rows.  Candidate
artifacts must then equal the independently reconstructed canonical JSON and
deterministic GZIP bytes exactly.

All `36/36` targeted attacks are rejected:

- `27` fully recommitted semantic attacks, covering owner/shadow swaps,
  forged signatures, geometry, lineage, roots, endpoint pairs, row
  identities, duplicate/drop/add/reorder cases, Round300-A overlap forgery,
  and every forbidden promotion credit;
- `9` parser/path attacks covering duplicate keys, nonfinite numbers, NUL
  input, malformed GZIP, producer parsing/import/execution, and output-path
  aliasing.

## Artifact commitments

| artifact | SHA-256 |
|---|---|
| producer | `a47cec07e10fd01d44953b4df101f8aafe6768502c1b39d6baf4d9a06b2e7523` |
| attachment ledger | `1f958f9f3e3aff7327d897b39851f2d2d030d820e00859d302241702613cae1f` |
| result file | `bcb3fbba8ec6ba6777e904b8d4935533bf23babd5f756989e512a0b2dc4a0dfa` |
| embedded result | `48e7fa4e534cb372562c0fc44f4f84a6c4e728843b5e1b8d7b48a52ba0020938` |
| verifier | `82da41e36ac34b663c87f52ae6e533a2269a2b29d84769abc99da154301b43a2` |
| attack-suite file | `b84b661d176f1ece2552f9cb350d7f64e82708ef52088f33ce9df110b23435fe` |
| embedded attack suite | `5ea43ed860c277686655617735dff058feb1e9f8d807b1e95aac9adc8ae15185` |
| verification file | `d34071117fae6914812d99852e0936c29564f40e03559fb885018d71735e6f96` |
| embedded verification | `f9f02ac65343ea60e048c9e93ee6e4b72dc81d04463fbc384a8f69f78737bf3a` |

## Required next

Audit the disjoint Round300-A explicit `128`-pair frontier against the full
R246/R247 source-seam owner lineage, then combine only independently sealed
and pairwise-disjoint legal edge inventories in the later expanded-registry
DSU rebuild.
