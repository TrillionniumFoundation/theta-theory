# CM2 Round 68 — common-root and all-gate frontier assault

Date: 2026-07-21  
Scope: append-only direct assault from the frozen Round-67 aggregate  
Strict verdict: **Round 68 closes no composite gate.  It replaces four vague
existence debts by exact, independently replayable data contracts, sharp
separators, and quantitative sufficient criteria.  The actual common-root
crosswalk and the required physical estimates are still absent; CM2 remains
`NO-GO_FOR_CLAIM`.**

## 1. Common-root join

Round 67 produced two graph-supported physical subroots.  Round 68 proves the
exact join criterion: both views must be deterministic Borel maps from one
half-open path namespace and must agree recordwise on a twelve-field composite
key.  Equal marginals, an abstract measure-space isomorphism, or equality after
forgetting time/side/owner are insufficient.  A four-atom exact separator has
identical key marginals but zero diagonal graph mass after the owner swap.

The minimum export contract is:

`restriction_id, return_component, insertion_time, collision_index,
event_signature, primitive_key, owner_key, rank_zero_component, plaque_side,
word_cell, endpoint_coordinate, root_coordinate`.

Neither Round-67 subroot exports this full contract, so actual joined rows stay
`0/12` and no global registry is promoted.

## 2. Gate 1/3

For a keyed transfer `C`, the transported cocycle identity is exact.  The
endpoint and four-wedge claims require uniform keyed errors, not selected-word
agreement.  Round 68 records the sharp product perturbation ledger

`||(A+dA)(B+dB)-AB|| <= ||dA||||B||+||A||||dB||+||dA||||dB||`

and its weighted direct-sum analogue.  A diagonal sequence with one unobserved
cell of unit error at each depth passes every finite selected test while its
uniform error remains one.  Thus no all-cell endpoint, Piola, trace/current, or
`MT_DQ` row follows from the frozen finite selections.

The sufficient contract now has explicit fields: a positive common material
radius, uniform factor bounds, weighted block remainder tending to zero,
side-tagged two-sided trace/current summability, and the nonautonomous stopped
derivative sum.  None is supplied on the actual return root.

## 3. Gate 2/4

The exact hazard product is replayed.  With `h_n=1/(n+1)^2`, survival through
depth `N` is `(N+2)/(2(N+1))` and tends to `1/2`; with `h_n=1/(n+1)`, survival
is `1/(N+1)` and tends to zero.  The first sequence proves that strict positive
survival is compatible with infinitely many losses; the second proves that
every finite-depth survivor being positive is not enough.

A separate alternating tail separator has bounded per-level variation but
divergent total transverse variation.  Therefore the actual proof still needs
same-key full hazards, marker saturation, a strict path budget, weighted
transverse variation, and a physical strong recipient.  Official Gate-2 fields
remain `0/17`; Gate-4 landing join remains `1/7` with fields 1, 4 and 7 partial.

## 4. Gate 5

For each charged sector, the positive potential is the minimal nonnegative
solution `H=r+KH=sum K^j r`.  A uniform contraction `||K||<=alpha<1` gives
`||H||<=||r||/(1-alpha)`.  At charge weight `w=3/2`, geometric attenuation
`beta=1/3` yields total potential `2`; the critical value `beta=2/3` diverges.
Finite integrated charge is separated again from an `L-infinity` operator
bound.

The same test must be paid independently for active-clock, raw-`Z`, Orlicz,
complement, variation, oriented common-mode, and one-shot cemetery sectors on
the joined actual root.  Conditional terminal killing cannot be replaced by
deterministic eligibility.  Gate-5 maturity stays `10/18`, with zero complete
blocks.

## 5. Technology boundary

The arXiv API was checked directly on 2026-07-21 for moving-scatterer response,
dispersing-billiard linear response, sequential dispersing billiards, and recent
SRB/Markov-partition work.  The search returned no moving-billiard response
theorem.  `arXiv:2502.07765v2` and `arXiv:2104.06947v3` remain the nearest
sequential-billiard results, but start after standard-family/cone inputs and do
not construct the required registry or moving-boundary derivatives.
`arXiv:2604.18929v3` concerns smooth Axiom-A diffeomorphisms and does not cover
collision singularities.  No external theorem is promoted.

## Strict state and shortest route

1. Emit the twelve composite-key fields from both physical subroots and prove
   the two maps land on the same half-open return-path records.
2. On that root, export `Q,E,u,v` and all-cell material/Piola/trace data under
   the same keys, then replay endpoint defects and four wedges.
3. Prove summable full conditional hazards and weighted transverse variation,
   not only collision-shadow rows.
4. Construct the conditional charged terminal kernel and pay all seven positive
   potentials sectorwise.

Until those actual rows coexist, all five gates remain incomplete.
