# CM2 Round145 finite connected leaf-corridor atlas — direct assault

Date: 2026-07-24

Strict verdict: **the complete fixed-`s=0`, slope-four R1648 physical leaf
branch between the collision-three D0 boundary and the terminal
`G:S, p=-1/50` boundary is now certified by a finite connected atlas.**
This is not a proof that the containing two-dimensional H1 component is
globally maximal, and it does not recover any unnamed historical
enumeration.

## First exact failures and correction

The requested translated `K4292` feasibility run exposed a false audit
dispatch before it exposed a dynamical failure.  Round141's collision-three
special audit assumes the cell contains the D0 double root at `x_star`.
That assumption is correct only for cell zero.  Every translated cell lies
strictly on the other side, so collision three must be tested as an ordinary
radius-four miss.

Round145 pins a separate wrapper that makes exactly this dispatch change;
the frozen Round141 and Round143 files were not edited.  With the corrected
ordinary-miss audit, `K4292` cells 0 through 7 passed the complete audit.
The broad final cell `[1,9/8]` reached collision 1,648 with the entire owner,
word, chart, homogeneity, incidence and radius-four history intact, but its
terminal C24 enclosure straddled `p=-1/50`.  This was a fail-closed interval
resolution limit, not an owner or itinerary change.  `K4293` still
straddled the same face; `K4294` was the first tested scale giving a strict
terminal classification.

The formal atlas therefore uses:

```text
K4292 indices 0..7:       lambda in [0,1]
K4294 indices 32..35:     lambda in [1,9/8]
one clipped cell:         lambda in [9/8, Round143 bracket upper endpoint]
```

These 13 cells meet at 12 exact shared lambda faces.

## What closed

Every regular cell independently replays all 1,648 collisions with:

```text
complete radius-four candidate universe
retained owner and official wall/word
strict event ordering and outgoing chart
H0_CENTRAL homogeneity
incidence rank 14
1,647 strict nonreturns and one strict terminal return
```

The clipped endpoint cell repeats the same complete owner, word, chart,
homogeneity and incidence audit.  Its first 1,647 stages are strict
nonreturns.  Its closed terminal box is deliberately unresolved only across
the `G:S, p=-1/50` face.  Round143 supplies the unique root and its strict
negative derivative, selecting the returning physical side.

Adjacent cells substitute the same exact lambda value and the same D0-root
generator into the same analytic initial leaf map and the same 1,648
selected-circle composition.  Their adjacency graph is a path, so no gap or
untracked itinerary switch remains.

An independent sensitivity propagation on all 13 cells proves:

```text
dp_1648/dlambda < 0
d[asin(t_1648)+asin(p_1648)]/dlambda < 0
```

Both `asin` arguments stay strictly in their domains.  Hence the connected
return image has no hidden internal extremum, and the endpoint difference is
the full adapted image span.

## Corrected versioned promotions

The certified connected branch promotes the formerly conditional Round143
data within the explicit corrected-v1 chain:

```text
connected interval ID:
round145-v1-connected-physical-r1648-leaf:a863f420f91c4975d892c454eb2ba7f62564186f44c93c7861fc7d894d4557c7

canonical x=sqrt(17)r level: 4290
canonical rank decimal SHA256:
2650e59e81848f02e30fabbce2cad195e95c979c8bc695a7f577ae9c73ffb1c9

normalized-t cross-check level: 4283
normalized-t rank decimal SHA256:
da159470a46c7e5b75b099dda6c7aa939daee12b72fb47b471f08f8ea549eaa0

corrected natural source short-cell k: 0
corrected source-cell ID:
round145-v1-source-short-cell:ef713d451631988da505537ae3fcd9e8d11ccf062efcf81cd86bec4404e53d29

corrected image-recut count:
18482025737079285768196755871683993421335748959797565447882270854942728583699291803410531

corrected image-recut registry:
round145-v1-image-recut-registry:ad828d2da71cf42c1b5fcee3245fcde0c40244f6436b505c19617f6c00be55db
```

The image ranks are exactly `0` through `count-1`.

## Independent assault

```text
atlas engine SHA256:    c2bf266f715a8214a13f87308dd9c982e520b39a2747e23fedc1c7955fd62c78
producer SHA256:        6deed0506b0105eee9ee9a89dd4c28ee9bed81aa4922586eb8b8005a433ba4c7
certificate SHA256:     5edac93e1425c72992ab671f3b3f7db269d236819ea3ad689b612505426ccec6
certificate result:     16cec30f7af9d3f0d278d0bae85f8dca4a9a3041186021fc7ebd1b9c43a7fc0b
secondary SHA256:       c96dd56c7070e6f35d3302245c2e132bbb061388c54ebc96880f232626af2192
secondary result:       b62020c00b861e1634ab7a4c77ce280aa48b29117d7fc096c39dfe39d4826108
verifier SHA256:        47f283b48bba7c28a94ca01fcaec0389728c1aebd90d31a1f8c36fca0ee4f255
verification SHA256:    6101a4bdd7e1bea160a6d1bf36f0b4be281d340f217eb87d38b47f9961fd24a8
verification result:    c94ebce061403c7c99f09f8db26391e0470fe66217ba922b58aec80061e655b0
```

The first pre-freeze cross-precision verifier correctly rejected whole-file
byte equality.  A persistent structural diff proved that the only native
differences were the 13 generation-precision-specific model-radius ledger
hashes; branch IDs had changed only because those evidence hashes had leaked
into the identity payload.  The final producer separates a precision-stable
atlas identity from those ledgers.  The verifier now requires every other
field to be byte-exact across 8,192 and 12,288 bits, while a distinct
8,192-bit hash-seed replay must be wholly byte-identical.

The verifier rejected `48/48` semantic mutations, `7/7` cross-precision
contract mutations, `9/9` strict-JSON attacks and `7/7` hostile output
paths.  Separate harnesses
rejected `18/18` producer output paths and `13/13` combined malformed
JSON/file-shape inputs.

## Strict remaining global door

Round145 closes the finite connected physical leaf corridor and makes its
corrected-v1 local ranks and recuts executable.  It does not supply the
historical dyadic-enumeration crosswalk, the historical maximal Round27
component certificate/ID, the Round35 owner/restriction chain, or the
remaining downstream global objects.

```text
historical component rank / ID:     null
historical source interval rank:    null
historical short-cell k:            null
historical parent-W ID:             null
historical image-recut rank:        null
historical restriction ID:          null
global complete 18-field blocks:       0
Gate5 maturity:                    10/18
Gate5:                    NOT_CERTIFIED
CM2:                   NO-GO_FOR_CLAIM
```
