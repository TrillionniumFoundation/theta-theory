# CM2 Gate 5 Round-41 physical parameter-jet assault

Date: 2026-07-19  
Status: **physical rank-indexed `U/V/W` and all-five-face base parameter jets
are certified; the same-law arbitrary-path moment and global F10 remain open**

## One-step physical jets

In the fixed gauge, only the relative horizontal center displacement of
unlike colours depends on `s`.  Same-colour branches have zero parameter
jets.  For a cross-colour ray-circle branch, write

```text
a=u dot d,
w=u_perp dot d,
q=sqrt(R^2-w^2)=R*c,
tau=a-q.
```

Using

```text
R>=4/25, R<=9/25, kappa<=25/4, tau<3,
1/cp_source,1/cp_target<=2^B,
```

an exact rational majorant gives

```text
|tau_s|<=2/c,
|tau_ss|<=25/(4c^3),
|tau_xs|<=575/(2c^3),
|theta_s|<=75/(4c),
|theta_xs|<=155625/(8c^3)<20000/c^3,
|theta_ss|<=625/(16c^3)<40/c^3.
```

Both Birkhoff output coordinates are dominated by the target-normal-angle
bound.  Therefore every regular one-step branch has the physical envelopes

```text
U(B)=20*2^B,
V(B)=20000*2^(3B),
W(B)=40*2^(3B),
```

with `U=V=W=0` on same-colour branches.

## Base-face parameter jets

For a tangent/owner face use the raw discriminant level

```text
G=Delta_T=R_T^2-w_T^2.
```

The same geometry yields

```text
|G_s|<18/25,
|G_xs|<97/2,
|G_ss|<=2.
```

An integer-corner ray has bounds `(1,7,0)`.  Target/source coordinate,
homogeneity and velocity-zero levels are stationary in their moving collision
charts.  Together these rows cover the seven frozen one-step boundary kinds
and all five Gate-5 face grammars:

```text
source core,
intermediate core preimage,
terminal core preimage,
collision singularity/owner change,
moving occurrence.
```

Thus the missing one-step `U/V/W` inventory and all base-face parameter jets
from Round 40 are now installed.

## Exact global obstruction

The strongest existing one-time physical incidence moment is

```text
integral 2^(3B/2) dmu_s < 134217735/64.
```

It cannot imply integrability of the new coarse `2^(3B)` mixed/second jet.
The exact law

```text
P(B=b0+2k)=(15/16)*16^-k
```

has finite `2^(3B/2)` moment (ratio `1/2`) but divergent `2^(3B)` moment
(ratio `4`).  Consequently the Round-39 occurrence seed `L^(3/2)` bound does
not automatically survive an arbitrary rank-path pullback.

The first remaining Gate-5 payload is now precisely one of:

1. an anisotropic/cancellation estimate reducing the effective mixed-jet
   rank exponent;
2. or a stronger joint spacetime rank-path tail for the nonlinear
   `D/H/P/Q/S` recurrence;
3. together with same-law denominator and component-persistence margins.

## Strict boundary

No arbitrary-`R_n` global F10 field, F12/F13, strong cemetery, F14--F18 or
complete operator block is promoted.  Gate-5 maturity remains `7/18` with
zero complete 18-field blocks.

## Validation

The certificate replays the fixed-gauge law, first-order incidence rank,
F9 path envelopes, exact rational ray-circle arithmetic, seven-face join and
the dyadic countermodel.  Its verifier rejects `28/28` hostile mutations and
exits fail-closed by default.

Artifacts:

- `deliverables/cm2_gate5_round41_physical_parameter_jet_envelopes_cert.py`;
- `deliverables/cm2_gate5_round41_physical_parameter_jet_envelopes_verifier.py`;
- `deliverables/cm2-gate5-round41-physical-parameter-jet-envelopes-manifest-2026-07-19.json`.
