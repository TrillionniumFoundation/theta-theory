# CM2 Gates 3/4/5: occurrence-core-component-slot join

Date: 2026-07-18 (Asia/Shanghai)  
Mode: append-only, fail-closed, exact canonical-key joins  
Strict verdict: **all 64 occurrence owners and 128 parameter-side cylinders
are now joined through their 14 destination cores to 14 selected roof-one
components and the existing field-1/3/4/7 objects.  This removes a frozen
incidence/typing gap but adds no new Gate-5 field and does not construct a
common strong-space operator or physical Kac closure.**

## 1. Exact destination join

The join uses the canonical physical key

```text
[core.chart_id, core.target_id, core.crossings, len(core.crossings)+1].
```

For every occurrence, hit and miss sides have the same strict destination
core.  The exact registry is

```text
occurrence owners:                                  64,
parameter-side first-stopping cylinders:           128,
distinct destination cores:                         14,
distinct selected maximal components:               14,
destination component roof:                    1 on 14/14,
destination roof level:                        j=0 on 14/14.
```

The number of occurrence owners landing in each destination core has
histogram

```text
multiplicity 1: 2 cores,
multiplicity 2: 4 cores,
multiplicity 5: 1 core,
multiplicity 6: 3 cores,
multiplicity 7: 1 core,
multiplicity 8: 3 cores.
```

## 2. Existing Gate-5 object bindings

Every one of the 14 destination components is bound to

```text
field 1: selected homogeneous component row entity,
field 3: immutable homogeneous-prefix-chart slot,
field 4: immutable homogeneous-suffix-chart slot,
field 7: immutable one-step-cut-growth slot.
```

Field 1 has no independent `slot:` ID; the verifier rejects fabrication of
one.  Fields 3 and 4 are regenerated by their certificate formulas and
checked against the frozen chart-slot registry; field 7 is joined directly by
component, homogeneous-row and roof-level IDs.

This is a binding of four already certified fields, not a promotion beyond
the current `4/18` maturity.

## 3. Independent typing axes and single charge

Three axes with equal-looking counts are kept distinct:

```text
parameter side:       hit / miss actual-parameter branches,
recovery orientation: fw / rev alternative nonadditive views,
Kac coordinate:       dot(S)h / dot(r)*mu(h) linear outputs.
```

They are not positionally zipped and no `2x2x2` cross-product record is
materialized.  The charge owner is the occurrence, with exactly one

```text
q_e=max(C_fw(e),C_rev(e),2)*m_e
```

expression.  The forward/reverse views share that charge rather than each
receiving one; the two singular Kac coordinates share the same `m_e` and
mark `[+1,-1]`.

## 4. Strict boundary

```text
OCCURRENCE / PARAMETER / CORE / COMPONENT JOIN: 64/128/14 CERTIFIED
DESTINATION FIELD-1/3/4/7 BINDINGS:                  14 CERTIFIED
PARAMETER / RECOVERY / KAC AXES DISTINCT:       CERTIFIED TYPED
q CHARGE MULTIPLICITY PER OCCURRENCE:                 1 CERTIFIED TYPED

NEW FIELD PROMOTIONS:                                 0
SELECTED COMPONENT MATURITY:                       4/18
COMPLETE KEY HOMOGENEITY TABLES:                      0
COMPLETE 18-FIELD OPERATOR BLOCKS:                    0
PHYSICAL FW/REV RECOVERY CARRIER JOIN:           NOT CERTIFIED
COMMON TWO-VIEW RESTRICTION ID:                  NOT CERTIFIED
COMMON STRONG-SPACE OPERATOR:                    NOT CERTIFIED
THREE NORM INTERTWINERS / COMPLETE PHYSICAL KAC: NOT CERTIFIED
GATE 3 / GATE 4 / GATE 5:                       NOT CERTIFIED
```

## 5. Replay

```bash
PYTHONPATH=deliverables .venv-neurips/bin/python \
  deliverables/cm2_gate345_occurrence_core_component_slot_join_verifier.py \
  --replay

PYTHONPATH=deliverables .venv-neurips/bin/python \
  deliverables/cm2_gate345_occurrence_core_component_slot_join_verifier.py \
  --self-test
```

Replay and integrity pass.  The verifier rejects `46/46` hostile mutations;
live mode exits `2` because the composite gates remain open.
