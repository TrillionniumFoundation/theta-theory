# CM2 Gate-4/5 induced strong-coefficient obstruction assault

Date: 2026-07-18  
Status: append-only strict leaf; no prior artifact modified.

## Frozen advance

The round-23 entrance join is now audited against the fixed-core Growth and
Gate-5 strong-space stacks:

- 64 occurrence owners and 128 hit/miss parameter branches land in 14
  destination cores and 14 selected roof-one components;
- the selected destination levels have exactly fields 1, 3, 4 and 7 bound;
- fields 5 and 6 remain 24 component-local seeds, with zero completed
  homogeneous roof-level slots;
- the selected-component maturity therefore remains exactly `4/18`, with 14
  missing completed fields and zero complete operator blocks;
- the recovery-view common restriction ID and physical four-term Kac typing
  remain `NOT_CERTIFIED`.

This closes a type audit that the entrance incidence is real but is not yet a
common fw/rev recovery-carrier or strong-space operator join.

## Exact open-to-induced obstruction

The frozen scalar coefficient

\[
b_{\rm core}=\frac{720269600000}{720626832337}
\]

belongs to the open operator

\[
O=L M_C.
\]

The required first-return and survivor operators instead have signatures

\[
R_n=M_C L(M_{C^c}L)^{n-1}M_C,
\qquad
Q_n=(M_{C^c}L)^nM_C.
\]

The producer replays an exact two-state finite model with basis
`(delta_core, delta_exterior)`, `L` swapping the two states, and exact core
and complement projections. It obtains

\[
O^2=0,
\qquad
R_2=M_C,
\qquad
\|O^2\|_{\ell^1}=0,
\qquad
\|R_2\|_{\ell^1}=1.
\]

Thus even an exact power bound for `O` does not formally imply the same bound
for `R_n`. This is a logical operator-signature countermodel, not a
counterexample to the CM2 dynamics.

The four frozen local CM2 boxes confirm that the semantic distinction is
active in the current data: their first positive return times are
`545, 649, 1531, >2018`, so all four are strictly outside the core at the
first post-core step. Repeated open powers clip such excursions; the induced
operator must retain them through the complement until their first return.
Consequently neither `b_core^2018<3/8` nor the scheduled scalar dwell ledger
can be installed as an induced Lasota--Yorke coefficient.

## Exact missing interface

The machine ledger freezes 13 required records with zero complete records:

1. complete collision-SRB source partition;
2. raw first-return branch rows for `R_n` and `Q_n`;
3. every preterminal complement guard;
4. homogeneity and hidden-recut margins;
5. exact branch source masses `m_n`;
6. unstable Jacobian and inverse;
7. accumulated log-Jacobian distortion;
8. regular-density, standard-family, flux-face and dynamic-test costs;
9. common fw/rev restriction ID and carriers;
10. physical four-term Kac typing;
11. singular-cemetery and survivor outer masses;
12. weighted excursion tail `sum_{tau>n} q_tau <= C rho^n`;
13. the induced common-space contraction coefficient, compatible with
    `rho*exp(A_loss)<1`.

The required conservation identity remains

\[
\text{source mass}
=\sum\text{first-return mass}
+\text{singular-cemetery mass}
+\text{survivor mass}.
\]

## Strict verdict

- round-23 entrance incidence join `64/128/14`: `CERTIFIED`;
- selected destination field maturity: `4/18`;
- field-5/6 seed-to-slot promotion: `NOT_CERTIFIED`;
- open-to-induced coefficient nonimplication: `CERTIFIED`;
- induced common strong coefficient: `NOT_CERTIFIED`;
- Gate 4: `NOT_CERTIFIED`;
- Gate 5: `NOT_CERTIFIED`.

## Reproduction

Use `.venv-neurips/bin/python` with `PYTHONPATH=deliverables`:

```text
cm2_gate45_induced_strong_coefficient_obstruction_verifier.py --integrity-only
  REPLAY_AND_INTEGRITY: PASS

cm2_gate45_induced_strong_coefficient_obstruction_verifier.py --replay
  REPLAY_AND_INTEGRITY: PASS

cm2_gate45_induced_strong_coefficient_obstruction_verifier.py --self-test
  SELF_TEST: PASS (57/57 mutations rejected)

cm2_gate45_induced_strong_coefficient_obstruction_verifier.py
  exits 2 (strict live non-promotion)
```

The shortest valid next certificate is therefore not another scalar reuse of
`b_core`. It is a raw physical `R_n/Q_n` ledger carrying guards, Jacobians,
`m_n`, `q_n`, the four strong-space costs, common restriction IDs, and
cemetery/survivor payloads.
