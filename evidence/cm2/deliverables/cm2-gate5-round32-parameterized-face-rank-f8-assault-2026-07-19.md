# CM2 Gate 5 round 32: parameterized connected faces and numeric F8

Date: 2026-07-19  
Status: **connected rank and F8 certified on actual parameterized faces**

The fixed parent-W tangent is `(1,4)`. Every nonvertical physical boundary
branch is stable-oriented with slope `m<=-25/9`; vertical and horizontal
coordinate faces are treated separately. Therefore every physical carrier
family intersects one parent W in zero or one simple root. Every nonempty
intersection has connected rank zero and two regular one-sided traces; corners
and simultaneous roots retain cemetery typing.

For a stable graph, the normalized wedge is

```text
abs(4-m)/(sqrt(17)*sqrt(1+m^2)).
```

Its infimum for `m<=-25/9`, including the vertical limit, is `1/sqrt(17)`.
Since `1/17>1/25`, all five physical face grammars receive the common strict
F8 lower bound `1/5`. Horizontal faces are even stronger at `4/sqrt(17)`.

The 64 moving occurrence seeds and 128 hit/miss traces retain the corrected
coarea-density upper `18/5` on their parameterized rank-zero instances. A
numeric coarea regularity bound for every other physical face kind, and the
complete physical F9 C2 atlas, remain absent.

Global Gate-5 maturity advances from `5/18` to `6/18`; Gate 5 and CM2 remain
open.
