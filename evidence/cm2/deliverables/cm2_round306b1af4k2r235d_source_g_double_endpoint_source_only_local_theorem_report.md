# CM2 Round306B1AF4K2R235D — double-endpoint source-only local theorem

## Verdict

`PASS_16_ROW_SOURCE_ONLY_LOCAL_THEOREM__OLD_R248_R306A_B0_B1G0_AF2_K2I0_K2I1_K2I2_K2I3_K2I4_BINDINGS_SUPERSEDED__FRESH_REFREEZE_REQUIRED__ZERO_GLOBAL_CREDIT`.

Exactly `16` old Round235 double-endpoint rows now have an input-bound local
theorem.  On `64` rational cells the target factor is uniformly nonzero
(`8` positive rows and `8` negative rows), while the source factor is exactly
`(9/25)t`, has derivative `9/25`, and vanishes only on the endpoint face
`t=0`.  This gives exactly `16` local source-only endpoint graph/key rows.
K1 contributes only `16` distinct input-bound zero-credit cross-check
receipts.

This package does not produce a corrected global universe.  It proves that
selected identities in the old freeze cannot survive the corrected local
partition and records the exact replay obligations.

## Bound downstream impact

- invalid old members: `32` = `16` TARGET_SHEET + `16` target-only side;
- the `16` same-sign side members shared with a valid source reference survive,
  while their target references are removed;
- all `48` affected target-feature R306A rows are closed-row joined;
- old base-root dispositions: `16` DELETE_ROOT + `4` REKEY_ROOT;
- old B0 component impacts: `16` singleton components vanish and `4` retained
  components require replay;
- those four components contain `21,864` old members; after removing the
  `16` invalid G2A sheets, `21,848` survivors require component rebind;
- the six-family survivor census is PRESERVED `14,400`, NON_GRAPH `1,776`,
  R2 `800`, R292 `3,776`, G2A `584`, G2B `512`;
- the survivor lane census is I0 `3,776`, I1 `800`, I2 `16,176`, I3 `1,096`.

The I0/I1/I2/I3 manifests and results and the I4 manifest/result/member stream
are explicitly pinned and replayed.  Their old component bindings are
superseded, so fresh I0–I4 replay is required.

The removal-only/no-split numbers (`564,460` members, `92,672` components,
`158,820,108,554` cross-component pairs, and related support counts) are
marked prediction-only and non-minted.  No package-wide invalidation is
claimed; only the pinned results and selected ledger rows named in the result
are declared non-reusable, and unhashed package members are not asserted
invalid.

## Verification

Two fresh producer processes generated byte-identical result and ledger
digests.  The independent verifier does not import or execute the producer or
K1.  It rebuilt all local theorems and downstream joins from frozen held-file
descriptors, compared the complete result object and all `16` ledger rows, and
passed `27/27` coherent attacks, including cross-certificate target-graph and
shared-survivor substitutions.

Frozen core hashes:

- producer: `d6d78b8ffb60c52a70294a83ff575c9d45efac4a1e618743e96ff503a15e7423`;
- row ledger: `6eaeee6a158ad7c0c3d62380214e1ae24ac2a63cb7678b312401cca91cb4cfe0`;
- result file: `3856a04a5b88574f1ffd6aac7f4fda42c014eac401c2a73efbc6c0ea10c9e5ff`;
- result object: `abe7b63dc02f5d3772fa50add64030d3822b3832a33df8a05fcf98926b0dfbbe`;
- independent verifier: `cc80855562d8ea8022881314647e6fe974ea8060b41e2c49c8ab48a28ac18559`;
- attack suite: `238309c6fea17edbcbe383e966383e57f8dd9bf888baff152bd09a0a28cd5236` (`27/27`);
- verification file: `865f2aab8fab721aa24c0772d516806968555a28eb5529056e3cb6438d9d1468`;
- verification result: `b0b43f4acb3da02e9e4615e2670dfaeab511a3437b2449c834d68bf0faa46867`.

## Mandatory next step and nonpromotion

Regenerate R248 from the corrected partition; then perform a fresh forward
and reverse replay of all `478,718` legal edges, refreeze B0, and replay B1G0,
AF2, and K2I0–K2I4.  Until those steps pass, normalized support, physical
incidence/equivalence, representation pullback, G2, B1A, B2, maximality,
fibre, corrected-universe/DSU, and CM2 credits are all exactly zero.  CM2
remains `NO-GO_FOR_CLAIM`.
