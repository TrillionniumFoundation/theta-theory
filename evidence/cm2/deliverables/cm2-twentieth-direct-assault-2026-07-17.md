# CM2 twentieth full direct assault

Date: 2026-07-17 (Asia/Shanghai)  
Frozen baseline: the first nineteen rounds remain read-only; this pass is
append-only  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; fully closed
composite gates remain `0/5`.  The actual moving-parameter transverse
Jacobian is now exact on all 64 open maximal occurrence rows and 64 selected
positive-width patches carry 128 two-sided all-scale parameter germs.  A
separate exact period-two orbit proves that the frozen 24-core union is not a
global absorbing trap, forcing a core/cemetery or positive-fraction recovery
interface.  Gate 5 selected maturity advances from `2/18` to `4/18` by
materializing fields 3 and 4 on all 28 selected roof levels.**

## 1. Executive result

- **Gate 1:** unchanged at the nineteenth common-plaque `2x2` transport
  reduction.  No common stable/unstable frame, uniform `q>=q_*`, or selected
  twisting in that same representative is promoted.
- **Gate 2:** unchanged at `0/17`.  Collision recovery carriers and component
  charts still do not construct the stable-saturated base, quotient branches,
  reverse weights, endpoint packet, native stopping, or PPE drift.
- **Gate 3:** the phase-angle proxy is replaced on every open maximal row by
  the exact fixed-coordinate physical parameter derivative.  One certified
  positive-width patch per occurrence supports hit/miss germs for every
  `0<|h|<=2^-16`, with exact collision-time offset one and a common regular
  suffix chart.  A full-row tubular atlas and bounded common-strong-space
  grazing pushforward remain absent.
- **Gate 4:** the proposed deterministic route "every regular state reaches
  the frozen 24-core" is rigorously false.  The exact centered QNL period-two
  orbit avoids all 24 rectangles forever.  Recovery must therefore be typed
  by a measurable core/cemetery stopping ledger or by an unnormalised
  positive-fraction theorem.  No quantitative cemetery payload or native
  no-recut dwell is yet certified.
- **Gate 5:** fields 3 and 4 are now physical immutable slots on all 28
  selected roof levels.  Together with fields 1 and 7, selected minimum
  maturity is `4/18`.  Complete key homogeneity tables and complete 18-field
  blocks remain zero.

## 2. Gate table

| gate | strongest twentieth-round layer | exact remaining core gap | status |
|---|---|---|---|
| 1 | common-plaque transport reduction retained | shared determinant-one frame, `q>=q_*`, same-representative twisting | **OPEN** |
| 2 | physical recovery/component IDs retained | stable quotient base, `pi^s`, `rho`, `p_a`, endpoint/stopping/PPE packet; `0/17` | **OPEN** |
| 3 | exact parameter-angle Jacobian on 64 rows; 64 selected all-scale patches and 128 parameter germs; selected `FACE_TIME=1` | full boundary tubular atlas and bounded strong `DQ/MT_DQ/FACE` maps | **OPEN** |
| 4 | global frozen-core absorption refuted; correct core/cemetery interface identified | selected-germ stopping ledger, cemetery payload, `2018/12108` no-recut dwell, common norm | **OPEN** |
| 5 | fields `1,3,4,7` on all 28 selected roof levels; selected maturity `4/18` | field 2 full tables, fields `5–6,8–18`, three norms and physical Kac | **OPEN** |

## 3. Exact parameter-DQ transverse coordinate

Fix the source collision coordinate and translate the physical white centre
by `h`.  In the tangent outgoing frame, every candidate centre obeys the
exact affine identities

```text
ell_j(h)=ell_j+eta_j u_x h,
w_j(h)=w_j-eta_j u_y h.
```

For the tangent target `w_T=epsilon R_T`, so its collision discriminant is

```text
Delta_T(h)=2 eta epsilon R_T u_y h-u_y^2 h^2.
```

The exact first-order identification between physical parameter displacement
and signed clearance angle is

```text
d alpha/dh = eta u_y/ell_T.
```

Consequently the tangent graph and signed face coarea satisfy

```text
dp_tangent/dh       = -eta cp u_y/ell_T,
signed face coarea  =  eta epsilon cp u_y/ell_T.
```

Because the collision coordinate is `dr dp`, the positive face law contains
one, not two, powers of `cp`:

```text
R_source cp |u_y|/ell_T dtheta.
```

The earlier `cp^2` density is explicitly superseded and is not used in this
chain.

## 4. Selected all-scale physical germs

For each of the 64 maximal occurrence rows, adaptive 512-bit Arb replay
selects one positive-width base patch and proves both physical branches for
every

```text
0<|h|<=1/65536.
```

The physical sign convention agrees exactly with the corrected coarea
polarity:

```text
hit:  sign(h)=coarea polarity,
miss: sign(h)=-coarea polarity.
```

The complete local first-hit comparison gives

```text
hit:  source -> tangent target -> fixed miss target,
miss: source ------------------> fixed miss target.
```

Thus the selected physical `FACE_TIME` offset is one.  Both branches land in
the same regular suffix chart with successor `cos(phi)>1/20`,
`|p|<999/1000`, and every owner flight below two.

Adaptive base half-width counts are

```text
2^-10: 40,
2^-11: 16,
2^-12:  4,
2^-14:  4.
```

The 64 labelled base patches have coordinate area
`2833/16777216`; both parameter sides through radius `2^-16` have labelled
coordinate volume `2833/549755813888`.  These are occurrence-sheet
coordinate quantities, not invariant mass.

The Jacobian formula is global on every open maximal row, but the certified
Arb tubes cover only one selected base patch per occurrence.  Endpoint
collars and the remaining row interiors are not yet one full tubular atlas,
so no global strong-space pushforward is inferred.

## 5. Fixed-core absorption obstruction

The frozen contracting registry consists of

```text
8 axis cores:       1/100 <= t <= 1/50,
16 diagonal cores: 69/100 <= |t| <= 7/10.
```

Their union has positive normalized collision-SRB mass, but positive mass is
not global absorption.  At `s=0`, the centered normal branch is the exact
period-two physical word

```text
G(0,0) -> W(0,0) -> G(0,0),
```

with phases

```text
G: n=( 1/sqrt(2),  1/sqrt(2)), p=0,
W: n=(-1/sqrt(2), -1/sqrt(2)), p=0.
```

Both have `|t|=1/sqrt(2)>7/10`, since
`1/2>(7/10)^2=49/100`.  Therefore neither phase enters any frozen core,
independently of half-open seam ownership, and periodicity gives

```text
tau_core(QNL)=infinity.
```

The orbit has zero collision-SRB mass, so it does not provide a cemetery-tail
estimate.  It does decisively reject an all-state first-core lemma.  The
correct physical interface is

```text
tau_C(x)=inf{n>=0:T^n x in C},
cemetery={tau_C=infinity},
```

together with either a charged core/cemetery tail or a record-preserving
positive-fraction unnormalised recovery theorem.  The counterexample is not
claimed to lie in one of the 128 selected germs, so selected-germ core
reachability remains open rather than refuted.

## 6. Gate 5 selected chart fields

Every selected maximal component already fixes a physical word, source
collision chart, target open-semicircle chart, and each intermediate oriented
transparent-wall chart.  Joining these defining predicates to all selected
roof levels materializes

```text
field 3: homogeneous_prefix_chart,
field 4: homogeneous_suffix_chart.
```

Exact counts are

```text
selected maximal components: 24,
roof-one components:          20,
roof-two components:           4,
selected roof levels:          28,
field-3 slots:                 28,
field-4 slots:                 28.
```

Each selected level now contains fields `1,3,4,7`, hence

```text
selected component-level maturity: 4/18,
remaining fields per selected level: 14,
complete key homogeneity tables: 0,
complete 18-field blocks: 0.
```

Universal inverse-Jacobian and log-distortion packets are joined to all 24
component IDs only as component-local field-5/6 seeds.  They are not counted
as completed roof-level fields, and first-derivative wall-chart bounds are
not silently promoted to logarithmic distortion.

## 7. Strict final boundary

```text
COMPOSITE GATES FULLY CLOSED:                         0/5
UNCONDITIONAL CM2:                                    NO-GO FOR CLAIM

PARAMETER-ANGLE JACOBIAN ON 64 OPEN ROWS:             CERTIFIED
SELECTED ACTUAL-PARAMETER ALL-SCALE PATCHES:           64 CERTIFIED
ORIENTED ACTUAL-PARAMETER GERMS:                      128 CERTIFIED
SELECTED PHYSICAL FACE-TIME OFFSET:                     1 CERTIFIED
EXACT QNL ORBIT AVOIDS FROZEN 24-CORE:                CERTIFIED
GLOBAL ALL-STATE FIRST-CORE ABSORPTION:               REFUTED
SELECTED FIELD-3 PREFIX CHART SLOTS:                   28 CERTIFIED
SELECTED FIELD-4 SUFFIX CHART SLOTS:                   28 CERTIFIED
SELECTED COMPONENT-LEVEL MATURITY:                    4/18

COUPLED HALF-DENSITY FRAME PLUS TWISTING:             NOT CERTIFIED
STABLE QUOTIENT / PHYSICAL PPE:                       NOT CERTIFIED
FULL-BOUNDARY ALL-SCALE TUBULAR ATLAS:                NOT CERTIFIED
BOUNDED STRONG DQ / MT_DQ / FACE:                     NOT CERTIFIED
SELECTED-GERM CORE/CEMETERY LEDGER:                   NOT CERTIFIED
QUANTITATIVE CEMETERY PAYLOAD:                        NOT CERTIFIED
NATIVE 2018/12108 NO-RECUT DWELL:                     NOT CERTIFIED
COMPLETE KEY HOMOGENEITY TABLES:                       0
COMPLETE 18-FIELD OPERATOR BLOCKS:                     0
THREE NORMS / PHYSICAL KAC:                           NOT CERTIFIED
```

## 8. Next direct attacks

1. **Full-row parameter tubular atlas:** cover every maximal-row interior and
   endpoint collar by compatible parameter charts, then prove the grazing
   pushforward is bounded in one common strong space rather than merely on 64
   selected patches.
2. **Branchwise core/cemetery ledger:** propagate the 128 selected germs with
   validated adaptive subdivision to their measurable first-core time or a
   typed cemetery record.  Seek a charged tail or positive-fraction
   unnormalised recovery, not global absorption.
3. **Native no-recut dwell:** on every recovered charged branch, certify no
   hidden singular cut over the `2018` shell dwell and `12108` raw field-7
   dwell, then bind the existing scalar Green constants to physical carrier
   IDs.
4. **Gate 5 field expansion:** construct field 2 as the complete physical
   homogeneity subbranch table, promote fields 5 and 6 to roof-level objects,
   and then bind face/coarea/moving-DQ fields 8–13.
5. **Remaining structural closure:** solve the Gate 1 common frame separately,
   construct Gate 2 stable quotient/PPE, and close fields 14–18, the three
   norm intertwiners and four physical Kac terms.

The highest-leverage next line is the full-row tubular atlas followed by the
branchwise core/cemetery ledger.  This preserves the exact parameter-DQ gain
while respecting the newly proved failure of global 24-core absorption.

## 9. Reproduction

Three new fail-closed verifiers support replay/integrity, hostile self-tests,
and live mode.  They reject `23/23`, `20/20`, and `21/21` hostile mutations,
respectively: `64/64` new rejections and `260/260` cumulatively.  All three
replays pass, every live verifier deliberately exits `2` because no composite
gate closes, and all individual plus recursive aggregate SHA-256 sidecars
verify.

No external theorem is used in this round.  Every promotion above is derived
from exact algebra, rational inequalities, or validated Arb replay against
the frozen local geometry.
