# CM2 Round 109 — registered-arc immutable whole-tube reaudit

Round109 closes the registered-arc audit asymmetry left after the Round98–100 immutable-candidate correction.  Every `REGISTERED_PHYSICAL_ARC` link in the corrected Round102 rank-3 tangent-face quotient is replayed over its complete IFT tube with an immutable 57-candidate tuple.

## Certified census

- Corrected registered physical arcs: **52/52**.
- Remaining unaudited registered arcs: **0**.
- Certified whole-tube strips: **41,984**.
- Strip census: `128 × 8`, `512 × 8`, `1024 × 36`.
- Dependent collar depth census: `12 × 24`, `16 × 28`.
- Complete immutable tuple replays: **41,984**.
- Translated candidate equations: **2,393,088**.
- Non-target competitor equations: **2,351,104**.
- IFT axis: `t_as_function_of_p` on **52/52** arcs.

Every strip certifies the first two owners, the designated third-tangency event, all 57 translated candidates, absence of an earlier competitor, the IFT derivative sign, and opposite boundary signs.  The candidate generator is converted to a tuple before any membership test or iteration, preventing the generator-consumption defect found in Round98.

## Exact scope boundary

This is a certificate for the **tangent-face whole tube only**.  It does not certify either transverse `D > 0` or `D < 0` adjacent-cell collar.  It creates no homogeneity child, no official word-path join, and no F1–F6 operator slot.

In particular, a HIT collar whose closure reaches third-collision grazing still meets a countable family of high homogeneity strips accumulating at the trace.  The finite IFT subdivision used here is not that transverse homogeneity decomposition.

The 56 immutable gap links and the exterior endpoint collars remain outside Round109.  Global Gate5 remains `10/18`, complete blocks remain zero, and CM2 remains `NO-GO_FOR_CLAIM`.

## Independent verification

The verifier does not import the Round109 producer.  At **640 bits**, it reconstructs each stored fixed grid directly from the two registered endpoint roots and the frozen `(strip_count, dependent_collar_depth)` pair, then replays all candidate equations.

- Reconstructed arcs: **52/52**.
- Reconstructed strips: **41,984/41,984**.
- Immutable tuple replays: **41,984/41,984**.
- Candidate equations: **2,393,088/2,393,088**.
- Semantic projection digest: `489d769d56a400d9afa38906149aaf7a44c98e92d3970ed9c9f0ab3407e392b4`.
- Hostile semantic mutations rejected: **6/6**.
- Duplicate/nonfinite/unknown-key JSON attacks rejected: **3/3**.
- Producer module imported: **false**.

## Next lawful assault

1. Export and independently side-split all 56 immutable gap tubes.
2. Construct transverse endpoint collars for the eight exterior rays.
3. Build root-centered HIT collars in `(eta,c3)` coordinates.
4. Freeze the countable `H_{sigma,k}` child schema, half-open ownership, official path join, and three-step canonical recuts.
5. Install F5/F6 only per actual homogeneous child, then continue F7–F18.
