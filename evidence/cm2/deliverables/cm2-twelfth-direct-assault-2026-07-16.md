# CM2 twelfth full direct assault

Date: 2026-07-16 (Asia/Shanghai)  
Frozen baseline: v51/v52 and the first eleven assault rounds were preserved  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; composite
gates fully closed remain `0/5`**

## 1. Executive result

This pass advances every gate while preserving each missing interface.

- Gate 1 now has one compactly supported `C^{1,alpha}` logarithmic gauge on
  the QNL return section, all-pairs canonical holonomies separately on the
  declared sufficiently local stable and unstable plaques, exact propagation
  over every registered finite word, and existential typed endomorphisms for
  regular phase-aligned homoclinic points in the clean `F=T^2` cyclic
  component.  No
  selected immutable homoclinic orbit, numerical twisting loop, global
  all-plaque Hoelder family, class `H`, or PPE follows.
- Gates 2/5 now contain at least 24 rigorously nonempty physical return keys
  of positive collision-SRB mass.  On each of those 24 keys only the
  full-key nonemptiness field is decided in the 18-field schema; each compact
  core also has seven useful local seeds.  They are not a stable-saturated
  quotient.
- Gate 3 now has one fixed 64-row rectangular carrier over the full
  `|s|<=1/400` window and an exhaustive outer-atlas replay of all `4,704`
  future candidate discriminants.  It separates `626,274` immutable boxes,
  `6,246` strictly transverse candidate-root strips and `129,634` terminal
  unresolved boxes.  The nonphysical, equal-weight 64-row carrier has
  normalized `(t,v)` outer-cover area fraction `68501/2097152`; this is not
  a probability or singularity mass.  The positive fixed-`s` unresolved
  intercept blocks a complete root-isolated atlas.  Separately, candidate
  roots are not physical-first and the formal mark is not a current, while
  strong-space invariance needed for operator DQ is still absent.
- Gate 4 now has a one-step bound of at most 152 true-singularity cuts and
  153 continuity components, an all-physical-continuity-branch coordinate
  infinity `D2T` grazing envelope, and one-step log-`r`-Jacobian distortion
  on the 128 certified carriers.  It also has an exact finite-cemetery
  resolution/cost obstruction on normalized `u` under the leafwise
  inverse-mass interface.  A schema audit proves that the present aggregate
  ledgers cannot identify the missing weighted Growth sum.  The first global
  executable crude upper proof bound is greater than one, so no contraction
  or propagated `q` is promoted.

## 2. Gate table

| gate | strongest newly certified layer | exact remaining hard gap | strict status |
|---|---|---|---|
| 1. holonomy/QNL | compact determinant-one section gauge; local all-pairs limits; finite-word propagation and existential typed QNL homoclinic endomorphisms | select one immutable regular bi-infinite homoclinic orbit with physical itinerary and plaque-entry times and compute/bound `psi_z`; alternatively certify `J` plus an equality/error comparison to the 96-collision shadow; uniform all-plaque Hoelder holonomies, class `H`, PPE | **OPEN** |
| 2. physical PPE | 24 positive-mass physical return cores, with exact nonemptiness on their keys | stable-saturated product base, quotient `rho`, reverse weights, endpoint carrier, native stopping and PPE | **OPEN** |
| 3. global event/DQ | full-window 64-row finite rectangular future-candidate outer atlas; 626,274 immutable boxes and 6,246 strict candidate-root strips; uniform fixed-`s` artificial rectangular `t`-boundary and candidate-root outer charges | eliminate every terminal fixed-`s` unresolved interval, or analytically dominate the uniform positive intercept `64953/1280` on the same carrier; physical-first root/current typing, strong-source/operator DQ, branch-record `MT_DQ`, `FACE_2CUT/FACE_TIME` | **OPEN** |
| 4. same-occurrence `q` | `<=152` raw signed-tangency-sheet intersection upper bound and `<=153` components; physical-branch grazing envelope/carrier distortion; aggregate weighted-sum nonidentifiability; normalized-`u` finite-cemetery resolution--cost obstruction under leafwise `p_a^{-1}` charging | componentwise weighted join or an at-most-one-central-child theorem plus one single global `<=1/5` tail; invariant curves, unnormalised/reweighted-family recovery and numerical `C_p,vartheta_p,C_fw,C_rev,q` | **OPEN** |
| 5. fixed/full transfer | 24 rigorously nonempty positive-mass word cores; one completed full-key field and seven typed local seeds | maximal homogeneous word blocks, per-roof-level 18 fields, three CM2 lifts, full Kac and operator phase | **OPEN** |

## 3. Gate 1: compact section gauge and local all-pairs holonomies

Choose nested non-grazing QNL return neighborhoods

```text
U_core compactly contained in U_chart
```

and a smooth cutoff `chi` equal to one on `U_core` and zero on a chart
boundary collar.  With

```text
k=-325/(144 log(mu)),
t_s=chi k y^2 log|y|,
t_u=chi k x^2 log|x|,
B_hat=(I+t_u E_12)(I+t_s E_21),
```

the gauge is the identity outside the return chart, is `C^1` and
`C^{1,alpha}` for every `0<alpha<1`, and has determinant one.  The zero collar
removes any support seam.

Every point on the declared sufficiently local stable plaque of the QNL
point eventually remains inside `U_core`; every point on the corresponding
sufficiently local unstable plaque does the same under backward iteration.
The finite entry prefix is harmless, and the old exact resonant cancellation
supplies summable tails after entry.  Reference-point limits therefore
exist.  At every finite time,

```text
H^s_(x,y;n)=H^s_(p,y;n) (H^s_(p,x;n))^-1,
```

and the same identity holds backward on the unstable plaque.  Taking limits
gives all-pairs groupoid holonomies on those two local plaques.

Because the cutoff vanishes on a chart-boundary collar, this gauge is one
intrinsic bundle automorphism on the declared QNL return section.  On the
full collision section it is used on the selected QNL obstacle component and
the identity is used on every other disjoint obstacle component.  Write `T`
for the one-collision map and `C=DT`; every registered finite regular
collision word obeys

```text
C_hat^n(x)=B_hat(T^n x)^-1 C^n(x) B_hat(x).
```

All intermediate factors cancel and a closed collision word is changed only
by basepoint conjugacy.  For QNL holonomies use the phase-aligned return
`F=T^2`, which fixes the selected QNL phase, and its induced cocycle `A=C^2`;
the identical telescope holds for every regular finite `F`-word.  There is no
additional connector-chart gauge patch to construct at either finite-word
level.

The local limits also extend through a finite prefix whenever a genuine
global stable or unstable point enters the declared local plaque.  After
passing to the cyclic `F=T^2` component containing `p`, the clean basic-set
topology supplies regular phase-aligned QNL homoclinic points existentially,
and for each such point `z` the endomorphism

```text
psi_z=H^s_(z,p) o H^u_(p,z): E_p -> E_p
```

is therefore well typed.  This does not select or numerically identify one
such `z`.  The 96-collision shadow is instead periodic at a different fiber
`E_(z_*)`; putting its matrix in the same gray coordinate chart is not a
canonical fiber identification.  The first exact missing interface is

```text
SHADOW_TO_HOMOCLINIC_ORBIT_COCYCLE_IDENTIFICATION.
```

It first requires one immutable regular bi-infinite
`p`-tail/excursion/`p`-tail orbit, its physical itinerary and stable/unstable
local-plaque entry times, and a direct computation or bound for its actual
`psi_z` loop.  If the separate 96-collision shadow is used as a
surrogate, one must additionally certify a transport `J:E_(z_*)->E_p` and an
equality or gauge-covariant error bound comparing `psi_z` with `J L J^-1` so
that the four wedge signs transfer.
Thus there is still no selected twisting loop, uniform all-plaque Hoelder
family, Butler--Park class `H`, or physical PPE conclusion.  The frozen
natural codings remain outside fiber bunching.

## 4. Gates 2 and 5: first positive-mass physical key cores

Inside the frozen 441,280-key envelope, exact 384-bit Arb first-hit replay
certifies 24 distinct keys uniformly for `|s|<=1/400`:

```text
20 keys of roof 1,
 4 keys of roof 2.
```

They consist of eight axial translate cores and sixteen diagonal
two-obstacle cores.  All selected roots beat the complete retained candidate
list; transparent-wall records, corner avoidance, source collision charts
and target local-semicircle charts are strict.  On all cores,

```text
|p_in|,|p_out|<3/10,
cos(phi_in),cos(phi_out)>19/20.
```

The cores are pairwise disjoint in the regular collision section.  Since
`dr=R dtheta`, `dtheta/dt>=1`, their unnormalised collision mass is at least

```text
273/156250.
```

Dividing by `4*pi*(R_G+R_W)` and using `pi<22/7` gives the strict normalized
bound

```text
mu_collision(union cores)>147/550000.
```

Each compact core carries seven correctly typed local seeds: nonemptiness,
central homogeneity, endpoint source and target charts, collision-SRB
area-Jacobian identity one and log-area-Jacobian identity zero in `(r,p)`,
plus a local full-collision-branch/full-return `C^alpha` test-pullback seed
below 158 for `0<alpha<=1`.  They are **not seven completed 18-schema
fields**.  In particular, area Jacobian is not unstable-curve
Jacobian, a central subcore is not a maximal homogeneous word domain, and
the roof-two intermediate transparent-wall physical charts and levelwise
costs have not been constructed.  The symbolic ledger already contains 28
prefix/suffix roof-level pairs and 52 split slots, but those slots are not
physical operator blocks.  Only the full-key nonemptiness field is decided
on all 24 keys; complete 18-field operator blocks remain zero.

These are two-dimensional collision cores, not stable-saturated Young
rectangles.  They supply no stable projection, quotient density `rho`,
reverse weights, Gate-2 same-carrier endpoint maps/carrier, native stopping,
or PPE.  The prior `>0.903` unsaturated fraction belongs to a different
declared common rectangle and is unchanged.

## 5. Gate 3: finite-parameter future-singularity frontier

The new certificate fixes the common finite-parameter carrier

```text
A_fs = disjoint_union_(e=0)^63 ([0,1]_t x [0,1]_v),
s=(2v-1)/400,
```

where `t` is the trimmed inward-angle coordinate of a moving maximal row.
Two-variable first-order automatic differentiation with 384-bit Arb
intervals replays every one of the `4,704` next-collision candidate
discriminants.  Half-open parameter-slab ownership prevents double counting.
A terminal box is accepted only as immutable, as a strict single-root strip,
or as unresolved; no failed or ambiguous box is discarded.

The full materialization performs `1,507,924` strict interval audits and
produces

```text
immutable terminal components                 626,274
transverse candidate-root strips                6,246
terminal unresolved boxes                     129,634

immutable normalized (t,v) area          2028651/32768
root-strip normalized (t,v) area              921/8192
unresolved normalized (t,v) area             64817/32768
total carrier area                                      64

root-strip plus unresolved outer area          68501/32768
outer fraction of the 64-row carrier       68501/2097152
```

Every retained root strip has `t` width at most `1/1024`, a strict
`partial_t` sign and opposite vertical-edge signs, hence exactly one analytic
candidate root on each parameter slab.  The ordered row, work and fixed-s
slice digests are respectively

```text
8c202faa1491cdca7351bd3ff9d0402f23504ff0eb3d8181ee20b72f0e810a28
4817377eb109fd82d228a29a96a82aa3eab992554236a14fd11d7022512d9afc
b17617ec5e766e3575aac2ab5210ab12c35f31787279008602902ec79bd50b2c
```

The normalized `v` coordinate is a parameter coordinate, not a probability
law.  Thus all two-dimensional `(t,v)` areas, perimeters and graph tubes are
only parameter-integrated bookkeeping.  Neither the outer area/fraction nor
any parameter-integrated tube is physical singularity mass or current, and
multiplying a two-dimensional `(t,v)` quantity by `126/5` does not make it
physical.  Only fixed-`s` supremum row-law outer charges are valid.  The exact
uniform slice sweep gives at most `32,295` artificial intervals, `129`
candidate-root graphs and `2,062` unresolved intervals, whose total
unresolved `t` width is at most `1031/512`.  Consequently

```text
artificial rectangular t-boundary row-law coefficient       3255336
candidate-root row-law coefficient                           32508/5
full future-candidate row-law outer charge
                         <=64953/1280 + (547092/5) rho.
```

The positive intercept `64953/1280` is the exact fail-closed boundary.  A
candidate root has not been typed as the physical-first collision boundary;
the formal two-point mark is not a physical current.  Hence this pass
certifies a finite full-window future-candidate **outer** atlas and uniform
fixed-`s` artificial rectangular `t`-boundary and candidate-root outer
charges, but not a complete finite-`s` root-isolated atlas, strong-space
invariance, operator DQ, branch-record `MT_DQ`, `FACE_2CUT/FACE_TIME` or
Gate 3.

## 6. Gate 4: global one-step complexity and distortion

The frozen 35,024-leaf horizon cover gives `tau<3` uniformly on
`|s|<=1/400`, and the first-hit reduction retains 448 of 1,296 source-target
rows.  Within that complete conservative retained chart-target universe, the
source-wise raw signed-tangency-sheet upper bounds are

```text
gray source: 2*76=152,
white source: 2*68=136.
```

These are conservative sheet-count bounds: the 448 rows are not a
materialized physical first-hit continuity partition, and no claim is made
that every retained sheet is simultaneously physical or intersects the
curve.  Each signed tangency sheet has slope below `-25/9`, while an admissible
unstable graph has slope above `25/9`; hence each graph meets each sheet at
most once.  One collision therefore has at most 152 true singularity cuts
and at most 153 continuity components.

Combining this with the certified one-true-branch homogeneity sum gives the
first global executable but deliberately crude bound

```text
Xi_1 <=153*(900337/901685)
     =137751561/901685
     =152.771268...>1.
```

This is a diagnostic, not a Growth contraction.  It shows that the unweighted
continuity-component bound cannot close Gate 4; a physical contraction-weighted
multiplicity ledger is required.

Exact differentiation of the circular Birkhoff matrix on every physical
continuity branch gives

```text
|partial_r DT_ij|   <18493/c_1^3,
|partial_phi DT_ij|< 2843/c_1^3,
||D2T||_infinity   <42672/c_1^3.
```

On the 128 already certified oriented occurrence carriers, with
`25/9<V<29` and `|V'|<4949`, the projected Jacobian `A/c_1` satisfies

```text
|d_r log|r_1,W'||<74750/c_1^2.
```

After target homogeneity cutting this yields a one-third log-`r`-Jacobian
constant `1024000` on high target strips `k>=41` and `6000000000000` as a
uniform constant including the central strip.  These constants are for the
one-step projected `r` Jacobian on those
128 carriers.  They are not Euclidean arclength distortion and not an
invariant curvature/distortion theorem for arbitrary iterated standard
curves.

## 7. Gate 4: the existing aggregates do not identify a weighted sum

The 35,024 horizon leaves and 448 retained chart-target rows live in
different schemas.  The former are ambient ray witnesses for `tau<3` and
penetration slack; the latter are a conservative candidate registry.
Neither contains a short-unstable-curve ID, physical continuity-component
ID, homogeneity-child ID, component multiplicity or inverse-expansion
weight.  A row digest is not a typed join relation.

This absence is mathematically decisive, not merely a formatting issue.
Let

```text
theta=144000/180337,
q_branch=theta+1/5=900337/901685,
N<=153.
```

The current aggregate contract is `1<=N<=153` and
`0<w_j<q_branch`.  Both of the following abstract completions also satisfy
the stronger inequality `w_j<theta`:

```text
contracting:     every weight=q_branch/(2*153), total=q_branch/2<1;
noncontracting:  every weight=3/4,       total=459/4>1.
```

These are schema completions, not claims about billiard branches.  They prove
that the recorded counts and uniform envelopes cannot decide contraction.
The frozen `1/5` bound is only the two-sided grazing-tail envelope on one
true continuity branch; a single global tail shared by every noncentral
child is **not** certified.  It nevertheless gives the exact conditional
budget for the missing theorem: if a future incidence argument charges all
noncentral children to one global tail `<=1/5`, then it must prove

```text
joined central sum      <4/5.
```

Under that still-missing global-tail premise, one central uniform envelope
gives `q_branch<1` with margin `1348/901685`; two repeated central envelopes
already exceed one.  This only proves that repeating the uniform envelope is
an invalid proof strategy; it is not a lower bound on the physical weighted
sum.  The next sufficient input is therefore either an explicit
`delta_1` incidence theorem giving at most one central child and one single
global grazing tail, or a complete componentwise
multiplicity-times-inverse-expansion join.

## 8. Gate 4: finite cemetery cannot repair inverse-mass charging

On a normalized physical cumulative-mass coordinate `u in [0,1]`, let a
finite deterministic native stopped partition retain actual mass
`P_ret>=1-epsilon` in `N` positive-mass atoms of `u`-diameter at most
`delta`.  Lebesgue pushforward in this coordinate gives `p_a<=delta`, hence

```text
N delta>=P_ret,
N>=ceil((1-epsilon)/delta).
```

Under the existing leafwise inverse-mass interface,

```text
E[cost]>=sum_a p_a p_a^-1=N,
E[cost | retained]>=N/P_ret>=1/delta.
```

The denominator is the actual retained mass; an independent audit corrected
an earlier draft that had used the lower bound `1-epsilon` in the wrong
direction.  Multiplying by `exp(gamma R_a)>=1` cannot improve the estimate.
For `delta=2^-K`, `epsilon<=2^-K`, the unconditional charge is at least

```text
2^K-1.
```

Thus finite native antichains with simultaneously vanishing cemetery and
fine resolution cannot have a uniform leafwise inverse-mass recovery moment.
This is specific to the normalized mass coordinate and the current cost
interface.  It does not refute an unnormalised/reweighted-family recovery
theorem, a changed functional, or an allowed external random depth mark.

## 9. Latest theorem audit

Official arXiv queries were run on 2026-07-16.  The directly relevant or
nearby 2026 results found in those queries remain:

- Canestrari, `arXiv:2604.19671v2`, whose small-hole linear response uses a
  standard-family framework but does not provide this moving-scatterer
  common atlas, weighted multiplicity, or native repeated-cut recovery;
- Climenhaga--Day, `arXiv:2604.25881v1`, which constructs the MME for each
  fixed finite-horizon table but not the collision-SRB stable quotient or
  parameter DQ required here;
- Demers--Liverani, `arXiv:2606.10155v1`, whose review does not close general
  characteristic-restriction memory/recovery;
- Alves--Matias--Melbourne, `arXiv:2605.05968v1`, whose billiard-facing
  conclusions assume an existing Young-tower model; its separate tower
  construction concerns mostly expanding partially hyperbolic
  diffeomorphisms and does not construct the missing Gate-2 billiard tower;
- Goverse--Kleptsyn, `arXiv:2607.11421v2`, whose atomic physical stationary
  measure examples warn against extending diffeomorphism-driven random
  stationary-regularity claims to merely locally invertible systems; it is
  not itself a theorem for this billiard.

The earlier Butler--Park cocycle criterion, `arXiv:1909.11548v2`, still
requires global canonical Hoelder holonomies and class-`H` input not supplied
by the local logarithmic repair.  Lima--Obata--Poletti,
`arXiv:2405.04676v2`, supplies the clean basic-set/symbolic topology used
above, but not a compatible derivative-cocycle gauge, global holonomy,
twisting or PPE.  No result found in the audit supplies the missing
Gate-3/4/5 interfaces.  No bypass was used.

## 10. Validation and exact remaining frontier

All seven new certificate/verifier stacks compile and pass their direct
replay/integrity checks.  The Gate-3 outer-atlas stack was materialized twice
from clean inputs with 32 workers: the first run took `15:40.73`, the
independent verifier replay took `15:21.60`, both exited zero, and all three
frozen row/work/slice digests matched exactly.

The mutation rejection vector is

```text
Gate 1 compact gauge/plaque holonomy          9/9
Gate 1 word gauge/homoclinic frontier       18/18
Gates 2/5 physical return cores             11/11
Gate 3 finite-s future outer atlas          11/11
Gate 4 global Growth/distortion              9/9
Gate 4 finite-cemetery tradeoff              9/9
Gate 4 weighted-join diagnostic             11/11
total                                       78/78
```

Every live default remains fail-closed with exit `2`.  The seven direct SHA
sidecars, the twelfth-round subordinate manifest, the aggregate manifest and
the frozen v52 chain all pass `sha256sum -c`; no CM2 computation process
remains after freeze.  Thus the strict composite verdict remains
unconditional `NO-GO FOR CLAIM`, with exactly `0/5` gates fully closed.

The next honest attack order is:

1. construct a global faithful physical-cocycle coding, compatible gauges
   and uniform all-plaque Hoelder holonomies, then select an immutable
   homoclinic orbit, compute or bound `psi_z`, certify the four nonzero
   QNL-fiber twisting wedges, and close class `H`/projective PPE;
2. eliminate or analytically dominate the finite-`s` unresolved
   future-singularity outer cover, type retained candidate roots by
   physical-first owner and construct the genuine marked current, then prove
   strong-source/operator DQ and branch-record `MT_DQ` on the resulting
   common atlas;
3. replace the unweighted `153` branch count by a physical
   contraction-weighted multiplicity and close invariant standard-curve
   curvature/distortion;
4. prove recovery for an unnormalised/reweighted native family, or provide a
   permitted alternative interface, then propagate numerical
   `C_p,vartheta_p,C_fw,C_rev,q`;
5. extend the 24 nonempty word cores to maximal homogeneous domains with all
   roof levels and all 18 operator fields;
6. construct the full stable quotient/PPE and close the three CM2 norm lifts,
   physical `FACE_2CUT/FACE_TIME`, Kac and operator phase.
