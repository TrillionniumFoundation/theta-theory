# Round306B1AF4K2R2W2 local R2 W-tail artificial-face reglue theorem

## Decision and exact scope

The exact eight-member package manifest, together with its pinned independent
verification receipt, seals **four local R2 W-tail artificial-face reglue
theorems**.  The producer document by itself is explicitly conditional and
has zero effective pre-seal credit.  The manifest-pinned verifier receipt is
the package-seal condition that makes the four local reglue credits effective.

This package does **not** mint normalized-support, representation-cover,
physical-incidence/equivalence, A1/A2, B1A, B2, transition, pair-routing,
maximality, fibre, global-disposition, D02, D03, D04, or CM2 credit.  The old
global universe count is not pinned, the old `824864` obligation census is not
pinned, and the corrected global schema is not sealed.  Therefore CM2 remains
`NO-GO_FOR_CLAIM`.

## Local theorem

For each of the four frozen Round271 W-tail pairs, the exact physical support
is represented as the canonical owned rational box intersected with the
source-free predicate

`target_normal_x^2 - target_normal_y^2 < 0`.

The two child boxes use the canonical rule `[lower, upper)` in `t`, except the
parent's outer upper endpoint is owned.  Child 1 owns the artificial midpoint.
The verifier independently proves the five-atom half-open partition and the
set identity `(B0 ∩ P) ⊔ (B1 ∩ P) = (B ∩ P)`.

It independently reconstructs the physical AST, proves strict predicate
domain positivity over every parent, and checks strict negativity over all
four complete artificial faces and eight positive-volume inward corridors.
Each face has exact area `1/204800`; each corridor has exact volume
`177/209715200000`.  Aggregate face area is `1/51200`; aggregate corridor
volume is `177/26214400000`.

## Independent replay evidence

- Producer, verifier, and cold replay require `sys.flags.isolated=1`,
  `sys.flags.dont_write_bytecode=1`, `sys.flags.no_site=1`, and
  `sys.dont_write_bytecode=True`.  Every sealed run used `python3 -I -B -S`
  with a minimal Python-neutral environment.
- 20 inputs/package members were held by file descriptor, hashed twice, and
  revalidated against their final paths; five ordered tables were recomputed.
- Semantic replay covered 4 old blocked rows, 4 R182 occurrences, 4 R182
  leaves, 8 R271 children, 8 B1R0 cells, and 4 B1R0 normalizations.
- All 4 parent domains, 4 partitions, 4 complete faces, and 8 corridors passed.
  All 16 stored 128-bit receipts were recomputed field-for-field, with an
  independent 192-bit outward-rational sign/domain pass.
- Eight receipt/domain mutation probes were rejected.
- Two isolated producer seeds produced byte-identical bundle SHA-256
  `fcc2cd10a7f7499b67bc7526657631493ab6a570c63ff46ea3912f40012550ad`.
- The verifier did not import producer or K1; K1 remained zero-credit.
- Two complete isolated verifier runs produced stdout byte-identical to each
  other and to the sealed verification file.  Nondeterministic timing is kept
  only in external `/usr/bin/time` logs and excluded from the sealed receipt.
- `strace` observed zero deliverables-directed write/create/rename/unlink/
  truncate syscalls.  The R2W2 namespace remained unchanged.  This receipt
  deliberately does not claim the entire shared `deliverables` directory was
  unchanged while other workers were active.
- Producer and verifier publication is no-clobber and idempotent-exact.

## Frozen primary hashes

- producer: `a9d1ca8100f8ac7b882dcb836066aa7c10925229451b228f30683b40068aa993`
- theorem ledger: `e3c27d4e7159aadd4adf35fe98c81f8534de6af0b4d6b6436ec1cffa9fe51bb4`
- conditional result: `ed28b179673e0fcd108d4b99be31c0b4ced75236c6e29187218cd06ba4e5dac1`
- independent verifier: `eb7d38a917e230a4bffe8a1ac5a54775842c87d871f590b2f491343a9756779e`
- attack suite: `182ce9c724c65eb8a91998dad264e02fd5c24212816bccbbce474023efa89fc4`
- independent verification: `700b362c6b82ec32eb92262c17a1942ce2a1ba84764b8873cb298a6b593a19ed`

The final manifest is authoritative only when it contains exactly eight rows,
in order: producer, theorem ledger, result, independent verifier, attack suite,
verification, this report, and the cold-replay program.  Cold replay validates
that manifest before executing any theorem code.

## Superseded unsealed candidates

Unsealed snapshots remain recoverable at:

- `/tmp/cm2-k2r2w2-isolation-preseal-8xW6si/`
- `/tmp/cm2-k2r2w2-runtime-preseal-9S4itW/`
- `/tmp/cm2_k2r2w2/superseded_preseal_4085eddefa56/`

Each archive has recorded SHA-256 and sizes.  No sealed or unrelated artifact
was moved or overwritten.
