# CM2 Round 71 full direct assault

Date label: 2026-07-21

Strict verdict: **0/5 complete composite gates; Gate 2 official fields 0/17;
Gate 4 landing join 1/7; Gate 5 maturity 10/18 with zero complete blocks;
CM2 remains `NO-GO_FOR_CLAIM`.**

## Round result

Round 71 attacks the finite limiting-R1 face-atlas bottleneck directly.  The
old registry contained 1,152 terminal-core-preimage candidate families but no
nonempty connected component IDs.  This round converts 32 of those symbolic
families into actual base-fibre physical face germs.

1. **Actual nonempty witness registry.**  The Round-69 `s=0` return graph has
   16 positive reciprocal directed core edges.  Each edge now carries two
   rigorously sign-changing target-core boundary witnesses, one `t` face and
   one `p` face.  The packet contains 32 distinct family IDs, 32 distinct local
   connected rank-zero component IDs, 32 typed incidence edges and 64 distinct
   one-sided traces.

2. **Independent physical replay.**  Every source endpoint is rational.  A
   separate 100-digit collision-map implementation recomputes all endpoint
   signs.  The maximum source segment length is
   `23/23058430092136939520000`; against the frozen rank-14 Lipschitz upper
   `2457600`, the worst other-coordinate boundary margin/drift ratio remains
   above `3.7e11`.  Thus every crossing stays in its declared destination face
   and yields a regular local analytic component germ.

3. **Actual F8/F9 attachment.**  Every new face receives the common normalized
   F8 lower `1/5`.  For affine terminal core faces at depth one and rank path
   `[14]`, each receives F9 C2 upper `691839371876953699123200`; the exact
   32-face finite sum is `22138859900062518371942400`.

4. **F10/F16 and operator probe.**  The canonical compact-germ F10 search now
   has 32 actual inputs, but no least integer has been executed, so numeric F10
   remains `0/32`.  The arbitrary-suffix Piola identity attaches a
   parameterized F16 law to all 32 rows, but numeric F16 remains `0/32` because
   actual F13 is absent.  Existing F14, F15 and F17 frontiers still require a
   complete F10/F13 atlas and common anisotropic operator recipient; no field
   promotion is justified.

5. **Exhaustion frontier.**  This is a certified positive subatlas, not a
   complete atlas.  The other 1,120 candidate families have not been proved
   empty, global connected pieces have not been assembled and shared source
   faces have not been subdivided.  The finite F9 sum is not a limiting
   rank/path-weighted global sum.

## Verification

- Producer replay: 16 cells, 32 faces, 64 traces.
- Independent replay: `16/16` positive cells, `32/32` sign-changing witnesses,
  `64/64` traces, minimum margin/drift `>3.7e11`.
- Deterministic manifest audit: pass.
- Hostile semantic mutations rejected: `631/631`.
- Strict JSON mutations rejected: `4/4`.
- Byte-identical re-emission: pass.

## Frozen artifacts

- `deliverables/cm2-round71-r1-nonempty-face-germs-assault-2026-07-21.md`
- `deliverables/cm2-round71-r1-nonempty-face-witnesses-2026-07-21.json`
- `deliverables/cm2-round71-r1-nonempty-face-germs-manifest-2026-07-21.json`
- `deliverables/cm2_round71_r1_nonempty_face_germs_cert.py`
- `deliverables/cm2_round71_r1_nonempty_face_germs_verifier.py`

## Shortest next route

Run an interval sign/exclusion classification over all 1,152 families, prove
the 32 positive germs extend to their full clipped connected components and
reject the remaining 1,120 including corner coincidences.  Then execute the
canonical F10 search, materialize F13/F16 and prove the finite weighted face
sum.  F14/F15/F17 and the Gates 1--4 stable/material crosswalk remain separate
core obstructions.
