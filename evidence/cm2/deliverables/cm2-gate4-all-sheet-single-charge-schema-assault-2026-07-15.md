# CM2 Gate 4: all-sheet same-occurrence and single-charge schema

Date: 2026-07-15  
Model: centred rational two-disk torus pilot on the standard solid-boundary
section `N=G disjoint-union W`  
Verdict: **the exact Gate-4 construction has been generalized from one local
row to every possible moving signed tangency sheet, but the immutable
physical event rows and stopped-parent recovery remain absent; Gate 4 is
`NOT_CERTIFIED`.**

## 1. Decision-changing result

The conservative Gate-3 universe contains 288 signed tangency sheets after
the four source charts are merged.  They are not 288 physical event rows.
The new executable separates the ledger as follows:

```text
288 global raw signed sheets
  = 160 same-colour sheets with partial_s Delta identically zero
  + 128 genuinely parameter-active cross-colour sheets.

128 active sheets
  = 32 exact Jx/Jy symmetry orbits of size four.
```

For every connected physical subrow subsequently cut out of one of those
128 sheets, the report proves an exact universal implication:

1. group its grazing contact with its certified next non-grazing miss
   collision;
2. reverse that same ghost/direct path between its two non-grazing endpoints;
3. obtain two restrictionwise nonadditive representations of one current;
4. transport one and the same positive coarea law between them; and
5. charge the occurrence once by

   \[
      q_e=\max\{C_e^{\rm fw},C_e^{\rm rev},2\}\,m_e.       \tag{1.1}
   \]

This is no longer a theorem about only the previously certified
`G[0,0]--tangent W[0,0]--G[1,1]` band.  It is an all-sheet construction
theorem.  It is still conditional on the physical row fields: the current
Gate-3 owner work gives exact Boolean owner predicates and a finite raw
endpoint registry, but has not yet frozen all connected components with
constant miss trace, constant polarity, homogeneity typing and endpoint
margins.  A raw sheet therefore receives no `q` charge.

The second strict limitation is recovery.  The two representations agree on
every Borel restriction by change of variables.  That fact does not turn an
arbitrary Borel restriction into a proper standard family.  Gate 2's
adaptive two-map energy drift is a positive benchmark, not yet the physical
full-mass quotient needed to discharge this layer.

## 2. Exact raw universe

Let `C_S(s)` and `C_T(s)` be the source and target lift centres, and set

\[
 \eta_{S,T}={\bf1}_{T=W}-{\bf1}_{S=W}\in\{-1,0,1\}.
\]

For a source state `(q,u)`, put

\[
 d=C_T(s)-q,\qquad
 \ell_T=u\cdot d,\qquad
 w_T=u^\perp\cdot d,
\]

and use the signed tangency sheet

\[
 \Delta_T=R_T^2-w_T^2=0,\qquad w_T=\epsilon R_T,
 \quad\epsilon\in\{-1,1\}.                               \tag{2.1}
\]

At fixed source collision coordinates,

\[
 \partial_s\Delta_T=2\eta_{S,T}\epsilon R_Tu_y,
 \qquad
 \partial_p\Delta_T=\frac{2\epsilon R_T\ell_T}{c_p},      \tag{2.2}
\]

where `c_p=u dot n_S>0`.  Thus the signed graph-coarea coefficient is

\[
 \frac{\partial_s\Delta_T}{|\partial_p\Delta_T|}
   =\eta_{S,T}\epsilon\frac{c_pu_y}{\ell_T}.              \tag{2.3}
\]

For `G/G`, neither centre moves.  For `W/W`, both centres translate by the
same vector.  Hence `eta=0` and (2.2) vanishes identically on all 160
same-colour raw sheets.  They carry no interior first-order moving-face
current.  Their changing visibility endpoints are not lost: every such
endpoint also appears as a target/competitor boundary in the active
cross-colour registry.

The exact active raw endpoint universe is:

| descriptor | count |
|---|---:|
| ordered target/competitor common-tangent branches | 36,352 |
| source-grazing equations | 256 |
| parameter-window boundary descriptors | 256 |
| `u_y=0` constant-polarity splits | 128 |
| **total** | **36,992** |

These are analytic descriptors, not 36,992 physical atoms.  Empty curves,
occluded contacts, duplicate chart/lift traces and later-target miss switches
must be classified before connected event rows can be frozen.

## 3. Raw descriptor versus physical event row

The Gate-3 projection-order theorem makes the owner predicate finite and
exact: pairwise disjoint target disks have disjoint intersection intervals
on any ray, so incoming-root order is the order of their centre projections
`ell_T`.  This gives exact Boolean first-visibility predicates on all 95,596
parent multi-candidate leaves.  It does not by itself provide the connected
row decomposition.

An object is admitted as a physical Gate-4 row only after it contains all of
the following immutable fields:

```text
occurrence id and parent raw-sheet id;
connected base component;
first-visible owner witness;
one constant miss target and a strict miss-after-tangent gap;
non-grazing source and miss endpoints;
one constant parameter polarity;
one homogeneity subrow;
forward and reverse carrier typings.
```

Common tangents to two disjoint target circles are not multiple first events:
their contact times are strictly different.  They are nevertheless required
row endpoints, because the later miss target can change along a connected
first-tangency sheet.  This is precisely why the raw 128-sheet ledger cannot
be mistaken for the physical occurrence ledger.

## 4. Universal same-occurrence theorem

### Theorem 4.1 (grouped reversal of every completed row)

Let `e` be any physical row satisfying the fields above.  Write `a` for its
base coordinate, `x_e(a)` for the non-grazing source phase point,
`z_T(a)` for the compactified grazing trace, and `y_M(a)` for the first
non-grazing miss collision obtained by continuing the tangent velocity.
Let `F_e x_e(a)=y_M(a)` be this clean ghost/direct collision branch.

Then on oriented path records

\[
 \rho_e=R\circ F_e,
 \qquad \rho_e^2={\rm id},                                \tag{4.1}
\]

and the one physical face current has the two formulas

\[
 J_e(\Phi)=\sigma_e\int
       \{\Phi(z_T(a))-\Phi(y_M(a))\}\,dm_e(a),             \tag{4.2}
\]

\[
 J_e(\Phi)=\sigma_e\int
       \{\Phi(z_T(F_e^{-1}y))-\Phi(y)\}\,
       d(F_e)_*m_e(y).                                    \tag{4.3}
\]

For every Borel `A` in the base, the restriction of (4.2) to `A` equals the
restriction of (4.3) to `F_e(A)`.

#### Proof

The physical row guarantees that the open segment from `x_e(a)` to
`y_M(a)` has exactly one tangent contact and no intervening collision.  A
specular reflection at a tangent contact fixes the velocity, so the segment
is a legitimate boundary value of the billiard branch.  Reversing the
oriented segment swaps its non-grazing endpoints, preserves the same contact,
and gives (4.1).  Both integrands have the identical formal trace vector
`(+1 at z_T,-1 at y_M)`.  Ordinary change of variables under the analytic
one-to-one map `F_e` proves (4.2)--(4.3) on every restriction.  The formulas
are alternative views; they are never summed.

This proof uses no symmetry and applies separately to every completed
component of all 128 active signed sheets.

## 5. One common coarea law

On the source view the positive coefficient law, up to the fixed global SRB
normalization, is

\[
 dm_e=
 \cos\varphi\,
 \frac{|\partial_s\Delta_T|}{|\partial_p\Delta_T|},dr
 =\cos\varphi\,
 \left|\eta_{S,T}\epsilon\frac{c_pu_y}{\ell_T}\right|dr.
                                                               \tag{5.1}
\]

Define the reverse event equation by transporting the whole moving event
through `R F_{e,s}`.  Both the clean collision branch and time reversal
preserve the collision flux form `cos(phi) dr dphi`.  Consequently

\[
 (F_e)_*\{\delta(\Delta_T)|\partial_s\Delta_T|\,d\mu_N\}
\]

is exactly the reverse coarea law.  This includes the flux/Jacobian
cancellation and never divides by the zero flux at the intermediate grazing
contact.  Thus (4.2) and (4.3) use one measure `m_e`, viewed in two
coordinates, rather than separately normalized forward and reverse laws.

## 6. The exact single charge

After homogeneity cuts and Banach typing, let `C_e^fw` and `C_e^rev` contain
the frozen source, density, boundary, operator, inverse-chart and test
pullback costs of the two views.  Before the product-time query, define
(1.1).  Since the maximum dominates both costs, either formula is controlled
by the same law.  The occurrence is charged once; the unused endpoint view
is not an additive current and is not tail mass.

Different physical rows remain additive.  In particular, a `Jx`-reflected
row is a second physical occurrence and has its own isometric copy of `q_e`;
it is not collapsed with the first row.  This exactly matches the local
reflection-pair certificate.

At the current stage (1.1) is a **universal pre-recovery schema**, not the
global numerical `q` ledger.  The row partition and its constants have not
all been frozen.

## 7. Exact reflection ledger

The 128 active raw sheets form 32 four-element orbits under `Jx,Jy`.  On
each orbit:

```text
Jx:    parameter polarity reverses;
Jy:    parameter polarity is preserved;
JxJy:  parameter polarity reverses.
```

Hence every orbit has two `+sigma` and two `-sigma` members.  Once the
physical component registry is made equivariant, `Jx` pairs scalar roof
mass exactly, including fixed-label internal involutions.  This proves the
global scalar test-1 cancellation schema.  It does not cancel the norm of a
current against a general non-symmetric test.

## 8. Recovery attempt and exact obstruction

The strongest unconditional statement currently available is the Borel
restriction identity (4.2)--(4.3).  It is not a stopped-parent recovery
theorem.

Take a positive-`m_e` compact nowhere-dense subset `K` of one analytic row,
after removing finite-time singular preimages.  Every finite clean billiard
branch is a local diffeomorphism, so every finite image of `K` is nowhere
dense.  A positive regular standard pair has density bounded below on a
nondegenerate interval and therefore has support containing an interval.
It cannot equal that image.  For a countable integer-valued recovery clock,
one level has positive restricted mass and the same obstruction applies.

Thus recovery under **arbitrary Borel restrictions is false**, not merely
unproved.  There are exactly two viable routes:

1. freeze the actual stopped parents to a controlled interval/cylinder
   algebra and prove both recovery moments there; or
2. instantiate Gate 2's full-mass physical quotient, pair-energy/PPE drift,
   normalized amplitudes and same-carrier endpoint identity, then pay the
   common parentwise recovery envelope once in (1.1).

The certified adaptive two-map value
`kappa=(17/25)169^(1/20)<1` supplies the correct abstract small-ball
mechanism, but those two maps are not the physical full-mass return alphabet.
It therefore cannot be inserted as a hidden recovery mark.

## 9. Exact remaining Gate-4 boundary

Certified here:

```text
RAW_PARAMETER_ACTIVE_SIGNED_SHEETS_128: CERTIFIED
SAME_COLOUR_ZERO_FACE_COEFFICIENT_160: CERTIFIED
ACTIVE_ENDPOINT_DESCRIPTOR_LEDGER_36992: CERTIFIED
JxJy_ACTIVE_SHEET_ORBITS_32: CERTIFIED
UNIVERSAL_SAME_OCCURRENCE_TWO_VIEW_THEOREM: CERTIFIED
UNIVERSAL_COMMON_COAREA_THEOREM: CERTIFIED
UNIVERSAL_PRE_RECOVERY_q_MAX_THEOREM: CERTIFIED
```

Still open:

```text
CONNECTED_IMMUTABLE_PHYSICAL_EVENT_ROWS: NOT CERTIFIED
ALL_ROW_FORWARD_REVERSE_TYPING_AND_COSTS: NOT CERTIFIED
CONTROLLED_STOPPED_RESTRICTION_ALGEBRA: NOT CERTIFIED
PHYSICAL_BIDIRECTIONAL_RECOVERY_MOMENTS: NOT CERTIFIED
GLOBAL_SINGLE_CHARGE_q_LEDGER: NOT CERTIFIED
GATE_4: NOT CERTIFIED
```

The remaining Gate-4 work is therefore no longer the discovery of an
eventwise reverse formula.  It has been reduced to (i) finishing the Gate-3
connected owner/miss/polarity registry, (ii) attaching finite oriented costs
to its typed homogeneity rows, and (iii) proving the two recovery moments on
the actually admissible stopped-parent algebra under that identical frozen
`q`.

## 10. Reproduction

```bash
PYTHONPATH=deliverables /tmp/cm2-flint-venv/bin/python -m py_compile \
  deliverables/cm2_gate4_all_sheet_single_charge_schema_cert.py \
  deliverables/cm2_gate4_all_sheet_single_charge_schema_verifier.py

PYTHONPATH=deliverables /tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_gate4_all_sheet_single_charge_schema_cert.py

PYTHONPATH=deliverables /tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_gate4_all_sheet_single_charge_schema_verifier.py --self-test

# Expected exit 2 until the five global completion fields are certified.
PYTHONPATH=deliverables /tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_gate4_all_sheet_single_charge_schema_verifier.py
```

