# CM2 forty-second direct assault

Date: 2026-07-19  
Verdict: **C24 hereditary Growth is now numerical in exact symbolic form;
the raw all-path F10 coordinate route is sharply ruled out, but all five
composite gates remain open**

## Round result

| gate | Round-42 advance | exact remaining core gap | status |
|---:|---|---|---|
| 1 | no unsafe transfer from the shared Gate-3/4/5 work | physical cancellation/Green representative and same-representative twisting/PPE | NOT CERTIFIED |
| 2 | no cross-carrier promotion | invariant stable plaques, physical holonomy, quotient branches and PPE | NOT CERTIFIED |
| 3 | numerical C24 killed Growth gives a stronger future dynamic-current carrier | arbitrary-depth dynamic DQ/`MT_DQ`, moving-current propagation and cemetery | NOT CERTIFIED |
| 4 | explicit length pullback gives `n_*=9148`, exact `Z0,Z1`, and an exact block-index aggregate-`Z` resolvent | numerical scheduled `N_open`, collision-time `C_fw,C_rev,q`, and strong cemetery | NOT CERTIFIED |
| 5 | target momentum parameter jets become rank zero; exact current splitting replaces the failed raw-coordinate route | arbitrary-`R_n` F13 traces/F16 flux, all-face F10, F12/F13, cemetery and F14--F18 | NOT CERTIFIED (`7/18`) |

Strict unconditional progress remains `0/5`; CM2 remains
`NO-GO_FOR_CLAIM`.

## 1. Gate 4 becomes numerical at the native Growth level

The new physical branch estimate is

```text
length_E(T_s W)
 <(5962448355/5191)*sqrt(length_E(W)).
```

It produces an explicit finite pullback scale by

```text
delta_(j+1)=(delta_j/C_len)^2,
delta_open=(5/27)*delta_9148.
```

All intermediate images remain inside the frozen one-step `Xi` regime.
Including the density ratio, the exact minimal half-block is

```text
n_*=9148,
gamma=(2000/1999)*(1+48*9148)*(900337/901685)^9148<1/2,
Z0=54/(5*delta_9148),
Z1=18367592526/360493663.
```

Thus hereditary C24 killed Growth is no longer merely qualitative.

Joining it to the scheduled mass factor gives an explicit block-index weight

```text
rho=(111718729/111718750)^9148,
w_Z=(1+rho^(-1))/2>1
```

and a closed weighted aggregate-`Z` resolvent.  The common collision block is
`9148*N_open`; because `N_open` is still nonnumerical, this does not yet
produce collision-time `q` or full `C_fw/C_rev`.

## 2. Gate 5 raw-coordinate route is decisively pruned

In canonical area coordinates `(r,p=sin phi)`, the target momentum is
`p_1=w/R_1` and all of its parameter jets have rank exponent zero.  The
target boundary position retains mixed/second `c_1^-3` growth.

The exact shell law

```text
P(B=2k)=(15/16)*16^-k
```

has finite `2^(3B/2)` but divergent `2^(3B)` moment.  Therefore the known
physical incidence moment cannot sum the raw position-jet majorant.  This
rules out the coordinate-change-only plan without asserting actual physical
divergence.

The surviving route is the exact area-preserving decomposition

```text
moving face seed (F10)
+ Eulerian transport current/two traces (F13)
+ flux operator cost (F16).
```

It avoids taking two singular coordinate derivatives of the complete
pullback, but its arbitrary-path trace and flux bounds remain open.

## 3. Latest-technology audit

The official arXiv API snapshot remains

```text
2104.06947v3 / 2604.19671v2 / 2606.10155v1.
```

Direct keyword searches found no newer dispersing-billiard theorem that
supplies the missing arbitrary-`R_n` moving-face F10/F13/F16 estimates.  No
shape-mismatched moving-wall result is imported.

## 4. Validation

The two new suites pass syntax, pinned dependencies, strict JSON and
independent exact replay.  They reject

```text
24/24 + 32/32 = 56/56
```

hostile mutations.  All four certificate/verifier default modes exit `2`.
Leaf and recursive SHA chains freeze every new report, certificate, verifier
and manifest.

## Next shortest line

1. Extract or independently quantify the theorem-supplied scheduled
   projective block `N_open`; then convert the exact block-index aggregate
   `Z` resolvent into a numerical collision-time rate.
2. Feed numerical `n_*,Z0,Z1,N_open` into same-ID `C_fw,C_rev` and final `q`;
   close strong cemetery separately.
3. Implement the area-preserving Eulerian generator on every regular
   arbitrary-`R_n` branch and prove its two physical traces (F13).
4. Prove the matching F16 flux cost, reassemble all five face types, and then
   close full F10/F12/F13/F14--F18 plus the induced coefficient.
5. Return the resulting common current ledger to Gate-3 dynamic DQ/`MT_DQ`;
   continue Gate 1 and Gate 2 as independent structural poles.

Strict unconditional CM2 remains `NO-GO_FOR_CLAIM`, with zero of five
composite gates fully closed and Gate-5 maturity unchanged at `7/18`.
