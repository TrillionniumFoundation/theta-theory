# CM2 Gate 5 round-33 safe F9/F10 subatlas

Date: 2026-07-19  
Verdict: **two typed subatlases filled; global Gate-5 maturity remains 6/18**

The 96 genuine level-zero C24 faces are affine `t=constant` or `p=constant`
segments in their fixed collision charts.  Their face-C2 bound is exactly
zero.  Their parameter speed and parameter-DQ coarea density are also exactly
zero, while their collision-line trace measure is explicitly not declared
zero.  This fills typed level-zero F9/F10 slots on all 96 source-core faces.

For the 64 moving first-event occurrence seeds, the immutable occurrence IDs
join the corrected density rows to the canonical endpoint rank

```text
B=max(14,ceil(log2(1/scale))).
```

Combining the density upper `18/5`, radius upper `9/25`, and the bidirectional
log-density bounds yields

```text
|d_theta rho_rev| < (4374/125) 2^B,
|d_theta rho_fw | < (8424/125) 2^B.
```

Thus parameterized seed-level F10 is numeric on all 64 occurrence seeds and
their 128 oriented traces.  The single-collision carrier curvature `4949` is
not a physical-face F9 bound and is not promoted.

Intermediate-core, terminal-core and owner-change pullback faces still lack
arbitrary-time physical face-C2 and coarea regularity bounds.  Consequently
complete F9/F10 fields, fields F11--F18, every complete operator block and
Gate 5 remain open; global maturity stays exactly `6/18`.

