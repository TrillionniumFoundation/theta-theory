# CM2 Gate 5 Round-48 seven-boundary F10 seed assault

Date: 2026-07-19  
Strict verdict: **all seven frozen one-step collision-boundary kinds now have
numeric regular-noncorner F10 base seeds.  The resulting formal all-face
ledger is subunit, but its terms still live on three unmatched measures, so
full arbitrary-`R_n` F10 is not certified.  Gate-5 maturity remains
`10/18`.**

## 1. Complete seven-kind candidate inventory

Across the 24 seeded physical components the one-step grammar contains
`6888` candidate slots:

```text
candidate signed tangencies:       2688
  moving cross-colour:             1152
  stationary same-colour:          1536

forward integer-corner rays:       2904
  moving W-source:                 1452
  stationary G-source:             1452

five zero-speed kinds:             1296
```

The five zero-speed rows are target endpoint/seam, target momentum
homogeneity, coordinate-velocity zero, source endpoint/seam and source
momentum homogeneity.  Their base parameter jets and F10 coarea currents
vanish.  Candidate slots are not asserted to be nonempty connected face
components.

## 2. Two nonzero regular base seeds

For a cross-colour tangency in collision area coordinates, the signed density
is

```text
a_tan = eta*c*u_y/ell.
```

Using `g=36337/800000` and `Rmax=9/25`, differentiation on a regular
noncorner germ gives

```text
|a|+|partial_tau a|+|partial_s a|
 < A_tan
 = 30/g + 5/g^2 + 2*Rmax/g^3
 = 516607461656000000/47978559724753
 < 10768.
```

Same-colour tangencies have zero relative translation current.

For a W-source forward integer-corner ray, `lambda>1/2` and the point-caustic
graph slope is below 9.  With `a_corner=c*h_s`,

```text
|a_corner|<2,
|partial_tau a_corner|<26,
|partial_s a_corner|<12,
```

so its F10 seed cost is below `40`.  G-source corners are stationary.

These estimates complete the seven one-step boundary-kind base-seed matrix on
regular germs.  Corner simultaneous-root intersections remain assigned to
cemetery.

## 3. Subunit formal ledger and the measure mismatch

For the worst W-source insertion, the new collision-boundary seed cost is

```text
52*A_tan + 121*40
 < 27095804235179804520/47978559724753.
```

Relative to the minimum `151*2^14` D1 insertion charge this is

```text
3386975529397475565/14837273637760415744
 ~= 0.228275.
```

Adding the Round-39 occurrence target `103/151` and the Round-47 C24 core
target `165/38656` produces the exact formal total

```text
13571096530812446357/14837273637760415744
 ~= 0.914662 < 1.
```

This inequality is certified arithmetic, not a physical F10 theorem.  The
occurrence term uses a raw endpoint-coarea measure, D1 uses collision SRB,
and the C24 term is a boundary trace.  The common arbitrary-`R_n`
restriction/disintegration kernel and return-depth weighted coarea
integrability are still absent.

## 4. Strong F13/F17 frontier

The Round-47 two-trace `(C1)*` sublayer and Round-46 Piola flux remain valid.
They do not remove the bulk Duhamel term.  After a nonempty suffix the safe
bound is still

```text
D_suffix*c_X + c_F13,
D_suffix=product_i(150*2^B_i).
```

Closing it requires an F17 dynamic-test estimate or a joint insertion/suffix
rank tail, followed by return-depth convergence and cemetery domination.

Consequently full all-face F10, complete strong F13, F14/F15/F17/F18 and
strong cemetery remain `NOT_CERTIFIED`.  Gate-5 maturity stays `10/18`, with
zero complete 18-field operator blocks.

