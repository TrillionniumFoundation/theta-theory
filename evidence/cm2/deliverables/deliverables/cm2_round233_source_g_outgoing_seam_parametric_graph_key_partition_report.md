# CM2 Round233 — source-G outgoing-seam parametric graph key partition

Date: 2026-07-27

## Verdict

`PASS_PARTIAL_FORMAL_ROUND233`.

The remaining `3,148` Round231 outgoing-seam roots all have a strict interval
`t` derivative for
`target_normal_x^2-target_normal_y^2=0`.  Their outgoing normal `x` and `y`
components also have strict fixed signs on the whole retained root.  Hence the
zero set is a two-dimensional parametric graph where present, while the two
open three-dimensional sides have uniquely determined outgoing cells.

Both side signatures share the same official exact-key row, ordinal, and ID.
The graph has zero three-dimensional coordinate volume.  Therefore each
retained root, and its one resolved sibling, has one local whole-origin exact
key almost everywhere even when graph existence over every base point remains
unresolved.

Combined with the `2,220` Round232 whole-signature promotions, the complete
outgoing-seam channel now has `5,368 / 5,368` local whole-origin exact-key
dispositions.  The new rows cover `44` exact keys and cross-check all `15,544`
previously released descendants belonging to the residual roots.

## Independent verification

The independent verifier does not import or execute the Round233 producer.  It
reconstructs the `3,148`-root universe from frozen Rounds 179, 220, 231, and
232; reruns all interval geometry; rebuilds both side signatures; and matches
every candidate row exactly.

Result: `PASS_INDEPENDENT_ROUND233`.

Five semantic credit, dimension, volume, key, and side mutations are rejected.
Cold replays under `PYTHONHASHSEED=233071` and `233929` reproduce the frozen
producer and verifier result hashes.

## Frozen hashes

- producer: `820085392577265eabcbdfea19f095e942736c0fe938554b811b04f1bd1f3546`;
- certificate: `cb2f74daa9836841ce311d8d555c2d94ba897f346a63c1e29551d7f99e8d1a41`;
- certificate result: `6be5b81be350a7d575fb34dd80fce53046fb017096d497070a218c04f92a88fc`;
- verifier: `6c2eb0729a90866c4d98743462f5e379764c7ce8bbfe330680b18d49965b516d`;
- verification: `184121d738cfcac44f06e65d14cf8581bc82c215ecf6f6bfb1ac2f2baf97bc63`;
- verification result: `d01cafb426600dadbba6e874be609f55666273a510a7a1f267d5399eb5d54669`.

## Strict boundary and next route

Round233 issues local whole-origin exact-key disposition credit only.  It does
not issue known-block incidence, physical-component, maximal-component, or
global exact-key-fibre credit.  Official global incidence remains
`35,896 / 53,968`; maximal components remain `0 / 53,968`; exhausted fibres
remain `0 / 116`; global source-G exact-key dispositions remain `0 / 224,580`;
Gate 5 remains `10 / 18`; CM2 remains `NO-GO_FOR_CLAIM`.

Next, attach the `5,368` locally disposed whole origins through retained
event/common-refinement strata to the frozen known blocks.  In parallel, apply
the same strict-predicate partition method to the `2,640` wall
endpoint/count-transition roots.
