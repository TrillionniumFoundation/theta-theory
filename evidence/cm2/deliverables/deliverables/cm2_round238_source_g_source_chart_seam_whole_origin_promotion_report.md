# CM2 Round238 — source-chart-seam whole-origin promotion

`PASS_PARTIAL_FORMAL_ROUND238`.

All `264` source-chart-seam roots have the exact full-base graph
`2*t^2-1=0`.  Direct whole-box signature recomputation, bypassing only the
chart-class precheck, is strict and equals the resolved sibling on all rows.
The physical side satisfies `2*t^2-1<0`; the outer side is a coordinate guard;
the seam has zero three-dimensional volume and follows the frozen half-open
ownership convention.

Round238 promotes `264` whole-origin local return signatures across `36` exact
keys.  Chart counts are `G:E=64`, `G:W=64`, `G:N=68`, and `G:S=68`.

The independent verifier returns `PASS_INDEPENDENT_ROUND238`.  Hash seeds
`238071` and `238929` reproduce certificate
`8200ba9c35ba32c938eb66beb7a4040908db9b81fc449881a55b09e67b517446`
and verification result
`b8ce33f3c1d25ce9190523ba9f0c2f85df85d0b656eba4cda26f905fd79f5557`.

No known-block, component, maximality, or global-fibre credit is issued.  CM2
remains `NO-GO_FOR_CLAIM`.
