# CM2 Gate 4 Round-49 sharp common-law recovery assault

Date: 2026-07-19  
Strict verdict: **the exact shortest closed half-contraction block and an
abstract single-common-parent-law two-orientation recovery theorem are
certified.  Their physical arbitrary-`R_n` whole-family join is absent, so
`C_fw/C_rev/q`, strong cemetery, Gate 4, and CM2 are not certified.**

## 1. Exact sharp closed block

The closed Growth coefficient is

```text
a = 360134800/360493663.
```

Exact integer arithmetic gives

```text
2*360134800^695 > 360493663^695,
2*360134800^696 < 360493663^696.
```

Thus `696` is the shortest positive block with `a^L<1/2`; the former `1005`
is safe but not minimal.  Since

```text
2e90/(1-a)=C_p/2,
```

the Round-47 one-step post-cut envelope and a dyadic survivor shell `k` give
the sharp safe per-orientation whole-family clock

```text
C_sigma(H,k)=H+696*(318+k)=H+221328+696*k.
```

Here `H` includes the terminal C24 test.  This route does not add the Round-42
`9148` killed block.

At `eta=1/4176`, set

```text
A_H=(6/5)^(318+ceil(H/696)).
```

The exponential comparison is also frozen without floating point:
`e<11/4<(6/5)^6`, hence `exp(1/6)<6/5`.

For each orientation separately,

```text
h_sigma*exp(C_sigma/4176)
 < p*A_H*(3/5)^k_sigma,

integral h_sigma*exp(C_sigma/4176) dlambda
 < A_H*integral p dlambda.
```

This theorem is conditional on a standard-Borel outer kernel of **whole
canonical proper families**.  It is not a leafwise properness statement.

## 2. Single-common-parent-law theorem

Assume one law

```text
m(dy,dx)=lambda(dy) K_par(y,dx)
```

on whole proper parent families, and require both forward and reverse Borel
survivor predicates to be pulled back to the identical physical restriction
ID in this same law.  They must not be separately normalised.  Survivor mass
zero is assigned `k_sigma=C_sigma=0`, contributes a zero survivor-supported
term, and is never normalised.  The finite-kernel normalization is explicitly

```text
K_par(y,X)=p(y)=mass(G_y),
m(YxX)=integral p dlambda.
```

Each whole post-cut survivor also inherits the Round-47 envelope
`Z(H_sigma)<=P*p`, with `P<2^317`.

For any integer `r>=1`, define

```text
W_r=max(
  1,
  1_Sfw*exp(C_fw/(4176*r)),
  1_Srev*exp(C_rev/(4176*r))
).
```

Then pointwise

```text
W_r^r
 <=1+1_Sfw*exp(C_fw/4176)+1_Srev*exp(C_rev/4176),
```

and hence

```text
integral W_r^r dm < (1+2*A_H)*integral p dlambda.
```

The parent is charged once.  No equal survivor, common shell, common
intersection, or proper-intersection hypothesis is used.

Two useful Hölder instances are:

```text
r=6, gamma=1/25056:
integral c*W_6 <= ||c||_(6/5)*(1+2*A_H)^(1/6),

r=12, gamma=1/50112:
integral (c*W_12)^(12/11)
 <= (integral c^(6/5))^(10/11)*(1+2*A_H)^(1/11),
```

for unit parent mass.

There is also a synchronized ambient clock

```text
C_pair=H+221328+696*max(k_fw,k_rev)
```

after which both separate ambient survivor families are proper.  It does not
make their intersection proper and is not a joint physical return.

## 3. Conditional D1 consequence

For the Round-35 additive physical charge

```text
d_D1=151*sum_i 2^B_i,
c0=max(2,d_D1),
M_D=K_rank*N_open^3,
K_rank=3055930500533353804145008325576782226562500/453789,
```

one has `integral c0^(6/5)<M_D+3`.  If the missing whole-family common-law
join is supplied, the preceding theorem yields the following formulas for the
Round-35 normalized physical parent probability `integral p dlambda=1`:

```text
integral c0*W_6
 <=(M_D+3)^(5/6)*(1+2*A_H)^(1/6),

integral (c0*W_12)^(12/11)
 <=(M_D+3)^(10/11)*(1+2*A_H)^(1/11).
```

Combining the second bound with the existing physical tail transfer gives the
conditional block-`1/12` estimate

```text
Wbar_D1rec(n)
 <(M_D+3)^(5/6)
  *((1+2*A_H)*A_tail)^(1/12)
  *r_tail^(floor(n/N_open)/12),

A_tail=550000/147,
r_tail=111718729/111718750.
```

This is only an absolutely-continuous additive-D1 recovery subcharge.  D1
does not dominate complete `C_fw/C_rev`, and the estimate is not the final
`q` tail or strong singular/corner/trace cemetery.

## 4. Why the physical grouping remains missing

A Borel leaf kernel and finite mass do not imply finite aggregate `Z`.  The
exact countermodel

```text
ell_n=4^-n,
p_n=4^-n
```

has total mass `1/3`, while

```text
sum p_n/ell_n=sum 1=infinity.
```

A physical whole-family join must add:

- a Borel `family_id` and exact outer disintegration;
- a finite boundary numerator
  `J(z)=integral p(y)/ell_*(y) dlambda_z(y)`;
- group mass `m(z)>0` and defect
  `D_Z=0` when `J<=C_p*m`, otherwise
  `ceil(log2(2J/(C_p*m)))`;
- a physical tail or exponential moment for the extra clock `696*D_Z`.

Even recordwise finite `J` is insufficient.  If group `n` has mass `2^-n`
split over `2^(n^2)` leaves of length `2^(-n^2)`, then
`J_n/m_n=2^(n^2)` and the recovery defect grows quadratically.  For every
positive exponential weight, the groupwise moment can diverge.

Likewise, two proper ambient survivors need not have a proper intersection.
Finite Borel component truncations do not repair strong cemetery: discarded
mass may tend to zero while discarded boundary `Z` remains infinite.

## 5. Exact frontier

Still not certified:

- numeric `H_cover` and actual positive `beta_Wdiag` (strict inferred lower
  bound remains zero);
- physical arbitrary-`R_n` whole-proper-family grouping, finite `J`, and a
  physical `D_Z` tail;
- a complete same-ID common-law `C_fw/C_rev` density in `L^(6/5)`;
- a proper common forward/reverse intersection;
- complete numeric `C_fw`, `C_rev`, collision time `q`, and strong cemetery.

Therefore Gate 4 remains `NOT_CERTIFIED`; the global verdict remains `0/5`,
CM2 `NO-GO_FOR_CLAIM`.

## 6. Mechanical verification

- Python syntax: `2/2` PASS;
- strict duplicate-key-rejecting JSON: PASS;
- independent exact arithmetic: PASS;
- integrity and deterministic replay: PASS;
- hostile mutations rejected after entering substantive replay/field checks:
  `108/108` (unsafe-path early rejection is explicitly not counted);
- certificate and verifier default modes: both exactly exit `2`;
- manifest replay is byte-identical.

Artifacts:

- `cm2_gate34_round49_sharp_common_law_recovery_cert.py`
- `cm2_gate34_round49_sharp_common_law_recovery_verifier.py`
- `cm2-gate34-round49-sharp-common-law-recovery-manifest-2026-07-19.json`
