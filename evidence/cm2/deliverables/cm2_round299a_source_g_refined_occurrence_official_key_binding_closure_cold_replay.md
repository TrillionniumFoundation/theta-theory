# Round299-A Source-G official-key binding closure: dual-seed cold replay

## Verdict

PASS for producer and independent-verifier determinism. Producer seeds
`299101` and `299929` generated byte-identical ledger, result, and producer
self-attack files. Independent-verifier seeds/hash seeds `299311` and `299929`
generated identical independent attack-suite and verification commitments.
The seed is accepted for replay bookkeeping only and does not select, order,
or transform evidence.

## Producer commands

The first run wrote the canonical package artifacts:

```text
python deliverables/cm2_round299a_source_g_refined_occurrence_official_key_binding_closure.py \
  --seed 299101
```

The second run wrote to an isolated temporary directory:

```text
round299a_tmpdir=$(mktemp -d)
python deliverables/cm2_round299a_source_g_refined_occurrence_official_key_binding_closure.py \
  --seed 299929 \
  --ledger "$round299a_tmpdir/ledger.json.gz" \
  --result "$round299a_tmpdir/result.json" \
  --attacks "$round299a_tmpdir/attacks.json"
```

Before the independent verifier replaced the producer-only attack-suite
artifact, all three producer outputs compared byte-identically. From the final
eight-item package, the producer replay is checked by comparing the still
canonical ledger and result and by checking the temporary producer-only attack
suite against its historical fixed pin:

```text
cmp deliverables/cm2_round299a_source_g_refined_occurrence_official_key_binding_closure_ledger.json.gz \
  "$round299a_tmpdir/ledger.json.gz"
cmp deliverables/cm2_round299a_source_g_refined_occurrence_official_key_binding_closure_result.json \
  "$round299a_tmpdir/result.json"
printf '%s  %s\n' \
  '8a9c30bc732871934ee50a83db227e7965d6ec29fa6ed493740a119e1cb6fe38' \
  "$round299a_tmpdir/attacks.json" | sha256sum -c -
```

The final canonical `_attack_suite.json` is intentionally the independent
suite with pin `2a6b4b32…`, so it must not be compared to the historical
producer-only suite with pin `8a9c30bc…`.

## Byte-identical commitments

Both runs reported:

```text
ledger_file_sha256
ffea8120af2179990d5c9e7ff385193e2c5a08bed161cf5b570aa28b1f8b1ee0

result_file_sha256
4835bab4ebe7afc0da8d00395f83f881dd2aaf31e0697994fed54dc7d86302f5

result_sha256
5a3cece69ad8f7737b8f80ca6828d34da969de1a22d56af5f6ed422356d88233

attack_suite_file_sha256
8a9c30bc732871934ee50a83db227e7965d6ec29fa6ed493740a119e1cb6fe38

attack_suite_sha256
bb501c8312a2025500b93d3e062cac53bccc2c696b8c1bd4bc7240f228e7921e
```

Both runs also reported the same exact census:

```text
9404 append-only binding rows
8212 rows bound to 36 keys from the old 116-key universe
1192 rows bound to 8 new raw keys
116 -> 124 raw observed keys
0 occurrence-ID rewrites
0 key merges
```

## Result self-closure check

The result closure was separately recomputed without importing the producer:

```python
result = json.loads(result_path.read_bytes())
claimed = result.pop("result_sha256")
assert hashlib.sha256(
    json.dumps(
        result,
        sort_keys=True,
        separators=(",", ":"),
        ensure_ascii=False,
        allow_nan=False,
    ).encode("utf-8")
).hexdigest() == claimed
```

The recomputed value was:

```text
5a3cece69ad8f7737b8f80ca6828d34da969de1a22d56af5f6ed422356d88233
```

Construction order was held fixed in both runs:

1. exhaust the five sealed manifests and selected input pins;
2. recommit all source tables and reconstruct the 9,404 binding rows;
3. deterministically compress the ledger and compute its file SHA-256;
4. embed the ledger commitment and all final scope fields in the result;
5. compute `result_sha256` and perform no later result mutation; and
6. build the producer self-attack suite against that final result closure.

## Producer self-attack replay

Both runs recorded:

```text
producer_self_attack_rejection
33 rejected
0 accepted
all_33_attacks_rejected = true
all_28_reclosable_semantic_attacks_reclosed = true
scope = PRODUCER_SELF_AUDIT_ONLY
independent_verification_status = PENDING
```

The attack-suite self-closure was also recomputed from canonical JSON after
removing only `attack_suite_sha256`, and matched the value above.

These results show deterministic producer-side rejection. They do not replace
the independent cacheless verifier described below.

## Independent verifier commands

The first cacheless verifier run wrote the canonical independent attack suite
and verification:

```text
PYTHONHASHSEED=299311 /usr/bin/time -p \
  python -B \
  deliverables/cm2_round299a_source_g_refined_occurrence_official_key_binding_closure_verifier.py \
  --seed 299311
```

The second cacheless verifier run rebuilt the full expected object under a
distinct seed and hash seed while leaving the canonical files untouched:

```text
PYTHONDONTWRITEBYTECODE=1 PYTHONHASHSEED=299929 /usr/bin/time -p \
  python -B \
  deliverables/cm2_round299a_source_g_refined_occurrence_official_key_binding_closure_verifier.py \
  --seed 299929 --no-write
```

Observed wall-clock times were `199.05 s` and `251.42 s`. Both runs reported:

```text
PASS_INDEPENDENT_CACHELESS_ROUND299A
9404 bindings
8212 old-universe rows over 36 used keys
1192 rows over 8 new raw keys
raw observed key universe 116 -> 124
8 new keys nonmerged
33/33 attacks independently rejected
28/28 semantic attacks fully reclosed
```

Both runs emitted the same commitments:

```text
independent attack-suite file SHA-256
2a6b4b320dc3caac321a8bc73747240acfbb638e42dad605f7546d9912e69957

independent attack-suite self-closure
972f019be81f8cb3ab52d41a6b252070d3b8682423ba204d2b71b0a3c02b6642

verification file SHA-256
3a3ab65fdd6b5bd00062e1cebbd141e1c38bded7d920705a83a57ba24db0f27d

verification self-closure
a6db2ff40098c3d818014f17f31e9e57c123c20c3a9bd65733a068c002f15644
```

The verifier reconstructs the expected ledger/result before opening the
candidate, streams and recommits all 431,208 Round294 registry rows, and treats
the producer only as an inert SHA-256-pinned byte object. It imports, executes,
and parses no producer code.

## Scope held fixed in both replays

The eight new raw keys remain distinct from their old-universe wall-transition
siblings and from one another. Round285 transported-equivalence incidence is
zero. No occurrence, alias, component, DSU-rank, Jx/Jy, maximality, fibre,
global-disposition, D02, or CM2 credit is inferred from this binding closure.
