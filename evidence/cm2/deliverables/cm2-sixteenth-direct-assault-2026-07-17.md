# CM2 sixteenth full direct assault

Date: 2026-07-17 (Asia/Shanghai)  
Frozen baseline: v51/v52 and the first fifteen direct-assault rounds were
preserved; this pass adds only append-only evidence and explicitly audits the
reproducibility of inherited compact ledgers  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; composite
gates fully closed remain `0/5`**

## 1. Executive result

This pass removes the last positive-width Gate-3 interval class, resolves the
new critical graph class, replaces two overbroad Gate-1 obstruction claims by
an exact gauge-escape theorem, and gives the 24 positive return components a
physical boundary-root grammar and finite component-local characteristic
bound.  Gate 4 gains a genuine retained-fraction shell and an arbitrary-time
unnormalised resolvent for one fixed open operator.  None of these results
supplies the missing full-mass stable quotient, all-plaque QNL representative,
strong transfer DQ/MT_DQ, or native repeated recovery.

- **Gate 1.**  Value and first-jet equality at the 96-collision shadow and a
  locally constant connector gauge really do preserve the old divergent
  witnesses.  They are not universal obstructions: an explicit local
  `SL(2,R)` first jet cancels either periodic coefficient, and two disjoint
  smooth bumps can perform both repairs while remaining the identity near the
  selected homoclinic orbit, hence preserving its loop and four nonzero
  wedges.  A full-shift model proves only that canonical holonomy families and
  weak typicality need not survive arbitrary Holder cohomology; both model
  representatives are in class `H`, so class-`H` membership invariance is not
  addressed.  The actual all-plaque variable-diagonal groupoid equations are
  still unsolved.
- **Gates 2/5.**  The selected physical QNL homoclinic occurrence is now tied
  by a connected strict-first-hit corridor to one of the 24 positive seeded
  maximal components.  The raw `394/402` boundary over-ledger reduces to at
  most `285/289` physical cut branches on a canonical curve, hence at most
  `286/290` component pieces and the uniform component-local unnormalised
  multiplier `580000/1999`.  A further exact full-window reachability proof
  identifies the selected component itself with one connected three-predicate
  band; its multiplicity is one, its local multiplier is `2000/1999`, and its
  restriction/step coefficient is
  `720269600000/720626832337<1`.  This contracting seed is only one component,
  not a full-key characteristic field or PPE.
- **Gate 3.**  Exact tangent-line algebra factors every one of the 151,500
  interval-geometry exception boxes without cancellation.  A rigorous
  160,716-call refinement gives 156,108 immutable owner leaves, zero retained
  positive-width terminal and fixed-`s` width zero.  The 888 newly exported
  critical graph leaves also contribute no physical future face: 840 are
  strict-`Delta` exclusions and 48 have strictly nonphysical tangent time.
  The last compact aggregate of 164 graphs is now reproduced exactly on rows
  `8,15,36,51`.  Eight further dyadic depths give 100 conditionally
  physical-first leaves and 2,372 strict-`Delta` exclusions; all 2,064
  remaining descendants have another uniformly positive root strictly before
  the candidate tangent time.  Thus all 1,052 predecessor candidate graphs
  are resolved.  The complete 41,444-chart marked side-owner current has TV
  at most `518152320`.  Strong operator DQ, branch-record `MT_DQ` and physical
  `FACE_2CUT/FACE_TIME` remain absent.
- **Gate 4.**  Direct intersection of the 24 regular inner cores with the 64
  singular occurrence bases is correctly zero on all 1,536 pairs; the needed
  object is a transported recovery-carrier incidence.  On every positive
  occurrence the two views share the retained fraction
  `p` and depth `D=ceil(log2(1/p))`.  For one registered interval cut the
  mass-weighted two-view recovery factor is `<15/8`.  Separately, the fixed
  24-core open operator has coefficient
  `b=720269600000/720626832337<1`, `b^2018<1/2`, and a complete
  arbitrary-time unnormalised resolvent.  Transported incidence, a tail on an
  unbounded number of cuts, strong cemetery payload, and numerical
  `C_fw,C_rev,q` remain absent.

## 2. Gate table

| gate | strongest certified layer in this pass | exact remaining hard gap | status |
|---|---|---|---|
| 1. holonomy/QNL | finite periodic first-jet repair; disjoint loop-preserving bumps; exact cohomologous class-`H` toy holonomies | actual all-plaque third gauge solving both stable/unstable groupoid equations; full-mass coding/PPE | **OPEN** |
| 2. physical PPE | selected homoclinic occurrence-to-component incidence; finite grammar on 24 seeds; one selected component-local contraction | stable-saturated base, quotient `rho`, reverse weights/kernel, same-carrier endpoint maps and PPE | **OPEN** |
| 3. global event/DQ | all interval exceptions removed; all 1,052 candidate graphs resolved; complete finite-s side-owner current with TV `<=518152320` | common strong-space DQ, branch-record `MT_DQ`, physical `FACE_2CUT/FACE_TIME` | **OPEN** |
| 4. same-occurrence `q` | retained-fraction shell, one-cut mass-weighted recovery, fixed-core arbitrary-time unnormalised propagation | transported incidence, native/unbounded repeated recovery, strong payload, complete `C_fw,C_rev,q` | **OPEN** |
| 5. fixed/full transfer | 24 seeded component-local finite `Z` bounds and one selected contracting local field-7 seed | all-component/full-key boundary field, 18 fields, three norms, Kac/phase | **OPEN** |

## 3. Gate 1: exact scope of the third-gauge frontier

At a periodic base point with

```text
A(p)=diag(Lambda,nu),  DG(p)v_s=nu v_s,  Lambda*nu=1,
E(p)=I,  X=dE_p[v_s],
```

the corrected derivative is

```text
dA_E[v_s]=dA[v_s]-nu X A(p)+A(p)X,
c_E=c+(nu-1)X_21.
```

Thus `X_21=c/(1-nu)` cancels the lower-left witness without changing the
periodic product or pinching.  Smooth traceless exponential bumps with
`chi=1` near the base point and `dchi=0` there realise this jet.  The shadow
and connector periodic orbits are finite and disjoint from the closure of the
selected homoclinic orbit; mutually disjoint supports avoiding both that
closure and all other points of those two finite periodic orbits preserve
every selected finite holonomy approximant, its limiting loop and all four
wedges.

The explicit full-shift example uses

```text
A=diag(2,1/2), rho=1/4,
D(x)=(I+v(x)E_12)(I+u(x)E_21),
B(x)=D(sigma x)^-1 A D(x).
```

Both `A` and `B` have Holder canonical holonomies and belong to class `H`.
`A` has identity holonomies and zero twisting; `B` has explicit nontrivial
stable/unstable loops and is weakly typical.  Therefore canonical holonomy
families and weak typicality are representative-dependent under unrestricted
Holder cohomology.  This example does **not** refute invariance of class-`H`
membership and does not construct the required billiard representative.

## 4. Gates 2/5: physical component incidence and finite local `Z`

At `s=0`, the selected QNL initial collision lies in the connected corridor

```text
source: G:E,
p=sin(phi) in [-1/50,1/50],
t in [69/100,35355339059327/50000000000000],
target: W[0,0].
```

The strict upper endpoint is below the `G:E/G:N` seam; complete competitor
replay keeps the selected first hit strict.  The corridor joins the selected
point to the positive seed of component

```text
7359148da1c43a255f2238d9c831b8638f2c9885035034a5792699c968b2f3b5.
```

For each frozen component row, `N=57` on a G source and `N=55` on a W source.
The `N` zero roots, `N` horizon-three roots and `N-1` selected/competitor
root ties are physically inactive on an exact first-owner boundary.  The
remaining discriminants give `2N` signed tangency branches.  The frozen word
grammar contributes exactly

```text
44 endpoint-wall +121 corner-time ties +2 velocity-zero =167,
2 source seams +2 target seams +4 homogeneity faces =8.
```

All rows carry the oriented roof-two wall grammar.  The frozen horizon gives
`tau<3`, the invariant cone gives `V>25/9`, and every active nonvertical
boundary branch has source slope at most `-25/9`; vertical/horizontal faces
are also transverse.  Hence each branch has at most one isolated root and
roots admit a weak source-`r` order.  The safe component-piece bounds are
`290/286`, giving

```text
Z_*(1_M F) <= (580000/1999) Z_*(F)
```

uniformly on the 24 seeded components.  It is intentionally not promoted:
the exact reachable root sequence is not materialised, other components of a
key are not covered, and

```text
(580000/1999)*(360134800/360493663)
=208878184000000/720626832337 >1.
```

For the selected `G:E -> W[0,0]` component, the physical predicates sharpen
substantially.  Writing `T(t,p)` for the target transverse coordinate, exact
full-window bounds on

```text
D=(1/2,1/sqrt(2)) x (-3/10,3/10)
```

give

```text
A>953/4000, |B|<161/800,
partial_p T <-13277/76000,
partial_t T <-11/64.
```

The three boundary signs are

```text
T(t,-3/10)>2759/40000>6/125,
T(1/2,p)>833/16000>6/125,
lim_{t->1/sqrt(2)}T(t,3/10)<-2759/40000<-6/125.
```

Thus the pruned band has one `t` projection, interval vertical fibres,
continuous endpoints and a continuous midpoint section; it is path connected.
The full-window seed corridor lies inside it, while the original maximal-word
definition implies the three retained predicates and those predicates imply
all other physical word/chart conditions.  Hence this band equals the seeded
maximal component, rather than merely containing it.  The exact predicate is

```text
G:E source chart AND |p_0|<3/10 AND |p_1|<3/10.
```

Each of its intersections with a canonical unstable graph is one interval.
Consequently the selected component alone satisfies

```text
Z_*(1_M F) <=(2000/1999) Z_*(F),
(2000/1999)*(360134800/360493663)
=720269600000/720626832337<1.
```

This is a contracting selected-component-local field-7 seed.  It says nothing
sharp about the other 23 seeds, other components of the same key, the full
441,280-key registry, reverse weights, stable quotient or PPE.

## 5. Gate 3: cancellation-free future geometry

For every active matching later-miss endpoint row the exact tangent-normal
identity gives

```text
w(0,s)=epsilon_other*r_m,
F(x,s)=x integral_0^1 partial_x F(theta x,s) dtheta,
Delta_m=F(2r_m-F).
```

The rationalised root-gap formula then removes the spurious interval
cancellation without inferring a numerical zero.  Reconstructing the full
coordinate ledger proves that it contains exactly the frozen 151,500 boxes.
Every final leaf has a strict factor/root-gap witness and immutable owner;
the class contributes no physical graph, current or FACE term.

The 888 conservative critical graph leaves are independently exported from
the exact coarse tasks and typed on their own full boxes.  Their final split
is

```text
strict Delta exclusion:          840,
strict nonphysical tangent time:  48,
remaining critical graphs:         0.
```

The final compact aggregate is independently recovered on exactly four rows:

```text
row indices:                         8,15,36,51,
graphs per row:                               41,
audit calls per row:                       39648,
total exported graphs:                       164.
```

Refining each graph by eight further depths in both normalised coordinates
gives 100 conditionally physical-first leaves and 2,372 strict-`Delta`
excluded descendants.  The 2,064 remaining order-failure descendants are not
discarded by resolution alone: on every full closed box the audit exhibits a
distinct candidate with

```text
discriminant >0,
0 < positive near root <3,
upper(other positive near root)
    < lower(candidate tangent projection).
```

On the selected candidate zero its collision time equals the tangent
projection, so one such earlier root suffices to prove that it is not a
physical first future face; uniqueness of the preemptor is neither assumed
nor needed.  All 2,064 boxes pass this strict test and no untyped graph
remains.  Combining the 100 new charts with the previous 41,344 charts gives
41,444 conditionally physical-first charts.  The complete preclassification
outer

```text
2*(126/5)*200*51404 = 518152320
```

therefore bounds the total variation of the complete marked side-owner
current.  This closes the finite-s depth-two geometric input only.  It does
not provide common strong-space invariance, global branch-test convergence,
iterated boundary-`Z` control or the physical FACE lifts needed for strong
DQ/`MT_DQ`.

## 6. Gate 4: retained fraction rather than corner normalisation

The core/occurrence audit proves that a useful incidence must contain

```text
(occurrence id, view, recovery carrier/branch record,
 transport time, core component id, identical pulled-back restriction).
```

Source-only label matching gives 768 candidates and source/target matching
112; neither is such an incidence.  On a pre-registered positive restriction,
however, both views share the same

```text
p=m_e(A)/m_e(row),  D=ceil(log_2(1/p)).
```

Using the numerical Growth clock gives, for a one-interval restriction,

```text
R_fw+R_rev <=2010(D+2),
p exp[(R_fw+R_rev)/12060] <15/8.
```

This is a conditional one-cut mass-weighted charge, not an installed physical
`C_fw` or `C_rev`.  The independent fixed open operator
`O=T_* o M_core` obeys

```text
Z_*(O^n F)
 <=b^n Z_*(F)+2*10^90 (1-b^n)/(1-b) mass(F),
b=720269600000/720626832337,
1/(1-b)=720626832337/357232337.
```

It is a genuine arbitrary-`n` result for this one fixed unnormalised operator,
but not a native stopping antichain and not arbitrary later indicators.

## 7. Latest-technology audit

The official arXiv `math.DS` recent page was rechecked through the Thursday,
16 July 2026 batch.  The closest new general papers inspected include
`arXiv:2607.13556` (Margulis measures on an already given one-dimensional
expanding foliation) and `arXiv:2607.13530` (`C^{1+Dini}` diffeomorphisms with
dominated splittings and Pesin unstable manifolds).  Their smooth invertible
foliation hypotheses do not provide a stable quotient, characteristic-cut
operator or moving-singularity current for this billiard.  In the audited
new entries, no theorem supplies any missing interface used in this report.

## 8. Validation and frozen boundary

Nine append-only certificate stacks were frozen in this pass.  Their semantic
mutation suites reject respectively

```text
16/16, 20/20, 17/17, 26/26, 27/27,
25/25, 15/15, 20/20, 21/21,
```

for a total of `187/187`.  Every stack passes integrity, complete replay and
its SHA sidecar; every live verifier deliberately exits `2`.  The expensive
Gate-3 coordinate export reproduces the inherited per-row digests before any
new typing is accepted.  Python compilation and the aggregate v52/prior-round
hash chain also pass, and no CM2 replay process is left running.

Every live verifier remains deliberately fail-closed with exit `2`.  The
strict end state is therefore

```text
Gate 1: NOT CERTIFIED
Gate 2: NOT CERTIFIED
Gate 3: NOT CERTIFIED
Gate 4: NOT CERTIFIED
Gate 5: NOT CERTIFIED
unconditional composite CM2 claim: NO-GO
```

The next executable frontiers are: solve or obstruct the actual all-plaque
third gauge; materialise reachable boundaries on every maximal return-word
component; build common strong-space branch-record DQ/`MT_DQ` and physical
`FACE_2CUT/FACE_TIME`; build the transported core-to-occurrence ledger and an
unbounded-cut tail; then close the stable quotient/PPE, 18 fields, three
strong norms and Kac/phase transfer.
