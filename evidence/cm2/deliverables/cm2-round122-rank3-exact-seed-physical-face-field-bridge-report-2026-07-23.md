# CM2 Round122 — exact-seed physical-face bridge and child-local Gate5 F1–F13 + F16

Date: 2026-07-23  
Verdict: **VERIFIED child-local Gate5 `14/18` on all 24 Round121 exact-seed children.**

## Result

Round122 extends the frozen Round121 exact-`b` seed through one common
parameter collar

```text
|s| <= 2^-512.
```

It pays two deliberately separate face interfaces:

- the complete five-kind physical-face and seven-kind boundary grammar has
  zero incidence on all 24 children and all three legs; and
- the 23 moving image-recut pullbacks and two stationary source outer faces
  remain genuine parameterized artificial faces.

This distinction installs child-local F7–F13 and F16 without erasing the
actual recut boundaries.  Together with Round121 F1–F6, the exact seed now
has:

```text
certified child-local fields  F1-F13 and F16 = 14/18
new full-key slots            960
combined full-key slots       1680
global Gate5 maturity         10/18
complete 18-field blocks      0
CM2                           NO-GO_FOR_CLAIM
```

F14, F15, F17 and F18 remain uninstalled.

## Frozen seed and parameter collar

The Round121 seed, its 24 common-refinement child IDs, 23 `s=0` pullback
endpoints, 48 inherited owner sides and 720 F1–F6 slots are byte-pinned and
unchanged.  The parameter moves every `W` center by `(s,0)` while every `G`
center remains stationary.

The producer and independent verifier both use exact dyadic
`epsilon=2^-512`.  Every moving root stays in its Round121 `2^-200` guard:

```text
stage 1: |dx/ds| < 4
stage 2: |dx/ds| < 13
13 epsilon < 2^-201.
```

The total displacement of two cuts is below `1/1000`, whereas distinct
Round121 cuts are separated by more than `1/200`.  The 23-face order and the
24 child ranks therefore remain fixed on the whole collar.

## Complete physical-face empty audit

The physical audit contains `72` child-stage rows and `96` collision-section
C24 core-clearance rows.  Each child independently replays the full rank-3
candidate census

```text
stage candidate counts  57 + 55 + 57 = 169
all children             24 x 169 = 4056 checks.
```

The old `64` parameterized moving-occurrence grammar seeds are not reused as
Q2/time-2 IDs.  The new immutable rank-3 census replaces that locator.

The complete strict-check census is:

```text
candidate tangencies            4056
coordinate-velocity zeros        144
C24 core faces                   1152
active integer-corner rays       2040
owner gaps                        144
root signs                        384
source chart / wall / H checks    216
target chart / wall / H checks    216
```

All checks have positive interval margins.  The certificate retains the
actual dyadic lower minima and conservative rational claims.  It then closes:

- all five physical face grammars;
- all seven boundary kinds;
- all source, intermediate and terminal core faces;
- all candidate tangency, owner-change, wall, chart, homogeneity,
  coordinate-velocity and active corner-ray predicates.

The physical face instance count and physical residual count are both zero.
This zero statement does not include artificial recut faces.

## Twenty-five parameterized artificial faces

The common-child chain has exactly:

```text
stationary source outer faces       2
stage-1 roots U1(x,s)=j delta        6
stage-2 roots U2(x,s)=j delta       17
total artificial faces              25
```

Here `delta=10^-90`.  For each moving face the producer and 3072-bit verifier
recompute the complete second-order jet of

```text
g(x,s)=Ui(x,s)-j delta.
```

They prove fixed positive `g_x`, opposite signs at the retained Round121
bracket endpoints and the implicit formulas

```text
x'  = -g_s/g_x
x'' = -(g_ss + 2 g_xs x' + g_xx (x')^2)/g_x.
```

The simple strict bounds are:

```text
stage 1: |g_x| > 5 delta,  |x'| < 4,  |x''| < 56
stage 2: |g_x| > 16 delta, |x'| < 13, |x''| < 616.
```

The two outer faces are exactly `u0=0` and `u0=delta`; all three stored
`g_x` lower fields equal the canonical fraction `1/10^90`.

Every internal face has two one-sided signed traces.  Each outer face has one
seed-side trace.  Thus the registry contains `46+2=48` trace rows and exactly
24 child-face incidence rows, each binding one lower and one upper face.

## Installed field values

| Field | Exact seed-local value or strict bound |
|---|---|
| F7 | `(2000/1999)*(900337/901685) = 360134800/360493663` |
| F8 | normalized artificial-face wedge `> 1/6` |
| F9 | outer `0`; stage 1 `< 80253367137726629098291200`; stage 2 `< 310359909615573026525432020225398669312000000` |
| F10 | `0`, because the complete physical face family is empty |
| F11 | stage 0 `< 4915200`; stages 1 and 2 `< 2457600` |
| F12 | outer `< 29686813949952000001`; stage 1 `< 6039797760001`; stage 2 `< 2457601` |
| F13 | `0`, because the physical moving current and physical one-sided traces are empty |
| F16 | `0`, because the physical flux trace is empty |

F7 stores both factors.  The bare

```text
Xi = 900337/901685
```

is not installed as the field value.

For F8, the source pullback formula is

```text
(kappa+V)/(sqrt(1+kappa^2)*sqrt(1+V^2)),
25/9 < V < 29.
```

The worst curvature is `kappa=25/4`; after squaring, the strict residual over
`1/6` is `87997/8>0`.

F9 and F12 retain the exact value attached to every face origin.  Since a
common child may be bounded by cuts originating at different image stages,
each slot also carries its actual lower/upper face IDs and pairwise maxima.
The scalar slot values use the safe maxima over all 25 faces.

F11 deliberately uses the authoritative full-phase envelopes.  The much
tighter along-seed diagnostics `<7,<3,<12` are recorded but do not serve as
field values.

Transparent-wall roof levels are symbolic prefix/suffix splits of one
collision leg.  Every new slot explicitly records that they add neither an
F7 factor nor an F11 factor.

## Full-key slot binding

Each of the 24 refined homogeneous subbranches has five official roof levels.
For every one of the eight newly installed fields, Round122 creates:

```text
24 children x 5 roof levels = 120 slots.
```

Hence:

```text
8 new fields x 120 = 960 new slots
Round121 F1-F6      = 720 inherited slots
combined            = 1680 unique slots.
```

Every immutable key is exactly

```text
(official-word-key-id,
 refined-homogeneous-subbranch-id,
 roof-level-j,
 field-name).
```

Every F8/F9/F12 slot binds the common child's actual two artificial faces.
Every F10/F13/F16 slot separately binds the complete typed-empty physical
audit.  The two registries cannot be substituted for one another.

## Independent verification

The independent verifier uses 3072-bit Arb arithmetic and imports neither the
Round122 producer nor a shared Round122 proof helper.  It independently
rebuilds:

- all `59/59` byte pins and the exact Round121 seed crosswalk;
- the common `2^-512` parameter geometry;
- all `169` candidate occurrences and every physical-margin census;
- all `23` moving implicit faces and two stationary outer faces;
- all `48` traces and `24` child-face incidence rows;
- the F7 arithmetic, F8 wedge, F9/F12 origin bounds and full-phase F11
  envelopes;
- all `960` new full-key slots and the combined `1680`-slot registry;
- every status, residual and strict nonclaim.

It rejects `120/120` re-signed semantic mutations and `15/15` strict-JSON
mutations.  Two producer runs and two complete verifier runs with different
hash seeds are byte-identical to the canonical artifacts.

## Strict nonclaims and next blocker

Round122 does not claim:

- a uniform materialization or ranking theorem for the full Borel-`b` family;
- any nonempty physical face, physical current, physical trace, coarea or
  flux contribution on this collar;
- that an artificial recut face is a physical singular face;
- F14 regular-density operator cost;
- F15 standard-family operator cost;
- F17 dynamic-test operator cost;
- F18 operator phase block;
- a complete 18-field block or global Gate5 upgrade;
- an endpoint-inclusive physical collar, cross-trace union reach or CM2.

The next core obstruction is now explicit: pay F14/F15/F17/F18 on actual
full-key children, or extend the exact-seed construction across the Borel
cut-equality strata.  Until then the strict state is seed-local `14/18`,
global `10/18`, complete blocks `0`, and `NO-GO_FOR_CLAIM`.
