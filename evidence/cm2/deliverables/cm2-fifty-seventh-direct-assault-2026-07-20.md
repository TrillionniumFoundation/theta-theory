# CM2 Round 57 full direct assault

Date: 2026-07-20  
Strict verdict: **0/5 complete gates; Gate 5 maturity 10/18 with zero complete
18-field blocks; CM2 NO-GO_FOR_CLAIM**

## Round result

Round 57 closes the physical common-refinement numerator `J_cap,total`, its
cross-endpoint energy, and the single common properisation clock `D_cap`.  It
also identifies an exact same-ID physical first-return graph on the common
raw law and isolates its remaining defect to unshifted landing properness.
No composite gate is closed.

The five append-only leaves are:

1. a palindromic stopped-cut proof of physical `J_cap,total<infinity` and the
   resulting `D_cap` moments;
2. exact typing of the unshifted `tau_cap=n` first-return graph and the
   positive-recovery first-hit obstruction;
3. an independent mathematical and executable audit of the `J_cap/D_cap`
   closure;
4. the Gate-5 clearance negative-moment, hybrid suffix and optimal-transport
   frontier;
5. the Gate-1/2/3 projective, holonomy and fixed-depth semialgebraic frontier.

All physical dynamical claims are parameterwise for every fixed
`|s|<=1/400` and use the stationary physical billiard map `T_s`.  No older
artifact was edited.

## Gate 4: full dyadic preclock control follows from `J_pair`

For each once-charged physical terminal cell define

```text
M_c=ceil(log2(1/min(ell_fw,c,ell_rev,c)))_+.
```

The Round-56 physical adapted-metric numerator gives directly

```text
p_c 2^M_c
 <=p_c+2p_c/min(ell_fw,c,ell_rev,c),

integral sum_c p_c 2^M_c
 <=nu(X)+2J_pair
 <infinity.
```

Since `Dbar=0` for `M<=310` and `Dbar=M-309` for `M>=311`, this strengthens
the former exponential estimate to the full dyadic moment

```text
integral sum_c p_c 2^Dbar(c)
 <=nu(X)+2^-309[nu(X)+2J_pair]
 <infinity.
```

This is a moment for the already constructed preclock.  It is not an
all-stage bound for independent later recovery clocks.

## Gate 4: the palindromic stopped-cut replay closes `J_cap`

Keep separate the Round-52 parent index `y`, on which the SYZ mass estimate
holds, and the finite physical cell index `c` below `y`, on which
`Dbar,n,K` and the proof cuts live.  The common-mass estimate is only

```text
h_cap(y)>249p(y)/250;
```

no cellwise `249/250` lower bound is used.

Set

```text
L(c)=696Dbar(c),
K(c)=L(c)+H_joint>=1.
```

The terminal survivor operator `O_s` performs one collision and then kills
the terminal `C24` descendants.  Therefore the exact regular-branch replay
is

```text
x -> T_s^(K-1)x ->[O_s] T_s^Kx
  ->[I] I T_s^Kx ->[T_s^K] Ix ->[I] x,

T_s^K I T_s^K=I,
I T_s^K I T_s^K=id.
```

The operator off-by-one is essential.  So is the scope of the pinned
Round-42 one-step theorem: for every unnormalised controlled canonical
finite-`Z` family,

```text
Z(O_s G)<=Z1 Z(G).
```

Consequently one palindrome obeys, uniformly in the unbounded finite tag
`K`,

```text
Z_return
 <=Z1 Z_in+[(Z1+1)C_p/2]mass_in.                    (3.1)
```

After imposing the forward predicate, the input to the reverse palindrome
may be arbitrarily thin and nonproper.  Formula (3.1) remains legal because
the universal Round-42 `Z1` estimate, rather than the proper-source
Round-47 `P*mass` envelope, is used there.  Properness is used only on the
two original marginal laws for the already frozen SYZ mass bound.

The exact once-charged sequential construction is

```text
A --forward predicate/palindrome--> A_fw
  --first-return replay--> B_fw --I--> I(B)_fw
  --reverse predicate/palindrome--> I(B)_cap
  --reverse first-return replay--> I(A)_cap --I--> A_cap.
```

All singularity, homogeneity, Growth and deterministic recuts are
subinterval refinements with immutable tags.  There is no transverse
redisintegration, same-measure shortcut, survivor normalization or second
physical mass charge.

Let `P(Z,m)` denote the uniform affine palindrome bound (3.1), and let
`R(Z,m)=A_R Z+B_R m` be the pinned hereditary total first-return resolvent.
Starting from `Z_0<=J_pair`, the global charges satisfy

```text
Z_1<=P(J_pair,nu(X)),
Z_2<=R(Z_1,nu(X)),
Z_3<=P(Z_2,nu(X)),
Z_4<=R(Z_3,nu(X)).
```

All constants are independent of `y,c,n,Dbar,K`.  This proves global
integrability, not merely recordwise finiteness.

Canonical half-open proof intervals are merged into physical components
only when their actual union is connected and the shared endpoint belongs
to the regular common set.  Closure-touching across an omitted singular,
null or cemetery puncture is never merged.  Positive coarsening gives

```text
J_cap,physical<=Z_cap,proof,

J_cap,total=integral J_cap(y)dlambda(y)<infinity.
```

The cross-component comparison also yields `E_cross<infinity`.  Round 53
may therefore be applied with its hypothesis discharged.  In addition to
the finite `exp(D_cap/6)` moment, minimality of `D_cap` gives

```text
integral h(y)2^D_cap(y)dlambda(y)
 <=H+(4/C_p)J_cap,total
 <infinity.
```

The actual promoted object is a proper, once-charged common terminal
two-view reference carrier.  It is not yet a proper physical first-return
kernel.

## Gate 4: the first-return defect is now exactly localized

The common raw restriction lies on subintervals of the original cells

```text
A_c subset R_n,
B_c=T_s^n(A_c) subset C_s.
```

With the immutable Borel return-depth tag, define

```text
tau_cap(y,x)=n(y),
Q_cap(y,x)=T_s^n x.
```

The inherited `R_n` predicate gives

```text
T_s^j x notin C_s, 1<=j<n,
T_s^n x in C_s.
```

Thus the once-charged common law already has an exact same-ID physical
first-return graph, and both its source and landing marginals have finite
adapted `Z`.  The strict triplet is

```text
exact same-ID tau_cap=n first-return graph: CERTIFIED_BUT_UNPROPER
physical source and landing finite-Z:       CERTIFIED
proper landing kernel at the same time n:   NOT_CERTIFIED
```

Finite `Z` is not properness.  A unit-mass isolated carrier of length
`1/(2C_p)` has finite normalized boundary functional `2C_p>C_p`; positive
cuts cannot improve that support-length lower bound.

Nor may a positive recovery clock be hidden inside the first return.  If
`x` first returns at time `n` and `r(x)>0`, then `T_s^(n+r)x` is either
outside `C_s`, or time `n` is already an earlier hit.  Hence `n+r` is never
the original first return.  The synchronized `696D_cap` evolution supplies
a legal proper reference graph lift with Borel endpoint maps, but not a
physical `Q_cap` coordinate.

The original `R_n` segment has its certified intermediate `C_s` avoidance.
It does not certify `C24` avoidance throughout any added properisation or
terminal schedule.  A 1001-cycle separator shows that terminal nonhit mass
can equal one while all-intermediate-avoidance mass is zero.

The shortest remaining Gate-4 interface is therefore one of:

1. prove the exact time-`n` common landing marginal already satisfies the
   properness bound, or construct an exact same-time positive
   coarsening/disintegration that does;
2. otherwise extract all same-ID first-hit branches with exact once coverage
   and prove every retained unshifted landing marginal proper.

Only after that object exists can the killed intermediate-avoidance ledger,
all later/repeated clocks, physical `q` and the strong cemetery be joined.

## Gate 5: exact clearance negative moment and hybrid frontier

For the actual owner law and

```text
alpha_opt=0.0012406563164308641348787500994058785...,
```

the optimal collar clock moment is now exactly equivalent to

```text
M_neg
 =integral min(1,d_other)^(-alpha_opt)dnu
 <infinity.
```

The layer-cake identity gives the sharp critical Dini boundary: a tail
`t^alpha_opt/(log(e/t))^(1+epsilon)` is sufficient, while log power one may
diverge.  A weaker recordwise physical route is also isolated.  If record
`a` supplies density, boundary-neighbourhood and pullback-gap constants
`D_a,A_a,g_a`, it suffices that

```text
sum_a D_a A_a g_a^(-eta/q)<infinity,
eta/q>alpha_opt.
```

For the inverse-square abstract exponent `eta=2/3`, this allows
`q<537.349995996...`; the missing input is the same-owner-law joint ledger,
not exponent size.

The long/short suffix split replaces an unnecessarily uniform horizon by

```text
C_rec sum_k w_Z^r_k m_k^long
 +sum_k 2^(k+1)m_k^short
 <infinity.
```

The long part still needs the Round-54/Round-42 same-operator join, and the
short part must pay debt rather than merely mass.

For the signed coupling row, truncated optimal transport satisfies

```text
d_OT<=min(d_sync,d_product),
```

with a strict flip example.  A critical polynomially weighted `d_OT` sum
can close without a uniform geometric rate.  This remains a signed
cancellation interface and cannot pay the positive collar/cemetery fields.

Finally, the exact records

```text
p_n=2^-n,
B_n=14,
d_n=2^-n^2
```

have full `A_col`, finite parent `Z`, stronger-than-required rank tails and
positive clearance, but divergent optimal clearance moment.  Therefore no
physical field is promoted and Gate 5 remains `10/18`, with zero complete
blocks.

## Gates 1, 2 and 3: sharper structural interfaces, no promotion

Gate 1 gains the exact general `2x2` weighted-projective defect identities

```text
(D_yD_x^-1)_12=a_ya_x(p_y-p_x)/Delta_x,
(D_yD_x^-1)_21=e_ye_x(q_y-q_x)/Delta_x.
```

Changing shear order or using an additive gauge only moves the nonlinear
compatibility defect.  A uniformly invertible additive gauge with
determinant at least `29/40` still admits a nonconvergent normalized critical
coordinate.  Physical convergence of both weighted defects and replay of
all twisting wedges in the identical representative remain missing.

Gate 2 gains the conditional stable-holonomy product bound

```text
|log J_hol|<=K,
|log J_hol-log J_hol^(N)|<=K lambda^(alpha N).
```

But an area-zero future transverse line can meet every spanning candidate
plaque after an arbitrarily long identical finite prefix.  A physical
stable-saturated base, stable projection and immutable holonomy fields are
still absent; Gate 2 remains `0/17`.

Gate 3 gains, at every fixed finite depth, a qualitative full-parameter
semialgebraic branch atlas and Hardt stratification, plus norm-one weak
Borel/TV restriction and assembly maps.  These give regular-bulk weak
intertwining only.  They do not give a depth-uniform boundary-complexity
bound, physical strong `R_s/Q_s`, directional Piola control or `MT_DQ`.
In particular, a uniform component bound `N(n,B)` requires a uniformly
bounded semialgebraic format `B`; unbounded `Dbar` supplies no global
`N_H` and cannot replace the Gate-4 palindrome.

The targeted current-source check, including Demers--Liverani
`arXiv:2606.10155v1`, found useful transfer-operator, coupling and
absolute-continuous-holonomy context but no theorem directly constructing
the required same-ID physical landing kernel, all-intermediate avoidance,
Gate-2 stable quotient, Gate-3 strong operator join or strong cemetery.

## Independent audit and full validation

The independent Gate-4 audit reports `PASS` with no remaining blocker for
the claims actually made.  It separately replays the reversible direction,
operator off-by-one, Borel clock registry, two-closed-leg/one-cut affine
ledger, nonproper second input, paired directions, once charge, physical
component policy, global `J_cap` integral and the exact nonpromotion fields.

The final five-leaf validation matrix is:

```text
syntax:                    10/10 PASS
dependency pins:           38/38 PASS
deterministic replay:       5/5 PASS
byte-identical re-emission: 5/5 PASS
hostile mutations:          349/349 rejected
leaf SHA entries:           20/20 PASS
default cert/verifier:      10/10 exit 2
temporary/stale artifacts:  none
```

The first-return SHA ledger intentionally uses workspace-relative
`deliverables/...` paths, while the other four ledgers use deliverables-local
basenames; each was validated from its declared path scope.

## Strict status after Round 57

```text
Gate 1: NOT_CERTIFIED
Gate 2: NOT_CERTIFIED (0/17 physical fields)
Gate 3: NOT_CERTIFIED
Gate 4: NOT_CERTIFIED
Gate 5: NOT_CERTIFIED (10/18; complete blocks 0)
complete composite gates: 0/5
CM2: NO-GO_FOR_CLAIM
```

The next shortest primary route is the exact time-`n` physical landing
properness/coarsening problem.  In parallel, Gate 5 should materialize the
same-owner-law `D_a,A_a,g_a` ledger or the actual long/short same-operator
join; Gate 1 should prove weighted-projective convergence in the final
twisting gauge; Gate 2 should construct the stable quotient; and Gate 3
should turn the fixed-depth atlas into a depth-uniform strong restriction,
directional Piola and `MT_DQ` block.

## Frozen Round-57 leaves

- `cm2-gate34-round57-exact-slope4-cross-endpoint-frontier-assault-2026-07-20.md`
- `cm2-gate4-round57-unshifted-first-return-frontier-assault-2026-07-20.md`
- `cm2-gate34-round57-palindromic-jcap-independent-audit-2026-07-20.md`
- `cm2-gate5-round57-clearance-negative-moment-ot-frontier-assault-2026-07-20.md`
- `cm2-gate123-round57-projective-holonomy-semialgebraic-dq-frontier-assault-2026-07-20.md`

Their recursive SHA ledgers are pinned by
`cm2-fifty-seventh-direct-assault-manifest-2026-07-20.sha256`.
