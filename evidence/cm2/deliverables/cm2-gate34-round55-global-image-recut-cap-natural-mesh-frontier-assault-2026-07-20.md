# CM2 Round 55 Gate-4 global image-recut cap / natural-mesh frontier

Date: 2026-07-20  
Strict status: **the arbitrary-time image-recut derivative product is replaced
by one uniform fixed multiplier.  A connected unstable image has adapted
length `<2397`, so deterministic adapted recutting at `10^-90` creates at
most `2397*10^90+1` cells under the frozen Round-50 endpoint-owner
convention.  The remaining natural `delta_B` path aggregation,
metric alignment, and same-ID canonical-atom join are not pinned.  Physical
`J_pair`, `I_D`, Gate 4, and CM2 remain not certified.**

## 1. Frozen inputs and scope

This append-only leaf hash-pins eight manifests: the Round-35 arbitrary-`R_n`
carrier and physical incidence moment, the Round-36 retained-depth separator,
the Round-42 numerical killed-Growth block, the Round-50 physical whole-family
grouping convention, the Round-53 `J_pair` target, and both relevant Round-54
Gate-4 leaves.  It changes no earlier artifact.

The claim applies to every fixed `|s|<=1/400` and every connected image

```text
H(A)=T_s^n(A)
```

on one regular finite rank-path branch.  It does not enumerate the branches
and does not identify the remaining natural mesh with the terminal canonical
family.

## 2. Global connected-image recut count

The frozen maximum Euclidean length and metric comparison are

```text
ell_E(H(A)) < 68,
ell_*(H(A)) < (141/4) ell_E(H(A)) < 2397.
```

Round 35 deterministically recuts the image in adapted arclength at

```text
delta_* = 10^-90.
```

Round 50 freezes the half-open endpoint-owner convention as
`ceil(length(H(A))*10^90)+1`.  Since `2397*10^90` is an integer, strictness
therefore gives the exact uniform bound

```text
N_img
 <= ceil(ell_*(H(A))/delta_*)+1
 <= 2397*10^90+1.                                    (2.1)
```

No rank-path derivative product appears in (2.1).  In particular, the old
upper `length(H(A))<=D_path length(A)` is unnecessary for proving finiteness
or multiplicity of this fixed image recut.

## 3. Exact density-ratio refinement lemma

Let one measured parent have mass `p`, length `ell`, and normalized density
`rho`.  Suppose

```text
sup rho / inf rho <= R.
```

The average of `rho` is `1/ell`, hence `sup rho<=R/ell`.  For any half-open
interval refinement into at most `N` positive cells,

```text
p_j/ell_j <= p sup rho <= R p/ell,

sum_j p_j/ell_j <= R N p/ell.                        (3.1)
```

Using the registered `R=2000/1999` and (2.1), one valid multiplier is

```text
(2000/1999)*(2397*10^90+1)
=(4794*10^93+2000)/1999.                             (3.2)
```

On compatible once-charged parents in the two orientations, (3.1) gives the
conditional paired interface

```text
J_pair
 <= N_img * (
      R_source Z_source,natural
      + R_target Z_image,coarse
    ).                                                (3.3)
```

Equation (3.3) is an abstract same-metric refinement theorem.  It is not a
physical `J_pair` estimate until the natural source family and the coarse
terminal image family are placed on the exact same Round-35 IDs.

## 4. What happens to the Round-36 separator

The Round-36 manifest remains valid and is pinned at

```text
8b6bd9a1b72e1d222ea0b370f046defbf90b10935c55271d4f5bf81a36835ba5.
```

Its perfectly correlated rank model has

```text
P(B>b)=4^-b,
E[2^(3B/2)]<infinity,
B_i=B,
N_recut,n proportional to 2^(sum_i B_i)=2^(nB).
```

It correctly proves that the installed one-time rank/D1 fields do not imply a
product-moment bound.  It does not disprove (2.1), because its product model
does not include the newly joined global connected-image length cap.  The
precise update is therefore:

```text
image-recut derivative product: replaced by fixed multiplier;
natural-mesh path product:      still fail-closed.
```

## 5. Exact natural-mesh frontier

The frozen natural scale is

```text
delta_B=2^-ceil(3(B+1)/2).
```

Parity gives the useful exact arithmetic

```text
delta_B^-1 <= 4*2^(3B/2),
delta_B^-2 <= 16*2^(3B).                              (5.1)
```

The factor four is sharp on even `B`; odd `B` has squared ratio eight.  The
physical collision-SRB input

```text
integral 2^(3 B_inc/2) dmu_s < 134217735/64
```

is therefore at the correct formal exponent for an additive mesh count.
Three same-law facts are nevertheless absent from the frozen registry:

1. whether the arbitrary-`R_n` natural mesh uses a path maximum or an
   additive timewise union, together with a Kac-tower domination rather than
   a product;
2. one explicit metric ledger joining `delta_B`, adapted `10^-90` recuts, and
   both `ell_fw/ell_rev` functionals;
3. a same-ID identification of those natural cells as refinements of the
   Round-42/Round-54 coarse terminal canonical atoms.

Until all three are proved, the `q0=3/2` moment cannot be silently multiplied
by a representation-dependent coarse `Z` ledger.

## 6. Strict boundary

Certified by this leaf:

```text
connected image ell_*<2397:                         CERTIFIED
N_img<=2397*10^90+1:                                CERTIFIED
density-ratio N-cell refinement inequality:         CERTIFIED ABSTRACTLY
image-recut derivative product required:            FALSE
```

Still not certified:

```text
natural path-rank aggregation / Kac join:           NOT_CERTIFIED
natural/image metric alignment:                     NOT_CERTIFIED
same-ID canonical-atom refinement:                  NOT_CERTIFIED
coarse terminal Z l1 by this leaf:                   NOT_CERTIFIED
terminal refinement -> physical J_pair:             NOT_CERTIFIED
physical I_D, C_fw, C_rev, q and strong cemetery:    NOT_CERTIFIED
Gate 4 / Gate 5 / CM2:                              NOT_CERTIFIED / NO-GO
```

## 7. Verification

New artifacts:

- `cm2_gate34_round55_global_image_recut_cap_natural_mesh_frontier_cert.py`;
- `cm2_gate34_round55_global_image_recut_cap_natural_mesh_frontier_verifier.py`;
- `cm2-gate34-round55-global-image-recut-cap-natural-mesh-frontier-manifest-2026-07-20.json`;
- `cm2-gate34-round55-global-image-recut-cap-natural-mesh-frontier-manifest-2026-07-20.sha256`.

The verifier independently reconstructs `2397`, the integer ceiling bound,
the registered density multiplier, all parity rows in (5.1), the Round-36
separator scope, dependency hashes, the internal replay digest, and every
nonpromotion field.  It rejects duplicate JSON keys and `NaN/Infinity`,
checks AST literal guards, and compares deterministic re-emission byte for
byte.

Final replay targets:

```text
syntax:                         2/2 PASS
dependency SHA validation:     8/8 PASS
independent replay:             PASS
hostile mutations:             27/27 REJECTED
deterministic re-emission:      byte-identical PASS
certificate/verifier defaults: both exit exactly 2
physical J_pair / Gate 4 / CM2: NOT_CERTIFIED / NOT_CERTIFIED / NO-GO
```
