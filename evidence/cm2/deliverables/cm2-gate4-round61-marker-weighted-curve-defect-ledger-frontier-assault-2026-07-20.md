# CM2 Round 61 Gate 4 — marker-weighted curve / positive graph-defect frontier

Date: 2026-07-20  
Strict status: **the actual common landing marker now has an exact
marker-weighted physical cone-curve measure lift, and the full
good/bad original-time graph has an exact endpoint-preserving positive
decomposition in a finite weighted graph-total-variation Banach lattice.
This is a real measure/carrier upgrade, but it is not a dynamically invariant
unstable Rokhlin quotient, stable holonomy, a fibrewise proper landing, an
anisotropic/current strong space, or an accepted strong cemetery.  Gate 4
remains `NOT_CERTIFIED`; the global state remains `0/5` and
`CM2=NO-GO_FOR_CLAIM`.**

## 1. Frozen Round-60 baseline

This leaf is append-only.  It pins the complete Round-60 aggregate and the
entire Gate-4 leaf:

```text
Round-60 aggregate report:
ef3f2739a7a0ed8c91e82400c05564f97f0c3326ebc1373764b51bbd48f04212

Round-60 recursive SHA ledger:
5f6c90735cbb74ec7b36f6c1d2b012ccccaafa40793d291dfcb6a0e212044e9e

Round-60 Gate-4 report:
88f042ed434f6199123a5f164385572dabfcde0af13a8d6b9d7ca1385079d00f

Round-60 Gate-4 manifest:
08cda3c966ce03967471c5d87216f4b32ee29a293d81d9dcdd434eb39f7696b3

Round-60 Gate-4 SHA ledger:
4d7c3f06e1671319482c064ec3ea817562567202b111fd014434b7be0a460fac
```

The pinned physical facts are:

```text
0<=kappa_B<=mu_C,
g_B=d kappa_B/d mu_C, 0<=g_B<=1,

Gamma_cap=Gamma_G+Gamma_B,
G={z_land<C_p}, B={z_land>=C_p},

integral_B h(y)2^D_land(y) dlambda(y)<infinity.
```

The seven-field physical landing join was `1/7`, with only the tagged Borel
branch inverse complete.  The present leaf never replaces the physical
landing endpoint, return time, path, immutable ID, half-open owner, or once
charge.

## 2. Exact marker-weighted physical curve-measure lift

Round 35 constructs, on every regular finite-depth `R_n` component, the
standard-Borel slope-four registry

```text
Psi_s(b,r)=(r,phi=4r+b),
p=sin(4r+b),
dell_A=sqrt(17)dr,
rho_A=cp/sqrt(17).
```

The curves lie in the frozen invariant unstable cone.  Integrating their
leaf measures recovers the collision-SRB measure on the component exactly.
Round 56 then performs the physical first-return replay from the finite-`Z`
source family and proves

```text
E_fw,total represents
(T_C_s)_#(mu_s|C_s)=mu_s|C_s
exactly once.
```

Every terminal proof curve is a regular image

```text
B'=T_s^n(A') subset B=T_s^n(A),
```

where `A'` is a subinterval of one exact Round-35 slope-four source leaf and
the full `n/path/ID/owner` tag is retained.

Write this actual physical standard-family representation as

```text
mu_C=integral mu_omega dP(omega).
```

The Round-60 marker may now be multiplied into that representation.  For
every Borel landing event `E`, Tonelli gives the exact identity

```text
kappa_B(E)
 =integral_E g_B dmu_C
 =integral [integral_E g_B dmu_omega] dP(omega).
                                                        (2.1)
```

Thus

```text
kappa_B=integral (g_B mu_omega)dP(omega)               (2.2)
```

is an actual marker-weighted physical cone-curve **measure** lift.  The
unmarked `mu_omega` are the frozen regular standard-family elements; arbitrary
Borel multiplication by `g_B` is not claimed to leave that regular class.  It uses neither
conditional normalization nor singleton quotient mass.  Zero marked proof
curves are simply absent.

On a source branch put

```text
a=d kappa_A/dmu_C,
0<=a<=1.
```

The source marked leaf density is exactly

```text
rho_A,marked=a cp/sqrt(17).                            (2.3)
```

For a fixed tagged regular branch `H=T_s^n|U`, simultaneous pushforward of
the leaf and its outer tag gives

```text
rho_B,marked(Hx)
 =a(x)[cp(x)/sqrt(17)]/J_ellE H(x),                   (2.4)
```

where `J_ellE H` is the one-dimensional Jacobian for the same Euclidean
carrier arclength used in `dell_A=sqrt(17)dr`.  The image is a regular
unstable-cone `C^1` curve, not generally another slope-four curve.  Equation
(2.4) is a curve-measure formula, not a regular-standard-family or two-sided
estimate.

### Exact type boundary

Equations (2.1)--(2.4) do **not** complete the physical Rokhlin rows:

- the slope-four registry is an artificial unstable-cone foliation, not a
  partition subordinate to dynamically invariant unstable manifolds;
- after proof tags are forgotten, standard-family curves need not be a
  disjoint physical partition;
- no invariant stable projection or two-sided stable-holonomy Jacobian has
  been constructed;
- an arbitrary Borel `0<=g_B<=1` may have zero lower density, arbitrarily
  many support components and unbounded logarithmic or BV oscillation;
- neither `F`, `R`, `theta`, nor a compatible plaque span `L` is bounded.

Accordingly, fields 1 and 4 gain exact **partial** interfaces; no new
seven-field row becomes complete.

## 3. Exact weighted graph-defect Banach lattice

The Round-60 five-interface hybrid can now be made exact at the tagged graph
measure level.

Let `Y` denote the frozen physical landing fibre and let `D=D_land(Y)`.  Define

```text
X_D^graph
 ={finite signed tagged graph measures nu:
   integral 2^D d|nu|<infinity},

||nu||_X=integral 2^D d|nu|.                          (3.1)
```

Multiplication

```text
nu -> 2^D nu
```

is an isometric order isomorphism from `X_D^graph` onto the finite signed
measures with total-variation norm.  Hence (3.1) defines a Banach lattice.

Embed the bad graph without changing it:

```text
E_B Gamma_B=Gamma_B.                                  (3.2)
```

The pinned dyadic moment gives the exact norm identity

```text
||E_B Gamma_B||_X
 =integral_B h(y)2^D_land(y)dlambda(y)
 <infinity.                                           (3.3)
```

Since `2^D>=1`, both physical endpoint pushforwards satisfy

```text
||(pr_source)_#nu||_TV<=||nu||_X,
||(pr_landing)_#nu||_TV<=||nu||_X.                   (3.4)
```

All branch tags are part of the graph coordinate.  Therefore the exact
positive hybrid identity is

```text
Gamma_cap
 =Gamma_G+iota_B(E_B Gamma_B).                        (3.5)
```

Equation (3.5) retains the original source and landing, `n`, first-hit path,
immutable physical ID, half-open owner and one parent charge.  It performs
no normalization, endpoint projection, duplicate charge, deletion, or
replacement by the stopped `D_cap` reference coordinate.

There is also an explicit ordinary-total-variation tail:

```text
|Gamma_B|{D_land>=k}
 <=2^-k ||E_B Gamma_B||_X.                            (3.6)
```

The weighted tail itself vanishes qualitatively by integrability.

This closes four of five interfaces in a precisely declared **graph-TV
defect ledger**:

| interface | graph-TV state | requested strong current/cemetery state |
|---:|---|---|
| 1. positive endpoint-preserving embedding | certified by (3.2) | not certified |
| 2. exact tags and once charge | certified | certified as the one already-typed strong-hybrid row |
| 3. norm paid by `D_land` | equality (3.3) | not certified |
| 4. exact full=good+bad identity | certified by (3.5) | not certified |
| 5. downstream CM2 acceptance theorem | not certified | not certified |

The ratio `4/5` refers only to this new graph-ledger interface.  Among the
five requested strong-hybrid rows, only the tag/once-charge row is complete
(`1/5`).  Neither number is a Gate score or a strong-cemetery maturity claim.

## 4. Why the `D_land` moment is not by itself a BV/derivative strong defect norm

An exact smooth separator rules out the missing promotion.  It is a logical
product model, not a claim that the billiard realizes the marker.

Set

```text
C_p=4*10^90*360493663/358863,
L=2/(3C_p).
```

On the short full plaque `[0,L]`, use identity first return, identity
holonomy, one physical ID and the smooth marker

```text
g_N(x)=1/2+(1/4)sin(2*pi*N*x/L),
1/4<=g_N<=3/4.                                        (4.1)
```

There are no support gaps.  Direct calculation gives

```text
h_N=integral g_N=L/2,
J_N=h_N/L=1/2,
z_N=J_N/h_N=1/L=3C_p/2,
D_land=2,
h_N 2^D_land=2L=4/(3C_p),                             (4.2)

integral_0^L |g_N'(x)|dx=N.                          (4.3)
```

The graph defect norm in (4.2) is independent of `N`, while the BV derivative
charge in (4.3) tends to infinity.  The example has a strictly positive
smooth marker, perfect endpoints and perfect holonomy.  Therefore no
universal estimate of the form

```text
BV/derivative trace defect
 <=C integral h2^D_land
```

can follow from the installed fields alone.  This example does not purport to
refute every possible anisotropic norm; rather, no typed identification with
the requested anisotropic/current space is installed, and any route that
requires BV or derivative-trace control needs genuinely additional physical
regularity.

## 5. Updated seven-field audit

| # | Physical landing field | Round-61 state |
|---:|---|---|
| 1 | physical product-rectangle cover | `PARTIAL`: exact tagged finite-depth cone-curve atlas; no stable product rectangles |
| 2 | stable projection and two-sided holonomy Jacobian | `NOT_CERTIFIED` |
| 3 | full span or quantitative marker fragmentation | `NOT_CERTIFIED` |
| 4 | same-measure unstable conditionals and density bounds | `PARTIAL`: exact marker-weighted curve-measure lift; it need not be a regular standard family and gives no Rokhlin partition or two-sided bounds |
| 5 | physical boundary charge below `C_p` | `NOT_CERTIFIED` |
| 6 | tagged Borel branch inverse retaining `n/path/ID/owner` | `CERTIFIED` (Round 59) |
| 7 | strong restriction and assembly | `PARTIAL`: exact graph-TV assembly only; no current/operator assembly |

The complete count stays exactly `1/7`; Gate 2's official immutable score
stays `0/17`.  The numerical bridge

```text
F(u)R(u)<C_p theta(u)L(u)                             (5.1)
```

remains unpaid on the actual common law.

## 6. Latest official-technology audit

The official arXiv API was checked through 2026-07-20.  The latest relevant
billiard entry remains Demers--Liverani `2606.10155v1`, a transfer-operator
review; it contains no theorem with the present common-landing measure,
marker geometry and operator type.

Climenhaga--Day `2604.25881v1`, *Every finite horizon Sinai billiard map has
a unique measure of maximal entropy*, gives a strong total-length result:
for unstable `C^1` curves above a fixed length scale, `|T^nV|` is controlled
above and below by a constant times `exp(n h_top)`.  This does not close
(5.1):

- the paper's product law and symbolic holonomy are for its MME, not the
  pinned Liouville/SRB marker `g_B mu_C`;
- total image length controls neither the number of retained marker
  components nor the inverse component lengths in `J_land,min`;
- it supplies no retained fraction `theta`, two-sided marker density ratio
  `R`, or actual common-landing field-7 assembly.

Canestrari `2604.19671v2` evolves an **already regular** standard family.
Thin restrictions pay survival/conditioned-mass normalization, and its
qualitative output constant is not a strict `B<C_p` theorem.  The paper also
notes that no known general piecewise-hyperbolic Banach space contains
standard pairs.  Its `C^1`-test coupling therefore cannot identify
`X_D^graph` with CM2's requested anisotropic/current strong space.

No external theorem is promoted into the certificate.

## 7. Downstream typing

The original `R_n` first-hit predicate

```text
[product_(j=1)^(n-1) 1_(C_s^c)(T_s^j x)]
*1_(C_s)(T_s^n x)=1
```

is retained on both `Gamma_G` and `E_B Gamma_B` in (3.5).  Thus the graph
ledger keeps killed intermediate `C24` semantics exactly.  It becomes an
accepted killed strong kernel only if a future theorem accepts the positive
bad remainder in the required target space.

The remaining joins do not follow:

- `Gamma_G` may have zero mass and its physical source properness is not
  proved;
- `D_land` is landing geometry, not a later/repeated recovery clock;
- `X_D^graph` is weighted `L^1` total variation, not physical
  `q in L^(6/5)` and not same-law all-time `C_fw/C_rev`;
- the bad law is ordinary positive collision-SRB mass, not collision-null
  singular cemetery;
- the separator in Section 4 blocks any free BV/derivative-trace upgrade;
  the broader anisotropic/current join remains untyped and uncertified.

## 8. Strict frontier

```text
actual physical landing RN marker:                   CERTIFIED (pinned)
actual marker-weighted cone-curve measure lift:      CERTIFIED_EXACT
physical invariant unstable Rokhlin/stable holonomy: NOT_CERTIFIED
seven-field landing join:                            1/7 complete; fields 1,4,7 partial
fibrewise J_land,min<C_p h:                          NOT_CERTIFIED
positive good/bad weighted graph hybrid:             CERTIFIED_EXACT_IN_X_D_GRAPH_ONLY
D_land pays weighted graph-TV defect:                CERTIFIED_EXACT
D_land pays BV/current strong defect:                NOT_CERTIFIED; BV/derivative route false
downstream accepts positive graph defect:            NOT_CERTIFIED
proper physical same-ID first-return landing/kernel: NOT_CERTIFIED
original R_n intermediate C24 avoidance:             CERTIFIED (pinned)
later/repeated recovery-clock moments:               NOT_CERTIFIED
physical q in L^(6/5):                               NOT_CERTIFIED
strong singular/current cemetery:                    NOT_CERTIFIED
Gate 4:                                              NOT_CERTIFIED
complete composite gates:                            0/5
CM2:                                                 NO-GO_FOR_CLAIM
```

## 9. Executable evidence

- `cm2_gate4_round61_marker_weighted_curve_defect_ledger_frontier_cert.py`;
- `cm2_gate4_round61_marker_weighted_curve_defect_ledger_frontier_verifier.py`;
- `cm2-gate4-round61-marker-weighted-curve-defect-ledger-frontier-manifest-2026-07-20.json`;
- `cm2-gate4-round61-marker-weighted-curve-defect-ledger-frontier-manifest-2026-07-20.sha256`.

The suite pins and semantically replays the Round-60 aggregate and Gate-4
leaf, the Round-35 slope-four collision-SRB disintegration, the Round-56
exact once-covered physical curve representation, the Round-58 dyadic moment
and the Round-57 first-hit graph.  It independently replays the weighted
graph lattice and smooth-BV separator, checks strict JSON and deterministic
regeneration, requires byte-identical re-emission, rejects hostile semantic
mutations, and requires both default entry points to fail closed with exit
code `2`.
