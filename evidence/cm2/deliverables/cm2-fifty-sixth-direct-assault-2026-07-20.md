# CM2 Round 56 full direct assault

Date: 2026-07-20  
Strict verdict: **0/5 complete gates; Gate 5 maturity 10/18; CM2 NO-GO**

## Round result

Round 56 closes the physical adapted-metric terminal numerator `J_pair`, the
matching defect moment `I_D`, and the downstream ambient preproperisation
clock condition.  It also installs the exact pointwise-minimal Gate-5 collar
recovery clock and independently audits the remaining Gate-1/2/3 poles and
the Round-55 aggregate ledger.  No composite gate is closed.

The four append-only leaves are:

1. exact-leaf `B_max`/Kac natural-mesh closure of physical `J_pair` and `I_D`;
2. acyclic discharge of the Round-51/52 full ambient clock/envelope condition;
3. optimal exact-`gamma` delayed collar recovery and its sharp clearance
   threshold;
4. independent Gate-1/2/3, current-literature and Round-55 bookkeeping audit.

All dynamical claims are parameterwise for every fixed `|s|<=1/400` and use
the stationary physical map `T_s`.  No stationary Growth theorem is promoted
to an arbitrary moving-map sequence.

## Gate 4: exact physical leaf typing is repaired by a paired replay

Equality of represented measures is not equality of standard-family leaf
representations, because boundary `Z` depends on the carriers.  The new leaf
therefore starts from the exact Round-35 slope-four collision-SRB kernel

```text
phi(r)=4r+b,
p(r)=sin(4r+b),
rho_leaf=cp/sqrt(17).
```

It uses only finite safe envelopes of the 24 compact central-homogeneity
cores.  On every retained source parent,

```text
|p|<3/10,
cp=sqrt(1-p^2)>sqrt(91)/10>19/20,
|d_r log(cp)|<24/19.
```

After the fixed `delta_open<10^-90` chop and the certified core
characteristic multiplier, this gives an exact finite-`Z` slope-four
representation of `mu_s|C_s`.  The full-grazing collision section is not
used as a fixed-scale input.

The leaf type is then preserved through the explicit paired chain

```text
exact A --T_s^n--> exact B
        --I-->     exact I(B)
        --T_s^n--> exact I(A)
        --I-->     exact A.
```

The first forward hereditary replay produces finite total terminal `Z` on
tagged subcurves `B' subset B`.  Induced-map invariance makes their union one
copy of `mu_s|C_s`.  Time reversal gives a finite-`Z` tagged `I(B')` family;
the identity

```text
T_s^k(I(B))=I(T_s^(n-k)(A)), 0<=k<=n,
```

and the `R_n` no-intermediate-return tag make its reverse first return occur
at exactly time `n`, on `I(A)`.  A second hereditary replay and one final
application of `I` therefore give a finite-`Z` proof refinement of the exact
Round-35 slope-four `A` leaves.  There is no transverse redisintegration, no
same-measure shortcut and no double charging.

## Gate 4: `B_max` and the Kac tower close the natural-mesh charge

For a regular first-return path set

```text
B_i(x)=B_inc(T_s^(i-1)x), 1<=i<=n,
B_max=max_i B_i,
delta_B=2^-ceil(3(B+1)/2).
```

One source grid at `delta_(B_max)` is enough.  Exact parity arithmetic gives

```text
delta_(B_max)^-1
 <=sum_(i=1)^n delta_(B_i)^-1
 <=4 sum_(i=1)^n 2^(3B_i/2).
```

The occupied first-return tower levels are `0,...,n-1`, including one level
when `n=1`.  Their disjointness and the physical collision-SRB moment yield

```text
sum_n integral_(R_n) delta_(B_max)^-1 dmu_s
 <4*(134217735/64)
 =134217735/16.                                      (2.1)
```

No ergodicity or full-saturation equality is used, and (2.1) is not
multiplied by an independent coarse-`Z` random variable.

The exact source law also contains the adapted `10^-90` source recut.  The
static additive ledger is therefore applied in three stages:

```text
finite-Z exact path parent
 -> adapted delta_open proof chop
 -> Euclidean delta_(B_max) grid
 -> adapted 10^-90 source grid.
```

Full rank-grid cells are paid by (2.1) and
`ell_E<=(27/5)ell_*`, giving the strict numerical contribution

```text
(27/5)*(134217735/16)=724775769/16.
```

At every stage there are at most two clipped endpoint cells per current proof
parent, paid additively by the density ratio `2000/1999` and the preceding
finite `Z`.  This avoids the invalid inference from two unrelated `L1`
bounds.

Replaying hereditary Growth from the resulting exact source-short law gives
finite terminal image `Z`.  Round 55's global image-recut cap

```text
N_img=2397*10^90+1
```

and the density-ratio refinement lemma then give

```text
J_pair
 <=(2000/1999)(2397*10^90+1)
   (Z_src,short,*+Z_img,short,*)
 <infinity.                                          (2.2)
```

Both length functionals in (2.2) are the physical adapted carrier lengths
of the same once-charged Round-35/50 cell.  The exact Round-53 bridge now
applies on that same measure:

```text
I_D<nu(X)+[35/(99*2^309)]J_pair<infinity.             (2.3)
```

The full Gate-4 leaf passed an independent adversarial mathematical audit.

## Gate 4 downstream: the ambient full-clock condition is discharged

Round 51 constructs the two proper view kernels and one selected
once-charged proper reference law recordwise, before assuming any global
`Dbar` moment.  Round 52 supplies one uniform finite `H_joint`, removes the
marginal shells (`k_fw=k_rev=0`), and proves the conditional total clock

```text
C_sigma,total=696*Dbar+H_joint+221328.
```

Because `696/4176=1/6`, (2.3) is precisely the previously missing condition

```text
I_D=integral p(y) exp(Dbar(y)/6) dlambda(y)<infinity.
```

The substitution is acyclic: the `J_pair -> I_D` proof uses neither the
full-clock conclusion nor its finiteness.  Thus, with

```text
A_Hjoint=(6/5)^(318+ceil(H_joint/696))<infinity,
```

the actual once-charged common-law bound is

```text
integral Wtilde_r,*^r dm_*
 <nu(X)+2 A_Hjoint I_D
 <infinity,
```

or, after (2.3),

```text
integral Wtilde_r,*^r dm_*
 <nu(X)+2 A_Hjoint
   (nu(X)+[35/(99*2^309)]J_pair)
 <infinity.
```

Consequently the full ambient two-view max-clock exponential moment and the
parent-charged total ambient `L^(6/5)` envelope are qualitatively finite, and
the absolutely continuous ambient envelope mass outside every increasing
full-mass Borel exhaustion tends to zero.

This is not physical `q`: the conclusion is an ambient envelope and does not
construct a geometrically proper common same-ID return, control intermediate
`C24` avoidance, sum later/repeated recovery clocks, or prove a singular/current
cemetery theorem.  `H_joint` and the finite `J_pair` upper are not materialized
numerically, so no numerical `C_fw,C_rev,q` is claimed.

## Gate 5: the exact minimal collar recovery clock

The frozen numerical C24 contraction is

```text
gamma
 =0.4999208855127157913660046706058806115637447433649505...,
beta=log(2)/log(1/gamma)
 =0.9997717578875635395264334017740554984542370979858221....
```

At collar level `k`, the pointwise shortest integer clock that reduces the
inherited term below `m_k` is

```text
r_k=min{r>=0:gamma^r 2^(k+1)<=1}
   =ceil(beta(k+1)).                                  (4.1)
```

Every other legal clock is at least `r_k`, so (4.1) is also pointwise
minimal for every increasing emission weight.  The first actual saved block
occurs at `k=4381`.

For a geometric clearance law `m_k=C 2^(-alpha k)`, the exact critical
exponent improves strictly from

```text
alpha_55=log_2(w_Z)
        =0.0012409395510954121047151792824672...
```

to

```text
alpha_opt=beta log_2(w_Z)
         =0.0012406563164308641348787500994059....
```

The explicit separator `alpha=0.0012408` makes the optimal-clock ledger
converge while the Round-55 `k+1` geometric ledger diverges.

An inverse-square boundary shell has an `O(epsilon^(2/3))` collar-neighbourhood
length and would be more than sufficient if the physical owner trace law had
a uniform bounded density in the same collar coordinate, together with a
uniform pullback gap and controlled extra-boundary complexity.  The frozen
input permits an arbitrary finite Borel owner law and supplies none of those
uniform joins.  The `2/3` route is therefore conditional and Gate-5 maturity
stays `10/18`, with zero complete 18-field blocks.

## Gate 1/2/3 and current technology audit

The independent audit keeps the structural poles precisely typed:

```text
Gate 1 physical full-cross/common vertex:        ALREADY CERTIFIED
Gate 1 same representative class-H + twisting:  NOT CERTIFIED

Gate 2 candidate affine cone-product layers:     7/7 (NON-OFFICIAL)
Gate 2 invariant stable-product layers:          0
Gate 2 immutable physical fields:                0/17

Gate 3 fixed free graph-current slots:           41,508
Gate 3 physical Q_s/R_s, dynamic Piola, MT_DQ:   NOT CERTIFIED
```

The first Gate-1 missing row remains a physical same-representative
nonlinear cross-term estimate, such as an instantiated `omega<m` row or an
exact finite-memory cancellation, followed by replay of the selected
twisting wedges in that representative.

Gate 2 still first needs a positive stable-saturated product base
`Lambda_A`, a reference unstable interval, a physical stable projection and
the conditional stable-holonomy SRB Jacobian.  Label abundance and mixing
theorems do not construct those objects.

Gate 3 has a large finite free carrier but still lacks the physical
finite-`s` `Q_s/R_s` carrier/operator join, dynamic directional Piola control
in the required strong space, suffix bounds and `MT_DQ`.

The current official-source audit included, among others,
`arXiv:2607.06242v2` (generalized coupling for Markov cocycles) and
`arXiv:2607.11467v1` (tensor divergence-measure/BV normal traces).  The first
does not construct the Gate-2 stable quotient, Frostman law or stopping
registry; the second does not supply the billiard dynamic Piola identity,
anisotropic physical strong norm or `MT_DQ`.  No checked 2025--2026 theorem
directly closes a missing physical interface.

## Round-55 ledger corrections

Two append-only corrections are frozen:

1. the live Round-55 hostile total is `40+27+127=194`, not the stale
   aggregate `38+27+115=180`; all live tests pass, so this is bookkeeping and
   not a leaf-theorem failure;
2. the synchronized and normalized-product Gate-5 coupling witnesses are
   incomparable.  Only

   ```text
   d_best=min(d_sync,d_product)
   ```

   is never worse and sometimes strictly better.  The synchronized witness
   remains a valid same-source BL coupling, but it has no certified physical
   separation rate.

## Strict status

```text
Gate 1: NOT CERTIFIED
Gate 2: NOT CERTIFIED (0/17 physical fields)
Gate 3: NOT CERTIFIED
Gate 4: NOT CERTIFIED
Gate 5: NOT CERTIFIED (10/18; complete blocks 0)
complete composite gates: 0/5
CM2: NO-GO_FOR_CLAIM
```

Certified Round-56 advances:

```text
exact slope-four paired reverse leaf replay:     CERTIFIED
B_max natural mesh / Kac inverse-mesh charge:   CERTIFIED
physical adapted terminal J_pair:                CERTIFIED_FINITE
physical defect moment I_D:                      CERTIFIED_FINITE
full ambient two-view max-clock exp moment:      CERTIFIED_QUALITATIVELY_FINITE
parent-charged ambient L^(6/5) envelope:         CERTIFIED_QUALITATIVELY_FINITE
ambient AC Borel-exhaustion tail:                CERTIFIED_VANISHING
exact minimal collar recovery clock:             CERTIFIED
strictly improved collar threshold:              CERTIFIED
```

Still open:

```text
J_cap/common-intersection boundary geometry,
physical proper same-ID first-return kernel,
intermediate C24 avoidance and later/repeated clocks,
physical q and strong singular/current cemetery,
physical same-source separation or weak-clearance owner-law theorem,
Gate-3 physical Q_s/R_s, Piola/suffix joins and MT_DQ,
Gate-1 same representative and Gate-2 stable quotient.
```

## Validation

- Python syntax: `8/8 PASS`;
- strict JSON, dependency pins, exact arithmetic, provenance/verdict parity,
  deterministic replay and AST nonpromotion guards: PASS;
- hostile manifest mutations:
  `55/55 + 37/37 + 109/109 + 38/38 = 239/239` rejected;
- certificate/verifier defaults: `8/8`, each exactly exit `2`;
- deterministic manifest regeneration/re-emission: the three generative
  leaves are `3/3` byte-identical; the independent audit leaf passes strict
  integrity and live semantic replay;
- all four leaf SHA ledgers: PASS;
- Round-56 aggregate SHA ledger: PASS.

These regression suites establish package integrity and fail-close behavior;
they do not replace the analytic arguments.  The Gate-4 closure was separately
audited after both the source-`10^-90` recut and exact-leaf representation
issues were found and repaired.

## Next route

1. Put the two terminal predicates and the view-transfer map into one physical
   curve metric and prove finite common-intersection `J_cap`; properise that
   same-ID subkernel without losing the once-charge law.
2. Prove intermediate `C24` avoidance after properisation and sum every later
   and repeated recovery clock on that carrier; only then materialise physical
   `q`, numerical `C_fw,C_rev,q`, and the strong singular/current cemetery.
3. For Gate 5, prove either a same-source hit/miss separation rate with
   `w_Z delta_sync<1`, or a physical weak-clearance tail with same-operator,
   owner-density, pullback-gap, boundary-complexity and unbounded-suffix joins.
4. Install the physical finite-`s` `Q_s/R_s` current carrier, directional
   Piola/suffix bounds in the target strong space, and finish `MT_DQ`.
5. In parallel, instantiate the Gate-1 same-representative cross-term row and
   construct the Gate-2 stable-saturated quotient base and holonomy Jacobian.

Leaf reports:

- `cm2-gate34-round56-terminal-j-pair-kac-mesh-closure-assault-2026-07-20.md`;
- `cm2-gate34-round56-preproperisation-full-clock-downstream-assault-2026-07-20.md`;
- `cm2-gate5-round56-optimal-collar-recovery-clock-frontier-assault-2026-07-20.md`;
- `cm2-round56-independent-core-poles-and-r55-ledger-audit-2026-07-20.md`.
