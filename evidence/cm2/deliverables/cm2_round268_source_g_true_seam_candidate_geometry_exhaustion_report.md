# CM2 Round268: true source-seam candidate geometry exhaustion

## Certified result

Round268 exhausts the Round182 cyclic adjacent-chart candidate pool against
the exact Round171 Source-G seam contract.

- eight chart/root buckets contain exactly `59` rows each;
- the four true seams are E↔N, N↔W, W↔S, and S↔E;
- all `4 * 59 * 59 = 13,924` adjacent-chart pairs are tested exactly;
- each seam has `38` strict positive-area owner/shadow overlaps;
- total positive same-point seam patches: `152`;
- exact empty p- or s-overlap pairs: `13,772`;
- exact common area sum: `1/80`.

Every positive row pins the Round171 identity of source position, normal, and
velocity and records exact rational common p/s intervals.  Jx/Jy are excluded
from this transition pool because they are nonidentity physical symmetries.

Certificate result SHA256:
`ce8718d74189e678b7ebc7ac9beeec44305d01091cafcfde012f6bb3a882cf24`.
Complete certificate SHA256:
`10d5e42f4353e981e7e8d5aacc002bed119453ee14a13398c524d5cb4ac2f7b9`.

## Verification

The independent verifier pins the producer as inert bytes, reconstructs the
complete pair pool before opening the candidate, and requires complete object
equality.  It returns `PASS_INDEPENDENT_ROUND268`; all `10/10` semantic
attacks are rejected.  Seeds `268071` and `268929` produce identical bytes.

## Strict non-promotion

The geometry is now exhausted, but Round182 did not materialize
side-specific exact-key return signatures on the two incident closed collars.
Therefore Round268 deliberately awards zero component edge, quotient,
maximality, fibre, global-disposition, or Jx/Jy credit.  The quotient remains
`63,224`; frontiers remain `126,468 / 116 / 133,284`; Gate5 remains `10/18`,
D02 remains blocked, and CM2 remains `NO-GO_FOR_CLAIM`.

Next: materialize signed return data on all `184,452` closed collar leaves,
attach the frozen `152` patches to incident components, and reverse-rechart
the `880` positive-volume chart guards.
