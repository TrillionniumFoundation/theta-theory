# CM2 Round272: boundary dual-factor WALL closure

## Certified result

Round272 closes all `496` apparent dual-factor WALL leaves retained by
Round271 and materializes all `720` remaining connected side signatures.

On every leaf, the source endpoint factor is exactly the chart-dependent
`±(9/25)t`; its only zero is confined to one excluded `t=0` face.  Hence the
source factor is strict on the relative-open 3D interior and the target
factor is the sole active graph.  The closed-leaf signature residual is now
exactly zero.

Certificate result SHA256:
`96eb126f34d819c97b9d50857d3207efad013a3c4e65d085f5b55270e8522354`.
Complete certificate SHA256:
`16050c7087deb546d39b2c7922274ccae7cec24a799ecafd9a8304ae1186d8f2`.

## Verification

The independent verifier pins the producer as inert bytes and recomputes all
`496` parent decisions and `720` signature rows before opening the candidate.
It returns `PASS_INDEPENDENT_ROUND272`; all `5/5` semantic attacks are
rejected.  Seeds `272071` and `272929` produce identical bytes.

## Strict non-promotion

Round272 awards zero expanded-occurrence, component-union, maximality, fibre,
global-disposition, or Jx/Jy glue credit.  The quotient remains `63,224`; the
expanded occurrence and exact-key frontiers remain `126,468 / 116`;
maximality remains `0/63,224`, fibres `0/116`, and dispositions
`0/224,580`.  Gate5 remains `10/18`, D02 remains blocked, and CM2 remains
`NO-GO_FOR_CLAIM`.

Next: bind every frozen side signature from Rounds208/269/270/271/272 and all
`152` true-seam patches to the expanded component frontier; only then begin
component maximality and complete `116`-fibre exhaustion.
