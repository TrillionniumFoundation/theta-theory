# CM2 Round 59 Gate 1/2/3: physical formula, shadow and Dini frontier

Date: 2026-07-20  
Scope: append-only structural assault from the frozen Round-58 Gate-1/2/3
package  
Strict verdict: **the actual circular-pilot branch relation is now proved to
fit the declared Round-58 CAD budget at every fixed depth.  This promotes the
fixed-depth CAD recurrence from a conditional encoding statement to an
actual physical-pilot formula bound.  Gate 1 also gains a correctly typed
physical selected-QNL-germ Dini ledger, and Gate 2 gains 96 finite physical
collision-shadow rows plus an exact seven-field landing-join audit.  None of
these supplies the missing all-plaque combined gauge, an all-depth stable
quotient, depth-integrated complexity, strong restriction/Piola maps or
`MT_DQ`.  Gates 1/2/3 remain `NOT_CERTIFIED`, the composite score remains
`0/5`, and CM2 remains `NO-GO_FOR_CLAIM`.**

## 1. Frozen baseline and discipline

This package hash-pins the Round-58 structural leaf and the exact older
artifacts used for each physical instantiation:

- the 5000-bit selected QNL holonomy/twisting enclosure;
- the positive-width actual 96-collision strip;
- the strictly interior cone-product candidate tile;
- the exact raw physical first-return landing registry;
- the complete 162-lift first-hit universe and the depth-two physical atlas.

It preserves these official statuses:

```text
Gate 1 physical all-plaque weighted Dini rows:        NOT_CERTIFIED
Gate 1 same representative class H plus twisting:     NOT_CERTIFIED

Gate 2 affine candidate layers:                        7/7 NON-OFFICIAL
Gate 2 immutable physical fields:                      0/17
Gate 2 stable-saturated base/projection/J_hol:         NOT_CERTIFIED

Gate 3 fixed-depth physical formula budget:            attacked below
Gate 3 physical strong R_s/Q_s, Piola, MT_DQ:          NOT_CERTIFIED
```

No weak Borel/TV or fixed-depth conclusion is promoted to a strong physical
operator conclusion.

## 2. Gate 1: what the old QNL numerics really instantiate

### 2.1 A physical selected-germ Dini ledger exists

The frozen selected homoclinic computation is stronger than a bare
finite-matrix enclosure.  On the canonical QNL eigenfibre `E_p`, in the
compact logarithmic gauge, it proves

```text
critical tail ratio rho < 0.09281 < 1/10,
noncritical contraction q=0.091, hence q^2 < 1/100,
```

and the critical and diagonal per-level terms have geometric-log form.  Up
to the already certified finite positive prefactors, they are bounded by

```text
(m+1)(1/10)^m,       (m+1)(1/100)^m.
```

The certificate checks the exact sums

```text
sum_(m>=0) (m+1)(1/10)^m  = 100/81,
sum_(m>=0) (m+1)(1/100)^m = 10000/9801.
```

Thus the selected physical QNL germ and its two-sided loop have an honest
summable tail registry.  The depth-260 infinite-loop entry error satisfies

```text
||psi_infinity-psi_260||_max < 8.568e-49 < 1e-29.
```

The smallest frozen wedge is strictly larger than `1.7344e-28`, so this
truncation error preserves all four selected twisting wedges in the same
fibre and the same normalized basis.

### 2.2 Why this is not the Round-58 missing Gate-1 row

Three types must remain separate:

1. the old physical compact-log gauge on one selected QNL germ;
2. the two weighted-projective pair sequences on every stable and unstable
   plaque;
3. the still-unsolved combined half-density gauge in which class `H` and
   twisting must coexist.

The numerical tail certifies item 1.  It neither provides a common
`eta_n(x,y)` registry for every plaque pair nor compares the compact-log loop
with a newly constructed combined-gauge loop.  Consequently the statement

```text
same-fibre combined-gauge loop error <= 1e-29
```

is still `NOT_CERTIFIED`.  A selected orbit may have a summable tail while an
unrelated plaque family has the triangular oscillatory increments from
Round 58.  Therefore no local-to-global promotion is legal.

The exact Gate-1 boundary is now:

```text
selected physical QNL germ/loop Dini tail:             CERTIFIED
selected compact-gauge truncation error <1e-29:        CERTIFIED
physical all-plaque weighted-projective Dini rows:     NOT_CERTIFIED
combined half-density gauge loop comparison:           NOT_CERTIFIED
same representative class H plus twisting:             NOT_CERTIFIED
Gate 1:                                                 NOT_CERTIFIED
```

## 3. Gate 2: finite physical shadows and the landing join

### 3.1 Ninety-six real collision-shadow rows

The 2400-bit actual-plaque continuation package contains a positive-width
physical source strip on which every point follows the same strict
96-collision first-hit word.  Hence, on its affine transverse source base,
the collision singularity shadow is empty at every recorded depth:

```text
B_j^collision = empty,      Leb(B_j^collision)=0,
1 <= j <= 96.
```

These are actual physical finite rows, not an abstract model and not merely
area-nullity.  They are still weaker than the Round-58 all-depth bad shadow

```text
B_j={base points hitting a depth-j singularity
     or losing the required graph-transform chart}.
```

The old strip proves regular collision continuation.  It does not provide a
continuum of invariant stable plaques, a common graph-transform chart at
every future depth, or an all-depth compactness registry.  Thus these 96
zeros may not be extended to `j>=97` and may not be renamed stable-holonomy
rows.

### 3.2 Exact audit of the seven-field Gate-2 to Gate-4 join

Round 58 isolated seven fields needed for a same-point physical Rokhlin
redistribution of the unshifted landing law.  Reading them against the
actual Round-57/58 landing artifacts gives:

| field | physical requirement | exact status |
|---:|---|---|
| 1 | countable stable-product rectangles on the landing support | `NOT_CERTIFIED` |
| 2 | stable projection and two-sided Borel holonomy Jacobian | `NOT_CERTIFIED` |
| 3 | full-span restriction or uniform fragmentation bound | `NOT_CERTIFIED` |
| 4 | same-measure unstable Rokhlin conditionals with density/log distortion | existing curve-carrier densities only; not this field |
| 5 | average boundary charge strictly below `C_p` | exactly the open `J_land,min<C_p h` a.e. inequality |
| 6 | Borel branch inverse retaining `n`, path, same ID and half-open ownership | **`CERTIFIED` on the raw exact first-return graph** |
| 7 | strong restriction and assembly bounds | `NOT_CERTIFIED`; weak Borel graph only |

Thus exactly one of seven join fields is fully present.  Fields 4 and 5 have
useful typed partial information but are not completed.  The join remains
open.

### 3.3 Latest billiard product technology does not change the measure type

`arXiv:2604.25881`, *Every finite horizon Sinai billiard map has a unique
measure of maximal entropy*, constructs the MME from one-sided symbolic
Hausdorff measures.  Its local product structure is for that MME.  CM2 needs
the collision Liouville/SRB law and its physical conditionals.  The paper
does not supply the required physical Radon--Nikodym registry,
fragmentation/boundary-`Z` estimate or strong assembly map.

Its total-length growth statement for connected images also does not bound
the number of connected components or the sum of inverse component lengths.
Those are exactly the quantities relevant to physical boundary charge.

Strict status:

```text
finite physical collision-shadow rows:                96/96 CERTIFIED
physical all-depth projected bad-shadow rows:          0
stable-saturated base/projection/J_hol:                NOT_CERTIFIED
Gate-2 -> Gate-4 physical seven-field join:            1/7
official immutable Gate-2 fields:                      0/17
Gate 2:                                                 NOT_CERTIFIED
```

## 4. Gate 3: the actual circular-pilot formula fits the CAD budget

This is the decision-changing part of Round 59.

### 4.1 A degree-four physical branch encoding

Fix a depth `n`, one of the eight source chart cells, and a word in the
complete conservative alphabet

```text
2 obstacle types * 9 * 9 torus lifts = 162 targets per collision.
```

The horizon and source-cell table proves this alphabet complete on
`|s|<=1/400`.  Empty words are retained.

Use physical state variables

```text
q_j=(x_j,y_j),       u_j=(u_j^x,u_j^y),       |u_j|^2=1,
```

and, at collision `j`, auxiliary variables

```text
tau_j, q_(j+1), n_(j+1), c_j, u_(j+1).
```

The flight and chosen contact are

```text
q_(j+1)=q_j+tau_j u_j,
|q_(j+1)-C_i(s)|^2=R_i^2,       0<tau_j<3.
```

All centres are affine in `s` and both radii are rational.  Put

```text
R_i n_(j+1)=q_(j+1)-C_i(s),
c_j=u_j dot n_(j+1),
u_(j+1)=u_j-2 c_j n_(j+1).
```

Introducing `c_j` keeps reflection at degree two instead of expanding a
cubic product.

### 4.2 Exact polynomial no-earlier-root predicate

For a distinct competing disk set

```text
a = u dot(C-q),
b = |C-q|^2-R^2,
Delta = a^2-b.
```

On the physical domain `|u|=1`.  Distinct periodic scatterers are disjoint,
so `b>0`; the current source disk is treated separately by the outgoing
convexity sign `a<=0`.  The ray equation is

```text
r^2-2 a r+b=0.
```

There is no root in the open interval `(0,tau)` exactly when

```text
a<=0
OR Delta<0
OR [a-tau>=0 AND Delta<=(a-tau)^2].                 (4.1)
```

The last equivalence is squared only after the sign `a-tau>=0` is present;
there is no illicit radical squaring.  Since `a` has degree two, (4.1) has
degree at most four.  The chosen word, outgoing signs, disjoint-disk rows,
half-open chart ownership and grazing cemetery handle the endpoint/tie
strata explicitly.  Distinct disks cannot share one contact point, so no
simultaneous interior target is silently assigned twice.

The certificate replays all Boolean cases of (4.1) with exact rationals.

### 4.3 Count audit

Initial variables are `(s,q_0,u_0)`, five scalars.  Every step adds

```text
1 tau + 2 q_next + 2 normal + 1 c + 2 u_next = 8
```

scalars.  A deliberately padded count allocates five rows to every one of
the 161 nonchosen target slots.  On a nonempty physical branch one of those
slots is the current source and uses only the simpler outgoing-convexity
row, so this allocation is a genuine upper bound.  It also includes all
outside-domain checks, the current-source convexity row,
chosen contact, flight, normal, reflection, owner signs and cemetery rows:

```text
161 nonchosen slots * five-row upper budget           = 805 rows,
flight/contact/normal/reflection/source/owner/cemetery <= 25 rows,
total per step                                      <= 830 rows,

physical variables             <= 8n+5,
physical atomic predicates     <= 830n+12,
physical polynomial degree     <= 4.
```

For every `n>=1`, these fit strictly inside the Round-58 envelope

```text
k_n = 400n+20,
S_n = 2000n+100,
D_n = 8.
```

The raw fixed-depth word universe remains

```text
8*162^n.
```

Therefore the physical circular-pilot formula really instantiates the
Round-58 CAD recurrence.  The resulting certified component majorant is

```text
8*162^n * product_(r=0)^(400n+19) (2 S_r D_r+1),

S_0=2000n+100,        D_0=8,
S_(r+1)=2(S_r D_r+1)^2,
D_(r+1)=2D_r^2.
```

It is enormous, but it is explicit and is now tied to the actual physical
formula rather than a separately assumed encoding budget.

### 4.4 What the physical budget still does not buy

The CAD bound is fixed-depth.  Its ambient dimension grows linearly in `n`
and the recurrence is doubly exponential in that dimension.  The installed
physical moment

```text
integral exp(Dbar/6) < infinity
```

does not integrate this majorant.  Nor does a component count control
boundary inverse lengths, restriction norms or directional Piola
amplification.  Hence the following remain open:

```text
actual CAD cell enumeration:                           NOT_CERTIFIED
depth-integrated physical atlas complexity:            NOT_CERTIFIED
strong R_s/Q_s on one anisotropic space:               NOT_CERTIFIED
directional Piola:                                     NOT_CERTIFIED
MT_DQ and moving-test convergence:                     NOT_CERTIFIED
Gate 3:                                                NOT_CERTIFIED
```

## 5. New CAD/o-minimal technology audit

The official arXiv API was checked on 2026-07-20.

- `arXiv:2603.25380`, *Complex cells in sharply o-minimal structures*, gives
  polynomial effectiveness relative to the degree filtration of a sharply
  o-minimal structure.  It does not provide the pilot's physical branch
  filtration, boundary-`Z` or a strong restriction norm.
- `arXiv:2604.06144`, *Analytically generated sharply o-minimal structures*,
  develops structures whose complexity filtration is generated by complex
  cells.  It does not instantiate the moving billiard branch relation or
  the CM2 operators.
- `arXiv:2601.09548`, *Further results on Minimal and Minimum Cylindrical
  Algebraic Decompositions*, studies coarsening/minimality of CADs.  Minimal
  does not mean uniformly bounded physical component count or boundary
  charge.
- `arXiv:2605.04718`, *On Minimum CADs for Algebraic Sets in Dimension
  Three*, concerns a fixed low dimension; the physical depth-`n` formula has
  growing dimension and quantified branch data.

These papers sharpen possible future algorithms, but none supplies the
depth-integrated or strong-space interface.  No literature claim is used to
promote a CM2 field.

## 6. Strict final ledger

```text
Gate 1 selected physical QNL Dini/twisting tail:       CERTIFIED_LOCAL
Gate 1 all-plaque combined class H plus twisting:      NOT_CERTIFIED

Gate 2 physical finite collision shadows:              96/96
Gate 2 all-depth stable quotient/J_hol:                 NOT_CERTIFIED
Gate 2 immutable fields:                               0/17

Gate 3 actual physical fixed-depth formula budget:     CERTIFIED
Gate 3 fixed-depth explicit CAD majorant:               CERTIFIED
Gate 3 depth-integrated/strong RQ/Piola/MT_DQ:          NOT_CERTIFIED

Gate 1:                                                NOT_CERTIFIED
Gate 2:                                                NOT_CERTIFIED
Gate 3:                                                NOT_CERTIFIED
composite gates:                                       0/5
CM2:                                                   NO-GO_FOR_CLAIM
```

The shortest next interfaces are now:

1. Gate 1: an all-plaque combined-gauge weighted increment registry and a
   same-gauge selected-loop comparison;
2. Gate 2: physical all-depth stable graph transforms and the first five
   missing landing-join fields, especially the exact
   `J_land,min<C_p h` inequality;
3. Gate 3: a complexity estimate integrable under the installed clock law,
   or direct strong restriction/Piola estimates that bypass global CAD
   counting.

## 7. Reproduction

```bash
python -m py_compile \
  deliverables/cm2_gate123_round59_physical_formula_shadow_dini_frontier_cert.py \
  deliverables/cm2_gate123_round59_physical_formula_shadow_dini_frontier_verifier.py

python deliverables/cm2_gate123_round59_physical_formula_shadow_dini_frontier_cert.py --audit
python deliverables/cm2_gate123_round59_physical_formula_shadow_dini_frontier_verifier.py --integrity-only
python deliverables/cm2_gate123_round59_physical_formula_shadow_dini_frontier_verifier.py --replay
python deliverables/cm2_gate123_round59_physical_formula_shadow_dini_frontier_verifier.py --self-test
python deliverables/cm2_gate123_round59_physical_formula_shadow_dini_frontier_verifier.py --reemit

# Expected exit 2: Gates 1/2/3 and CM2 remain fail-closed.
python deliverables/cm2_gate123_round59_physical_formula_shadow_dini_frontier_cert.py
python deliverables/cm2_gate123_round59_physical_formula_shadow_dini_frontier_verifier.py

sha256sum -c \
  deliverables/cm2-gate123-round59-physical-formula-shadow-dini-frontier-manifest-2026-07-20.sha256
```
