# CM2 Eighty-Fourth Direct Assault — 2026-07-22

## Scope

Round 84 closes the 614 positive-area common-tangent boxes left by Round 83,
integrates the resulting different-candidate intersection atlas relative to
the pinned patch registry, repairs the prospective `42 -> 40` centered-frontier
accounting, and tests both available routes for attaching stable/material data
to the C24 landing root.  Every conclusion remains fail-closed with respect to
complete physical faces, all-depth stable products, operator rows, and the CM2
claim.

## Reverse common-tangent closure

For an oriented common-tangent target `(n,h)`, put `v=(n_y,-n_x)`.  Reverse
the target line through the second obstacle, the first obstacle, and the source
circle.  At each stage the certificate checks the radicand, strict flight
order, incidence identity, reflection, source chart, and recovered `(t,p)`.
This finite inverse calculation replaces further blind dyadic subdivision.

The 512-bit exhaustive pass evaluates all `42*8=336` oriented targets and
obtains the exact branch partition

```text
MISS_SECOND                 176
MISS_FIRST                  114
MISS_SOURCE                   4
INVERSE_STRICTLY_SEPARATED   42
UNRESOLVED                    0
```

There is exactly one valid inverse target for each of the 42 component pairs.
All 42 inverse enclosures are outside the relevant source core with a uniform
coordinate gap greater than `83/4000`, and all 614 inverse/box comparisons are
strictly separated with a uniform coordinate gap greater than `87/4000`.
All 42 forward replays overlap their target enclosures.  Unit-normal and both
candidate signed-distance identities are also checked interval-exactly.

Consequently the full different-candidate box partition is now

```text
165608 = 127732 + 21154 + 16108 + 614,
```

with zero remaining overlap boxes and zero registered cross-candidate
intersections relative to the pinned patch registry.

## Prospective Round-83 accounting repair

Round 83 correctly froze its conservative input count of 42 hard component
pairs, but two centered rows already had `unresolved_leaf_count=0`.  Round 84
therefore records the distinct quantities explicitly:

```text
input hard pairs                         42
centered-closed pairs                     2
post-centered live pairs                 40
input hard boxes                        614
centered-closed input boxes              54
post-centered live input boxes          560
post-centered unresolved leaves       11840
post-centered live input area   173/20480000
```

No Round-83 frozen artifact is rewritten.

## Registered atlas terminology

The Round-82 value `1,672` is retained only as the number of frozen
box-connectivity aggregates.  It is not called a zero-set component count or
a physical-face count in the Round-84 manifest.  A follow-on Round-85 exact
union-boundary certificate found 4,216 exposed zero-set ports, comprising 1,004 source ports
and 3,212 non-source ports.  It refines the registered scope to 2,108
elementary arcs: 1,568 frozen rows contain one arc, while 104 overaggregated
rows contain 540 arcs and contribute the correction `540-104=436`.

This diagnostic does not close the 3,212 off-registry continuations.  Complete
rank-three physical faces, their numeric/RN rows, and the final face count
therefore remain unmaterialized.

## Gate-4 support separator

The frozen depth-96 stable/material anchor was replayed as 97 phase-state
Taylor tubes.  Each tube was compared with all 24 C24 cores under all eight D4
actions:

```text
state/action/core comparisons          18624
different-component separations         9312
strict coordinate separations           9312
unresolved                                  0
```

The maximum phase-tube radius is less than `1/10^13`.  The minimum center
coordinate gap is greater than `19/10000`, and the minimum full-tube gap is
greater than `1/1000`; the closest registered witness occurs at phase state
46.  A 2700-bit independent recomputation verifies every comparison.

Thus the existing 96-word stable anchor and its depth-96 material anchor
cannot be attached to the C24 root by choosing one of their 97 registered
phase states or a D4 image.  This does not rule out unrelated time shifts or
stable plaques constructed directly on C24, and it promotes no Gate-4 field.

## Gate-4 same-root escape

A separate 512-bit certificate attacks the constructive Round-69 base root
itself.  Every one of its 176 lower-closed `s=0` owner atoms lands strictly
inside its declared destination C24 core.  The landing image is nevertheless
strictly disjoint, in the `p` coordinate, from every Round-69 owner atom in
that destination core:

```text
same-destination comparisons                    1360
landing_p_below_target_p0                        680
landing_p_above_target_p1                        680
uniform coordinate gap                         >1/100
depth-2 same-key self-intersection count             0
```

The invariant counts and gap are independently reproduced at 768 bits.  The
current Round-69 owner union therefore cannot itself carry the requested
all-depth same-key stable/material crosswalk.  The lawful next construction
must reselect or enlarge the physical base root.  This result does not claim
that every C24 point escapes or that no other invariant C24 subroot exists.

## Verifier hardening

The Round-83 frozen producers and SHA ledgers replay correctly in the pinned
`python-flint==0.9.0` environment.  A systematic audit nevertheless found
that its configured 12 semantic mutations did not cover the full result
schema: after coordinated digest regeneration, 45 of 60 scalar result-field
mutations were accepted.  This is a verifier-coverage defect, not evidence
that the frozen producer bytes were altered.

Round 84 therefore requires fresh exact producer equality and closed schemas.
Its intersection verifier rejects 72/72 result-scalar mutations, 3/3
structural mutations, 4/4 strict-JSON attacks, and 4/4 coordinated reverse
evidence attacks, while independently recomputing the reverse certificate at
384 and 768 bits.  The C24 support verifier rejects all configured result,
pin, nested-row, structural, and strict-JSON attacks and independently checks
all 18,624 separations at 2700 bits.  The same-root escape verifier reproduces
the decisive invariants at 768 bits and rejects 4/4 configured hostile
mutations.

All replays use `.venv-neurips/bin/python`; the system Python lacks `flint`
and is not a valid certification environment.

## Strict state

```text
Gate 1                         NOT_CERTIFIED
Gate 2                         0/17
Gate 3                         NOT_CERTIFIED
Gate 4                         1/7
Gate 5                         10/18, complete blocks 0
complete composite gates       0/5
CM2                             NO-GO_FOR_CLAIM
```

Closing the different-candidate atlas is real progress on the rank-three
geometry, but it does not supply same-candidate continuation, complete faces,
rank-transition contraction, or an all-depth stable/material root.

## Next shortest route

1. Rebuild the 2,108 registered elementary arcs with their 4,216 ports and
   continue every one of the 3,212 non-source ports through chart, owner,
   translation, radicand, and flight-order seams using reverse billiards.
2. Compute the physical face quotient from the continued arcs and typed
   physical endpoints; do not assume its count is 1,672 or 2,108.
3. Search the full C24 return atlas for a nonempty recurrent same-key subroot,
   or enlarge the current base registry until one exists, before exporting
   `Q,E,u,v`, stable plaques, or material rows.
4. On that recurrent root, construct the all-depth stable/holonomy family,
   uniform material/Piola and stopped-derivative ledgers, and the missing
   Gate-5 RN/potential sectors.  Finite geometric closure alone cannot promote
   any of these operator gates.

No external theorem is promoted in this round.
