# CM2 Round 62 full direct assault

Date label: 2026-07-21  
Strict verdict: **0/5 complete composite gates; Gate 5 maturity 10/18 with
zero complete 18-field blocks; CM2 NO-GO_FOR_CLAIM**

## Round result

Round 62 closes no composite gate and does not promote any missing physical
strong interface. It does replace four diffuse bottlenecks by exact typed
objects or sharp nonimplication frontiers:

1. Gate 1 gains an exact forward/backward plaque-tempered cohomology
   criterion for transporting class-`H` holonomies and the selected twisting
   loop through one determinant-one same-token gauge.
2. Gate 2/4 gains actual tagged first-return RN covariance, an exact
   stable-holonomy marker-defect cocycle, and a canonical endpoint-traced
   normal metric-current recipient on a tagged graph cylinder.
3. Gate 3 gains the exact moving-branch Reynolds bulk-plus-face formula,
   the directional regular-branch Piola formula, and the minimal conditional
   stopped-current recipient.
4. Gate 5 gains a fixed-time source-grazing null subtrace, a canonical
   minimal time-labelled all-insertion registry, and exact outer
   Abel/Orlicz/Jordan ledgers with sharp harmonic no-decay separators.

Three root red-team corrections were required before freeze.

- The Gate-1 plaque-tempered criterion now requires a uniform Hölder modulus
  for the defect/holonomy approximants; uniform defect convergence by itself
  does not preserve the class-`H` Hölder exponent or constant.
- Gate 5 now separates record spaces `X_j/X_all`, scalar complement charges
  `c_j`, generic Jordan integrands `phi_j`, and separator masses `b_j`.
  Its registry is a canonical minimal implementation, not a unique encoding:
  any injectively equivalent Borel code is legal, while every legal owner
  quotient must retain `j` or an equivalent immutable insertion coordinate.
- For Gate 4/2, the boundary of the weighted cylinder current is explicitly
  the weighted trace `2^D nu`, not the original `nu`; the physical unweighted
  trace is recovered only by multiplying the boundary 0-current by `2^-D`.

All affected report/certificate/verifier/manifest/SHA artifacts were
regenerated before independent audit. No older artifact was edited.

## Gate 1: exact plaque-tempered cohomology interface

Use the frozen convention

```text
B(x)=C(fx)^(-1)A(x)C(x).
```

For a local stable pair, define the renormalized endpoint defect

```text
Delta_n^s(x,y)
=A^n(y)^(-1)[C(f^n y)C(f^n x)^(-1)-I]A^n(x).
```

If the canonical `A`-stable holonomy exists, then exactly

```text
H_B^s(x,y)=C(y)^(-1)H_A^s(x,y)C(x)
iff Delta_n^s(x,y)->0.
```

The backward formula uses `f^(-n)`, `A^(-n)` and `Delta_n^u`. A valid
physical third gauge must be defined and Hölder on every actual plaque,
make both defect families converge uniformly, give their approximants a
uniform Hölder modulus, and retain the immutable periodic/homoclinic loop
token. Equi-Hölder control plus uniform convergence then passes the modulus
to the limit.

At the selected base, simultaneous transport of the loop and eigenvectors
multiplies every twisting wedge by `det(C(p)^(-1))`; an `SL(2)` transfer
preserves them numerically. This is the exact prerequisite behind the
Round-61 same-token covariance.

Determinant one and same-token equality are not enough. For

```text
A=diag(2,1/2),
C(f^n x)=I,
C(f^n y)=I+2^(-n)E_21,
```

the transfer is Lipschitz, determinant one, in the positive big cell and
fixes the periodic token, but

```text
Delta_n^s=2^n E_21.
```

Thus the missing physical row is the all-plaque two-sided renormalized
endpoint-defect registry with uniform Hölder control. The faithful diagonal
gauge still has class `H` but zero selected twisting; the compact QNL gauge
has nonzero selected wedges but refuted connector class `H`. Gate 1 remains
`NOT_CERTIFIED`.

## Gate 2/4: actual branch RN covariance

On one immutable tagged invertible branch of the exact physical first-return
graph,

```text
H=T_s^n|U:U->V,
mu_V=H_#mu_U,
kappa_V=H_#kappa_U,
a=d kappa_U/dmu_U,
g_B=d kappa_V/dmu_V.
```

Uniqueness of Radon--Nikodym derivatives gives the actual same-law identities

```text
g_B=a o H^(-1)  mu_V-a.e.,
a=g_B o H       mu_U-a.e.
```

before branch tags are forgotten. The one-dimensional branch Jacobian
appears in both marked and unmarked coordinate densities and cancels in the
ratio; inserting an additional Jacobian into this formula is a type error.
The identity preserves `n/path/physical-ID/half-open owner/once charge`.
It is dynamic branch covariance, not covariance across a physical stable
holonomy.

For a future nonsingular stable holonomy put

```text
P_uv f=(f o h_uv^(-1))J_uv,
Delta_uv=g_v-P_uv g_u.
```

Then `P_uv` is a positive `L^1` isometry and, along a holonomy composition,

```text
Delta_uw=Delta_vw+P_vw Delta_uv,
||Delta_uw||_1<=||Delta_vw||_1+||Delta_uv||_1.
```

This exact cocycle compresses the future marker join to one zero-defect
registry. It neither constructs the invariant product rectangles nor proves
any physical defect zero. Gate 2's official immutable score stays `0/17`.

## Gate 4: tagged graph-cylinder normal current

Let `Z` be the standard-Borel tagged bad-graph record space. After choosing a
bounded compatible bookkeeping metric making the endpoint/tag maps
Lipschitz, define on `[0,1]xZ`

```text
T_nu(f,pi)
=integral_Z integral_0^1 f(t,z) partial_t pi(t,z)dt dnu(z).
```

The exact current identities are

```text
M(T_nu)=|nu|(Z),
partial T_nu=(i_1)_#nu-(i_0)_#nu,
M(partial T_nu)=2|nu|(Z).
```

Its two traces push to the exact physical landing and source measures. For
`w=2^D_land`, the Round-61 moment pays both ordinary and weighted currents:

```text
M(T_nu)+M(partial T_nu)=3|nu|(Z)<=3||nu||_X,
M(T_(w nu))=||nu||_X,
M(partial T_(w nu))=2||nu||_X.
```

The weighted boundary carries `w nu`; the original trace is recovered by

```text
w^(-1) partial T_(w nu)=partial T_nu.
```

For the positive bad graph, the original-time decomposition becomes

```text
Gamma_cap=Gamma_G+trace_1(partial T_Gamma_B),
```

with endpoints, first-hit semantics, immutable tags and once charge intact.
This completes `4/5` rows of the newly typed graph-cylinder current ledger,
not a Gate score.

The cylinder metric is bookkeeping geometry. The current is not in collision
phase space, no billiard transfer operator acts on it, and no anisotropic
Piola/current recipient accepts it. On a discrete two-point physical endpoint
space every metric 1-current is zero, while the labelled cylinder still has
boundary `delta_b-delta_a`; hence a cylinder current cannot be renamed a
physical phase-space current.

The quantitative properness inequality

```text
F(u)R(u)<C_p theta(u)L(u)
```

remains unpaid. Physical landing fields 1, 4 and 7 have partial interfaces,
field 6 remains the sole complete row, and the count stays `1/7`. Proper
same-ID landing, physical `q in L^(6/5)`, later/repeated clocks and strong
cemetery remain uncertified. Gate 4 remains `NOT_CERTIFIED`.

## Gate 3: moving bulk-plus-face current and directional Piola

For one moving branch

```text
Lambda_i(s,phi)
=integral_(a_i(s))^(b_i(s)) f_i(s,x)phi(Y_i(s,x))dx,
```

the exact derivative is

```text
Lambda_i'(0,phi)
=integral_I[partial_s f_i phi(Y_i)
            +f_i Dphi(Y_i) dot partial_s Y_i]dx
 +b_i' f_i(b_i-)phi(Y_i(b_i-))
 -a_i' f_i(a_i+)phi(Y_i(a_i+)).
```

At a shared moving cut `e=b_i=a_(i+1)`, the endpoint current is

```text
B_e=e'[f_i(e-)delta_(Y_i(e-))
       -f_(i+1)(e+)delta_(Y_(i+1)(e+))].
```

Half-open ownership alone does not cancel it. When the landing points
`Y_i(e-)` and `Y_(i+1)(e+)` are distinct,

```text
||B_e||_TV=|e'|(|f_i(e-)|+|f_(i+1)(e+)|).
```

If the landing point is the same but the two trace weights differ, the TV is
instead `|e'||f_i(e-)-f_(i+1)(e+)|`.

A countable stopped assembly therefore needs one parameter-neighbourhood
dominator, an absolutely summable bulk series, and

```text
E_stop=sum_e||B_e||_TV<infinity.
```

For one regular moving diffeomorphism `Phi_s`, direct differentiation of the
contravariant Piola pairing gives

```text
d/ds|_0 <Piola_(Phi_s)K,psi>
=integral[Dpsi V dot dK+psi dot DV dK],
```

with `(C^1)^*` bound

```text
(||V||_infinity+||DV||_infinity)||K||_TV.
```

This is an exact local branch formula, not the physical strong billiard
operator. The determinant-one family

```text
U_L=[0,1/L]x[0,1],
S_L=diag(L,L^(-1)),
K=e_1
```

has source `L^1` norm `1/L` and target Piola `L^1` norm `1`, so the bulk
multiplier is unbounded. Independently, `m` internal faces with distinct
landing labels have face-current TV `2m` while the stopped weak-TV norm stays
one. Thus weak stopped TV controls neither endpoint fragmentation nor the
bulk strong Piola norm.

The current-completed conditional recipient is precisely the regular bulk
directional-Piola series plus the signed physical face-current series. A
common finite-`s` moving atlas, actual trace maps, all-depth endpoint sum,
uniform anisotropic bulk bound, duplicate/artificial-trace assembly and
same-carrier cemetery/tightness remain missing. Physical strong `R_s/Q_s`,
directional Piola/current and `MT_DQ` stay `NOT_CERTIFIED`; Gate 3 remains
`NOT_CERTIFIED`.

## Gate 5: fixed-time source-grazing null subtrace

On the actual raw endpoint coarea law,

```text
m_occ{B>b}<=(9158592/6875)4^(-b).
```

The source-marked fixed-time owner law projects to a measure dominated by
`m_occ`. Since `{eta=0}` lies in `{B>b}` for every `b`, each fixed-`j`
regular owner law satisfies

```text
m_j^own{eta=0}=0,
nu_j{retained eta=0}=0.
```

Corners and simultaneous physical events are absent inside the already
regular owner domain, but their pre-removal cemetery charge is not paid.
Exact artificial/homogeneity/chart/endpoint/owner cuts and later/full-word
accumulations with source `eta>0` remain open. Therefore global
`A_col`-complement nullity and full coverage are still `NOT_CERTIFIED`.

## Gate 5: canonical minimal all-insertion registry

One canonical minimal implementation is

```text
X_all=disjoint_union_(j>=0)({j}xX_j).
```

Injectively equivalent Borel encodings are equally legal. The invariant
requirement is that `j`, or an equivalent immutable insertion coordinate,
remain recoverable: different insertion times are different physical/Duhamel
terms and cannot be cross-time collapsed by owner minimization.

For the actual fixed-time complement charge

```text
c_j=integral_(A_col^c)C_bad,j^bi dnu_j,
```

define the time-labelled positive weighted measure `Xi_bad^w`. Positivity and
Tonelli give

```text
Xi_bad^w(X_all)<infinity
iff sum_j w_Z^j c_j<infinity,

sum_j w_Z^j c_j
=T_0+(w_Z-1)sum_(n>=1)w_Z^(n-1)T_n,
T_n=sum_(j>=n)c_j.
```

An actual geometric row `c_j<=C kappa^j` with `w_Z kappa<1` would pay the
series. The frozen estimate is uniform in `j` and has no `kappa<1`.

The exact separator

```text
b_j=w_Z^(-j)/(j+1)
```

has finite unweighted total mass and every fixed-time charge finite, while
`sum_j w_Z^j b_j` is the harmonic series. Distinct immutable insertion
coordinates prevent legal owner minimization from merging the records.

## Gate 5: outer Abel/Orlicz and positive Jordan frontier

On every actual `nu_j|A_col`, retain

```text
Y_j=w_Z^r_K,
Phi(t)=t^(1/alpha_opt),
Z_col,j=integral 2^(K+1)dnu_j.
```

Round-61's pointwise two-sided comparison and Tonelli give

```text
sum_j w_Z^j Z_col,j<infinity
iff
sum_j w_Z^j integral Phi(Y_j)dnu_j<infinity.
```

The active-clock identity likewise lifts exactly to the time-labelled direct
sum. These are extended identities, not finite bounds. A local-perfect
separator can make `A_col` full, set `K=0`, `R=infinity`, and make all seven
suffix bits true on every `j`, while using the finite-unweighted masses
`b_j` above. Every outer clock/raw-`Z`/Orlicz series still diverges
harmonically. Thus clearance and suffix properisation do not replace
insertion-time decay.

For nonnegative Borel integrands `phi_j`, the positive measure-lattice
identity is

```text
sum_j w_Z^j integral phi_j d(mu_j^++mu_j^-)
=sum_j w_Z^j integral phi_j d|J_j|
 +2 sum_j w_Z^j integral phi_j d(mu_j^+ wedge mu_j^-).
```

Using `b_j`, mutually singular `mu_j^+,mu_j^-` gives finite unweighted mass
but divergent weighted variation. Taking `mu_j^+=mu_j^-` gives `J_j=0` but
divergent weighted common mode. The Round-61 forward/reverse RN densities
are not identified with these Jordan marginals or their charge.

Consequently the three positive payments—variation, common mode and
`A_col` complement—remain independent. The seven suffix predicates remain
Borel `7/7`, but only `2` universal values are true; the five input, `C24`,
operator, output and horizon values and physical `R>=r_K` remain open.
Gate-5 maturity stays `10/18`, with zero complete blocks.

## Latest-technology audit

The official-source surface was checked through 2026-07-21, including
`arXiv:2603.19509v3`, `2604.25881v1`, `2604.19671v2`, `2605.18110v3`,
`2606.10155v1`, `2606.19621v2` and the class-`H` reference
`1909.11548v2`.

- Current class-`H` theory defines canonical limits and Hölder regularity but
  does not make them invariant under an arbitrary non-fibre-bunched Hölder
  transfer.
- Nonautonomous response frameworks assume the common strong spaces,
  differentiability and memory loss missing for the moving singular billiard.
- Small-hole standard-family evolution starts from already regular families
  and uses survival-mass normalization; it constructs neither the physical
  landing regularity nor the moving stopped trace/Piola operator.
- Fixed-table MME length growth is not the pinned Liouville/SRB landing law
  and does not control marker fragmentation or inverse lengths.
- Current semialgebraic and inter-sign transport results supply neither the
  all-depth moving current nor positive Jordan/common-mode decay.

No external theorem is imported into a physical CM2 certification.

## Independent audit and acceptance

The append-only independent audit records `PASS_AFTER_RED_TEAM_CORRECTIONS`
with no remaining claimed-scope blocker or cross-leaf contradiction. Because
the audit agent authored the Gate-4/2 leaf, the root independently reran that
leaf's syntax, semantic/type review, hostile suite, producer reemit, SHA and
default entries. The audit agent independently checked the Gate-1/3 and
Gate-5 mathematics and executables.

The audit explicitly keeps all carriers and marks distinct:

- Gate-4's `D_land`-weighted graph cylinder is a bookkeeping current and
  does not pay Gate-3's moving physical face series or bulk Piola norm.
- Gate-3's signed face current cannot absorb Gate-4's ordinary positive bad
  collision-SRB mass; neither object is Gate-5's positive all-time cemetery.
- Gate-5 insertion time `j` indexes distinct Duhamel terms and cannot be
  removed using Gate-4's once-charge tag.
- Gate-4 dynamic branch covariance is not Gate-2 stable holonomy, and
  Gate-1 plaque-tempered gauge covariance belongs to another operator layer.
- `D_land`, stopped `Dbar`, endpoint rank `B`, collar clearance `K` and
  insertion time `j` remain different marks; no moment crosses these types
  without a same-law inequality.

Final four-leaf acceptance matrix:

```text
syntax:                         8/8
dependency / artifact pins:    39/39
integrity / audit-or-replay:   4/4 / 4/4
producer reemit:               4/4 byte-identical
hostile + strict JSON:          765/765 rejected
SHA sidecar rows:               16/16
default cert/verifier:          8/8 exit 2
recursive aggregate ledger:     15/15
Round-62 stale/temp files:      0 after cleanup
```

The hostile total is the three-source-leaf `652` plus the audit leaf's
`109` semantic and `4` strict-JSON mutations. All four leaves agree on the
strict state below.

## Strict status

```text
Gate 1 / Gate 2 / Gate 3 / Gate 4 / Gate 5: NOT_CERTIFIED
Gate 2 immutable fields:                     0/17
Gate 4 seven-field landing join:              1/7; fields 1,4,7 partial
Gate 5 maturity / complete blocks:           10/18 / 0
complete composite gates:                    0/5
CM2:                                         NO-GO_FOR_CLAIM
```

## Shortest remaining routes

1. **Gate 1:** materialize one determinant-one physical transfer on every
   actual plaque; prove uniform forward/backward renormalized endpoint-defect
   decay and a uniform Hölder modulus; then join the immutable loop token.
2. **Gate 2 / Gate 4:** construct the actual invariant product rectangles,
   two-sided stable holonomy and marker zero-defect registry; estimate
   fragmentation/span/density to prove `F R<C_p theta L`; then install a
   physical anisotropic/current strong restriction/assembly accepting the
   positive bad remainder.
3. **Gate 3:** build one common finite-`s` stopped atlas, prove the all-depth
   endpoint-current sum and an anisotropic bulk directional-Piola bound,
   then install physical `R_s/Q_s`, the finite-time telescope and `MT_DQ`.
4. **Gate 5:** estimate the remaining `N_cut/N_acc` and pre-regularization
   cemetery strata; prove a same-owner `w_Z^j`-summable all-time decay and
   the active Abel/raw-`Z_col`/power-Orlicz bound; prove the five suffix
   values and `R>=r_K`; then separately pay weighted Jordan variation,
   common mode and complement cemetery.

Until those same-law, all-time and strong-space joins are paid, CM2 remains
a strict no-go claim.
