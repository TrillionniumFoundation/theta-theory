# CM2 twenty-eighth direct assault: finite nonempty anchors, Q2 branch rules, physical face carriers, and weighted-tail transfer

Date: 2026-07-18 (Asia/Shanghai)  
Mode: append-only, fail-closed, exact rational ledgers and 384-bit Arb replay  
Parent root: `cm2-twenty-seventh-direct-assault-manifest-2026-07-18.sha256`  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; no composite
gate is fully closed (`0/5`).  This round materializes finite nonempty R1 and
Q2 adaptive anchors, proves chart-free two-step homogeneity on every finite
Q2 anchor, installs universal branch-rule F5/F6 estimates, freezes the first
limiting physical face-carrier grammars, and proves exact conditional routes
from the unweighted C24 return tail to a normalized regular-component
weighted first-return tail under same-ID physical charge hypotheses.  The
strong cemetery charge remains separate.  This round does not
enumerate maximal physical path-fibre components, instantiate actual
canonical curve recuts or physical one-dimensional face pieces, prove the
required strong recovery moment, close F14--F18, or derive an induced strong
Lasota--Yorke coefficient.**

## 1. Executive result

| Gate | New strict progress | Remaining decisive blocker | Status |
|---|---|---|---|
| 1 | round-25 exact common frame and `q>999/1000` remain frozen | the same representative still fails connector class `H` | **OPEN (`0/1`)** |
| 2 | the positive-area affine cone-product anchor remains valid | invariant stable plaques and the 17-field physical stable-product interface | **OPEN (`0/17`)** |
| 3 | 4,216 finite nonempty R1 cells and 114,006 finite nonempty Q2 cells now have immutable IDs and typed parameter-averaged coordinate-base masses | Q1, limiting R2 and maximal arbitrary-depth physical component enumeration with a uniform termination theorem | **OPEN** |
| 4 | every finite Q2 branch is two-step H0-homogeneous; exact conditional `L^p` and pointwise weighted-tail transfers are frozen | actual strong recovery carriers, numerical `C_fw/C_rev`, `q_n`, cemetery charge and an induced strong coefficient | **OPEN** |
| 5 | universal branch-rule F5/F6 slots and physical face-carrier families are frozen | actual curve/face-component instances, F7 and F14--F18 on one common physical registry | **OPEN (global `4/18`)** |

The R1 candidate-local maturity from round 27 remains `13/18`.  The new Q2
F5/F6 rows are separately typed universal branch-rule theorems and do not
promote either the R1 local count or the global Gate-5 count.

## 2. Finite nonempty adaptive anchor registry

The round-25/26 adaptive replays are now converted from counts into immutable
finite open-cell rows.  After removing their artificial dyadic faces, the
strict inner boxes themselves give connected finite adaptive cells:

```text
strict nonempty R1 adaptive cells:                    4,216
distinct frozen one-step regular keys:                   16
parameter-averaged coordinate-base mass:   6473/256000000

strict nonempty Q2 adaptive cells:                  114,006
distinct frozen depth-two regular prefixes:              36
parameter-averaged coordinate-base mass: 5257799/5120000000
```

Each row carries an exact rational coordinate-base mass

```text
R_source * Delta t * Delta p * (Delta s / Delta S)
```

and, separately, the exact parameter-averaged unnormalised collision-area
expression

```text
R_source * Delta p * (Delta s / Delta S)
         * (asin(t_1)-asin(t_0)).
```

Only the rational coordinate-base masses are summed in this leaf and proved
to reproduce the frozen finite-anchor totals.  The exact `asin` payload is
kept rowwise; it is not relabelled as a globally summed collision-SRB mass.
Likewise, a parameter-averaged mass is not relabelled as a fixed-parameter
mass.

The artificial boundary types are also separated:

- dyadic `t` and `p` faces are collision-area null on every fixed-parameter
  two-dimensional slice;
- an `s` endpoint face is null for the parameter-product measure, but at the
  exceptional slice `s=c` it is not claimed to be slice-null;
- limiting grazing, corner, owner and core-face carriers retain their
  physical collision-null typing from the earlier boundary-tube theorems.

These objects are connected components of the **finite open adaptive
registry**, not certified maximal connected components of a complete
physical path fibre.  The first unmaterialized extension depth is `3` only
when starting from the certified strict Q2-inner anchors.  This statement
does not imply that the Q1 registry or the limiting physical R2 component
enumeration has already been materialized.  The old depth-16 admitted R2
count `0` remains a finite-depth result and is not a claim that physical R2
is empty.

## 3. Two-step homogeneity and universal Q2 F5/F6 branch rules

All `114,006` strict finite Q2 atoms were replayed with 384-bit Arb.  At both
collisions every whole box lies strictly in the enlarged central
homogeneity strip

```text
H0(k0=6121),
c0 = 1/74933282.
```

No atom is admitted by a point sample.  The whole-box test proves the
required strict inequality for `1-p^2` at both steps.

For each Q2 branch and each of its two collision steps, the leaf now freezes
one deterministic canonical-recut **branch-rule** ID:

```text
canonical-recut branch-rule IDs:                    228,012
actual curve-instance IDs materialized:                   0.
```

An actual curve instance would still require the key

```text
(branch-rule ID, parent canonical-W ID, natural cell index j).
```

The current count is therefore not an enumeration of canonical unstable
curves or recut components.  What is certified is a universal theorem on
every curve instance generated by the pair of frozen branch rules on each
Q2 branch.  It yields one branch-level numerical slot of each type per Q2
atom:

```text
F5 universal branch-rule slots:                    114,006
||(D T^2|E^u)^(-1)||_*
  < (144000/180337)^2
  = 20736000000/32521433569,

F6 universal branch-rule slots:                    114,006
osc(log J^u T^2) < 2*(3/200000) = 3/100000.
```

The slot counts are branch counts, not actual curve-instance counts.  The
literal round-27 missing field `canonical_recut_component_ids` is therefore
not declared filled; instead its conditional estimate is reinstantiated as
a rigorously typed universal branch-rule theorem.

At time two, `108,726` Q2 whole boxes strictly lie in one target normal chart.
For the remaining `5,280`, a representation seam cannot be strictly
excluded.  The seam is not treated as a physical singularity or artificial
physical cut, and no full Q2 F4 single-chart atlas is claimed.  Artificial
recut endpoints and word boundaries still lack their charged
characteristic-`Z` contribution, so F7 remains open.  F14--F18 and numerical
strong `q2` also remain open.

## 4. Limiting physical face-carrier families

The physical C24 core union now has an explicit carrier-family registry:

```text
physical core faces:                                      96
one-sided core trace IDs:                                192
R1 terminal-core-preimage equation families:           1,152
R1 hit/miss trace-family IDs:                           2,304
instantiated nonempty connected face pieces:                0
```

The 64 moving-occurrence coarea/DQ seeds and their 128 traces are replayed
independently.  All `24*64=1,536` core/occurrence pairs are strictly disjoint.
The 16,864 artificial dyadic R1 faces and their 33,728 computational traces
remain strongly typed as numerical partition faces and are never promoted
to physical occurrence carriers.

For each of the five physical carrier classes, the intended instance grammar
is

```text
face:(component ID, time j, carrier-family/seed ID, connected rank),
trace:(same fields, side label).
```

The connected rank requires a canonical one-dimensional parameterization
and enumeration of all nonempty pieces.  That rank assignment is not yet
certified, so this leaf freezes carrier-family grammars only.  R2 and general
`R_n` face IDs remain uninstantiated.  In particular, no carrier-family ID is
mistaken for an actual physical face component, and F14--F18 remain zero.

## 5. Exact conditional weighted-tail transfer

Let, for each fixed parameter,

```text
S_n = mu_s(Q_n)/mu_s(C_s),
mbar_j,k = m_j,k/mu_s(C_s),
qbar_j,k = q_j,k/mu_s(C_s),
c_j,k = qbar_j,k/mbar_j,k = q_j,k/m_j,k.
```

Here `(j,k)` ranges over positive-mass regular first-return components.  The
forward and reverse views use the same physical restriction and are charged
once; absolute continuity requires `q_j,k=0` on a zero-mass component.  A
strong singular-cemetery charge is a different object and is not absorbed
into this regular-component ledger.

The frozen unweighted return tail is

```text
S_n < A r^floor(n/N_open),
A = 550000/147,
r = 111718729/111718750,
N_open >= 1.
```

The new leaf closes three exact transfer routes on the same physical
first-return registry.

### 5.1 Survivor-conditioned `L^p`

If `p>1`, `0<K_p<infinity`, and

```text
sum_(j>n,k) mbar_j,k c_j,k^p <= K_p^p S_n
```

uniformly in `n`, then the normalized weighted tail satisfies

```text
Wbar_n < K_p A r^floor(n/N_open).
```

This route preserves the full unweighted block exponent.

### 5.2 Global `L^p`

If instead

```text
sum_(j>=1,k) mbar_j,k c_j,k^p <= M_p,
0<M_p<infinity,
```

then

```text
Wbar_n
 < M_p^(1/p) A^(1-1/p)
   r^((1-1/p) floor(n/N_open)).
```

The exponent remains exponential but loses the factor `1-1/p`.

### 5.3 Pointwise/blockwise growth

If `c_j,k<=C G^j` with `C>0`, `G>=1`, the geometric block series converges
under the exact compatibility condition

```text
G^N_open r < 1.
```

For `c_j,k<=C H^ceil(j/N_open)`, the condition is `H r<1`.  The one-block
window is extremely narrow:

```text
1/r = 111718750/111718729
    = 1 + 21/111718729,

21/111718750 < -log r < 21/111718729.
```

These are conditional transfer theorems, not current physical CM2 tail
certificates.  Presently the first-return registry contains no numerical
same-ID `C_fw`, `C_rev`, `q`, survivor-conditioned `L^p` envelope, compatible
pointwise envelope, global physical first-return `L^p` envelope, or numerical
strong cemetery charge.  Consequently the
strong-q excursion/cemetery tail and induced strong Lasota--Yorke estimate
remain `NOT CERTIFIED`.

## 6. Verification and independent audit

The four round-28 leaves use strict JSON parsing, frozen dependency hashes,
fresh producer reconstruction, independent replay, hostile mutations, and
fail-closed live exits.

The independent audits rejected and repaired four subtle type promotions
before the round root was created:

1. the weighted-tail proof now uses one normalized mass convention, retains
   the `mu_s(C_s)` factor correctly, and requires strictly positive finite
   constants where a strict inequality is stated;
2. physical face instance IDs now include component, time and connected-rank
   data instead of using a carrier family as if it were an instantiated
   piece;
3. Q2 recut-rule IDs are no longer called actual curve-component IDs, and
   F5/F6 counts are explicitly universal branch-rule slots;
4. the adaptive registry separates `t/p` and `s` face nullity, parameter-
   averaged and fixed-parameter masses, coordinate-base and physical
   collision-area masses, and Q2-anchor extension depth from unmaterialized
   Q1/limiting-R2 registries.

```text
round-28 hostile tests:                361/361
historical cumulative hostile tests: 2406/2406
live verifier exit code:                    2
```

All four round-28 leaf SHA files pass every listed entry.  The checksum
entries listed directly by the round-27 parent root, the v52 manifest, the
round-28 aggregate and the new root also pass SHA-256 verification.

A separate full transitive expansion of 143 historical `.sha256` files
exposed 12 pre-existing stale content entries: rounds 5--12 recorded an
earlier state of the subsequently appended
`cm2-continuous-research-log.md`, and rounds 9--12 likewise recorded an
earlier state of `memory/2026-07-16.md`.  These append-mutated historical
log/memory entries predate round 28 and are not advertised here as
recursively revalidated.  No round-28 leaf depends on relabelling those
stale content entries as current; the new four-leaf closure and its direct
aggregate/root checks remain valid.

## 7. Technology check

The current arXiv record was checked again for new open-billiard,
standard-family, coupling, inducing and weighted-transfer results.  The most
closely aligned new source remains arXiv:`2604.19671v2`, whose recovery
argument is tailored to boundary-position holes and explicitly does not
cover general phase-space holes such as the C24 union of parameter-uniform
rectangles.  It supplies a useful survivor-conditioned recovery blueprint,
but not the missing C24 same-ID component moment, connected face ranks,
strong cemetery charge, or induced common-space coefficient.  The review
arXiv:`2606.10155v1` likewise provides no theorem that replaces those
physical interfaces.

References:

- <https://arxiv.org/abs/2604.19671v2>
- <https://arxiv.org/abs/2606.10155v1>

## 8. Strict verdict and shortest next route

Strict status after round 28:

```text
CM2:                                             NO-GO FOR CLAIM
fully closed composite gates:                                0/5
Gate 2 official fields:                                     0/17
Gate 5 global maturity:                                     4/18
Gate 5 candidate-local R1 maturity:                        13/18
finite nonempty R1 adaptive anchors:             CERTIFIED 4,216
finite nonempty Q2 adaptive anchors:           CERTIFIED 114,006
Q2 universal F5/F6 branch-rule slots:          114,006 / 114,006
actual curve-recut instance IDs:                              0
instantiated physical connected face pieces:                 0
strong-q weighted return tail:                   NOT CERTIFIED
induced strong Lasota--Yorke coefficient:          NOT CERTIFIED
```

The highest-leverage next route is:

1. extend the certified Q2 anchors to physical Q3/R3 and prove a uniform
   adaptive termination/singularity-growth theorem while retaining the typed
   parameter-face convention;
2. instantiate actual parent-`W` recut cells and canonical connected ranks
   for nonempty one-dimensional physical face pieces, then certify numerical
   transversality, coarea-density and one-sided trace bounds on those pieces;
   refine the 5,280 target-chart seam boxes rather than guessing a full F4
   chart there;
3. charge F7 characteristic-`Z` at word and recut boundaries, then attach
   numerical same-ID `C_fw`, `C_rev` and `q` rows and assemble F14--F18;
4. target a uniform survivor-conditioned `L^p` envelope and a separate strong
   cemetery charge, because this route preserves the known unweighted block
   exponent and avoids the tiny pointwise compatibility window;
5. only then sum the return branches and derive the induced strong
   Lasota--Yorke coefficient on the common forward/reverse strong space.

Until those interfaces coexist on one immutable physical registry, the CM2
claim remains fail-closed.
