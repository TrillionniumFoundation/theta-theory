# CM2 one-hundred-thirty-second direct assault

Date: 2026-07-24

Round 132 closes the finite materialization and auditability of the Round28
moving-occurrence source records.  It does not close the Round67 owner/path
crosswalk.

Certified:

- 64 moving first-event grazing occurrence-face records;
- exactly one hit and one miss trace per face, hence 128 oriented traces;
- exact reconstruction of the frozen Round28 face, face-ID, trace-ID and
  parameter-germ registry digests;
- nine canonical source-side fields with typed qualifiers;
- three explicitly missing fields: return component, owner key and connected
  rank-zero component;
- 208 closed finite rows with unique hashes;
- independent verification verdict `PASS`.

Qualifier boundaries:

- oriented hit/miss is not a stable plaque side;
- an occurrence word cell is not an official return-path word;
- exact local endpoint/root coordinates are not an exact `(s,t,p)` path map;
- `9/12` source materialization is not `9/12` owner/path joining.

Open:

- a materialized Round67 owner map `q_j` and owned `Omega_j` record;
- a nonempty occurrence-pullback return component at depth at least two;
- its canonical connected rank;
- an exact Borel endpoint/root-to-path-coordinate map;
- any typed-incidence match row;
- all-time positive potentials and global F14/F15/F17/F18 assembly.

Strict verdict: owner/map/match counts remain zero, Gate5 remains
`NOT_CERTIFIED`, global maturity remains `10/18`, global complete blocks remain
zero, and CM2 remains `NO-GO_FOR_CLAIM`.
