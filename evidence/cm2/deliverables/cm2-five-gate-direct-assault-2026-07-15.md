# CM2 five-gate direct assault — consolidated stop/go ledger

Date: 2026-07-15 (Asia/Shanghai)  
Execution: direct interactive research only; **no cron or detached CM2
scheduler was created or used**  
Frozen baseline: v51/v52 and every file in `cm2-v52-manifest.sha256` were
read-only and revalidated after the assault

## Verdict

Unconditional CM2 remains **`NO-GO FOR CLAIM`**.  Under the physical
acceptance criteria needed by v52, the five requested main gates remain
`0/5` fully closed.  This is not a null result: several previously qualitative
or ambiguous sublayers are now theorems or machine certificates, and two
tempting but invalid promotions have been ruled out exactly.

| gate | new certified content | physical main-gate status |
|---|---|---|
| 1. common magnet | LOP Lemma 5.1 closes the existential clean heteroclinic cycle/common basic set; a 400-bit finite-word **eigentangent** matching seed is certified | **OPEN**: no validated invariant graphs, explicit common vertex, four-face full cross, or transported twisting |
| 2. physical PPE | actual one-state quotient kernel formula and word telescoping; arbitrary-history separated binary stopped-parent Frostman; endpoint Moebius/wedge algebra | **OPEN**: no full-mass physical quotient/tree, complete countable-kernel Frostman, normalized amplitude ledger, or same-carrier endpoint typing |
| 3. global event/DQ | `tau_max<3`; all target lifts lie in `G,W:[-4,4]^2`; eight exact source cells; every already selected forward circle tangency is a submersion | **OPEN**: no chart/target event rows, selected first roots, pair/triple inventory, global DQ, or scalar matching |
| 4. same-occurrence face time | one explicit physical moving tangency; complete-defect reversibility identity; local compactified face current is order zero | **OPEN**: reversal does not act eventwise on one occurrence id; no two typed nonadditive views, one `q=max(C_-,C_+)m`, or bidirectional recovery moments |
| 5. fixed-to-full transfer | `SF1`; explicit return-depth bound `K_N<=9`; Kac algebra; target standard-`N` spectral layer; regular-density norm equivalence | **OPEN**: return-boundary DQ and all four Kac currents are not typed; singular curve/face sources, physical tests, and both CM2 norm intertwiners are missing |

## Gate 1 — topology closed, physical common vertex open

The QNL and connector are clean regular hyperbolic periodic points of the
same finite-horizon standard collision map.  Lima--Obata--Poletti,
arXiv:2405.04676v2, Section 5, Lemma 5.1, applied in both orders gives genuine
clean transverse heteroclinic connections.  The ordinary inclination/Markov
construction therefore gives an existential compact clean basic set with
full-cross transition strips.

That theorem-level result is non-effective.  It does not output the common
vertex, words, widths, transport holonomies, or parameter interval needed by
physical PPE.

The new 400-bit script fixes one `10+14` collision shooting candidate and
proves

```text
t = -1.396101069259116746830594312272814e-9 +/- 4.92e-43
u =  2.186137101021440040809485447235071e-12 +/- 5.57e-46
det matching = 6.815572776012535e14 +/- 5.49e-9
flight > 0.1871067811, discriminant > 0.0102643235
incidence > 0.6332062369, clearance > 0.2228385811
```

The source curves in that calculation are the affine stable/unstable
**eigentangents**, not validated nonlinear invariant manifolds.  An
independent audit caught this typing issue.  The final script was renamed and
fails closed:

```text
TANGENT_LINE_MATCHING_SEED: CERTIFIED
HETEROCLINIC_INTERSECTION: NOT CERTIFIED
MICRO_FULL_CROSS_TRANSITION: NOT CERTIFIED
FULL_CROSS_COMMON_VERTEX: NOT CERTIFIED
TRANSPORTED_TWISTING: NOT CERTIFIED
```

The failed micro-cross thickening was discarded; it is neither a proof nor a
dynamical falsification.  A validated invariant-graph remainder enclosure is
the next required numerical primitive.

Evidence:

- `cm2-gate1-common-magnet-assault-2026-07-15.md`
- `cm2-gate1-topology-audit-2026-07-15.md`
- `cm2_gate1_tangent_line_matching_cert.py`
- `cm2-gate1-common-magnet-manifest-2026-07-15.sha256`

## Gate 2 — three exact bridges, physical instantiation open

For a genuine one-state full-branch unstable quotient with inverse branches
`h_a` and invariant SRB density `rho`, the actual reverse kernel is forced to
be

```text
p_a(x) = rho(h_a x) |h_a'(x)| / rho(x).
```

Invariance normalizes the kernel, and word weights telescope.  Thus a fixed
clean word has a uniform arbitrary-history hazard once the physical quotient,
bounded density, and branch derivative lower bound are constructed.

A second theorem now covers a place-dependent, non-i.i.d. separated binary
tree: if every conditional weight is in `[eta,1-eta]`, then on every stopped
parent

```text
nu_N(I) <= C (|I|^kappa + q^N),
q=1-eta,  kappa=log(q)/log(lambda_-).
```

The actual endpoint derivative ratio is also exactly a Moebius function of
the projective tangent, with derivative equal to the endpoint wedge divided
by the squared denominator.

None of these results constructs the pilot's full-mass quotient.  The two
periodic matrices still generate only an artificial negative-pressure binary
subsystem.  Exact counterexamples additionally prove that marginal Frostman
does not imply frozen-parent Frostman and that a raw `L^p(A)` estimate does
not control the normalized tilt `A/Z_A`.

Evidence:

- `cm2-gate2-physical-ppe-assault-2026-07-15.md`
- `cm2_gate2_quantifier_obstruction_cert.py`
- `cm2-gate2-physical-ppe-manifest-2026-07-15.sha256`

## Gate 3 — finite horizon and finite universe are now numerical

The moving white disk contains a fixed nominal core of radius `31/200`.
A 160-bit Arb adaptive cover of
`[0,1]^2 x (R/2*pi*Z)` with 35,024 leaf boxes proves that every ray meets a
gray disk or a white core at a witness time in `[3/4,3]`.  Because
`3/4>18/25`, this cannot be residual residence in the initial core; convexity
also excludes re-entry of the physical source obstacle.  Hence

```text
tau_max < 3.
```

It follows rigorously that the conservative next-target universe is

```text
G[i,j], W[i,j],  (i,j) in [-4,4]^2,
```

162 lifts in total.  The phase interior is covered by the eight exact
dominant-coordinate cells `G/W x E/W/N/S`; `p=+/-1` remains a named grazing
boundary.  Together with the earlier lower flight margin, every specified
physical forward circle tangency has

```text
|partial_phi Delta| > 1821/31250 = 0.058272.
```

The fail-closed manifest still has zero instantiated global event rows.
Each source-chart/target row needs the selected incoming root, first-hit
comparisons, physical traces, coarea coefficient, immutable occurrence
record, and scalar-current witness.  All pair/triple incidences must then be
classified before the global DQ can be assembled.

Evidence:

- `cm2-gates34-event-q-assault-2026-07-15.md`
- `cm2_standard_section_horizon_lift_cert.py`
- `cm2-gates34-event-q-manifest-2026-07-15.json`
- `cm2_gates34_manifest_verifier.py`
- `cm2-gates34-assault-manifest-2026-07-15.sha256`

## Gate 4 — reversibility is global, not automatically eventwise

An exact quadratic-field witness on the standard section starts at
`q=(9/25,0)` and is tangent to the central white disk.  It is a physical
first singularity and satisfies

```text
partial_s H = 0.266565911815513... > 0
partial_phi H = 4 sqrt(610)/625 = 0.158068339650924... > 0
d phi / d r = -3.8977898921...
```

For transfer on measures and the time-reversal involution `R`, exact
differentiation of `P_s^{-1}=R P_s R` gives

```text
dot P = -P R dot P R P.
```

This controls the complete defect.  It does not imply the same equation for
each tagged event summand: reversal normally sends a forward row to a
distinct past-event row, swaps traces, and reverses coarea orientation.  In
the explicit witness, the genuinely reversed source is the white grazing
boundary (`cos phi=0`), so two-dimensional SRB invariance cannot manufacture
a bounded-density reverse interior proper family from the nonzero forward
coarea law.

Thus a single-charge bridge remains conditional on two restrictionwise exact
representations of the same occurrence and recovery moments under the same
pre-query law `q=max(C_forward,C_reverse)m`.

Evidence:

- `cm2-gate4-reversibility-bridge-2026-07-15.md`
- `cm2_gate4_explicit_tangency_cert.py`
- `cm2-gate4-reversibility-manifest-2026-07-15.sha256`

## Gate 5 — explicit bounded common refinement, singular lift open

Take the common phase space `X=M*=M sqcup W`, which contains both the fixed
section `M` and standard solid section `N=G sqcup W`.  Return to `M` has depth
at most two.  The new `tau_max<3` certificate gives the conservative bound

```text
K_N <= 2 ceil(tau_max) + 3 <= 9
```

and a finite Borel return-word registry.  This closes `SF1` quantitatively.
Fixed flux and the Kac quotient close the centering algebra in `SF3`; the
standard-`N` spectral/Wiener target layer is available; Demers--Liverani
arXiv:2606.10155v1, Theorem 6.1 closes the regular-density norm-equivalence
sublayer.

The moving return-boundary terms `dot S` and `dot r` remain singular currents.
The available Banach theorem does not embed the one-dimensional standard-pair
or oriented face-current source required here.  Therefore `SF2`, the four
typed Kac terms, the singular part of `SF5`, the physical test lift, and both
CM2 norm intertwiners remain open.  Proving Gates 3--4 directly on `N` would
make a separate fixed-to-full theorem unnecessary, but it would not count as
closing this gate retroactively.

Evidence:

- `cm2-gate5-full-boundary-transfer-assault-2026-07-15.md`
- `cm2_gate5_transfer_manifest.json`
- `cm2_gate5_transfer_audit.py`

## Literature audit

The latest directly relevant sources checked were:

1. Lima--Obata--Poletti, arXiv:2405.04676v2, Lemma 5.1: closes only the
   existential clean homoclinic topology.
2. Canestrari, *On linear response for discontinuous perturbations of smooth
   endomorphisms*, Advances in Mathematics 497 (2026), 111008,
   arXiv:2411.16628v3: an exact abstract template, but its initial-defect and
   summable-foliation hypotheses are the missing Gates 3--4 inputs.
3. Canestrari, *Linear response for Sinai billiards with small holes*,
   arXiv:2604.19671v2: supplies standard-family recovery for a fixed map and a
   shrinking hole, not a moving-scatterer event current or bidirectional
   occurrence law.
4. Demers--Liverani, arXiv:2606.10155v1, Theorem 6.1: regular density/cone
   norm equivalence, not the singular face-source embedding.

No checked 2025--2026 theorem proves linear/CM2 response for this moving
scatterer while simultaneously constructing the global event current and
typing the singular curve source.  Invoking any of the above as a complete
shortcut would assume the open gates.

## Reproduction and acceptance

The consolidated verification requires:

```bash
sha256sum -c deliverables/cm2-v52-manifest.sha256
sha256sum -c deliverables/cm2-gate1-common-magnet-manifest-2026-07-15.sha256
sha256sum -c deliverables/cm2-gate2-physical-ppe-manifest-2026-07-15.sha256
sha256sum -c deliverables/cm2-gates34-assault-manifest-2026-07-15.sha256
sha256sum -c deliverables/cm2-gate4-reversibility-manifest-2026-07-15.sha256
```

The positive certificates must exit zero.  The Gates 3--4 evidence snapshot
and Gate-5 manifest are intentionally incomplete; their verifiers must reject
them with exit status `2`.  An exit zero on either incomplete snapshot would
be a verifier failure, not a theorem.

Final labels:

```text
GATE_1_PHYSICAL: NOT_CERTIFIED
GATE_2_PHYSICAL_PPE: NOT_CERTIFIED
GATE_3_GLOBAL_EVENT_DQ: NOT_CERTIFIED
GATE_4_SAME_OCCURRENCE_FACE_TIME: NOT_CERTIFIED
GATE_5_SECTION_FULL_TRANSFER: NOT_CERTIFIED
UNCONDITIONAL_CM2: NO-GO_FOR_CLAIM
```
