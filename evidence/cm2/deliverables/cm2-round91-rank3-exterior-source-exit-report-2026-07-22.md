# CM2 Round 91 — rank-three exterior source-exit frontier

## Certified result

Round 90 left exactly 65 exterior projective rays after all 440 interior gaps
were closed and 47 projective ends were capped by registered source endpoints.
Round 91 reverses every live third-tangent target through the second obstacle,
the first obstacle, and the source circle in the projective coordinate
`q = tan(theta/2)`.

For 61 of the 65 rays, the certificate reaches the first boundary of the
frozen source core while retaining, on a complete interval chain:

- strict first- and second-collision owner selection;
- strict flight order and outgoing-state existence;
- the complete translated competitor table;
- the prescribed third-candidate tangent sign; and
- a 112-step rational bracket for the first source-core boundary root.

The source-exit boundary census is:

| Boundary | Rays |
|---|---:|
| `P_LOWER` | 22 |
| `P_UPPER` | 24 |
| `T_LOWER` | 8 |
| `T_UPPER` | 7 |
| **Total certified** | **61** |

## Exact residual frontier

Four rays remain.  At dyadic chain sizes through 32,768, their only failure is
an interval enclosure straddling a competitor discriminant zero.  No owner,
flight-order, outgoing-chart, target-tangency-sign, or source-exit failure was
found.  The four frozen branch keys are:

1. `(12, W[0,0], G[3,-1], +1)`, left projective end;
2. `(13, W[0,0], G[3,2], -1)`, right projective end;
3. `(14, W[0,0], W[-2,1], +1)`, left projective end;
4. `(16, W[0,0], G[2,3], +1)`, left projective end.

This is an event-isolation frontier.  Blindly increasing the uniform dyadic
depth is not a legitimate substitute for isolating the discriminant roots and
certifying the physical classification on both sides.

## Strict interpretation

The certificate proves reverse-projective physical continuation of 61/65
exterior rays to the first boundary of their frozen source cores.  A frozen
source-core boundary is a registry endpoint, not automatically an endpoint of
the global physical tangency face.  Therefore Round 91 does not install a
complete physical-face quotient, RN rows, Gate 5 fields, or any global gate.

The strict global state remains:

- Gate 1: `NOT_CERTIFIED`;
- Gate 2: `0/17`;
- Gate 3: `NOT_CERTIFIED`;
- Gate 4: `1/7`;
- Gate 5: `10/18`, complete blocks `0`;
- composite gates: `0/5`;
- CM2: `NO-GO_FOR_CLAIM`.

## Next shortest attack

1. Isolate the four competitor-discriminant roots with interval Newton or a
   centered Taylor/Krawczyk chart, type the event, and continue each ray on
   both sides to its source-core exit.
2. Continue all 65 registry exits beyond the frozen source cores until a
   source exit, owner/chart/translation seam, or genuine rank-transition event
   is reached; only then rebuild the complete physical-face quotient and RN
   frontier.
3. In parallel, continue the independent C24 return search, all-depth Gate 3
   recipient construction, and arbitrary-regular-density F14 intertwiner.

