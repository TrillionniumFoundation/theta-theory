# CM2 forty-seventh direct assault

Date: 2026-07-19  
Verdict: **Round 47 repairs the adapted-metric covering interface, gives a
sharp conditional W-diagonal hit threshold and a per-orientation familywise
post-cut return, while adding two rigorously scoped Gate-5 seeds.  No
composite gate is yet closed.**

## Round result

| gate | Round-47 advance | exact remaining core gap | status |
|---:|---|---|---|
| 1 | no unsafe promotion | physical Green/cancellation representative and same-representative twisting/PPE | NOT CERTIFIED |
| 2 | no cross-carrier promotion | invariant stable plaques, physical holonomy, quotient branches and PPE | NOT CERTIFIED |
| 3 | two-trace/current typing is sharpened | bulk dynamic-test `MT_DQ`, branch convergence and cemetery | NOT CERTIFIED |
| 4 | corrected adapted cells; W-diagonal conditional hit; familywise post-cut return | actual `H_cover/beta_Wdiag`, Borel mass kernel, joint fw/rev clock, `q`, cemetery | NOT CERTIFIED |
| 5 | two-trace `(C1)*` sublayer, rank-zero C24 F10 seed, frozen 720-case Piola audit | full F10, complete strong F13, F14/F15/F17/F18, cemetery | NOT CERTIFIED (`10/18`) |

Strict unconditional progress remains `0/5`; CM2 remains
`NO-GO_FOR_CLAIM`.

## 1. Gate 4: corrected and narrowed numerical frontier

Two inherited type mismatches are now explicit and repaired in the new
certificate:

1. the source-cell registry is recut in adapted arclength, not by reusing a
   Euclidean `10^-90` bound as an adapted bound;
2. the crossing mass fraction is computed in the same adapted line element
   controlled by `R_ext`, not by multiplying an Euclidean fraction by an
   adapted-density ratio.

For a white-obstacle diagonal C24 crossing,

```text
ell_*(parent) < 66/7,
ell_*(crossing) > 1/25,
R_ext < 400000000/399794003,
h_Wdiag > 2798558021/660000000000.
```

The sufficient terminal-time crossing-family weight is therefore

```text
beta_Wdiag >= 230400/5197322039.
```

`1/22557` is safe and `1/22558` fails.  The actual weight and its common
terminal covering time remain unknown, so this is not a proper-family
minorisation yet.

Replaying the one-step killed Growth bound on a whole canonical proper family
gives `2^316<Z1*C_p<2^317`.  A positive survivor in dyadic family-mass shell
`k` returns, separately in each orientation, after

```text
R_post(k)=319590+1005*k.
```

This is not leafwise and not a same-ID forward/reverse joint statement.  The
finite/countable shell moment is a conditional discrete-family lemma; the
standard-Borel parent/survivor mass kernel remains missing.

## 2. Gate 5: strong sublayers without overpromotion

The Round-44 two-trace boundary measure embeds in `(C1)*` with multiplier one
on every finite regular suffix at `s=0`.  Its charge remains

```text
c_F13,n <= (3816937/47112000)c_D1,n.
```

The bulk Duhamel current is deliberately excluded.  Its source bound is
`(11616937/47112000)c_D1<c_D1/4`, but a nonempty suffix still pays
`D_suffix*c_X`; Piola does not cancel that term.

Separately, the area-coordinate translation generator supplies a rank-zero
affine C24 core-edge F10 seed:

```text
|X|_1<8,  ||DX||_1<47,
|a|+|partial_tau a|<55,  partial_s a=0.
```

The missing all-face same-measure join prevents F10 field credit.  Gate-5
maturity stays `10/18`, complete blocks stay zero.

The 180 integer `SL(2)` matrices and 720 exact Piola flux cases are now frozen
with an independently replayed digest.  They are regression evidence, not a
field-completion claim.

## 3. Latest-technology audit

Direct fetches of the official arXiv records on 2026-07-19 confirm the latest
versions remain

```text
1210.0011v4 / 2104.06947v3 / 2604.19671v2 / 2606.10155v1.
```

The small-hole linear-response result still concerns a boundary hole and does
not supply the target-sensitive W-diagonal covering weight, arbitrary-depth
moving-face same-measure join, bulk F17 bound, or strong cemetery required
here.  No theorem is imported across those shape or norm mismatches.

## 4. Validation

Both Round-47 suites use duplicate-key-rejecting JSON loaders, pinned direct
dependencies, independent rational arithmetic, deterministic manifest replay
and fail-closed default modes.  The Gate-5 verifier independently enumerates
all 180/720 Piola cases.  Validation passes:

```text
syntax:                 4/4
strict JSON:            2/2
integrity/replay:       4/4
hostile mutations:      47/47 + 46/46 = 93/93
default fail-close:     4/4 at exit 2
deterministic manifests: 2/2
```

Leaf and recursive SHA manifests freeze the final sources, reports and JSON
outputs.

## Next shortest line

1. Build a target-sensitive terminal-time covering registry and prove actual
   `H_cover` plus `beta_Wdiag>=230400/5197322039` without double-counting
   artificial children.
2. Construct the standard-Borel parent/survivor mass kernel and the common
   same-ID forward/reverse survivor ledger; then assemble numerical
   `C_fw/C_rev/q` and strong cemetery.
3. Join occurrence coarea and C24 core traces on the same arbitrary-`R_n`
   path measure to close full all-face F10.
4. Control the bulk Duhamel suffix by F17/dynamic tests or a joint rank tail,
   then complete strong F13, F14/F15/F17/F18 and Gate-3 `MT_DQ`.
5. Continue Gate 1 and Gate 2 as independent structural poles.

Strict unconditional CM2 remains `NO-GO_FOR_CLAIM`, with zero of five
composite gates fully closed and Gate-5 maturity at `10/18`.
