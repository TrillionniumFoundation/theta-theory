# CM2 forty-first direct assault

Date: 2026-07-19  
Verdict: **the hereditary C24 Growth base and qualitative aggregate-`Z`
resolvent are installed; physical one-step parameter jets are numerical, but
all five composite gates remain open**

## Round result

| gate | Round-41 advance | exact remaining core gap | status |
|---:|---|---|---|
| 1 | no unsafe transfer from the shared Gate-3/4/5 work | physical cancellation/Green representative and same-representative twisting/PPE | NOT CERTIFIED |
| 2 | no cross-carrier promotion | invariant stable plaques, physical holonomy, quotient branches and PPE | NOT CERTIFIED |
| 3 | every-collision killed standard-family Growth is now available as a future dynamic-current carrier | arbitrary-depth dynamic DQ/`MT_DQ`, moving-current propagation and cemetery | NOT CERTIFIED |
| 4 | C24 matches Demers `H1/H2`; hereditary killed Growth and a qualitative weighted aggregate-`Z` resolvent are certified | numerical `n_*,Z0,Z1`, then numerical `C_fw,C_rev`, final `q` and cemetery | NOT CERTIFIED |
| 5 | physical `U(B),V(B),W(B)` and all-five-face base parameter jets are certified | reduce the `2^(3B)` mixed-jet loss or prove a stronger same-law path tail; then full F10/F12/F13/cemetery/F14--F18 | NOT CERTIFIED (`7/18`) |

Strict unconditional progress remains `0/5`; CM2 remains
`NO-GO_FOR_CLAIM`.

## 1. Gate-4 Growth bottleneck is removed qualitatively

Round 40 mis-typed Canestrari's `hat F` as the conditional survivor map.
The original construction instead keeps all mass and adds the hole boundary
as an artificial singularity set.  Demers' general-hole theorem needs only
finite short-curve cutting complexity and weak transversality.

C24 already has

```text
B0=49,
Ct=1493,
```

uniformly on `|s|<=1/400`, so it belongs to that general class.  With the
closed-map expansion

```text
theta=900337/901685<1,
```

the artificial-cut kernel is

```text
(1+48n)*theta^n.
```

Its exact bare `1/2` threshold is `n=9148`.  Finite nonnumerical density and
metric prefactors require a larger existential final block, so `9148` is not
promoted to `n_*`.

Deleting C24-hit descendants from the all-mass extra-cut family decreases
unnormalised `Z`.  Therefore, for any selected `gamma<1`, there are uniform
finite constants with

```text
Z(O_s^n_* G)<=gamma Z(G)+Z0 mass(G).
```

This is hereditary under every subsequent C24 cut.

## 2. Aggregate Z no longer needs cellwise retained-depth products

Joining the killed-family recurrence to the Round-38 scheduled mass factor

```text
r=111718729/111718750<1
```

on a common block gives

```text
z_(p+1)<=g z_p+C m_p,
g<1,
m_p<=rho^p m_base,
rho<1.
```

The explicit convolution decays geometrically, so a weighted aggregate
canonical-`Z` Green sum is finite for controlled finite-`Z` physical initial
families.  This route uses neither a cellwise `2^D` moment nor a
projective-norm-to-canonical-`Z` trace.

The result is qualitative: `N_open,n_*,Z0,Z1,w_Z` and the resolvent constant
are not numerical.  Therefore numerical `C_fw,C_rev,q` and Gate 4 remain
open.

## 3. Gate-5 one-step parameter payload is numerical

Exact ray-circle differentiation in the fixed horizontal-translation gauge
gives, on every cross-colour regular branch,

```text
U(B)=20*2^B,
V(B)=20000*2^(3B),
W(B)=40*2^(3B),
```

and zero parameter jets on same-colour branches.  Tangency levels satisfy

```text
|G_s|<18/25,
|G_xs|<97/2,
|G_ss|<=2,
```

while the remaining frozen base-face types are affine/stationary or have the
integer-corner bound `(1,7,0)`.  All five physical face grammars now have base
parameter jets.

## 4. The next F10 obstruction is supercritical and exact

The current physical incidence theorem controls `2^(3B/2)`, not `2^(3B)`.
The law

```text
P(B=b0+2k)=(15/16)*16^-k
```

has finite `2^(3B/2)` moment and divergent `2^(3B)` moment.  Thus the new
coarse `V/W` envelope cannot be summed from the existing marginal rank law.

The next shortest F10 move is either to expose cancellation/anisotropy that
lowers the effective exponent, or prove a substantially stronger joint
spacetime path tail.  The same-law denominator and branch-persistence margin
must be joined at the same time.

## 5. Latest-technology audit

The official version snapshot remains

```text
2104.06947v3 / 2604.19671v2 / 2606.10155v1.
```

Round 41 additionally checked Demers' 2014 general-hole proof directly
(DOI `10.1007/978-1-4939-0419-8_8`).  Its `H1/H2` interface, not the special
boundary-strip shape in the 2026 application, is the relevant Growth input.
No small-hole spectral statement is imported for C24.

## 6. Validation

Both new suites pass syntax, pinned dependency hashes, strict JSON,
independent exact replay and live fail-close.  They reject

```text
27/27 + 28/28 = 55/55
```

hostile mutations.  Both certificate defaults and both verifier defaults
exit `2`; replay modes pass.  Leaf and recursive SHA chains freeze every
report, certificate, verifier and manifest.

## Next shortest line

1. Materialize uniform numerical density-ratio/cone-metric/chopping constants
   in the C24 Growth proof, yielding explicit `n_*,Z0,Z1,w_Z` and resolvent.
2. Feed that numerical aggregate-`Z` tail into same-ID `C_fw,C_rev` and final
   `q`; close strong cemetery independently.
3. Rework the mixed parameter jet in anisotropic coordinates to lower the
   effective `2^(3B)` loss, or prove a matching joint spacetime rank-path tail.
4. Install arbitrary-`R_n` all-face F10, F12/F13, F14--F18 and the induced
   coefficient; then return the common ledger to Gate-3 dynamic DQ/`MT_DQ`.
5. Continue Gate 1 cancellation/twisting and Gate 2 plaque/holonomy/quotient/
   PPE as independent structural poles.

Strict unconditional CM2 remains `NO-GO_FOR_CLAIM`, with zero of five
composite gates fully closed and Gate-5 maturity unchanged at `7/18`.
