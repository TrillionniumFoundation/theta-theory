# CM2 round306c50b D02-B global cemetery/disconnected-exterior oracle report

Date: 2026-08-12 (CST)

## Verdict

The occurrence-bound API and independent audit boundary are implemented, but
the global mathematical oracle is still absent.  The only lawful result is

```text
PENDING_MISSING_GLOBAL_MATHEMATICAL_ORACLE_FAIL_CLOSED
disposition       UNRESOLVED
terminal_class    null
terminal_credit   0
D02_credit        0
formal_credit     0
```

No round306c50b file installs an authority, pointer, seal, receipt, canonical
status, or D02 credit.  A bounded continuation, a finite-horizon absence of a
contact, a local chart exit, or an endpoint lying outside a local box is never
accepted as `STRICT_CEMETERY_OR_DISCONNECTED`.

## Audited semantic boundary

The C35--C37 material supplies a 1,648-collision template registry, occurrence
margin regression data, and a reflected transport.  Those rows explicitly
remain template/regression inputs, not occurrence-level exterior proofs.

C46-B freezes both physical sides and an occurrence continuation interface,
but its positive work is collision-3 diagnostic materialization and its live
branches remain resumable.  Its own primitive registry names the global
cemetery/disconnected-exterior oracle as missing.

C49 supplies local arbitrary-history continuation evidence and leaves every
locally live step in `PENDING_GLOBAL_ORACLE`.  The round306c50b implementation
does not pin or import a C49 producer source: it accepts the complete embedded
`original_box`, `owner_history`, current step, hashes, and an independent replay
attestation.  This keeps the exterior contract decoupled from the concurrently
hardened C49 v4 source hash.  A diagnostic compatibility run against the
current C49 collision-3-to-4 history passed, but that run is not an authority
pin and grants no credit.

The repository still lacks a reconstructible theorem/engine for all of:

1. a complete four-chart physical fundamental-domain atlas with exact quotient;
2. exact face, corner, source-grazing gluing and ownership;
3. an exterior branch-and-bound leaf ledger with `unresolved=0`;
4. independent component adjacency and the known connected-sheet anchor;
5. an independently reconstructed strict cemetery/disconnected decision;
6. a pinned no-producer-import arbitrary-history numeric replay authority.

Without those objects, self-hashed booleans such as “all charts checked” or
“unresolved zero” are not evidence and cannot enable a terminal.

## Implemented API

`cm2_round306c50b_d02b_global_cemetery_disconnected_exterior_oracle_v1.py`
accepts one canonical JSON query file and performs a stable single-link,
`O_NOFOLLOW` read with before/after/name inode and byte metadata checks.  Its
physical-side occurrence binding includes:

- frozen `original_box`, source preimage, physical side and chart;
- contiguous `owner_history`, including genesis and later numeric evidence
  preimages;
- the current closed step and its evidence hash;
- exact owner, discriminant, root order, official word, chart, wall,
  homogeneity, incidence, core and structured terminal margin;
- an independent replay attestation committed to the original box, complete
  history, current step, and all ten exact fields.

The API verifies the available structural commitments and cross-bindings.  It
does not treat hashes as mathematical authenticity.  Positive terminal output
is hard-disabled because `approved_global_decider_source_sha256` is null.

Supported commands are:

```bash
python -B deliverables/cm2_round306c50b_d02b_global_cemetery_disconnected_exterior_oracle_v1.py --contract
python -B deliverables/cm2_round306c50b_d02b_global_cemetery_disconnected_exterior_oracle_v1.py --self-test
python -B deliverables/cm2_round306c50b_d02b_global_cemetery_disconnected_exterior_oracle_v1.py --query QUERY.json
```

## Hostile tests and independent audit

The producer self-test executed 15/15 attacks.  It covers a baseline binding
with a later numeric history preimage, history order, owner, root order,
official word, chart, wall, core, original box, physical side, bounded/no-current-
contact shortcuts, a coherently self-hashed but unapproved global claim, and a
same-name TOCTOU swap.

```bash
python -B deliverables/cm2_round306c50b_d02b_global_cemetery_disconnected_exterior_oracle_v1.py --self-test
```

```text
PASS_15_OF_15_EXECUTED_HOSTILE_TESTS_FAIL_CLOSED
```

The independent verifier imports and executes no producer.  It reconstructs
the exact missing-oracle contract, pins the producer and both result object
hashes, checks the 15-test receipt, and executes 30/30 independent semantic,
JSON, symlink, hardlink and TOCTOU attacks.

```bash
python -B deliverables/cm2_round306c50b_d02b_global_cemetery_disconnected_exterior_oracle_independent_verifier_v1.py
```

```text
PASS_INDEPENDENT_NO_PRODUCER_IMPORT_FAIL_CLOSED_ORACLE_AUDIT
producer_imported   false
producer_executed   false
independent_attacks 30/30
terminal_class      null
D02_credit          0
```

A compatibility smoke test used the workspace `python-flint==0.9.0` runtime,
constructed the current C49 collision-3 live step, appended its authenticated
numeric evidence to a three-row history, and validated the resulting
collision-4 `original_box + owner_history` binding:

```text
PASS_CURRENT_C49_V4_COLLISION3_TO_4_HISTORY_BINDING
history_length  3
next_collision  4
D02_credit      0
```

This smoke test intentionally does not become a C49 source pin.

## Cryptographic bindings

```text
producer source
  6c57f7ed935603e7094b73e4f6492cd1ea1d26b7beeac8c43733d5a006965a4b

missing-oracle contract file
  25248a20e5a81f5119b204d856ef61ce18c2307412f3b304f2bb35213729ce1e
contract object
  ddd1776bc133cac340b872f03325e97e8526a545e3e1a3fb11d56a159a4f9d97

producer hostile-test file
  32092e165689b0612cfb0c443e9cfaae6fc288af4a1ddcf44944c799634ac404
hostile-test object
  2bfe14bd0694f96d9e85ac8f361019b0474d7d78ba8c93150070747a17217dbf

independent verifier source
  d8907c2215c2b401405d824c1c435f8254fe491c115230beb038ca3ea434de02

independent verification file
  5fbaa3a171cf26650b8ee3179b4acbe5107d58707923aa6288058e1475db2bdb
independent verification object
  b782d342c306d102f056d94af01797a8a08d717bd0562f680f0a375bbdf23ebf
```

## Next mathematical step

The next implementation cannot be another local continuation shortcut.  It
must materialize the four-chart fundamental-domain BnB and exact glue ledger,
then let a separate implementation reconstruct every leaf disposition and
component adjacency with zero unresolved leaves.  Only a pinned decider that
recomputes those objects may replace the null approved-decider field.  Until
then this oracle remains deliberately useful only as a binding/denial boundary.
