# CM2 Round271: WALL and outgoing-tail side-signature materialization

## Certified result

Round271 evaluates the complete `45,916`-leaf residual from Round270.

- all `4` OUTGOING-W derivative tails close after one exact t bisection,
  yielding `12` strict factor-region signatures;
- all `8` OUTGOING-G wall-event tails are proved to meet their transition
  only on the excluded `t=0` face, yielding `8` open-interior signatures;
- `45,408 / 45,904` WALL leaves have a strict or single-active-factor
  Round182 arrangement and yield `70,400` connected-side signatures;
- exactly `496` WALL leaves (`720` prospective regions) retain both endpoint
  factors as whole-leaf overwraps and remain fail-closed.

Total newly materialized complete ten-field signatures: `70,420` on `45,420`
parent leaves.  Certificate result SHA256:
`30f509eb10b1a25c5195e672e980d7e4e651dadc3defd5e150245c6798060506`.
Complete certificate SHA256:
`c2a6b66c6fc6ac0b353b36254339a90b91f18c52246c324307ee49569bd7b747`.

## Verification

The independent verifier pins the producer as inert bytes and recomputes all
`45,916` parent decisions and `70,420` signature rows before opening the
candidate.  It returns `PASS_INDEPENDENT_ROUND271`; all `5/5` semantic attacks
are rejected.  Seeds `271071` and `271929` produce identical bytes.

## Strict non-promotion

Round271 awards zero expanded-occurrence, component-union, maximality, fibre,
global-disposition, or Jx/Jy glue credit.  The quotient remains `63,224`; the
expanded occurrence and exact-key frontiers remain `126,468 / 116`;
maximality remains `0/63,224`, fibres `0/116`, and dispositions
`0/224,580`.  Gate5 remains `10/18`, D02 remains blocked, and CM2 remains
`NO-GO_FOR_CLAIM`.

Next: resolve the `496` dual-factor WALL leaves by an exact two-curve
arrangement; then bind every frozen side signature and all `152` true-seam
patches to the expanded component frontier before maximality and fibre
exhaustion.
