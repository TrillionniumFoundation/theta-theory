# CM2 one hundred thirty-third direct assault

Date: 2026-07-24

Strict verdict: **the 64 Round132 moving-occurrence records can be closed as
owner-candidate requests, but no nonempty subset is certified to lie in the
domain of the Round67 owner map.  Actual owner membership remains unknown,
not empty.  Gate5 and CM2 are unchanged.**

## Exact owner-map boundary

Round50 supplies a Borel owner selector on a complete active representation
fibre:

```text
E_i^owner = E_i \ union_(h<i) E_h.
```

Round61/67 then applies `q_j` only after this selection, on

```text
Omega_j={(a,x):x in E_(j,a)^owner intersect R_(j,a)^reg}.
```

Its output is a point of the standard-Borel Round54 owner/root base with the
full owner token and endpoint/root coordinates.  The frozen theorem is an
actual deterministic same-law pushforward statement, not an anonymous
coupling.  It is nevertheless not a row-generating implementation: no
canonical serialized `q_j` output or owner-key ID is frozen.

## Why the 64 records cannot be minimized

The Round132 source event identifier hashes the primitive key together with
the physical label, boundaries and polarity.  It therefore cannot group
alternative primitive representations of one Round50 event.  Uniqueness
among the observed 64 identifiers says nothing about completeness of any
Round50 candidate fibre.

Moreover, a Round28 global occurrence seed is not an occurrence-pullback
component.  The source rows do not supply a path, parent curve, canonical
component, regular active-root membership, connected rank-zero witness,
Round50 side, Round54 word cell or executable `q_j` output.  Naming an owner
from an occurrence ID, global label, target, core or side would be an
ill-typed splice.

## Frozen output

Round133 materializes only:

- 64 owner-candidate request rows with exact Round132 provenance;
- 5 blockers in dependency order;
- 17 fields required for a future executable owner row.

The certified/materialized owner count is zero.  The actual number of owned
records among the underlying physical occurrences is
`UNKNOWN_NOT_CERTIFIED`; no emptiness theorem is claimed.

The first missing object is an exact Round132-to-Round50
active-representation domain crosswalk.  One nonempty completion requires a
particular path/component/parent-`W`, a complete primitive-free event fibre,
regular activity, a unique transverse rank-zero root, owner minimization,
side and word-cell reconciliation, and a versioned recordwise `q_j` encoding.

## Independent verification

The verifier independently reconstructs all 86 rows and rejects:

- every attempt to select one of the 64 observed rows as an owner;
- empty-set promotion of the unknown actual membership;
- target/core/occurrence surrogate owner keys;
- a fabricated `q_j` serialization;
- row, blocker, contract, pin and envelope mutations;
- global field, block, Gate5 and CM2 promotions.

The final verification is `PASS` with 90 semantic mutations and 19
strict-JSON attacks rejected.  Dual `PYTHONHASHSEED` replays are byte
identical, and missing/tampered/alias/symlink/hardlink/FIFO paths fail
closed.

## Strict frontier

```text
Round132 owner-candidate requests:                  64
certified/materialized Round67 owned records:       0
actual nonempty owned subset existence:             UNKNOWN_NOT_CERTIFIED
recordwise q_j outputs:                             0
Round67-to-return typed matches:                    0
global complete 18-field blocks:                    0
Gate5 maturity:                                     10/18
Gate5:                                              NOT_CERTIFIED
CM2:                                                NO-GO_FOR_CLAIM
```
