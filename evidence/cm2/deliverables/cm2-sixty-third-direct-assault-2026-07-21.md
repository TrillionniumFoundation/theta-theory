# CM2 Round 63 full direct assault

Date label: 2026-07-21  
Strict verdict: **0/5 complete composite gates; Gate 5 maturity 10/18 with
zero complete 18-field blocks; CM2 NO-GO_FOR_CLAIM**

## Round result

Round 63 closes no composite gate and does not promote a missing physical
strong interface. It does make four strict improvements to the frontier.

1. Gate 1 removes a previously over-specified finite-approximant
   equi-Hölder obligation. Once the canonical `A` holonomies, a uniformly
   bounded Hölder transfer `C,C^-1`, and uniform two-sided renormalized
   defect decay are installed, the exact endpoint-conjugacy formula itself
   passes the Hölder modulus to the `B` holonomies.
2. Gate 2/4 gains the exact branch--stable-holonomy commuting-square defect,
   the two-plaque `L^1` stable-saturation distance, and the exact `m^2`
   distortion cost for transporting the quantitative landing bound.
3. Gate 3 removes proof-only CAD/dyadic subdivisions and exact duplicate
   chart/lift incidences before total variation. The regular base physical
   faces join the pinned same-ID F13 Borel ledger; a distinct stopping-clock
   jump current is isolated as a sixth debt outside the five F13 grammars.
4. Gate 5 joins the actual fixed-time forward/reverse orientation-cost laws
   in one measure lattice and gives an exact positive killed trace-kernel /
   Lyapunov interface for all-time weighted decay. The actual drift is still
   absent, and the Round-52 collision-null face tower continues to forbid a
   collision-survival shortcut.

All new claims remain typed as exact identities, conditional sufficient
interfaces, fixed-time physical rows, or logical separators. No conditional
row is counted as a Gate field. No older artifact is edited.

## Gate 1: endpoint conjugacy and the sharp scalar budget

Use the frozen convention

```text
B(x)=C(fx)^(-1)A(x)C(x).
```

For a local stable pair put

```text
Delta_n^s
=A^n(y)^(-1)[C(f^n y)C(f^n x)^(-1)-I]A^n(x).
```

The finite identity is

```text
H_B^s(x,y;n)
=C(y)^(-1)[H_A^s(x,y;n)+Delta_n^s(x,y)]C(x).
```

If `H_A^s,H_A^u` already belong to the target uniform Hölder class,
`C,C^-1` are uniformly bounded and Hölder on every actual plaque, and the
stable/unstable defects converge uniformly to zero, then

```text
H_B^s=C(y)^(-1)H_A^s C(x),
H_B^u=C(y)^(-1)H_A^u C(x).
```

The right sides inherit a uniform Hölder modulus directly. Thus a separate
equi-Hölder bound on every finite approximant is not an independent debt
under these rows. This corrects the over-strong Round-62 interface without
constructing the missing physical transfer.

A directly checkable stable sufficient row is

```text
d(f^n x,f^n y)<=L lambda^n d(x,y),
||A^n(y)^(-1)|| ||A^n(x)||<=M kappa^n,
||C(z)-C(z')||<=H d(z,z')^alpha,

||Delta_n^s||
 <=M H ||C^-1|| L^alpha
   (kappa lambda^alpha)^n d(x,y)^alpha.
```

Hence `theta=kappa lambda^alpha<1` is sufficient, with the analogous
backward row. In the exact diagonal `SL(2)` logical model the regimes

```text
theta=9/16: defect decays,
theta=1:    defect stays nonzero,
theta=2:    defect diverges
```

show that the scalar majorant is sharp in that model. It is not asserted to
be a necessary condition in an arbitrary cocycle, where cancellations may
occur. The actual all-plaque `C`, its two-sided defect registry and the same
physical representative carrying both class `H` and the selected twisting
remain `NOT_CERTIFIED`.

## Gate 2/4: branch--holonomy square and stable-saturation debt

Let `H_i:U_i->V_i` be actual tagged invertible first-return branches and

```text
D_i f=f o H_i^(-1)
```

the positive `L^1` isometries between the source and landing reference laws.
For future physical source and landing stable holonomies let `P_S,P_L` be
their positive density-transfer isometries. Define

```text
Delta_S=a_v-P_S a_u,
Delta_L=g_v-P_L g_u,
C_uv=D_v P_S-P_L D_u.
```

Since the actual branch marker obeys `g_i=D_i a_i`, direct algebra gives

```text
Delta_L=D_v Delta_S+C_uv(a_u),
||Delta_L||_1<=||Delta_S||_1+||C_uv(a_u)||_1.
```

If the physical square commutes, `H_v o s=ell o H_u`, then `C_uv=0` and

```text
||Delta_L||_1=||Delta_S||_1.
```

Thus an exact product square transports stable-saturation debt; it does not
erase it.

For exactly two plaques of outer weight `1/2` and one positive `L^1`
holonomy isometry `P`, the distance to a common quotient marker is

```text
delta_sat
=1/2 inf_f (||g_u-f||_1+||g_v-Pf||_1)
=1/2||g_v-Pg_u||_1.
```

The finite replay has marker-defect norm `1/6` and `delta_sat=1/12`. The
proof uses the triangle inequality and the competitor `f=g_u`; it does not
misuse conditional expectation as an `L^1` projection.

For an adapted-arclength holonomy with `m<=dh/ds<=M`, retained set
`E_v=h(E_u)`, and transported plaque density

```text
f_v(hx)=c f_u(x)/(dh/ds),
```

one gets

```text
F_v=F_u,
|E_v|>=m theta_u L_u,
R_v<=R_u M/m,
z_v<=F_u R_u M/(m^2 theta_u L_u).
```

One factor `m` pays span contraction and the other the density-ratio
conversion. A two-sided measure holonomy Jacobian alone does not determine
the adapted-arclength bounds `m,M`. Moreover, exact square commutation and
`delta_sat=0` still admit independent short-plaque and fragmentation models
with `z>C_p`. Therefore the actual product/holonomy/zero-defect rows do not
replace the quantitative `F R<C_p theta L` debt.

No physical product rectangles, stable projection, zero actual marker
defect, strict quantitative budget or physical strong assembly is
constructed. The landing join remains `1/7` with fields `1,4,7` partial;
Gate 2's immutable official score remains `0/17`.

## Gate 3: incidence-reduced faces and the sixth clock debt

For two oriented moving cells sharing a face `e`, the exact Reynolds face
current is

```text
B_e=v_e[(Y_i)_*(rho_i trace_e)-(Y_j)_*(rho_j trace_e)].
```

It cancels before absolute values only when source density trace, signed face
velocity, physical landing trace and every retained tag agree under the same
exact quotient. Half-open ownership alone does not cancel a current.

The actual stopped cells are

```text
{Dbar=d} intersect {physical word w}
             intersect {maximal connected component c}.
```

Therefore proof-only CAD boxes and dyadic root-certification boxes do not
create faces in the actual chain. Duplicate source-chart and Euclidean-lift
seams cancel exactly; internal chart/homogeneity cuts cancel only where the
pinned quotient traces agree. Genuinely physical collision, occurrence,
`C24`, owner, grazing and cemetery faces remain.

At the base parameter and on one immutable regular `R_n` ID, the five
regular physical face grammars map to the pinned Round-44 F13 Borel
two-trace payload. Consequently

```text
c_F13,n<=(3816937/7800000)c_X,n
       <(1/2)c_X,n=(25/302)c_D1,n,
```

and the existing same-ID `L^(6/5)` moment and block exponent `1/6` pay this
incidence-reduced **base regular Borel** face ledger. They do not pay a
finite-parameter trace derivative or strong pullback.

The stopped clock creates a distinct current. With

```text
R0=696 Dbar,
M=310 -> Dbar=0,
M=311 -> Dbar=2,
```

the first output-time jump is `0 -> 1392`, and later levels jump by `696`.
Across a moving threshold face,

```text
B_clock
=v_e rho_e[(Y_d)_*delta_e-(Y_d')_*delta_e].
```

The immutable time tags generally differ, so this is not one of the five
F13 grammars. The exact reduced face debt is

```text
E_stop^red=E_regular,physical+E_clock+E_cemetery.
```

The moving clock/cemetery sums, a common finite-parameter stopped atlas, the
anisotropic bulk directional-Piola sum, physical strong `R_s/Q_s` and
`MT_DQ` remain `NOT_CERTIFIED`.

## Gate 5: positive trace drift and fixed-time cost Jordan law

For insertion slice `j`, let `Lambda_j` be a positive debt law on a
standard-Borel carrier `E_j`, let `K_j:E_j->SubProb(E_(j+1))` preserve the
immutable lineage while updating the explicit time coordinate, and let
`V_j>=1`. If

```text
Lambda_(j+1)<=Lambda_j K_j,
K_j V_(j+1)<=kappa V_j,
Lambda_0(V_0)<infinity,
w_Z kappa<1,
```

then positivity gives

```text
sum_j w_Z^j Lambda_j(1)
 <=Lambda_0(V_0)/(1-w_Z kappa)<infinity.
```

A one-point killed kernel proves the threshold sharp. An identity or
charge-preserving shift-covariant kernel keeps a nonzero mass on every slice
and makes the weighted series diverge because `w_Z>1`. The frozen CM2 laws
provide no `K_j,V_j,kappa`; the collision-null face tower shows exactly why
collision survivor decay cannot be imported onto the singular trace law.

On each actual fixed-time owner law, the Round-61 orientation-positive cost
measures now form one typed lattice:

```text
xi_j^f=C_bad,j^f nu_j,
xi_j^r=C_bad,j^r nu_j,
J_j^cost=xi_j^f-xi_j^r,
lambda_j^cost=xi_j^f wedge xi_j^r,

xi_j^f+xi_j^r=|J_j^cost|+2 lambda_j^cost,
|J_j^cost|(X_j)+2lambda_j^cost(X_j)
 <2395081816467609/880000.
```

Restriction to `A_col^c` preserves the identity, so fixed-time cost
variation, common mode and complement total are finite. Absolute continuity
with respect to `nu_j` also upgrades the fixed-time source exact-grazing
subtrace to zero orientation cost. This is an actual **orientation-cost**
Jordan law, not the Round-54 physical signed hit/miss flux law, and it earns
no global Gate-5 field credit.

The remaining complement is compressed to two exact interfaces:

```text
N_cut^rem = countable union of remaining exact-cut coincidences,
chi_j(N_cut^rem)=0 iff every enumerated coincidence has zero chi_j charge;

N_acc = intersection_k {all registered gaps positive,
                        inf_m d_(j,m)<=2^(-k)},
chi_j(N_acc)=lim_k chi_j(A_(j,k)).
```

No same-law zero-set theorem, small-gap tail or pre-regularization cemetery
bound is available. The seven suffix predicates remain Borel `7/7`, with
only `2` universal values true; the five input, `C24`, operator, output and
horizon values and physical `R>=r_K` remain open. Gate-5 maturity remains
`10/18` with zero complete blocks.

## Latest-technology audit

The official-source surface was checked through 2026-07-21, including the
current dispersing-billiard review and transfer-space literature, small-hole
linear response, nonautonomous response, operator-renewal/open-system work,
and current cocycle-holonomy references.

- Current billiard holonomy/anisotropic results do not construct the pinned
  common physical landing quotient, its zero marker defect, retained-span /
  fragmentation budget, and a compatible strong assembly.
- Existing moving/sequential billiard frameworks do not supply the required
  all-depth endpoint-current sum, stopping-clock trace and anisotropic
  moving-domain Piola derivative.
- Open-system/renewal theorems do not identify the singular time-labelled
  owner/coarea trace or provide the positive Lyapunov drift above.
- Canonical class-`H` theory does not materialize the missing all-plaque
  determinant-one transfer for the compact twisting representative.

No external theorem is promoted into a physical CM2 certification.

## Independent audit and acceptance

The append-only independent audit records `PASS`; no source-leaf correction
was required. It independently recomputed every displayed algebraic row,
strict type boundary and executable chain after all three leaf sidecars were
frozen. Two additional cross-red-team reads independently checked the
Gate-2/4 leaf and the Gate-1/3 plus Gate-5 leaves. The aggregate draft was
then checked against all three manifests. No mathematical, typing, status or
counting blocker remained.

The audit keeps the principal carriers and debts distinct:

- endpoint conjugacy removes a redundant proof obligation but does not
  materialize the all-plaque Gate-1 transfer;
- the branch--holonomy square preserves marker debt and does not pay the
  quantitative Gate-4 boundary threshold;
- Gate-3's Round-44 join is only the base regular Borel-TV layer and does not
  absorb the clock-jump, cemetery or bulk-Piola debts;
- Gate-5's orientation-cost Jordan law is not Round-54 signed physical flux,
  and its killed-kernel theorem is conditional rather than an actual trace
  recurrence;
- `D_land`, stopped `Dbar`, endpoint rank `B`, collar clearance `K` and
  insertion time `j` remain distinct marks.

Final four-leaf acceptance matrix:

```text
syntax:                         8/8
dependency / baseline pins:    41/41
integrity / audit:              4/4
semantic replay:                4/4
producer reemit:                4/4 byte-identical
hostile semantic:               853/853 rejected
strict JSON:                    27/27 rejected
hostile + strict JSON:          880/880 rejected
SHA sidecar rows:               16/16
default cert/verifier:          8/8 exit 2
recursive aggregate ledger:     15/15
Round-63 stale/temp files:       0 after cleanup
```

The source leaves contribute `41` dependency/baseline pin rows and `748`
hostile/JSON rejections; the audit leaf contributes `132` further hostile /
strict-JSON rejections. All four leaves agree on the strict state below.

## Strict status

```text
Gate 1 / Gate 2 / Gate 3 / Gate 4 / Gate 5: NOT_CERTIFIED
Gate 2 immutable fields:                     0/17
Gate 4 seven-field landing join:              1/7; fields 1,4,7 partial
Gate 5 maturity / complete blocks:           10/18 / 0
complete composite gates:                    0/5
CM2:                                         NO-GO_FOR_CLAIM
```

## Shortest remaining routes

1. **Gate 1:** materialize one actual all-plaque `SL(2)` transfer and prove
   either the two strict `kappa lambda^alpha<1` budgets or direct uniform
   stable/unstable renormalized-defect decay; then join the immutable token.
   No separate finite-approximant Hölder row is needed under these inputs.
2. **Gate 2 / Gate 4:** construct the actual invariant product rectangles,
   source/landing stable holonomies and commuting square; prove zero marker
   defect and materialize `F,R,theta,L,m,M` with the strict quantitative
   budget; then install physical strong restriction/assembly.
3. **Gate 3:** construct a common finite-parameter incidence-reduced stopped
   atlas, sum the clock-jump and cemetery traces, and prove the anisotropic
   bulk Piola bound; only then install strong `R_s/Q_s` and `MT_DQ`.
4. **Gate 5:** construct the actual time-labelled positive trace kernels and
   one Lyapunov drift with `w_Z kappa<1`; prove all remaining exact-cut and
   small-gap rows, the five suffix values and `R>=r_K`; align or lawfully
   bridge the orientation-cost and physical flux Jordan laws, and separately
   pay complement, common-mode and pre-regularization cemetery.

Until those same-law, all-time and strong-space joins are paid, CM2 remains
a strict no-go claim.
