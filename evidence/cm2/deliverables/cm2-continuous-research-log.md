# CM2 continuous research log

## 2026-07-16 10:30 CST — five-gate direct assault, eleventh pass

### Discipline and verdict

- Frozen v51/v52 and all first-ten-round artifacts were preserved.  Six new
  certificate stacks cover Gates 1, 2, 3, two distinct Gate-4 fronts and
  Gate 5.
- Unconditional CM2 remains `NO-GO FOR CLAIM`; the strict composite gate
  count remains **0/5 fully closed**.
- Consolidated boundary: `cm2-eleventh-direct-assault-2026-07-16.md`.

### Gate 1 and Gate 2

- The QNL `325/144` resonance has an explicit local two-axis logarithmic
  repair.  With `k=-325/(144 log(mu))`, the stable and unstable gauges
  `I+k y^2 log|y|E_21` and `I+k x^2 log|x|E_12` cancel the critical quadratic
  terms exactly.  They are `C^{1,alpha}` for every `alpha<1`, not `C2`.
- Binding the frozen two-sided orbit asymptotic gives summable transformed
  envelopes `O(n mu^n)`, `O(n mu^(2n))` and `O(mu^(4n))` on both selected
  local tails.  Global coding, all-pairs holonomies, class `H` and the
  projective conclusion remain open.
- Gate 2 now has a full-measure countable 2D first-return path schema over the
  441,280-key collision alphabet.  Exact invertibility proves that every
  deterministic key refinement keeps the reverse conditional Dirac and its
  diagonal pair-energy coefficient equal to one.  It cannot replace the
  stable quotient.
- The Borel path mass is one modulo zero, while stable-quotient and endpoint
  PPE masses remain undefined before their registries exist.  The two raw
  strips still cover `<0.097`; all 17 Gate-2 physical fields remain false.

### Gate 3 and Gate 4

- Pulling moving cumulative-mass endpoints back to `(tau,u)` places every
  finite dyadic stopped cutoff on one common finite-`s` carrier.  At level
  `L`, there are `64*2^L` components and the exact graph-current collar is
  `min(16128/5,(32256/5)(2^L+1)rho)`.
- A genuine `s=0` next-collision replay gives 185,068 explicit immutable
  boxes covering `128399/131072=97.9606628%` of the normalized trimmed rows.
  Separately, all 4,704 tangency discriminants are certified non-identically
  zero, so the actual second singular set is discrete/countable and has zero
  row/coarea mass.  This yields a full-measure countable depth-two atlas.
- Finite-`s` continuation of that future singular atlas, uniform dynamic
  collars, strong invariance, operator DQ, `MT_DQ` and physical
  `FACE_2CUT/FACE_TIME` remain open.
- A native physical mass-coordinate prefix antichain now covers full mass,
  but leafwise inverse-mass normalization obeys the exact general obstruction
  `E[p_A^-1]=sum_a 1=infinity`.  The concrete depth charge is `3*2^K` per
  level.  One open cut may have `Z=infinity`, and repeated quarter cuts give
  `Z_h=4^h`.
- Numerical fixed-table Growth leaves are now explicit:
  `Lambda>180337/144000`, inverse contraction `<144000/180337`,
  `C_metric=141/4`, `C_cone=30`, `c_hat=20/3807`, `L_0<68`, and
  `C_s=1296803367/80000`.  On one true continuity branch the complete
  homogeneity cut sum is `<900337/901685<1`.
- Global true-singularity weighted multiplicity, `delta_n`/complexity,
  all-branch distortion and hence numerical Growth/recovery and propagated
  `q` remain open.

### Gate 5, literature and verification

- The return-word envelope contains 441,280 candidate keys, 3,286,976
  symbolic prefix/suffix level pairs and 3,728,256 boundary splits.  Empty
  fibres remain; the physical homogeneity registry and all 18 quantitative
  operator fields are absent.  The three CM2 lifts, full Kac and operator
  phase remain open.
- The official arXiv audit through 2026-07-16 found no theorem supplying the
  global class-`H`, full-mass quotient, future-singularity DQ,
  characteristic-cut recovery or wordwise strong-operator interfaces.
- All six new stacks compile and replay, reject 9/9/9/9/8/9 mutation classes
  respectively, and fail closed live with exit 2.  Their SHA chains and the
  unchanged frozen-v52 chain pass.

## 2026-07-16 09:07 CST — five-gate direct assault, tenth pass

### Discipline and verdict

- Frozen v51/v52 and all first-nine-round artifacts were preserved; three
  disjoint Gate-3/4/5 fronts were developed in parallel and independently
  replayed before the aggregate freeze.
- Unconditional CM2 remains `NO-GO FOR CLAIM`; the strict composite gate
  count remains **0/5 fully closed**.
- Consolidated boundary: `cm2-tenth-direct-assault-2026-07-16.md`.

### Uniform finite-parameter recovery

- A 384-bit interval replay covers all 128 moving endpoint incidences with a
  common `1/48` collar: 20,104 endpoint leaves of maximum parameter depth 5.
  The 64 moving cores have 110,336 leaves of maximum angular depth 6 and all
  five relevant scales are strictly larger than `2^-20`.
- Uniformly on `|s|<=1/400`, the endpoint rank tail is
  `(4839232/3125)4^-b` for `b>=20`, and the one-collision geometric costs are
  bounded by `204*2^B_s` in both orientations.
- The common mesh gives the explicit initial constant
  `C_mesh=69986663973833932800`.  Replaying 35,024 horizon boxes with squared
  penetration slack `>2^-19` places the path in one explicit
  Stenlund--Young--Zhang class with horizon `(3,1/512)`.
- SYZ `arXiv:1210.0011v4`, Lemmas 12 and 16, give uniform fixed-`s`, one-time
  recovery:
  `R_fw+R_rev<=2A0+2A1*K` and
  `E[2^K exp(gamma(R_fw+R_rev))]<infinity` for
  `0<gamma<log(2)/(2A1)`.  The theorem constants remain nonnumeric; this is
  neither native stopping nor hereditary repeated-cut recovery.

### Gate 3 and Gate 5 rebinds

- The limiting `s=0` face--product centered cluster has typed norm at most
  `48384/5` before `Z_N^-1`, exact zero marginals, and auxiliary charged
  depth mass `a_K=(3/4)2^-K`.  Its recovery and clock-only two-time array are
  now uniform on `|s|<=1/400`.
- The moving dyadic endpoints are not registered in the iterated common DQ
  atlas.  Branch-record `MT_DQ`, physical `FACE_2CUT/FACE_TIME`, and the
  finite-`s` physical current remain open.
- Every actual physical prefix/suffix is a TV/L-infinity contraction on the
  Borel pair; the Kac height is at most 9.  The singular output envelopes are
  `16128/5`, `32256/5`, and `290304/5` for the event current, two Kac
  coordinates, and worst phase lift respectively.
- Those weak Borel constants do not control return-wide standard-family,
  flux-face or dynamic-test norms; the complete Kac/phase transfer remains
  open.

### Gates 1--2, literature and verification

- A fresh bypass audit found no theorem applicable to the exact QNL
  resonance or the missing full-mass singular quotient.  Gate 2 still has
  more than `90.3%` unregistered physical mass.  `arXiv:2607.11421` supplies
  a cautionary atomic-stationary-measure example rather than a bridge.
- All three new certificate stacks compile and replay fully; Gate 3 rejects
  eight mutation classes, Gate 4 six, and Gate 5 five.  Live modes all exit
  2.  Seven current Gate-1/2 obstruction verifiers and their SHA chains pass
  and remain fail closed.  Frozen v52 SHA verification passes unchanged.

## 2026-07-16 01:20 CST — five-gate direct assault, ninth pass

### Discipline and verdict

- Work was performed directly; no CM2 cron, detached scheduler or subagent
  was used.
- Frozen v51/v52 and all first-eight-round certificate/report artifacts were
  read only.
- Unconditional CM2 remains `NO-GO FOR CLAIM`; the strict composite gate
  count remains **0/5 fully closed**.
- Consolidated boundary: `cm2-ninth-direct-assault-2026-07-16.md`.

### Bidirectional costs

- Certified all 128 simple endpoint germs and the exact rank tail
  `(9158592/6875)4^-b`, `b>=14`.  Raw rank moments exist for exponent `<2`;
  the first-order charged window is `<1`.
- Certified both exact arclength densities, 48 density-zero endpoints per
  orientation, and raw boundary bounds `Z_rev<=163855/256` and
  `Z_fw<=102409375/72674`.
- Corrected and matrix-audited the invariant slope recurrence to
  `V_1=kappa_1+cp_1/(tau+cp_0/(V_0+kappa_0))`.  The fixed cone
  `25/9<V<4108425/145348<29`, carrier `C2<4949`, and log-density costs
  `<27*2^B`, `<52*2^B` close the raw one-collision geometric charge.

### Controlled stopped recovery

- Added a query-independent product depth law `w_K=(3/4)4^-K` with exact
  tail `4^-K`, `E[2^K]=3/2`, and shared forward/reverse `(e,K,j)` records.
- A density-regular `3/2`-power mesh gives uniform `1/3` log-Hölder constant
  52, carrier `C2<4949`, `Z<=C_mesh 2^K`, and row mass
  `>169/2147483648000` for every one-time atom in either orientation.
- The closed-map Growth Lemma yields
  `R_fw+R_rev<=A0+A1*K` and therefore
  `E[2^K exp(gamma(R_fw+R_rev))]<infinity` for
  `0<gamma<log(2)/A1`.  This certifies controlled one-time stopped-parent
  recovery at `s=0`, not finite-`s` or hereditary repeated-indicator
  recovery.

### MT_DQ and strict boundary

- Depth-one norm DQ now permits strongly moving `C^{1,alpha}` tests.  The
  noncommutative fixed-time telescope was formally replayed for depths 1--64.
- Full branch-record `MT_DQ`, uniform finite-`s` moving-face recovery,
  complete numeric propagated `C_fw,C_rev,q`, physical prefix/suffix sums,
  and all three CM2 norm lifts remain open.  Gates 1--2 retain their prior
  same-carrier holonomy and full-mass physical PPE obstructions.
- The newest relevant review, Demers--Liverani `arXiv:2606.10155v1`, leaves
  general loss of memory after characteristic-function restrictions open in
  Problem 8.7; it cannot replace the missing hereditary interface.

### Verification

- Eight new certificate/verifier pairs compile, replay fully, pass integrity
  and mutation tests, and fail closed live with exit 2.  Their JSON
  self-hashes were independently checked.  The frozen eighth baseline was
  audited before the mutable log advanced; the ninth SHA chain binds the new
  state.

## 2026-07-15 23:55 CST — five-gate direct assault, eighth pass

### Discipline and verdict

- Work was performed directly; no CM2 cron, detached scheduler or subagent
  was used.
- Frozen v51/v52 and prior round artifacts were not edited.
- Unconditional CM2 remains `NO-GO FOR CLAIM`; the strict composite gate
  count remains **0/5 fully closed**.
- Consolidated boundary: `cm2-eighth-direct-assault-2026-07-15.md`.

### Mandatory current-scale correction

- The seventh current used `p=sin(phi)` but multiplied by `cos(phi)` once
  after the identity `cos(phi)dphi=dp` had already absorbed that factor.
- The superseded law `R_source*cp^2*abs(u_y)/ell_T dtheta` is replaced by
  the exact collision-flux law
  `R_source*cp*abs(u_y)/ell_T dtheta` on all 64 maximal rows.
- The corrected row digest is
  `5c03da290697ac25b814848c5aee50b22387866f9d72303a60649466cad896bd`.
  The TV upper bound `16128/5`, 32 exact `Jx` pairs and scalar mass zero are
  unchanged.

### Depth-one DQ and controlled stopping

- The finite 64-row atlas, zero simultaneous-first multiplicity, exact seam
  quotient and the uniform angular submersion `>4/125` close the complete
  one-step fixed-gauge quotient for `C^1` sources against
  `C^{1,alpha}` static tests.  The limiting defect is the finite smooth-core
  derivative plus the corrected hit-minus-miss current; regular radical
  collars have `O(rho^1/2)` mass.
- Each corrected row receives its cumulative-mass coordinate `u_e`.  Dyadic
  depth-`K` atoms have exact mass `2^-K m_e(row)`, normalization cost at most
  `2^K`, and form a countable nested Boolean algebra.  Trimming level `L`
  leaves exact endpoint cemetery mass `2^(1-L)`.  Fat-Cantor restrictions are
  excluded inside the declared policy.
- Exact slope identities prove every source row stable-oriented and every
  miss image unstable-oriented.  Source slopes lie in
  `(-65/4,-25/9)` and miss-image slopes in
  `(25/9,4108425/145348)`, hence all absolute raw slopes are `<29` and one
  numeric slope-only subcharge `q_e^slope=29m_e` is valid per occurrence.
- The complete structural Kac layer is rebound to the corrected laws: 64
  corrected `m_e`, 64 symbolic `q_e`, 64 slope subcharges, 64 controlled
  mass coordinates and 128 singular Kac coordinates sharing 64
  `(+1,-1)` marks.

### Strict remaining boundary

- The full iterated three-space `MT_DQ`, homogeneity-weighted numeric
  `C_fw,C_rev`, the moment
  `E_q[2^K exp(gamma(R_fw+R_rev))]`, physical stopped recovery, prefix/suffix
  distortion sums and all three CM2 norm lifts remain open.
- A fresh arXiv audit found no theorem newer than or stronger for this target
  than `arXiv:2604.19671v2`; its small-hole standard-family geometry does not
  supply the missing moving-scatterer recovery or norm interfaces.

### Verification

- Four new certificate/verifier pairs, their mutation self-tests, full
  replays and live fail-close checks passed.  The subordinate SHA chain and
  frozen v52 audit also passed before the eighth-round freeze.

## 2026-07-15 23:05 CST — five-gate direct assault, sixth pass

### Discipline and verdict

- Work was performed directly; no CM2 cron or detached scheduler was used.
- Frozen v51/v52 files were not edited.
- Unconditional CM2 remains `NO-GO FOR CLAIM`; the strict composite gate
  count remains **0/5 fully closed**.
- Consolidated boundary: `cm2-sixth-direct-assault-2026-07-15.md`.

### Gate 3: complete active stratification

- Exact source-target separation gives squared `cp=0` angular and dominant-
  chart derivative lower bounds `36337/160000` and `36337/320000`.
- All 23,376 source-grazing boxes are stratified: 18,980 have a uniformly
  occluded outgoing side and 4,396 have a physical outgoing half-row carrying
  32 labels in eight symmetry four-orbits.  The `cp=0` boundary has zero
  two-dimensional measure and zero signed parameter-coarea coefficient.
- All 816 original and 768 newly generated `u_y=0` polarity boxes are split
  into their two strict sign strata and analytic zero set.
- Of 82,048 target-target descriptors, the existing resolver leaves no
  physical row among the 320 former unresolved descriptors.  Exactly 48
  strict curves meet active sheets: 16 earlier visibility boundaries and 32
  later miss switches.
- The depth-thirteen residual has 72,324 boxes.  Exactly 59,028 meet no
  physical transition curve and have one strict typed state; 13,296 meet one
  curve and zero meet more than one.  Merging grid duplicates gives 12,288
  strict strips, exactly `48 x 256`, with strict distinct side states.
- Consequently the remaining two-dimensional unresolved active parameter
  area is exactly zero.  The 48 curves are retained as future DQ faces rather
  than discarded.
- A half-open diagonal ownership rule assigns all eight chart seams to `E`
  or `W`.  On the 11,812 rectangular bulk boxes, 96 seam overlap pairs reduce
  the connected-component count from 64 to 52.

### Other gates and strict boundary

- Gate 1 remains blocked by the exact natural-holonomy resonance; a current
  arXiv audit found no applicable non-fiber-bunched bypass.
- Gate 2 retains the `>8.29e65` saturation-scale deficit and `>90.3%`
  unregistered collision-SRB complement.
- Gates 4--5 gain a complete active row-cut stratification and unique chart
  owner, but maximal connected rows, coarea/current assembly, numeric
  forward/reverse costs, stopped recovery, four Kac currents and CM2 norm
  lifts remain open.

### Verification

- The source-grazing/polarity, first/miss and chart-seam certificates exit
  zero; integrity and mutation self-tests pass.
- Live completion checks remain fail-closed with expected exit two.
- Full replay, consolidated SHA verification and frozen v52 verification are
  run after the reports and manifests are pinned.

## 2026-07-15 18:30 CST — five-gate direct assault, fifth pass

### Discipline and verdict

- Work was performed directly; no CM2 cron or detached scheduler was used.
- Frozen v51/v52 files were not edited.
- Unconditional CM2 remains `NO-GO FOR CLAIM`; under strict composite
  theorem typing the main-gate count remains **0/5 fully closed**.
- Consolidated boundary: `cm2-fifth-direct-assault-2026-07-15.md`.

### Gate 1

- The exact QNL eigencoordinate jet has no quadratic terms and satisfies
  `F2_xyy/mu=-325/72`.
- For every sufficiently local nonzero stable point,
  `lim(H_n^-1 H_(n+1))_21=(325/144)c(z)^2>0`; an independent audit verified
  the cocycle order, coefficient, stable asymptotic, even-time passage and
  finite-prefix passage.  The frozen natural QNL canonical stable holonomy
  therefore does not converge.
- Park--Piraino on the two frozen natural codings is strict `NO-GO`, and the
  faithful frozen representative fails the Butler--Park class-`H` canonical
  limit.  Arbitrary Hölder cohomology, other non-FB theorems and genuinely
  alternative codings remain unrefuted but uncertified.
- The actual common vertex/full-cross and 96-collision finite-shadow wedges
  remain certified; the finite shadow is not a QNL canonical holonomy loop.

### Gate 2

- The actual single stable leaf and two local physical crossings remain
  certified, but the present 96-word tube halfheight is `10^-80` while the
  stable endpoint separation exceeds `8.29e-15`, a scale ratio `>8.29e65`.
- Exact collision-SRB mass accounting gives QNL raw fraction `27/280`; the
  loop rectangle contributes `<4.54e-119`.  The two current raw strips cover
  `<0.097`, leaving an unregistered physical complement `>0.903`.
- Stable saturation, a common physical holonomy, the full-mass countable
  quotient, actual weights/tail PPE, stopped antichain, endpoints and the
  amplitude registry remain open.

### Gate 3

- Exact identities `cp^2+p^2=1` and
  `sign(eta*epsilon*cp*uy/ell)=sign(eta*epsilon*uy)` remove dependency-only
  endpoint and polarity collars.
- Total unresolved area drops from `35721/102400` to `5263/51200`, removing
  the exact fraction `25195/35721` (`70.53%`) of the old uncertainty.
- The refined atlas has 11,812 physical boxes, 64 components and 16
  `Jx/Jy` four-orbits.  All remaining endpoint collars are genuine `cp=0`
  source-grazing collars; 16,304 boxes certify unique transverse graphs.
- A complete refined replay constructs 5,906 exact `Jx` box pairs and 32
  component-label pairs.  Positive coarea laws push forward exactly, giving
  scalar `mu(dot r)=0` on all 64 certified refined-bulk components.
- Arbitrary-test DQ, outgoing labels on every grazing half-box, remaining
  first/miss collars, seam quotient and maximal global rows remain open.

### Gates 4--5

- The refined 64-component bulk has one structural occurrence, one common
  positive law and one pre-recovery `q=max(C_fw,C_rev,2)m` expression per
  component, not one charge per singular coordinate.
- The two bounded-Borel singular Kac coordinates per occurrence share the
  fixed mark `(+1,-1)` and the same coefficient law.  This freezes 128
  coordinates but only 64 pre-recovery charges.
- Scalar roof cancellation does not imply an arbitrary-test current norm;
  the exact family `n(delta_a-delta_b)` remains a counterexample.
- Numeric forward/reverse recovery costs, global stopped recovery, physical
  four-current typing, distortion/cut/test constants and all CM2 norm lifts
  remain open.

### Verification

- Gate-1 resonance, Gate-2 scale/mass, Gate-3 refinement and refined `Jx`
  coarea certificates independently exit zero.
- Live completion checks remain fail-closed with expected exit two; verifier
  self-tests reject promoted gate flags.
- New SHA-256 manifests and the frozen v52 manifest are checked after this
  log entry and the consolidated report are pinned; no residual CM2 process
  is retained.

## 2026-07-15 16:05 CST — five-gate direct assault, fourth pass

### Discipline and verdict

- Work was performed directly; no CM2 cron or detached scheduler was used.
- Frozen v51/v52 files were not edited.
- Unconditional CM2 remains `NO-GO FOR CLAIM`; under strict theorem typing
  the composite main-gate count remains **0/5 fully closed**.
- Consolidated boundary: `cm2-fourth-direct-assault-2026-07-15.md`.

### Gate 1

- The actual `W^u_loc(QNL)`/`W^s_loc(connector)` Krawczyk intersection,
  24-collision four-face full-cross, reversed inherited strip, 96-collision
  finite shadow loop and four same-orbit wedges remain certified.
- Strict promotion to the canonical QNL-fibre holonomy loop remains open:
  the natural derivative cocycle fails the Park--Piraino fiber-bunching
  bound and the finite shadow orbit is a different periodic orbit.

### Gate 2

- The short-QNL strip full-crosses a common Perron rectangle containing both
  the QNL point and the 96-word point.
- A 2400-bit interval-Newton/Taylor continuation takes the actual local
  unstable plaque through two 96-word returns.  Reversibility certifies the
  actual stable-plaque crossing of `v=0`, with uncertainty `<8.53e-172`.
- A positive-width strip `(r_u,r_v)=(8e-68,1e-80)` around the crossing keeps
  all 96 first hits, crosses both unstable faces, enters in the stable
  direction and satisfies uniform cones.  Thus two disjoint local physical
  full-cross branches in one common rectangle are certified.
- Stable saturation, common invariant holonomy, two onto quotient branches,
  full-mass countable return weights/tails, physical stopped PPE, endpoints
  and the amplitude registry remain open.

### Gate 3

- A mandatory forward-time audit retracted the earlier sixteen alleged
  physical transition vertices: every partner contact has `ell_other<0`.
- An independent closed-cover resolver classifies all 320 original suspect
  descriptors as physically empty, with `physical=0` and `unresolved=0`.
  Exact witnesses split as 164 third-target-before, 60 target-behind-source,
  52 partner-behind-source, 32 no-source-intersection and 12 later-not-next.
- One compact four-row `Jx/Jy` immutable physical subrow orbit is certified
  over the full parameter window, with constant miss traces and polarity
  `[+1,-1,+1,-1]`.  Only symmetry-invariant scalar cancellation is claimed.
- Maximal connected components over all 288 sheets, the duplicate quotient,
  global physical event rows, DQ and scalar matching remain open.

### Gates 4--5

- The Gate-4 raw sheet universe is exactly the corrected Gate-3 universe:
  288 sheets, of which 128 are active and 160 inactive.  The completed-row
  same-occurrence/common-`m`/single-charge theorem and exact four-term Borel
  Kac algebra remain certified, but global physical Banach typing and
  stopped recovery remain open.
- Actual physical spanning graphs on `M*` and standard `N` are strongly
  connected and contain weight-one and weight-two cycles; their component
  weighted cycle gcd is exactly one.
- The preferred direct-standard-`N` route does not use a separate phase
  tower.  Component gcd one does not imply operator `D(z)` invertibility.
  The complete return-word/operator ledger, return-boundary DQ, singular
  currents, physical source/test constants and two CM2 norm lifts remain
  open.

### Verification

- Positive Gate-2, corrected Gate-3 and Gate-4/5 integration certificates
  independently exit zero.
- Live physical completion verifiers remain fail-closed with expected exit
  two; verifier self-tests pass.
- New SHA-256 manifests and the frozen v51/v52 manifests are checked after
  this log entry is pinned; no residual CM2 process remains.

## 2026-07-15 12:40 CST — five-gate direct assault, third pass

### Discipline and verdict

- Work was performed directly; no CM2 cron or detached scheduler was used.
- Frozen v51/v52 files were not edited and their manifest remained entirely
  `OK`.
- Unconditional CM2 remains `NO-GO FOR CLAIM`; physical main-gate count is
  still **0/5 fully closed**.  The consolidated boundary is recorded in
  `cm2-second-direct-assault-2026-07-15.md`.

### Gate 1

- A 300-bit graph transform certifies the actual local QNL unstable graph on
  `|x|<=2e-9`, with `|h_A|<=x^2` and `|h_A'|<=1e-4`.
- A 400-bit recentered collision-jet calculation plus exact reversal
  certifies the actual connector stable graph on `|y|<=3e-12`, with
  `|h_B|<=0.01y^2` and `|h_B'|<=1e-8`.
- The old shooting-point corrections are at most `1.95e-18` and
  `4.7792e-26`; both complete physical graph-tube replays pass.
- A correlation-preserving two-graph Krawczyk inclusion is still missing, so
  common vertex, full-cross and transported twisting remain open.

### Gate 2

- A stationary-Frostman plus pointwise Holder-mixing hypothesis now yields a
  uniform finite-depth stopped-parent small-ball bound by cutoff smoothing.
- Exact countability proves that a full-branch reverse kernel from a point is
  at maximal TV distance from a non-atomic SRB density at every finite time;
  the CDKM TV-uniform-ergodicity/Doeblin shortcut is impossible.
- A compact continuous adaptive projective-law example preserves a two-atom
  stationary law although each component law has the GKM moment and
  no-deterministic-images properties.  GKM 2.6/2.8 therefore cannot be
  Markovized under their original hypotheses alone.
- The actual full-mass quotient, joint place-dependent projective Frostman,
  normalized amplitude and same-carrier endpoint identity remain open.

### Gate 3

- All eight source cells against 162 conservative lifts give an executable
  1296-row table: 848 pairs are strictly excluded and 448 retained.
- A 192-bit adaptive atlas completely covers a rational superset of `G:E`
  uniformly on `|s|<=1/400`: 16,580 leaves, with 5,276 unique first-hit
  leaves, 38 first-tangency graphs and 11,266 fail-closed multi-candidate
  leaves.  The exact resolved volume fraction is `1902927/2899968`.
- Of 1,596 pair rows, 1,137 are separated; of 29,260 triple rows, 27,522 are
  separated.  The remaining collars, seven source cells, global DQ and
  scalar matching are still open.

### Gates 4--5

- A 200-bit certificate constructs one positive-width grouped path
  `G[0,0]--tangent W[0,0]--G[1,1]`, exhausts 95 competitors and gives actual
  stable/unstable endpoint carriers.
- Path reversal yields two restrictionwise nonadditive views of the same
  incidence with a common coarea measure and one pre-recovery charge
  `q_0=max(C_-,C_+,2)m`.
- On the same row, the singular `dot(S)` and `dot(r)*muhat(h)` Kac
  coordinates share one vector mark `(+1,-1)`, the same `m`, and the same
  once-charged `q_0`.
- A fat-Cantor restriction obstruction shows why arbitrary stopped Borel
  restrictions still require controlled cylinder algebra or Gate-2 PPE.
  Global eventwise reversal, recovery, four-term Kac typing, roof-current
  cancellation and both CM2 norm lifts remain open.

### Verification

- New positive certificates and self-tests exit zero; deliberately
  incomplete live Gate-2/Gate-3 manifests exit two.
- Every new gate-specific SHA-256 manifest and the frozen v52 manifest checks
  `OK`; no residual CM2 certificate process remains.

## 2026-07-15 11:40 CST — five-gate direct assault, second pass

### Discipline and verdict

- Work was performed directly in the interactive session; no CM2 cron or
  detached scheduler was created or used.
- Frozen v51/v52 files were not edited.  The frozen manifest was rechecked
  after all work and remained entirely `OK`.
- Physical main-gate count remains **0/5 fully closed** and unconditional CM2
  remains `NO-GO FOR CLAIM`.  The consolidated evidence boundary is
  `cm2-five-gate-direct-assault-2026-07-15.md`.

### Gate 1

- Lima--Obata--Poletti arXiv:2405.04676v2, Lemma 5.1, applied in both
  directions closes the non-effective clean heteroclinic/common-basic-set
  topology.
- A 400-bit `10+14`-collision calculation certifies a transverse finite-word
  match of the two affine eigentangents, with determinant
  `6.815572776e14` and strict first-hit/clearance margins.
- Independent audit caught the decisive typing boundary: affine eigentangents
  are not validated `W^u/W^s` graphs.  The artifact was renamed
  `cm2_gate1_tangent_line_matching_cert.py`; it explicitly prints
  heteroclinic/full-cross/transported-twisting as `NOT CERTIFIED`.  The failed
  micro-cross attempt was discarded.

### Gate 2

- On any actual one-state full-branch quotient, the SRB kernel is forced to be
  `p_a=rho(h_a)|h_a'|/rho`; word weights telescope and a fixed clean word has
  a pointwise arbitrary-history hazard.
- A place-dependent non-i.i.d. separated binary tree now has an every-stopped-
  parent finite-time Frostman theorem.  The endpoint Moebius/wedge identity is
  exact.
- The pilot still lacks a full-mass physical quotient/countable-kernel law,
  normalized amplitude registry, and same-carrier endpoint/cocycle typing.
  Exact counterexamples reject marginal-to-parent Frostman and raw-to-
  normalized-amplitude substitutions.

### Gates 3--4

- A 160-bit Arb cover with 35,024 leaves proves `tau_max<3` and the complete
  conservative target-lift universe `G,W:[-4,4]^2` (162 lifts).  Eight exact
  dominant-coordinate source cells are recorded.
- Gate 3 still has no chart/target root table, first-hit partition,
  pair/triple incidence inventory, global DQ, or scalar-current matching.
- An exact gray-to-white tangency has
  `partial_s H=0.2665659...` and
  `partial_phi H=4 sqrt(610)/625>0`.  Reversibility gives the full-operator
  identity `dot P=-P R dot P R P`, but not an eventwise same-id identity.
  The true reversed source is a white grazing boundary point, so SRB
  invariance does not construct the reverse proper family or single-charge
  `q=max(C_fw,C_rev)m`.

### Gate 5 and literature

- The horizon bound makes `SF1` quantitative: common-refinement return depth
  to standard `N` is at most `9`.  Kac algebra, target spectral/Wiener, and
  the regular-density norm-equivalence sublayer are closed.
- Return-boundary DQ, four typed Kac currents, singular curve/face-source
  embedding, physical tests, and the two CM2 norm intertwiners remain open.
- Canestrari arXiv:2411.16628v3 and 2604.19671v2 and Demers--Liverani
  arXiv:2606.10155v1 were checked.  They supply abstract or fixed-map
  templates, not the missing moving-scatterer event/source conjunction.

## 2026-07-15 direct session — five-gate theorem/literature assault

### Execution discipline

- This was a direct local research session. No CM2 cron or detached scheduler
  was created or used.
- The frozen v52 manifest was rechecked with `sha256sum -c`; every v51/v52
  TeX/PDF, ledger, blueprint and frozen certificate reports `OK`.
- The full proof audit and exact source references are recorded in
  `cm2-direct-assault-2026-07-15.md`.

### Corrected two-stage atlas falsification

- Code audit found that the draft two-stage atlas and its rectangular
  stage-two dependency omitted the denominator in the interval extension of
  the `atan2` derivative. It also found non-fail-closed selection of
  overlapping Arb balls in several clearance/minimum diagnostics. All runs
  predating the fix were stopped and discarded.
- The final versions use the full positive `atan2` denominator and Arb
  `min/max` throughout. Their hashes are frozen in
  `cm2-two-stage-atlas-manifest-2026-07-15.sha256`.
- Four post-fix 400-bit Arb slices, `[0,16384)`, `[16384,32768)`,
  `[32768,49152)`, and `[49152,65536)`, all returned exit code zero. They
  exactly cover the 65536 closed tile boxes on
  `|b-h/2|<=2e-11`, `a=g(b)-6e-20`.
- Uniformly over the full window, the complete 28-collision / 16-return word
  is physical and

  ```text
  -3.037598028e-9 < B_2(b) < -3.037597839e-9 < 0.
  ```

  The physical minima are `flight>0.1871087142206`,
  `discriminant>0.01026437292622`, `incidence>0.6332077600841`, and
  `clearance>0.2228387554787`.
- This strictly falsifies a stable root on this translated quartic branch and
  doubled window. It does not exclude more distant/refitted graphs or another
  connector/QNL branch; the monotone stable inverse and `COMMON_MAGNET` remain
  `NOT_CERTIFIED`.

### New positive reductions

1. **Topological common homoclinic class.** Lima--Obata--Poletti
   arXiv:2405.04676v2, Lemma 5.1, puts any two regular hyperbolic points of a
   finite-horizon dispersing billiard in one clean homoclinic class through a
   common positive-measure magnet. Applied to the physical QNL and connector
   orbits, this removes the existential topological-connector question on the
   standard collision section. It does not transport the certified point
   matrices to one common vertex, so physical common-vertex twisting remains
   open.
2. **`SF1` common refinement.** Stenlund's
   `M*=G sqcup C_clean sqcup W` contains both
   `M=G sqcup C_clean` and `N=G sqcup W`. Return to `M` has depth at most two.
   Uniform finite horizon bounds the number of clean grid-wall crossings
   before the next solid collision, for instance by
   `2 ceil(tau_max)+3`; hence return to `N` also has a uniformly bounded
   finite Borel word registry. The measurable/bounded-coding content of
   `SF1` is proved.
3. **`SF3` Kac algebra.** All fixed-gauge collision-flux laws are independent
   of the white-center displacement. Differentiating the exact Kac quotient
   determines the formerly free scalar
   `partial_s muhat_s(h_s)` and gives the four-term centered current. The
   integral of `dot r` is zero because the mean roof is constant, although
   the boundary current `dot r` itself must remain. Boundary-current Banach
   typing is still `SF2/SF5`.
4. **Standard-target Wiener layer.** Demers--Zhang arXiv:1210.1261,
   Theorems 2.2, 2.5--2.6 and 2.11, put the moving fixed-radius standard
   collision maps on fixed Banach pairs with a uniform spectral gap. Thus the
   centered target resolvents have exponentially weighted absolutely
   summable operator coefficients. This does not differentiate the moving
   singularity or return partition and does not type the CM2 current.
5. **Direct standard-section simplification.** For a one-step candidate
   target circle, with `Delta=R^2-(u_perp dot (a-q))^2`, every forward
   tangency satisfies
   `|partial_phi Delta|=2 R ell >= 2 R_min tau_min>0`.
   The new exact rational certificate
   `cm2_standard_section_tangency_submersion_cert.py` specializes this to
   `R_min=4/25`, `tau_min>1821/10000`, and proves the strict derivative margin
   `|partial_phi Delta|>1821/31250=0.058272`.
   Distinct disjoint target circles have no common event hypersurface, and
   first-hit ordering cannot switch while two transverse positive roots
   coexist. Rebasing the final proof directly on `N` removes transparent-wall
   cuts and nested white-entry/exit radicals from the one-step inventory and
   avoids a separate spectral transfer theorem.

### Negative audit and exact remaining boundary

- Gate 2 remains open: no result gives finite-time projective Frostman on
  every actual stopped parent, normalized `A/Z_A` conditional moments, or the
  pointwise physical endpoint/projective identity on the same record.
- Gate 3 remains open: existing scripts contain no fail-closed
  chart/word/lift event manifest with squarefree/gcd/resultant/incidence and
  exact occurrence/source matching.
- Gate 4 remains open: the local `i_ent` and `i_exit` are different additive
  atoms. They cannot replace two alternative factorizations of one physical
  occurrence charged once by the same `q` law.
- Gate 5 is reduced, not closed: `SF1`, Kac algebra and the standard-target
  spectral layer are proved; block DQ/boundary currents and the CM2
  source/test norm lift remain.

Therefore unconditional CM2 remains `NO-GO FOR CLAIM`. The next shortest
route is to assemble the centered current directly on the standard section
`N`, use one single-copy forward/reverse incidence registry, and pair that
typed current with the already available standard-target spectral gap.

Reproduction of the new exact submersion margin:

```bash
python3 -m py_compile \
  deliverables/cm2_standard_section_tangency_submersion_cert.py
python3 deliverables/cm2_standard_section_tangency_submersion_cert.py
```

## 2026-07-15 07:39 CST — COMMON_MAGNET tenfold stable-translation falsification

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work. `sha256sum -c` passed every manifest entry,
  including the frozen v51 TeX and v52 TeX/PDF hashes. No frozen artifact was
  edited.

### Primitive and falsifiability

Primitive: continue the preceding lower-boundary repair test on the tenfold
larger closed window

```text
b = h/2 + delta,   |delta| <= 1e-11,
a = g(b) - 6e-20.
```

The test asks whether the complete two-return connector word remains physical
and whether its stable coordinate can reach zero. A failed occurrence margin,
a tile whose final stable sign cannot be separated from zero, or a certified
root would falsify the proposed exclusion. The window is eleven orders of
magnitude wider than the certified `1e-22` source stable half-width and ten
times wider than the preceding exclusion.

### Certified falsification

The new 400-bit Arb certificate
`cm2_connector_quartic_expanded_translation_falsification.py` covers the full
closed window by 32768 tiles. Each tile has stable half-width approximately
`3.052e-16`; the correlated one-dimensional occurrence recurrence retains
`a=g(b)-6e-20` through all 28 collisions. Uniformly on the entire window it
certifies

```text
flight                         > 0.187108714220621,
target discriminant           > 0.0102643729262338,
incidence cosine              > 0.633207760084326,
unintended-obstacle clearance > 0.222838755478728,
transparent-wall crossings    = 0.
```

The final coordinates satisfy the strict enclosures

```text
-3.5750396543e-9 < B_2 < -2.5001181805e-9 < 0,
-0.000693366176 < A_2 < -0.000693365100.
```

Consequently `B_2(h/2+delta)=0` has no solution for
`|delta| <= 1e-11`, and no source tile of half-width `1e-22` whose center lies
in `|delta| <= 1e-11-1e-22` can repair the lower stable side. This is a strict
tenfold extension of the previous local falsification, not an exclusion over
the full stable span `[0,h]`.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python -m py_compile \
  deliverables/cm2_connector_quartic_expanded_translation_falsification.py
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_quartic_expanded_translation_falsification.py
```

### Scope: proved and not proved

- **Proved/falsified:** the complete correlated lower boundary on the stated
  enlarged translation window keeps the 28-collision physical word but has
  strictly negative two-return stable coordinate everywhere; this repair has
  no root there.
- **Not proved:** absence of a root outside `|delta| <= 1e-11`, a re-fitted or
  curved graph, transported cones, a different connector branch, or a QNL
  strip in a common invariant rectangle. No physical PPE or unconditional CM2
  follows. `COMMON_MAGNET` remains open.
- A direct coarse covering of the full stable span `[0,h]` wrapped in the
  long-word occurrence enclosure. This is a method failure, not a physical
  event failure or a dynamical counterexample.

### Next kill test

Implement the genuine two-stage curve atlas at collision 14: use the first
return image stable coordinate as the second-stage parameter, certify its
monotonic inverse on each source tile, and enclose the image unstable graph as
a function of that parameter. Then extend beyond `|delta|=1e-11` adaptively
until either a strict interval-Newton root of `B_2=0` is found or the first
physical occurrence margin vanishes while `B_2<0`. The reparameterization is
required to avoid the exponential tile count of the source-parameter
long-word enclosure.

## 2026-07-15 06:46 CST — COMMON_MAGNET local stable-translation falsification

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work. `sha256sum -c` passed every manifest entry,
  including the frozen v51 TeX and v52 TeX/PDF hashes. No frozen artifact was
  edited.

### Primitive and falsifiability

Primitive: test the minimal repair prescribed by the preceding round. Translate
the lower boundary of the quartic two-return strip by a common stable amount

```text
b = h/2 + delta,   |delta| <= 1e-12,
a = g(b) - 6e-20,
```

and ask whether its two-return stable coordinate `B_2` can reach zero while
the complete 28-collision word stays physical. The translation window is ten
orders of magnitude wider than the certified stable source half-width
`1e-22`. A lost occurrence margin or a tile on which `B_2<0` cannot be proved
falsifies the certificate; a proved zero would instead produce the requested
repair candidate.

### Certified falsification

The new 400-bit Arb certificate
`cm2_connector_quartic_translated_two_return_cert.py` uses 2048 closed tiles
and a one-dimensional centered affine occurrence recurrence. Unlike a
two-dimensional box, this recurrence retains the exact correlation
`a=g(b)-6e-20` through all 28 collisions. It certifies, uniformly on the full
translation window,

```text
flight                         > 0.187108714220628,
target discriminant           > 0.0102643729262376,
incidence cosine              > 0.633207760084442,
unintended-obstacle clearance > 0.222838755478741,
transparent-wall crossings    = 0.
```

The strict global upper bound is

```text
B_2(h/2+delta) < -1.663889836067766e-9 < 0
for every |delta| <= 1e-12.
```

The corresponding unstable images stay in

```text
-0.000693367011754577 < A_2 < -0.000693364264831163.
```

Consequently the equation `B_2(delta)=0` has no root in this local window,
and no stable translation of the already certified source tile whose center
lies in `|delta| <= 1e-12-1e-22` can repair stable containment. This is a
strict local falsification, not a global exclusion over the full stable span
`[0,h]`.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python -m py_compile \
  deliverables/cm2_connector_quartic_translated_two_return_cert.py
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_quartic_translated_two_return_cert.py
```

### Scope: proved and not proved

- **Proved/falsified:** every stable translation in the stated local window
  retains the full physical two-return word but leaves the lower boundary
  strictly below the rectangle's stable side; the minimal local translation
  repair fails.
- **Not proved:** absence of a root for larger translations, translated or
  curved stable sides, a re-fitted graph, transported cones, or a different
  connector branch. No QNL strip, common invariant rectangle, physical PPE,
  or unconditional CM2 follows. `COMMON_MAGNET` remains open.

### Next kill test

Build a re-centered two-stage curve atlas: propagate a correlated source tile
through the first word, reparameterize its image graph in the fixed chart,
then certify the second word. Extend the translation window adaptively beyond
`1e-12` until either (i) a strict `B_2=0` interval-Newton root with the full
word is found, or (ii) the first physical event margin vanishes while `B_2<0`.
The latter would kill the entire connected translated branch before stable
containment, without the long-word wrapping seen in a single 28-collision
enclosure.

## 2026-07-15 06:26 CST — COMMON_MAGNET connector two-return rectangle falsification

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work. `sha256sum -c` passed every manifest entry,
  including the frozen v51 TeX and v52 TeX/PDF hashes. No frozen artifact was
  edited.

### Primitive and falsifiability

Primitive: execute the prescribed two-return test for the enlarged quartic
connector strip in

```text
R_2 = [-6.05e-12,h] x [0,h].
```

Propagate both boundary graphs through two complete connector words. Loss of
the second physical word, stable containment, the right endpoint inequality,
or the fixed `1e-6` cone is a falsification. Natural interval propagation of
the second word wrapped, so the certificate uses the already audited centered
affine occurrence recurrence for those 14 collisions and a centered
Jacobian mean-value enclosure for the final chart image.

### Certified falsification

The new 400-bit Arb certificate
`cm2_connector_quartic_two_return_falsification.py` divides each boundary into
128 stable tiles and certifies the complete 28-collision/16-fixed-section-return
word. Uniform margins over both words are

```text
flight                         > 0.1871087142206291,
target discriminant           > 0.0102641309286948,
incidence cosine              > 0.633200295642810,
unintended-obstacle clearance > 0.2228379010731692,
transparent-wall crossings    = 0.
```

The unstable endpoint inequalities are much stronger than required:

```text
lower two-return image < -0.00069336563804 < -6.05e-12,
upper two-return image >  0.00069338602463 > h.
```

However, every lower-boundary tile has two-return stable image

```text
B_2 < -3.037597933e-9 < 0.
```

Thus that image boundary leaves `R_2` through its stable side and cannot be a
full-cross boundary. This strictly falsifies the proposed axis-aligned
two-return rectangle despite successful unstable crossing. Independently,
the second word fails the previously used fixed forward cone of width `1e-6`:
its certified cone margin is strictly negative on every tile. This concerns
that fixed cone only, not absence of a wider or transported cone field.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python -m py_compile \
  deliverables/cm2_connector_quartic_two_return_falsification.py
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_quartic_two_return_falsification.py
```

### Scope: proved and not proved

- **Proved/falsified:** the fixed enlarged connector strip is not a two-return
  full-cross strip for the stated axis-aligned `R_2`; stable containment fails
  strictly while the entire physical word remains regular.
- **Proved/falsified:** the second connector word does not preserve the fixed
  width-`1e-6` forward cone on these endpoint tubes.
- **Not proved:** nonexistence after translating the stable source graph,
  using transported cones, curved stable sides, a different connector branch,
  or more iterates. No QNL strip, invariant stable boundary, physical PPE, or
  unconditional CM2 follows. `COMMON_MAGNET` remains open.

### Next kill test

Solve the lower-boundary two-return equation `B_2(delta)=0` for a common stable
translation `b -> b+delta` of the quartic source strip. Certify the translated
28-collision word and test simultaneously whether the upper boundary satisfies
`0 < B_2 < h`, both unstable endpoint inequalities persist, and a transported
second-word cone can be enclosed. Failure of the root/word or upper stable
containment kills this minimal axis-aligned repair; success yields a shifted
connector two-return full-cross candidate to compare with the QNL inverse
graphs.

## 2026-07-15 06:03 CST — COMMON_MAGNET QNL inverse-graph scale falsification

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every manifest entry,
  including the frozen v51 TeX and v52 TeX/PDF hashes.  No frozen artifact was
  edited.

### Primitive and falsifiability

Primitive: execute the preceding inverse-graph scale test on the complete
clean stable span

```text
0 <= b <= h,
h = R_G (pi/4-theta_connector)/2.
```

For the exact QNL word `G(0,0)->W(0,0)->G(0,0)`, locate the inverse graphs
of the current two target endpoints and also of the enclosing target values
`+/-6.46e-12`.  Independently certify that both image boundaries of the fixed
enlarged connector strip stay inside that enclosing target interval.  If all
QNL source graphs stay above `2e-4`, the present strip cannot share any
axis-aligned one-return rectangle on this stable span with this QNL branch:
connector full crossing forces the rectangle's right side below `6.46e-12`,
whereas source containment of the QNL endpoint graphs forces it above `2e-4`.

### Certified falsification

The new 400-bit Arb certificate
`cm2_qnl_inverse_graph_scale_falsification.py` divides the stable span into
1024 closed tiles.  On every tile it proves

```text
d A_QNL / da > 5                 on 2e-4 <= a <= h,
A_QNL(2e-4,b) < -6.46e-12,
A_QNL(h,b)     >  6.46e-12.
```

Thus each of the four targets

```text
-6.46e-12, -6.05e-12, 0, 6.46e-12
```

has exactly one inverse graph on every stable tile.  Uniform parametric
interval Newton inclusion localizes every graph to

```text
2e-4 < a < 2.139e-4,
minimum certified gap above 2e-4 > 1.38327e-5.
```

The single physical tube containing all Newton boxes retains the declared
two-collision QNL word, has no transparent-wall crossing, and satisfies

```text
flight                         > 0.18,
target discriminant           > 0.025,
incidence cosine              > 0.99,
unintended-obstacle clearance > 0.35.
```

An independent 128-tile recomputation of both enlarged connector boundary
images proves

```text
-6.46e-12 < A_connector(boundary,b) < 6.46e-12
```

with the previously certified opposite orientations.  Consequently an
axis-aligned one-return rectangle on stable span `[0,h]` full-crossed by this
fixed connector strip has right unstable side below `6.46e-12`, while either
QNL endpoint boundary would require a contained source coordinate above
`2e-4`.  This is a strict contradiction, so the present axis-aligned
one-return connector--QNL construction is falsified.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python -m py_compile \
  deliverables/cm2_qnl_inverse_graph_scale_falsification.py
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_qnl_inverse_graph_scale_falsification.py
```

### Scope: proved and not proved

- **Proved/falsified:** on the clean stable span `[0,h]`, no axis-aligned
  one-return rectangle can use both the fixed enlarged connector strip and
  the exact period-two QNL branch as full-cross strips.
- **Proved:** the obstruction is a strict source/image scale separation, not
  grazing, a wrong physical word, interval wrapping, or a missing local root.
- **Not proved:** nonexistence of a connector two-return construction, a
  curved-boundary magnet, a different connector/QNL branch, or a larger stable
  chart.  Invariant stable sides, conditional Gibbs weight, physical PPE, and
  unconditional CM2 remain open.  `COMMON_MAGNET` is not certified.

### Next kill test

Test the promising two-return enlargement directly.  Propagate both certified
connector boundary graphs through a second complete 14-collision connector
word and certify the full 28-collision physical itinerary, singularity
margins, double cones, stable-span containment, and opposite endpoint
inequalities for

```text
R_2 = [-6.05e-12,h] x [0,h].
```

The first-return boundary scale `6.45e-12` multiplied by the certified
connector unstable derivative suggests a second-return image of order
`1e-3`, large enough to cross the QNL center at `a=h`.  Loss of the second
physical word, stable containment, or the right inequality `A_16>h` is the
falsification; otherwise solve the QNL inverse graphs for targets
`-6.05e-12` and `h` to seed the first same-rectangle candidate not excluded by
the present scale obstruction.

## 2026-07-15 05:43 CST — COMMON_MAGNET same-rectangle QNL inverse falsification

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every manifest entry,
  including the frozen v51 TeX and v52 TeX/PDF hashes.  No frozen artifact was
  edited.

### Primitive and falsifiability

Primitive: carry out the prescribed inverse-branch test for the exact QNL
full-boundary period-two word

```text
G(0,0) -> W(0,0) -> G(0,0)
```

in the same connector eigenchart and on the complete proposed rectangle

```text
R_* = [-6.05e-12,0] x [0,h],
h = R_G (pi/4-theta_connector)/2.
```

Cover all of `R_*`, certify that the declared physical word is the first-hit
word, and test whether either target unstable endpoint `-6.05e-12` or `0`
has a preimage in `R_*`.  A missing root is the declared falsification of
this rectangle; roots with strict physical and derivative margins would seed
a QNL full-cross tube.

### Certified falsification

The new 400-bit Arb certificate
`cm2_qnl_same_rectangle_inverse_falsification.py` divides the stable span into
256 closed tiles and retains the full unstable interval on each tile.  It
certifies throughout `R_*`

```text
flight                         > 0.17,
target discriminant           > 0.02,
incidence cosine              > 0.9,
unintended-obstacle clearance > 0.12,
transparent-wall crossings    = 0.
```

Thus the failure below is not caused by a wrong word or a singularity.  A
centered mean-value enclosure of the complete QNL return proves on every tile

```text
A_QNL(a,b) < -0.0023,
d A_QNL / da > 11,
0 < B_QNL(a,b) < h.
```

Since the target unstable interval is `[-6.05e-12,0]`, its entire left-edge
gap from the QNL image is strictly greater than `0.0022`.  Therefore both
endpoint inverse equations have zero roots in `R_*`.  The connector strip
from the preceding rounds does full-cross this rectangle locally, but this
same `R_*` cannot also contain the requested QNL full-cross strip.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python -m py_compile \
  deliverables/cm2_qnl_same_rectangle_inverse_falsification.py
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_qnl_same_rectangle_inverse_falsification.py
```

### Scope: proved and not proved

- **Proved/falsified:** the exact regular QNL branch has no inverse root in
  `R_*` for either unstable target endpoint.  Hence the current rectangle is
  falsified as a one-return connector--QNL common full-cross rectangle.
- **Proved:** the whole tested QNL source rectangle has one physical word and
  strict singularity/clearance margins; the inverse-root absence is a genuine
  coordinate-scale obstruction, not interval wrapping or grazing.
- **Not proved:** nonexistence of a shifted/enlarged or curved-boundary common
  magnet, or nonexistence after additional connector/QNL iterates.  No QNL
  strip cone, Gibbs weight, physical PPE, or unconditional CM2 statement
  follows.  `COMMON_MAGNET` remains open.

### Next kill test

Use interval Newton to locate both QNL target-endpoint preimage graphs in the
larger connector eigenchart and certify their full stable-span `a` range.
Compare their smallest source/right-side coordinate with the already
certified connector boundary-image ceiling `6.46e-12`.  If the QNL preimage
graphs stay above `2e-4`, prove that no axis-aligned one-return rectangle can
simultaneously use the present connector strip and these QNL branches as
full-cross strips; otherwise the first overlapping interval supplies the
next candidate common rectangle.

## 2026-07-15 05:23 CST — COMMON_MAGNET enlarged interior-tube certificate

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every manifest entry,
  including the frozen v51 TeX and v52 TeX/PDF hashes.  No frozen artifact was
  edited.

### Primitive and falsifiability

Primitive: execute the prescribed centered occurrence-margin kill test on the
entire enlarged period-eight strip

```text
b in h/2 +/- 1e-22,       a in g(b) +/- 6e-20.
```

At every one of the 14 collisions, enclose the state by a point orbit plus a
two-generator affine part and a rigorous remainder.  Propagate interval first
derivatives on that centered state enclosure, and use centered mean-value
bounds for flight, target discriminant, incidence, wall coordinates, and
unintended-obstacle clearance.  Any failed event inequality or cone inclusion
falsifies the proposed interior tube.

### Certified result

The new 400-bit Arb certificate
`cm2_connector_quartic_interior_tube_cert.py` divides only the stable interval
into 128 tiles and retains the complete `6e-20` unstable half-width on every
tile.  It certifies the complete 14-collision/eight-return physical word, no
transparent-wall crossing, and uniform forward-unstable/inverse-stable cone
inclusion throughout the two-dimensional interior.  The global minima are

```text
flight                         > 0.187108714220629105,
target discriminant           > 0.0102643729262380,
incidence cosine              > 0.633207760084455,
unintended-obstacle clearance > 0.222838755478742,
forward cone margin           > 107.275975,
inverse cone margin           > 107.214507.
```

The previous natural enclosure `+/-26.6` was therefore interval dependency,
not a physical obstruction.  Combined with the independently certified two
boundary endpoint inequalities in
`cm2_connector_quartic_enlarged_endpoint_cert.py`, this upgrades the candidate
to a genuine local one-return connector full-cross strip for

```text
R_* = [-6.05e-12,0] x [0,h].
```

Here “full-cross” records source containment, a single regular physical branch,
uniform cones, images of the two unstable boundary components beyond opposite
unstable sides, and image containment in the stable span.  It does not assert
that the horizontal sides of `R_*` are invariant stable manifolds.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python -m py_compile \
  deliverables/cm2_connector_quartic_interior_tube_cert.py \
  deliverables/cm2_connector_quartic_enlarged_endpoint_cert.py
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_quartic_interior_tube_cert.py
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_quartic_enlarged_endpoint_cert.py
```

### Scope: proved and not proved

- **Proved:** the complete enlarged two-dimensional connector strip has one
  physical word, strict occurrence margins, strict uniform double cones, and,
  with the endpoint certificate, one-return full-cross inequalities for the
  explicit `R_*`.
- **Not proved:** the stable sides of `R_*` are invariant local stable
  manifolds, or that the resulting curved-boundary rectangle satisfies every
  proper-family convention needed later.
- **Not proved:** a QNL branch full-crosses this same `R_*`.  Thus
  `COMMON_MAGNET` remains open, and no physical PPE or unconditional CM2
  conclusion follows.

### Next kill test

Construct the exact period-two QNL return in the same connector eigenchart and
test whether any regular preimage strip contained in `R_*` maps across both
unstable sides while remaining in `0<b'<h`.  Begin with interval Newton on the
two QNL inverse branches for the target unstable endpoints `-6.05e-12` and
`0`; a missing root in `R_*`, a wrong physical word, or loss of a strict cone
is a falsification of this rectangle, while two regular roots seed the QNL
full-cross tube certificate.

## 2026-07-15 05:03 CST — COMMON_MAGNET enlarged boundary endpoint certificate

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every manifest entry,
  including the frozen v51 TeX and v52 TeX/PDF hashes.  No frozen artifact was
  edited.

### Primitive and falsifiability

Primitive: execute the preceding round's prescribed enlargement from
half-thickness `1e-25` to `6e-20`.  First test the complete two-dimensional
tube with the existing natural-interval physical-word checker; then, without
turning interval wrapping into a dynamical claim, subdivide the two boundary
graphs until their physical words, singularity margins, centered cones, and
endpoint inequalities are decided.  The fixed endpoint test rectangle is

```text
R_* = [-6.05e-12,0] x [0,h],
h = 0.0002350090580140563309067271170095427... .
```

Failure of either boundary itinerary, cone inequality, containment in the
stable span, or opposite unstable endpoint inequality falsifies this enlarged
boundary candidate.

### Certified result and method limitation

The new 400-bit Arb certificate
`cm2_connector_quartic_enlarged_endpoint_cert.py` divides
`b=h/2 +/- 1e-22` into 128 tiles.  On both graphs

```text
a = g(b) - 6e-20,       a = g(b) + 6e-20
```

it certifies the same complete 14-collision/eight-return physical word, no
transparent-wall crossing, strict forward unstable and inverse stable cone
inclusion, and

```text
lower boundary:  a' < -6.4500122502e-12 +/- 5.76e-23 < -6.05e-12,
upper boundary:  a' >  6.4500122502e-12 +/- 5.94e-23 > 0,
image b span:     1.0930661969926996649e-12
               < b' < 1.0930661969932975866e-12 < h.
```

The minimum certified margins over all 256 boundary tiles are

```text
flight                         > 0.18710871422062910628,
target discriminant           > 0.01026437292623805,
incidence cosine              > 0.6332077600844558,
unintended-obstacle clearance > 0.22283875547874295,
forward cone margin           > 107.500197399073,
inverse cone margin           > 107.214507645009.
```

Thus enlargement by `6e5` relative to the preceding certified thickness
passes the necessary boundary endpoint test for `R_*`, with large regularity
and cone margins.  This reverses the one-return endpoint obstruction proved
at thickness `1e-25` for this fixed test interval.

The unsplit two-dimensional tube is **not** certified by the old natural
interval propagation: the unintended-obstacle distance enclosure inflates to
`+/-26.6`.  Point evaluation at both endpoints is regular, and the subdivided
boundary proof is regular, so this failure is recorded as interval dependency,
not as a dynamical falsification.  No claim is made about the interior between
the boundary graphs.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python -m py_compile \
  deliverables/cm2_connector_quartic_strip_cone_cert.py \
  deliverables/cm2_connector_quartic_full_cross_falsification.py \
  deliverables/cm2_connector_quartic_enlarged_endpoint_cert.py
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_quartic_enlarged_endpoint_cert.py
```

### Scope: proved and not proved

- **Proved:** the two enlarged boundary graphs uniformly retain the declared
  physical word, singularity margins, strict cones, and cross the two unstable
  sides of the explicitly stated `R_*`, while their images stay in its stable
  span.
- **Not proved:** the intervening two-dimensional strip has one physical word
  or uniform cones.  Boundary certification alone is not a full-cross strip.
- **Not proved:** invariant stable sides, a QNL full-cross strip in the same
  rectangle, or that `R_*` is a common Markov rectangle.  Hence
  `COMMON_MAGNET` remains open, and no physical PPE or unconditional CM2
  conclusion follows.

### Next kill test

Replace natural interval iteration on the enlarged interior by a centered
occurrence-margin enclosure (point orbit plus first/second derivatives for
every flight, discriminant, incidence, wall coordinate, and unintended
segment distance).  Apply it to the full `6e-20` unstable width, subdividing
only if a centered remainder still overlaps a threshold.  Stop at the first
strictly wrong event inequality, or certify the complete interior physical
tube and cone margins; only the latter upgrades the present boundary result
to a genuine local full-cross strip candidate.

## 2026-07-15 04:43 CST — COMMON_MAGNET one-return full-cross falsification

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every manifest entry,
  including the frozen v51 TeX and v52 TeX/PDF hashes.  No frozen artifact was
  edited.

### Primitive and falsifiability

Primitive: execute the preceding round's endpoint kill test for the certified
period-eight quartic strip.  Propagate both unstable-thickness boundary graphs
through the complete certified return, using the centered Hessian/Jacobian
enclosures rather than dependency-inflated natural intervals, and compare
their image `a` coordinates with the source strip's `a` coordinate.  Opposite
endpoint inequalities would permit an axis-aligned product full-cross;
failure of either inequality falsifies this strip width and one-return choice.

### Certified falsification

The new 400-bit Arb certificate
`cm2_connector_quartic_full_cross_falsification.py` subdivides the stable
interval into the same 16 tiles, rechecks the complete physical tube, and
applies the mean-value enclosure to each of the two boundary graphs.  It
proves uniformly

```text
entire source strip:                 a < -6.03e-12,
both one-return boundary images:    |a'| < 1.08e-17.
```

Consequently, every axis-aligned product rectangle in the fixed connector
eigenchart that contains the source strip has left unstable side below
`-6.03e-12`, while the complete image of either boundary remains to the right
of `-1.08e-17`.  The image therefore cannot cross that left side.  The strict
left-endpoint shortfall is greater than

```text
6.0299892e-12,
```

which is more than `558332` times the certified image bound.  This is a
rigorous scale mismatch for the present half-thickness `1e-25`; it is not an
extrapolated claim that multiplying the source thickness by that factor will
remain on the same physical branch.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python -m py_compile \
  deliverables/cm2_connector_quartic_full_cross_falsification.py
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_quartic_full_cross_falsification.py
```

### Scope: proved and not proved

- **Proved/falsified:** this certified quartic strip, at half-thickness
  `1e-25`, does not full-cross after one return any axis-aligned product
  rectangle in the fixed eigenchart that also contains its source.
- **Not falsified:** a wider physical strip, an additional return, curved
  invariant product boundaries, or a different common rectangle.
- **Not proved:** stable-manifold boundary continuation or a QNL strip in the
  same rectangle.  Thus `COMMON_MAGNET` remains open, and no physical PPE or
  unconditional CM2 conclusion follows.

### Next kill test

Increase the source half-thickness directly to `6e-20` on the same 16 stable
tiles and rerun the physical-word, singularity, centered-cone, and endpoint
checks.  Either this width remains physical and reaches both unstable sides,
or the first failed flight/discriminant/clearance/cone inequality supplies a
rigorous upper obstruction and forces an additional iterate or a different
strip construction.

## 2026-07-15 04:23 CST — COMMON_MAGNET connector strip cone certificate

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed all nine manifest entries,
  including the frozen v51 and v52 TeX/PDF hashes.  No frozen artifact was
  edited.

### Primitive and falsifiability

Primitive: certify a forward unstable cone and inverse stable cone uniformly
on the nonzero period-eight quartic strip certified in the preceding round.
For each of its 16 physical tiles, enclose the complete return derivative by
a centered point Jacobian plus a rigorous Hessian-times-radius remainder.
Failure of a flight/discriminant inequality in the second-order propagation,
of either cone inclusion, or of the expansion threshold falsifies this
strip-cone subgate.

Direct natural-interval differentiation was also tested and explicitly fails
as a proof method: despite tile stable half-width only `6.25e-24`, wrapping
inflates the Jacobian determinant enclosure to an interval containing zero.
This is not a dynamical counterexample.  The centered second-order jet removes
that dependency loss.

### Certified result

`cm2_connector_quartic_strip_cone_cert.py` now propagates value, gradient, and
Hessian Arb enclosures through every one of the 14 circular collisions.  On
each tile it evaluates the point Jacobian at the tile center and applies the
mean-value enclosure

```text
DT(X) subset DT(x0) + D^2T(X) (X-x0).
```

In the fixed connector eigenchart `(a,b)`, let

```text
C^u = {|db| <= 1e-6 |da|},
C^s = {|da| <= 1e-6 |db|}.
```

The certificate proves on all 16 tiles

```text
DT(C^u) is strictly inside C^u,
DT^{-1}(C^s) is strictly inside C^s,
forward and inverse dominant expansion > 100.
```

For the inverse calculation it uses the exact identity `det DT=1`: circular
billiard collision maps preserve `dr dp`, and the same fixed linear
`(a,b)->(r,p)` chart occurs at source and target, so the inverse is exactly the
adjugate.  The minimum printed margins are

```text
forward cone margin  > 107.500199184145,
inverse cone margin  > 107.214507645038,
forward dominant     > 107500204.170125,
inverse dominant     > 107500204.170125.
```

The same run rechecks the complete physical word and retains strict minimum
flight `>0.1871`, target discriminant `>0.01026`, incidence cosine `>0.6332`,
and unintended-obstacle clearance `>0.2228`.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python -m py_compile \
  deliverables/cm2_connector_quartic_strip_cone_cert.py
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_quartic_strip_cone_cert.py
```

### Scope: proved and not proved

- **Proved:** the certified nonzero period-eight physical strip has uniform
  forward/inverse cone inclusions, expansion, and singularity margins.
- **Not proved:** its graph boundaries are stable-manifold pieces; its image
  full-crosses a specified rectangle; a QNL full-cross strip lies in that
  same rectangle.  Hence `COMMON_MAGNET` remains open.
- No strip weight, actual-SRB parent law, physical PPE identification, or
  unconditional CM2 statement is inferred.

### Next kill test

Propagate the two unstable-thickness boundary graphs of the certified quartic
strip with the same centered jet and compare their image `a`-coordinates to a
frozen candidate rectangle.  Either certify opposite strict crossings and
then shoot stable boundary graphs, or produce the first endpoint inequality
that has the wrong sign.  The latter falsifies this strip width/rectangle and
quantifies the enlargement or additional iterate required.

## 2026-07-15 04:03 CST — COMMON_MAGNET nonempty quartic physical strip

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every v52-manifest entry,
  including the frozen v51 TeX hash
  `da0f4ac7a6a914bef07d4e3b05d844803b251cc60f2486f943aca3abed4736bc`.
  No frozen artifact was edited.

### Primitive and falsifiability

Primitive: replace the preceding three isolated quartic interpolation nodes
by a genuinely nonempty two-dimensional source strip.  Around the middle
node of the unique three-node quartic interpolant

```text
a = c b^2 + d b^3 + e b^4
```

cover a closed stable interval by interval tiles, thicken every graph value
in the unstable direction, and propagate every tile through the complete
14-collision/eight-return connector word.  A nonpositive flight or target
discriminant, a possible unintended collision, loss of incidence
transversality, or a transparent-wall crossing falsifies the strip.

An attempted direct natural-interval propagation of the whole interpolation
interval `b0 +/- 1e-6` failed already at the first target discriminant
(`+/-0.0438`).  This is dependency blow-up, not a physical counterexample.
Likewise, the natural interval Jacobian on a much smaller tile enclosed zero
in its determinant, although its center matrix is nonsingular.  Neither
failure is recorded as a cone or geometry falsification.

### Certified result

The new 400-bit Arb script
`cm2_connector_quartic_strip_cone_cert.py` repeats the three-dimensional
Krawczyk inclusion for `(c,d,e)`, then covers

```text
b in b0 +/- 1e-22,     |a-(c b^2+d b^3+e b^4)| <= 1e-25
```

by 16 closed interval tiles.  This is a strict nonempty two-dimensional
strip, not a list of point samples.  Every tile retains the declared physical
word, with rigorous aggregate balls

```text
minimum flight                         0.187108714220629106285 +/- 6.01e-22
minimum target discriminant            0.0102643729262381      +/- 6.21e-17
minimum incidence cosine               0.63320776008446        +/- 4.90e-15
minimum unintended-obstacle clearance  0.222838755478743       +/- 1.79e-16
transparent-wall crossings             0
```

Thus the simpler theorem margins `flight>0.18`, `discriminant>0.01`,
`incidence>0.63`, and `clearance>0.22` all hold uniformly.  The repeated
three-node Jacobian determinant is

```text
6.5391694150907526145e-18 +/- 5.66e-108,
```

and the coefficient enclosures agree with the preceding certificate,
including `e>0.03`.

Reproduction:

```bash
python3 -m py_compile \
  deliverables/cm2_connector_quartic_strip_cone_cert.py
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_quartic_strip_cone_cert.py
```

### Scope: proved and not proved

- **Proved:** one explicit nonzero-area connector source strip carries the
  complete physical period-eight word with uniform flight, target,
  incidence, unintended-obstacle, and transparent-wall margins.
- **Proved:** the preceding finite-node quartic continuation is compatible
  with a genuine local physical tube; the result is not based only on node
  samples.
- **Not proved:** a centered/affine derivative enclosure on the strip.  The
  script deliberately prints `uniform_derivative_cone_margin=not tested`.
- **Not proved:** invariant stable boundaries, full crossing, enlargement to
  the QNL center, or a common QNL/connector rectangle.  Therefore
  `COMMON_MAGNET` remains open; this is not physical PPE or unconditional
  CM2.

### Next kill test

Implement a centered first-order/affine collision jet on the certified strip,
using the exact center orbit plus a rigorous second-derivative remainder,
rather than differentiating the natural interval iterate.  First certify the
forward unstable and inverse stable cones on the present strip.  Then enlarge
the stable and unstable half-widths geometrically until either a strict cone
or physical margin fails.  Stop at the first explicit remainder inequality
that loses sign; do not interpret a singular natural interval Jacobian as a
physical singularity.

## 2026-07-15 03:43 CST — COMMON_MAGNET nondegenerate quartic three-node continuation

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every v52-manifest entry.
  Direct checks reproduced the frozen v51 TeX, v52 TeX, and v52 PDF hashes.
  No frozen artifact was edited.

### Primitive and falsifiability

The preceding joint remainder test uses nodes separated by only `1e-24`.
At a remainder scale near `2e-42`, direct interval evaluation of the
three-coefficient Jacobian becomes too ill-conditioned to certify a local
branch: this is a certificate failure, not evidence against the stable
manifold.  The present kill test therefore asks a sharply local but
well-conditioned question.  For

```text
g(b)=c b^2+d b^3+e b^4
```

solve the complete period-eight invariance equation at the three regular
nodes `b0-1e-6`, `b0`, and `b0+1e-6`, and use a three-dimensional Krawczyk
step to certify the local continuation of the earlier cubic interpolant.
The test is falsified if the Krawczyk image leaves its coefficient box, the
quartic coefficient meets the old `2e-42` scale, or any node loses its
14-collision/eight-return physical word.

### Certified result

The new 400-bit Arb certificate
`cm2_connector_quartic_three_node_cert.py` proves a unique zero inside its
explicit local coefficient box, with

```text
c = -0.0012896404367082769325632414662066...,
d =  7.2526609638318980247172659517197767...,
e =  0.0366791301013858930914149042381417...,
det D(F_left,F_mid,F_right) = 6.53916941509e-18 +/- 5.66e-108.
```

In particular the certified local continuation has `e>0.03`, hence lies
strictly outside `|e|<=2e-42`.  All three nodes retain the complete physical
word.  Across the node certificates the minimum flight is `>0.187`, target
discriminant `>0.0102`, incidence cosine `>0.633`, unintended-obstacle
clearance `>0.222`, and there is no transparent-wall crossing.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_quartic_three_node_cert.py
python3 -m py_compile \
  deliverables/cm2_connector_quartic_three_node_cert.py
```

### Scope: proved and not proved

- **Proved:** the three-node quartic equation has a unique solution in the
  declared local Krawczyk box, and that branch requires a positive quartic
  coefficient larger than `0.03`.
- **Not proved:** global uniqueness among all coefficient triples, exclusion
  of arbitrary `C^4` remainder functions, or physical-word uniformity on the
  entire interval between the three nodes.  The result must not be promoted
  to a stable-manifold or graph-transform certificate.
- **Not proved:** a true stable boundary, full crossing, uniform cone and
  singularity margins, or a common QNL/connector rectangle.  Therefore
  `COMMON_MAGNET` remains open and this is not physical PPE or unconditional
  CM2.

### Next kill test

Build a validated continuation in the node coordinate: subdivide
`[b0-1e-6,b0+1e-6]`, certify the local implicit coefficient branch on each
subinterval with centered affine/Krawczyk enclosures, and extract a rigorous
fourth-derivative/remainder bound.  Stop if neighbouring boxes fail to
overlap uniquely, the interval Jacobian loses rank, or any subinterval loses
the physical word.

## 2026-07-15 03:23 CST — COMMON_MAGNET joint Taylor-box three-node falsification

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every v52-manifest entry.
  Direct checks reproduced the frozen v51 TeX, v52 TeX, and v52 PDF hashes.
  No frozen artifact was edited.

### Primitive and falsifiability

In connector eigen-coordinates consider the joint Taylor box

```text
g(b)=c b^2+d b^3+r(b),              |r(b)| <= M b^4,
M=1e-55,
c=c_*+[-1e-40,1e-40],               d=d_*+[-1e-36,1e-36],
```

where `(c_*,d_*)` is the unique two-endpoint cubic interpolant.  Unlike the
preceding fixed-center test, this kill test allows the two polynomial
coefficients to move and propagates the complete source/output remainder at
the left, midpoint, and right nodes.  A robust two-variable interval-Newton
step must contain the endpoint-induced coefficient displacement.  A
three-node linear functional whose two coefficient derivatives cancel at the
center must then remain separated from every admitted remainder error.
Failure of Newton inclusion, three-node separation, image-domain inclusion,
or any physical margin fails the certificate.

### Certified result and falsification

The new 400-bit Arb certificate
`cm2_connector_joint_taylor_ball_falsification.py` certifies the robust
endpoint Newton images

```text
c image radius < 3.14e-43       inside the declared c box,
d image radius < 3.15e-39       inside the declared d box,
endpoint Jacobian determinant   4.4062e-24 +/- 9.49e-29, excluding zero.
```

Thus the declared coefficient box contains the entire locally induced
endpoint displacement for the admitted remainder right-hand sides.  The
elimination weights are `alpha=0.5000000000000000000085...` and
`beta=0.4999999999999999999915...`.  On the full coefficient box the
certificate obtains

```text
eliminated center defect                 5.4442433384298385e-50,
coefficient-variation bound              4.1471002773e-65,
each node remainder-error bound          < 2.0494046314e-63,
allowed eliminated remainder error       < 4.0988092626e-63,
final rigorous three-node separation     > 5.4442433384e-50 > 1e-50.
```

Consequently no graph in this explicit joint `(c,d,r)` box satisfies the
connector invariance equation at all three nodes.  The complete
14-collision/eight-return tube remains physical, with minimum flight `>0.18`,
target discriminant `>0.01`, incidence cosine `>0.63`, unintended-obstacle
clearance `>0.22`, and no transparent-wall crossing.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_joint_taylor_ball_falsification.py
python3 -m py_compile \
  deliverables/cm2_connector_joint_taylor_ball_falsification.py
```

### Scope: proved and not proved

- **Proved:** even after allowing and rigorously enclosing the endpoint-driven
  displacement of both cubic Taylor coefficients, an arbitrary value
  remainder with `|r(b)|<=1e-55 b^4` cannot repair the strict three-node
  invariance defect inside the displayed local coefficient box.
- **Not proved:** exclusion for arbitrary Taylor coefficients outside this
  local box, or for the much larger remainder sizes relevant to graph-
  transform closure.  This finite-node obstruction is compatible with the
  existence of the genuine nonlinear stable manifold.
- **Not proved:** a self-mapped contracting Taylor graph box, a true stable-
  manifold tile, full crossing, uniform cone boundaries, or a common
  QNL/connector rectangle.  Therefore `COMMON_MAGNET` remains open; the
  result is not physical PPE or unconditional CM2.

### Next kill test

Replace the natural interval-gradient enclosure in the three-node functional
by a centered second-order Taylor/affine enclosure.  Increase `M` toward and
above the previous `2e-42` fixed-center floor while retaining robust endpoint
Newton inclusion.  Stop if the affine remainder exceeds the three-node
defect, the endpoint Jacobian loses invertibility, the image stable coordinate
leaves the tile, or any collision margin is lost.

## 2026-07-15 02:54 CST — COMMON_MAGNET fourth-order remainder-ball lower bound

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every v52-manifest entry.
  Direct checks reproduced the frozen v51 TeX, v52 TeX, and v52 PDF hashes.
  No frozen artifact was edited.

### Primitive and falsifiability

Let `P(b)=c b^2+d b^3`, with `(c,d)` the unique interval-Newton enclosure
from the preceding two-endpoint certificate, and consider the explicit
fourth-order value ball

```text
g(b)=P(b)+r(b),             |r(b)| <= M |b|^4,   0 <= b <= b0.
```

The kill test propagates the entire allowed source remainder at `b0` through
the complete period-eight connector word, verifies that the image stable
coordinate remains in `[0,b0]`, and subtracts the entire allowed output
remainder there.  Strict positivity of the resulting graph defect falsifies
that Taylor ball.  Loss of the image-domain inclusion, defect separation, or
any physical margin also fails the certificate.

### Certified result and falsification

The new 400-bit Arb certificate
`cm2_connector_fourth_remainder_lower_bound_cert.py` proves that the ball
centered on the fixed two-node polynomial cannot contain an invariant graph
when `M <= 2e-42`.  At `b=b0` it certifies

```text
base polynomial defect                         5.4442433384e-50 +/- 9.17e-66,
source remainder radius, M b0^4                3.8128385841e-58,
dA/da on the complete source tube              1.0750020417e8,
propagated unstable error                      4.0988092626e-50,
image stable coordinate B                     1.09306619699e-12,
output remainder radius, M B^4                 2.8550640237e-90,
corrected-defect rigorous lower bound           1.3454340695e-50 > 1e-50.
```

Thus this declared fourth-order ball is rigorously too small.  The argument
uses a mean-value enclosure of the complete return map rather than direct
interval propagation of the tiny source ball, avoiding dependency blow-up.
The entire source tube retains the 14-collision/eight-return word, with
minimum flight `>0.18`, target discriminant `>0.01`, incidence cosine
`>0.63`, unintended-obstacle clearance `>0.22`, and no transparent-wall
crossing.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_fourth_remainder_lower_bound_cert.py
python3 -m py_compile \
  deliverables/cm2_connector_fourth_remainder_lower_bound_cert.py
```

### Scope: proved and not proved

- **Proved:** no graph in the displayed value ball around the fixed unique
  two-endpoint polynomial satisfies the invariance equation at `b0`; for this
  polynomial center, any successful fourth-order remainder coefficient must
  exceed `2e-42`.
- **Not proved:** a lower bound for arbitrary cubic Taylor coefficients.  If
  a remainder is admitted at the endpoint equations, the admissible `(c,d)`
  center may move; this certificate deliberately does not freeze that shift
  into a claim about every stable graph.
- **Not proved:** a self-mapped and contracting Taylor graph box, a true
  stable-manifold tile, full crossing, uniform cone boundaries, or a common
  QNL/connector rectangle.  Therefore `COMMON_MAGNET` remains open and this
  is not physical PPE or unconditional CM2.

### Next kill test

Enlarge to a joint `(c,d,r)` Taylor box: include the endpoint displacement
caused by `|r(b)| <= M|b|^4`, derive the induced coefficient box by a
two-variable interval Newton/Krawczyk step, and test graph-transform closure
for `M` above the certified `2e-42` floor.  Stop if the coefficient image or
fourth-order remainder image leaves its declared box, stable-coordinate
monotonicity fails, or any collision margin is lost.

## 2026-07-15 02:34 CST — COMMON_MAGNET cubic two-node interpolation falsification

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every v52-manifest entry.
  Direct checks reproduced the frozen v51 TeX, v52 TeX, and v52 PDF hashes.
  No frozen artifact was edited.

### Primitive and falsifiability

In connector eigen-coordinates set

```text
g(b)=c b^2+d b^3,
T_connector(g(b),b)=(A,B),
F(c,d;b)=A-cB^2-dB^3.
```

The kill test asks whether the extra cubic coefficient can repair the
constant-quadratic obstruction on the centered tile of radius `1e-24`.
First solve `F=0` simultaneously at both endpoints by a two-variable interval
Newton step.  Then evaluate the entire unique-root enclosure at the midpoint.
Failure of Jacobian invertibility, Newton inclusion, midpoint separation, or
any physical margin falsifies the certificate.

### Certified result and falsification

The new 400-bit Arb certificate
`cm2_connector_cubic_two_node_cert.py` proves that the endpoint system has a
unique solution in the explicit coefficient boxes

```text
c = -0.001289640943146331361651045203600220681663150208119486830322132913991
      +/- 2.56e-70,
d =  7.25266958374078078835324856857712390388824465740614705154723994237
      +/- 5.90e-66.
```

The interval Jacobian determinant is

```text
4.406228325828311135128774011599258e-24 +/- 4.20e-58,
```

and both interval-Newton images lie strictly inside their declared boxes.
For the resulting unique endpoint interpolant, the midpoint residual is

```text
F(c*,d*;b0) = 5.444243338429839e-50 +/- 9.17e-66 > 1e-50.
```

Thus no quadratic--cubic graph in the declared coefficient box is invariant
on the centered tile.  This is a strict mathematical obstruction, not merely
failure of an interval enclosure.  The complete 14-collision/eight-return
tube remains physical, with minimum flight `>0.18`, target discriminant
`>0.01`, incidence cosine `>0.63`, unintended-obstacle clearance `>0.22`,
and no transparent-wall crossing.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_cubic_two_node_cert.py
python3 -m py_compile \
  deliverables/cm2_connector_cubic_two_node_cert.py
```

### Scope: proved and not proved

- **Proved:** the endpoint equations have one unique quadratic--cubic
  interpolant in the stated box, and that interpolant has a rigorously
  nonzero midpoint defect.  Therefore adding one cubic coefficient does not
  produce an invariant connector graph even on this `2e-24` tile.
- **Not proved:** nonexistence of the genuine local stable manifold.  A
  nonpolynomial graph with a controlled Taylor remainder is expected from
  hyperbolicity and is not excluded by this finite-dimensional test.
- **Not proved:** a graph-transform invariant tile, full crossing, uniform
  cone/singularity margins, or a common QNL/connector rectangle.  Hence
  `COMMON_MAGNET` remains open and no physical PPE or unconditional CM2 claim
  follows.

### Next kill test

Stop finite-degree interpolation.  Build a third-order interval Taylor model
with an explicit fourth-order remainder ball and certify that the connector
graph transform maps a declared value/slope/curvature/remainder box strictly
into itself and contracts there.  Stop if the image remainder exceeds the
box, the transformed stable coordinate loses monotonicity, or any physical
margin fails.

## 2026-07-15 02:14 CST — COMMON_MAGNET constant-quadratic tile falsification

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every v52-manifest entry.
  Direct checks reproduced the frozen v51 TeX, v52 TeX, and v52 PDF hashes.
  No frozen artifact was edited.

### Primitive and falsifiability

Write the one-cycle connector graph residual in the certified eigen-chart as

```text
F(c,b) = A(c b^2,b) - c B(c b^2,b)^2.
```

The preceding point certificate isolated a local coefficient satisfying
`F(c,b0)=0`.  The present kill test asks whether that constant coefficient can
solve the same equation on any centered nondegenerate `b`-tile.  It is
falsified if the unique center coefficient has nonzero residual at either
endpoint; loss of interval-Newton inclusion, a derivative sign, or any
physical margin also fails the certificate.

### Certified falsification

The new 400-bit Arb certificate
`cm2_connector_constant_quadratic_tile_falsification.py` proves that on the
explicit local curvature box

```text
C = -0.000437419419665271895210239743171794 +/- 1e-18
```

the center equation is strictly increasing, with

```text
dF/dc = 1.484 +/- 7.28e-4 > 1.
```

Interval Newton isolates its unique root to radius `8.05e-100`.  At the two
exact nodes `b0 +/- 1e-24`, the residuals for this entire root enclosure are

```text
F(c*,b0-1e-24) =  1.076505848365028145395497766...e-23
                   +/- 6.48e-86 > 1e-24,
F(c*,b0+1e-24) = -1.076505848365028145432142980...e-23
                   +/- 5.58e-86 < -1e-24.
```

Moreover the direct automatic-differentiation enclosure on the centered tile
gives

```text
dF/db = -11 +/- 0.498 < -9.
```

Consequently no constant coefficient in `C` defines an invariant quadratic
graph on the declared tile.  The whole interval tube retains the complete
14-collision/eight-return word with strict margins

```text
minimum flight                         > 0.18,
minimum target discriminant            > 0.01,
minimum incidence cosine               > 0.63,
minimum unintended-obstacle clearance  > 0.22,
transparent-wall crossings             = 0.
```

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_constant_quadratic_tile_falsification.py
python3 -m py_compile \
  deliverables/cm2_connector_constant_quadratic_tile_falsification.py
```

### Scope: proved and not proved

- **Proved:** the constant-curvature continuation of the certified center
  point is impossible even on the displayed nondegenerate local tile; the
  obstruction has a strict signed `b`-derivative and is not a collision-word,
  grazing, wall, or clearance artifact.
- **Not proved:** impossibility of a variable-curvature stable graph.  A cubic
  or higher graph can absorb the signed coefficient drift.
- **Not proved:** a graph-transform invariant tile, full crossing, uniform
  cone boundaries, or the common QNL/connector rectangle.  Thus
  `COMMON_MAGNET` remains open; no physical PPE or unconditional CM2 claim
  follows.

### Next kill test

Use `a=c b^2+d b^3`.  Solve the invariance residual simultaneously at
`b0-1e-24` and `b0+1e-24` with a two-variable interval Newton/Krawczyk box,
then test the residual and graph-transform slope on the whole centered tile.
Stop if the two-node Jacobian is singular, inclusion fails, or any physical
margin is lost.

## 2026-07-15 01:54 CST — COMMON_MAGNET quadratic point invariance

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every v52-manifest entry.
  Direct checks also reproduced the frozen v51 TeX, v52 TeX, and v52 PDF
  hashes.  No frozen artifact was edited.

### Primitive and falsifiability

In connector eigen-coordinates fix `b=h/2` and test the same quadratic graph
at source and image:

```text
a = c b^2,                 (A(c),B(c)) = T_connector(c b^2,b),
F(c) = A(c) - c B(c)^2 = 0.
```

This is the first nonlinear pointwise graph-invariance equation suggested by
the preceding signed tangent defect.  Interval-Newton non-inclusion, a
coefficient derivative containing zero, or loss of any declared physical
margin falsifies it.  The test deliberately makes no interval-in-`b` claim.

### Certified result

The new 400-bit Arb certificate
`cm2_connector_quadratic_point_invariance_cert.py` applies four nested
interval-Newton reconditionings and proves that the initial coefficient box

```text
c in -0.00043741941966527186 +/- 1e-18
```

contains exactly one solution, enclosed by

```text
c_* = -0.0004374194196652718952102397431717940573307476054078099724...
      +/- 7.82e-100,
dF/dc = 1.48428911028618530640654070532360368038594926049954...
        +/- 5.49e-51 > 1.
```

At this root,

```text
a_* = -6.0395874244980567528944193530249706832664749016269554...e-12,
B_* =  1.0930661969930522782501638687607789143077819645104795...e-12,
A_* = -5.2262597168920957115078700618154905858519063900...e-28,
A_* = c_* B_*^2 exactly by the certified root equation.
```

The full 14-collision/eight-return word retains strict margins

```text
minimum flight                         > 0.18,
minimum target discriminant            > 0.01,
minimum incidence cosine               > 0.63,
minimum unintended-obstacle clearance  > 0.22,
transparent-wall crossings             = 0.
```

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_quadratic_point_invariance_cert.py
python3 -m py_compile \
  deliverables/cm2_connector_quadratic_point_invariance_cert.py
```

### Scope: proved and not proved

- **Proved:** at the fixed nonzero slice `b=h/2`, there is a unique strictly
  negative quadratic coefficient satisfying the exact one-cycle invariance
  equation; its coefficient transversality is bounded below by one.
- **Proved:** this nonlinear equality is compatible with the complete physical
  connector word and is not a grazing, target-switching, or obstacle-clearance
  artifact.
- **Not proved:** the same coefficient works on any nondegenerate `b`-interval.
  A single invariant point does not certify a graph-transform invariant tile,
  a true local stable manifold, or a full-cross strip.
- **Not proved:** strip-uniform cone/singularity margins or a common rectangle
  with the QNL branch.  Thus `COMMON_MAGNET` remains open and no physical PPE
  or unconditional CM2 conclusion follows.

### Next kill test

Use a centered `b`-tile around `h/2` and the certified `c_*` as preconditioner.
Bound the functional residual `A(c b^2,b)-c B(c b^2,b)^2` and its `c` derivative
uniformly on the tile.  First test whether one constant coefficient box closes;
if it fails, add a cubic term `d b^3` and certify a two-coefficient/two-node
Krawczyk interpolation before attempting a full graph transform.

## 2026-07-15 01:34 CST — COMMON_MAGNET affine-tangent invariance falsification

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every v52-manifest entry,
  including the frozen v51 TeX hash
  `da0f4ac7a6a914bef07d4e3b05d844803b251cc60f2486f943aca3abed4736bc`.
  No frozen artifact was edited.

### Primitive and falsifiability

Test the first graph-transform invariance condition for the affine stable
tangent used as the terminal target in the previous shooting certificate.
At `b=h/2`, first isolate the unique root `a_1` of

```text
A_1(a,b)=0,
```

where one connector cycle is the complete 14-collision/eight-return word.
Its image is exactly `(A,B)=(0,B_1)`.  Restart the collision map at that exact
unstable coordinate and interval-enclosed `B_1`; if the next-cycle unstable
coordinate is nonzero, the affine tangent is not an invariant graph.  Loss of
interval-Newton inclusion, a collision/clearance failure in either cycle, or
an interval defect containing zero falsifies the certificate.

### Certified falsification

The new 400-bit Arb certificate
`cm2_connector_two_cycle_invariance_cert.py` gives the strict interval-Newton
inclusion

```text
X = -6.0395874244980567528944144913972582175e-12 +/- 3.28e-50,
N(X) = -6.039587424498056752894414491397258217522753433829...e-12
       +/- 7.69e-87,
dA_1/da = 1.075002041701258975...e8 (strictly positive).
```

Thus `A_1=0` has exactly one root in `X`.  Propagating only its stable image
coordinate and imposing the root equation exactly gives

```text
B_1 = 1.0930661969930523e-12 +/- 3.91e-29,
A_1(0,B_1) = 1.656412904242181e-19 +/- 2.85e-35 > 1e-20.
```

The second enclosure uses a centered mean-value form: the point value at the
midpoint of `B_1` plus an interval enclosure of `dA/db` times
`B_1-mid(B_1)`.  This reconditioning retains the exact equation `A_1=0` and
avoids the dependency blow-up of a raw 28-collision natural interval
extension.  Both constituent physical words are separately audited.  Across
the two cycles the certificate retains, cycle by cycle,

```text
minimum flight                         > 0.18,
minimum target discriminant            > 0.01,
minimum incidence cosine               > 0.63,
minimum unintended-obstacle clearance  > 0.22,
transparent-wall crossings             = 0.
```

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_two_cycle_invariance_cert.py
python3 -m py_compile \
  deliverables/cm2_connector_two_cycle_invariance_cert.py
```

### Scope: proved and not proved

- **Proved:** the center stable tangent is not invariant at the certified
  nonzero stable image coordinate; its next-cycle unstable defect is strictly
  positive.  Therefore the affine terminal target in the previous shooting
  certificate cannot itself serve as a graph-transform invariant edge.
- **Proved:** the defect is a regular nonlinear effect, not a wrong collision
  word or a grazing artifact: all 28 declared collisions / 16 fixed-section
  returns have the displayed strict physical margins.
- **Not proved:** the one-cycle shooting root is off the true stable manifold.
  A curved invariant graph can pass through its image even though the affine
  tangent is not invariant.  The result supplies a signed curvature seed, not
  a stable-manifold exclusion.
- **Not proved:** a curved stable graph tile, continuation to a full crossing,
  unstable boundary inequalities, or strip-uniform cone margins.  Hence
  `COMMON_MAGNET` remains open; this is not physical PPE or CM2.

### Next kill test

Replace the affine target by a quadratic graph `a=c b^2` in both source and
image tiles.  Use the certified positive defect to seed `c`, then apply an
interval Newton/Krawczyk test to the two-tile invariance residual and its
coefficient derivative on a rational `b`-interval around `B_1`.  Stop if the
coefficient box fails strict inclusion or the physical/cone margins fail;
otherwise certify the first genuinely curved graph-transform tile before any
attempt at full crossing.

## 2026-07-15 01:14 CST — COMMON_MAGNET nonlinear connector shooting tile

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every v52-manifest entry,
  including the frozen v51 TeX hash
  `da0f4ac7a6a914bef07d4e3b05d844803b251cc60f2486f943aca3abed4736bc`.
  No frozen artifact was edited.

### Primitive and falsifiability

In the rationalized connector eigen-chart

```text
(s,p)=a(1,k)+b(1,-k),
k=6.779460879726527387379610900452...,
```

fix `b=h/2`, where `h` is half the connector--QNL center separation.  Let
`A_8(a,b)` be the unstable coordinate after the complete period-eight
connector word.  Test the scalar shooting equation

```text
A_8(a,h/2)=0.
```

Failure of interval-Newton inclusion, loss of derivative transversality, any
wrong/early collision, a transparent-wall crossing, or loss of a stated
physical margin falsifies this tile.  The chart origin and slope are fixed
exact decimal rationals lying respectively in the Krawczyk root box and the
certified center eigen-slope ball; this avoids iterating a dependent interval
parameter while retaining an exact chart.

### Certified result

The new 400-bit Arb certificate
`cm2_connector_stable_shooting_tile_cert.py` proves on

```text
X = -6.039587424498056752898335641043...e-12 +/- 1e-25
```

that

```text
dA_8/da > 10^8,
N(X) = -6.0395874244980567528944e-12 +/- 2.29e-35
     subset interior(X).
```

The one-dimensional interval-Newton theorem therefore gives exactly one
root in `X`.  In particular the nonlinear source correction is rigorously
negative and separated from zero by `5e-12`; the straight choice `a=0` is
not the corrected source on this slice.  On the whole root box the declared
14-collision/eight-return word is physical, with strict aggregate bounds

```text
minimum flight                         > 0.1,
minimum target discriminant            > 0.01,
minimum incidence cosine               > 0.5,
minimum unintended-obstacle clearance  > 0.15,
transparent-wall crossings             = 0.
```

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_stable_shooting_tile_cert.py
python3 -m py_compile \
  deliverables/cm2_connector_stable_shooting_tile_cert.py
```

### Scope: proved and not proved

- **Proved:** at `b=h/2`, the eight-return preimage of the connector center's
  stable tangent is a unique regular point in the displayed nonzero interval;
  the shooting derivative has a strict `10^8` transversality margin.
- **Proved:** the nonlinear correction required at this slice is nonzero and
  the complete physical itinerary survives on its whole certified interval.
- **Not proved:** the root is on the true local stable manifold.  The target
  condition is the center stable *tangent*, not an invariant curved graph;
  finite-time tangent shooting must not be substituted for stable-manifold
  invariance.
- **Not proved:** continuation in `b`, full crossing, stable/unstable boundary
  inequalities, or strip-uniform cone margins.  Thus `COMMON_MAGNET` remains
  open, and this is not physical PPE or CM2.

### Next kill test

Replace the terminal tangent condition by a two-tile graph-transform
invariance equation.  On a small rational interval around `b=h/2`, solve
simultaneously for a source graph `a=g(b)` and its image graph, using interval
Newton/Krawczyk plus the certified cone.  Stop if dependency prevents strict
inclusion or any itinerary margin fails; otherwise certify the first genuine
curved stable-manifold continuation tile and its overlap with the present
shooting root.

## 2026-07-15 00:54 CST — COMMON_MAGNET affine stable-centerline falsification

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every v52-manifest entry,
  including the frozen v51 TeX hash
  `da0f4ac7a6a914bef07d4e3b05d844803b251cc60f2486f943aca3abed4736bc`.
  No frozen artifact was edited.

### Primitive and falsifiability

Primitive: test the most economical cone-aligned common rectangle rather than
extrapolating the center cone.  In connector center coordinates let

```text
e_+=(1,k), e_-=(1,-k),
k=sqrt((J_B)_{21}/(J_B)_{12}),
(s,p)=a e_+ + b e_-.
```

If `Delta_s` is the certified connector--QNL center separation, the two
centers are opposite vertices of the product hull
`P_0={0<=a,b<=h}`, `h=Delta_s/2`.  Propagate the midpoint `(a,b)=(0,h/2)`
of the connector-side stable edge through the complete connector word.  A
failed target collision, earlier unintended obstacle, wall crossing, weak
margin, or an output satisfying `a_out<=h` falsifies this certificate.

### Certified falsification

The new 400-bit Arb script
`cm2_connector_affine_hull_falsification.py` proves

```text
k                         = 6.779460879726527...,
Delta_s                   = 0.000470018116028112...,
h                         = 0.000235009058014056...,
source momentum           = -0.000796617357593838...,
output momentum           > 0.004,
a_out                     = 0.00064926730... > 2h > h.
```

The test point follows all 14 solid collisions / eight fixed-section returns
of the declared connector word.  The strict aggregate physical margins are

```text
minimum flight                         > 0.1,
minimum target discriminant            > 0.01,
minimum incidence cosine               > 0.5,
minimum unintended-obstacle clearance  > 0.15,
transparent-wall crossings             = 0.
```

Thus the forward-containment failure is dynamical, not a collision singularity or an
interval-arithmetic ambiguity: the image of a point on the stable edge of
`P_0` lies beyond its opposite unstable face by a factor greater than two.
Consequently that straight edge cannot be used as the centerline of a
full-height connector stable strip whose image is required to remain in
`P_0`.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_connector_affine_hull_falsification.py
python3 -m py_compile \
  deliverables/cm2_connector_affine_hull_falsification.py
```

### Scope: proved and not proved

- **Proved:** the connector-side straight stable edge of the minimal affine
  product hull fails forward containment, despite a completely regular
  physical connector word at the witness point.
- **Falsified:** using the center derivative eigendirections as straight
  centerlines all the way across the common-center scale when their image is
  required to remain in the minimal product hull.  Center cone invariance
  alone does not control the required nonlinear stable graph.
- **Not proved:** nonexistence of a curved common rectangle, an enlarged
  straight rectangle, a shifted straight strip, or a thinner
  singularity-cut full-cross strip.  A Markov rectangle is not required to
  be globally trapping, so the certified point does not by itself exclude a
  different full-cross construction.
  Therefore `COMMON_MAGNET` remains open; this is not physical PPE or CM2.

### Next kill test

Replace the failed straight stable edge by an interval Newton graph for the
local connector stable manifold.  Continue that graph in certified tiles
until its stable coordinate reaches `b=h/2`.  Stop at the first tile where
the graph-transform cone, connector itinerary, or singularity margin fails;
otherwise compare the curved edge image with a quantitatively enlarged
common rectangle.  No affine center-cocycle extrapolation may be used.

## 2026-07-15 00:34 CST — COMMON_MAGNET common one-return regular tube

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  `sha256sum -c` passed every v52-manifest entry,
  including the frozen v51 TeX hash
  `da0f4ac7a6a914bef07d4e3b05d844803b251cc60f2486f943aca3abed4736bc`.
  No frozen artifact was edited.

### Primitive and falsifiability

Primitive: on a genuinely two-dimensional tube containing both the corrected
connector center and the exact QNL center, exhaust the first two physical
flights and test whether the common fixed-section one-return word is

```text
G(0,0) -> W(0,0) -> G(0,0).
```

Any tile with a nonpositive target discriminant or flight, a possible earlier
unintended solid collision, a transparent-wall crossing, or loss of target
transversality falsifies the certificate.  The finite lift enumeration is the
frozen certificate's `[-3,3]^2` gray/white enumeration; its separate exact
lattice-exhaustion argument applies unchanged.

### Certified result

The new 400-bit Arb script `cm2_common_one_return_tube_cert.py` covers the
closed rational tube

```text
theta in 0.785 +/- 0.001,    p in 0 +/- 1e-5
```

by `16 x 4` closed interval tiles.  It verifies that the tube contains both
`ROOT_CENTERS[0] +/- 1e-50` and `pi/4`, evaluates both target collisions, and
checks every other gray/white lift in the finite exhaustive lattice on both
segments.  The rigorous aggregate output is

```text
COMMON_ONE_RETURN_TUBE: CERTIFIED
minimum_flight                         > 0.17
minimum_target_discriminant            > 0.02
minimum_incidence_cosine               > 0.9
minimum_unintended_obstacle_clearance  > 0.12
transparent_wall_crossings             = 0
```

The displayed Arb balls are respectively approximately
`[0.2 +/- 0.0219]`, `[0.0256 +/- 4.79e-5]`,
`[1 +/- 0.0352]`, and `[0.364 +/- 4.90e-4]`; only the rational strict
lower bounds above are theorem statements.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_common_one_return_tube_cert.py
python3 -m py_compile deliverables/cm2_common_one_return_tube_cert.py
```

### Scope: proved and not proved

- **Proved:** no first-hit singularity separates the corrected centers.  They
  lie in one explicit nonzero regular source tube for the same physical
  fixed-section one-return branch, with uniform singularity/clearance margins.
- **Proved:** the earlier center-coordinate correction is geometrically
  compatible with a common local chart; the horizontal grazing identified in
  the falsified separator certificate does not intervene.
- **Not proved:** continuation of all eight fixed-section returns of the
  connector on a source strip, full crossing of a common rectangle, or a
  strip-uniform cone graph transform.  This certificate is not a Markov
  rectangle or physical PPE theorem.  Therefore `COMMON_MAGNET` remains open.

### Next kill test

Propagate a cone-aligned interval source box about the connector through its
complete eight-return word, using the certified one-return tube as the common
rectangle.  Compare the eight-return image of both unstable boundary faces
with the two stable faces.  Either certify a nonzero full-cross connector
strip together with a uniform derivative cone margin, or isolate the first
return index/tile whose itinerary or face inequality fails.  Do not infer
full crossing from the center derivative alone.

## 2026-07-15 00:14 CST — COMMON_MAGNET center-coordinate audit

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before new work.  The v52 manifest passed all entries; the v51
  TeX hash is
  `da0f4ac7a6a914bef07d4e3b05d844803b251cc60f2486f943aca3abed4736bc`.
  No frozen artifact was edited.

### Primitive and falsifiability

Primitive: audit the claimed common global gray-angle chart used by the
previous center-hull separator.  The exact QNL orbit is the normal flight
from `G(0,0)` to `W(0,0)`, whereas the connector's first certified collision
is represented by `ROOT_CENTERS[0]`.  Failure of the exact normal-flight
identity or any strict Arb ordering would falsify this audit.

### Certified falsification

The new 400-bit Arb script `cm2_common_center_coordinate_audit.py` proves

```text
asin(9/25) < theta_connector < theta_QNL = pi/4,
0.0013 < theta_QNL-theta_connector < 0.0014,
0.00046 < (9/25)(theta_QNL-theta_connector) < 0.0005.
```

It also verifies directly that the gray point with outward-normal angle
`pi/4` and the facing point of `W(0,0)` are collinear, with positive normal
flight

```text
1/sqrt(2) - 9/25 - 4/25 > 0.18.
```

Therefore the previous script
`cm2_common_rectangle_separator_cert.py` used incompatible coordinates when
it set `qnl_angle=0`.  Angle zero is the horizontal gray--gray normal orbit,
not the diagonal gray--white QNL orbit.  The grazing at `asin(9/25)` lies far
below both relevant centers and is not inside their center hull.  Hence the
claimed `COMMON_REGULAR_CENTER_HULL` falsification and its proposed frozen
separator are themselves **falsified** and must not be used.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_common_center_coordinate_audit.py
```

### Scope: proved and not proved

- **Proved:** the two relevant centers are adjacent in one global gray
  arclength--momentum chart, with a rigorous center arclength gap below
  `1/2000`; the earlier horizontal grazing is not between them.
- **Falsified:** the 2026-07-14 23:54 center-hull separator conclusion and its
  next-test requirement to cut at `s_*=(9/25)asin(9/25)`.
- **Unaffected:** the center derivative cone certificate uses the exact QNL
  derivative and connector derivative; it does not rely on the erroneous
  center angle and remains only a center-cocycle subgate.
- **Not proved:** a regular neighborhood joining the corrected centers,
  full-cross strips, or strip-uniform cone and singularity margins.  Thus
  `COMMON_MAGNET` remains open.

### Next kill test

Build the interval first-return evaluator on the corrected narrow center hull
`theta in [ROOT_CENTERS[0], pi/4]`, beginning at `p=0`.  Exhaust all first-hit
discriminants for the connector word and the QNL gray--white word.  Either
certify a nonzero common regular tube around this hull, or isolate the first
intervening event surface with a strict interval box and use it as the next
necessary strip cut.

## 2026-07-14 23:54 CST — COMMON_MAGNET center-hull separator

### Frozen baseline

- Read `AGENTS.md`, the v52 stop--go ledger and manifest, and the Paper 1
  blueprint before doing new work.
- `sha256sum -c deliverables/cm2-v52-manifest.sha256` passed all nine entries.
  There is no separate `cm2-v51-manifest.sha256`; the frozen v51 TeX was
  therefore checked directly against the ledger value
  `da0f4ac7a6a914bef07d4e3b05d844803b251cc60f2486f943aca3abed4736bc`.
  No frozen artifact was edited.

### Primitive and falsifiability

Primitive: test whether the convex hull of the QNL period-two center and the
certified period-eight connector center can be a single regular return-map
chart.  On their common zero-momentum line, locate the first horizontal-gray
grazing event and certify both its ordering between the centers and its
physical first-hit status.  Failure of an ordering, tangency, clearance, or
event-submersion Arb inequality falsifies the certificate.

### Certified result

The new 400-bit Arb script `cm2_common_rectangle_separator_cert.py` imports
the frozen v52 orbit definitions without changing them.  In common gray
normal-angle coordinates the two centers have

```text
theta_A = 0,
theta_B = 0.78409255751959244111062347294760048...,
p_A = p_B = 0.
```

The normal ray from `G(0,0)` is tangent to `G(1,0)` at the exact angle

```text
theta_* = asin(9/25) = 0.36826789343663998093230247350803724... .
```

Thus `theta_A < theta_* < theta_B`.  In arclength `s=(9/25) theta`, the two
strict center-to-event gaps are respectively

```text
s_* - s_A = 0.13257644163719039313562889046289340... > 13/100,
s_B - s_* = 0.14969687906986288566419555979824276... > 7/50.
```

The certificate exhausts periodic gray/white obstacles, proves that the
tangent flight is forward, and finds minimum clearance from every other
obstacle

```text
0.12647615158762403766993223020364664... > 1/10
```

at `W(0,0)`.  The gray-target collision discriminant has derivative

```text
-2 sin(theta_*) cos(theta_*)
 = -0.67172565828617861424470241149325117... < -2/3,
```

so this is a regular codimension-one grazing germ, not an unresolved critical
event.  Consequently any convex rectangle containing both centers meets a
return singularity on its zero-momentum chord: a common rectangle cannot be
treated as one globally regular chart.  This falsifies only
`COMMON_REGULAR_CENTER_HULL`, not a singularity-cut common magnet.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_common_rectangle_separator_cert.py
```

### Scope: proved and not proved

- **Proved:** an exact, physically unobstructed, uniformly regular grazing
  separator lies strictly between the two periodic centers, with explicit
  arclength, event-submersion, and other-obstacle margins.
- **Falsified:** any argument that continues one regular return branch over
  the entire convex center hull.
- **Not proved:** existence of two singularity-cut full-cross strips in one
  rectangle, strip-uniform cones, or all other first-hit/singularity margins.
  Therefore `COMMON_MAGNET` remains open.

### Next kill test

Use `s_*=(9/25)asin(9/25)` as a frozen separator and implement an interval
first-return evaluator on two disjoint candidate strips.  Start with the QNL
side: bisect a cone-aligned strip contained in `s<s_*` until either its two
unstable boundary graphs cross a rectangle spanning both center arclengths
with every collision discriminant and first-hit clearance strict, or one
specific interval box meets another singularity.  The latter is the next
explicit falsification certificate and determines the needed additional cut.

## 2026-07-14 23:33 CST — COMMON_MAGNET center-cone subgate

### Frozen baseline

- Ran `sha256sum -c deliverables/cm2-v52-manifest.sha256`: all nine v52/v51
  entries passed.  No frozen artifact was edited.
- The system interpreter lacks `python-flint`; the recorded environment
  `/tmp/cm2-flint-venv` has exactly `python-flint==0.9.0`.

### Primitive and falsifiability

Primitive: determine whether the exact QNL period-two loop derivative `J_A`
and the certified period-eight connector derivative `J_B`, in their common
gray arclength--momentum chart, admit one strict unstable cone and one strict
backward stable cone at the two periodic centers.  Failure of any displayed
strict Arb inequality falsifies this center-cocycle subgate.

### Certified result

The new independent script `cm2_common_center_cone_cert.py` imports the frozen
v52 certificate without changing it and proves the following finite theorem.
Let

```text
C+ = {(x,y): x>0, y>0},    C- = {(x,y): x>0, y<0}.
```

For both `M=J_A,J_B`,

```text
M(C+) subset {(x,y): x>0, 13/2 < y/x < 7},
M^{-1}(C-) subset {(x,y): x>0, -7 < y/x < -13/2}.
```

Moreover `M` on `C+` and `M^{-1}` on `C-` expand the `l1` norm by a factor
strictly greater than `6`.  The proof reduces the projective inclusions to
four endpoint inequalities for each positive `2 x 2` matrix and checks all
of them with 400-bit Arb balls.  The smallest printed strict margins occur
for `J_A`: upper projective numerator margin `>0.08887` and forward/inverse
`l1` column-sum lower bound `>6.4057`.  Thus the center derivatives do not
obstruct a common cone field.

Reproduction:

```bash
/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_common_center_cone_cert.py
```

### Scope: proved and not proved

- **Proved:** one common strict pair of center cones, with explicit rational
  slope bounds and expansion margin, for the exact QNL loop and certified
  period-eight loop in the same trivialization.
- **Not proved:** continuation of these cones over any two-dimensional strip;
  existence of a common rectangle; either word's full crossing; strip-uniform
  avoidance of grazing, transparent walls, unintended obstacles, and
  singularity preimages.  Therefore `COMMON_MAGNET` remains open.
- No conditional strip weights, stopped-parent law, or physical endpoint PPE
  identification is claimed.

### Next kill test

Construct an interval return-map evaluator on a proposed common rectangle
centered between the two certified periodic points.  Subdivide the candidate
period-eight domain until either (i) two opposite boundary graphs map across
the rectangle while every flight retains strict first-hit/grazing clearance,
or (ii) an Arb box meets a discriminant/first-hit singularity, which falsifies
that rectangle and supplies the offending occurrence and box.

# Seventh direct assault — maximal rows and global Borel current (2026-07-15)

- Replaced the sixth assault's rectangular/analytic fragments with a global
  source-normal-circle arrangement on all 128 active signed sheets.  The
  exhaustive registry has 328 candidate curves: 256 source-grazing, 24
  forward `u_y=0`, 16 first-visibility and 32 miss-owner curves.
- Proved all 320 same-sheet candidate pairs disjoint over the full parameter
  window.  Of these, 304 certify without subdivision and 16 after one
  bisection.  The cyclic order is therefore constant.
- Exactly 88 candidates change physical state: 32 source-grazing, 16 first,
  32 miss and 8 polarity boundaries.  Their 128 endpoint incidences give 64
  maximal connected physical rows with 64 unique chart-free labels and 16
  `Jx/Jy` four-row orbits.
- Attached the exact law `dm=R_source*cp^2*abs(u_y)/ell_T dtheta` and the
  signed hit-minus-miss current to every maximal row.  Exact geometry gives
  `ell_T^2 >= 36337/3260000 > 1/100` and the global unnormalised TV bound
  `16128/5`.  The current is a finite Borel measure with no endpoint atoms.
- Constructed 32 exact `Jx` row pairs, proving equality of positive laws and
  global signed scalar coarea mass zero.
- Rebound the Gate-4/5 structural ledger to the true maximal partition: 64
  common laws, 64 symbolic one-charge `q` expressions and 128 singular Kac
  coordinates sharing 64 `(+1,-1)` marks.  The finite-Borel four-term Kac
  layer is global.
- Full transfer DQ convergence, dynamic-`C^1`/CM2 bounds, numeric
  `C_fw,C_rev`, controlled stopped recovery and all three CM2 norm lifts
  remain open.  The strict unconditional verdict remains `NO-GO FOR CLAIM`,
  with `0/5` composite gates fully closed.
- Latest-theory audit included `arXiv:2604.19671v2` (Sinai billiards with
  small holes), `arXiv:2602.02317v2` (contracting fibres) and
  `arXiv:2505.17462v1` (cusp systems).  None directly supplies the missing
  moving-scatterer DQ/recovery/CM2 hypotheses.

# Twelfth direct assault — finite-parameter outer atlas and global Growth frontier (2026-07-16)

- The strict unconditional verdict remains `NO-GO FOR CLAIM`, with `0/5`
  composite gates fully closed.  Frozen v51/v52 and the first eleven rounds
  were not rewritten.
- Gate 1 now has a compact determinant-one `C^{1,alpha}` logarithmic gauge for
  every `0<alpha<1`, separately all-pairs holonomies on the declared local
  stable and unstable plaques, and exact finite-word gauge telescoping.  In
  the clean `F=T^2` cyclic component, regular phase-aligned homoclinic points
  have existentially typed endomorphisms
  `psi_z=H^s_(z,p) o H^u_(p,z)`.  No immutable physical homoclinic itinerary,
  plaque-entry times, numerical loop, four twisting wedges, uniform
  all-plaque family, class `H`, or PPE is certified.
- Gates 2/5 contain at least 24 uniformly nonempty physical return keys with
  collision-SRB core mass `>147/550000`: 20 roof-one and four roof-two keys.
  Only the full-key nonemptiness field is decided in the 18-field schema;
  seven core-local typed seeds are not seven completed operator fields.
  Stable saturation, quotient `rho`, PPE, maximal word domains, all roof
  levels and all complete 18-field blocks remain absent.
- Gate 3 replays all 4,704 next-collision candidate discriminants on the
  common 64-row carrier over `|s|<=1/400`.  It retains the corrected row law
  `dm=R_source*cp*abs(u_y)/ell_T dtheta`, with no second `cp` factor.  The
  replay yields 626,274 immutable boxes, 6,246 strict candidate-root strips
  and 129,634 unresolved boxes after 1,507,924 interval audits.  The
  nonphysical normalized `(t,v)` outer-cover fraction is `68501/2097152`.
  The exact fixed-`s` row-law outer charge is
  `64953/1280+(547092/5)rho`; its positive intercept prevents a complete
  root-isolated atlas.  Artificial rectangular `t`-boundary charges are
  distinguished from full rectangle boundaries, candidate roots are not
  physical-first, and no physical current, operator DQ, `MT_DQ` or face
  theorem is promoted.
- Gate 4 obtains raw signed-tangency-sheet intersection bounds 152/136 and
  hence the conservative one-step component ceiling 153.  Every physical
  continuity branch has `||D2T||_infinity<42672/c_1^3`; the 128 certified
  carriers have one-step projected-`r` log-Jacobian distortion
  `<74750/c_1^2`.  The crude proof bound
  `137751561/901685>1` is not a physical lower bound and gives no Growth
  contraction.  The existing ledgers do not identify the required weighted
  component join.
- On normalized cumulative-mass `u`, any finite retained partition with
  atom diameter at most `delta` has conditional leafwise inverse-mass cost
  at least `1/delta`; this is only a resolution--cost obstruction for the
  present `p_a^-1` interface, not a no-go for unnormalised or reweighted
  recovery families.
- Official arXiv queries found no theorem closing the missing physical
  interfaces.  All seven new stacks compile and replay, reject the mutation
  vector `9/18/11/11/9/9/11` (78 total), and live-fail-close with exit 2.
  The Gate-3 materialization and independent replay both reproduce the same
  row/work/slice digests.  Direct SHA sidecars, the twelfth subordinate and
  aggregate chains, and frozen v52 pass at final freeze.

# Twenty-third direct assault — first stopping and local return tail (2026-07-18)

- Strict unconditional CM2 remains `NO-GO FOR CLAIM`, with `0/5` composite
  gates fully closed.  All work is append-only from the twenty-second root.
- Re-audited the exact charged source boxes for all 64 maximal occurrences.
  The physical row label, hit-side global tangent/suffix owners, miss-side
  global suffix owner and common suffix chart pass on all 64 occurrences.
- Classified every displayed source-to-core word against the full frozen
  24-core union at 2048 bits.  All 128 hit/miss positive cylinders now have
  genuine suffix-relative first-core stopping times: 756 strict preterminal
  outside-core states, 128 strict terminal core interiors, no ambiguity and
  maximum time 20.  This is not whole-germ coverage or normalized mass.
- Classified all 8,072 post-core states on the four materialized `2^-8000`
  source-current boxes at 8192 bits.  Three boxes have strict first returns at
  times 545, 649 and 1531; the fourth has no return through 2018.  On the four
  equal labelled boxes the exact truncated tail is `1,3/4,1/2,1/4` over the
  corresponding time ranges.  This labelled coordinate tail is not a
  collision-SRB probability, and the survivor is not nonreturning cemetery.
- Materialized the exact 64-occurrence / 128-parameter-side join through 14
  destination cores to 14 selected roof-one components and their existing
  field-1 entities and field-3/4/7 slots.  No new Gate-5 field is promoted;
  maturity remains `4/18`, complete tables and blocks remain zero.
- Froze the typing guard that parameter side, fw/rev recovery orientation and
  the two singular Kac output coordinates are distinct axes.  Each occurrence
  retains one `q_e=max(C_fw,C_rev,2)m_e` charge; no positional zip or repeated
  orientation/Kac charge is permitted.
- Latest technology check revisited arXiv `2604.19671v2` and
  `2606.10155v1`.  Standard-family/coupling ideas may inform a physical
  survivor-tail attack, but neither paper supplies the CM2 source/core
  partition, common restriction, weighted tail or induced strong coefficient.
- Three new replay suites pass and reject `38/38 + 43/43 + 46/46 = 127/127`
  hostile mutations.  Together with the previous `420/420`, the cumulative
  count is `547/547`.  All leaf and recursive twenty-third SHA chains pass;
  live modes exit 2.
- Next shortest line: materialize the full physical first-return partition
  `R_n=M_C L(M_Cc L)^(n-1)M_C` and survivor operators, attach exact mass and
  strong `q` envelopes, prove a collision-SRB weighted excursion/cemetery
  tail and derive a new induced Lasota--Yorke coefficient without reusing the
  fixed-core `b_core` along excursions.

# Twenty-fourth direct assault — source-clock ledgers and Kac baseline (2026-07-18)

- Strict unconditional CM2 remains `NO-GO FOR CLAIM`, with `0/5` composite
  gates fully closed.  All work is append-only from the twenty-third root.
- Upgraded all 128 charged cylinders from a regular-suffix clock to a true
  source-collision clock.  All 64 source states and 64 hit pre-suffix states
  are strictly outside the full 24-core union; the 128 source-relative words
  contain 948 strict preterminal outside states, 128 unique terminal core
  interiors, no ambiguity and maximum time 22.
- A raw-domain audit initially rejected 124 occurrence-only joins to the old
  selected-germ ledgers.  New exact-domain registration then created 128
  canonical charged Borel ledgers without reusing an old germ.  Their finite
  prefixes contain 128 nonempty destination slots, 25,696 empty core slots
  and 1,076 empty singular slots.  No post-stop/nonreturning slot or
  collision-SRB mass is manufactured.
- Applied Poincare recurrence and the general Kac formula parameterwise to
  the complete 24-core union.  Aggregate collision-SRB first-return masses
  sum to the core mass, nonreturning and singular-orbit parts are null, and
  `E_mu_C tau_C^+<550000/147`.  The tail sum has the same strict upper bound;
  Markov gives `P_mu_C(tau>n)<550000/[147(n+1)]`.  The measurable induced
  `L1(mu_C)` Perron operator exists with norm one, but is not a
  branch-materialized CM2 strong operator.
- Froze the open-to-induced signature obstruction: `b_core` belongs to
  `O=L M_C`; a two-state exact model has `||O^2||_1=0` while `||R_2||_1=1`.
  The existing open power cannot be reused as the induced Lasota--Yorke
  coefficient.  Gate-5 maturity stays `4/18`; fields 5/6 remain local seeds
  with zero completed roof-level slots.
- Re-audited Gate 2 field by field.  Core IDs, hit/miss sides, fw/rev views,
  Kac coordinates and first-core clocks do not create stable fibres, inverse
  branches or reverse weights; all 17 physical fields remain false.  The
  shortest new input is `Lambda_A`, invariant stable plaques, `I_A`, `pi^s`
  and the conditional stable-holonomy SRB Jacobian.
- The current open-billiard literature does not directly upgrade Kac to an
  exponential survivor tail on the 24 phase rectangles.  The missing
  theorem interface is uniform core-specific `O1'/O2`, a mass upper bound,
  quantitative small-hole admissibility or a sparse recovery block, and one
  compact-parameter family of cone/admissibility constants.
- Six new verifier suites reject `339/339` hostile mutations.  Together with
  the previous `547/547`, the cumulative count is `886/886`; all leaf and
  recursive twenty-fourth SHA chains pass and every live mode exits 2.
- Next shortest paired line: certify the uniform 24-core open-hole interface
  for an unweighted exponential survivor tail, while adaptively materializing
  the full two-dimensional `R_n/Q_n` return partition with branch mass,
  Jacobian, distortion, common restriction and strong `q` payloads.
# 2026-07-19 — thirtieth direct assault

Completed two new full replays. The all-Q2 time-three depth-two adaptive ledger
enlarges certified finite-open Q3 cells from 4,088 to 38,972 while conserving
exact mass and retaining 388,890 unresolved cells. The Q2 target-chart seam
refinement was extended from additional depth six to eight, enlarging the F4
strict single-chart subledger to 272,056 cells and reducing residual seam mass
to `2559121/655360000000`. A current arXiv audit found useful exact symbolic
Hausdorff-measure holonomy, but no physical Green-rate/cohomology theorem that
closes Gate 1. Composite gates remain `0/5`; CM2 remains no-go.
# 2026-07-19 — thirty-first direct assault

Materialized the missing Q2 parent-W identity layer as a standard-Borel
parameterized unstable foliation `phi(r)=4r+b`, fixed-s and naturally recut at
adapted arclength `1e-90`. This joins actual parameterized curve IDs to all
228,012 branch rules and supplies instance-level recut endpoint formulas,
while leaving the family-integrated F7 charge unpromoted. A second complete
384-bit replay extended time-three refinement to depth four: finite Q3 cells
rose to 244,704 and unresolved mass fell by the exact factor
`36334055/40380532` relative to depth two. The factor is not iterated as a
uniform tail. All composite gates remain open.
# 2026-07-19 — thirty-second direct assault

Round 31's parent-W identity layer unlocked two same-ID joins. The global
componentwise growth coefficient and invariant density ratio now materialize
numeric F7 on every parameterized Q2 recut instance, with one-step coefficient
`360134800/360493663<1`. The fixed leaf slope 4 also makes every physical
carrier intersection empty or one simple root, assigns connected rank zero,
and gives a common normalized F8 transversality lower `1/5` across all five
face grammars. Gate-5 maturity advances to `6/18`. The carrier C2 constant
`4949` was not retyped as physical-face F9, and the occurrence coarea bound
`18/5` was not promoted to all-face F10.

# 2026-07-19 — thirty-third direct assault

Replayed all 114,006 Q2 anchors through additional time-three depth six at
384-bit Arb precision. Strict Q3 cells rose to 965,360 and unresolved mass
fell by the exact depth-six/depth-four ratio `24955951/29067244`, without
promoting that finite ratio to a uniform tail. A current theorem audit found
that arXiv `2604.19671` gives every-step small-hole response only for one
full-angle boundary strip, not the 24 rectangular C24 holes; the projective
cone route still lacks an unnormalized recovered-density C24 hit lower.
Gate 5 gained safe typed subatlases: all 96 stationary source-core faces have
F9/F10 equal to zero, and all 64 moving occurrence seeds have parameterized
F10 derivative bounds `(4374/125)2^B` and `(8424/125)2^B`. Arbitrary-time
pullback faces keep global F9/F10 open, Gate-5 maturity stays `6/18`, all five
composite gates remain open, and CM2 remains no-go.

# 2026-07-19 — thirty-fourth direct assault

Corrected the round-33 frontier: the frozen 2026-07-18 sparse-hit certificate
already supplies the recovered-cone C24 hit gap `21/111718750` and the uniform
unweighted exponential return tail with block factor
`111718729/111718750`; only the block length remains nonnumeric.  The true
Gate-4 tail gap is the physical strong `q`/cemetery transfer.  All 96 C24
faces were rewritten as affine Birkhoff levels, yielding the exact rank-path
recurrence `H_j=M_j D_(j-1)^2+L_j H_(j-1)` and strict curvature bound
`<(3/2)E_jH_j` for intermediate and terminal core preimages on every finite
regular homogeneous path.  A separate endpoint-tail summation certifies the
one-collision coarea moment
`integral(151*2^B)^(3/2)dm<91292149088554323/13750`, without retyping it as a
fixed-`s` physical first-return `L^p` envelope.  The first true tail blocker
is now a same-ID common fw/rev recovery carrier with numerical `q/m` rows;
strong cemetery and F14--F18 remain absent.  Numeric Q2 F7 remains certified
on all 228,012 base rules, but no arbitrary-depth Rn F7 charge follows from it.
Both suites reject `31/31`
hostile mutations, Gate-5 maturity remains `6/18`, all five composite gates
remain open, and CM2 remains no-go.

# 2026-07-19 — thirty-fifth direct assault

Bound every existing nonempty regular finite-depth `R_n` component to an
actual standard-Borel slope-four parent-W registry and one common fw/rev
physical restriction.  Collision SRB disintegrates with leaf density
`cp/sqrt(17)`; area preservation and time reversibility give equal masses on
both views.  Every positive carrier cell now has the numerical recovery clock
`R(D)<=301500+1005D`, while the global physical retained-depth law remains
open.  A separate direct collision-SRB argument proves
`mu{B_inc>b}<2*4^-b` and
`integral 2^(3B_inc/2)dmu<134217735/64`.  On physical first-return components,
the additive charge `c_D1,n=151 sum_i 2^B_i` has a global `L^(6/5)` moment
`<K_rank*N_open^3` and an exponential weighted tail with block exponent
`1/6`.  This is not complete `C_fw,C_rev`, final `q`, or cemetery.  Both
suites reject `33/33` hostile mutations; Gate-5 maturity remains `6/18`, all
five composite gates remain open, and CM2 remains no-go.

# 2026-07-19 — thirty-sixth direct assault

Selected the aggregate unnormalised standard-family boundary ledger as the
primary Gate-4 route.  It obeys
`Z_(n+1)<=a Z_n+b m_n+J_n` with
`a=360134800/360493663<1`, exact resolvent `360493663/358863`, and an
explicit weight `w=720628463/720269600` satisfying `wa<1`.  A correlated-rank
countermodel proves that the Round-35 one-time rank tail and additive D1
moment do not logically control arbitrary-time image-recut products, so the
cellwise retained-depth route is deferred.  Gate 5 now has parameterized F9
on all five physical face grammars: all seven one-step boundary kinds receive
typed base C2 bounds and the arbitrary finite rank-path recurrence.  Maturity
advances to `7/18`.  Every closure-contained compact regular face germ also
has a positive analytic parameter radius and a terminating dyadic interval
search for a finite F10 integer, but no global radius, arbitrary-Rn face-row
enumeration or weighted F10 sum is certified.  The exact first missing object
is the weighted physical forcing
`J_n=J_core,n+J_owner,n+J_occurrence,n+J_cemetery,n`, followed by F12/F13,
cemetery and F14--F18.  Three suites reject `51/51` hostile mutations; all
five composite gates remain open and CM2 remains no-go.

# 2026-07-28 — Round251 wall p-face saturation

Certified all 3,240 strict same-chart, same-target wall p-face patches across
408 root pairs using explicit positive-area common faces and positive-volume
two-sided corridors at 256-bit interval precision.  Of these edges, 584
reduce quotient rank and 2,656 are independently certified redundant.  The
mixed-sheet quotient decreases from 82,620 to 82,036 components while all
116 exact-key quotient-fibre inventories remain complete and key-pure.  The
occurrence deficit remains 17,768 and no maximal-component, global-fibre, or
global-disposition promotion is claimed.  Independent producer/verifier
replays are byte-identical under `PYTHONHASHSEED=251071` and `251929`; report,
cold replay, and manifest are complete.  A full next-axis census finds 1,048
s-face root pairs with 22,008 cell contacts plus 256 cross-root t-face
contacts; signature intersection is necessary but will not be used as glue
without strict two-sided interval patches.  Gate5 stays 10/18 and CM2 stays
no-go.

# 2026-07-28 — Round252 wall s/t-face saturation

Exhaustively reconciled the final 22,264 same-chart/same-target wall contacts.
All 22,008 s-face contacts and 8 of 256 cross-root t-face contacts admit strict
256-bit two-sided positive-volume corridors with one closed ten-field return
signature.  The other 248 t-face contacts remain fail-closed: signature
intersection and coordinate touch do not produce a common strict corridor
through dyadic depth four and therefore receive zero physical-glue credit.
The 22,016 accepted edges contain 16,296 rank reductions and reduce the
mixed-sheet quotient from 82,036 to 65,740 components; cumulative certified
edges rise to 84,468.  All 116 exact-key quotient-fibre inventories remain
complete and key-pure, while occurrence incidence stays 36,200 / 53,968.
Independent producer/verifier replays are byte-identical under seeds 252071
and 252929.  The next core gate is the 17,768 unattached occurrence deficit,
starting with 16,444 Round179 resolved-child occurrences.  Gate5 stays 10/18
and CM2 remains no-go.

# 2026-07-28 — Round253 Round179 occurrence assignment

Reconstructed the exact Round244-to-Round251-to-Round252 component lineage and
assigned all 17,192 Round179 resolved-child occurrences to one saturated
mixed-sheet quotient component.  The assignments touch 7,932 components and
all 116 exact keys; 748 assignments are seeded and agree with their existing
known-block incidence, while 16,444 remain unseeded with no promotion.  The
full 53,968-row occurrence frontier and all 116 key rows were rebuilt, making
80 keys complete for all gauges present on those keys.  Independent replays
are byte-identical under seeds 253071 and 253929.  A follow-up seed audit finds
436 distinct known-block IDs across 440 seeded quotient components, so four
same-block component pairs must be merged before 2,020 compatible Round208
occurrences can be uniquely assigned.  The remaining 34,020 Round208 and 736
Round204 occurrences need new physical bridges.  Gate5 stays 10/18 and CM2
remains no-go.

# 2026-07-28 — Round254 seed-block quotient closure

Audited all 440 seeded quotient components and found 436 distinct existing
known-block IDs.  Four IDs each seeded two components, so the already-proved
block connectivity closes four quotient merges and reduces the component
count from 65,740 to 65,736.  The resulting unique block-to-component map
assigns 2,020 Round208 strict-open-region occurrences without creating any
new block incidence.  Cumulative occurrence-to-quotient assignments reach
19,212; 34,020 Round208 and 736 Round204 occurrences still require new
explicit physical bridges.  Independent replays are byte-identical under
seeds 254071 and 254929.  Maximality, global fibre exhaustion, and global
disposition remain open; Gate5 stays 10/18 and CM2 remains no-go.

# 2026-07-28 — Round255 block-carrier quotient augmentation

Added all 6,952 certified known blocks absent from the mixed-sheet quotient as
block-carrier components, yielding a unified 72,688-component lower-bound
quotient in which all 7,388 known blocks appear exactly once.  This assigns
33,432 previously block-only Round208 occurrences through their existing
block IDs and advances occurrence-to-quotient assignment to 52,644 / 53,968.
Only 588 sheetless Round208 and 736 Round204 occurrences remain unassigned.
No new incidence or maximality is claimed.  Independent replays are
byte-identical under seeds 255071 and 255929.  Gate5 stays 10/18 and CM2
remains no-go.

# 2026-07-28 — Round256 local occurrence-carrier completion

Materialized the last 1,324 unassigned occurrences as certified local
lower-bound carriers: 736 Round204 strict-open 3D regions and 588 sheetless
Round208 strict-open 3D regions.  Occurrence-to-quotient assignment is now
53,968 / 53,968 and all-gauge key assignment is 116 / 116.  The unified
lower-bound quotient contains 74,012 components.  These local carriers do not
grant cross-component connectivity or maximality; known-block incidence stays
36,200 / 53,968.  Independent replays are byte-identical under seeds 256071
and 256929.  P2 is complete; P3 maximal-component exhaustion is next.  Gate5
stays 10/18 and CM2 remains no-go.
