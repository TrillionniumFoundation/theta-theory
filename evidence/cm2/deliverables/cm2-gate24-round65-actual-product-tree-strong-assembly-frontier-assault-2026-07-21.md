# CM2 Round 65 Gate 2/4 — actual product-tree and strong-assembly frontier

Date: 2026-07-21

Strict verdict: **Gate 2 remains `0/17`; Gate 4's landing join remains
`1/7`, with fields 1, 4 and 7 partial.  This leaf upgrades the finite-tree
calculus to a general standard-Borel plaque law, gives an exact all-depth
first-failure criterion, and isolates the shortest lawful tagged-BV lift.
The pinned physical chain still has no actual collision-SRB product tree,
zero stable marker dispersion, strict path budget or physical
Piola/intertwining recipient.  Gate 2 and Gate 4 remain `NOT_CERTIFIED`, all
complete composite gates remain `0/5`, and CM2 remains
`NO-GO_FOR_CLAIM`.**

## 1. Frozen scope and actual inputs

This is an append-only Round-65 leaf.  It pins both Round-64 Gate-2/4 leaves,
the Round-64 aggregate and independent audit, the five direct physical
manifests from Rounds 58--62, and the Round-60 depth-97 separator.  No frozen
artifact is modified.

The physical chain still supplies exactly these compatible fragments:

```text
tagged Borel first-return inverse:                     CERTIFIED
actual landing marker g_B=d kappa_B/d mu_C, 0<=g_B<=1: CERTIFIED
tagged finite-depth cone-curve marker lift:            CERTIFIED
actual dynamic branch RN covariance:                   CERTIFIED
finite physical owner/root law on its stated carrier:  CERTIFIED
```

It supplies no common stable-tree key identifying the owner law, the landing
collision-SRB law and a stable quotient law.  The words `root`, `owner` and
`plaque` in different manifests are not measure-law equalities.

## 2. Standard-Borel plaque-law saturation theorem

Round 64 treated finite and countable rooted atlases.  The actual unstable
Rokhlin outer law may be nonatomic, so a finite tree is not yet the correct
closure interface.  The exact extension is as follows.

Let `(U,eta)` be a standard-Borel probability space of plaques and let
`(X,mu_0)` be a probability reference space.  For `eta`-almost every `u`,
suppose a positive onto `L1` isometry

```text
P_u:L1(mu_0)->L1(mu_u)
```

is supplied by the physical stable holonomy from a measurable
trivialisation.  For plaque markers `g_u`, assume the pulled field

```text
a(u,x)=P_u^(-1)g_u(x)
```

is jointly measurable and integrable.  Define

```text
delta_eta
 =inf_f integral_U ||g_u-P_u f||_1 deta(u).
```

The lower weighted median

```text
m(x)=inf{t: eta{u:a(u,x)<=t}>=1/2}
```

is measurable.  Tonelli and the scalar median theorem give the exact formula

```text
delta_eta
 =integral_X min_t integral_U |a(u,x)-t| deta(u) dmu_0(x)
 =integral_U ||g_u-P_u m||_1 deta(u).                 (2.1)
```

Put

```text
P_eta
 =1/2 integral_(U x U) ||a(u,.)-a(v,.)||_1
                         deta(u)deta(v).             (2.2)
```

Pointwise symmetrisation and averaging the competitor `t=a(v,x)` give the
sharp universal bounds

```text
P_eta <= delta_eta <= 2P_eta.                         (2.3)
```

Consequently

```text
delta_eta=0
 iff P_eta=0
 iff a(u,x)=a(v,x) for eta x eta x mu_0 almost every (u,v,x). (2.4)
```

This compresses an uncountable edge-zero audit to one same-law scalar
dispersion.  Applied to the actual marker `g_B`, (2.4) is exactly the claim
that `g_B mu_C` descends through the physical stable quotient after the
collision-SRB conditional laws and stable transfers have been installed.
The mere domination `0<=g_B<=1` does not imply (2.4): on an identity product
law, a marker varying with the plaque coordinate has positive `P_eta`.

If source and landing trivialisations satisfy the measurable family of
commuting branch squares

```text
D_u P_u^S=P_u^L D_0,
```

where `D_0` is an onto `L1` isometry, then (2.1)--(2.2) give exactly

```text
delta_eta^L=delta_eta^S,        P_eta^L=P_eta^S.      (2.5)
```

As before, dynamics transports saturation debt; it does not erase it.

The executable rational replay has five outer plaques, four root atoms and
computes `delta_eta=2/5`, `P_eta=19/80`, so the strict sandwich holds.

## 3. Exact all-depth stable-transform frontier

Let `S_N` be the stable-base parameters whose compatible graph transforms
exist through depth `N`, using one immutable chart/cone registry.  Then
`S_N` is decreasing.  Let

```text
F_N=S_(N-1)\S_N
```

be the first-failure cells.  Finite-measure continuity from above and
disjointness give the exact identities

```text
eta(S_infinity)=lim_N eta(S_N)
               =eta(S_0)-sum_(N>=1) eta(F_N),        (3.1)

S_infinity=intersection_N S_N.                       (3.2)
```

Thus the shortest measure row is not a per-depth union bound.  It is either

```text
inf_N eta(S_N)>0
```

or, equivalently, a strict first-failure budget

```text
sum_N eta(F_N)<eta(S_0).                              (3.3)
```

Together with common graph-transform charts, uniform cone/contraction and
`C1` compactness, every point of `S_infinity` produces an infinite physical
stable plaque.  The already frozen geometric-series holonomy theorem can
then supply two-sided `J_hol` after its physical Hölder constants are joined.

Two separators prevent finite-depth promotion:

1. the pinned physical strip certifies only depths `1,...,96`; the logical
   completion `S_1=...=S_96=S_0`, `S_97=empty` agrees with all those rows;
2. even `eta(S_N)>0` for every finite `N` is insufficient: take
   `S_N=[0,2^-N]`, whose intersection has zero outer mass.

Hence the Round-59 96-depth strip can give a finite subtree only.  It does
not materialise an all-depth invariant product tree.

## 4. Exact actual-interface matrix and minimum evidence debt

The strict field-level join now has the following shortest rows.

| row | required object on one immutable carrier | pinned state |
|---:|---|---|
| 1 | all-depth positive stable base `S_infinity` and product-rectangle key | absent; 96-depth prefix only |
| 2 | collision-SRB outer plaque law `eta` and measurable root trivialisation | absent |
| 3 | source/landing stable holonomies with two-sided RN and adapted-arclength derivatives | absent |
| 4 | same branch keys and measurable commuting-square family | dynamic branch keys present; stable square absent |
| 5 | actual marker dispersion `P_eta=0` | absent |
| 6 | actual root `F,R,theta,L` and essential path `m,M` strict budget | absent |
| 7 | endpoint-preserving strong restriction/intertwining/Piola recipient | absent |

The actual stable-tree instantiation count therefore remains `0/7`.  The
minimum new evidence is not another abstract tree theorem.  It is one
strict-JSON registry carrying these seven rows with the same
`rectangle/tree/branch/physical-ID/owner` keys.

For a nonatomic outer law, the Round-64 root-to-node budget becomes the
essential path bound

```text
F_0 R_0 esssup_u [M(u)/m(u)^2]
   < C_p theta_0 L_0.                                (4.1)
```

No current pin provides any actual input to (4.1).

## 5. Tagged graph to BV: shortest lawful bridge and exact no-go

### 5.1 Trace-factorisation obstruction

Suppose a bounded positive lift

```text
E:L1(kappa_B)->B_strong
```

and a bounded physical trace

```text
Tr:B_strong->BV
```

satisfied `Tr E f=f` for every `L1` density.  Then the identity
`L1->BV` would be bounded.  This is impossible.  The smooth sequence

```text
f_N(x)=1/2+(1/4)sin(2 pi N x)
```

has `||f_N||_1=1/2` and `Var(f_N)=N`.  Thus no exact
endpoint-preserving trace factorisation can upgrade the full `L1` marker
class into BV.  A lawful physical strong route must either start from a
stronger source class or prove additional smoothing before taking the exact
trace.

### 5.2 A sufficient countable tagged-BV theorem

On `[0,1]`, let a countable positive tag kernel have BV densities `k_r` with
`sum_r k_r=1`, and let tag weights be `a_r>=1`.  Define

```text
E_K f=(k_r f)_r,
||E_K f||_tagBV=sum_r a_r ||k_r f||_BV.
```

If

```text
A_0=||sum_r a_r k_r||_infinity < infinity,
A_1=sum_r a_r Var(k_r) < infinity,                   (5.1)
```

then the one-dimensional BV product rule and
`||f||_infinity<=||f||_BV` give

```text
||E_K f||_tagBV <= (A_0+A_1)||f||_BV.                (5.2)
```

The trace `sum_r k_r f=f` is exact and bounded because `a_r>=1`.  For the
Round-61/64 graph weights one takes `a_r=2^D`.  This identifies the missing
strong regularity row precisely: the frozen one-vector moment and even
`W_D in L-infinity` do not control the weighted transverse variation
`A_1`.

The exact oscillatory separator takes two tags, weight one, and

```text
k_1,N=(1+sin(2 pi N x))/2,   k_2,N=1-k_1,N.
```

Then `W_D=1` identically, but

```text
Var(k_1,N)+Var(k_2,N)=4N.                             (5.3)
```

So even the Round-64 necessary `L1` operator condition cannot be promoted
to BV assembly.  The theorem (5.2) is only a tagged-BV carrier bridge; a
physical billiard recipient still needs endpoint charts, restriction
intertwining, moving-domain Piola bounds and the accepted cemetery theorem.

## 6. Latest-technology boundary

`arXiv:2604.25746v1` contains Pesin rectangles, holonomy and SRB structure
for a compact boundaryless `C^{1+beta}` flow; its singularities are
vector-field fixed points, not billiard collision discontinuities.  It does
not instantiate the pinned collision-SRB first-return law.  Likewise,
`arXiv:2604.25881v1` concerns the billiard MME built from symbolic Hausdorff
leaf laws, not `mu_C`.  Neither wrong-interface result is promoted.

An attempted fresh official-API query during this leaf timed out without a
response.  The last successfully frozen 2026 review/small-hole checks remain
the applicable literature boundary; no new external theorem is made a pin.

## 7. Strict frontier

```text
standard-Borel plaque-law median formula:             CERTIFIED_EXACT
P_eta<=delta_eta<=2P_eta and zero criterion:          CERTIFIED_EXACT_SHARP
measurable commuting-family source/landing equality: CERTIFIED_CONDITIONAL
actual collision-SRB quotient/root/holonomies:        NOT_CERTIFIED
actual marker P_eta=0:                                NOT_CERTIFIED

all-depth first-failure identity:                     CERTIFIED_EXACT
96-prefix implies all-depth product tree:             FALSE_BY_SEPARATOR
positive every finite depth implies positive limit:  FALSE_BY_SEPARATOR
physical all-depth stable base and compact transforms:NOT_CERTIFIED

L1 exact trace lift to BV:                            FALSE_BY_SEPARATOR
countable weighted tagged-BV lift under A0+A1:        CERTIFIED_CONDITIONAL
actual weighted transverse BV constant:               NOT_CERTIFIED
physical restriction/intertwining/Piola assembly:     NOT_CERTIFIED

actual stable-tree instantiation:                     0/7
seven-field landing join:                             1/7; fields 1,4,7 partial
official immutable Gate-2 fields:                     0/17
Gate 2:                                               NOT_CERTIFIED
Gate 4:                                               NOT_CERTIFIED
complete composite gates:                            0/5
CM2:                                                  NO-GO_FOR_CLAIM
```

## 8. Executable evidence

- `cm2_gate24_round65_actual_product_tree_strong_assembly_frontier_cert.py`;
- `cm2_gate24_round65_actual_product_tree_strong_assembly_frontier_verifier.py`;
- `cm2-gate24-round65-actual-product-tree-strong-assembly-frontier-manifest-2026-07-21.json`;
- `cm2-gate24-round65-actual-product-tree-strong-assembly-frontier-manifest-2026-07-21.sha256`.

The producer and independent verifier replay the continuum-atlas finite
witness, exact first-failure ledgers, depth-97 and vanishing-survivor
separators, trace-factorisation obstruction, tagged-BV sufficient bound and
oscillatory-kernel separator.  They pin all frozen roots, reject semantic
mutations and duplicate/non-finite JSON, re-emit byte-identically, and fail
closed with default exit code `2`.
