# CM2 thirty-fourth direct assault

Date: 2026-07-19  
Verdict: **stale hit-gap blocker corrected; arbitrary-time core-preimage F9 advanced; composite gates remain 0/5**

## Round result

| gate | round-34 advance | exact remaining core gap | status |
|---|---|---|---|
| 1 | no unsafe promotion | global physical twisting/PPE completion | NOT CERTIFIED |
| 2 | no unsafe promotion | stable fibres, quotient density, inverse branches and PPE | NOT CERTIFIED |
| 3 | corrected baseline: recovered-cone hit gap and unweighted exponential C24 return tail were already certified | complete physical branch/current interfaces | NOT CERTIFIED |
| 4 | strong-tail interface refreshed; explicit one-collision endpoint-coarea `L^(3/2)` seed | fixed-`s` physical `R_n` `q/m` envelope and cemetery | NOT CERTIFIED |
| 5 | intermediate and terminal core-preimage F9 rank-path templates | complete F9/F10 atlas and F11--F18 | NOT CERTIFIED (`6/18`) |

## Critical correction

The shortest route frozen at the end of round 33 was based on a stale status.
The append-only certificate
`cm2-gate34-c24-sparse-hit-gap-manifest-2026-07-18.json` already gives

```text
epsilon_hit = 21/111718750,
r = 111718729/111718750,
P(tau_C24^+>n) < (550000/147) r^floor(n/N_open).
```

Therefore the recovered-cone hit lower and uniform unweighted exponential
return tail are not missing.  `N_open` remains nonnumeric, and the result is
not a strong `q`-weighted tail.

## Gate-5 F9 advance

All 96 C24 boundaries are represented by affine `r=constant` or
`phi=constant` Birkhoff levels.  On a finite regular incidence-rank path,

```text
L(B)=150*2^B,
M(B)=42672*2^(3B),
H_j=M(B_j)D_(j-1)^2+L(B_j)H_(j-1),
curvature < (3/2) E_j H_j.
```

This supplies strict finite F9 templates for intermediate and terminal core
preimages on each regular homogeneous component.  Owner-change,
collision-singularity and moving-occurrence F9 remain open.  Since the
limiting component atlas and same-ID operator costs are not instantiated,
Gate-5 maturity stays `6/18`.

## Strong-tail advance and boundary

The six-interface ledger now records that actual parameterized parent-W
recuts and numeric Q2 F7 exist, while F8/core-preimage-F9 pieces have also advanced.  The first true
strong-tail blocker is one common fw/rev standard-family recovery carrier
with numerical fixed-`s` `C_fw,C_rev,c=q/m` rows.  The endpoint rank tail also
gives the explicit one-collision coarea bound

```text
integral (151*2^B)^(3/2) dm_e
  < 91292149088554323/13750.
```

Its measure is not the physical first-return mass ledger, so the conditional
`L^p` transfer theorem remains uninstantiated.  Strong cemetery charge,
F14--F18 and the induced Lasota--Yorke coefficient remain absent.

## Technology boundary

Official pages for arXiv `2604.19671v2`, `2606.10155v1`, and
`2104.06947v3` were checked.  The first relies on special boundary-hole image
geometry, the second is a review/blueprint, and the third is already used by
the sparse-hit certificate.  None supplies the missing C24 same-ID numerical
`q`, cemetery and induced operator payload.  No external theorem was
promoted.

## Validation

The two new verifier suites pass integrity and independent replay, reject

```text
14/14 + 17/17 = 31/31
```

hostile mutations, and default live modes both exit 2.  Python syntax, strict
JSON parsing, exact recurrence replays and dependency hashes pass.  Leaf and
recursive SHA sidecars freeze the final artifacts.

## Next shortest line

1. Bind one common fw/rev standard-family recovery carrier to the actual
   parent-W and physical arbitrary-depth `R_n` component IDs.
2. Materialize fixed-`s` numerical `C_fw,C_rev,c=q/m` rows with the F7, F9
   and F10 charges on those same IDs.
3. Prove a survivor-conditioned physical `R_n` `L^p` envelope, separately
   sum the strong singular/corner cemetery, then assemble F14--F18 and the
   induced common-space contraction.

Strict unconditional CM2 remains `NO-GO_FOR_CLAIM`, with zero of five
composite gates fully closed.
