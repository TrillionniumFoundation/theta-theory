# CM2 Round 55 full direct assault

Date: 2026-07-20  
Strict verdict: **0/5 complete gates; Gate 5 maturity 10/18; CM2 NO-GO**

## Round result

Round 55 closes one genuine Gate-4 aggregate interface, removes one false
image-recut obstruction, and sharpens two Gate-5 subroutes.  It does not
close a composite gate.

The three append-only leaves are:

1. hereditary every-collision Growth `->` all-time survivor/coarse
   terminal-`Z`;
2. global connected-image length `->` a fixed image-recut multiplicity;
3. same-source synchronized pairing and debt-level delayed collar recovery.

All claims below are parameterwise and uniform for every fixed
`|s|<=1/400`.  No stationary Growth theorem is promoted to an arbitrary
moving-map sequence.

## Gate 4: the `Phi_pair` route is bypassed for coarse terminal `Z`

Round 41 distinguishes the all-mass artificial-cut operator `hat_F_sigma`
from the killed survivor operator `O_sigma`.  Replaying the numerical
one-step proof before deletion gives

```text
Z(hat_F_sigma G) <= Z1 Z(G),

Z1
 = (2000/1999)*49*(900337/901685)+2
 = 18367592526/360493663.
```

Put

```text
n_* = 9148,
L   = 9148*N_open,
g   = gamma^N_open < 1,
rho = (111718729/111718750)^9148 < 1,
z_p = Z(F_(sigma,pL)).
```

The frozen hereditary killed-Growth block gives

```text
sum_(p>=0) z_p
 <= z_0/(1-g)+C_N*m_base/((1-g)*(1-rho)) < infinity,
C_N<=Z0/(1-gamma).
```

Every residue `n=pL+j`, `0<=j<L`, obeys

```text
Z(F_(sigma,n)) <= Z1^j z_p.
```

Because `N_open` is one finite uniform integer, the nonnumerical multiplier

```text
A_L=sum_(j=0)^(L-1)Z1^j<infinity
```

is enough to conclude

```text
sum_n Z(F_(sigma,n))<infinity.
```

The bridge explicitly pins the inner-core finite-`Z` source multiplier,
identifies `C_s=C24` modulo the frozen core faces, joins the Round-42 killed
operator to the Round-54 `Q_n` half-open survivor IDs, joins complementary
all-mass descendants to `R_(n+1)`, and uses time-reversal invariance of the
proper cone together with preservation of mass, arclength, density ratio and
`Z` on `I(C_s)`.  It does not claim `I(C_s)=C_s`.

Terminal return pieces are positive subfamilies of the all-mass image:

```text
Z(E_(sigma,n+1)) <= Z1 Z(F_(sigma,n)).
```

Hence

```text
Z_term,coarse,pair
 <= Z1 sum_n[Z(F_(fw,n))+Z(F_(rev,n))]
 < infinity.
```

This genuinely removes `Phi_pair` summability from the shortest route to
the **coarse** terminal conclusion.  It neither proves `Phi_pair` itself
summable nor identifies coarse terminal `Z` with physical `J_pair`.

## Gate 4: image recutting now has one fixed multiplier

For every connected unstable image parent `H(A)=T_s^n(A)`, the frozen global
curve and metric bounds give

```text
ell_E(H(A)) < 68,
ell_*(H(A)) < (141/4)*68 = 2397.
```

Under the exact Round-50 one-sided endpoint convention, deterministic
adapted recutting at `10^-90` therefore creates at most

```text
N_img <= ceil(ell_*(H(A))*10^90)+1
      <= 2397*10^90+1.                              (2.1)
```

This bound is independent of return depth and incidence-rank derivative
products.  It replaces the old product model by one fixed number.

For one measured parent with normalized density ratio at most `R`, any
half-open refinement into at most `N` positive cells satisfies

```text
sum_j p_j/ell_j <= R*N*p/ell.
```

At the registered ratio `R=2000/1999`, the exact image-recut multiplier is

```text
(2000/1999)*(2397*10^90+1)
 = (4794*10^93+2000)/1999.                          (2.2)
```

Thus the Round-36 perfectly correlated rank separator remains correct for
the fields it tested, but its arbitrary-time image-recut product is no
longer required once the physical connected-image cap is installed.

## Gate 4: the exact remaining `J_pair` interface

The natural mesh still uses

```text
delta_B=2^-ceil(3(B+1)/2),
delta_B^-1<=4*2^(3B/2).
```

This is formally matched to the frozen collision-SRB moment

```text
integral 2^(3B_inc/2)dmu_s < 134217735/64,
```

but three physical same-law joins remain absent:

1. declare whether the arbitrary-`R_n` natural mesh uses a path maximum or
   an additive timewise union, and prove its Kac-tower domination rather
   than a hidden product;
2. put `delta_B`, the adapted `10^-90` recut, and both orientation length
   functionals in one metric ledger;
3. identify those natural cells as same-ID refinements of the Round-42/55
   coarse terminal canonical atoms.

After those joins, the already certified coarse terminal `Z` and fixed
`N_img` would give physical

```text
J_pair<infinity,
I_D<nu(X)+[35/(99*2^309)]J_pair.
```

They are not yet installed, so physical `J_pair`, `I_D`, proper first return,
`q`, strong cemetery and Gate 4 remain open.

## Gate 4: the apparent gap-density shortcut is rejected

The official source of arXiv `1210.0011v4` was retrieved and audited.  Its
regular extension on a gap `V=(x,y)` is constant on `(x,z)` and `(z,y)` but
may jump at `z`.

- Splitting at `z` makes each half constant-density, but there is no uniform
  lower bound on either half-length; one half can have arbitrarily large
  entry boundary `Z`.
- Keeping the whole gap retains a density jump as large as `11/9`, while the
  registered numerical cone ratio is only `2000/1999`.

Therefore the proposed `I_density=0` join and the conditional
`9997/10000` promotion are rejected.  A cutpoint-position theorem or a new
one-jump paired-carrier Growth lemma is required.

Pinned source provenance:

```text
1210.0011v4 source tar:
b705ed4fc89a42ac8e78957d65873211c71f9566599bfba90173812fe5ffe5c5

Moving_final.tex:
921fe3477c2f3280a256a459c9e2a2bc6b719a971016a8320e6d0d5e9cd25f44
```

## Gate 5: same-source synchronized hit/miss pairing

Let `lambda=lambda^+-lambda^-` be the Jordan source and let `H_x,M_x` be
the hit/miss Markov kernels at the **same immutable source point**.  The new
coupling is

```text
pi_sync
 = integral H_x tensor M_x d lambda^+(x)
 + integral M_x tensor H_x d lambda^-(x).
```

Its marginals are exactly

```text
mu^+ = H lambda^+ + M lambda^-,
mu^- = M lambda^+ + H lambda^-.
```

Consequently

```text
||J_p||_(BL*)
 <= integral min(2,d(y_H,y_M)) d pi_sync.
```

This is strictly sharper than the Round-54 normalized-product witness.  On
the two-point law with deterministic `H=M=identity`, the signed current and
synchronized cost are zero, whereas the product cost is exactly `1/2`.

Equal mass still gives no decay.  A physical same-source separation estimate

```text
d_p^sync<=C_sync*delta_sync^p,
w_Z*delta_sync<1
```

is still missing.  Signed BL cancellation cannot pay positive F10 or
cemetery mass.

## Gate 5: full collar mass does not imply finite collar debt

Take `t in (-1,1)`, `nu=dt/2`, anchor boundary `b_0(t)=0`, and one analytic
transverse boundary `b_1(t)=t`.  Then

```text
d(t)=|t|,
A_col=(-1,1)\{0},
nu(A_col)=1.
```

For the Round-54 dyadic selector, level `k>=1` has

```text
m_k=2^-k,
ell_k=2^(-(k+1)),
m_k/ell_k=2.
```

Therefore

```text
Z_col=sum_(k>=1)2=infinity.
```

This exact separator proves that full trace mass, a zero-mass coincidence
set, and even one analytic transverse intersection do not imply finite
collar boundary debt.

## Gate 5: delayed recovery lowers the needed clearance moment

At collar level `k`, the initial debt is

```text
z_(k,0)=2^(k+1)m_k.
```

Conditionally join the physical labelled collar output to the identical
Round-42 killed operator and retain at least `k+1` compatible blocks.  Then

```text
z_(k,k+1)
 <= gamma^(k+1)2^(k+1)m_k
    +Z0*(1-gamma^(k+1))/(1-gamma)m_k
 < [1+Z0/(1-gamma)]m_k,
```

because `2gamma<1`.  The aggregate ledger now needs only

```text
sum_k w_Z^(k+1)m_k<infinity,
```

instead of the much stronger raw inverse-clearance moment
`sum 2^(k+1)m_k<infinity`.  If `m_k<=C*2^(-alpha k)`, the exact threshold is

```text
alpha>alpha_*=log_2(w_Z)
             ~=0.0012409395510954121.
```

This is a certified conditional theorem.  The physical weak clearance tail,
same-operator owner join, and arbitrarily long compatible suffix horizon are
not certified.  Gate-5 maturity therefore stays `10/18`, with zero complete
18-field blocks.

The current arXiv `2503.09536v2` Arens--Eells normal-trace result was also
audited.  It supplies no positive face tower, killed intertwining, physical
constant, or immutable F17 block and is not promoted.

## Strict status

```text
Gate 1: NOT CERTIFIED
Gate 2: NOT CERTIFIED
Gate 3: NOT CERTIFIED
Gate 4: NOT CERTIFIED
Gate 5: NOT CERTIFIED (10/18; complete blocks 0)
CM2:    NO-GO_FOR_CLAIM
```

Certified Round-55 advances:

```text
all-mass hat_F one-step Z1:             CERTIFIED_NUMERICAL_REPLAY
all-collision survivor Z l1:            CERTIFIED_FINITE_NONNUMERIC_N_OPEN
two-orientation coarse terminal Z l1:   CERTIFIED_FINITE_NONNUMERIC_N_OPEN
Phi_pair needed for coarse terminal Z:  BYPASSED
global image recut count cap:            CERTIFIED
same-source synchronized BL pairing:     CERTIFIED_STRICTLY_SHARPER
analytic full-mass / infinite-Z separator:CERTIFIED
delayed recovery weak threshold:         CERTIFIED_CONDITIONAL
```

Still open:

```text
natural-mesh path/metric/same-ID join,
physical J_pair, I_D, J_cap and first return,
physical q and strong cemetery,
physical same-source separation rate,
physical weak clearance moment and suffix/operator join,
F10/F13/F14/F15/F17/F18 and Gate-3 MT_DQ.
```

## Validation

- Python syntax: `6/6 PASS`;
- strict JSON, dependency pins, exact arithmetic, provenance/verdict parity
  and deterministic replay: PASS;
- hostile manifest mutations:
  `38/38 + 27/27 + 115/115 = 180/180` rejected;
- certificate/verifier defaults: `6/6`, each exactly exit `2`;
- deterministic manifest regeneration/re-emission: `3/3` byte-identical;
- all three leaf SHA ledgers: PASS.

The regression suites establish package integrity and fail-close behavior;
they do not replace the analytic arguments.  An independent read-only audit
was run before freezing the aggregate ledger.

## Next route

1. Freeze the actual natural-mesh path rule (path maximum versus additive
   union), prove its Kac-tower moment, align the carrier metrics, and bind the
   resulting cells to the coarse terminal IDs.  Combine this with (2.1) and
   the new terminal-`Z` ledger to close `J_pair`, then `I_D`.
2. Prove recordwise/global `J_cap`, choose one physical proper first-return
   kernel, establish intermediate C24 avoidance and sum all later recovery
   clocks; only then materialise final `q` and the strong cemetery.
3. On Gate 5, prove either the physical same-source hit/miss separation rate
   or the weak clearance tail plus same-operator/suffix join.  Neither signed
   route pays the positive face tower.
4. Install a uniform physical `BV_X^tr` source/normal-trace bound, same-ID
   cancellation of artificial boundaries, and the real fixed-slot F17
   embedding; then finish F10/F13/F14/F15/F17/F18.
5. For the magnet route, prove a one-jump paired-carrier Growth lemma or a
   uniform cutpoint-position theorem, and separately populate a genuine
   interval-wide physical atlas.  The conditional `9997/10000` base remains
   unpromoted.
6. Return to Gate-3 `MT_DQ`; Gate 1 and Gate 2 remain independent structural
   poles after the Gate-4/5 shortest line is exhausted.

Leaf reports:

- `cm2-gate34-round55-hereditary-terminal-z-refinement-frontier-assault-2026-07-20.md`;
- `cm2-gate34-round55-global-image-recut-cap-natural-mesh-frontier-assault-2026-07-20.md`;
- `cm2-gate5-round55-synchronised-pairing-delayed-collar-assault-2026-07-20.md`.
