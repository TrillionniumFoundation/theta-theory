# CM2 thirty-ninth direct assault

Date: 2026-07-19  
Verdict: **the Gate-4 norm mismatch is reduced to one precisely typed trace
operator and a nonzero Gate-5 moving F10 subatlas now has a physical
`L^(3/2)` moment; all five composite gates remain open**

## Round result

| gate | round-39 advance | exact remaining core gap | status |
|---:|---|---|---|
| 1 | no unsafe transfer from the shared Gate-3/4/5 work | one physical representative with valid cancellation/Green hypothesis and same-representative twisting/PPE | NOT CERTIFIED |
| 2 | no cross-carrier promotion | invariant stable plaques, physical holonomy, quotient branches and PPE | NOT CERTIFIED |
| 3 | the scheduled strong-tail route now has an exactly typed installation interface | arbitrary-depth dynamic DQ/`MT_DQ`, moving currents and cemetery on canonical same IDs | NOT CERTIFIED |
| 4 | canonical parent-`W` geometry and projective strong tail are joined at the type level; allowable bridge loss is explicit | bounded transverse trace/boundary-`Z` operator or direct full-`Q_N C_N a^N<1`, then numeric `C_fw,C_rev,q` | NOT CERTIFIED |
| 5 | moving-occurrence seed F10 has a physical raw-coarea `L^(3/2)` moment on 64 seeds / 128 traces | arbitrary-`R_n` all-face F10, F12/F13, cemetery and F14--F18 | NOT CERTIFIED (`7/18`) |

Strict unconditional progress remains `0/5`; CM2 remains
`NO-GO_FOR_CLAIM`.

## 1. Gate-4 missing map is now exact

The Round-38 killed operator obeys

```text
||K_s^k f||_* < K_cone
  (111718729/111718750)^k mass(f).
```

The canonical parent-`W` registry consists of unstable curves
`phi(r)=4r+b`.  Passing from the first object to the second is not an
identity: it is a transverse trace/disintegration operation.  A local
dyadic concentration model has stable short-leaf tests tending to zero and
trace identically one, so the currently certified stable-test controls do
not themselves supply that operator.

This sharpens Round 38's generic disintegration warning.  The target is no
longer merely “compare two norms”; it is:

```text
construct a bounded same-ID transverse trace into the frozen canonical
parent-W disintegration, with a canonical boundary-Z output.
```

No impossibility is claimed.  An augmented cone carrying trace/boundary-`Z`
data is still an admissible route.

## 2. The bridge has almost no exponential-loss budget

If the bridge loss has root growth `gamma`, the unweighted tail needs

```text
gamma < 111718750/111718729
      = 1 + 21/111718729.
```

Preserving Round 38's exact weighted resolvent needs

```text
gamma < 223437500/223437479
      = 1 + 21/223437479.
```

Fixed and polynomial losses are subexponential and fit.  A generic `2^k`
boundary splitting loss does not.  This gives a fail-fast criterion for any
future trace-`Z` construction before attempting a full installation.

## 3. First nonzero physical F10 Lp realization

The endpoint-rank tail on the positive coarea measure yields

```text
integral 2^(3B/2) dm < 46506443753721/13750.
```

The same occurrence-ID join gives raw moving-face F10 envelopes

```text
N_rev < 35*2^B,
N_fw  < 68*2^B.
```

Therefore all 64 moving occurrence seeds and 128 oriented traces satisfy
explicit physical raw-coarea `L^(3/2)` bounds.  In the Round-38 notation,

```text
alpha=2, r=1, p=3/2, r*p<alpha.
```

This is a real positive subatlas result, not another pointwise compact-germ
frontier.  It does not yet cover arbitrary-`R_n` pullbacks, the other four
face classes, or fixed-`s` collision-SRB mass, so Gate-5 maturity remains
`7/18`.

## 4. Downstream installation frontier

The shared Gate-3/4/5 shortest line is now:

1. add a canonical transverse trace/boundary-`Z` component to the scheduled
   projective cone and prove subexponential bridge loss;
2. in parallel lift the moving-occurrence `L^(3/2)` estimate through every
   finite-rank pullback and derive physical exponents for owner/singularity
   and core-preimage F10 faces;
3. assemble complete F10 with return-wide F12 and moving-current F13;
4. close the independent strong cemetery, then F14--F18 and the induced
   coefficient;
5. feed the same-ID ledger into Gate-3 dynamic DQ/`MT_DQ` and numerical
   `C_fw,C_rev,q`.

Gate 1 and Gate 2 remain independent structural poles.  Closing Gates 3--5
would not by itself prove unconditional CM2.

## 5. Technology boundary

Round 39 imports no new external theorem.  It uses the official-version
interfaces already frozen and rechecked in Round 38
(`2104.06947v3`, `2604.19671v2`, `2606.10155v1`) plus exact local trace and
coarea calculations.  None of those sources supplies the missing canonical
C24 transverse trace, arbitrary-`R_n` all-face F10 ledger, or strong
cemetery, so no theorem is promoted beyond its typed measure and geometry.

## 6. Validation

Both suites pass Python syntax, frozen dependency hashes, strict JSON,
independent exact replay and live fail-close.  They reject

```text
21/21 + 23/23 = 44/44
```

hostile mutations.  Both certificate defaults and both verifier defaults
exit `2`; integrity and replay modes pass.  Leaf and recursive SHA chains
freeze every report, certificate, verifier and manifest.

## Next shortest line

1. Construct the augmented projective trace-`Z` cone and test its block loss
   against the exact weighted threshold
   `223437500/223437479`; keep direct full-`Q_N C_N a^N<1` as fallback.
2. Pull the certified occurrence-seed `alpha=2,r=1,p=3/2` moment through
   arbitrary-`R_n` rank paths on identical face IDs.
3. Derive corresponding physical F10 exponents for core-preimage and
   owner/singularity classes, then complete global F10.
4. Install F12/F13, strong cemetery, F14--F18 and the induced coefficient;
   then return to Gate-3 dynamic DQ/`MT_DQ` and numerical Gate-4 charges.
5. Continue Gate 1's same-representative cancellation/twisting construction
   and Gate 2's invariant plaque/holonomy/quotient/PPE construction as
   independent long poles.

Strict unconditional CM2 remains `NO-GO_FOR_CLAIM`, with zero of five
composite gates fully closed and Gate-5 maturity unchanged at `7/18`.
