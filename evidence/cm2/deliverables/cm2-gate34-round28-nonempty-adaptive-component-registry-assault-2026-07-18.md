# CM2 Gate 3/4 round 28 — nonempty adaptive component registry

Date: 2026-07-18  
Status: append-only strict leaf; no round-27 artifact, aggregate, or root modified

## Frozen conclusion

This leaf converts two already frozen finite physical inner registries into an
executable registry of **nonempty connected regular adaptive component
cells**.  It does not infer nonemptiness from membership in the 441,280-word
candidate universe.  Every registered object has a positive rational source
box, a strict whole-box physical collision history, an immutable component
ID, and one exact frozen word-prefix owner.

The complete finite results are:

- all `4,216` frozen strict `R1-inner` atoms become `4,216` nonempty
  depth-one adaptive components carried by `16` actually realized Gate-5
  word keys;
- all `114,006` frozen strict `Q2-inner` atoms become `114,006` nonempty
  depth-two adaptive components carried by `36` actually realized two-word
  prefixes;
- every Q2 atom resolves its second relative target lift, source chart and
  ordered clean-wall record at its original depth, so the extra recut
  histogram is exactly `{0: 114006}` and the finite wall/corner outer count
  and parameter-averaged coordinate-base outer mass are both exactly zero.

This is a finite-anchor materialization.  It is not a maximal connected
component enumeration of the full path fibres and is not an arbitrary-`n`
component registry.

## Registered object and immutable ownership

A registry component is the open interior of one positive dyadic source box
on which all predicates needed for its finite collision word are strict.  For
every parameter strictly inside the cell's open `s` interval, its source slice
is a nonempty connected open `(t,p)` rectangle.  The corresponding `T` or
`T^2` restriction is a regular real-analytic local diffeomorphism on that
slice.

The immutable ID payload contains:

```text
schema, physical level, origin atom ID, adaptive-recut suffix,
source-core ID, exact source box, frozen word-prefix IDs,
terminal return/survival predicate, and destination core (when present).
```

The Gate-5 word ordinal is replayed from the complete frozen product

```text
448 retained chart/target pairs x 985 ordered wall patterns = 441280 keys.
```

The replayed alphabet digest is

```text
841cb96798c9bd41e1440c8b2cdd93af5d80f2f64d693f2aa00175a440045ab9.
```

Thus a candidate word remains only a candidate.  A nonempty component is
installed only after a positive whole box proves the associated physical
domain is nonempty.

## Complete finite R1-inner registry

The full frozen step-one manifest is replayed and all `4,216` strict
`RETURN_AT_1_INNER` boxes are consumed.  Each inherits the exact physical
source-core word

```text
(source chart, retained target lift, ordered clean-wall record, roof).
```

All source-core dyadic paths remain prefix-free.  The parameter-averaged
coordinate-base mass is exactly

```text
6473/256000000,
```

and equals the sum of the `4,216` registered component masses.  The full row
and component-ID digests are, respectively,

```text
4cb7a1df76eab5a54513c6c1c38074b2610806a070e606c0922a860ef4f143c2
8b4c70c1940f3c45b4d0b490c5cb0f253b33fecdc39e13ee3bdc148163d82e00.
```

This finite `R1-inner` union is not promoted to the complete limiting
physical `R1` component enumeration.

## Complete finite Q2-inner depth-two ownership

The certificate freshly replays all `416,994` round-26 time-two leaves with
384-bit Arb and extracts the `114,006` strict `Q2-inner` atoms.  For every
atom it independently recomputes the first outgoing state and unique second
collision owner, translates the selected absolute target back to its frozen
relative lift, and checks every possible integer-wall endpoint and every
cross-axis wall-time ordering.

All `114,006` atoms resolve without an extra recut:

```text
origin Q2-inner atoms                     114006
terminal adaptive cells                   114006
nonempty depth-two adaptive components    114006
distinct realized two-word prefixes           36
extra recut depth histogram               {0: 114006}
finite wall/corner outer cells                 0
parameter-averaged finite outer
  coordinate-base mass                         0
```

The parameter-averaged admitted coordinate-base mass is exactly

```text
5257799/5120000000,
```

and the admitted plus unresolved coordinate-base ledger equals the frozen
Q2-inner coordinate-base mass exactly.  The full row and component-ID
digests are

```text
6ef491b7a0610e7a8db9e09b3c3798a9e56e26ce1c59b0df05cd13b289052a4e
5fc52479fec523b7958f4518c7a3af542686b4e9a8b6414c26e6fbd7b887c7a5.
```

## Boundary and mass typing

The artificial faces are strongly typed:

- dyadic `t` and `p` faces have collision area zero on every fixed-`s`
  two-dimensional slice;
- an artificial `s=c` endpoint face is null only for the
  parameter-averaged three-dimensional product measure.  On the exceptional
  fixed slice `s=c`, its section can be a positive-area `(t,p)` rectangle;
- therefore per-cell fixed-parameter claims apply for every `s` strictly
  inside that cell's open `s` interval, and global parameterwise statements
  exclude the finite/countable endpoint set, hence hold for almost every
  `s`;
- a finite unresolved outer box would not be declared collision-null.  Its
  mass is zero here because the unresolved box count itself is zero;
- the limiting grazing/corner/owner/core-face carrier is collision-null by
  the frozen limiting physical `R2/Q2` theorem.  That limiting statement is
  distinct from the finite outer-box ledger.

The globally summed rational mass is the parameter-averaged coordinate-base
quantity

```text
R_source * Delta t * Delta p * Delta s / Delta S.
```

Every component also carries an exact symbolic parameter-averaged
unnormalized collision-area mass of the form

```text
c * (asin(t1) - asin(t0)).
```

The latter is not renamed as the rational coordinate-base mass, and this leaf
does not claim a separately evaluated global sum of those exact physical
collision-area expressions.

## Exact remaining blocker and nonpromotion

The first unmaterialized **extension from the certified strict Q2-inner
anchors** is depth three.  No frozen Q2-to-time-three whole-box
collision-owner/core classifier and no arbitrary-`n` uniform adaptive
termination theorem currently exist.

This wording does not claim that all levels through depth two are fully
materialized:

- a Q1 nonempty component registry is not materialized by this leaf;
- limiting physical R2 components are not enumerated;
- the frozen depth-16 admitted R2 count `0` does not imply that physical R2
  is empty;
- maximal connected components of the complete physical path fibres are not
  merged or enumerated.

Homogeneity-child/canonical-recut IDs, numeric unstable Jacobian and
distortion payloads, strong `q`, the weighted excursion/cemetery tail, and an
induced strong Lasota--Yorke coefficient all remain open.  Gates 3, 4 and 5
remain `NOT_CERTIFIED`, and the global verdict remains

```text
CM2 NO-GO FOR CLAIM.
```

## Frozen artifacts and replay

- `cm2_gate34_round28_nonempty_adaptive_component_registry_cert.py`
- `cm2_gate34_round28_nonempty_adaptive_component_registry_verifier.py`
- `cm2-gate34-round28-nonempty-adaptive-component-registry-manifest-2026-07-18.json`
- `cm2-gate34-round28-nonempty-adaptive-component-registry-manifest-2026-07-18.sha256`

```bash
.venv-neurips/bin/python \
  deliverables/cm2_gate34_round28_nonempty_adaptive_component_registry_verifier.py \
  --manifest deliverables/cm2-gate34-round28-nonempty-adaptive-component-registry-manifest-2026-07-18.json \
  --integrity-only

.venv-neurips/bin/python \
  deliverables/cm2_gate34_round28_nonempty_adaptive_component_registry_verifier.py \
  --manifest deliverables/cm2-gate34-round28-nonempty-adaptive-component-registry-manifest-2026-07-18.json \
  --replay

.venv-neurips/bin/python \
  deliverables/cm2_gate34_round28_nonempty_adaptive_component_registry_verifier.py \
  --manifest deliverables/cm2-gate34-round28-nonempty-adaptive-component-registry-manifest-2026-07-18.json \
  --self-test
```

Integrity, the final fresh full replay, and `92/92` hostile tests pass.
Default live mode exits `2` because arbitrary-`n`, maximal physical
components, and all strong gates remain open.
