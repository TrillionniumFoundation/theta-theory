# CM2 eighteenth full direct assault

Date: 2026-07-17 (Asia/Shanghai)  
Frozen baseline: v51/v52 and the first seventeen rounds remain read-only;
this pass is append-only  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; fully closed
composite gates remain `0/5`.  Three new executable chains remove one
noncommutative algebra obstruction, replace an external cut-count tail by
deterministic sparse-dwell contraction under an explicit schedule, and bind
field 7 to 28 selected physical roof slots.**

## 1. Executive result

- **Gate 1:** the determinant-one biprojective gauge
  `D=(1-uv)^(-1/2)[[1,v],[u,1]]` eliminates both cubic critical
  off-diagonal cross terms exactly.  A combined class-`H` representative is
  reduced to two explicit coupled half-density groupoid equations plus a
  uniform big-cell bound `1-uv>=q_*>0`.  The criterion is certified; its
  physical solution and twisting in that same representative are not.
- **Gate 4 / repeated cuts:** exact arithmetic sharpens the fixed-core block
  from `b^2018<1/2` to `b^2018<3/8`.  A same-interval shell cut contracts
  after one `2018`-step dwell; a raw all-key field-7 cut contracts after six
  blocks, or `12108` steps.  Arbitrarily many registered cuts then have
  deterministic pointwise and rational exponentially weighted Green ledgers,
  without a probabilistic cut-count tail.  Native occurrence-to-core
  transport, no-hidden-recut dwell, common strong-space typing and cemetery
  payload remain absent.
- **Gate 5 / 18 fields:** the 24 positive maximal components now receive 24
  selected homogeneous-component row IDs and 28 roof-level field-7 slot IDs.
  Field 1 and field 7 are complete on each selected level, giving selected
  maturity `2/18`.  The selected QNL homoclinic component has one explicit
  field-7 slot.  Complete homogeneity tables and complete 18-field blocks
  remain zero.
- **Gate 2:** replaying the 64 singular occurrence rows confirms that their
  frozen fields still have no recovery carrier, transport time, core
  destination or common pulled-back restriction.  The new component and
  field-7 IDs are collision-section labels, not stable-quotient branch
  labels.  Gate 2 remains `0/17`.
- **Gate 3 / norms / Kac:** no strict promotion is made in this pass.  The
  previous 41,508-slot free current carrier, exact two-FACE algebra,
  direct-standard-`N` phase choice, three-norm frontier and Kac typing status
  remain unchanged.

## 2. Gate table

| gate | strongest eighteenth-round layer | exact remaining core gap | status |
|---|---|---|---|
| 1 | biprojective relative matrix with both clean differences; exact combined half-density class-`H` criterion | solve the coupled pair equations with `q>=q_*`, then certify selected twisting in the same family | **OPEN** |
| 2 | selected QNL collision component now has a physical field-7 slot | stable-saturated base, `pi^s`, `rho`, `p_a`, endpoint/stopping/energy packet; `0/17` | **OPEN** |
| 3 | seventeenth 41,508-slot carrier retained | physical strong-space lift/quotient, `DQ`, `MT_DQ`, `FACE_2CUT/FACE_TIME` | **OPEN** |
| 4 | scheduled arbitrary-cut scalar Green ledgers: shell dwell `2018`, raw field-7 dwell `12108` | occurrence-to-core incidence, no-hidden-recut schedule, common strong norm, cemetery payload, `C_fw,C_rev,q` | **OPEN** |
| 5 | 24 selected component rows and 28 physical field-7 roof slots; selected maturity `2/18` | complete homogeneity tables, other 16 fields, three norm intertwiners and physical four-term Kac output | **OPEN** |

## 3. Gate 1: biprojective half-density normal form

Put

```text
q(x)=1-u(x)v(x),
D(x)=q(x)^(-1/2)[[1,v(x)],[u(x),1]],
q(x)>=q_*>0.
```

Then `det D=1` and

```text
D(y)D(x)^(-1)
 =(q(y)q(x))^(-1/2)
   [[1-v(y)u(x), v(y)-v(x)],
    [u(y)-u(x), 1-u(y)v(x)]].
```

Both critical coordinates are clean differences.  The old terms
`-v_y du v_x` and `-u_yu_x dv` are absent exactly; the coupling has moved
into the scalar half-density.

For local stable pairs the sufficient equation is

```text
u(sigma y)-u(sigma x)
 =r(x)sqrt(q(sigma y)q(sigma x)/(q(y)q(x)))
  [u(y)-u(x)].
```

It makes

```text
c_s(x,y)=[u(y)-u(x)]/sqrt(q(y)q(x))
```

the exact renormalised lower coordinate and gives conjugated limit
`I+c_sE_21`.  The time-reversed equation for `v` gives `I+c_uE_12`.
Uniform Hölder control of these pair coordinates, together with inherited
Hölder `u,v` and `q>=q_*`, is sufficient for one combined class-`H` family.

```text
BIPROJECTIVE OFF-DIAGONAL CROSS TERMS:              ELIMINATED EXACTLY
COMBINED CLASS-H HALF-DENSITY CRITERION:            CERTIFIED
COUPLED HALF-DENSITY GROUPOID SOLUTION:             NOT CERTIFIED
UNIFORM BIG-CELL BOUND:                             NOT CERTIFIED
SAME REPRESENTATIVE CLASS H PLUS TWISTING:          NOT CERTIFIED
GATE 1:                                             NOT CERTIFIED
```

## 4. Gates 4/5: sparse-cut dwell blocks

For

```text
b=720269600000/720626832337
```

exact integer arithmetic gives

```text
b^2018<3/8.
```

### 4.1 Same-interval shell

One registered same-interval two-view cut followed by at least `2018` valid
core steps has

```text
lambda_shell<(15/8)(3/8)=45/64,
1/(1-lambda_shell)<64/19.
```

With cut weight `w=4/3`,

```text
w lambda_shell<15/16,
1/(1-w lambda_shell)<16.
```

### 4.2 Raw all-key field 7

Five coarse `3/8` blocks do not beat `580000/1999`; six are the first that
do.  After `6*2018=12108` steps,

```text
lambda_7
 <(580000/1999)(3/8)^6
 =13213125/16375808<1.
```

With cut weight `w=6/5`,

```text
w lambda_7<7927875/8187904<1,
1/(1-w lambda_7)<8187904/260029<32.
```

For a recurrence `E_(h+1)<=lambda E_h+J_(h+1)`, these bounds control every
registered cut count `h`.  The weighted `l1` conclusion requires weighted
summability of transported injections, but no separate geometric tail for
`h` is used.  This is stronger than the seventeenth external-tail criterion
on the scheduled scalar ledger and strictly weaker than native physical
recovery.

## 5. Gate 5: selected physical field-7 slots

The 24 positive maximal components split as

```text
source G / W: 12 / 12,
roof 1 / 2:  20 / 4,
roof-level slots: 28.
```

Each receives a selected component-row ID frozen from its word key, maximal
component ID, central source/target homogeneity and defining predicate.  The
all-component characteristic theorem is then attached to every selected
roof slot:

```text
G source: C_7=580000/1999,
W source: C_7=572000/1999.
```

The selected QNL homoclinic component

```text
7359148da1c43a255f2238d9c831b8638f2c9885035034a5792699c968b2f3b5
```

is bound to slot

```text
slot:530fb5797b653dca584f9c9ac32ce70328dea04f8fca3db195349dd663948c8f.
```

The exact maturity is now

```text
selected physical component-level fields: 2/18 (fields 1 and 7),
selected physical field-7 roof slots:        28,
complete key homogeneity tables:              0,
complete 18-field operator blocks:            0.
```

Attaching the `12108`-step dwell packet to each slot is a certified
conditional statement, not a native operator installation.

## 6. Physical incidence and quotient audit

The restored `python-flint` replay regenerates all 24 component rows and 64
singular occurrence rows.  The occurrence schema contains event source,
target, side, miss target and a physical witness, but not

```text
recovery-carrier/branch ID,
forward or reverse transport time,
destination 24-core component ID,
identical pulled-back restriction in both views.
```

Therefore the previous `112` source/target-compatible pairs remain a
many-to-many diagnostic.  No row can be promoted to the transported
incidence required by the `15/8` shell or the `12108`-step field-7 schedule.
Likewise, a regular collision-component ID supplies neither stable saturation
nor `pi^s,rho,p_a`; Gate 2 remains `0/17`.

## 7. Technology audit

- Butler--Park `arXiv:1909.11548` remains the class-`H` definition context.
- Kalinin--Sadovskaya `arXiv:2604.13401` supplies periodic-data rigidity
  context but no coupled half-density solution for this non-fibre-bunched
  gluing problem.
- Demers--Liverani `arXiv:2606.10155`, section 5.6.1, explicitly notes that
  sparse holes with sufficient intervening mixing can recover cone
  contraction.  This supports the dwell strategy but does not provide the
  missing CM2 physical incidence or common-space typing.

No theorem is used beyond its verified scope.

## 8. Strict final boundary

```text
COMPOSITE GATES FULLY CLOSED:                         0/5
UNCONDITIONAL CM2:                                    NO-GO FOR CLAIM

BIPROJECTIVE CROSS-TERM ELIMINATION:                  CERTIFIED
COMBINED CLASS-H SUFFICIENT HALF-DENSITY CRITERION:    CERTIFIED
SCHEDULED SHELL ARBITRARY-CUT CONTRACTION:             CERTIFIED ABSTRACTLY
SCHEDULED RAW FIELD-7 ARBITRARY-CUT CONTRACTION:       CERTIFIED ABSTRACTLY
SELECTED PHYSICAL MAXIMAL-COMPONENT ROWS:              24
SELECTED PHYSICAL FIELD-7 ROOF SLOTS:                  28
SELECTED COMPONENT FIELD MATURITY:                     2/18

COUPLED HALF-DENSITY SOLUTION PLUS TWISTING:           NOT CERTIFIED
STABLE QUOTIENT / PHYSICAL PPE:                        NOT CERTIFIED
PHYSICAL STRONG DQ / MT_DQ / FACE:                     NOT CERTIFIED
NATIVE OCCURRENCE-TO-CORE SCHEDULED RECOVERY:          NOT CERTIFIED
COMPLETE 18 FIELDS / THREE NORMS / PHYSICAL KAC:       NOT CERTIFIED
```

The shortest honest next attacks are now:

1. solve the two half-density pair equations by a coupled Hölder fixed-point
   or implicit-function scheme while preserving `q>=q_*`;
2. generate actual forward/reverse recovery carriers from the 64 event rows,
   with transport time, common restriction and first 24-core entrance;
3. extend each selected component row into a complete key homogeneity table,
   then fill fields 2--6 and 8--18 on the same immutable slot IDs;
4. construct an interval-indexed stable-saturated return base and bind its
   quotient branch IDs to the selected collision-component/endpoint packet;
5. install the physical strong-space lift/quotient for `DQ`, `MT_DQ`, FACE,
   the three norm intertwiners and all four Kac terms.

## 9. Reproduction

The three new fail-closed verifiers support replay/integrity, hostile
self-tests and live mode.  New hostile mutations rejected: `50/50`; with the
five seventeenth frontier verifiers the cumulative frozen frontier total is
`120/120`.  Live mode deliberately exits `2` for every still-open composite
gate.  Individual and aggregate SHA-256 sidecars freeze every artifact.
