# CM2 ninth full direct assault

Date: 2026-07-16 (Asia/Shanghai)  
Frozen baseline: v51/v52 and the first eight assault artifacts were read only  
Strict verdict: **unconditional CM2 remains `NO-GO FOR CLAIM`; composite
gates remain `0/5`**

## 1. Executive result

This pass closes the complete raw one-collision bidirectional geometric-cost
layer and, for the first time, a controlled stopped-parent recovery moment at
the closed map `s=0`.  It also upgrades the depth-one transfer difference
quotient to strongly convergent moving tests and certifies the exact
noncommutative fixed-time telescope through depth 64.

These advances do not close a composite gate.  The recovery theorem applies
to one-time controlled atoms under a declared query-independent product-depth
kernel; it is not a native dynamical antichain, a hereditary
repeated-indicator theorem, or a uniform finite-`s` moving-face result.
Complete numeric finite-`s` `C_fw,C_rev`, branch-record `MT_DQ`, physical
prefix/suffix bounds, and the three CM2 norm lifts remain open.

## 2. Gate table

| gate | strongest certified layer after this pass | exact remaining hard gap | strict status |
|---|---|---|---|
| 1. holonomy/QNL | unchanged exact natural-holonomy obstruction and pilot QNL data | physical same-carrier holonomy transport | **OPEN** |
| 2. physical PPE | unchanged stopped-parent bridge and finite benchmarks | full-mass physical quotient/kernel, spatial Frostman and normalized amplitude | **OPEN** |
| 3. global event/DQ | corrected 64-row current; complete depth-one norm DQ; strong moving tests; exact fixed-time telescope | dynamic branch-record tightness, common four-type atlas, fixed-time convergence and summable two-time `MT_DQ` | **OPEN** |
| 4. same-occurrence `q` | numeric one-collision costs; product depth law; controlled `s=0` recovery moment | uniform finite-`s` recovery and the final propagated numeric `C_fw,C_rev,q` | **OPEN** |
| 5. fixed/full transfer | corrected 128-coordinate finite-Borel Kac layer and previous component gcd one | complete operator blocks, physical prefix/suffix sums and three CM2 norm lifts | **OPEN** |

## 3. New exact advances

### 3.1 Endpoint rank and bidirectional cost

All 128 maximal-row endpoint germs are simple.  With canonical endpoint rank
`B>=14`, the exact global raw tail is

```text
sum_e m_e{B>b} <= (9158592/6875) 4^-b,  b>=14.
```

The one-collision derivative costs satisfy

```text
C_fw^(1)=C_rev^(1)=151*2^B.
```

The two exact arclength densities are

```text
rho_rev = cp_source*abs(u_y)/ell_T,
rho_fw  = cp_miss*abs(u_y)/(t_miss-ell_T).
```

Each orientation has 48 simple density-zero endpoints, with global raw
boundary bounds

```text
Z_rev <= 163855/256,
Z_fw  <= 102409375/72674.
```

The exact Birkhoff slope recurrence is

```text
V_1=kappa_1+cp_1/(tau+cp_0/(V_0+kappa_0)).
```

It makes the fixed cone

```text
25/9 < V < 4108425/145348 < 29
```

strictly invariant.  All 64 forward and 64 reverse views have entry time
zero.  Carrier-curvature bounds are `<623` in the reverse source view and
`<4949` in the forward miss view; logarithmic-density costs are
`<27*2^B` and `<52*2^B`.  Consequently,

```text
C_rev^(geom,Z)=204*2^B+C_Z_rev,
C_fw^(geom,Z)=204*2^B+C_Z_fw.
```

The resulting global partial `q` mass before `Z_N^-1` is at most

```text
344740673672569503213/63953120000.
```

This is the complete raw one-collision geometric charge, not the final
finite-`s` propagated charge.

### 3.2 Product stopped depth and controlled recovery

The query-independent auxiliary kernel samples

```text
w_K=(3/4)4^-K,  K>=0,
j=floor(2^K u_e).
```

Each `(K,j)` atom has joint mass `(3/4)8^-K m_e`, forgetting the marks returns
exactly `m_e`, and the same record is used in both orientations.  Exactly,

```text
P(K>=k)=4^-k,
E[2^K]=3/2,
E[2^(5K/4)]<15/8.
```

On density-zero shell `b`, the orientation-specific mesh

```text
delta_b=2^-ceil(3(b+1)/2)
```

gives a uniform `1/3` log-Hölder constant `52`, one-endpoint `Z` series at
most `753687/32`, and common positive row-mass lower bound

```text
m_e(row)>169/2147483648000.
```

Every one-time depth atom is therefore an initial standard family with the
same invariant cone, carrier `C2<4949`, uniform log-Hölder control, and
`Z<=C_mesh 2^K`.  Applying the closed-map Growth Lemma separately to the two
orientations gives finite table-dependent constants `A0,A1` such that

```text
R_fw(K,j)+R_rev(K,j) <= A0+A1*K.
```

Hence, for every `0<gamma<log(2)/A1`,

```text
E[2^K exp(gamma(R_fw+R_rev))] < infinity.
```

This certifies controlled one-time stopped-parent recovery at `s=0`.  The
constants are theorem-supplied rather than newly interval-evaluated rational
constants, and no uniform finite-`s` or hereditary restriction claim is
made.

### 3.3 Moving-test DQ frontier

The previously certified depth-one norm convergence

```text
(P_s-P_0)h/s -> D_0h in (C^{1,alpha})*
```

now permits every strongly moving test `Phi_s->Phi_0` in `C^{1,alpha}` by a
two-term norm estimate.  Both centered and uncentered depth-one moving-test
DQ are certified.

For every fixed `n`, the exact identity

```text
(P_s^n-P_0^n)/s
 = sum_(j=0)^(n-1) P_s^(n-1-j) ((P_s-P_0)/s) P_0^j
```

was replayed by formal word cancellation for `1<=n<=64`.  What remains is
analytic rather than algebraic: iterated branch sources must be controlled
in the frozen strong space on one common moving atlas, followed by the
summable two-time majorant.

## 4. Mandatory convention audit

The eighth-pass collision-flux correction remains binding:

```text
dm_e=R_source*cp_source*abs(u_y)/ell_T dtheta,
```

with one power of `cp`, not the superseded `cp^2` expression.  During this
pass, an early cone draft also used `V_0-kappa_0` in the recurrence.  Exact
matrix division showed that the denominator is `V_0+kappa_0`; the draft was
corrected before manifest freeze, and an exact matrix-slope identity plus
mutation test now guards the sign.

## 5. Latest technology audit

The newest directly relevant source found on 2026-07-16 is Demers--Liverani,
*Recent Progress in the Application of Transfer Operators to Dispersing
Billiards*, `arXiv:2606.10155v1` (submitted 2026-06-08).  Its Theorem 5.7
surveys uniform cone contraction for sequential finite-horizon billiards,
but Problem 8.7 explicitly records the obstruction caused by characteristic-
function restrictions concentrating densities on atypical trajectories.

The controlled bridge here avoids that overclaim by first representing each
one-time atom as a genuine standard family and then applying the closed-map
Growth Lemma from Canestrari, `arXiv:2604.19671v2`, Lemma 6.14.  No source
found supplies the missing repeated-indicator recovery, finite-`s`
moving-face DQ, or three-space norm interfaces.

## 6. Exact next target

1. build a finite-`s` common mesh with uniform endpoint-rank constants;
2. interval-evaluate the moving recovery and complete numeric `C_fw,C_rev`;
3. promote the telescope to branch-record fixed-time `MT_DQ`;
4. close the `FACE_2CUT` middle tail and summable two-time majorant;
5. export the Kac layer through physical prefix/suffix and all three norms.

The complete proof ledger and exact remaining boundary are in
`cm2-gates345-cost-recovery-mt-assault-2026-07-16.md`.

## 7. Validation status

All eight new certificate/verifier pairs compile, pass full replay,
integrity-only verification, and mutation self-tests.  Every live default
fails closed with exit `2`.  Independent certificate/verifier self-hashes
match all eight JSON manifests.  The frozen eighth-round baseline was audited
before advancing the mutable continuous log, and the ninth-round subordinate
and aggregate SHA manifests bind the resulting current state.
