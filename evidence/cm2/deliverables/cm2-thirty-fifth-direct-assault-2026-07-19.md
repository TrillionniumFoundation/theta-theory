# CM2 thirty-fifth direct assault

Date: 2026-07-19  
Verdict: **arbitrary-Rn common fw/rev carriers and one physical weighted subcharge tail are now certified; composite gates remain 0/5**

## Round result

| gate | round-35 advance | exact remaining core gap | status |
|---:|---|---|---|
| 1 | no unsafe promotion | global physical twisting/PPE completion | NOT CERTIFIED |
| 2 | no unsafe promotion | stable-saturated product base, quotient density, inverse branches and PPE | NOT CERTIFIED |
| 3 | no unsafe promotion; frozen unweighted C24 tail retained | complete physical branch/current interfaces | NOT CERTIFIED |
| 4 | arbitrary-depth same-ID fw/rev carrier; physical `L^(6/5)` and exponential tail for additive D1 rank-sum charge | full numerical `C_fw,C_rev,q/m`, recovery-depth moment and cemetery | NOT CERTIFIED |
| 5 | arbitrary-Rn parent-W/restriction join imported on common carrier | owner/moving F9, complete F10 and F11--F18 | NOT CERTIFIED (`6/18`) |

## Same-ID carrier advance

On every existing nonempty regular rank-refined component of every finite
`R_n`, the slope-four physical foliation now has canonical standard-Borel
parent-W IDs.  Collision SRB disintegrates on these leaves with density
`cp/sqrt(17)`.  A branch `H=T_s^n|U`, its image recut and the time-reversed
branch are bound to one immutable physical restriction ID.  Area invariance
and reversibility give equal physical masses, so fw/rev are two views of one
charge.

For every positive cell, retained-mass depth `D` has the numerical clock

```text
R(D)<=301500+1005D,
R_fw(D)+R_rev(D)<=603000+2010D.
```

The missing object is now the global physical law of `D`, not carrier
existence or per-cell recovery finiteness.

## Physical weighted-tail advance

The direct collision-SRB incidence rank

```text
B_inc=max(14,ceil(log2(1/cp_source)),ceil(log2(1/cp_target)))
```

satisfies `mu_s{B_inc>b}<2*4^-b`.  Hence

```text
integral 2^(3B_inc/2)dmu_s < 134217735/64.
```

For the additive physical first-return channel

```text
c_D1,n(x)=151 sum_(i=1)^n 2^B_i(x),
```

Hölder, Jensen and the frozen unweighted survivor tail produce

```text
sum mbar*cbar_D1^(6/5) < K_rank*N_open^3,
K_rank=3055930500533353804145008325576782226562500/453789,
```

and therefore

```text
Wbar_D1(n)
 < (K_rank*N_open^3)^(5/6)*(550000/147)^(1/6)
   *(111718729/111718750)^(floor(n/N_open)/6).
```

This is the first certified physical fixed-`s` `R_n` weighted exponential
tail in the stack.  It is strictly typed to the additive first-derivative
incidence-rank channel and is not promoted to final `q`.

## Remaining hard boundary

The direct retained-mass route is still critical: dyadic mass scale alone
allows the exact countermodel `P(D>=d)=2^-d`, for which the required `2^D`
moment grows with the cutoff.  Closing Gate 4 therefore needs either a
supercritical physical `D` tail or an aggregate standard-family `Z` estimate
that bypasses cellwise normalization.  The latter still requires arbitrary-
depth owner/singularity and moving-occurrence F9, complete F10, and the
branchwise F7/F9/F10 join on the same IDs.

Strong singular/corner cemetery charge and F14--F18 remain absent.  Gate-5
maturity stays `6/18`; no template is counted as a completed global operator
field.

## Technology audit

The official arXiv pages were fetched directly on 2026-07-19.  Current
versions remain `2604.19671v2`, `2606.10155v1`, and `2104.06947v3`.
The small-hole paper has special boundary-hole geometry, the review supplies
no CM2-specific same-ID numerical payload, and the projective-cone paper is
already the source of the frozen sparse-hit tail.  None closes the physical
recovery-depth law, full `q`, cemetery, or F14--F18.  No external theorem was
promoted.

## Validation

Both new suites pass syntax, dependency hashes, strict JSON parsing,
independent replay and live fail-close.  They reject

```text
16/16 + 17/17 = 33/33
```

hostile mutations.  Leaf and recursive SHA chains freeze the final artifacts.

## Next shortest line

1. Replace cellwise retained-depth normalization by a physical aggregate
   standard-family `Z` ledger, or prove a supercritical physical `D` tail.
2. Extend rank-path F9 to owner/singularity and moving-occurrence pullbacks;
   install complete all-face F10 on the same arbitrary-`R_n` IDs.
3. Assemble full numerical `C_fw,C_rev,q/m`, sum the strong cemetery, then
   materialize F14--F18 and the induced common-space contraction.

Strict unconditional CM2 remains `NO-GO_FOR_CLAIM`, with zero of five
composite gates fully closed.

