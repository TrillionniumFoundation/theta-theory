# CM2 Gate 2: closed-loop local return strip and stable-quotient audit

Date: 2026-07-15 (Asia/Shanghai)  
Model: frozen centered rational fixed-section pilot  
Scope: thicken the certified 96-collision closed shadow orbit; do not edit
v51/v52 or the shared research log  
Verdict: **one positive-SRB local return strip and its fixed-point
hyperbolic plaques are certified; an interval-valued stable quotient, a
full-mass countable partition, and physical Gate 2 remain OPEN / NO-GO**

## 1. Result

The Gate-1 predecessor supplies an exact reversible fixed point `z_*` of the
96-solid-collision word

```text
Q^10 B^4 Q^10
```

and its same-orbit derivative

```text
D_loop = [[A,B],[C,A]],       det D_loop=1,
lambda=A+sqrt(B C)>1.0722583849*10^53.
```

This report thickens that point for the first time.  In the exact Perron
chart

```text
delta s = u+v,
delta p = k_*(u-v),
k_*=sqrt(C/B)=6.7795323317182630386...,
```

let

```text
R   = {|u|<=10^-43, |v|<=10^-43},
S_0 = {|u|<=1.86*10^-96, |v|<=10^-43}.                    (1.1)
```

A 900-bit correlated second-order Arb replay of every collision proves that
the declared word is regular on all of `S_0` and that `T^96(S_0)` crosses
`R` in the unstable direction while entering it in the stable direction.
In particular,

```text
u_out(-1.86*10^-96,v) < -1.89*10^-43,
u_out(+1.86*10^-96,v) >  1.89*10^-43,
|v_out(u,v)|           <  9.57*10^-45.                    (1.2)
```

The fixed source half-width in (1.1) is an exact decimal rational.  It is
strictly smaller than the independently enclosed diagnostic value

```text
2*10^-43/lambda
 =1.8652220659163345651...*10^-96.                        (1.3)
```

Thus (1.2) is a genuine positive-width covering relation, not a derivative
at one point and not an interval-valued choice of the width.

The normalized collision-SRB mass of the source strip is also explicit:

```text
mu(S_0)=25/(52*pi) * 2k_* * (4*1.86*10^-96*10^-43)
       =1.5437943948818226472...*10^-138 > 10^-139.        (1.4)
```

This closes the requested **single local branch geometry**.  It does not
close the stable quotient.  The same word is certified only on the very
narrow vertical strip `S_0`; its image is a horizontal strip and is not an
invariant or stable-saturated return domain.  The maximal bi-infinite set
generated by this one symbol has only the closed orbit as its quotient
base.  It therefore cannot provide an interval-indexed family of physical
stable leaves.

## 2. Exact closure and correlated replay

The predecessor proves a unique root

```text
a_*=-8.2937621943296019634...*10^-15,
p_*=0,
```

for the reversible 48-collision half word.  Reflection gives exact closure
of the full 96-collision word.  The new certificate imports that root box
and reconstructs the same exact rational billiard geometry at 900 bits.

The raw 96-step interval evaluation of the centre contains zero but is wide
because it forgets the correlation between the root and its image.  The
covering test therefore uses the exact fixed-point identity to recenter at
zero, while retaining interval enclosures for every derivative and every
second-order remainder.  It does not use the midpoint of the raw closing
box as a numerical correction.

For all of `S_0`, each output-coordinate Taylor remainder is bounded by

```text
2.391*10^-45.                                              (2.1)
```

The entire physical state perturbation stays below

```text
6.780*10^-43.                                              (2.2)
```

The replay exhausts all prescribed first hits and obtains

```text
minimum flight        > 0.18710678118,
minimum discriminant  > 0.01026432354,
minimum incidence     > 0.63320623699,
minimum clearance     > 0.22283858113.                    (2.3)
```

Hence no singularity, transparent-wall crossing, wrong lift, or alternate
first hit is hidden in the thickening.

## 3. Covering relation

Write `F=T^96` in `(u,v)` coordinates.  The source strip face estimates are
formed as

```text
F_u(+r_u,[-R,R])
  = D_uF_u*r_u + [-|D_vF_u|R-E_u, |D_vF_u|R+E_u],

F_u(-r_u,[-R,R])
  =-D_uF_u*r_u + [-|D_vF_u|R-E_u, |D_vF_u|R+E_u],
```

with `r_u=1.86*10^-96` and the rigorous remainder `E_u`.  The two resulting
Arb boxes are, in compact display,

```text
negative face = -2*10^-43 +/- 1.02*10^-44,
positive face =  2*10^-43 +/- 1.02*10^-44.                (3.1)
```

Their strict signs imply the first two inequalities of (1.2).  The full
stable image is

```text
F_v(S_0) subset [-9.57*10^-45,9.57*10^-45],               (3.2)
```

which lies in the interior of the stable faces of `R`.

This certifies a 96-step return strip in the usual covering-relation sense:
the prescribed branch maps a positive-width vertical strip across the
local rectangle.  It does **not** assert that 96 is the first return time to
`R`; earlier visits to this newly declared microscopic rectangle have not
been exhaustively excluded.  All later typing therefore says
`96-collision return strip`, not `first-return branch`.

## 4. Uniform cones and the exact amount of graph transform obtained

On all of `S_0`, the derivative enclosure in the Perron chart is

```text
DF(S_0) subset
[[ >1.0722583849*10^53,  [-0.192,0.192]],
 [ [-6.39*10^11,6.39*10^11], [-0.192,0.192] ]].           (4.1)
```

For the unstable cone `|dv|<=10^-40|du|`, its image slope is at most

```text
5.959*10^-42 < 10^-40.                                    (4.2)
```

For the stable cone `|du|<=10^-53|dv|`, the inverse image slope is at most

```text
1.784*10^-54 < 10^-53.                                    (4.3)
```

A stable graph across the `v`-height of `R` therefore has

```text
|u|<=10^-53*10^-43=10^-96<1.86*10^-96,                   (4.4)
```

so it fits strictly in the certified word domain.  The strict covering,
entry, and cone conditions give the local stable and unstable plaques of
the exact hyperbolic fixed point by the standard graph transform.  This is
the precise certified plaque statement.

It is important not to enlarge (4.4) into a foliation statement.  A cone
field controls the slope of a graph whenever the graph transform is
defined.  It does not produce a plaque through every point of an unstable
reference interval when the iterated domain is absent.

## 5. Why there is not yet a one-step stable quotient branch

The obstruction is now sharply typed.

### 5.1 Earliest obstruction: domain invariance and stable saturation

The word domain is the vertical strip `S_0`, whereas its image inside `R`
is a horizontal strip of stable half-height below `9.57*10^-45`.  Neither

```text
F(S_0) subset S_0
```

nor a multi-strip Markov return domain has been certified.  Repeating the
one symbol in both time directions leaves only the fixed orbit as the
maximal hyperbolic base.  Consequently the graph transform gives one
stable plaque and one unstable plaque at `z_*`, not a stable plaque family
indexed by a nondegenerate unstable interval.

The missing object for even a **single nontrivial local quotient branch** is
a pair of stable-saturated product sets and a plaque family

```text
Lambda^-, Lambda^+,
W^s_loc(x) for x in I^-, I^+,
F(W^s_loc(x) cap Lambda^-)
    subset W^s_loc(Fx) cap Lambda^+.                       (5.1)
```

This is a domain-invariance/stable-saturation gap.  It precedes any issue
of BV or absolute-continuity estimates for holonomy.

### 5.2 The affine Perron projection is not a substitute

The tempting projection

```text
pi_aff(u,v)=u
```

has not been proved constant on physical stable leaves.  In (4.1), the
cross derivative is only bounded by `|partial_v F_u|<0.192`; it is not
identically zero on the strip.  Therefore different points on an affine
vertical fibre need not have the same projected future.  Declaring these
fibres to be stable holonomy would simply assume the missing quotient.

### 5.3 What the existing covering would imply after (5.1)

If a certified stable holonomy `pi^s` satisfying (5.1) were added on these
same source and target sets, then the two unstable face inequalities in
(1.2) would immediately give an onto scalar branch

```text
bar F:I^- -> I^+,
h_loop=(bar F)^-1:I^+ -> I^-.                             (5.2)
```

Thus no further 96-collision covering computation is needed for the local
onto property.  What is missing is the leaf-consistent quotient on which
`bar F` is well-defined.

Holonomy BV/absolute continuity and its Jacobian would then be the next
requirements for physical weights.  They are not the earliest blocker to
the topological branch, but they remain necessary before a branch can enter
the actual SRB/Gibbs kernel.

## 6. Single branch versus full-mass countable partition

Even a future completion of (5.1)--(5.2) would certify only one local
quotient branch.  It would not certify

```text
{I_a}_a countable, disjoint mod zero,
sum_a 1_{I_a}=1,
bar F_a(I_a)=I,
```

on a full-mass physical quotient.  A finite clean hyperbolic subsystem can
have zero conditional SRB mass or negative relative pressure inside the
ambient billiard.  The positive two-dimensional mass (1.4) does not by
itself turn its stable projection into a full-mass quotient alphabet.

The strict status separation is therefore

| layer | status |
|---|---:|
| exact 96-collision fixed point and derivative | certified |
| positive-width prescribed-word return strip | certified |
| positive collision-SRB mass of that strip | certified |
| uniform stable/unstable cones | certified |
| fixed-point local stable/unstable plaques | certified |
| interval-indexed local stable foliation | not certified |
| one nontrivial onto stable-quotient branch | not certified |
| full-mass countable quotient partition | not certified |
| quotient density and actual reverse weights | not instantiated |
| stopped-parent PPE / normalized amplitude | not certified |
| physical Gate 2 | not certified |

The two-dimensional first-return kernel from the previous Gate-2 audit
also remains deterministic Dirac.  The new local strip does not change that
fact; random inverse branches arise only after a genuine non-invertible
stable quotient with multiple physical return branches.

## 7. Minimal next certificate

The smallest constructive continuation is now:

1. add at least a local stable-saturated Markov product domain containing
   `S_0`, with forward-compatible plaques through every point of a declared
   unstable interval;
2. certify plaque invariance for the 96-word branch and define `pi^s` on
   both source and target products;
3. use the already certified face covering to instantiate (5.2), and
   certify its holonomy Jacobian/conditional SRB weight;
4. embed that branch into a complete countable full-mass return partition,
   with a certified return/grazing/context tail;
5. only then instantiate the full physical pair-energy drift, native
   stopping antichain, same-carrier endpoint identity, and exhaustive
   normalized amplitude registry.

The BV QNL-fibre holonomy still open in Gate 1 concerns transport of the
finite-shadow twisting endomorphism to the periodic fibre.  The stable
foliation missing here is a related geometric technology but a distinct
object: it must saturate a physical return base and make every quotient
branch leaf-consistent.

## 8. Reproduction

```bash
/tmp/cm2-flint-venv/bin/python -m py_compile \
  deliverables/cm2_gate2_closed_loop_local_return_cert.py \
  deliverables/cm2_gate2_closed_loop_local_return_manifest_verifier.py

/tmp/cm2-flint-venv/bin/python \
  deliverables/cm2_gate2_closed_loop_local_return_cert.py

python3 \
  deliverables/cm2_gate2_closed_loop_local_return_manifest_verifier.py \
  --self-test

python3 \
  deliverables/cm2_gate2_closed_loop_local_return_manifest_verifier.py

sha256sum -c \
  deliverables/cm2-gate2-closed-loop-local-return-manifest-2026-07-15.sha256

sha256sum -c deliverables/cm2-v52-manifest.sha256
```

The arithmetic certificate and verifier self-test must exit zero.  The live
physical verifier must exit two by design.  Final verdict:

```text
POSITIVE_WIDTH_96_COLLISION_RETURN_STRIP: CERTIFIED
FIXED_POINT_LOCAL_STABLE_UNSTABLE_PLAQUES: CERTIFIED
INTERVAL_INDEXED_LOCAL_STABLE_FOLIATION: NOT CERTIFIED
FIRST_ONTO_STABLE_QUOTIENT_INVERSE_BRANCH: NOT CERTIFIED
FULL_MASS_COUNTABLE_RETURN_PARTITION: NOT CERTIFIED
PHYSICAL_GATE2: OPEN / NO-GO
```
