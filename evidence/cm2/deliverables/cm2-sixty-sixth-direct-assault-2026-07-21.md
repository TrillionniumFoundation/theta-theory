# CM2 Round 66 full direct assault

Date label: 2026-07-21

Strict verdict: **0/5 complete composite gates; Gate 2 official fields 0/17;
Gate 4 landing join 1/7 with fields 1, 4 and 7 partial; Gate 5 maturity
10/18 with zero complete blocks; CM2 remains `NO-GO_FOR_CLAIM`.**

## Round result

Round 66 freezes four compatible source leaves and one independent aggregate
audit.  It makes several exact advances, including one genuinely physical
local Gate-3 promotion, but it does not substitute local, finite-depth,
same-marginal or average information for the unpaid global interfaces.

1. **Gate 1 same-gauge audit and relative transfer.**  The selected compact
   QNL expression `B_u(x)B_s(y)` and the combined-Green expression `U_vL_u`
   have similar triangular syntax but do not share pinned scalar functions,
   orbit/plaque/gauge keys or a finite-loop crosswalk.  Their exact relative
   transfer is

       C=G_q^{-1}G_c=L_{-u_q}U_{v_c-v_q}L_{u_c}

   with

       C=[[1+dv*u_c,dv],
          [u_c-u_q(1+dv*u_c),1-u_q*dv]],

   and `C=I` exactly when `dv=0` and `u_c=u_q`.  Thus the frozen selected-QNL
   constants still cannot be inserted into the combined all-plaque gauge.

2. **All-plaque tail and loop transport.**  On one common registry, if the
   source cocycle is class `H` and `C,C^{-1}` are bounded Hölder, then the
   target is class `H` exactly when the forward and backward renormalized
   endpoint defects have uniform Hölder limits.  Zero tails are not required.
   For limits `L_s,L_u`, the old selected loop is transported without a
   correction exactly when

       H_A^s L_u + L_s H_A^u + L_s L_u = 0.

   In the diagonal-plus-triangular model this reduces to `l=u=0`; a nonzero
   resonant tail cannot be called pure covariance of the old loop.

3. **Gate 3 local physical material atlas.**  The frozen positive-width
   strict 96-collision word and the exact circular branch formula do yield,
   by the implicit-function theorem, a finite-parameter `C^2` material atlas
   on every compact core strictly inside that regular word cell.  On each
   fixed smooth `W^{2,1}->L^1`-type scale its remainder is uniformly
   `O(s^2)`.  This is an actual local promotion, but it does not cover all
   cells, all depths or the physical anisotropic recipient.

4. **Global Piola and stopped derivative boundary.**  For a countable direct
   sum, the global `o(s)` remainder is equivalent to

       sup_k epsilon_k(s) -> 0.

   Differentiability for every fixed branch is insufficient.  For stopped
   weights `p_n`, `||T||<=M` and `||DT||<=L`, the sharp positive majorant is

       L P'(M)=L sum_n p_n n M^(n-1).

   With `p_n=1/[n(n+1)]`, this is finite for `M<1` and diverges at `M=1`.
   The frozen weak graph-TV norm one does not give a common strong `M<1`.

5. **Gate 2/4 collision density in a genuine product chart.**  The Sinai
   invariant density becomes flat only in collision coordinates:

       dmu=c cos(phi)dr dphi=c dr dp,  p=sin(phi).

   For a genuine product chart `Phi(u,s)`, put
   `q=c|det D Phi|`, `a=|partial_u Phi|` and `Z(s)=integral q(u,s)du`.
   Then the exact conditional and holonomy formulas are

       rho_s=q/[Z(s)a],
       lambda_st=a(u,t)/a(u,s),
       J_st=q(u,s)Z(t)/[q(u,t)Z(s)].

   They satisfy `J_st rho_t=rho_s/lambda_st`.  The flat collision density
   does not manufacture the missing stable chart, common root or bounds.

6. **Immutable owner/landing crosswalk and marker obstruction.**  Equal
   owner and landing marginals do not determine same-key support: two binary
   couplings with identical marginals have compatible immutable-key mass
   respectively `1` and `0`.  Even a flat invariant product, identity
   holonomies, a commuting square and equal total landing mass can have
   `P_eta=5/64` and `delta_eta=1/8`.  Hence product geometry and same mass do
   not prove the actual zero-marker row.

7. **All-depth and variation boundary.**  The frozen 96 rows certify zero
   collision shadow, not survival of the required graph-transform charts.
   The number of actual full first-failure upper-bound rows is therefore
   zero, and the sharp positive infinite-survivor lower bound remains zero.
   A future same-key geometric tail

       eta(F_(96+k))<=A sigma^k

   would give `eta(S_infinity)>=eta(S_0)-A sigma/(1-sigma)`.  Separately,
   `P_eta=0` can coexist with transverse variation `N->infinity`.  A
   fixed-material tagged-BV multiplier bridge is now exact, but its physical
   strong/Piola recipient is still absent.

8. **Gate 5 maximal terminal-record overlap.**  On one occurrence measure,
   the round constructs the maximal Borel overlap while retaining and
   updating the time coordinate, and separates overlap, terminal departure,
   owner-switch departure and birth.  The elementary inclusion

       {n>j+1} subset {n>j}

   concerns terminal eligibility only; it does not imply nesting of owner
   sets after owner labels and regularity are imposed.

9. **Sharp full-label RN frontier.**  A positive owner birth or label-switch
   target gives coefficient `infinity`.  If births vanish and overlap is
   nonzero, the surviving full-label density is exactly one, so the optimal
   live coefficient is `1`; charging all departures once still gives
   accounting coefficient `1`.  A total-mass ratio can be `2/3` while the
   `L-infinity` RN ratio remains `1`.  Consequently the terminal-nesting
   route itself cannot reach `w_Z c<1`.

10. **Seven sectors and oriented positive bridge.**  On the overlap, each
    sector has the exact coefficient

        kappa_s^*=ess sup h_next^s/h_j^s,

    after positive-birth and zero-denominator singular parts vanish; the
    common typed coefficient is their maximum.  The actual raw-`Z`, Orlicz
    and pre-cemetery rows remain unpaid.  Before the Round-54 positive pair
    can be identified with the Round-61 oriented owner pair, one must first
    prove the scalar equality

        xi_j^f(X)=m_p=xi_j^r(X),

    followed by two RN rows and common-mode equality.  The frozen data give
    upper bounds, not those actual equalities.

11. **One immutable global registry.**  For a finite standard-Borel
    junction tree satisfying running intersection, a global coupling exists
    exactly when every edge's separator marginals match.  A parity triangle
    (`A=B`, `B=C`, `A!=C`) has uniform compatible singleton marginals but no
    global atom, proving that pairwise compatibility around a cycle is not
    enough.  A physical join additionally requires graph support on pinned
    immutable IDs or pinned Borel crosswalks.  Even a perfect common registry
    plus a finite actual positive moment does not give strong assembly:
    the all-time positive potential must still lie in `L-infinity` on that
    same root.

## Independent audit

The independent Round-66 audit passed without modifying or correcting a
source leaf.

    new Python syntax including Round-66 common:        9/9
    dependency pin rows across four source leaves:   134/134
    source-leaf SHA artifact rows:                     19/19
    source audit/replay/self-test/reemit:                4/4
    source hostile semantic mutations:               980/980 rejected
    source hostile strict-JSON payloads:               16/16 rejected
    source default entry points:                         8/8 exit 2

The audit's own canonical replay, regeneration and byte-identical reemit
pass.  It rejects `87/87` hostile semantic mutations and `4/4` hostile JSON
payloads; its five SHA rows resolve and both default entry points exit `2`.

The cross-leaf replay confirms in particular that:

- shared triangular syntax is not a same-gauge physical crosswalk;
- a local finite-depth material atlas is not an all-depth anisotropic atlas;
- flat Sinai density is not a collision-SRB stable product chart;
- equal marginals and pairwise overlap tests are not a global immutable join;
- zero marker defect is independent of transverse variation;
- terminal eligibility nesting is not owner nesting or strict RN decay; and
- registry gluing and the `L-infinity` positive-potential bound are separate
  obligations.

## Latest technology boundary

Current official sources were checked directly on 2026-07-21.

- `arXiv:2502.07765v2` proves a CLT with error bounds for sequential systems,
  including sequential dispersing billiards, using complex projective cones.
  It does not supply moving-boundary differentiation, a material current,
  strong Piola remainder or the required stable product registry.
- `arXiv:2604.19671v2` differentiates the conditional survival measure of a
  fixed Sinai billiard with respect to the size of a small boundary hole.
  It is a hole-size/survivor-normalization perturbation, not CM2's moving
  collision geometry or all-input strong current.
- `arXiv:2504.16532v3` treats `C^5` Anosov diffeomorphisms of the two-torus on
  smooth Gouëzel--Liverani spaces.  Its smooth full-domain derivative has no
  billiard collision singularity or moving boundary.
- `arXiv:2402.02496v2` gives asymptotic local product structure for invariant
  measures of smooth diffeomorphisms of closed manifolds, not the actual
  singular collision atlas or marker/variation recipient.
- `arXiv:2504.17879v1` studies countable-state uniformly lazy Markov chains
  killed by confining potentials under the direct-step property.  Those are
  not the frozen owner lineage or seven-sector hypotheses.
- `arXiv:2606.10155v1` is a transfer-operator review and supplies no missing
  immutable crosswalk or first-failure constants.

No external theorem is promoted.

## Strict final state

    Gate 1: NOT_CERTIFIED
    Gate 2: NOT_CERTIFIED; official fields 0/17
    Gate 3: NOT_CERTIFIED
    Gate 4: NOT_CERTIFIED; landing join 1/7, fields 1,4,7 partial
    Gate 5: NOT_CERTIFIED; maturity 10/18, complete blocks 0
    complete composite gates: 0/5
    CM2: NO-GO_FOR_CLAIM

## Shortest remaining routes

1. **Common physical root:** emit one graph-supported actual root registry
   carrying path/time/owner, collision/plaque, material/face, live/arrival,
   sector and oriented-pair keys.  Prove exact fragment marginals and shared
   immutable-ID support; anonymous same-law couplings do not count.

2. **Gate 1:** compute the actual relative transfer `C` and both uniform
   endpoint-defect limits on every plaque, verify the loop-correction
   identity, and evaluate all four wedges in that same representative.

3. **Gate 3:** extend the local depth-96 atlas to one all-cell/all-depth
   material atlas with a bounded two-sided trace, uniform direct-sum Piola
   remainder, strong cemetery/current sums and either a common `M<1`
   stopped bound or the exact all-time positive potential.

4. **Gate 2/4:** construct the actual collision-SRB stable product chart and
   common root; prove a same-key full first-failure tail, `P_eta=0`, the
   essential path budget, weighted transverse variation and the physical
   strong/Piola recipient.

5. **Gate 5:** do not reuse terminal nesting as a contraction proof.  Either
   certify the full all-time `L-infinity` positive potential directly or
   supply a physical attenuation mechanism on surviving labels; also remove
   births, pay every sector and one-shot arrival, and complete the oriented
   total-mass/two-RN/common-mode bridge before F5/F6/F10/F11/F14/F15/F17/F18
   can close.

Until these rows coexist on one actual root, the CM2 claim remains a strict
no-go.
