# CM2 Round 57 Gate 4 — palindromic common-refinement `J_cap` closure

Date: 2026-07-20  
Strict status: **the physical common-refinement numerator `J_cap,total`, the
cross-endpoint energy, the single common properisation clock `D_cap`, and a
proper once-charged common terminal two-view carrier are now certified.  This
is not yet one physical proper same-ID first-return kernel.  Intermediate
`C24` avoidance after properisation, later/repeated clocks, physical `q`, the
strong cemetery, Gate 4, and CM2 remain not certified.**

## 1. Frozen scope and the object-level issue

This append-only leaf pins the Round-35 exact slope-four carrier, the
Round-41/42 hereditary and numerical `C24` Growth theorems, the Round-47
postcut ledger, the Round-50 stopped properisation, the Round-51 two-view
measure isomorphism, the Round-52 terminal survivor/cell retyping, the
Round-53 conditional `J_cap -> D_cap` bridge, and both relevant Round-56
physical closures.  It changes no older artifact.

The Round-42 report is additionally hash-pinned at

```text
c81dfbf71e591a34e282cd03f595da947a6db38e4b4c2b2fb503a751c0ef5823
```

because it states the operator scope needed below verbatim:

```text
every unnormalised C24-killed canonical family satisfies
Z(O_s G) <= Z1 Z(G),
Z1=18367592526/360493663.
```

This is stronger than the Round-47 `Z<=P mass` envelope.  The latter requires
a whole proper source and is **not** used on the twice-restricted common
subfamily.

Round 56 proves

```text
J_pair
 = integral sum_c p_c(ell_fw,c^-1+ell_rev,c^-1) dlambda
 < infinity.
```

That alone does not control the branch jumps of the later stopped maps
`P_fw,P_rev`.  A same-metric exact slope-four separator remains valid at the
field level: in the conditional-mass coordinate of one safe slope-four leaf,
a countable piecewise-analytic measure-preserving transport can have

```text
J_pair=2,
M=0,
finite marginal Z,
common mass=499/500,
J_cap,fw=infinity.
```

Thus the closure below uses a genuinely new physical fact—the time-reversal
palindrome—not a false marginal-to-intersection inference.

## 2. Two index layers must not be confused

Use two distinct indices throughout:

```text
y = the Round52 same-parent outer record carrying the SYZ mass estimate,
c = a Round52/56 once-charged physical cell below y carrying
    (Dbar, first-return path, n, K) and proof-only cut tags.
```

The physical cells under one `y` form a finite fibre and reconstruct its
charge exactly once.  The terminal union bound is only asserted after summing
at the `y` layer:

```text
h_cap(y)>249 p(y)/250.
```

No `249/250` lower bound is asserted for an individual proof cell or an
arbitrarily thin survivor subinterval.

## 3. A stronger direct consequence of `J_pair`

For each physical cell put

```text
M_c=ceil(log2(1/min(ell_fw,c,ell_rev,c)))_+.
```

If the minimum length is at least one, `2^M=1`.  Otherwise
`2^M<2/min(ell_fw,ell_rev)`.  Therefore

```text
p_c 2^M_c
 <= p_c+2 p_c/min(ell_fw,c,ell_rev,c),

integral sum_c p_c 2^M_c
 <= nu(X)+2 J_pair
 < infinity.                                      (3.1)
```

Round 51/52 gives

```text
Dbar=0       for M<=310,
Dbar=M-309   for M>=311.
```

Consequently the full dyadic defect moment—not merely `exp(Dbar/6)`—is
finite:

```text
integral sum_c p_c 2^Dbar(c)
 <= nu(X)+2^-309[nu(X)+2 J_pair]
 < infinity.                                      (3.2)
```

This does not pay a new independent later clock.

## 4. Exact stopped-cut palindrome and its off-by-one

Choose the uniform Round-52 terminal time beyond both SYZ thresholds and, if
needed, enlarge it so `H_joint>=1`.  On cell `c` define

```text
L(c)=696 Dbar(c),
K(c)=L(c)+H_joint>=1.
```

Both are immutable integer Borel tags.  The terminal operator `O_s` is not a
pure restriction at the current point.  It performs one collision transfer
and then deletes the descendants in `C24`.  Hence the correct first leg is

```text
closed T_s^(K-1), then one O_s collision.
```

Its surviving output lies at `T_s^K x`.  From there apply

```text
I, then closed T_s^K, then I.
```

Time reversal gives, on every regular branch,

```text
T_s^K I T_s^K=I,
I T_s^K I T_s^K=id.                               (4.1)
```

Thus every survivor returns to the original point `x`.  Singularity,
homogeneity, Growth and deterministic recut operations only split analytic
branch intervals.  With every tag retained, the returned atoms are
subintervals of their exact original slope-four carriers; there is no
transverse redisintegration.

Write

```text
a=360134800/360493663<1,
C_p=4*10^90*360493663/358863,
Z1=18367592526/360493663.
```

The closed recurrence and universal Round-42 one-step bound give, uniformly
in the unbounded finite tag `K`,

```text
first closed leg:  Z <= Z_in+(C_p/2) mass_in,
terminal O_s:      Z <= Z1 Z_previous,
return closed leg: Z <= Z_previous+(C_p/2) mass_in,

Z_return
 <= Z1 Z_in+[(Z1+1)C_p/2] mass_in
 < infinity.                                      (4.2)
```

The stopped kernel is Borel by its countable decomposition over `{K=k}`.
The palindrome uses no survivor normalization.

### The second palindrome may be nonproper

This scope is essential.  After imposing the forward predicate and moving
the restriction to `I(B)`, the input to the reverse palindrome may be an
arbitrarily thin, highly nonproper positive canonical subfamily.  The
original `Dbar` does not re-properise that restriction.  Equation (4.2)
remains valid because Round 42 gives `Z(O_s G)<=Z1 Z(G)` for **every
unnormalised canonical finite-`Z` family**.

Properness is used only in the already frozen Round-52 marginal SYZ theorem
on the two original full marginal laws.  Its `<1/500` hit bounds supply the
`y`-level common-mass union bound.  They are not rerun on the conditioned
reverse subfamily.  The Round-47 proper-source `P mass` estimate is likewise
not used in the second palindrome.

## 5. Sequential common-restriction construction

Start from the Round-56 exact once-charged law

```text
A_c <-> B_c=T_s^n(A_c) <-> I(B_c),
Z_A+Z_I(B) <= J_pair < infinity.
```

The construction is:

1. Apply the stopped-cut palindrome on `A_c`.  It returns the exact pullback
   of the forward terminal survivor as a finite-`Z` positive subfamily
   `G_A,fw` of the original `A_c` carriers.
2. Replay the Round-41/42 hereditary first-return terminal construction
   `A->B` on this dominated positive subfamily and apply `I`.  The result on
   the exact tagged `I(B_c)` carriers has finite total `Z`.
3. Apply the stopped-cut palindrome there.  The returned family is the exact
   intersection of the forward and reverse terminal predicates, still
   represented on `I(B_c)`, with finite `Z`.
4. Replay the paired reverse first return `I(B)->I(A)` and apply `I`.  This
   returns an exact common raw restriction `G_A,cap` on subintervals of the
   original `A_c subset R_n` source carriers, with finite `Z`.
5. Push `G_A,cap` by `T_s^L` to the Round-51 forward proper starting view.
   Separately replay it to `I(B)` and push by the same `T_s^L` to the reverse
   proper starting view.  The closed recurrence gives finite `Z` in both
   geometries.  Continuing through the fixed terminal schedule also leaves
   finite terminal-output `Z`.

All uses are positive-subfamily uses.  The original first-return path and
`n,L,K,c,y` tags are immutable.  Each raw point is charged once; forward and
reverse are two views of that one restriction.

## 6. Borel components and physical coarsening

Canonical half-open proof atoms form countable standard-Borel regular
connected interval kernels with exact outer disintegration.  That connected
refinement already supplies a valid finite-`Z` standard-family
representation for Round 53.

For the physical maximal-component version, work within one fixed
`(y,view,physical-carrier-chart)`.  Merge two proof intervals only when their
**actual union** is connected: the common endpoint has its unique half-open
owner, belongs to the regular common set, and the two-atom union is connected.
Closure-touching across an omitted singular, null or cemetery puncture is
never merged.  Components are never merged across `y`.

Every positive maximal interval contains a rational chart point.  The least
rational owner and the Borel infimum/supremum of its endpoint section give a
standard-Borel enumeration.  Null singleton components remain in the frozen
cemetery.

If a physical component is the union of proof intervals, then

```text
p/ell = a length-weighted average of the proof p_j/ell_j
      <= sum_j p_j/ell_j.
```

Therefore

```text
J_cap,physical
 <= Z_cap,fw-proper-view,proof+Z_cap,rev-proper-view,proof
 < infinity.                                      (6.1)
```

Each `y` has a finite physical `c`-fibre, and every tagged replay has finite
depth, so `J_cap(y)<infinity`.  Global integrability is not inferred from
this pointwise fact.  Write

```text
P(Z,m)=Z1 Z+[(Z1+1)C_p/2]m
```

for one stopped-cut palindrome.  The Round-41/42 hereditary total
first-return terminal resolvent supplies finite uniform constants `A_R,B_R`
such that

```text
R(Z,m)=A_R Z+B_R m
```

bounds each tagged `A->B` or `I(B)->I(A)` total replay after summing all
return depths.  Starting from `Z_0<=J_pair`, the successive global charges
obey

```text
Z_1 <= P(J_pair,nu(X)),
Z_2 <= R(Z_1,nu(X)),
Z_3 <= P(Z_2,nu(X)),
Z_4 <= R(Z_3,nu(X)).
```

The two proper-view outputs are closed affine images of `Z_4`.  The constants
`Z1,C_p,A_R,B_R` are independent of `y,c,n,Dbar,K`; `a^r<=1` absorbs every
unbounded stopped depth and the hereditary resolvent sums every first-return
depth.  Hence the global estimate, rather than pointwise finiteness, gives

```text
J_cap,total=integral J_cap(y) dlambda(y)<infinity.  (6.2)
```

The exact cross-component comparison from this leaf then also certifies the
physical crossed endpoint energy `E_cross<infinity`.

## 7. Actual Round-53 promotion

Round 53 may now be applied with its hypothesis discharged.  Put

```text
z_cap(y)=J_cap(y)/h_cap(y),

D_cap(y)=0                              if z_cap<C_p,
D_cap(y)=least d>=1 with
           2^-d z_cap<C_p/2             otherwise.
```

After `696 D_cap(y)` synchronized closed iterations, both
orientation-specific representations are proper.  The common raw points
remain once charged; one proper view may be selected and reverse data
transported exactly as in Round 51.

Besides the Round-53 `exp(D_cap/6)` moment, minimality gives the stronger

```text
2^D_cap <= 4 z_cap/C_p    on D_cap>0,

integral h(y)2^D_cap(y) dlambda(y)
 <= H+(4/C_p)J_cap,total
 < infinity.                                      (7.1)
```

Hence this leaf certifies:

```text
physical J_cap,total:                         CERTIFIED_FINITE
physical crossed endpoint energy E_cross:     CERTIFIED_FINITE
single D_cap exp/dyadic clock moments:         CERTIFIED_FINITE
proper common terminal two-view carrier:       CERTIFIED
```

## 8. Exact nonpromotion

The common raw restriction is a positive subkernel of the original
`A_c subset R_n` source law.  It inherits the original `R_n` no-earlier-`C_s`
property only along the first `A->B` return path.  It does **not** prove that
the postproperisation/terminal orbit avoids `C24` at every intermediate
collision.

The common raw restriction does retain the original Borel stopping graph
`tau_R=n`, `A->B`, its same-ID tag and its no-earlier-`C_s` property.  What is
missing is properness of that common landing restriction in the required
physical target geometry.  Nor do the two later stopped orientation-specific
pushforwards define one physical first-hit landing map.  The remaining join
must either properise the inherited landing kernel with the correct semantics,
or provide another `tau_cap` and `Q_cap(x)=T_s^tau_cap(x)` with the same
physical restriction ID, required first-hit/intermediate-avoidance semantics,
exact once charge, and a proper target kernel.

Independently, fixed-stage moments do not imply an all-stage moment.  On
atoms of mass `2^-n`, clocks `L_n=n^2` have finite moments at every fixed
stage while

```text
sum_n 2^-n exp(n^2/4176)=infinity.
```

Thus the full dyadic `Dbar` and `D_cap` moments do not silently pay all later
or repeated recovery clocks.

Strict frontier:

```text
physical proper same-ID first-return kernel:      NOT_CERTIFIED
intermediate C24 avoidance after properisation:   NOT_CERTIFIED
later/repeated recovery-clock moments:            NOT_CERTIFIED
physical collision-time q in L^(6/5):             NOT_CERTIFIED
strong singular/current cemetery:                 NOT_CERTIFIED
Gate 4:                                            NOT_CERTIFIED
complete composite gates:                         0/5
CM2:                                               NO-GO_FOR_CLAIM
```

## 9. Latest-tech audit

A targeted check of Demers--Liverani,
*Recent Progress in the Application of Transfer Operators to Dispersing
Billiards*, arXiv:2606.10155v1, found context on transfer operators, coupling
and absolute-continuous holonomy, but no theorem directly instantiating this
exact stopped two-view common-refinement numerator, same-ID first-hit kernel,
all-intermediate avoidance or strong cemetery.  The official PDF checked on
2026-07-20 has SHA-256

```text
3330b5467c5c7107e58ba7966a0386f2ab51415990399b387ad2f7e571f61798.
```

Fixed-depth semialgebraic/Hardt finiteness is useful only on one
`{Dbar=d}` stratum; its format grows with the unbounded stopped depth.  The
palindromic Growth replay is what removes the need for a component-count
constant uniform in `d`.

## 10. Executable evidence

Artifacts:

- `cm2_gate34_round57_exact_slope4_cross_endpoint_frontier_cert.py`;
- `cm2_gate34_round57_exact_slope4_cross_endpoint_frontier_verifier.py`;
- `cm2-gate34-round57-exact-slope4-cross-endpoint-frontier-manifest-2026-07-20.json`;
- `cm2-gate34-round57-exact-slope4-cross-endpoint-frontier-manifest-2026-07-20.sha256`.

The verifier independently reconstructs the dyadic `M/Dbar` inequalities,
the exact palindrome affine coefficients, the one-collision operator
off-by-one, the common-mass index layer, the component/coarsening policy, the
`D_cap` promotion boundary and every strict nonpromotion field.  It validates
all pinned hashes, deterministic replay and byte-identical re-emission.

Frozen validation targets:

```text
Python syntax:                    2/2 PASS
dependency manifest hashes:      10/10 PASS
pinned proof-report hashes:       1/1 PASS
independent arithmetic/replay:    PASS
hostile mutations:               73/73 REJECTED
deterministic re-emission:        byte-identical PASS
certificate/verifier defaults:   both exit exactly 2
strict Gate4 / CM2:               NOT_CERTIFIED / NO-GO_FOR_CLAIM
```
