# CM2 Round 54 full direct assault

Date: 2026-07-20  
Strict verdict: **0/5 complete gates; CM2 NO-GO**

## Gate 4: numerical fixed-map Growth, but not yet a numerical SYZ clock

For every registered frozen stationary map `T_sigma`, `|sigma|<=1/400`,
the Round-42 one-step square-root length bound now gives the explicit
official-type constant

```text
C_e=(5962448355/5191)^2<2^41.
```

After paying both adapted/Euclidean line-element conversions, the physical
endpoint Growth lemma is numerical:

```text
a=360134800/360493663,
m_E{r_W,n<epsilon}<=C_gr*(a^n+ell_E(W))*epsilon,
C_gr=(64566981624654*10^92)/239122379.
```

The dependency join also pins the Euclidean SYZ recurrence itself,

```text
Z_n^E/mass<=(C_p^E/2)*(1+a^n*Z_0^E/mass),
```

with properness test `a^N Z_0^E/mass<1`.  These are genuine numerical
fixed-map results.  They are not a theorem for arbitrary moving
configuration sequences.

## Gate 4: the gap-rate base is only a conditional candidate

For a rank-`R>=1` magnet gap, suppose the same magnet supplies

```text
c_g>=2^-G,
|W_tilde|>=2^-W,
```

and, crucially, a same-ID theorem puts the official `checkrho|V` law into
the registered numerical density cone after a rank-independent clock debit
`I_density`.  Then the exact conditional recovery bound is

```text
time < 697R+696(82+2G+2W)-1+I_density.
```

Exact rational arithmetic gives

```text
(9997/10000)^697>144000/180337=Lambda^-1,
(2499/2500)^697<=144000/180337.
```

Thus `9997/10000` is a safe replacement-base candidate **conditional on
that density join**.  Independent audit found that the join is not currently
installed, so the official Round-53 five-row `lambda` remains
`NOT_CERTIFIED`; no numerical `C_bar,R,zeta0,S,Delta,H_bump,H_out` follows.

The numerical Growth lemma also atomises the gap prefactor.  Conditional on
`tildeC<=T` and the same density carrier,

```text
C_g''<=K_g*T,
K_g=
2537103957434907110652*10^94/2481327254508611.
```

The still-physical rows are now precisely the stable-length comparison,
`c_g`, `|W_tilde|`, the `checkrho` density/intercept join, and the
rank-zero/top/excess recovery clock.

## Gate 4: metric-safe interval-atlas interface

The direct-C24 target remains

```text
eta_sigma^*>=43008/14285703575.
```

For a branch word of depth `N`, per-step derivative upper `J`, target width
`w`, and source-length upper `L`, the fail-closed builder uses

```text
adapted data:   eta>=w/(J^N L),
Euclidean data: eta^*>=(20/3807)*w/(J^N L).
```

Mixed metrics are rejected.  A successful physical row must include an
exact finite parameter cover, the full selected long-curve cover, direct-C24
target identity, clean branch word, singularity clearance, side margin and
interval-wide persistence.  The physical row count is still zero; hence no
uniform numerical `eta_sigma^*`, `H_cover` or `beta` is promoted.

## Gate 4/5: the survivor-mass normalization join is closed

The unique state of the Round-36/37 killed recurrence is now typed as

```text
F_n=(T_s^n)_#(mu_s|Q_n),
m_n=||F_n||=mu_s(Q_n)=mu_s(C_s)S_n.
```

The return-level alternative `m_n=mu_s(R_n)` is invalid: `R_n` does not
evolve to `R_(n+1)` under the one-step killed state machine.  From the exact
Round-28 survivor tail,

```text
sum_(n>=0)m_n
 <mu_s(C_s)*(61445312500000/3087)*N_open
 <(142656353125/18522)*N_open
 <infinity.
```

This closes the same-ID level/normalization join and physical survivor-mass
`l1`.  The bound is finite but nonnumerical because `N_open` is still not
materialised.

## Gate 4/5: coarse terminal `Z` is not `J_pair`

Keep the two terminal cores correctly oriented:

```text
C_fw=C_s,
C_rev=I(C_s).
```

Time reversal preserves the relevant collision measure and orientation
carrier norm, so the same core multiplier `2000/1999` applies.  With
`M=sum m_n` and `Phi_pair=sum(Phi_fw,n+Phi_rev,n)`, the certified coarse
terminal estimate is

```text
Z_term,coarse,pair
 <=[(2000/1999)/(1-a)]
   *[a Z_pair,0+2bM+a Phi_pair],
b=2*10^90,
(2000/1999)/(1-a)=360674000/358863.
```

It is deliberately not renamed `J_pair`.  A unit interval has coarse
`Z=1`, whereas subdivision into `N` equal natural/image cells gives refined
`Z=N`.  Therefore two independent interfaces remain:

```text
all-time C24-complement face forcing Phi_pair in l1: NOT_CERTIFIED,
same-ID terminal cell/image-recut Z -> J_pair:       NOT_CERTIFIED.
```

Physical `J_pair`, the defect moment `I_D`, physical `q` and strong cemetery
remain open.

## Gate 4: common-refinement atlas without a boundary sum

The common terminal survivor now has a physical Borel countable atlas.
After a countable compact regular exhaustion, each fixed
stopped-time/branch/rank substratum has finitely many analytic interval
components and the two stopped views are analytic diffeomorphisms there.

This does not imply recordwise `J_cap<infinity`.  In one original record,
take one component on each substratum with

```text
p_k=ell_fw,k=ell_rev,k=2^-k.
```

The total mass is one, every substratum is finite, but each orientation
contributes one at every level, so paired `J_cap=infinity`.  Accordingly

```text
Borel countable common atlas:                  CERTIFIED,
finite components per compact substratum:     CERTIFIED,
recordwise/global J_cap, D_cap, properisation: NOT_CERTIFIED.
```

## Gate 5: a legal collar bridge only on `A_col`

At `s=0`, define the collar-admissible owner subregistry

```text
A_col={omega:d(omega)>0},
```

where `d` is the selected one-sided clearance from the already-owned anchor
face to the next **other** fixed-word boundary.  The anchor itself is
excluded.  A strict dyadic half-open collar is carried on the joint labelled
space `(omega,x)`, so overlapping physical images never lose the record
label.  The stagewise kernels obey

```text
Tr_j E_j=Id,
K_trace_(j,t)=Tr_t K_word_(j,t) E_j,
||E_j||_mass=||Tr_t||_mass=1
```

on owner laws restricted to `A_col`.  The target is the branch-side mapped
root.

This is not a uniform proper trace bridge.  Neither positive/full owner
mass of `A_col` nor finiteness of

```text
Z_col(E nu)=integral_(A_col)ell(omega)^(-1)dnu(omega)
```

is known.  An infinite homogeneity-shell separator gives `Z_col=infinity`
at a grazing accumulation root, while the prior parent debt stays finite
(`Z_B=2^14` in the frozen example).  Thus trace contraction and the positive
face tower remain open.

## Gate 5: signed hit/miss and directional-BV subroutes

The signed route is now typed with the real physical flux, not with a fake
identity `sigma_e=2^B`.  Split the signed source flux by Jordan
decomposition, push its two parts through hit/miss in opposite order, and
couple the resulting equal-mass positive measures with the canonical
normalised-product Borel kernel.  Their mass satisfies

```text
m_p<=C_flux=8064/5,
||J_p||_(BL*)
 <=integral min(2,d(y+,y-))d pi_p.
```

If a future paired-image theorem supplies rate `rho`, the old weight works
directly because `w_Z*rho=(1+rho)/2<1`.  No such physical distance rate is
known, and signed cancellation pays neither positive F10 nor cemetery.

For the Eulerian current, with collision area form `mu=R dr dp`, define the
trace-equipped directional space `BV_X^tr`.  The exact branch identity is

```text
T_h=-D_Xh+gamma_Xh,
gamma_Xh=(R h X dot n_U)H^1|partial U.
```

After this order reduction, every finite regular suffix has TV multiplier
one.  This is a genuine `C_dyn=1` subcarrier, but the existing source ledger
does not control its directional variation or same-ID normal trace.  In the
fixed Gate-3 carrier, bulk `-D_Xh` belongs to the
`(C^{1,alpha}(N))*` summand; only boundary graph flux belongs to the `41,508`
`M(I)` slots.  Physical F17, `Q_s/R_s` and `MT_DQ` are not promoted.

## Latest-technology audit

The Round-54 leaf audits rechecked on 2026-07-20 the snapshots

```text
1210.0011v4 / 2104.06947v3 / 2604.19671v2 /
2604.25881v1 / 2606.10155v1.
```

The hash-pinned `1807.02330v4` review is retained from Round 53, and
`2502.07765v2` was also reviewed in the current provenance stack.  The
available mechanisms still leave
existential the magnet density/mixing rows, parameter-uniform interval
atlas, all-time positive trace contraction, terminal refinement cost,
collar debt and physical `BV_X^tr` operator embedding.  No checked source
directly closes one of those interfaces.

## Strict status

```text
Gate 1: NOT CERTIFIED
Gate 2: NOT CERTIFIED
Gate 3: NOT CERTIFIED
Gate 4: NOT CERTIFIED
Gate 5: NOT CERTIFIED (10/18; complete blocks 0)
CM2:    NO-GO
```

## Validation

- Python syntax: `6/6 PASS`;
- strict JSON, dependency pins, exact arithmetic and deterministic replay:
  PASS;
- hostile manifest mutations:
  `216/216 + 197/197 + 141/141 = 554/554` rejected;
- certificate/verifier defaults: `6/6`, each exactly exit `2`;
- deterministic manifest regeneration: `3/3` byte-identical;
- all three leaf SHA ledgers and the recursive Round-54 ledger: PASS.

Independent read-only audits forced the gap-rate carrier downgrade, the
coarse-terminal/cell-refinement separation, the countable-atlas downgrade,
the anchored collar typing, the physical-flux coupling, and the
area-density normal trace.  Every required correction was incorporated
before the final replay.  Mutation suites are fail-close regression tests,
not substitutes for the mathematical proofs.

## Next route

1. Put the official `checkrho|V` on the same-ID numerical density carrier,
   quantify its rank-independent entry clock, and then materialise
   `tildeC,c_g,|W_tilde|,C_g'',zeta0,r_base,S`; only then promote the
   conditional `9997/10000` gap base and compute `Delta,H_bump,H_out`.
2. Populate the interval-atlas builder with clean physical branch words,
   exact finite parameter/curve covers, adapted-compatible widths and
   interval persistence until the direct threshold
   `43008/14285703575` holds uniformly.
3. Prove both all-time `Phi_pair` summability and the same-ID terminal
   natural-cell/image-recut refinement bound; together with the closed mass
   join these yield `J_pair` and then `I_D`.
4. Prove within-record summability across the common countable atlas and an
   outer integrable `J_cap` majorant; then establish physical first-return,
   intermediate avoidance and every remaining recovery-clock moment.
5. On Gate 5, prove positive/full `A_col` trace mass plus summable collar
   debt, or instead a physical hit/miss paired-image rate; in parallel bind
   the `BV_X^tr` source/normal-trace cost to the physical block.
6. Finish all-face F10, strong F13, F14/F15/F17/F18 and cemetery, then return
   to Gate-3 `MT_DQ`.  Gate 1 and Gate 2 remain independent structural poles.

Leaf reports:

- `cm2-gate34-round54-numeric-growth-gap-rate-interval-atlas-frontier-assault-2026-07-20.md`;
- `cm2-gate34-round54-survivor-mass-terminal-extraction-common-atlas-assault-2026-07-20.md`;
- `cm2-gate5-round54-collar-pairing-directional-bv-frontier-assault-2026-07-20.md`.
