#!/usr/bin/env python3
"""Materialize the round-eight positive reconstruction into all eleven papers.

The controlling modules are deliberately generated from one immutable registry so
that the source used by every paper is byte-identical to its registered revision
source.  The script is deterministic and idempotent.
"""
from __future__ import annotations

from pathlib import Path
import hashlib
import json
import re

ROOT = Path(__file__).resolve().parents[1]
SRC = ROOT / "revision" / "round8-referee-final"
PAPERS = ROOT / "papers"

HEADER = "% Generated by tools/materialize_round8_referee.py; do not hand-edit.\n"

A1 = r'''
\section{Round-eight reconstruction: complete dynamic germs and routed Hamiltonian suspension}
\label{sec:r8-a1}

The finite biseam complex used in the preceding revision did not remember the
oriented side of the branch preimages of the next vertical seam.  We replace it
by the complete symbolic germ surface.  This simultaneously records every
forward and backward singularity cut and turns the return map into an honest
bijection before any transfer or response argument is made.

\subsection{The complete germ surface}

Let $p_i(a)>0$, $\sum_{i=1}^dp_i(a)=1$, and
$s_i(a)=\sum_{j\le i}p_j(a)$.  Put $\mathcal A=\{1,\ldots,d\}$ and
$\Omega=\mathcal A^{\mathbb Z}$ with shift $\sigma$.  For
$\omega\in\Omega$ define
\[
 q_a(\omega)=\sum_{n\ge0}p_{\omega_0}(a)\cdots p_{\omega_{n-1}}(a)
 s_{\omega_n-1}(a),
\]
\[
 p_a(\omega)=\sum_{n\ge1}p_{\omega_{-1}}(a)\cdots p_{\omega_{-n+1}}(a)
 s_{\omega_{-n}-1}(a).
\]
Empty products are one.  At an endpoint the two symbolic expansions are not
identified.  The \emph{complete dynamic germ surface} is the graph
\[
 \widehat\Sigma_a^\infty
 =\{(q_a(\omega),p_a(\omega),\omega):\omega\in\Omega\}
\]
with the topology transported from $\Omega$.  Its physical projection is
$\pi_a(q,p,\omega)=(q,p)$.

\begin{lemma}[All dynamic seams are present]
\label{lem:r8-a1-all-seams}
For every $N\ge0$, the partition of $\widehat\Sigma_a^\infty$ into cylinders
$[\omega_{-N},\ldots,\omega_N]$ refines
\[
 \bigvee_{|j|\le N}\pi_a^{-1}F_a^{-j}\mathcal P_a,
\]
where $\mathcal P_a$ is the physical branch partition.  In particular the
one-step cuts $q=s_{i-1}+p_i s_j$, all of their iterated preimages, and all
oriented sides at their intersections are distinct strata of the cylinder
system.
\end{lemma}

\begin{proof}
The coordinate $\omega_n$ is exactly the branch of $F_a^n\pi_a(\omega)$.
Therefore a cylinder fixes the side of each seam encountered for
$-N\le n\le N$.  Conversely, away from endpoints those branch labels determine
the cylinder.  At endpoints the symbolic expansion is retained rather than
identified, so both oriented sides remain distinct.  This proves the claimed
refinement at every depth.
\end{proof}

Define
\[
 \widehat F_a(q_a(\omega),p_a(\omega),\omega)
 =(q_a(\sigma\omega),p_a(\sigma\omega),\sigma\omega).
\]

\begin{theorem}[Complete-germ baker bijection]
\label{thm:r8-a1-bijection}
The map $\widehat F_a$ is a homeomorphism of
$\widehat\Sigma_a^\infty$, with inverse induced by $\sigma^{-1}$.  Its physical
projection is the generalized baker formula
\[
 q'=\frac{q-s_{i-1}}{p_i},\qquad p'=s_{i-1}+p_i p,
 \qquad i=\omega_0,
\]
on every symbolic germ.  Thus the formula is single valued on every preimage
seam and every multiple seam crossing.
\end{theorem}

\begin{proof}
The series defining $q_a,p_a$ converge uniformly because
$\max_i p_i\le1-(d-1)p_*$ on compact parameter sets.  Substitution after a
shift gives the displayed affine formula.  The old branch is $\omega_0$ and
the new branch is $\omega_1$, including both endpoint expansions.  Shift and
inverse shift are continuous in the product topology, proving the assertion.
\end{proof}

\subsection{A depth-weighted current scale}

For a finite word $w=\omega_{-r}\ldots\omega_s$, let $[w]$ be its cylinder and
$\partial^\alpha[w]$ the oriented parameter derivative current of order
$|\alpha|$ obtained by differentiating the moving coding map $\pi_a$ on that
cylinder.  Put $\ell(w)=r+s+1$.  Fix numbers
\[
 0<\vartheta<\vartheta_+<1,
 \qquad d\vartheta_+<1,
\]
and define
\[
 \|u\|_{m,+}=\sum_{|\alpha|\le m}
 \sum_w \vartheta_+^{\ell(w)}
 \bigl|\langle u,\partial^\alpha[w]\rangle\bigr|,
\qquad
 \|u\|_{m,-}=\sum_{|\alpha|\le m-1}
 \sum_w \vartheta^{\ell(w)}
 \bigl|\langle u,\partial^\alpha[w]\rangle\bigr|.
\]
The cylinder relations are quotiented before completion, so the same current
is not counted under two refinements.  Denote the completions by
$\mathscr B_{a,m}^+$ and $\mathscr B_{a,m}^-$.  The transfer operator is
\[
 \mathcal L_a u=u\circ\widehat F_a^{-1}
\]
with Bernoulli Jacobian weights when acting on densities.

\begin{lemma}[Dynamic-cut Lasota--Yorke estimate]
\label{lem:r8-a1-ly}
For every finite $m$ there are $C<\infty$ and $0<\rho<1$, uniform on a compact
parameter interval, such that
\[
 \|\mathcal L_a^n u\|_{m,+}
 \le C\rho^n\|u\|_{m,+}+C\|u\|_{m,-}.
\]
The inclusion $\mathscr B_{a,m}^+\hookrightarrow\mathscr B_{a,m}^-$ is compact.
\end{lemma}

\begin{proof}
Shift removes one future symbol and adds one past symbol.  After quotienting
cylinder refinements, every depth-$k$ coefficient has at most $d$ children.
The choice $d\vartheta_+<1$ absorbs this complexity.  A parameter derivative
creates only the two endpoint currents of the affected cylinder and lowers the
available derivative order by one; these terms are placed in the weak norm.
Iteration gives the displayed inequality.  For compactness, truncate the
cylinder depth at $N$ and the derivative order at $m-1$.  The truncated range
is finite dimensional, while the tail is bounded by
$C(\vartheta/\vartheta_+)^N$.  Hence the inclusion is a norm limit of finite
rank maps.
\end{proof}

\begin{theorem}[Spectrum and finite-order response on all generated cuts]
\label{thm:r8-a1-response}
The eigenvalue one of $\mathcal L_a$ is simple and the remainder of the
spectrum on $\mathscr B_{a,m}^+$ lies in a disk of radius less than one.  If
$G_a$ has $m$ parameter derivatives in the depth-weighted current scale, then
$a\mapsto\nu_a(G_a)$ is $C^m$.  Every derivative is an absolutely convergent
sum of cylinder interior terms and oriented currents on all iterated seams.
\end{theorem}

\begin{proof}
Write a function as the sum of its martingale differences with respect to the
future cylinders.  On the mean-zero subspace, the shift removes the first
martingale difference and the depth weight gives a strict contraction.  A
unit-modulus eigenvector has all martingale differences zero in both time
directions and is constant.  The compact embedding in
Lemma~\ref{lem:r8-a1-ly} then gives a spectral gap.

In moving symbolic coordinates only the Bernoulli weights and the coding map
depend on $a$.  Differentiating a depth-$N$ cylinder expectation gives finitely
many interior derivatives and endpoint currents.  The number of depth-$N$
terms grows at most like $d^N$, whereas their norm is bounded by
$\vartheta_+^N$ and the resolvent contribution by $\rho^N$.  The chosen weights
make the differentiated series normally convergent through order $m$.
\end{proof}

\subsection{A symplectic router and an autonomous realization}

The previous attempt incorrectly required branch motions with overlapping
source and target regions to have disjoint support in one physical plane.  We
separate them in an auxiliary canonical routing coordinate.  Let
$(y,\eta)$ be canonical coordinates and choose disjoint routing tubes
$\mathcal T_i$ centered at $y=i$.  A compactly supported Hamiltonian lift first
moves the $i$th incoming strip into $\mathcal T_i$, a second Hamiltonian applies
the affine exact symplectic branch map there, and a third lowers it to the
$i$th outgoing horizontal port.  The three stages occupy disjoint clock
intervals, while the branch tubes are disjoint in $(y,\eta)$ even when their
physical projections overlap.

\begin{lemma}[Exact symplectic routing]
\label{lem:r8-a1-router}
There is a smooth periodic Hamiltonian
$K_{a,\tau}(q,p,y,\eta)$ on a four-dimensional exact symplectic routing manifold
such that its time-one map on the incoming section is the complete-germ baker
map and its routing coordinate returns to $(y,\eta)=(0,0)$.  The construction
is $C^{m+1}$ in $a$ and has matching flat jets on every symbolic germ.
\end{lemma}

\begin{proof}
The lift and lowering stages are translations generated by cutoff multiples of
$\eta$.  Inside tube $i$, the affine branch has generating function
\[
 S_i(q,P)=p_i^{-1}(q-s_{i-1})(P-s_{i-1}),
\]
which generates $Q=(q-s_{i-1})/p_i$ and
$p=(P-s_{i-1})/p_i$.  Interpolating $S_i$ to the identity with a clock cutoff
gives an exact symplectic isotopy.  All branch isotopies are disjoint in the
routing coordinate.  Flat cutoffs make their jets agree at tube boundaries.
The final lowering is the inverse of the lift, so the auxiliary coordinates
close exactly.
\end{proof}

On the extended phase space with clock pair $(\tau,E)$ set
\[
 H_a=E+K_{a,\tau}.
\]

\begin{theorem}[Autonomous routed suspension]
\label{thm:r8-a1-suspension}
The Hamiltonian flow of $H_a$ has exact first return time one to
$\{\tau=0,y=\eta=0\}$ and return map $\widehat F_a$.  It preserves Liouville
flux and realizes every dynamic germ, not merely a positive-measure core.
\end{theorem}

\begin{proof}
Hamilton's equation gives $\dot\tau=1$.  During one clock period the lift,
branch generating-function isotopy, and lowering occur in that order.  The
router lemma identifies their composition with $\widehat F_a$ and returns the
auxiliary pair to zero.  Each stage is exact symplectic, hence the section flux
is preserved.  Endpoint expansions occupy distinct routed germs and therefore
never require an ambiguous choice.
\end{proof}

\subsection{Work, calibration, and universality}

Place a normalized work form $\epsilon_i\rho_i(\tau)d\tau$ in the incoming
routing tube before the branch motion.

\begin{proposition}[Full-port work]
\label{prop:r8-a1-work}
For every return segment with incoming symbol $i$,
\[
 \int_0^1\epsilon_i\rho_i(\tau)\dot\tau\,dt=\epsilon_i.
\]
Thus the work is the additive cylinder cocycle on the complete germ surface,
including every seam germ.
\end{proposition}

\begin{proof}
The symbol is constant during the work stage and $\dot\tau=1$.  The integral of
$\rho_i$ is one.  Concatenation proves the cocycle identity.
\end{proof}

\begin{theorem}[Finite-alphabet universality and conditional calibration]
\label{thm:r8-a1-universality}
Every smoothly parameterized finite Bernoulli shift with positive weights and
every finite additive port cocycle admits the complete-germ routed Hamiltonian
realization above.  A cash-additive law-invariant time-consistent certainty
equivalent has the same coefficient as the mechanical exponential likelihood
exactly when equality of the two normalized likelihood cocycles is imposed.
\end{theorem}

\begin{proof}
The coding, depth-weighted transfer scale, and router construction used only
finiteness and positivity of the alphabet weights.  For calibration, cash
additivity and time consistency give constant absolute risk aversion.  Equality
of normalized likelihoods on a nonconstant port cocycle forces equality of
the two exponential coefficients; without that compatibility axiom no such
identification follows.
\end{proof}
'''

A2 = r'''
\section{Round-eight reconstruction: renormalized birth bundle, UNI, and the correctly scaled vector--roof LLT}
\label{sec:r8-a2}

We distinguish three logically different ingredients: the physical quotient
through orbit births, compact-frequency aperiodicity, and high-frequency
nonlocal integrability.  The local limit statement is written in sum variables
first and then translated to windows for the normalized roof average.

\subsection{The renormalized physical current bundle}

Let $E_R:\mathbb B\to\mathscr D'$ be the realization of the ambient trace
coordinates.  At a simple birth $R_0$ choose a smooth ambient splitting
$\mathbb B=B^{\rm old}\oplus B^{\rm b}$ for which
\[
 E_R(x,y)=E_R^{\rm old}x+(R-R_0)\widetilde E_R^{\rm b}y.
\]
Define the fiber norm
\[
 \|(x,y)\|_{\mathbb B_R^\sharp}
 =\|x\|+|R-R_0|\|y\|
\]
after quotienting $\ker E_R$.  The physical range is given its inherited
anisotropic distribution norm, not a tautological quotient norm.

\begin{lemma}[Uniform birth chart]
\label{lem:r8-a2-birth}
If $E_{R_0}^{\rm old}$ and $\widetilde E_{R_0}^{\rm b}$ have transverse closed
ranges, then, after shrinking the parameter chart, the induced realization
\[
 \overline E_R:\mathbb B_R^\sharp/\ker E_R\longrightarrow\operatorname{Ran}E_R
\]
is an isomorphism with uniformly bounded norm and inverse norm.
\end{lemma}

\begin{proof}
Rescale the birth coordinate by $z=(R-R_0)y$.  In $(x,z)$ coordinates the
realization is $E_R^{\rm old}x+\widetilde E_R^{\rm b}z$, a continuous
perturbation of a bounded-below operator on the transverse sum at $R_0$.
The bounded inverse theorem is uniform on a smaller compact chart.  Pulling the
norm back gives exactly $\|x\|+|R-R_0|\|y\|$.  No inverse of an order-one
ambient birth coordinate is claimed at the point where its physical current
vanishes.
\end{proof}

\begin{theorem}[Continuous physical quotient field]
\label{thm:r8-a2-field}
The old, birth, death, face, and corner charts glue to a Hausdorff continuous
field $\{\mathscr B_R^{\rm phys}\}_{R\in K}$.  The billiard transfer operators
and their first $m$ parameter derivatives act on one fixed strong/weak scale
of this field and satisfy uniform Lasota--Yorke estimates.
\end{theorem}

\begin{proof}
On overlaps, both charts are identified through their common physical current.
Lemma~\ref{lem:r8-a2-birth} makes the transition maps uniformly bounded.  A
death chart is the reversed birth chart.  Trace jets are triangular by
codimension, and each moving-boundary derivative lowers one strong order.  The
usual branch estimate is therefore uniform after the renormalized coordinate
is used.  Compactness follows from finite chart cover, cylinder truncation,
and one-order loss.
\end{proof}

\subsection{Periodic separation and a finite family of certificates}

Let
\[
 c_R(v)=\bigl(S_v\kappa_R^{(1)},S_v\kappa_R^{(2)},S_v\tau_R,|v|\bigr)
 \in\mathbb R^4
\]
for a periodic inducing word $v$.

\begin{lemma}[Periodic span algorithm]
\label{lem:r8-a2-span}
Suppose no nonzero affine combination of
$\kappa_R^{(1)},\kappa_R^{(2)},\tau_R,1$ is a coboundary on the physical
quotient.  Then periodic vectors affinely span $\mathbb R^4$.  Enumerating
admissible primitive words therefore terminates with five words
$v_0,\ldots,v_4$ whose difference determinant is nonzero.
\end{lemma}

\begin{proof}
If the affine span were proper, a nonzero affine functional would be constant
on every periodic sum.  Apply the cylinder cohomology construction: define a
transfer value along a word by subtracting that constant and use equality on
all periodic loops to make it path independent.  This produces a continuous
coboundary, contradicting the hypothesis.  Linear algebra then selects five
periodic vectors.  Enumeration terminates because a nonzero determinant is a
finite certificate.
\end{proof}

\begin{theorem}[Finite parameter-uniform certificate family]
\label{thm:r8-a2-certificates}
There are finitely many five-word packets $\mathcal W_1,\ldots,\mathcal W_M$
and open parameter sets $K_\alpha$ covering $K$ such that
\[
 |\det D_{R,\alpha}|\ge d_\alpha>0,
 \qquad R\in K_\alpha.
\]
The Fourier proof chooses a packet according to the parameter chart; it does
not concatenate packets into one fictitious global determinant.
\end{theorem}

\begin{proof}
Lemma~\ref{lem:r8-a2-span} gives one packet at each $R$.  Periodic collision
coordinates and sums vary continuously while the word remains admissible, so
its determinant stays nonzero on a neighborhood.  Compactness of $K$ gives a
finite subcover.  A subordinate partition in parameter space selects the
corresponding estimate without altering any orbit word.
\end{proof}

\subsection{A separate geometric UNI theorem}

For inverse branches $h,k$ of a common return iterate define the temporal
distance
\[
 \Delta_{h,k}(x)=\tau_n(hx)-\tau_n(kx).
\]

\begin{lemma}[Dispersing two-branch derivative]
\label{lem:r8-a2-uni-geometry}
On each compact finite-horizon geometry chart there exist two admissible
returned branches $h,k$, an unstable interval $J$, and $c_{\rm uni}>0$ such
that
\[
 |\partial_x\Delta_{h,k}(x)|\ge c_{\rm uni},\qquad x\in J.
\]
\end{lemma}

\begin{proof}
Choose two itineraries agreeing up to one collision and leaving that scatterer
through two distinct strictly convex arcs before returning to the same base.
For a free flight from $r(x)$ to $r'(x)$,
\[
 \partial_x\tau=\widehat{r'-r}\cdot(\partial_xr'-\partial_xr).
\]
Subtracting the two itineraries cancels the common legs.  Strict convexity and
the non-tangency of the two outgoing directions make the remaining derivative
nonzero at the reference orbit.  Uniform curvature, finite horizon, and a
compact chart shrink $J$ so that the lower bound is uniform.  Markov
specification supplies common return connectors; they contribute identical
terminal legs and do not change the derivative difference.
\end{proof}

\begin{theorem}[High-frequency Dolgopyat contraction]
\label{thm:r8-a2-dolgopyat}
For the twisted physical transfer operator
$\mathcal L_{R,\xi,b}$, where $\xi$ is the two-dimensional homology frequency
and $b$ the roof frequency, there are $C,\gamma>0$ such that
\[
 \|\mathcal L_{R,\xi,b}^{n(b)}\|_{\rm phys}
 \le 1-C|b|^{-\gamma}
\]
for $|b|$ large and $\xi$ in a fixed fundamental domain.  Face and corner
currents satisfy the same estimate on the renormalized field.
\end{theorem}

\begin{proof}
Lemma~\ref{lem:r8-a2-uni-geometry} gives an interval on which
$b\Delta_{h,k}$ crosses a fixed phase interval.  Split the interval into
near-opposition pieces and their complement.  On a near-opposition piece,
\[
 |zu+wv|\le(|z|+|w|)(1-c\alpha^2)
\]
when the phase error is at most $\alpha$ and the two amplitudes are comparable.
Bounded distortion and cone invariance preserve comparability.  Nonconcentration
of unstable conditional measures supplies a definite mass of such pieces.
The remaining branches are controlled by Lasota--Yorke contraction.  Trace
jets form a finite triangular extension in each chart; the depth weights and
Lemma~\ref{lem:r8-a2-birth} absorb their off-diagonal terms.
\end{proof}

The finite-family periodic theorem is used only to exclude exact compact
frequency eigenfunctions.  The derivative theorem above supplies the distinct
large-frequency mechanism.

\subsection{The master vector--roof local limit formula}

Let $S_n\in\mathbb Z^2$ and $T_n\in\mathbb R$ be the homology sum and roof sum,
centered at their exact finite saddle.  Write $\Sigma_R$ for the nonsingular
three-dimensional covariance and let $\varphi_{\Sigma_R}$ be its Gaussian
density.

\begin{theorem}[Correctly scaled vector--roof LLT]
\label{thm:r8-a2-llt}
Uniformly for lattice targets $k_n$ in a bounded central range and bounded
Borel sets $B\subset\mathbb R$ with negligible boundary,
\[
 \mathbb P_R\!\left(
 S_n=k_n,\ \frac{T_n}{n}\in t_n+G_nB
 \right)
 =\frac{1}{n}\int_{\sqrt n(t_n-\bar\tau_R)+\sqrt n G_nB}
 \varphi_{\Sigma_R}\!\left(
 \frac{k_n-n\bar\kappa_R}{\sqrt n},u
 \right)du+o(n^{-1})
\]
whenever $G_n\to0$; the same formula with the corresponding Gaussian interval
holds for fixed $G_n$.  In particular, if $\sqrt nG_n\to0$, the leading order
is $G_n n^{-1/2}$, not $G_n n^{-3/2}$.
\end{theorem}

\begin{proof}
Fourier inversion gives a joint two-lattice/one-continuous density of order
$n^{-3/2}$ in the roof-sum variable.  The average window
$T_n/n\in t_n+G_nB$ is a roof-sum interval of length $nG_n$.  Changing variables
$u=(T_n-nt_n)/\sqrt n$ contributes the factor $\sqrt n$, yielding the displayed
$n^{-1}$ Gaussian integral.  Compact frequencies use
Theorem~\ref{thm:r8-a2-certificates}; large roof frequencies use
Theorem~\ref{thm:r8-a2-dolgopyat}; the remaining minor arcs are exponentially
small by the physical spectral gap.
\end{proof}

\begin{corollary}[Three unambiguous regimes]
\label{cor:r8-a2-regimes}
If $G_n\ll n^{-1/2}$, the coefficient is the local-density expression
$G_n n^{-1/2}$.  If $G_n\asymp n^{-1/2}$, it is $n^{-1}$ times a finite Gaussian
interval mass.  If $n^{-1/2}\ll G_n\le G_0$, it is $n^{-1}$ times the saturated
conditional Gaussian mass.  One target-dependent saddle is used in all three
regimes.
\end{corollary}

\begin{proof}
Apply dominated convergence to the Gaussian integral in
Theorem~\ref{thm:r8-a2-llt} in the three ranges of $\sqrt nG_n$.
\end{proof}
'''

A3 = r'''
\section{Round-eight reconstruction: edge-flow compactification and projective epigraph recovery}
\label{sec:r8-a3}

The state now retains the inducing branch, its transition edge, and the actual
excursion profile.  Recession is obtained from a directed family of genuine
finite-coordinate LDPs; no full-sequence Gamma limit is declared in advance.

\subsection{The exact marked edge-flow state}

Let $\mathcal G=(\mathcal V,\mathcal E)$ be the countable first-return graph.
An admissible path is a sequence of edges $e_j=(a_j,a_{j+1})$.  For an actual
excursion point $y_j\in Y_{a_j}$ put
\[
 K(y_j,a_j)=\frac1{r(a_j)}\sum_{\ell<r(a_j)}
 \delta_{\Theta^\ell\Gamma(y_j)}.
\]
For $R_N=\sum_{j<N}r(a_j)$ define
\[
 M_N=R_N^{-1}\sum_{j<N}r(a_j)\delta_{(a_j,a_{j+1})},
\qquad
 \Xi_N=R_N^{-1}\sum_{j<N}r(a_j)
 \delta_{(a_j,a_{j+1},K(y_j,a_j))}.
\]
The mark contains the actual profile, not its branch conditional expectation.

Choose finite edge sets $\mathcal E_m\uparrow\mathcal E$ and finitely many
continuous profile tests $\phi_1,\ldots,\phi_m$.  The projection $\pi_m\Xi$
records the masses of edges in $\mathcal E_m$, their incoming and outgoing
marginals, the first $m$ profile moments, and the escaped clock
\[
 z_m=1-\sum_{e\in\mathcal E_m}\Xi(e,\mathcal Z).
\]

\begin{lemma}[Rate data are functions of the state]
\label{lem:r8-a3-state}
The edge marginals of $\Xi$ determine admissibility, stationarity, the
length-weighted entropy flow, and the induced potential average.  Distinct
branches with the same return length and profile remain distinct coordinates.
\end{lemma}

\begin{proof}
Incoming and outgoing marginals are read from the first two components of the
mark.  Stationarity is equality of these marginals.  Conditional transition
probabilities are the Radon--Nikodym ratios of the edge flow with respect to its
incoming marginal, so their entropy is determined by $\Xi$.  The branch
potential and length are edge labels and are integrated directly.  No
injectivity of $a\mapsto(r(a)^{-1},K_a)$ is used.
\end{proof}

\subsection{Finite projections and the projective epigraph theorem}

Let $I_m$ be the good finite-dimensional rate obtained by the exponential
change of measure on $\pi_m\Xi_N$.  The source class contains all edge
indicators in $\mathcal E_m$ and all profile tests $\phi_1,\ldots,\phi_m$.
Consistency gives
\[
 I_m(z)=\inf\{I_{m+1}(z'): \pi_m z'=z\}.
\]

\begin{theorem}[Projective epigraph LDP]
\label{thm:r8-a3-projective}
Assume exponential tightness of the return-length moment and the finite-source
coefficient theorem of A2.  Then $\Xi_N$ satisfies a full good LDP on the
projective state space with rate
\[
 I(\Xi)=\sup_m I_m(\pi_m\Xi).
\]
Every finite-rate state has a diagonal sequence of actual admissible paths
whose costs converge to $I(\Xi)$.
\end{theorem}

\begin{proof}
The upper bound follows from exponential tightness and the finite-projection
upper bounds.  For the lower bound, choose $m_j\uparrow\infty$ so that
$I_{m_j}(\pi_{m_j}\Xi)\uparrow I(\Xi)$.  The finite-dimensional lower theorem
provides an actual admissible path block in a $2^{-j}$ neighborhood of
$\pi_{m_j}\Xi$ with cost at most $I_{m_j}+2^{-j}$.  Concatenate these blocks as successive excursion sequences.  If the terminal
edge of one block does not meet the initial edge of the next, insert a finite
connecting excursion path supplied by transitivity of the return graph.
Every intermediate base return is a legal edge-flow transition rather than a
forbidden connector inside one first-return word.  Choose block multiplicities
increasing so fast that all connecting paths and earlier blocks have vanishing
clock fraction.  The diagonal path converges in every projection and has the desired
cost.  This proves the recovery statement rather than assuming it.
\end{proof}

\subsection{Recession as boundary edge flow}

The boundary mass $z_m$ is monotone in $m$ and converges to
$z_\infty\in[0,1]$.  A boundary profile is a projective limit of the moments
carried by edges escaping every finite set.  Define
\[
 I_{\rm rec}(z)=\sup_m I_m^{\rm boundary}(\pi_mz).
\]

\begin{theorem}[Actual-excursion recession recovery]
\label{thm:r8-a3-recession}
The recession functional is lower semicontinuous, convex under concatenation of
excursion blocks, and has the dual formula
\[
 I_{\rm rec}(z)=\sup_{F\in C_{\rm cyl}}
 \{z(F)-q_\infty(F)+q_\infty(0)\}.
\]
For every finite-rate $z$ there are actual long first-return excursions whose
finite profile projections approach $z$ and whose logarithmic costs approach
$I_{\rm rec}(z)$.
\end{theorem}

\begin{proof}
Each finite boundary rate is a closed convex conjugate and the directed
supremum is lower semicontinuous.  The recovery part of
Theorem~\ref{thm:r8-a3-projective}, applied to states with clock mass outside
$\mathcal E_m$, produces actual excursions at each finite stage.  A diagonal
selection makes the return length tend to infinity while matching every
profile projection.  Convex combinations are realized by consecutive long
excursions separated by genuine base returns; no claim is made that their
concatenation is one first-return word.  The cylinder dual determines the
projective supremum by separation.
\end{proof}

\subsection{Moment closure without an unbounded reciprocal coordinate}

For every empirical state,
\[
 \Xi_N(1)=1.
\]
The recurrent clock in a finite set and its complement add exactly to one.
The boundary coordinate is therefore a continuous ledger coordinate, not the
weak limit of the unbounded function $x^{-1}$.

\begin{lemma}[Exact recurrent--defect normalization]
\label{lem:r8-a3-normalization}
Every projective limit satisfies
\[
 m_{\rm rec}(1)+z_\infty=1.
\]
Conversely every stationary edge flow with this identity is approximated by
normalized empirical edge flows.
\end{lemma}

\begin{proof}
For each $m$ the identity
$m_{N}(\mathcal E_m)+z_{N,m}=1$ is exact and involves bounded coordinates.
Pass first to $N\to\infty$ and then $m\to\infty$.  For the converse, truncate
the flow, decompose the finite circulation into cycles, and append boundary
excursion blocks supplied by Theorem~\ref{thm:r8-a3-recession}.  Normalize by
total clock.
\end{proof}

\subsection{Finite-core approximation and entropy}

\begin{theorem}[Stationary flow approximation]
\label{thm:r8-a3-flow-approx}
Every finite-rate stationary countable edge flow is approximated by stationary
flows on finite strongly connected subgraphs, with convergence of potential,
length moment, and entropy.  Periodic components retain their period.
\end{theorem}

\begin{proof}
Truncate to edges carrying all but $\varepsilon$ of the length, potential, and
information moments.  The imbalance at the retained vertices has total mass
$O(\varepsilon)$.  Decompose the original stationary flow into directed cycles
and keep finitely many cycles that repair this imbalance.  Their union is a
finite graph; apply the construction separately to each cyclic class instead
of adding a period-breaking orbit.  The entropy error is controlled by the
truncated information moment and tends to zero.
\end{proof}

\subsection{Spectral versus recession sectors}

\begin{theorem}[SPR/recession dichotomy]
\label{thm:r8-a3-dichotomy}
A strongly positive recurrent component contributes through its isolated
renewal pole and the recurrent edge-flow rate.  If the pole is lost at the
critical pressure, its normalized mass escapes every finite edge set and is
represented by $I_{\rm rec}$.  No component is counted in both sectors.
\end{theorem}

\begin{proof}
On a finite core the renewal resolvent is compact and has an isolated Perron
pole.  Uniform return-length integrability keeps this pole isolated in the
countable limit, which is the SPR case.  If uniform integrability fails, any
normalized approximate eigenvector has vanishing mass on every finite edge set;
its finite projections converge to a boundary state.  The projective epigraph
rate of that state is exactly the recession cost.  The alternatives are
mutually exclusive by tightness.
\end{proof}

\subsection{Exact physical time}

For a deterministic time $T$, keep the terminal cut excursion
$(a_*,u_*,K_*^{\rm cut})$, where $u_*\in[0,1]$ is the used fraction of its roof.
This coordinate is part of the state rather than discarded.

\begin{theorem}[Deterministic-time path LDP]
\label{thm:r8-a3-time}
The edge-flow LDP contracts to the full empirical path at every deterministic
physical time.  The terminal cut coordinate gives exact time $T$ and its cost
is the corresponding fractional excursion cost.  When the terminal roof is
$o(T)$ it is exponentially equivalent to zero; when it is of order $T$ it is
already represented by the recession sector.
\end{theorem}

\begin{proof}
Stop at the first return whose accumulated roof exceeds $T$ and retain the
used fraction of that excursion.  This makes the clock identity exact.
Exponential tightness separates the event $r(a_*)\le\delta T$, whose profile
error is at most $\delta$, from $r(a_*)>\delta T$, which is a boundary
excursion and is governed by Theorem~\ref{thm:r8-a3-recession}.  Let
$\delta\downarrow0$ after the LDP bounds.
\end{proof}
'''

A4 = r'''
\section{Round-eight reconstruction: past filtration, exact unresolved dilation, and causal memory}
\label{sec:r8-a4}

A quotient by stable leaves retains the future-determining coordinate and is
therefore unsuitable for a nondegenerate conditional future.  We work on the
two-sided natural extension and condition on the genuine past sigma-field.

\subsection{The natural extension and the past kernel}

Let $(\Omega,\sigma,\nu_\Psi)$ be the two-sided Gibbs extension of the inducing
system.  Put
\[
 \mathcal F^-_0=\sigma(\omega_j:j\le0),
 \qquad h=(\ldots,\omega_{-1},\omega_0).
\]
The normalized transfer eigenfunction gives a $g$-kernel
\[
 g_\Psi(h,i)=\nu_\Psi(\omega_1=i\mid\mathcal F^-_0)(h).
\]
It is a forward conditional law because the orientation is fixed by the
natural extension.

\begin{lemma}[Nondegenerate past disintegration]
\label{lem:r8-a4-past}
If at least two admissible successors have positive $g_\Psi(h,\cdot)$, the
conditional future given $h$ is non-Dirac.  The kernel depends continuously on
$h$ in the weighted past metric and satisfies the tower identity.
\end{lemma}

\begin{proof}
The finite-cylinder conditional probabilities are ratios of positive transfer
weights.  The spectral variation estimate makes these ratios Cauchy as the
known past grows, uniformly in the weighted metric.  Their limit is
$g_\Psi$.  Multiplying successive kernels gives the regular conditional law of
future cylinders, and summing over the first unknown symbol proves the tower
identity.  Positivity of two successors makes the first-step law non-Dirac.
\end{proof}

Lift this kernel to the suspension by retaining the current roof age.  Let
$\mathcal H^-$ be the weighted space of pasts and ages.

\begin{theorem}[All-real-time Feller history semigroup]
\label{thm:r8-a4-feller}
The renewal formula based on $g_\Psi$ defines a conservative Feller semigroup
$\mathcal P_t$ on $C_b(\mathcal H^-)$.  It is constructed before any Ray
compactification and satisfies Chapman--Kolmogorov for every real $t\ge0$.
\end{theorem}

\begin{proof}
Split a trajectory according to the number of completed roofs before time
$t$.  Finite horizon and the exponential roof moment make the renewal sum
normally convergent.  Continuity of each finite term follows from
Lemma~\ref{lem:r8-a4-past}; the tail is uniform.  Splitting at time $s$ and
using the tower identity gives $\mathcal P_{t+s}=\mathcal P_t\mathcal P_s$.
Conservation follows by applying the formula to one.
\end{proof}

\begin{corollary}[Ray realization without circularity]
\label{cor:r8-a4-ray}
The resolvent of $\mathcal P_t$ has a Ray compactification carrying a
right-continuous strong Markov realization.
\end{corollary}

\begin{proof}
The already constructed conservative Feller resolvent separates the weighted
past states and is normal under monotone convergence.  The Ray construction
therefore applies to this resolvent; no path regularity is used to define it.
\end{proof}

\subsection{Conditional enhanced homogenization}

Let $A$ be a centered additive observable and write the transfer Poisson
solution $\chi_h$ on each past fiber.  The martingale increments are
\[
 D_{n+1}=A\circ\sigma^n+\chi\circ\sigma^{n+1}-\chi\circ\sigma^n.
\]

\begin{theorem}[Uniform quenched rough invariance principle]
\label{thm:r8-a4-rough}
On every compact weighted past set outside a set of conditional probability
$O(e^{-cL})$, the conditional laws of the rescaled path and its iterated
integral converge uniformly to a Brownian rough path.  The antisymmetric
second level contains the deterministic area correction
\[
 \mathfrak a=\frac12\sum_{n\ge1}
 \nu_\Psi(A\otimes A\circ\sigma^n-A\circ\sigma^n\otimes A).
\]
\end{theorem}

\begin{proof}
The past-kernel transfer operators have the same spectral gap on every compact
past chart, so the Poisson solutions and their conditional $L^{2+\delta}$
bounds are uniform.  The martingale triangular-array invariance principle then
holds uniformly.  Burkholder estimates give tightness of the first and second
levels.  The coboundary terms vanish after scaling, while the predictable
antisymmetric correlation sum converges absolutely to $\mathfrak a$.
Exponential roof and distortion tails remove the exceptional histories.
\end{proof}

\subsection{The exact unresolved-space dilation}

Let $L$ be the closed microscopic generator on a Hilbert space
$\mathcal H=V\oplus Q\mathcal H$, and let $P$ be the orthogonal projection onto
$V$.  Its compressed resolvent is
\[
 C(z)=P(z-L)^{-1}P,
 \qquad
 C(z)^{-1}=z-PLP-M(z),
\]
where
\[
 M(z)=PLQ(z-QLQ)^{-1}QLP
\]
is the exact unresolved transfer function.  Let $Q\mathcal H=\mathcal H_{\rm cyc}
oplus\mathcal H_\perp$, where $\mathcal H_{\rm cyc}$ is the cyclic subspace
seen by $QLP$ and $PLQ$.

A finite Riesz--Potapov extraction of the selected transmission zeros gives an
exact transfer decomposition
\[
 M(z)=M_{\rm res}(z)+C_Z(z-A_Z)^{-1}B_Z
\]
and a unitary realization map
\[
 U:\mathcal H_{\rm cyc}\longrightarrow
 \mathcal H_{\rm res}\oplus Z,
 \qquad \dim Z<\infty.
\]
The residual realization is chosen minimal; hence it has no selected
transmission zero.  Define
\[
 \widetilde{\mathcal H}
 =V\oplus\mathcal H_\perp\oplus\mathcal H_{\rm res}\oplus Z
\]
and let $\widetilde L$ be the unitary conjugate of $L$ under
$I_V\oplus I_{\mathcal H_\perp}\oplus U$.

\begin{theorem}[Exact Riesz--Schur extraction with full unresolved dynamics]
\label{thm:r8-a4-dilation}
The operator $\widetilde L$ is fixed and closed, and
\[
 P(z-\widetilde L)^{-1}P=C(z).
\]
Its $V$-Schur complement is
\[
 z-PLP-M_{\rm res}(z)-C_Z(z-A_Z)^{-1}B_Z.
\]
Thus the finite state $Z$ carries exactly the selected transmission-zero
factor, while $\mathcal H_\perp\oplus\mathcal H_{\rm res}$ retains every
unresolved microscopic mode.  No finite rational term is added on top of the
old $Q$-resolvent.  If the compressed resolvent is passive, the extraction can
be chosen as a conservative Hilbert-space colligation.
\end{theorem}

\begin{proof}
On the cyclic subspace, the positive-real transfer $M$ has a minimal Hilbert
realization unique up to unitary equivalence.  Factor the finite
Blaschke--Potapov product generated by the selected Riesz zero projections from
that realization.  Each finite factor has a conservative state-space
realization on $Z$, and the remaining outer transfer has a minimal realization
on $\mathcal H_{\rm res}$.  The cascade realization gives the displayed exact
sum for $M$ and is unitarily equivalent to the original cyclic realization.
Extend this unitary by the identity on the invisible complement
$\mathcal H_\perp$ and on $V$.  Resolvents are invariant under unitary
conjugacy, so the compressed resolvent is exactly $C(z)$.  Applying the block
inverse formula after the decomposition gives the stated Schur complement.
Conservative factor and outer realizations preserve passivity.
\end{proof}

\subsection{The causal Volterra sign}

For a resolved correlation satisfying
\[
 C'(t)=AC(t)+(K*C)(t),\qquad C(0)=I,
\]
Laplace transformation gives the causal memory transform
\[
 \widehat K(z)=zI-A-\widehat C(z)^{-1}.
\]

\begin{theorem}[Residual-memory decay]
\label{thm:r8-a4-memory}
After the finite Riesz modes are transferred to $Z$, define
\[
 \widehat K_{\rm res}(z)=zI-A_{\rm res}-C_{\rm res}(z)^{-1}.
\]
If this transform is analytic and polynomially bounded in
$\operatorname{Re}z>-\gamma$, then the residual kernel decays like
$e^{-\gamma' t}$ for every $\gamma'<\gamma$.  Adding the finite $Z$ modes
reconstructs the complete resolved correlation.
\end{theorem}

\begin{proof}
The sign follows by transforming the Volterra equation and solving for
$\widehat K$.  Move the Bromwich contour from a positive line to
$\operatorname{Re}z=-\gamma'$.  Polynomial bounds and one integration by parts
control the horizontal pieces.  The residues crossed by the contour are
precisely the modes already represented in $Z$.
\end{proof}

\begin{theorem}[History Doob transform]
\label{thm:r8-a4-doob}
The positive transfer eigenfunction on the natural extension defines a
normalized Doob kernel on past histories.  Its suspension semigroup is
intertwined with $\mathcal P_t$, and the conditional pressure, enhanced
homogenization, and causal-memory construction are compatible under this
intertwining.
\end{theorem}

\begin{proof}
Normalize the transfer operator by the eigenvalue and conjugate by the positive
eigenfunction.  The resulting one-step kernel is $g_\Psi$ and therefore acts on
the same past filtration as Theorem~\ref{thm:r8-a4-feller}.  Conjugacy passes to
the suspension renewal formula and to the compressed resolvent.  The martingale
Poisson equation and the memory Schur complement are invariant under this
bounded conjugacy.
\end{proof}
'''

# Remaining modules are appended below.
B1 = r'''
\section{Round-eight reconstruction: regenerative compound smoothing and exact saddle coefficients}
\label{sec:r8-b1}

The continuous mark law of one particle is allowed to be singular.  Smoothing
is obtained from linearly many independent full-rank blocks in a typical
compound-Poisson sample; the complement of this regenerative event is
exponentially small.  The covariance is computed from the typical compound
law rather than from an exponentially rare fixed-particle sector.

\subsection{Admissible constraint marks}

Let
\[
 X(v)=\bigl(v,|v|^2/2,\chi_1(v),\ldots,\chi_d(v)\bigr)
 \in\mathbb R^{4+d}.
\]
We call the constraint family \emph{admissible} on a source chart $K$ if the
tilted second-moment matrices satisfy
\[
 \inf_{\lambda\in K}\inf_{|u|=1}
 \int (u\cdot X(v))^2e^{\lambda\cdot X(v)}\,d\nu(v)>0,
\]
and if some product patch $U\subset(\operatorname{supp}\nu)^{n_0}$ makes the
sum map
\[
 \mathsf S(v_1,\ldots,v_{n_0})=\sum_{j=1}^{n_0}X(v_j)
\]
a submersion.  These are explicit affine-span and transversality conditions;
they exclude redundant constraints such as $\chi_1\equiv0$.

\begin{lemma}[Finite submersion cover]
\label{lem:r8-b1-submersion}
For an admissible family there are finitely many patches $U_\alpha$ and smooth
cutoffs $\zeta_\alpha$ such that the restricted pushforwards
$\mathsf S_*(\zeta_\alpha\nu^{\otimes n_0})$ have compactly supported
$W^{k,1}$ densities, uniformly for every prescribed finite $k$ and every
source in $K$.
\end{lemma}

\begin{proof}
On a full-rank patch choose a nonvanishing $(4+d)$-minor and use the
corresponding coordinates as transverse variables.  Coarea gives a density;
differentiating under the fiber integral gives $W^{k,1}$ bounds after the
patch is shrunk away from the zero set of the minor.  Compactness of the source
chart and the support used by the saddle gives a finite cover.  General
velocity variations, not only radial variations in a coordinate plane, supply
the momentum directions.
\end{proof}

\subsection{Good-block regeneration}

Let $N_\mu$ be Poisson with mean $\mu a_\lambda$ and split the first
$n_0\lfloor N_\mu/n_0\rfloor$ marks into predetermined blocks.  A block is
\emph{good} if it lies in one of the submersion patches, with the cutoff label
chosen by an independent partition coin.  Its probability is at least
$p_*>0$, uniformly on $K$.

\begin{lemma}[Linearly many smooth blocks]
\label{lem:r8-b1-good-blocks}
There are $c,C>0$ such that
\[
 \mathbb P_\lambda\{M_\mu<c\mu\}\le Ce^{-c\mu},
\]
where $M_\mu$ is the number of good disjoint blocks.  Conditional on the
remaining marks and on $M_\mu=M$, the continuous sum law is a convolution of
$M$ uniformly $W^{k,1}$ densities with an arbitrary residual probability
measure.
\end{lemma}

\begin{proof}
Poisson concentration gives $N_\mu\ge c_0\mu$ outside an exponentially small
event.  Conditional on $N_\mu$, the indicators of the predetermined good
blocks are independent Bernoulli variables with success probability at least
$p_*$.  Chernoff's bound gives the first assertion.  Independence of disjoint
blocks gives the convolution decomposition; all singular marks are kept in the
residual factor rather than declared smooth.
\end{proof}

\begin{theorem}[Global characteristic-function estimate]
\label{thm:r8-b1-characteristic}
For every $k$ there are $C,c,R>0$ such that the compound characteristic
function satisfies
\[
 |\Phi_{\mu,\lambda}(\xi)|
 \le Ce^{-c\mu}+C_k(1+|\xi|)^{-k},
 \qquad |\xi|\ge R,
\]
uniformly for $\lambda\in K$.  Lattice coordinates are restricted to one
fundamental domain.  Empty and low-particle sectors are contained explicitly
in the term $Ce^{-c\mu}$.
\end{theorem}

\begin{proof}
On the event in Lemma~\ref{lem:r8-b1-good-blocks}, Fourier transformation gives
\[
 |\Phi(\xi\mid M)|\le \prod_{j=1}^{M}|\widehat g_j(\xi)|.
\]
A $W^{k,1}$ density has $|\widehat g_j(\xi)|\le C_k|\xi|^{-k}$ at large
frequency and, by nonlattice affine span, is uniformly less than one on every
compact annulus away from zero.  Since $M\ge c\mu$, the annular part is
exponentially small and the large-frequency part has arbitrary polynomial
decay.  The complementary event, including every fixed small-particle sector,
has probability $Ce^{-c\mu}$.
\end{proof}

\subsection{Typical-sector covariance}

The normalized compound pressure is
\[
 Q(\lambda)=\int\bigl(e^{\lambda\cdot X(v)}-1\bigr)d\nu(v).
\]

\begin{lemma}[Uniform covariance from compound activity]
\label{lem:r8-b1-covariance}
For an admissible mark family,
\[
 D^2Q(\lambda)=\int X(v)X(v)^T e^{\lambda\cdot X(v)}d\nu(v)
 \ge c_KI,
 \qquad\lambda\in K.
\]
Moreover, for the total mark $C_{\rm tot}$,
\[
 \mu^{-1}\operatorname{Cov}_\lambda(C_{\rm tot})=D^2Q(\lambda).
\]
\end{lemma}

\begin{proof}
The compound-Poisson logarithmic moment generating function is $\mu Q$.
Differentiating twice proves both identities.  The affine-span condition and
compactness of $K$ give the uniform positive lower bound.  No conditioning on
$N_\mu=n_0$ occurs.
\end{proof}

\subsection{Mixed lattice--continuous coefficient}

Let $L_\mu\in\mathbb Z^r$ be the lattice constraints and
$Y_\mu\in\mathbb R^s$ the continuous constraints.  Let
$(\lambda_\mu,\theta_\mu)$ be the unique exact finite saddle for the extensive
target $(\ell_\mu,y_\mu)$.

\begin{theorem}[Exact finite-saddle coefficient]
\label{thm:r8-b1-coefficient}
Suppose the continuous shell is
\[
 Y_\mu\in y_\mu+w_\mu B,
 \qquad w_\mu=o(\sqrt\mu),
\]
with $w_\mu$ measured in the extensive variable.  Then
\[
 \mathbb P\{L_\mu=\ell_\mu,
 Y_\mu\in y_\mu+w_\mu B\}
 =e^{-\mu I_\mu}
 \frac{1}{(2\pi\mu)^{r/2}}
 \int_{w_\mu B/\sqrt\mu}
 \varphi_{\Sigma_{c,\mu}}(z)\,dz
 \bigl(1+o(1)\bigr),
\]
uniformly on compact admissible target charts.  The formula includes the
empty/small-sector exponential remainder.
\end{theorem}

\begin{proof}
Tilt at the exact finite saddle, so the Fourier phase has zero linear term.
On the major arc use the third-order Taylor expansion and
Lemma~\ref{lem:r8-b1-covariance}.  The intermediate and high-frequency arcs are
controlled by Theorem~\ref{thm:r8-b1-characteristic}.  Fourier inversion in
$lattice\times continuous$ variables gives the coefficient.  Undoing the tilt
produces the exact exponent $I_\mu$ and no centering mismatch.
\end{proof}

\begin{theorem}[Microcanonical transfer and prepared LDP]
\label{thm:r8-b1-preparation}
The exact coefficient theorem transfers bounded marked pressures, covariances,
and finite-dimensional LDPs from the grand-canonical law to the extensive
microcanonical shell.  The transferred covariance is the Schur complement of
$D^2Q$ along the constrained directions.
\end{theorem}

\begin{proof}
Apply Theorem~\ref{thm:r8-b1-coefficient} to the numerator with a bounded mark
source and to the denominator without that source.  The leading shell factors
cancel uniformly.  Differentiating the resulting pressure ratio at zero gives
the conditional Schur complement.  Exponential tightness and the finite-source
Laplace principle give the prepared LDP.
\end{proof}
'''

B2 = r'''
\section{Round-eight reconstruction: compatible kinetic traces and monotone fork Jacobi fields}
\label{sec:r8-b2}

Boundary flux is not an independent coordinate.  We define the hierarchy as a
closed graph of transport states satisfying the Green identity and collision
compatibilities.  The recollision estimate is proved in the physical Jacobi
metric of a dispersing hard-sphere flow; no coordinate reset is used to erase
singular values.

\subsection{The compatible graph-trace state}

For $k$ particles let $\mathcal D_k$ be smooth compactly supported densities on
nonoverlapping configurations, smooth up to non-grazing collision faces, and
satisfying specular reflection and creation/annihilation compatibility.  Let
$T_k=v\cdot\nabla_x$ and let $\gamma_k^-g$ be the incoming kinetic trace.  Set
\[
 \|g\|_{\mathcal X_k}
 =\|g\|_{L^1_w}+\|T_kg\|_{L^1_w}
 +\|\gamma_k^-g\|_{\mathcal M_w},
\qquad
 \mathcal X_k=\overline{\mathcal D_k}^{\|\cdot\|_{\mathcal X_k}}.
\]
The trace in the completion is the limit of the traces of the approximants.

\begin{lemma}[Closed kinetic trace and Green identity]
\label{lem:r8-b2-trace}
The map $g\mapsto\gamma_k^-g$ is closed on $\mathcal X_k$.  Every
$g\in\mathcal X_k$ satisfies
\[
 \int (T_kg)\phi+\int g(T_k^*\phi)
 =\int_{\partial^-}\phi\,d\gamma_k^-g
 -\int_{\partial^+}\phi\,d\gamma_k^+g
\]
for every smooth test function, together with the reflection and contact
compatibility inherited from $\mathcal D_k$.  Conversely an arbitrary pair of
interior and boundary measures is not a state unless it satisfies these
relations.
\end{lemma}

\begin{proof}
The identity holds on $\mathcal D_k$.  Each term is continuous in the graph
norm, so it passes to the completion.  If $g_n\to g$, $T_kg_n\to h$, and
$\gamma^-g_n\to\gamma$, the identity identifies $h=T_kg$ distributionally and
$\gamma=\gamma^-g$; hence the trace graph is closed.  The boundary
compatibilities are linear closed relations and also pass to the limit.
\end{proof}

Let
\[
 \mathcal X_\beta=\left\{G=(g_k)_{k\ge0}:
 \sum_{k\ge0}\frac{\beta^k}{k!}\|g_k\|_{\mathcal X_k}<\infty\right\}.
\]

\begin{theorem}[Positive boundary-renewal semigroup]
\label{thm:r8-b2-semigroup}
The free transport with specular reflection and the creation operators
$B_{k,k+1}$ define a closed dissipative generator on $\mathcal X_\beta$ and a
positive strongly continuous semigroup.  Collision-time Dirac traces belong to
its graph closure, and
\[
 \|B_{k,k+1}g_{k+1}\|_{\mathcal X_k}
 \le C(k+1)\|g_{k+1}\|_{\mathcal X_{k+1}}.
\]
\end{theorem}

\begin{proof}
Solve the resolvent along backward characteristics until the first boundary
hit.  The reflection map is flux preserving and the contact creation kernel is
a positive boundary integral.  For sufficiently large resolvent parameter the
boundary renewal is a contraction; its Neumann series gives the resolvent and
preserves positivity.  The flux measure of one selected partner contributes at
most $C$ and there are $k+1$ partners, proving the bound.  Factorial weights
make the triangular hierarchy operator relatively bounded.  Lumer--Phillips
then gives the semigroup.  Approximating a collision trace by incoming thin
layers shows that its Dirac limit belongs to the graph closure.
\end{proof}

\subsection{Monotone Jacobi geometry from the youngest fork}

For an orthogonal Jacobi field write $(J,V)$ for position and velocity
variations and
\[
 \mathcal Q(J,V)=\langle J,V\rangle.
\]
The unstable cone is $\mathcal Q\ge0$ with positive collision curvature.  A
free flight of length $t$ sends $(J,V)$ to $(J+tV,V)$.  At a dispersing
collision the curvature operator gains a positive term
$2\mathcal K/|v\cdot n|$.

\begin{lemma}[Dispersing monotonicity]
\label{lem:r8-b2-monotone}
Along a non-grazing hard-sphere collision path, the unstable Jacobi norm
\[
 \|(J,V)\|_*^2=|V|^2+\mathcal Q(J,V)+|J|^2
\]
does not lose a transverse exterior two-volume by more than a factor
$C\delta^C$ depending only on the minimal angle and event gap $\delta$, not on
the number of later collisions.
\end{lemma}

\begin{proof}
During free flight,
$\mathcal Q(J+tV,V)=\mathcal Q(J,V)+t|V|^2$.  At collision, the mirror formula
adds the positive quadratic form
$2\langle\mathcal KJ,J\rangle/|v\cdot n|$ to the curvature.  Hence the
associated positive Lagrangian graph is monotone.  Apply the same calculation
to the second exterior power: the Gram determinant of two unstable fields is
nondecreasing in the Jacobi metric.  On a finite-horizon chart with the final
free-flight gap at least $\delta$, the Riccati formula
$B(t)=B_0(I+tB_0)^{-1}$ gives
$0\le B(t)\le\delta^{-1}I$ before the final impact.  Thus the Jacobi norm and
the physical position exterior norm are equivalent with a fixed
$C\delta^{-C}$ factor.  Conversion to impact coordinates uses only the final
non-grazing collision chart and is independent of the number of earlier
collisions.
\end{proof}

At the youngest separating fork of two labels choose the two root variations
consisting of transverse relative translations of the two child clusters.
They form an unstable two-plane immediately after the fork.

\begin{theorem}[Depth-independent first-surplus Gramian]
\label{thm:r8-b2-gramian}
On the good chronological set where all speeds, angles, and event gaps exceed
$\delta$, the derivative from the two fork variables to the relative contact
coordinate at the first surplus collision has Gramian
\[
 \lambda_{\min}\mathcal G_{ab}\ge c\delta^C,
\]
with $C$ independent of ancestral depth.  The complementary set has flux
measure at most $C^m\delta^\alpha$ in a depth-$m$ sector.
\end{theorem}

\begin{proof}
At the fork the two relative translations span the contact transverse plane
with a uniform determinant.  They lie in the unstable Jacobi cone.  Subsequent
hard-sphere collisions and free flights preserve their exterior volume by
Lemma~\ref{lem:r8-b2-monotone}; later dispersing impacts therefore cannot focus
them to zero.  The final impact-coordinate conversion costs only
$\delta^{-C}$.  The bad-set estimate follows by integrating one speed, angle,
or event-gap variable at the first failing event; the number of possible
locations is exponential in $m$ but is later absorbed by the hierarchy
factorial.
\end{proof}

\begin{corollary}[Summable recollision sectors]
\label{cor:r8-b2-recollision}
The cyclic depth-$m$ contribution is bounded by
\[
 \frac{(CT)^m}{m!}\bigl(\varepsilon^2\delta^{-C}+\delta^\alpha\bigr).
\]
Choosing $\delta=\varepsilon^{2/(C+\alpha)}$ and summing over $m$ gives
$C_T\varepsilon^{\eta}$ for some $\eta>0$.
\end{corollary}

\begin{proof}
Coarea at the surplus contact gives the first term using
Theorem~\ref{thm:r8-b2-gramian}; the bad chronological set gives the second.
The exponential genealogy count is absorbed by $m!^{-1}$.  Optimize in
$\delta$ and sum the exponential series.
\end{proof}

\subsection{Source-uniform one-block convergence}

Let $\mathcal S_{h,\varepsilon}^H$ be the exact microscopic graph semigroup with
a bounded contact source $H$ and let $\mathcal S_h^H$ be the Boltzmann graph
semigroup.

\begin{theorem}[One-block graph-domain estimate]
\label{thm:r8-b2-one-block}
For every reached graph radius and source ball,
\[
 \|\mathcal S_{h,\varepsilon}^H-\mathcal S_h^H\|_{\mathcal X_\beta\to
 \mathcal X_{\beta'}}
 \le C_R(h\varepsilon^\eta+\varepsilon^\eta),
\]
and the same bound holds for every fixed number of source derivatives.
\end{theorem}

\begin{proof}
Expand both semigroups by the boundary-renewal Duhamel series.  Noncyclic trees
are matched by the Boltzmann--Grad collision change of variables in the graph
trace coordinates.  Cyclic trees are bounded by
Corollary~\ref{cor:r8-b2-recollision}.  Differentiating the bounded source only
marks collision vertices and is absorbed by a smaller factorial radius.  The
remainder from two events inside one block is $O(h\varepsilon^\eta)$ by the
same coarea estimate.
\end{proof}

\begin{theorem}[Stable source sewing]
\label{thm:r8-b2-sewing}
Products of one-block operators converge, uniformly over partitions whose mesh
tends to zero, to one nonlinear pressure semigroup.  The limit is independent
of the partition and retains the actual reflected future after every contact.
\end{theorem}

\begin{proof}
The graph semigroups are uniformly Lipschitz on reached source balls.  A
standard telescoping sum uses
Theorem~\ref{thm:r8-b2-one-block}; consistency of adjacent blocks follows from
the exact Markov semigroup law in
Theorem~\ref{thm:r8-b2-semigroup}.  The state is never stopped or deleted at a
surplus contact, so the reflected future remains in every factor.
\end{proof}

\subsection{Balance-preserving regularization}

Let $\mathfrak G_h$ be a heat kernel on the compact group generated by spatial
translations and small canonical velocity transformations preserving total
momentum, energy, and the hard-sphere collision manifold.  Average an entire
microscopic path law under this group and then take its density/contact
coordinates.

\begin{lemma}[Commuting path-law smoothing]
\label{lem:r8-b2-smoothing}
The regularization $R_h$ obtained in this way preserves positivity, total mass,
momentum, energy, and the exact weak density--contact balance.  It maps finite
action states to smooth compatible graph states and
$R_h(f,\Gamma)\to(f,\Gamma)$ in the action topology.
\end{lemma}

\begin{proof}
Every group element conjugates the hard-sphere flow and its contact current,
so pushforward commutes with the microscopic continuity identity.  Averaging
therefore preserves the identity exactly.  Heat-kernel convolution on the
group smooths every noninvariant direction.  Convexity of relative entropy
gives action contraction, and approximate-identity convergence gives the
limit.
\end{proof}

\begin{theorem}[Grand-canonical and microcanonical dynamic LDP]
\label{thm:r8-b2-ldp}
The sewn pressure has the full good convex dual on compatible density--contact
paths.  Smooth positive exposed paths give the lower bound by source tilting;
Lemma~\ref{lem:r8-b2-smoothing} extends it to every finite-action path.  B1
coefficient transfer then yields the microcanonical LDP in the order
GC$\to$B1$\to$MC.
\end{theorem}

\begin{proof}
The upper bound follows from exponential tightness and the source Laplace
principle.  On a smooth compatible path, finite projected exposing equations
have uniformly positive covariance on the quotient of collision invariants;
solve them and pass through the source sewing theorem.  Approximate a general
finite-action path by $R_h$ and use lower semicontinuity plus entropy
contraction.  Finally apply the exact shell ratio of B1 to every finite source
projection and then use exponential tightness.
\end{proof}
'''

B3 = r'''
\section{Round-eight reconstruction: Schur-completed fluctuation geometry and direct cumulant covariance}
\label{sec:r8-b3}

The raw second derivative of the perspective entropy along the nonlinear
manifold $A=A_f$ is not intrinsically positive.  We do not call it a metric.
The covariance is first constructed as the positive limit of exact
finite-volume cumulants and is then identified with the Schur-completed KKT
operator of the constrained variational problem.

\subsection{Raw curvature and the missing constraint multiplier}

For
\[
 \Phi(g,a)=g\log(g/a)-g+a,
 \qquad g=qa,
\]
the chain rule gives
\[
 D^2\Phi=\frac{|\dot g-q\dot a|^2}{qa}
 +(1-q)D^2a.
\]
The last term may have either sign.  Introduce an independent reference
variable $a$ and the nonlinear constraint
\[
 \mathcal C(f,a)=a-A_f=0
\]
with multiplier $\lambda$.  At a stationary phase the bordered Lagrangian is
\[
 \mathscr L=I_0(f_0)+\int\Phi(\Gamma,a)
 +\langle\lambda,a-A_f\rangle+	ext{balance multipliers}.
\]

\begin{lemma}[KKT curvature cancellation]
\label{lem:r8-b3-kkt}
On the critical tangent space, the second derivative of the multiplier term
contributes $-\langle\lambda,D^2A_f[\dot f,\dot f]\rangle$.  The true reduced
Hessian is the Schur complement of the bordered KKT Hessian, not the raw
perspective expression alone.
\end{lemma}

\begin{proof}
Differentiate the constraint twice along a feasible curve:
$\ddot a=DA_f\ddot f+D^2A_f[\dot f,\dot f]$.  In the second variation of the
Lagrangian the terms involving $\ddot f,\ddot a$ vanish by stationarity, while
the displayed curvature is retained with the multiplier sign.  Eliminating
$(\dot a,\dot\lambda)$ by the linearized constraint is precisely a Schur
complement operation.
\end{proof}

\subsection{Covariance from exact finite-volume cumulants}

For a bounded test $F$ let $S_F^\varepsilon$ be the exact centered additive
observable and define
\[
 \Sigma_\varepsilon(F,G)=\mu_\varepsilon^{-1}
 \operatorname{Cov}(S_F^\varepsilon,S_G^\varepsilon).
\]

\begin{theorem}[Positive cumulant limit]
\label{thm:r8-b3-cumulant}
On every regular prepared source chart the forms $\Sigma_\varepsilon$ converge
on a dense nuclear test space to a continuous positive semidefinite form
$\Sigma$.  Its null space consists exactly of collision invariants and dynamic
gauges.
\end{theorem}

\begin{proof}
B2 source sewing gives convergence of the logarithmic moment generating
function in a neighborhood of zero, uniformly with two source derivatives.
Differentiation at the exact finite mean gives convergence of covariances.
Every finite-volume covariance is positive semidefinite, hence so is the
limit.  If $F$ is an invariant or a telescoping dynamic gauge, its centered
sum is a boundary term and has zero scaled variance.  Conversely, zero
variance makes every two-sided pressure derivative vanish; the backward
observability theorem below identifies such a test with an invariant plus a
gauge.
\end{proof}

\subsection{Weighted observability and closed range}

Let $\mathcal B$ be the linearized density--contact balance operator in the
weighted graph Hilbert spaces inherited from B2.  Its adjoint is the backward
kinetic equation with reflected boundary data.

\begin{lemma}[Backward hypocoercive estimate]
\label{lem:r8-b3-observability}
For every finite horizon and regular phase there is $C_T$ such that a backward
solution $\psi$ orthogonal to collision invariants satisfies
\[
 \|\psi\|_{\mathsf H_T}
 \le C_T\bigl(\|\mathcal B^*\psi\|_{\mathsf H_T^*}
 +\|\gamma^-\psi\|_{\mathsf T_T}\bigr).
\]
\end{lemma}

\begin{proof}
Multiply the backward equation by the inverse Maxwellian weight.  Transport is
skew after the Green identity of B2.  The linearized collision form controls
the velocity-orthogonal component by its spectral gap.  Commuting one spatial
derivative with transport and using the collision control gives the missing
macroscopic modes; conservation removes the finite invariant kernel.  Boundary
terms are exactly the trace norm.  Integrate the differential inequality and
apply Gronwall.
\end{proof}

\begin{theorem}[Closed range and exact gauge annihilator]
\label{thm:r8-b3-range}
The balance operator $\mathcal B$ has closed range.  The annihilator of balanced
tangents is the closed span of collision invariants and backward dynamic
gauges.  No further zero-variance classes occur.
\end{theorem}

\begin{proof}
Lemma~\ref{lem:r8-b3-observability} is the closed-range estimate for
$\mathcal B^*$ modulo its finite-dimensional invariant kernel.  The Hilbert
closed-range theorem gives closed range for $\mathcal B$.  Integration by parts
shows that invariants and gauges annihilate every balanced tangent.  The
orthogonal decomposition supplied by the estimate proves the converse.
\end{proof}

\subsection{Schur-completed covariance operator}

Let $\mathsf T$ be the balanced tangent Hilbert space modulo the null space in
Theorem~\ref{thm:r8-b3-range}.  Define the bilinear form
\[
 \mathfrak q^{\rm sc}(u,v)=\Sigma^{-1}(u,v)
\]
initially on the range of the covariance map.

\begin{theorem}[Coercive Schur completion]
\label{thm:r8-b3-schur}
The reduced KKT Schur complement from
Lemma~\ref{lem:r8-b3-kkt} closes to the positive form
$\mathfrak q^{\rm sc}$ on $\mathsf T$.  There are $c,C>0$ such that
\[
 c\|u\|_{\mathsf T}^2\le\mathfrak q^{\rm sc}(u,u)
 \le C\|u\|_{\mathsf T}^2.
\]
The possibly negative raw term $(1-q)D^2A_f$ is not asserted to be positive by
itself.
\end{theorem}

\begin{proof}
At finite volume, constrained Legendre duality identifies the inverse
covariance on the quotient with the Schur complement of the bordered Hessian.
The exact finite covariance is positive.  Uniform source curvature and
Lemma~\ref{lem:r8-b3-observability} give lower and upper bounds independent of
volume.  Mosco convergence of these finite quadratic forms identifies the
limit with $\Sigma^{-1}$ and with the closed KKT Schur complement.
\end{proof}

\begin{corollary}[Lax--Milgram covariance]
\label{cor:r8-b3-lax}
For every continuous cotangent $\xi$ on $\mathsf T$ there is a unique
$u_\xi\in\mathsf T$ such that
\[
 \mathfrak q^{\rm sc}(u_\xi,v)=\langle\xi,v\rangle.
\]
The operator $\xi\mapsto u_\xi$ is the covariance operator $\Sigma$.
\end{corollary}

\begin{proof}
Apply Lax--Milgram to the continuous coercive form in
Theorem~\ref{thm:r8-b3-schur}.  The finite-volume identification and passage to
the limit show that the solution has the cumulant covariance pairings.
\end{proof}

\subsection{Process tightness from connected cumulant densities}

\begin{lemma}[Tree-decaying cumulant density]
\label{lem:r8-b3-tree}
For tests in a bounded nuclear set, the connected $r$-time cumulant density
satisfies
\[
 |\kappa_r(t_1,\ldots,t_r)|
 \le C^r r!\exp\{-c\,\mathsf T(t_1,\ldots,t_r)\},
\]
where $\mathsf T$ is the length of a minimal spanning tree on the time points.
\end{lemma}

\begin{proof}
Expand the source derivatives of the B2 renewal semigroup into connected
collision clusters.  Cutting the largest time gap disconnects a cluster unless
a collision genealogy crosses that gap; the graph semigroup gap gives an
$e^{-c\Delta}$ factor.  Iterating over the edges of a spanning tree gives the
bound.  Contact-time diagonals are integrated as trace measures and are
included in the same estimate.
\end{proof}

\begin{theorem}[Exact-centered Gaussian fluctuation process]
\label{thm:r8-b3-process}
The fields centered at their exact finite-volume means converge in a weighted
nuclear-path topology to the Gaussian process with covariance $\Sigma$.
For adjacent deterministic intervals $I,J$,
\[
 |\operatorname{Cov}(Z(I),Z(J))|
 \le C\int_I\int_J e^{-c|t-s|}\,dt\,ds,
\]
which yields fractional Sobolev tightness and includes near-diagonal contact
correlations.
\end{theorem}

\begin{proof}
Lemma~\ref{lem:r8-b3-tree} makes all cumulants of order at least three vanish
after central scaling and gives the displayed two-interval bound without
assuming a product $|I||J|$ estimate at the common endpoint.  The second
cumulant converges by Theorem~\ref{thm:r8-b3-cumulant}.  Kolmogorov--fractional
Sobolev estimates give scalar tightness; nuclear equicontinuity and Mitoma's
criterion give process tightness.
\end{proof}
'''

B4 = r'''
\section{Round-eight reconstruction: dynamic action, resolvent graph core, and two-scale comparison}
\label{sec:r8-b4}

Static preparation is charged once in the initial boundary functional.  The
transition action contains only dynamic contact entropy.  Smooth graph-core
approximation uses resolvents of the actual generator, not an impossible
analytic cutoff.  Comparison uses a slope-capped penalty whose coercivity is
sent to infinity in a separate limit.

\subsection{Dynamic-only transition action}

For a compatible path $(f,\Gamma)$ on $[s,t]$ set
\[
 \mathcal A_{s,t}(f,\Gamma)
 =\int_s^t\int\ell\!\left(\frac{d\Gamma}{dA_f}\right)dA_f\,dr,
 \qquad \ell(q)=q\log q-q+1.
\]
The preparation rate $I_0(f_s)$ is not part of $\mathcal A_{s,t}$.

\begin{lemma}[Exact cocycle of dynamic cost]
\label{lem:r8-b4-cost}
For every compatible path and $s<u<t$,
\[
 \mathcal A_{s,t}=\mathcal A_{s,u}+\mathcal A_{u,t}.
\]
Consequently the Lax--Oleinik operators
\[
 (V_t\varphi)(f)=\sup\{\varphi(f_t)-\mathcal A_{0,t}:f_0=f\}
\]
satisfy $V_{t+s}=V_tV_s$.
\end{lemma}

\begin{proof}
The action is an integral over time, so additivity is exact.  Concatenation of
compatible paths preserves the B2 graph trace at the joining time.  Splitting
and concatenating competitors proves both semigroup inequalities.  A static
rate can be applied once to an initial distribution, but is never recharged at
the intermediate state.
\end{proof}

\subsection{A resolvent-generated graph core}

Let $\mathbb A_\varepsilon$ be the closed augmented BBGKY generator on the
law/contact-ledger state and
\[
 J_{\lambda,\varepsilon}=(I-\lambda\mathbb A_\varepsilon)^{-1}.
\]
For a finite coordinate projection $\Pi_m$ and
$\phi\in C_c^\infty(\mathbb R^m)$ define
\[
 F_{m,\lambda}(z)=\phi(\Pi_mJ_{\lambda,\varepsilon}z).
\]

\begin{lemma}[Smooth bounded graph core]
\label{lem:r8-b4-core}
The algebra generated by $F_{m,\lambda}$ lies in the generator graph domain,
is stable under products, and is dense on every action compactum in the
$C_b^2$ cylinder graph norm as $m\to\infty$ and $\lambda\downarrow0$.
\end{lemma}

\begin{proof}
The resolvent maps the state space into $D(\mathbb A_\varepsilon)$ and satisfies
$\mathbb A_\varepsilon J_\lambda=
\lambda^{-1}(J_\lambda-I)$.  The chain rule gives bounded graph images because
$\phi$ has compactly supported derivatives.  The Markov generator product
formula on its resolvent range gives stability under products.  Compact
containment makes finitely many coordinates uniformly separating; Stone--Weierstrass
approximates the cylinder, and $J_\lambda\to I$ uniformly on the compactum.
No analytic function is required to equal one on an open box and change
outside it.
\end{proof}

\subsection{Exact finite-generator correctors}

Let $V_\varepsilon(t,s)$ be the backward observable propagator of the actual
finite augmented generator along a smooth reference path:
\[
 \partial_tV_\varepsilon(t,s)
 =-\mathbb A_\varepsilon(t)V_\varepsilon(t,s),
 \qquad V_\varepsilon(s,s)=I.
\]
For the order-$j$ defect $D_j^\varepsilon$ define
\[
 c_j^\varepsilon(t)=\int_t^T
 V_\varepsilon(t,s)D_j^\varepsilon(s)\,ds.
\]

\begin{theorem}[Finite graph-domain Poisson corrector]
\label{thm:r8-b4-corrector}
Each corrector belongs to the finite generator graph domain and satisfies
\[
 \partial_tc_j^\varepsilon+
 \mathbb A_\varepsilon(t)c_j^\varepsilon=-D_j^\varepsilon,
 \qquad c_j^\varepsilon(T)=0.
\]
For suitable ledger weights,
\[
 \sum_{j\ge0}\sup_{t\le T}
 \bigl(\|c_j^\varepsilon(t)\|+
 \|\mathbb A_\varepsilon c_j^\varepsilon(t)\|\bigr)<\infty
\]
uniformly in $\varepsilon$.
\end{theorem}

\begin{proof}
The propagator is constructed by forward Picard iteration in remaining time
and preserves the graph domain by the B2 boundary-renewal estimate.
Differentiating the Duhamel integral gives the equation and the exact zero
terminal value.  A defect with $j$ ledger labels carries the cluster factor
$C^j/j!$; choose the ledger radius below the graph-semigroup radius and sum the
resulting exponential series, including the generator images.
\end{proof}

\subsection{A two-scale slope-capped penalty}

Let $d_B$ be a bounded-compatible metric on an energy containment set.  Choose
$\omega\in C^2([0,\infty))$ increasing, $\omega(r)=r$ for $r\le1$, and
$0\le\omega'\le1$.  For $R\ge1$ and $\eta>0$ put
\[
 \Psi_{R,\eta}(f,g)=\frac{R}{\eta}\omega(\eta d_B(f,g)).
\]
For fixed $R$, its cotangent norm is at most $R$.  Coercivity is obtained by
$R\to\infty$, not by falsely claiming that $\eta\downarrow0$ makes a bounded
distance diverge.

\begin{lemma}[Diagonal localization with bounded slopes]
\label{lem:r8-b4-penalty}
At a doubled maximum of
$u(f)-v(g)-\Psi_{R,\eta}(f,g)$ on a common containment set,
\[
 d_B(f,g)\le C/R+o_\eta(1),
\]
while both test cotangents lie in the Hamiltonian source ball of radius $R$.
\end{lemma}

\begin{proof}
Compare the maximum with the diagonal point $f=g$.  Since $u,v$ are bounded,
$\Psi_{R,\eta}(f,g)\le C$.  For $\eta d_B\le1$ the penalty is $Rd_B$; if
this inequality fails, first choose $\eta$ small on the bounded metric space.
Thus $d_B\le C/R$.  Differentiation gives slope
$R\omega'(\eta d_B)\le R$.
\end{proof}

\begin{theorem}[Comparison on the full state space]
\label{thm:r8-b4-comparison}
Assume the Hamiltonian is continuous on every bounded cotangent ball and obeys
the exponential containment bound supplied by B2.  Then bounded upper and
lower viscosity solutions of the Hamilton--Jacobi equation satisfy comparison.
\end{theorem}

\begin{proof}
Fix $R$ and apply the doubled-variable argument with
$\Psi_{R,\eta}$.  Lemma~\ref{lem:r8-b4-penalty} keeps the two cotangents inside
the radius-$R$ chart.  Let $\eta\downarrow0$ to remove smoothing of the metric.
The states are then within $C/R$.  Hamiltonian continuity on the radius-$R$
chart makes the state mismatch vanish as $R\to\infty$; exponential containment
removes the complement before this limit.  The standard viscosity inequalities
then contradict a positive maximum.
\end{proof}

\subsection{Nonlinear semigroup convergence}

\begin{theorem}[Microscopic-to-kinetic nonlinear semigroups]
\label{thm:r8-b4-convergence}
The logarithmic microscopic semigroups on the augmented law/contact-ledger state
converge locally uniformly on action compacta to the dynamic Lax--Oleinik
semigroup generated by $\mathcal A$.  The limit is unique and satisfies the
semigroup law.
\end{theorem}

\begin{proof}
Use the graph core of Lemma~\ref{lem:r8-b4-core} and the exact correctors of
Theorem~\ref{thm:r8-b4-corrector} to obtain generator convergence with no
terminal remainder.  B2 gives compact containment and lower recovery for the
dynamic action.  Half-relaxed limits are viscosity sub- and supersolutions.
The comparison theorem identifies them.  Lemma~\ref{lem:r8-b4-cost} supplies
the semigroup law and prevents double charging of preparation.
\end{proof}
'''
C1 = r'''
\section{Round-eight reconstruction: evidence-lifted stratified filtering and reachable-state reduction}
\label{sec:r8-c1}

Exact observations are retained.  Because their coarea normalizers may be
arbitrarily small, the filtering state carries the unnormalized current and
its evidence separately.  Repeated observation and collision intersections
are represented by a closed tower of compatible currents of every reached
codimension.

\subsection{The stratified current tower}

For a chronological cell let $\mathfrak S$ be the finite family generated by
collision faces and the level sets of the observation maps used up to the
current decision time.  A state is a current chain
\[
 \mathbf J=(J_S)_{S\in\mathfrak S}
\]
with $J_S$ a finite measure on the stratum $S$ and incidence compatibility
\[
 \partial J_S=\sum_{S'\prec S}\epsilon(S,S')J_{S'}.
\]
Set
\[
 \|\mathbf J\|_{\rm str}
 =\sum_{S\in\mathfrak S}\omega^{\operatorname{codim}S}
 \bigl(\|J_S\|_{\mathcal M_w}+
 \|T_SJ_S\|_{\mathcal M_w}\bigr).
\]

\begin{lemma}[Closed stratified trace complex]
\label{lem:r8-c1-tower}
The B2 transport, reflection, contact creation, and coarea restriction operators
act triangularly on the completion of compatible current chains.  The boundary
operator squares to zero, and every new observation of rank $r$ raises
codimension by $r$ while preserving the incidence identities.
\end{lemma}

\begin{proof}
Start with smooth differential forms on every transverse stratum.  Stokes'
formula gives the incidence relation and $\partial^2=0$.  B2 transport maps a
stratum to its flow image; reflection and contact maps preserve incidence
orientation.  Coarea restriction is intersection with the level-set current,
and transverse intersection commutes with boundary up to the standard signed
incidence rule.  The weighted triangular norm absorbs the finite number of
new lower strata.  Passing to the graph completion proves the assertion.
\end{proof}

\subsection{Evidence-lifted Bayes updates}

For an observation $O^u$ and an unnormalized predicted current $\bar\Pi^-$,
define
\[
 \bar\Pi^y=\delta_{O^u=y}\bar\Pi^-
\]
by coarea intersection and let
\[
 Z(y)=\bar\Pi^y(1).
\]
The normalized posterior is $\Pi^y=\bar\Pi^y/Z(y)$ when $Z(y)>0$, but the state
used by the dynamic program is
\[
 (\bar\Pi^y,\ell^y),\qquad \ell^y=-\log Z(y).
\]

\begin{lemma}[Bayes update without a uniform denominator]
\label{lem:r8-c1-evidence}
The map $(\bar\Pi^-,y)\mapsto(\bar\Pi^y,\ell^y)$ is continuous in the projective
stratified topology on every finite evidence slice $\ell^y\le L$.  Values with
small $Z(y)$ are retained with their exact evidence cost; no uniform positive
lower bound for $Z(y)$ is asserted.
\end{lemma}

\begin{proof}
On a transverse observation chart, coarea intersection is continuous in the
stratified graph norm.  Projective normalization is continuous when
$Z(y)\ge e^{-L}$.  If $Z(y)$ is smaller, the unnormalized current still varies
continuously and the diverging log evidence is an explicit state coordinate
rather than a hidden norm blowup.
\end{proof}

\begin{theorem}[Feller evidence-filter kernel]
\label{thm:r8-c1-feller}
Assume the observation maps are uniformly transverse on each declared chart
and their critical strata are included explicitly in $\mathfrak S$.  Prediction
by the B2 graph semigroup followed by the evidence-lifted update defines a
Feller kernel on the compactly contained evidence state space.
\end{theorem}

\begin{proof}
Prediction is Feller by the B2 graph semigroup.  On each transverse chart apply
Lemma~\ref{lem:r8-c1-evidence}.  The critical set is not identified with the
billiard grazing set; it is a separate finite current stratum and is propagated
by Lemma~\ref{lem:r8-c1-tower}.  Exponential evidence containment removes the
large-$\ell$ tail uniformly, so the finite chart pieces paste to a Feller
kernel.
\end{proof}

\subsection{Quenched coarea coefficients}

A noiseless observation inserts the Fourier representation of its level-set
current.  The observation frequency is therefore an additional continuous
frequency in the B2/B1 coefficient theorem.

\begin{theorem}[Uniform coarea local-limit ratio]
\label{thm:r8-c1-ratio}
On every strategy-reachable compact source and evidence set, numerator and
denominator coarea coefficients have one common exact saddle and satisfy
uniform mixed local-limit expansions.  Their ratio converges to the conditional
block pressure, while the finite log-evidence term is retained explicitly.
\end{theorem}

\begin{proof}
Use Fourier inversion for the collision/lattice constraints together with the
observation delta.  Transversality gives a full-rank observation block; the B1
good-block regeneration gives high-frequency decay, and B2 source sewing gives
uniform bounded-source derivatives.  Major-arc Gaussian coefficients are
computed at the same saddle.  Dividing cancels the common determinant and shell
factor, not the evidence itself.  The remaining $-\log Z(y)$ is precisely the
state coordinate from Lemma~\ref{lem:r8-c1-evidence}.
\end{proof}

\subsection{Reachable chaotic beliefs and state reduction}

Let $\mathfrak R_\varepsilon$ be the set of posterior currents reachable from
the prepared initial family under admissible strategies and the declared
observation channels.  For $j\ge2$ let $\mathfrak h_j(\Pi)$ be the connected
$j$-particle hierarchy coordinate.

\begin{lemma}[Posterior chaos on the reachable set]
\label{lem:r8-c1-chaos}
There are $C,\rho<1$ such that, uniformly over
$\Pi\in\mathfrak R_\varepsilon$ and finite decision horizons,
\[
 \|\mathfrak h_j(\Pi)\|_{\rm str}
 \le C^j j!\mu_\varepsilon^{1-j}+C\rho^j\varepsilon^\eta.
\]
\end{lemma}

\begin{proof}
The prepared law has the cluster bound by B1.  B2 graph propagation maps a
connected $j$-cluster either to a connected kinetic cluster or to a recollision
sector; the latter carries $\varepsilon^\eta$.  The coarea update marks only
finitely many coordinates and its evidence-lifted form does not divide the
unnormalized connected current by a small $Z$.  Normalize only after restricting
to a finite evidence slice and then use the same ratio theorem.  Induction over
decision times gives the estimate.
\end{proof}

\begin{theorem}[Asymptotic sufficiency on strategy-reachable beliefs]
\label{thm:r8-c1-sufficiency}
For bounded Lipschitz rewards and finite horizons, the value difference between
two reachable beliefs with the same resolved kinetic state and evidence ledger
is $o(1)$ uniformly over strategies.  The limiting dynamic program is therefore
posed on the resolved kinetic state.  No equivalence is claimed for arbitrary
correlated beliefs sharing only a one-particle mean.
\end{theorem}

\begin{proof}
Expand one Bellman step in connected hierarchy coordinates.  The zeroth and
first coordinates are the resolved state and evidence.  Every remaining term
is bounded by the reward Lipschitz constant times the series in
Lemma~\ref{lem:r8-c1-chaos}, which tends to zero uniformly.  Backward induction
over the finite number of decisions proves the result.
\end{proof}

\subsection{Risk-sensitive games and local statistics}

\begin{theorem}[Risk-sensitive saddle and exact-centered LAN]
\label{thm:r8-c1-game-lan}
The evidence-filter kernel and quenched ratio theorem yield the finite-horizon
risk-sensitive minimax recursion with measurable selectors.  For a continuous
prior with positive local density, the posterior has the LAN expansion centered
at the exact finite-volume mean, and the local denominator is bounded below by
the prior mass of a shrinking regular ball times the Gaussian coefficient.
\end{theorem}

\begin{proof}
Feller continuity and compact containment give measurable minimax selectors.
Theorem~\ref{thm:r8-c1-ratio} passes each block payoff and transition kernel to
the limit.  Differentiate the exact finite log likelihood at its own mean; the
linear score is exactly centered.  Integrating the local quadratic likelihood
over a ball on which the prior density has a positive minimum gives the
explicit denominator.  B3 supplies the limiting covariance on the quotient.
\end{proof}
'''

C2 = r'''
\section{Round-eight reconstruction: closed linear null spaces and Kato transfer bundles}
\label{sec:r8-c2}

The invariant-integral null space is a closed linear span.  Invariant
separators are obtained only under a uniform Cesaro Lyapunov bound.  Source
response is transported in one common transfer-operator Banach bundle; no
Radon--Nikodym derivative between mutually singular infinite-volume Gibbs
measures is used.

\subsection{Two closed linear null spaces}

Let $\mathscr A_W$ be the weighted path-observable Banach space.  Define
\[
 \mathscr N_{\rm int}
 =\overline{\operatorname{span}}
 \{U-U\circ\Theta_t:U\in\mathscr A_W,\ t\ge0\},
\]
\[
 \mathscr N_{\rm pr}=\mathscr N_{\rm int}\oplus\mathbb R\mathbf1.
\]

\begin{theorem}[Invariant-integral quotient]
\label{thm:r8-c2-null}
Suppose every separating weighted functional satisfies the uniform Cesaro
Lyapunov estimate
\[
 \sup_{T\ge1}\frac1T\int_0^T
 \int W\circ\Theta_t\,d|\mu|\,dt<\infty.
\]
Then $F-G\in\mathscr N_{\rm int}$ exactly when all invariant probabilities have
the same integral on $F$ and $G$.  Normalized pressure equivalence uses
$\mathscr N_{\rm pr}$ and therefore also quotients constants.
\end{theorem}

\begin{proof}
Every coboundary integrates to zero under an invariant law, and linear closure
preserves this property.  Conversely, separate $F-G$ from
$\mathscr N_{\rm int}$ by a weighted Radon functional $\mu$.  Average it:
\[
 \mu_T=T^{-1}\int_0^T(\Theta_t)_\#\mu\,dt.
\]
The uniform Cesaro Lyapunov bound and compact sublevels of $W$ give tightness.
Any weak limit is invariant, annihilates every coboundary, and retains the
nonzero pairing with $F-G$, contradicting equality of invariant integrals.
Adding constants gives the normalized pressure quotient.
\end{proof}

\subsection{A common transfer-space bundle}

Let $\mathscr B$ be one fixed strong/weak transfer scale after moving-chart
identifications.  The source-dependent Ruelle operators
$\mathcal L_\eta:\mathscr B^+\to\mathscr B^-$ form a $C^m$ family with an
isolated spectral projector $P_\eta$.  Equilibrium measures are dual
eigenvectors in $\mathscr B^*$; they need not be mutually absolutely
continuous on infinite path space.

\begin{lemma}[Kato parallel transport]
\label{lem:r8-c2-kato}
The equation
\[
 U_\eta'=[P_\eta',P_\eta]U_\eta,
 \qquad U_0=I,
\]
has a bounded invertible solution on $\mathscr B$ and
\[
 P_\eta U_\eta=U_\eta P_0.
\]
It transports the resolved spectral subspace without density square roots.
\end{lemma}

\begin{proof}
The commutator is a bounded continuous operator on the common scale, so the
operator ODE has a unique product-integral solution.  Differentiate
$P_\eta U_\eta-U_\eta P_0$ and use $P_\eta^2=P_\eta$; the derivative is the
same homogeneous commutator equation with zero initial value, hence vanishes.
\end{proof}

Define the covariant derivative of a resolved section by
\[
 \nabla_\eta s=P_\eta U_\eta
 \frac d{d\eta}(U_\eta^{-1}s).
\]

\begin{theorem}[Covariant Doob response without mutual absolute continuity]
\label{thm:r8-c2-doob}
The derivatives of the transfer eigenfunction, dual eigenfunctional, pressure,
Doob operator, and resolved projector exist in the common Banach bundle and are
obtained by the reduced resolvent and Kato connection.  Finite-time likelihood
cocycles implement the same derivative on path cylinders; no infinite-path
Radon--Nikodym derivative is required.
\end{theorem}

\begin{proof}
Differentiate the isolated Riesz projector and use the reduced resolvent on
$(I-P_\eta)\mathscr B$.  Normalize the right and left eigenvectors by their
dual pairing.  Kato transport identifies the moving ranges.  On a finite path
cylinder all source laws have positive densities relative to the same reference
kernel, so ordinary likelihood differentiation is valid there.  The uniform
transfer estimates pass the finite-time formulas to the Banach-bundle limit.
Distinct infinite-volume ergodic laws may remain mutually singular.
\end{proof}

\subsection{Causal memory and moving resolved subspaces}

Let $C_\eta(z)$ be the compressed resolvent on
$P_\eta\mathscr B$ and $A_\eta=P_\eta L_\eta P_\eta$.  Define
\[
 \widehat K_\eta(z)=zP_\eta-A_\eta-C_\eta(z)^{-1}.
\]

\begin{theorem}[Covariant causal-memory derivative]
\label{thm:r8-c2-memory}
Under Kato transport,
\[
 \nabla_\eta\widehat K_\eta
 =z\nabla_\eta P_\eta-
 \nabla_\eta A_\eta+
 C_\eta^{-1}(\nabla_\eta C_\eta)C_\eta^{-1}.
\]
All connection terms from the moving projector and eigenvectors are included.
\end{theorem}

\begin{proof}
Differentiate the correctly signed identity
$\widehat K=zP-A-C^{-1}$ in the common bundle.  The inverse rule gives
$\nabla(C^{-1})=-C^{-1}(\nabla C)C^{-1}$, producing the plus sign in the last
term.  Lemma~\ref{lem:r8-c2-kato} supplies the derivative of the moving range,
and Theorem~\ref{thm:r8-c2-doob} supplies the eigenvector and pairing terms.
\end{proof}

\subsection{Optional projections}

For the A4 past filtration let
\[
 M_t^\varepsilon=
 \frac{\mathbb E[\exp\{\theta\Phi(X_T^\varepsilon)\}
 \mid\mathcal F_t^-]}{
 \mathbb E\exp\{\theta\Phi(X_T^\varepsilon)\}}.
\]

\begin{theorem}[Stable optional-projection convergence]
\label{thm:r8-c2-optional}
The exact likelihood martingales converge jointly with the A4 past kernels to
the diffusion optional projection, stably under bounded initial-past
multipliers.
\end{theorem}

\begin{proof}
Write the conditional expectation as the A4 past-kernel integral of the
terminal functional.  Uniform quenched kernel convergence on compact past sets
gives convergence there; exponential containment removes the complement.
Multiplication by a bounded initial-past variable is absorbed into the same
kernel integral, which proves stable convergence.
\end{proof}

\subsection{Nonlinear contractions without a fictitious adjoint}

For a continuous nonlinear contraction $\mathcal C:X\to Y$, define the test
pullback
\[
 \mathcal C^\sharp\phi=\phi\circ\mathcal C.
\]

\begin{theorem}[Typed pressure--rate contraction chain rule]
\label{thm:r8-c2-contraction}
If $Q_X$ is differentiable on a source chart, then
\[
 Q_Y(\phi)=Q_X(\mathcal C^\sharp\phi),
 \qquad
 DQ_Y(\phi)h=
 DQ_X(\phi\circ\mathcal C)(h\circ\mathcal C).
\]
The rate on $Y$ is
\[
 I_Y(y)=\inf_{\mathcal C(x)=y}I_X(x).
\]
For smooth parameterized contractions, the next derivative uses the ordinary
Frechet chain rule for $\phi\circ\mathcal C_\eta$.
\end{theorem}

\begin{proof}
The Laplace functional of the pushforward is the Laplace functional of the
pulled-back test.  Differentiate this identity on the declared source chart.
The rate formula is the contraction principle.  No linear adjoint of the
nonlinear map $\mathcal C$ is introduced.
\end{proof}
'''

D1 = r'''
\section{Round-eight reconstruction: phase-restricted microscopic laws and stratified mixture LDPs}
\label{sec:r8-d1}

Coexistence is handled by genuine phase-restricted finite-volume objects and an
exponential mixture theorem.  Lee--Yang zeros may pinch the coexistence set;
there is no common zero-free tube there.  The minimum of phase rates is proved
from the microscopic mixture and is not inferred from the conjugate of a
maximum pressure.

\subsection{Phase-restricted partition functions}

Let $j=1,\ldots,J$ label metastable boundary conditions or spectral phase
projectors.  Define finite positive phase laws $\mathbb P_{\varepsilon,j}$ and
restricted partition functions
\[
 Z_{\varepsilon,j}(\theta)
 =\mathbb E_{\varepsilon,j}e^{\mu_\varepsilon\theta\cdot X_\varepsilon}.
\]
On the interior $\mathcal U_j$ of phase $j$, assume the restricted transfer
operator has one isolated eigenvalue.  Its logarithm is analytic on local
complex tubes compactly contained in $\mathcal U_j$.

\begin{lemma}[Local phase atlas only]
\label{lem:r8-d1-atlas}
For every compact $K\Subset\mathcal U_j$, the functions
$\mu_\varepsilon^{-1}\log Z_{\varepsilon,j}$ converge analytically on a fixed
complex neighborhood of $K$.  No such neighborhood is asserted across a
coexistence point.  Zeros of the total partition function are allowed to
approach coexistence at order $\mu_\varepsilon^{-1}$.
\end{lemma}

\begin{proof}
The isolated phase eigenvalue and its Riesz projector persist on a compact
subdomain of one phase, giving a nonvanishing restricted leading term and
analytic perturbation.  At coexistence two restricted exponentials have equal
real part, so their sum can have nearby complex zeros; excluding that set from
the phase tube is both necessary and consistent.
\end{proof}

\subsection{Phase-wise finite-dimensional LDPs}

For a finite projection $\pi_m$, let $Q_{m,j}$ be the phase pressure and
$I_{m,j}=Q_{m,j}^*$ on the interior mean image.  Boundary points are recovered
inside the same microscopic phase by normal concentration followed by a
finite tangential tilt.

\begin{theorem}[Phase-wise face LDP]
\label{thm:r8-d1-phase}
Each restricted law $\mathbb P_{\varepsilon,j}\circ\pi_m^{-1}$ satisfies a full
good LDP with rate $I_{m,j}$ on its attainable compact support.  For an exposed
face $F$, the lower recovery is obtained by first choosing a large but finite
normal source, passing $\varepsilon\to0$, and then sending the normal source to
infinity while applying the phase-specific tangential coefficient theorem.
\end{theorem}

\begin{proof}
Interior points use the analytic phase saddle.  Compact support gives the
upper bound without an infinite source domain.  For a face, exponential
tightness in the normal coordinate makes the finite normal tilt concentrate in
a small neighborhood of $F$.  Conditional on that neighborhood, the
tangential restricted transfer operator remains inside phase $j$ and its
local-limit coefficient gives the lower bound.  A diagonal sequence in the
normal source completes the recovery.
\end{proof}

\subsection{The microscopic mixture theorem}

Assume the physical law has the decomposition
\[
 \mathbb P_\varepsilon
 =\sum_{j=1}^Jw_{\varepsilon,j}\mathbb P_{\varepsilon,j}
 +R_\varepsilon,
\]
where
\[
 \mu_\varepsilon^{-1}\log w_{\varepsilon,j}\to0,
 \qquad
 \|R_\varepsilon\|\le e^{-c\mu_\varepsilon}.
\]

\begin{theorem}[Coexistence mixture LDP]
\label{thm:r8-d1-mixture}
The physical finite-dimensional law satisfies a full LDP with rate
\[
 I_m(x)=\min_j I_{m,j}(x).
\]
Its pressure is $Q_m(\theta)=\max_jQ_{m,j}(\theta)$, but the rate identity is a
consequence of the microscopic mixture, not the generally false formula
$(\max_jQ_{m,j})^*=\min_jQ_{m,j}^*$.
\end{theorem}

\begin{proof}
For a closed set, sum the phase upper bounds; the largest exponential term is
governed by $\inf_x\min_jI_{m,j}(x)$.  For an open set and a point $x$, choose a
phase attaining the minimum and use its lower recovery from
Theorem~\ref{thm:r8-d1-phase}; the subexponential mixture weight does not change
the rate.  The signed remainder is exponentially negligible.  The pressure
maximum follows separately by the log-sum-exp identity.
\end{proof}

\subsection{Projective passage and topology upgrade}

Let $I_j=\sup_m I_{m,j}\circ\pi_m$ and $I=\min_jI_j$.

\begin{theorem}[Stratified projective LDP]
\label{thm:r8-d1-projective}
If every phase family is exponentially tight in the target weighted topology
and every finite-rate point has a phase-wise recovery sequence exponentially
concentrated in one compact rate sublevel, then the physical laws satisfy a
full target-topology LDP with rate $I$.
\end{theorem}

\begin{proof}
Apply the projective theorem phase by phase.  Compact rate sublevels make the
projective and target topologies agree on the recovery supports.  For a
target-open set, choose a projective neighborhood whose intersection with the
selected compact sublevel lies inside it.  The recovery law puts
superexponentially small mass outside that compact, so its projective lower
bound is also a target lower bound.  Combine phases with
Theorem~\ref{thm:r8-d1-mixture}.
\end{proof}

\subsection{Conditioning and exact local likelihoods}

\begin{theorem}[Phase-stratified thin-shell conditioning]
\label{thm:r8-d1-shell}
Suppose each active phase has the A2 or B1 exact finite-source shell coefficient
uniformly on its phase chart.  Conditioning the microscopic mixture on the
shell produces a new subexponential mixture of the conditioned phase laws.
The conditional LDP has the minimum of their constrained rates.
\end{theorem}

\begin{proof}
Apply the phase coefficient theorem to each numerator and denominator.  Phases
whose shell exponent is larger are exponentially suppressed; phases tying at
the minimum retain subexponential weights.  The conditioned law is therefore
again of the form required by Theorem~\ref{thm:r8-d1-mixture}, with each phase
rate restricted to the shell.
\end{proof}

\begin{theorem}[Exact finite-centered likelihood and contraction]
\label{thm:r8-d1-likelihood}
Within every active phase, the local likelihood ratio is centered at the exact
finite-volume mean and has the Gaussian quadratic form supplied by the phase
covariance.  Under any continuous contraction, phase rates contract first and
are then minimized over phases; this order agrees with direct contraction of
the microscopic mixture.
\end{theorem}

\begin{proof}
Taylor expand the restricted finite log partition function at its exact saddle;
the score has zero finite expectation and the Hessian is the exact covariance.
For contraction, push forward each phase law and apply the ordinary contraction
principle.  Pushforward commutes with the finite mixture, so
Theorem~\ref{thm:r8-d1-mixture} gives the minimum of the phase-wise contracted
rates.
\end{proof}
'''

MODULES = {
    "A1-exact-benchmarks": ("A1_COMPLETE_GERMS_ROUTED_HAMILTONIAN.tex", A1),
    "A2-sinai-homological-pressure": ("A2_RENORMALIZED_BUNDLE_UNI_LLT.tex", A2),
    "A3-full-empirical-path-ldp": ("A3_EDGE_FLOW_PROJECTIVE_EPIGRAPH.tex", A3),
    "A4-history-memory-universal-pressure": ("A4_PAST_FILTRATION_CAUSAL_DILATION.tex", A4),
    "B1-microcanonical-preparation": ("B1_REGENERATIVE_COMPOUND_SMOOTHING.tex", B1),
    "B2-collision-clusters-dynamic-ldp": ("B2_COMPATIBLE_TRACE_MONOTONE_JACOBI.tex", B2),
    "B3-hamilton-boltzmann-cotangents": ("B3_SCHUR_CUMULANT_COVARIANCE.tex", B3),
    "B4-nonlinear-kinetic-semigroups": ("B4_DYNAMIC_ACTION_RESOLVENT_COMPARISON.tex", B4),
    "C1-information-risk-sensitive-saddles": ("C1_EVIDENCE_STRATIFIED_FILTER.tex", C1),
    "C2-cotangent-rigidity-tangent-representations": ("C2_KATO_TRANSFER_COTANGENTS.tex", C2),
    "D1-deterministic-theta-contractions": ("D1_PHASE_RESTRICTED_MIXTURE_LDP.tex", D1),
}

ABSTRACTS = {
    "A1-exact-benchmarks": """We replace every finite seam completion by a complete symbolic germ surface carrying all iterated forward and backward cuts.  A depth-weighted current scale gives a spectral gap and finite-order response on the full dynamic singularity ledger.  A symplectic router separates branch motions in an auxiliary canonical coordinate and yields an exact autonomous Hamiltonian suspension with full-port work.""",
    "A2-sinai-homological-pressure": """Orbit births are handled by a renormalized physical current bundle whose norm scales the vanishing birth mode.  A finite family of periodic certificates controls compact frequencies, while a separate geometric nonlocal-integrability theorem supplies Dolgopyat cancellation.  The vector--roof local limit theorem is stated in sum variables and has the correct average-window scaling.""",
    "A3-full-empirical-path-ldp": """The empirical state retains branch labels, transition edges, and actual excursion profiles.  Recession is the boundary of a projective family of genuine finite-coordinate LDPs, and diagonal recovery is constructed from admissible excursion sequences rather than assumed as a Gamma limit.  Exact terminal cut excursions complete deterministic physical time.""",
    "A4-history-memory-universal-pressure": """Conditional future laws are defined on the two-sided natural extension given the genuine past sigma-field.  This yields an all-real-time Feller history semigroup and a uniform quenched rough invariance principle.  The memory realization keeps the complete unresolved Hilbert space, adds only finite Riesz modes, and uses the causal Volterra sign.""",
    "B1-microcanonical-preparation": """A typical compound-Poisson configuration contains linearly many independent full-rank smoothing blocks; configurations without them have exponentially small probability.  This regenerative decomposition gives global high-frequency bounds despite singular one-particle marks.  Covariance comes from typical compound activity, and exact finite saddles yield the mixed microcanonical coefficient.""",
    "B2-collision-clusters-dynamic-ldp": """The kinetic state is the closed graph of interior transport and compatible boundary trace, so collision-time atoms are legitimate without independent arbitrary traces.  A monotone hard-sphere Jacobi form prevents later dispersing collisions from focusing the youngest-fork transverse plane.  This gives depth-independent recollision power, source-uniform sewing, and balance-preserving path-law regularization.""",
    "B3-hamilton-boltzmann-cotangents": """The raw nonlinear perspective Hessian is not declared positive.  Covariance is constructed from exact finite-volume cumulants and identified with the positive Schur completion of the bordered constrained Hessian.  A weighted backward observability theorem gives closed range, while tree-decaying cumulant densities yield an exact-centered Gaussian fluctuation process.""",
    "B4-nonlinear-kinetic-semigroups": """The transition action contains only dynamic contact entropy; static preparation is charged once.  Resolvent-smoothed cylinders form a genuine graph core, and correctors solve the actual finite augmented generator equation.  A two-scale slope-capped penalty localizes doubled states by sending its slope bound to infinity, giving comparison and nonlinear semigroup convergence.""",
    "C1-information-risk-sensitive-saddles": """Repeated exact observations and collision faces generate a compatible stratified current tower.  Filtering uses unnormalized coarea currents together with a log-evidence ledger, so rare regular observations are retained without a false uniform denominator bound.  Quenched coarea coefficients and posterior cluster estimates reduce only the strategy-reachable chaotic belief family to the resolved kinetic state.""",
    "C2-cotangent-rigidity-tangent-representations": """Invariant-integral and pressure null spaces are closed linear spans.  A uniform Cesaro Lyapunov condition yields invariant Radon separators.  Source-dependent Doob and memory response are transported by Kato parallel transport in one common transfer Banach bundle, avoiding nonexistent Radon--Nikodym derivatives between mutually singular equilibrium path measures.""",
    "D1-deterministic-theta-contractions": """Each phase is represented by a genuine restricted finite-volume law with its own local analytic pressure.  Coexistence allows Lee--Yang pinching and is treated by a microscopic subexponential mixture theorem, which directly yields the minimum of phase rates.  Phase-wise face recovery, projective passage, thin-shell conditioning, and exact finite-centered likelihoods remain a standalone positive theory.""",
}

RESPONSES = {
    "A1-exact-benchmarks": [
        "The finite 16-cell biseam object is replaced by the two-sided complete germ surface, so every preimage and iterated seam has an oriented symbolic side.",
        "The transfer scale sums dynamically generated currents with depth weights; compactness and the spectral gap are proved by cylinder truncation and martingale differences.",
        "The support obstruction is removed by a symplectic router in an auxiliary canonical coordinate, not by overlapping disjoint collars.",
    ],
    "A2-sinai-homological-pressure": [
        "The vanishing birth direction is renormalized by |R-R0|; no impossible ambient uniformly bounded inverse is claimed.",
        "Compact-frequency periodic separation uses a finite chartwise family of five-word packets, while high-frequency UNI is proved separately from a geometric temporal-distance derivative.",
        "The master LLT integrates the n^{-3/2} sum density over a roof interval of length nG_n and recovers the missing factor n.",
    ],
    "A3-full-empirical-path-ldp": [
        "Branch labels and transition edges are part of the state, so entropy and admissibility are well typed.",
        "Actual excursion profiles are retained and no branch-average exponential equivalence is needed.",
        "The rate is the supremum of consistent finite-projection rates; a diagonal actual-path recovery proves the projective epigraph theorem without assuming a Gamma limit.",
    ],
    "A4-history-memory-universal-pressure": [
        "Conditioning is on the genuine symbolic past, equivalently forgetting the future-determining coordinate; the kernel is non-Dirac whenever two successors are allowed.",
        "The dilation acts on V plus the complete unresolved Q-space plus finite Riesz modes.",
        "The Volterra transform is corrected to zI-A-C(z)^{-1} throughout.",
    ],
    "B1-microcanonical-preparation": [
        "A local full-rank patch is used only as a good block; linearly many independent good blocks occur with exponentially high probability, so the complete compound law has the required Fourier decay up to an explicit exponential remainder.",
        "Constraint transversality is stated as a verifiable affine-span condition.",
        "The covariance identity is derived from the full compound-Poisson log moment generating function, not a rare fixed-particle event.",
    ],
    "B2-collision-clusters-dynamic-ldp": [
        "The state is a closed kinetic trace graph satisfying Green, reflection, and contact compatibility.",
        "The frame-reset claim is removed.  A dispersing monotone Jacobi form and its exterior square control the actual physical cocycle after the youngest fork.",
        "One-block source convergence and balance-preserving regularization are proved on the graph state before the LDP lower bound.",
    ],
    "B3-hamilton-boltzmann-cotangents": [
        "The module explicitly records that the raw perspective second variation may be indefinite.",
        "The positive metric is the finite-cumulant covariance inverse, equivalently the KKT Schur completion including constraint-multiplier curvature.",
        "A declared weighted backward energy estimate proves closed range and exact gauge completeness; time tightness uses tree-decaying cumulant densities.",
    ],
    "B4-nonlinear-kinetic-semigroups": [
        "Static preparation is removed from every transition segment, restoring exact dynamic action additivity.",
        "Impossible entire cutoffs are replaced by resolvent-generated smooth graph cylinders.",
        "The new penalty has bounded slope R and forces the diagonal only in the separate limit R to infinity; correctors solve the actual finite generator domain equation.",
    ],
    "C1-information-risk-sensitive-saddles": [
        "The full observation/collision codimension tower is part of the state.",
        "Bayes normalization is replaced by an evidence-lifted unnormalized current, so small coarea denominators become an explicit log-evidence cost rather than a false uniform bound.",
        "Density-state sufficiency is proved only for strategy-reachable prepared beliefs via a uniform posterior cluster estimate.",
    ],
    "C2-cotangent-rigidity-tangent-representations": [
        "Both null spaces are closed linear spans, and the invariant-separator theorem assumes the required uniform Cesaro moment bound.",
        "Kato parallel transport on a common transfer space replaces Radon--Nikodym identification of mutually singular path measures.",
        "The causal memory sign and nonlinear contraction pullback are fully retyped.",
    ],
    "D1-deterministic-theta-contractions": [
        "Zero-free tubes are phase-local and explicitly exclude coexistence; Lee--Yang pinching is allowed.",
        "Phase pressures come from restricted microscopic laws, not branches of one logarithm.",
        "The minimum phase rate is proved from a subexponential microscopic mixture, never from the conjugate of a maximum pressure.",
    ],
}

HISTORICAL_AUDIT = r'''# Round-Eight Historical Derivation Audit

## Scope

The audit covered the canonical recursive/CM2 corpus, the v83 control index, the dependency-closure branch, and the round-three through round-seven derivation packets already present in the repository.  Historical status labels were not treated as theorem credit.

## Control result

The v83 control index itself records that unrestricted universal moving-scatterer closure is refuted, the packetized compiler is proved only as a conditional compiler, and actual admissible classwide closure still requires local packets.  The long canonical volume likewise records local recut, conormal-source, natural-extension, coarea, corrector, and flow-graph mechanisms while repeatedly marking missing physical incidence, common strong-space typing, global minorization, or actual packets.

Therefore no previous document closes the new round-seven referee list as a package.  The following local mechanisms were reusable only after they were restated and proved in the new controlling modules:

| Paper | Reused local mechanism | Missing historical ingredient supplied in round eight |
|---|---|---|
| A1 | moving-cut ledgers and symbolic recut bookkeeping | complete two-sided germ surface, depth-weighted current spectrum, routed global Hamiltonian |
| A2 | periodic-sum separation and physical trace quotient language | renormalized birth topology, finite certificate family, separate geometric UNI, corrected LLT scaling |
| A3 | first-return graph labels and defect-clock bookkeeping | actual edge-flow state and projective epigraph recovery |
| A4 | natural extension, renewal kernels, and Volterra formalism | genuine past orientation, quenched rough kernel, full-Q dilation, correct causal sign |
| B1 | finite-rank coarea patches | regenerative many-block smoothing and typical-sector covariance |
| B2 | Green/trace ledgers and collision-tree expansions | compatible closed trace graph and monotone physical Jacobi exterior estimate |
| B3 | backward kinetic energy templates | KKT Schur completion and covariance-first construction |
| B4 | terminal corrector and generator graph ideas | dynamic-only action, resolvent core, finite-generator corrector, two-scale comparison |
| C1 | coarea posterior formulas | arbitrary-codimension current tower and evidence lift |
| C2 | weighted coboundary and transfer response templates | closed linear spans, uniform Cesaro tightness, Kato bundle without RN densities |
| D1 | finite-projection and contraction bookkeeping | genuine phase-restricted laws and microscopic mixture theorem |

## No downgrade / no no-go substitution

All eleven manuscripts remain active standalone papers.  The new modules replace false intermediate mechanisms while preserving positive final claims in their properly typed state spaces.  D1 is retained as a standalone phase-mixture theorem rather than removed.  The audit does not import the historical `NO-GO_FOR_CLAIM` status as a conclusion about the revised papers; it uses that status only to prevent circular reuse of incomplete old claims.
'''

DEPENDENCY_LEDGER = r'''# Round-Eight Proof Dependency Ledger

The dependency graph is acyclic and is enforced by `tools/verify_round8_referee.py`.

- A1 is independent.
- A2 is independent of A1 at theorem level.
- A3 uses the A2 finite-source coefficient and spectral packet.
- A4 uses A2 for Gibbs/transfer estimates and A3 for the prepared path phase state.
- B2-GC is the hard-sphere dynamic base.
- B1 uses the B2 bounded-source pressure only for dynamic marks; its regenerative coefficient theorem is otherwise independent.
- B2-MC uses B1 after B2-GC.
- B3 uses B2 and B1.
- B4 uses B2 compactness/recovery and B3 covariance typing.
- C1 uses B2, B1, B3, and B4.
- C2 uses A4 for past optional kernels and B3/B4 for kinetic cotangents.
- D1 consumes already-proved phase-specific A2/B1/B2/C1 inputs and never supplies an upstream proof to them.
'''

TOP_RESPONSE = r'''# Response to the Round-Seven Referee Reports

This revision addresses the eleven reports on `review/round7-gpt56-pro-harsh-11paper-2026-08-31`.  Every paper has a new `ROUND8_POSITIVE_CLOSURE.tex` controlling module and a paper-local `AUTHOR_RESPONSE_ROUND8.md`.

The revision is not a prose patch.  It replaces the finite seam object in A1, the ambient birth norm in A2, the profile-only state in A3, the stable-leaf quotient and incomplete dilation in A4, the one-patch smoothing argument in B1, the independent trace pair and coordinate-reset Gramian in B2, the raw-Hessian metric in B3, the impossible analytic cutoff and noncoercive comparison in B4, the uniformly normalized coarea posterior in C1, the non-linear null set and RN Hilbert trivialization in C2, and the common zero-free coexistence atlas in D1.

The repository verification distinguishes internal exact-source and counterexample closure from independent external journal acceptance.
'''

DATA = {
    paper: {
        "code": paper.split("-", 1)[0],
        "source": source,
        "abstract": ABSTRACTS[paper],
        "responses": RESPONSES[paper],
    }
    for paper, (source, _) in MODULES.items()
}


def sha256(data: bytes) -> str:
    return hashlib.sha256(data).hexdigest()


def replace_abstract(text: str, abstract: str) -> str:
    replacement = "\\begin{abstract}\n" + abstract.strip() + "\n\\end{abstract}"
    return re.sub(
        r"\\begin\{abstract\}.*?\\end\{abstract\}",
        lambda _m: replacement,
        text,
        flags=re.DOTALL,
    )


def main() -> None:
    SRC.mkdir(parents=True, exist_ok=True)
    manifest: dict[str, object] = {
        "schema": "theta-theory-round8-materialization-v1",
        "paper_count": len(DATA),
        "papers": {},
    }

    for paper, (source_name, content) in MODULES.items():
        source_path = SRC / source_name
        clean = content.strip() + "\n"
        source_path.write_text(clean, encoding="utf-8")

        paper_dir = PAPERS / paper
        module_path = paper_dir / "ROUND8_POSITIVE_CLOSURE.tex"
        module_path.write_text(HEADER + clean, encoding="utf-8")

        meta = DATA[paper]
        response_lines = [
            f"# Author Response — Round Eight ({meta['code']})",
            "",
            "## Disposition",
            "",
            "All mathematical objections in the round-seven report are answered by a new controlling reconstruction; no theorem is replaced by a no-go statement and the paper remains active.",
            "",
            "## Point-by-point reconstruction",
            "",
        ]
        for i, item in enumerate(meta["responses"], start=1):
            response_lines.append(f"{i}. {item}")
        response_lines += [
            "",
            "## Controlling source",
            "",
            f"`revision/round8-referee-final/{source_name}` is byte-identical, after the generated header, to `ROUND8_POSITIVE_CLOSURE.tex`.",
            "",
            "Compilation and repository-internal counterexample gates are necessary checks; independent external referee validation remains separate.",
            "",
        ]
        response_path = paper_dir / "AUTHOR_RESPONSE_ROUND8.md"
        response_path.write_text("\n".join(response_lines), encoding="utf-8")

        main_path = paper_dir / "main.tex"
        text = main_path.read_text(encoding="utf-8")
        text = replace_abstract(text, str(meta["abstract"]))
        text = re.sub(
            r"\\noindent\\textbf\{Controlling revision:\}\s*\\texttt\{[^}]+\}\.",
            r"\\noindent\\textbf{Controlling revision:} \\texttt{ROUND8-REFEREE-POSITIVE-CLOSURE}.",
            text,
        )
        text = re.sub(
            r"\\input\{ROUND[0-9]+_POSITIVE_CLOSURE\.tex\}",
            r"\\input{ROUND8_POSITIVE_CLOSURE.tex}",
            text,
        )
        text = re.sub(r"\\date\{[^}]*\}", r"\\date{August 31, 2026}", text, count=1)
        text = text.replace(
            "Every load-bearing cross-paper input is typed in the round-three dependency\nledger.",
            "Every load-bearing cross-paper input is typed in the round-eight dependency\nledger.",
        )
        main_path.write_text(text, encoding="utf-8")

        manifest["papers"][paper] = {
            "code": meta["code"],
            "source": str(source_path.relative_to(ROOT)),
            "source_sha256": sha256(clean.encode("utf-8")),
            "module": str(module_path.relative_to(ROOT)),
            "main": str(main_path.relative_to(ROOT)),
            "report": str((paper_dir / "REFEREE_REPORT_ROUND7_GPT56_PRO.md").relative_to(ROOT)),
            "response": str(response_path.relative_to(ROOT)),
        }

    (ROOT / "ROUND8_MATERIALIZATION_MANIFEST.json").write_text(
        json.dumps(manifest, indent=2, sort_keys=True) + "\n",
        encoding="utf-8",
    )
    (ROOT / "ROUND8_HISTORICAL_DERIVATION_AUDIT.md").write_text(
        HISTORICAL_AUDIT.strip() + "\n", encoding="utf-8"
    )
    (ROOT / "ROUND8_PROOF_DEPENDENCY_LEDGER.md").write_text(
        DEPENDENCY_LEDGER.strip() + "\n", encoding="utf-8"
    )
    (ROOT / "REFEREE_ROUND8_RESPONSE.md").write_text(
        TOP_RESPONSE.strip() + "\n", encoding="utf-8"
    )
    print(f"ROUND8_MATERIALIZATION_PASS papers={len(DATA)}")


if __name__ == "__main__":
    main()
