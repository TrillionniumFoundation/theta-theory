# Materialization trigger

This no-op file triggers the fail-closed workflow that reconstructs the SHA-256-pinned eleven-paper source tree and embeds the eleven referee reports on a dedicated materialized review branch.
