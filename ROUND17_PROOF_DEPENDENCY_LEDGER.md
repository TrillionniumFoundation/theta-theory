# Round-Seventeen proof-dependency ledger

**Referee tree:** `d3833aaa2db4bf0a13a67083f27ff23f28ab6e4f`  
**Policy:** positive reconstruction only; no deleted paper, no no-go replacement, no silent weakening, no theorem credit for unmaterialized payloads or certificates.

## Dependency DAG

```text
A1 independent

A2 raw vector-return-roof Fourier theorem
  -> A3 entropy-controlled deterministic-clock stopped LDP
  -> A4 global past kernel / Doob / forced memory
  -> C2 strict/form response
  -> D1 typed phase contraction

B2-GC precontact atlas / marked pressure / GC joint LDP
  -> B1 direct canonical coefficient / shell conditioning
  -> B2-MC joint LDP
  -> B3 observability / joint process CLT / Mosco tangent
  -> B4 Nisio resolvent / nonlinear semigroup
  -> C1 regular belief filter / QMD
  -> C2 typed response
  -> D1 latent-phase contraction
```

## Paper gates

| Paper | Gate required before downstream use |
|---|---|
| A1 | one global finite-dimensional autonomous hybrid realization; fixed conormal current scale; closed all-order jet chain rule; explicit response words; Radon Hilbert-valued FCLT |
| A2 | common induced branch bundle; lattice rank; returned UNI; branchwise physical IBP; integrable unsmoothed vertical tail; raw 4D density LLT |
| A3 | exact predictable entropy representation; explicit stopped-state map; compact rate sublevels; legal finite-memory relaxation; full branch/prefix terminal contraction; collision and physical clock lower bounds |
| A4 | one unchanged global kernel; derived weak-Harris contraction and drift; fixed multiplier algebra; unsmoothed renewal resolvent; common-domain forced Mori--Zwanzig identity |
| B2-GC | compatible Green trace; genuine open precontact map; genealogywise coarea; factorial domination; positive exposed-pair recovery; projective GC lower bound |
| B1 | direct positive exact-N saddle; interacting canonical Fourier majorant; nondegenerate covariance; geometric shell coefficient; source-dependent conditioning |
| B2-MC | transfer of the GC joint rate only after B1; no upstream use of the MC theorem |
| B3 | finite-time closed range; localized cumulants; nuclear process tightness; correctly normalized joint Gaussian; independent quadratic recovery and Mosco limit |
| B4 | law--hierarchy realizability; compact dynamic action; state-dependent multiplier transfer; Nisio resolvent identity; m-dissipativity; one diagonal graph-core corrector; nonlinear Trotter--Kato |
| C1 | model-derived compact regular filter chart; integrated dominated disintegration; exact belief Markov kernel; measurable selectors; exact-experiment QMD/LAN |
| C2 | exact weighted strict dual; fixed-reference metric connection; operator-core/form-level compression; dissipative invertibility; model-specific rigidity; changing-filtration optional projection |
| D1 | genuine positive latent label; joint labelled LDP; exact sign; adapted posterior semigroup; complex dominance; second-order tie weights; typed contraction |

## Fail-closed publication rules

1. Every paper's `main.tex` must load exactly one `ROUND17_POSITIVE_CLOSURE.tex` and declare `ROUND17-REFEREE-POSITIVE-CLOSURE`.
2. Each active module must be byte-identical to its registered source in `revision/round17-referee-final/`.
3. Every theorem-like environment in the controlling module must have a proof; all local references must resolve.
4. Each of the sixty-three major Round-Sixteen objections must be listed in a paper-local author response and mapped to one or more Round-Seventeen labels.
5. The hostile verifier must replay the decisive counterexamples: A1 distributional-jet typing; A2 trace-class fallacy/raw-tail gap; A3 terminal entropy; A4 patched-kernel consistency; B1 interacting canonical dependence; B2 positive-cone feasibility; B3 Gaussian normalization/Mosco; B4 state-dependent admissibility/resolvent range; C1 oscillatory disintegration; C2 singular compression; D1 marginal log-sum non-semigroup.
6. All eleven papers must clean-build with zero undefined references or citations and nonempty PDFs.
7. The publication commit may differ from the verified mathematical commit only by the final certificate, publication record, and revision status.
8. The pre-publication `main` must be archived, the revision ref and `main` must point to the final publication commit, and the annotated tag must dereference to that commit.

Compilation is necessary but is not treated as independent external mathematical certification.
