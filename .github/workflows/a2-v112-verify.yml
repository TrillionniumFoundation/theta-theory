name: A2 v112 exact-source five-volume review
on:
  push:
    branches: [revision/a2-v112-components-schemes-quantitative-experiments-2026-09-21]
    paths:
      - 'papers/A2-v17-boundary-information-coarsening/article/v112/**.tex'
      - 'papers/A2-v17-boundary-information-coarsening/article/v112/parts/**'
      - 'papers/A2-v17-boundary-information-coarsening/article/v112/**.py'
      - 'papers/A2-v17-boundary-information-coarsening/article/v112/SOURCE_MANIFEST.json'
      - '.github/workflows/a2-v112-verify.yml'
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: a2-v112-review-${{ github.ref }}
  cancel-in-progress: false
jobs:
  verify:
    if: github.ref == 'refs/heads/revision/a2-v112-components-schemes-quantitative-experiments-2026-09-21'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 1
      - name: Fetch immutable review base
        run: git fetch --no-tags --depth=1 origin cafc6c1bd403dae4acc66177fb43feee54c02b1d
      - name: Install build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y latexmk texlive-latex-extra texlive-fonts-recommended texlive-science cm-super poppler-utils python3-sympy
      - name: Preservation, exact diagnostics and five-volume build
        run: /usr/bin/python3 papers/A2-v17-boundary-information-coarsening/article/v112/build_review.py
      - name: Upload source-bound evidence and PDFs
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: a2-v112-review-${{ github.sha }}
          retention-days: 90
          path: |
            papers/A2-v17-boundary-information-coarsening/article/v112/paper.pdf
            papers/A2-v17-boundary-information-coarsening/article/v112/evidence/
            papers/A2-v17-boundary-information-coarsening/article/v112/**/*.tex
            papers/A2-v17-boundary-information-coarsening/article/v112/*.py
            papers/A2-v17-boundary-information-coarsening/article/v112/*.md
            papers/A2-v17-boundary-information-coarsening/article/v112/SOURCE_MANIFEST.json
            papers/A2-v17-boundary-information-coarsening/article/v111/**/*.tex
            papers/A2-v17-boundary-information-coarsening/article/v111/*.py
            build-v112/
      - name: Publish evidence only to this revision branch
        env:
          TARGET_BRANCH: revision/a2-v112-components-schemes-quantitative-experiments-2026-09-21
        run: |
          set -euo pipefail
          test "$GITHUB_REF_NAME" = "$TARGET_BRANCH"
          remote_sha=$(git ls-remote origin "refs/heads/$TARGET_BRANCH" | cut -f1)
          test "$remote_sha" = "$GITHUB_SHA"
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          p=papers/A2-v17-boundary-information-coarsening/article/v112
          git add -f "$p/paper.pdf" "$p/evidence/ci-receipt.json" "$p/evidence/diagnostics.json" "$p/evidence/principal.log"
          git diff --cached --quiet && exit 0
          git commit -m "A2 v112: bind five-volume build and diagnostics to $GITHUB_SHA [skip ci]"
          git push origin "HEAD:refs/heads/$TARGET_BRANCH"
