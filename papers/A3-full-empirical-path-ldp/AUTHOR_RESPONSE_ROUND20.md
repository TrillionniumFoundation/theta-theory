# A3 Author Response — Round Twenty

The referee correctly rejected the former one-step countable edge chain as an
unproved surrogate for the Sinai billiard.  The active source now starts from
the exact regular conditional law of the induced billiard.  Its Markov state
contains the complete admissible past, the current oriented stable germ, and
the continuous unstable coordinate.  Every transition carries the actual
return count, roof, homology, complete excursion path, and singularity ledger.
This is the same history process later used by A4.

A model-derived exponential coercive cost is proved from the A2 return and
homogeneity tails.  It yields a Polish stopped/recession state whose recession
points are, by construction, limits of legal physical excursions.  The
stopped entropy representation treats the final continuous prefix by the
chain rule for relative entropy of its conditional prefix kernel; it does not
use negative logarithm of a zero-probability path point.

The revision proves entropy-controlled compactness with the random return
count, gives explicit collision-clock and physical-clock rates, and constructs
legal finite-memory recovery kernels using actual branch cells and bounded
regular connectors.  The retained terminal prefix makes the generalized clock
inverse continuous on rate sublevels.  Finally, the inserted A2 local theorem
is used in numerator and denominator to prove the conditional Laplace
principle in every declared window regime.

The full positive path-LDP package remains the target.  No abstract no-go,
one-step Markov assumption, or weaker topology is substituted for the claimed
Sinai process.
