# Frozen derivation and preservation audit

The controlling r20 report is frozen at `6da551c5b804b683240d5f5631e1c4f5689e21bd`; the reviewed v35 publication is `aabc0731a0c913f7b9e75e6efc08f7af665bdf23`. The new work branch was created remotely from that report before publication.

The current report was read in full, including its thirty specific points. The current Round-Seventeen proof-dependency ledger was read. Relevant v35 packet, rotation, compact-group, actual-error, model and literature modules were checked, together with the preceding v32–v34 lift/Fourier/resonant context carried by that source chain. The v24–v35 programs are retained for regression. This does not claim that all thousand pages of cumulative development were re-proved.

The mathematical changes are a full-word packet proof, a finite spectral-gap alphabet theorem with sparse spherical synthesis, an explicit rational five-gate/qubit instance, a matched resonant monomial class, and a charged sparse-row finite-coin compiler. The new theorem statements retain old binary and compact-input results in their original scopes. A word/product implication is clarified by a current corrected lemma rather than silently rewriting an old published PDF.

`PREDECESSOR_MANIFEST.json` binds the predecessor's native sources and PDFs. `supporting-results.pdf` is an unchanged copy of the v35 article. The two complete volumes append the unchanged predecessor cumulative volume after the current paper and a divider. Source files in prior packages are never edited. The compact referee package excludes all cumulative PDFs.

The A2→A3→A4→C2→D1 chain and the B2/B1/B3/B4/C1/C2/D1 chain keep their own geometric, Fourier/LLT, LDP, operator-domain, semigroup and filtering gates. No historical paper is assigned a false new dependency, no aggregate closure is inferred, and `main` is not a publication destination for this request.

Source hashes, regression results and PDF preservation are delivery evidence. They are not independent proof certification or an editorial assessment.
