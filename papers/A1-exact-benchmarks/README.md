# Deterministic Path Ensembles, Driven Collision Maps, and Endogenous Conjugate Parameters

This folder is a standalone `amsart` external-review project.

Controlling sources:

- `main.tex`
- `references.bib`

Build from this directory with `latexmk -pdf main.tex`.

Mathematical status: internally closed in the declared deterministic path-ensemble scope; independent line-by-line review is pending.

## External-review status

Research draft; independent mathematical review pending.

## Second referee-round revision

The controlling `main.tex` includes `REFEREE_REVISION_ADDENDUM.tex`. The addendum responds theorem-by-theorem to both independent reports. The report files remain unchanged as review provenance.
