# B2 Author Response — Round Twenty

The revision fixes the direct factorial contradiction.  A connected
`k`-root coefficient is defined from the outset with the exponential-generating
symmetry factor `1/k!`.  Removing the last creation edge yields a Catalan
recursion for the normalized coefficients and a geometric short-time
majorant.  No `k!` is dropped after an estimate.  Finite-time results follow by
positive-kernel concatenation, with the density and incoming trace retained at
slice boundaries.

The boundary analysis now contains uniform flux estimates excluding grazing
and multiple-contact concentration, followed by a closed Green--specular graph
theorem.  For each fixed genealogy, cutoff, and chronology separation, the
precontact zero map is treated on a compact semianalytic parameter set and
covered by a finite analytic rank stratification.  Coarea is applied before
the true reflection; the reflected future is then retained through its
flux-Jacobian identity.

The positive lower recovery respects the nonlinear reference
`A_f = ff_*B/2`: smooth relative multipliers are inserted into the controlled
Boltzmann equation, which generates a positive exactly balanced density and
contact current.  Conservation and endpoint corrections are carried by
collision-invariant coordinates.  At finite volume the likelihood is the
exact exponential tilt of the random initial configuration.  The Poisson-type
perspective entropy appears only as the convex dual of the limiting collision
Hamiltonian.

These ingredients prove the source-uniform marked grand-canonical pressure,
full exponential tightness and rate identification, the grand-canonical LDP,
and--only after B1 coefficient extraction--the microcanonical LDP.  The
grand-canonical root does not import B1, so the dependency is noncircular.
