# Author Response — Round Eight (B2)

## Disposition

All mathematical objections in the round-seven report are answered by a new controlling reconstruction; no theorem is replaced by a no-go statement and the paper remains active.

## Point-by-point reconstruction

1. The state is a closed kinetic trace graph satisfying Green, reflection, and contact compatibility.
2. The frame-reset claim is removed.  A dispersing monotone Jacobi form and its exterior square control the actual physical cocycle after the youngest fork.
3. One-block source convergence and balance-preserving regularization are proved on the graph state before the LDP lower bound.

## Controlling source

`revision/round8-referee-final/B2_COMPATIBLE_TRACE_MONOTONE_JACOBI.tex` is byte-identical, after the generated header, to `ROUND8_POSITIVE_CLOSURE.tex`.

Compilation and repository-internal counterexample gates are necessary checks; independent external referee validation remains separate.
