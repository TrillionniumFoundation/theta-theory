# History and preservation audit — v41

The controlling r25 report was read in full at `9707844addcd9eb478eb863514f288923f9d40fd`. The r24 report on the same v39 head was consulted for its matching intrinsic-gap objections. The reviewed article is v39 at `28890c62dd69f217bf2f1c205ff9542eb537c2ef`. Its actual native sources and remote artifact package were used, not guessed from the previous chat summary.

The frozen `ROUND17_PROOF_DEPENDENCY_LEDGER.md` was read at the r25 commit. The two analytic chains retain their Fourier/local-limit, stopped-LDP, global-kernel, nonlinear-semigroup, graph-core, filtering and optional-projection gates. This work does not claim a line-by-line re-proof of the 1067-page archive or independent closure of those gates.

The v39 machine, orbit chart, entropy, orbit compiler, reducible, matrix, semantics and arithmetic sources were examined as needed for this revision. The prior exact entropy and orbit proofs, matrix/projective results and local arithmetic normalization are retained. `machine-model.tex` gains only the effective-action clarification; the old reducible theorem retains its proof and sufficient range, with the former main-theorem cross-reference redirected; numerical semantics gains separate v1/v2 locators. The introduction, abstract and conclusion describe the strengthened theorem rather than erase the old claims.

The entire v39 article is copied byte-for-byte as `supporting-results.pdf`. Each cumulative archive follows the complete current article and a divider, then includes the unaltered v39 cumulative volume. Source hashes and all predecessor page texts are checked; selected predecessor pages are also compared as raster images.

Two previously existing v40 work branches are left untouched. The v41 remote branch was created from r25; `intrinsic-action.tex` was pushed first as a substantive source commit. All final changes are confined to the new v41 package, its branch-specific workflow and its root entry. No main branch, old review, manuscript path or other paper is mutated. The compact referee package excludes the large cumulative archives.
