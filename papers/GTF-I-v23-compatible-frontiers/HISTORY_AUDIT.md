# History and dependency audit — v23

## Frozen sources

The controlling r6 report was read in full at
`37ff0991b2764ce2fba14541c6191b26661e000e`. Its parent is the actual
v22 referee-ready head `88832f95b7dd0b8cc4a3061280a6e31f9fa67319`.
The native GitHub build artifact from run 35953073882 was downloaded;
selected main/front-matter/multicut/metadata git-blob hashes were checked
against the frozen repository sources. The v22 mathematical modules,
response, resource/pipeline/proof records and their preserved historical
graph were used as the baseline, not substituted by an older A1 draft.

The blueprint `foundations/general-theta/General_Theta_Foundations_v0.1.md`
was read through its initial typed objects and A0–A5 experimental/resource
axioms. The inherited program and dependency appendices were inspected.
The eleven historical components and their namespaced graph are preserved
in `HISTORICAL_PIPELINE_V20.json`, unchanged from v22.

The original B4 `papers/B4-nonlinear-kinetic-semigroups/ROUND17_POSITIVE_CLOSURE.tex`
was directly revisited through its control transfer, action compactness,
normalized resolvent and range assertions at
`c04845b6613208406703695c9c184ae461f95805`, blob
`1e6c5a870ca1ecf3319c1f1d950a1f0f1564dfb4`.
The normalized linear difference identity is not valid on nonzero
constants. The new occupation result does not prove the missing kinetic
control-transfer/range/compactness/corrector assertions.

The original C2 `papers/C2-cotangent-rigidity-tangent-representations/ROUND17_POSITIVE_CLOSURE.tex`
was directly revisited through its strict dual, transported form response,
compressed memory and rigidity setup at the same historical commit, blob
`6fa8639339ebae9a19122cbdba339ec6f275b562`.
The finite confidence and retained-kernel results do not certify this
broad operator/rigidity/optional-projection aggregate.

## Actual new proof dependencies

Compatible occupation conservation and common-row equations give the
exact controller class. Classical positivity gives its complete converse.
The noisy Bayesian interval theorem uses one common continuation matrix;
the minimax receiver recursion instead uses attainable binary channels and
parameter-independent garblings. The three-report all-stochastic theorem
has its own domination, equal-error and factorized-gap proof. Its lower
bound is not inferred by purification of a minimax objective.

The exact confidence consumer uses the inherited reset-to-Bernoulli mean
transformation, then applies the noisy theorem to a declared coin
interface and proves monotonicity for its composite alternative. The
physical reset consumer maps the original finite bit-level execution into
the exact occupation class. It does not use erasure counting as a physical
lower bound. Calibration uses actual independent target draws and retains
all counters through the auditor.

## Preservation and scope

Every previously included mathematical module remains included verbatim
in the canonical article. Changed v22 front matter, introduction, input
file, preamble and bibliography are archived verbatim under `history/v22`.
The v22 organizing theorem and proof are reproduced verbatim in the
appendix. Older organizing theorems and all old bibliography entries
remain. The complete 553-page predecessor is appended without altered
text or raster rendering; the executed receipt records the check.

This is a source/dependency audit and a revision of the stated theorem
chain. It is not a fresh independent proof certification of every page
in every historical branch. A2 remains an independent primary geometric
chain. B4 and broad C2 targets retain their original scope. No predecessor
repository path or other manuscript branch is modified or deleted.
