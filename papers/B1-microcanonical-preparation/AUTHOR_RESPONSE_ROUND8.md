# Author Response — Round Eight (B1)

## Disposition

All mathematical objections in the round-seven report are answered by a new controlling reconstruction; no theorem is replaced by a no-go statement and the paper remains active.

## Point-by-point reconstruction

1. A local full-rank patch is used only as a good block; linearly many independent good blocks occur with exponentially high probability, so the complete compound law has the required Fourier decay up to an explicit exponential remainder.
2. Constraint transversality is stated as a verifiable affine-span condition.
3. The covariance identity is derived from the full compound-Poisson log moment generating function, not a rare fixed-particle event.

## Controlling source

`revision/round8-referee-final/B1_REGENERATIVE_COMPOUND_SMOOTHING.tex` is byte-identical, after the generated header, to `ROUND8_POSITIVE_CLOSURE.tex`.

Compilation and repository-internal counterexample gates are necessary checks; independent external referee validation remains separate.
