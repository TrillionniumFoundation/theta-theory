# Resource ledger — revision 25

| Result | Preparations | Retained state | Randomness and timing |
|---|---|---|---|
| Controlled dual | At most N candidate training, then two validations | Equality has no cap; certificates upper-bound every declared finite-memory architecture | A mixed tree or history is not free at fixed width; one parameter and one prior, not an adaptive adversary |
| Exact marked U1 | One candidate training, two fresh validations | Three report-count values during training; the selected event and validation buffers are subsequently charged | One stochastic choice between specified event masks; the same mask is retained for both validations; dyadic approximation loses at most 2^-b |
| Physical two-preparation audit | Two candidate training, one candidate validation, one target validation | Full bit/mark profile (1,2,3,3,4,5,5,10,12,10,10,7,3); five event labels, peak twelve | Deterministic, no calibration or random bits; autonomous phase-tagging has the retained 75-state upper bound |
| Revelation joint law | At most N training and two validations | K training/decision states; atomic-outcome full peak at most max(2K,3) | k=log2 K fair initialization bits stored in the register; no extra revealed/unrevealed flag; declared clocked schedule |
| Finite-architecture certificates | H controller row uses in specified architecture | Original specified row/register profile | Reused rows share a single variable; dyadic precision and bit-level implementation distinguished |
| Autonomous binary comparison | n independent report observations | W atomic stochastic states; conservative fair-bit implementation 4W(b+2) | Classical stationary bound; finite-time upper construction only |

The exact marked stochastic optimum is not claimed to have the deterministic two-preparation machine's peak. The revelation decision width K is not the collision bit-level peak W. Its alphabet is atomic, and a bit-serial implementation would require additional explicitly counted cuts. A retained selector, phase, calibration table or random prefix is never exempted from the stated memory convention.
