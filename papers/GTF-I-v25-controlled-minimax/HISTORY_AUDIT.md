# Historical derivation and provenance audit — revision 25

The controlling r8 report is frozen at `c5c1a0d64f830bc3f036292357d8bbe2dbd04ca6`; its mathematical predecessor is v24 at `ad1336e48e414b2c01e928b844c796ab4765178b`. The new branch descends from that report and preserves its full repository tree.

The current audit traced the module inventory and dependency status and re-read the source-level arguments needed for the new theorem chain: the v24 posterior-predictive dual, dominating-row proof, rational certificate, continuum selector minimization, residual-state compilation, subdivision certificate, autonomous comparison, and resource/pipeline records; the v23 `physical-complexity.tex`, `microscopic-composition.tex` and `collision-resource-gap.tex` arguments supplying the actual marked product family, feedback completion, positive-volume collision boxes and sensor-noise estimates; and the preserved main module inventory. It does not purport to independently re-prove every page of the 664-page development.

The historical general route remains actual preparation and positive kernels → likelihood and posterior continuation → executable validation-test quotient → compatible retained state → charged resource comparison. The new controlled theorem keeps informative action selection in this route rather than eliminating it by a dominating row. The exact marked-family theorem extracts a saddle point and equality certificate from the same posterior dual. The physical bridge now proves its required interface facts locally, and the existing compatible residual machine supplies the two-preparation construction.

The predecessor's continuation, decision-continuations, weighted/joint residual, reset-audit, acquisition, finite-confidence, Gaussian/entropic and operator-consumer developments are retained through the unchanged repository paths and full volumes. In particular, the old minimum decision alphabet, longer three-event constructions, physical transport claims and majority-width results are not recast as new v25 theorems. The original v24 rational prior remains in the current article and is no longer mistaken for the exact optimizer.

A2 remains an independent geometric chain. The historical B4 normalized-resolvent/range/compactness/control-transfer obligations and broad C2 form/strict-dual/rigidity/optional-projection obligations are not closed by finite statistical duality. The pipeline is expanded, not declared universally closed. The new joint revelation law is a separate precisely typed example, not a replacement for those obligations or for the collision peak-width problem.

The build pins the v24 `main.tex` Git blob to `8574b9769a88ef71d3494b2d310f699cb3515f84`, records its source hash and exact assembly strategy, and compares every appended predecessor PDF page for unchanged text and raster samples. The workflow checks for no modified/deleted/renamed predecessor paths. These are preservation checks, not historical proof certification.
