# Historical derivation audit — revision 32

The controlling report is r16 at `0e69fdcfc11067a7de9a6122fc603f6a5e2253d8`, reviewing v31 publication `90dc2c8f0c75e2cfbb95cc61b247ce4d5e216eb9`. The new remote branch was created from that exact review before publication.

The complete r16 report was read. The v31 canonical affine-section proof, full two-cut incompatibility argument, additive-synthesis proof, resource/implementation conventions, references and pipeline/history records were inspected. The v29 intrinsic-continuation and streaming-access developments and the intervening source/receipt chain were used to distinguish static rank, normalized continuation generators and compatible rows. The frozen `ROUND17_PROOF_DEPENDENCY_LEDGER.md` was re-read at the controlling review commit.

The historical route remains actual complete experiments -> future-test continuations -> positive state realization -> charged profiles. The new argument starts from actual reachable *state distributions*, takes their affine section of the hidden simplex, and projects to observable means. This avoids the invalid assumption that each hidden unit state already lies in the minimal observable realization. Classical accessible-quotient precedents were also read directly, rather than treating the repository alone as authority.

The earlier two-cut primitive is reproduced with its entire lower and upper proof in the current appendix. Its only textual adjustments identify provenance, resolve local labels and remove a reference to a section not in the focused article. The v31 published source and PDF are not edited. The v31 additive sampler receives the requested explicit clarification that a zero-prefix dummy uses an existing label; no hidden new state is claimed.

The full previous article is retained byte-identically as supporting-results.pdf, and both cumulative predecessor volumes are appended unchanged. The new build checks every predecessor native source/PDF hash in the manifest. This is preservation evidence, not a re-certification of every historical argument.

The historical A2 -> A3 -> A4 -> C2 -> D1 and hard-sphere B2/B1/B3/B4/C1/C2/D1 chains keep their Fourier/LLT, stopped-LDP, nonlinear semigroup, graph-core, filtering and optional-projection requirements. No aggregate closure or indispensable external analytic consumer is fabricated. The present work is a new arbitrary-horizon finite-memory theorem, with actual proof dependencies inside its own focused article.
