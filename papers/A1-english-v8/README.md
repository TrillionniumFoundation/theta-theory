# A1 — English revision 8

**Sparse observation algebras, attainable resolution flags, and finite-state memory**  
Author: Qian Qi. Date: 6 September 2026.

This is the complete revised principal manuscript, not a replacement abstract or a proof sketch. Read `main.pdf` (generated by the publication build) or compile `main.tex`. The controlling review is `reviews/a1-english-v7-2026-09-06/REFEREE_REPORT.md` at commit `72eb41e358bd9af122367fea66d0de9bdda07456`; the reviewed v7 submission is `02f68484cf92ef312037cf455bd3f3737ae4facd`.

The revision branch is `revision/a1-english-v8-attainable-filtration-2026-09-06`. All pre-existing source, review and historical files are inherited unchanged. The 25 v7 result labels and their proofs remain in the principal text; nine new result labels provide the attainable-flag extension, its global covering input, uniform fixed-horizon streaming realization, seven-trial consequence, bit profile, and necessary decision budget.

## Mathematical entry point

Section `sec:attainable-flag` removes the full-future restriction by truncating the ordered future collision orders at the attainable past capacity. It proves both sides: transversality and unconditional minorization on each selected flag, and a bounded-format covering estimate on the entire curved reachable image. Raw moment updates then supply uniform causal compatibility at any fixed horizon in the affine class. The seven-trial law is a consequence of this argument, not a nine-dimensional extrapolation of the ambient future spectrum.

The decision comparison is retained as an operational consequence of the same geometry, not an independent optimal-exploration theory. Classical clustered Vandermonde scales, real integral geometry, and semialgebraic component bounds are explicitly attributed.

## Reproduce

From the repository root run `python3 scripts/materialize_a1_v8.py`, then `bash papers/A1-english-v8/build.sh`. Requirements are Python 3 with SymPy and NumPy, and pdfLaTeX with AMS packages, Latin Modern and microtype. The materializer copies the pinned predecessor into this new directory and applies the revision; it never modifies v7 or an earlier file. It is deterministic and idempotent on manuscript source.

`tests/test_v7.py` is the unchanged inherited suite, freshly rerun here. `tests/test_v8.py` contains the new exact flag-rank, normalization, seven-trial profile and raw-update fixtures. The new finite-input transducers retain one index only; their test harness separately keeps exact histories as a reference. These finite tests do not prove a continuum entropy theorem, arbitrary-prior result, or continuous-input optimal codebook existence.

`RESPONSE_TO_REFEREE.md` addresses every E/P item. `PROOF_LEDGER.md` isolates hypotheses and classical dependencies. `HISTORICAL_DERIVATION_MAP.md` specifies the actual historical sources consulted. `REFERENCE_AUDIT.md` records the theorem-level comparisons. The publication build produces `SOURCE_MANIFEST.json`, `BUILD_REPORT.json`, `DIAGNOSTICS.json` and `V8_DIAGNOSTICS.json` from its actual execution. No test count, page count or author response constitutes referee approval or a journal decision.
