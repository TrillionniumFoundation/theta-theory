# Common deterministic reference platform — corrected final control note

**Controlling revision:** `theta-referee-revision-v3-positive-closure-2026-08-28`  
**Controlling Paper-I file:** `paper-I-bilateral-response/main-submission.tex`

## Moving-cut family

For `a in [-1/40,1/40]`, set

\[
(w_1,w_2,w_3,w_4)
=(1/10+a,\ 1/5+2a,\ 3/10-a,\ 2/5-2a)
\]

and let `T_a` be the full-cover affine branch map with these widths.  The three
internal cuts move with velocities `1,3,2`.  A deterministic pinball collision
law on a cylinder has `T_a` as its bottom return map.

## Correct nonconjugacy proof

Only branches two and three contain regular interior fixed points.  Their
multipliers are `w_2(a)^(-1)` and `w_3(a)^(-1)`, with

\[
w_2(I)=[3/20,1/4],
\qquad
w_3(I)=[11/40,13/40].
\]

The intervals are disjoint.  A `C^1` conjugacy preserves regular periodic
multipliers and cannot interchange the two families.  Equality of the second
branch multiplier gives `w_2(a)=w_2(b)` and hence `a=b`.

The earlier draft wording referring to three regular interior fixed points is
superseded by this corrected two-orbit proof.

## Exact innovation theorem

If the parameter at step `k` is measurable with respect to the preceding
branch symbols and the initial fast coordinate is Lebesgue distributed, then:

1. the current fast coordinate is conditionally Lebesgue;
2. the next branch has conditional probabilities `w_i(a_k)`;
3. after applying the selected affine branch, the new coordinate is Lebesgue
   and independent of the enlarged past.

Thus centered branch increments form an exact predictable martingale array
generated by the deterministic nonautonomous map.

## Paper bindings

```text
Paper I
  moving cuts + nonconjugacy + all finite graded response + exact innovations

Paper II
  explicit pressure/root/covariance + Diophantine all-frequency response

Paper III
  predictable geometric rough lift + nonautonomous diffusion + entropic HJB

Paper IV
  noncompact refresh filtering + curvature-compensated pure Isaacs game

Paper V
  analytic terminal tangent laws + true Girsanov + BSDE/PPDE/2BSDE
```

The five papers use the same moving-cut branch process for their collision,
pressure, martingale, HJB, and game layers.  The noncompact hidden component in
Paper IV is a deterministic product extension of this platform.

## Review boundary

This note identifies the controlling construction and corrects an internal
orbit-counting error.  External referees have not yet reviewed the revised
proofs.
