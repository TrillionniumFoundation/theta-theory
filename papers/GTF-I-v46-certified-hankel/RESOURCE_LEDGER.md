# Resource ledger — revision 46

The primary optimization is over horizon-specific stochastic initialization, command rows and bounded terminal mean columns. All available labels at each command boundary count, including padded unreachable labels. The held binary answer uses two labels. No command history, seed or private randomness persists except in the label. The epoch, known horizon, nonuniform table, real arithmetic and atomic row draw are free in this particular model.

The all-word checker takes the complete proposed stochastic machine, rational target and displayed horizon. It counts arithmetic operations and, for rational data, exact bit complexity of the finite matrix computation. It verifies a given candidate rather than synthesizing a smallest one. The symbolic existence formula has real variables and finite polynomial constraints; it is not a finite-bit realization of their algebraic solution.

The symmetric-power recurrence is polynomial in its degree only at fixed augmented dimension. The globally optimized moment value is not computed by local numerical minimization. The exhaustive one-cut grids in the evidence are explicitly distinguished from a generic global optimizer. Dyadic approximation preserves boundary labels but may require a table and scratch registers. The inherited uniform finite-bit compiler charges these costs in its separate model.

The ETR-completeness comparison allows one categorical terminal output and counts pre-output memory. Its output alphabet may grow. It does not prove hardness for fixed planar binary-coordinate experiments. The exact frontier example counts its output cut separately.
