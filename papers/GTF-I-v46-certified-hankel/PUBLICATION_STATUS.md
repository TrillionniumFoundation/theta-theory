# Publication status — source-review release

This revision's complete LaTeX input graph and author response are published as readable files. The mathematical source is available for referee scrutiny. The branch is not labelled as a remotely verified binary release.

## Executed local evidence

After the local verifier input parser was changed from symbolic parsing to rational-literal parsing, the **full local build was run again successfully**. It produced a 26-page article, ordinary/optimized agreement, sixteen negative-control executions, twenty-six predecessor source checks, 1,765 preserved-page text comparisons and twelve sampled predecessor raster comparisons. The final core source ZIP was then extracted in an isolated directory and rebuilt: every page's text and raster agreed with the full-build article. See `evidence/LOCAL_VALIDATION_SUMMARY.json` for the executed values and artifact hashes.

The full local receipt identifies `local-uncommitted`, not a fabricated GitHub source binding. The readable mathematical manuscript is committed at `1fa2327b39c7fd6fb220d27250a20aac26d37e4e`; the complete local program package is not represented as an identical fully published native GitHub package. The local PDFs, source ZIPs and complete executed local receipt are supplied separately in the conversation.

## Remote status at this release

The source transport and branch-specific workflow were pushed before direct native-source publication. Workflow `36260243370`, triggered by `d158ff0f5f45bcbad96a2fadd8b9d0848a892638`, was still queued at the last read and had executed no steps. There is no remote success receipt. Additional native sources advance the branch beyond that trigger; ordinary non-force push checks prevent an older run from silently overwriting this source revision.

The tool blocked the direct native `certify.py` write. It was not retried through a different tool, encoding or executable indirection. Prior already-committed transport is preserved as provenance, not treated as a completed native verifier release. New `certify.py`, `verify.py` and `build.py` are not claimed to be readable native files in this source-review commit. They are included in the separately delivered local package.

The planned `v46-referee-ready` automated release is distinct from the manually published `v46-source-review` branch. A future Actions success is not assumed. Neither local tests nor source-review status are independent mathematical certification, priority clearance or a journal decision.
