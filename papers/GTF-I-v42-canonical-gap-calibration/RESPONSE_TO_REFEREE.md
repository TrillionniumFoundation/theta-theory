# Response to the independent twenty-seventh referee report

**General Theta Foundations I — Revision 42**  
**Alphabet Dynamics and Calibrated Numerical Memory**  
26 September 2026

Controlling report: `reviews/general-theta-foundations-i-v41-intrinsic-action-independent-harsh-top4-r27-2026-09-26/REFEREE_REPORT.md`, frozen at `fdc5acf7e7913dcfc379f5f19d5c97ab21306496`. Reviewed manuscript: v41 publication `08773b2705c5ac92716df5daf42a54f3b61bca34`, native source `282bc34bbbeaa56a2ec36c6daf438e545dcfeb37`. The existing first r26 review of that manuscript is left untouched. The new work branch was created remotely from r27 before the current source was published. The other v42 work branch is not overwritten.

The report accepts the principal v41 proofs but identifies the proof-side law, dominant-nongapped actions and opaque query calibration as the main mathematical limitations. The revision addresses them with three distinct results: canonical finite-block expansion, a matched hierarchy outside every finite-block gap condition, and finite calibration with exact and convex certificates. It does not present an optimized probability vector alone as a new width classification.

The stable labels below are resolved to actual numbers and pages in `evidence/THEOREM_LOCATIONS.json`. Proofs are in the article. Finite checks are not substitutes for them.

## 12.2 — Canonicalize the command-law certificate

**Addressed in `prop:law`, `prop:gap-dual` and `thm:block-gap`.**

The alphabet invariant is `g1* = max_p (1-||sum p(a)V_a||^2)` on the complement of the actual action invariants. Its optimum is attained. The gap is concave and 2-Lipschitz in the probability vector. Full-support laws have the same supremum, although a full-support optimizer and uniqueness are not asserted. Domination `q>=tau p` gives `g(q)>=tau g(p)`; consequently a uniform law has at least `g1*/|A|`, and every full-support law is positive exactly when the optimized gap is positive. Finitely many expanding constituents admit one common full-support law.

The article also supplies a Gram minimax dual and convergent finite harmonic SDPs. The truncations give decreasing upper bounds on the full optimized gap. Without a tail bound they are not positivity certificates. The dual's compact finite-dimensional Gram body still encodes infinitely many harmonic directions; it is not presented as an effective general gap oracle.

More importantly, `gL*` optimizes over actual length-L word laws. Theorem `thm:block-gap` proves positivity at some finite L equivalent to spectral radius less than one for the uniform one-step action average. Uniform word laws dominate every other word law, and the spectral-radius formula handles nonnormal averages. The same equivalence holds for every fixed full-support letter law. Common lengths and laws work across finitely many constituents. A word block is L paid ordinary inputs, never a free enlarged input symbol.

`thm:dominant-block` applies the inherited entropy occupation proof at independent block boundaries and obtains the matched dominant-type width under this strictly broader condition. The current paper reproduces all required inherited analytic proofs. No gap on unrelated constituents is added.

## A new distinction: one-step zero does not mean all-block zero

**Theorem `thm:binary-example` provides explicit rational matrices.**

Let a and b be rotations about the first and third axes, both with cosine 3/5 and sine 4/5. The alphabet is exactly `{a,ab}`. Every one-step average factors as `V_a((1-p)I+pV_b)`. A nonconstant b-invariant spherical function witnesses norm one for every p. Thus the optimized one-step gap is zero, not merely zero for a poorly selected test law.

The four equally likely two-step words factor as `V_(a^2)(I+V_c)(I+V_b)/4`, where `c=a^(-1)ba`. The subgroup generated by b and c is dense in SO(3), and all matrices are rational. The external Benoist–de Saxce Theorem 1.2 supplies a qualitative gap delta for their symmetric average. A fully displayed four-unitary variance identity gives a squared norm gap at least delta/4 for the two-step average. There is no computed numerical delta. The calibrated numerical width is therefore Theta(N), despite zero optimized one-step gap.

This example also forces the norm/spectral-radius distinction to be explicit. The proof imports only the symmetric algebraic group-gap statement, then derives the required nonnormal two-step estimate directly.

## 12.3 / Section 5.4 — the dominant-nongapped region

**Substantially advanced by a matched infinite hierarchy, not merely named as an exclusion.**

Theorem `thm:main` fixes r planar rotations together with the identity. Assume the dual bound

```
||m.alpha||_(R/Z) >= b* ||m||_1^(-r),   m in Z^r minus {0}.
```

Then, for fixed `rho<=1/10` and `epsilon<rho/(2 sqrt(2))`,

```
W_(N,epsilon) = Theta(N^(r/(2r+1))).
```

All optimized finite-block action gaps are zero. The underlying real irreducible representation is always the same standard two-dimensional SO(2) representation, its only active type is dominant, and its minimum orbit dimension is always one. Thus this is a result in the genuinely dominant-nongapped region, not another mixed example with an expanding dominant summand.

The lower proof uses L^r separated phase increments made by r segments of L-1 actual commands, with L chosen so L^r>=2K. The endpoint quantizer loses at least `b*^2 B^(-2r)` of its incoming conditional amplitude, where B=r(L-1). Independent complete packets yield the finite inequality

```
floor(N/B) b*^2 B^(-2r) <= log(1/kappa).
```

It applies to arbitrary hidden states and arbitrary rows varying in time. A greedy disjoint-interval selection also gives a nonuniform small-width occupation bound without restricting other cuts. The inherited one-packet argument is proved again; no independence of individual commands inside a packet is used.

The upper proof is equally causal. A simultaneous Dirichlet denominator q at scale Q satisfies `c Q^r<=q<=Q^r`; the lower bound on q is proved directly by a residue pigeonhole argument and the same dual inequality. One regular q-gon then supports every command with per-step logarithmic dilation `O(1/(q^2 Q))`. Choosing Q of order `N^(1/(2r+1))` gives q of order `N^(r/(2r+1))`. Common stochastic rows and the explicit bounded N-dependent decoder realize every numerical word exactly. All command steps use the same actual label set.

There are explicit examples for every r: `alpha_i=2^(i/(r+1))` and `b*=(2/9)^r`. Their separation is proved from the nonzero algebraic integer norm of a polynomial of degree at most r. The command matrices have computable-real entries; no algebraic-matrix or finite-fair-bit assertion is inferred from the algebraic angles.

The hierarchy gives exponents 1/3, 2/5, 3/7 and so on in one and the same orbit geometry. It also has separate positive minimum three at every internal cut. The r=1 mechanism and exponent are inherited, not claimed new. General nongapped actions outside the stated dual class are not classified, nor are all finite integer widths determined.

## 12.4 / Section 4.3 — make the actual calibration usable

**Addressed in `thm:calibration`, `prop:calibration-sdp`, `prop:calibration-examples` and `thm:frame`.**

The norm is made finite: `B(J)^2=max_sign sigma^T JJ^T sigma`. The copy J may be replaced by its rank-d orthogonal projection P, supported on the certified isotypic component and commuting with every command matrix. The exact objective is

```
c_lambda^2 = max_(x,P) x^T P x / max_sign sigma^T P sigma.
```

Projection, trace, support, commutation and ratio inequalities form a finite polynomial system. With fully specified real-algebraic data and a certified isotypic projection, quantifier elimination yields the algebraic optimum and an algebraic optimizer. This is not a claim of polynomial complexity or a procedure for finding an infinite action gap. The article strengthens the elementary bounds to `a/sqrt(D)<=c<=a/sqrt(d)`.

Real, complex and quaternionic multiplicity models are described through rank-one Hermitian projections in their respective matrix commutants. The original rank constraint is not dropped without notice. Homogenizing the objective and relaxing the rank gives a convex positive-commutant SDP; its dual has nonnegative weights on the sign inequalities. Relative Slater feasibility gives strong duality. A rank-d primal optimum certifies exactness; a larger-rank optimizer supplies only an upper bound.

The principal calibrations are evaluated: standard spherical `1/sqrt(d)`, projective `1/sqrt(q^2-1)`, and the standard SO(3) plus SO(2) coordinate blocks `1/sqrt(3)` and `1/sqrt(2)`. In the real-type coordinate multiplicity model, `c=max_(x,i)||x_i||/sqrt(d)`. A two-copy example has Euclidean activation one but `c^2=1/6`, with an explicit exact primal projection and one-sign dual certificate. The finite verifier checks both witnesses by rational covariance identities; it does not claim to solve every nonconvex instance.

For a physical query frame F, the relevant norm is the minimum of `||L||_(infinity->2)` subject to `LF=J*`. This is a finite cone program for fixed J. Duplicating a query changes nothing; adding queries cannot worsen calibration; deletion may lose observability. Seed perturbations have an explicit Lipschitz bound. Full-rank frame perturbations obey two-sided multiplicative bounds involving their smallest singular values. No uniform stability is claimed across loss of query rank.

## 12.5 / Section 9 — compact transformation groups

**The missing boundary is addressed by direct theorem comparisons and a concrete distinction.**

The main article compares Gorodski–Lytchak's Section 1/Proposition 1.1 on quotient-equivalence and orbit spaces, their Theorem 1.1 on orbifold sphere quotients, and Gorodski–Thorbergsson's Theorems 1.1–1.2 on variationally complete and taut representations. The inherited uniform small-ball proof is explicitly identified as a finite differential-chart argument, not a new slice theorem or representation classification.

`prop:quotients` separates the invariants. All standard SO(d) representations have the same half-line metric quotient, but their nonzero orbit dimensions differ; the finite reflection action also has that quotient. Ambient orbit-equivalence preserves orbit dimensions, whereas a metric quotient or polar reduction need not. To preserve a numerical experiment one must also intertwine actual commands and transform the queries. The nongapped hierarchy is stronger still: even the identical action in the identical ambient plane does not fix a width exponent.

Classical orbit geometry and arithmetic are credited. A targeted source comparison is not called exhaustive independent priority clearance. The current paper does not claim all highest-weight minima; all previously proved adjoint and exterior-power statements remain in the preserved v41 article and sources.

## 12.7 — disconnected actions and finite groups

**Addressed by an explicit paid compiler and matched O(2) examples.**

For a compact effective group with finite component group, `prop:components` writes `A_a r_c=r_(c') h_(a,c)`. A label stores the component c as well as the connected compiler state. This yields an upper factor at most `[H:H0]`, with transformed final queries and no free coset information. The statement does not assume that disconnected components preserve each displayed H0 irreducible copy.

The reflection extension of the r-angle hierarchy has the same matched exponent: its upper polygon is reflection-invariant and the lower bound restricts to rotation words. It remains nongapped for every finite block. Finite effective groups are treated separately by finite orbit tracking, with an explicit constant upper bound and the final held two-label output. No general equality of finite-extension exponents is inferred solely from the index.

## 12.6 and the resource/editorial comments

The model is centralized in Definition `def:machine`. Available labels, positive-probability labels for a chosen converse law, label bits, total row count, table description, fair-bit states, anytime autonomy and uniform space remain distinct. The word is externally supplied, not a policy optimized by the simulator. The explicit toral upper rows are time-independent but the decoder depends on N. The compact finite-dimensional optimizer discussed in the calibration section is not a global machine minimizer.

The title retains the General Theta Foundations I series prefix with a concrete mathematical subtitle. The mathematical response to the report is the new nongapped hierarchy and its accompanying alphabet/calibration theorems, not a change of editorial ambition. The conclusion makes no use of internal analytic-paper labels to justify the local results.

## Pipeline and preservation

The frozen Round-Seventeen dependency ledger was read at r27, together with the v41 action, activation, orbit, entropy and synthesis sources and their history audit. The original v35 packet source was also checked. Their exact meanings enter the new proof: packet distortion supplies the nongapped converse, while the Gaussian occupation and minimum-orbit compiler supply the finite-block expanding theorem. Three analytic modules are copied byte-for-byte into the new core.

The A2/A3/A4/C2/D1 and hard-sphere chains keep their independent Fourier/local-limit, stopped-LDP, global-kernel, graph-core, nonlinear-semigroup, filtering and optional-projection obligations. No fabricated downstream proof edge is introduced, and no claim of re-proving every archived page is made.

The entire v41 article is byte-identical as `supporting-results.pdf`; its cumulative mathematical and development volumes are appended unchanged after the current article and a divider. All earlier repository paths and review branches remain untouched. The compact referee package excludes the large archives. Source hashes, exact finite regressions, explicitly labelled 80-digit diagnostics and negative controls are recorded as reproducibility evidence only.

## Status for the next referee

The next review can test the all-hidden multi-angle lower bound and common-row upper compiler, the strict one-step/two-step expansion separation, and the exact calibration/dual/frame formulations. These are proved at their stated hypotheses. General gap positivity, arbitrary nongapped effective actions, optimized constants, all finite widths, original-page LPS verification and unrelated analytic aggregate closure are not claimed solved. An independent mathematical and priority assessment remains distinct from the executed build.
