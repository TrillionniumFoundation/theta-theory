# Proof and information ledger: multilevel revision

The pinned review is `90c7acd0e4efe3280eb1a8faa3c6d9461870c36f`. Labels below are native source identifiers, not fabricated PDF theorem numbers.

| Statement | Main assertion | Essential proof input and information convention |
|---|---|---|
| `thm:v30-menu-finite` | Optimized fixed-calibration all-resolution excess is finite; one order is eventually exact | Finite retained affine branches and finite order set; zero determinants omitted; calibration fixed |
| `prop:v30-critical-resolutions` | Exact finite critical-resolution formula on the half-line | All nonnegative intersections of affine constituents; prescribed menus of excessive eventual slope excluded |
| `cor:v30-stratum-uniform` | A single order has uniformly bounded excess on a positive-volume stratum | Same retained indices and uniform positive lower/upper volume bounds |
| `thm:v30-regenerative` | Exact statistical row-mixture minimax at every integer M | Fresh current block; route selected before data; finite decoder centers; one causal reset controller |
| `thm:v30-positive-realization` | Full mixed-law scalar quantization times a common design factor | Executed positive detector, explicit attenuated readout, projection and clipping; no atom removed |
| `lem:v30-atom-capacity` | Atom-aware full-probability-space capacities and exact cubic costs | Continuous density 26, atom mass 1/2, norm 8 sqrt(2), M actual centers; event mass one |
| `thm:v30-controlled-gap` | Committed-route anchored certificate has a uniform finite-budget gap | Matching design factor in risk and certificate; entire support midpoint quantizer; unanchored value zero for two routes |
| `cor:v30-checkpoint-gap` | Actual J-checkpoint consistency penalty J+1-1/J | Uniform route primal and uniform checkpoint dual; maximum taken after expectation |
| `thm:v30-predictable-exact` | Exact causal network flow minimax and shortest-path dual | Choose vertex before fresh block; prescribed conditional block law; finite public vertex descriptor; full-history scheduling converse; attaining finite-label reset controller |
| `prop:v30-predictable-bound` | Predictable certificate dominates initial-anchor and unanchored certificates | Conditional layer cake before each acquisition; common occupation flow; convex phi(x) <= x phi(1); terminal caps equal one |
| `cor:v30-network-gap` | Network/horizon/gain-uniform statistical gap and reusable optimal path law | Full acquired mixed law remains unchanged conditional on past selection; exact common design factor; arbitrary finite layered network |

## Preserved proof chain

The monomial acquired-rank theorem, complete confluent flags, unconditional acquired-law lower measures, whole-image bounded-format covers, gap-free raw causal updates, fixed-graph adaptive converse, whole-curve/menu results, local witnesses, augmented growing-graph theorem, occupation/perspective duality and complete companion developments remain active. No inherited theorem is removed to make the new realization theorem true. Only the false fixed-calibration infinity sentence, its vacuous contingency and the nearby ambiguous quantifier are corrected. The seven legacy proof bodies in the two patched files were compared byte for byte locally.

## Quantifiers that must remain visible

A fixed finite calibration is not a collision-uniform family. A fixed acquisition law is not a common nested codebook. An independent seed is not history-correlated free memory. A finite public vertex descriptor is not an unrestricted past. A local-and-tensor loss is not the original tensor-only metric at growing graph size. Conditional regeneration before acquisition is not independence after selecting on a revealed block. The maximum of expected checkpoint losses is not the expected maximum. A feasible dual is a lower certificate; a nonoptimal feasible primal is not.

## Verification direction

The ten-page proof packet verifies typesetting of the new modules only. The 50,790 rational diagnostics check specified finite identities and a scalar numerical enclosure, not universal theorem validity. The native builder is designed to verify all active labels, citations and proof preservation in the full checkout; its actual execution must be judged from `BUILD_RECORD_V30.json`, not from the existence of the script. The current session receipt explicitly distinguishes these scopes.
