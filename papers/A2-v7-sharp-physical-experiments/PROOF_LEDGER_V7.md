# A2 v7 proof ledger

All entries below are mathematical claims with written proofs in the new manuscript, submitted for independent review. A PASS in a finite diagnostic or build is not a proof certificate.

| Label | Statement and assumptions | Dependency and proof mechanism | Scope / provenance |
|---|---|---|---|
| `prop:v7-overlap` | Exact tangent TV `delta_j=(2/pi)asin(exp(-j gamma))`; exact selected and raw products | Equal-determinant endpoint Hessians; disk–ellipse intersection by polar integration; common-density overlap strata | Tangent experiment only. Benchmark first recorded in the latest referee memorandum; attribution is explicit. |
| `lem:v7-tangent` | Uniform physical-to-tangent TV `O(d)` conditionally and `O(p_j^0 d)` per raw preparation | Identical even local contact graphs; uniqueness gives simultaneous sign symmetry. Retained uniform `C^4` action and `C^2` normalized-twist estimates imply quartic/quadratic remainders. Residual-fibre integration includes moving supports and mass errors. | Positive offsets; common compact family; constants independent of flight number. Does not restrict the general forward theorem. |
| `thm:v7-phase` | Actual nonlinear positive-offset experiment distance tends to `1-exp(-2b/pi)` when `d_j / exp(-j gamma) -> 0` and the scaled sample size tends to `b` | Exact tangent products plus uniform nonlinear approximation. Finite critical sizes use the product triangle inequality. Infinite critical sizes use projection to a fixed finite critical subexperiment, then send that critical size to infinity. | Selected scale `k_j exp(-j gamma)`; raw scale `n_j p_j^0(d_j) exp(-j gamma)`. A simultaneous-limit theorem, not an interchange of fixed-j limits. Geometry may vary within the stated compact identical-even family. |
| `prop:v7-measurable` | Exact Borel measurable compact minimum-discrepancy fit; data error remains at most `2 delta` | Fixed closed dyadic cubes, fixed child order, closed tests for intersection with the minimizer set, nested diameters tending to zero | Specifies all compact fits and fallbacks. No uniqueness or numerical optimization efficiency assumed. |
| `lem:v7-cubic-count` | Actual finite-offset count discrepancy `O(d^2 s^3)` for physical opposite perturbations with curvature matching distance comparable to `|s|` | 60-degree lattice rotation acts as 120-degree parameter rotation; zero differential; uniform third derivatives; explicit radii `(R+36s,R-18s,R-18s)` | Fixed bounded flight numbers, all offsets in a common collar. Not merely a claim about leading amplitudes. Physical obstruction first identified in the referee memorandum. |
| `lem:v7-stopped-kl` | Stopped-transcript KL at most `C H^2 h^2 s^6 E_s T` | Bernoulli KL bound, common-history adaptive queries, padded stopping chain rule, increasing transcript sigma-fields | Parameter-independent strategy randomization; exact common gap may be supplied; indicator-only observation. |
| `thm:v7-count-lower` | Uniform matching accuracy epsilon at confidence `1-eta` costs at least `c h^-2 epsilon^-6 log(1/eta)` in worst-case expectation | Two physical alternatives induce a test; binary entropy lower bound is `(1-2eta)log((1-eta)/eta)`; combine with stopped KL | Bounded-J, all-history offsets at most `Hh`, Bernoulli observation. Adds an explicit confidence logarithm to the referee's constant-confidence precursor. |
| `cor:v7-design-optimal` | Matching fixed-m expected preparation order `epsilon^-(6+6/m) log(1/eta)` | Retained calibrated count upper bound; verify all-history pilot/stage-two collars; set `h` proportional to `epsilon^(3/m)` | Design-specific optimal order. Not a global minimax statement; no uncontrolled `m -> infinity` claim; selected-position data are different. |

## Preserved mathematical architecture

The complete v6 body remains active: general threshold localization; weighted finite action; physical integration; two nonlinear boundary layers; differentiated operators; relative experiment transfer; nonlinear information; contact rigidity and realization; fixed-order finite-jet stability; endpoint observability; higher-order and self-calibrated position acquisition; equal-gap inversion; coalescence and pairwise inversion; constrained-area three-amplitude inversion; count-only acquisition; record/marked response; literature comparisons; and the complete circular/flux/geometry/threshold/record/consequence appendices. The full two-collision companion and earlier historical sources remain available.

## Evidence distinctions

The new statements have written proofs in the manuscript. The 136-check standard-library suite verifies finite exact identities and explicitly labelled numerical benchmarks and survives `python -O`; it does not verify the infinite-dimensional nonlinear theorem by simulation. The build script implements source-preservation and compilation checks, but its GitHub run terminated before any job step executed. The actual status, local script-to-repository hash match, and separate evidence records are in [DELIVERY_STATUS_V7.md](DELIVERY_STATUS_V7.md). Independent referee diagnostics, inherited author diagnostics, new diagnostics, CI, visual inspection, and priority claims must not be conflated.
