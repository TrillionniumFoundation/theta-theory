# Preservation map — v15

No original v14 or earlier file is edited or deleted. The 345 files in the downloaded v14 source closure are hash-pinned as inherited inputs. The new branch begins at the controlling v14-r2 review and only adds the v15 directory, its dedicated workflow and review-entry documentation.

The canonical v15 core imports, unchanged, v13 `intrinsic-deficiency.tex`, `endogenous-tasks.tex`, `regenerative-risks.tex`, `active-hard-spheres.tex`, and v14 `local-certificates.tex`, `residual-certificates.tex`. The new introduction states the new main physical theorem. The old v14 introduction is included in the complete-development historical section; it is not deleted.

The complete-development view first includes the same v15 core and then the exact v14 `preserved-core.tex` and historical supporting imports. All earlier mathematical bodies thus remain available. Historical introductions are labeled as historical rather than rewritten to match the latest claims. The new build checks every v14 complete-development label, all resolved imported labels and equal numbering for every label shared by the new canonical and companion views.

The live dependency graph has a corrected fifteenth-edition field, but the old v14 graph including its edition typo remains byte-identical. Every inherited component field and internal edge is retained in the new graph before adding scoped v15 edges. The numerical count of inherited files and labels is reproducibility information, not a mathematical claim.

New mathematical files are: `finite-presentations.tex`, `enriched-certificates.tex`, `constructive-hard-spheres.tex`, `physical-certification.tex`, `physical-data.tex`, `behavior-witnesses.tex`, and `task-tolerance.tex`. New article entry/frontmatter/introduction files organize them. The bibliography is imported unchanged from the v14 sources; prior literature is not removed. New diagnostic and publication files are confined to this revision.
