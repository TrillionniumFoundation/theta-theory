# Proof ledger for A1 v23

This ledger separates analytic obligations from executable checks. Its presence is not a proof certificate.

| Request or conclusion | Active argument and dependency |
|---|---|
| E22.1: collision classification | Introduction `sec:contribution-v23` states the experiment-specific acquisition/global/causal conjunction. The existing `thm:resolution-main` and its complete proofs are unchanged. |
| E22.2: filtering comparison | Appendix `sec:filter-quantization-comparison` compares resource models and the role of a converse; it does not assert a theorem reduction without proof. |
| E22.3: saturated image | `prop:algebra-saturation` uses the existing product-chart bound and actual inverse acquisition patch to sandwich the reachable image between covariance ellipsoids. |
| Supported quotient rank | Existing finite-algebra indicator span makes the distinct evaluation rows independent. On positive-weight parts the covariance rank is their affine dimension, hence supported-part count minus one. This is qualitative only. |
| All-budget upper bound | `eq:algebra-direct-cover` is a grid bound on a containing rectangle. Empty cells are discarded and centers made reachable. The constants handle all `M >= 2`; `M = 1` and zero rank are separate. |
| Matching lower bound | A fixed positive-volume component of the unconditional coefficient distribution is rotated and projected before covariance scaling. Volume exclusion applies to arbitrary code centers, then to fixed randomness and its average. |
| Physical norm | `eq:algebra-direct-metric` keeps the exact delayed-query scaling. Covariance eigenvalues, rather than their square roots, are the raw-state semiaxes. |
| One causal filter | The raw-state update extends a realizing history, keeping codebook representatives reachable. The error recurrence gives a single filter upper bound; the lower bound is applied to every common filter before the outer infimum. |
| E22.4: risks | `def:algebra-risks` prints both checkpoint infima and both common-filter multistage infima, including expectations over actual exploration and coding randomness. |
| Alternative derivation | The original algebra transfer proof is unchanged and active in Appendix D, `sec:algebra-transfer-alternative`. |

The direct proof requires no covariance inverse, no lower bound on a positive eigenvalue and no semialgebraic-format estimate. It retains the bounded multiplication coefficients and fixed finite horizon of the existing algebra theorem. None of these observations removes the more delicate acquired-dimensional geometric obligations in the monomial theorem.

Executable checks verify inherited source identities, preservation of all 129 formal statements/127 proofs/389 labels, citation resolution, exact finite examples and typesetting. They do not prove uniform real-parameter density or optimal coding inequalities. The final mathematical check remains the printed argument.
