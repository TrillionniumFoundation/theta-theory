# Principal proof ledger — A1 English v4

Numbers below are read from the locally compiled source labels. There are **18 proof-bearing theorem/proposition/lemma statements**, including the synthesis, plus the explanatory intrinsic-dimension Remark 3.3. Counts are navigation, not evidence of significance.

| Label / number | Main content | Inputs and essential quantifiers |
|---|---|---|
| `thm:main` 1.1 | Structural/robust synthesis | The proved components below, with their stated model and reward hypotheses |
| `prop:raw` 1.2 | Actual positive cubic kernel | Ambient insertion attempts; entire collision tube; `T<0.06`; specified coordinate detector |
| `prop:realize` 1.3 | Exact common-gate family and pasting | One radius-independent gate; fresh independent cartridges; both failures retained |
| `thm:filter` 2.1 | Positive exact full-likelihood state | Non-null histories; normalize rare failed factors; compact strata at fixed budget |
| `prop:elevation` 2.2 | Positive degree elevation; rational denominator | Uniform positive numerator and coefficient bounds; retain `D(R)^(-n)` |
| `thm:rank` 2.3 | Iff full rank, `n(r-1)` dimension, interior and rank stability | Fixed normalized polynomial channel; known command family; continuous encodings; full-support prior for posterior lower bound; local rather than global minimal embedding when rank deficient |
| `thm:amplification` 2.4 | Passive `n` versus active `3n`; explicit inball | Same fixed nondegenerate mode; endpoint-amplitude degeneration explicit; not information improvement by a fixed garbling |
| `lem:body` 3.1 | Compact moment body and Borel representatives | Weak-star compact order interval; separable `L^1`; jointly continuous integrands |
| `thm:bellman` 3.2 | Attained full-gate feedback and finite menus | Bounded observable rewards; positive terminal payoff; weighted null-branch continuation, not null-posterior continuity |
| `rem:dimension` 3.3 | Body in `R^5` can have dimension four | Explicit constant-payoff example; not included in proof-bearing count |
| `thm:endpoint` 3.4 | Optimal endpoint feedback | Credited referee argument; homogeneous convex continuation; no nonlinear gate costs or coupling constraints |
| `thm:arc` 4.1 | Solved one-arc costly gate and four mode candidates | One remaining cartridge, two positive terminal decisions, constant acceptance multiplier `<1` |
| `prop:example` 4.2 | Strict gap over every report-blind gate | Exact rational interval bounds; full-support prior perturbation; not temporal adaptivity at `N=1` |
| `thm:model-error` 5.1 | Policy-uniform model comparison and regret | Same report/command/payoff class, uniform one-step TV; common fixed adaptive policies |
| `thm:hierarchy` 5.2 | Physically realizable polynomial detector hierarchy | Positive normalized collision detector; stated TV-Lipschitz / `C^2` / circle-Lipschitz assumptions for rates; finite table per fixed mode or finite menu |
| `thm:response` 6.1 | Variation response, DQM and adaptive information | Original cubic detector, controller fixed as a function of history, common support |
| `prop:bias` A.1 | Biased entropy and positive-probability event | Retained objective `D-QB`; bare entropy only in stated special cases |
| `thm:traces` A.2 | Both density traces and outer flux | Eulerian derivatives, branch transports and boundary envelopes |
| `thm:chamber-repair` A.3 | Smooth state, specified smooth report, preparation response | Fixed regular itinerary; smooth report map; weight/transport/normalizer envelopes; fixed reporting manifold/ambient dimension |

Section 6.4 supplies elementary coefficient-error and prior-value inequalities with their derivations, without separate numbered theorem claims. The central general statements are justified by their analytic proofs, not inferred from finite diagnostics.
