# A1 v29 proof ledger

Source labels, not PDF numbers, identify statements. The v26, v27 and v28 ledgers and every inherited mathematical module remain in the native source tree.

| Active statement | File | Dependencies and quantifiers |
|---|---|---|
| `thm:v29-occupation` | `v29/occupation.tex` | Unconditioned local visiting/small-loss bounds; one conditional unit flow at all thresholds and levels; terminal capacities `x_w <= c_w(1)`. No selected-node independence. |
| `prop:v29-separator-dominance` | `v29/occupation.tex` | Finite acyclic flow decomposition and layer cake. Compares the same event and capacities; the old factor `1/J` is retained on the comparator side. |
| `thm:v29-relaxation-exact` | `v29/occupation.tex` | Compact flow polytope and generalized inverse of nondecreasing right-continuous capacities. Exact within random-path loss models, not general statistical minimax equality. |
| `thm:v29-pre` | `v29/initial_information.tex` | Capacity bounds seed by seed; tape-independent coding randomness; first vertex chosen before acquisition; conditional Jensen at each fixed checkpoint before taking the maximum. |
| `thm:v29-dual` | `v29/initial_information.tex` | Convex supporting representation, classical compact minimax, separating hyperplanes and capacitated minimum-cost flows. Feasible dual values certify lower bounds; primal feasible values do not. |
| `cor:v29-graph` | `v29/initial_information.tex` | Inherited `lem:v26-first-block`, arbitrary-centre recovery and original visited-set decoder. Fixed experiment; additional finite descriptors multiply centre counts by their cardinality. |
| `prop:v29-two-choices` | `v29/evaluated_refinement.tex` | Cubic costs, two initial vertices, exact matching rational primal/dual witnesses. Prefix descriptor supplies no extra information at the only checkpoint. |
| `thm:v29-evaluated` | `v29/evaluated_refinement.tex` | Actual full mixed acquired law in `prop:v27-density`, query norm squared 128, mass 1/2, density bound 26. All integer budgets; completion evidence and density both scale by 1/2. |

## Dependencies kept distinct

The scalar all-budget/confluence/filter theory remains in `core/` and `text/`. The historical v10 source in `provenance/v10-scheduling.tex` supplies the original additive attained-scale and shared-memory route; it is not counted as a new occupation theorem. The v25 graph, v26 separator and analytic phases, v27 resolution-blind policy/menu and sharp two-trial law, and v28 localized growing-graph modules are inherited by Git identity.

The v28 graph-size-uniform theorem uses an explicitly augmented local-and-tensor query loss. The v29 original-task quantitative flow bounds do not silently inherit graph-size-uniform constants. The v27 uniform-curve theorem fixes the acquisition law across resolutions; predictor codebooks may be reoptimized. The v26 fixed-order regret fixes the permutation only. A prefix decoder and a visited-set-only decoder use different centre counts.

## Proof review and execution scope

The new layer-cake, inverse-CDF exactness, seedwise Jensen, duality, and evaluated-constant arguments were reread; the acquired-density dependency was checked from the pinned v27 source. The earlier uncommitted occupation draft was preserved and integrated rather than represented as a newly completed earlier publication. No new exhaustive independent audit of the entire companion or all eleven papers is claimed.

The eight formal statements above contain full proofs in the principal input tree. The diagnostic suite supplies finite arithmetic witnesses, not continuum proof certification. The separate twelve-page proof component was compiled; no completed native two-volume compilation is asserted in this session.
