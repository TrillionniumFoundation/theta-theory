# A1 v37 — proof preservation and publication ledger

## Controlling identities

The controlling report is the v36 independent assessment at `1e7af61f0812638d00ee0efad8fc9a68e2776c3c`, reviewing `8f074b8027627a71a81a362b9d15f47975e1f3ae`. Its recommendation is acceptance of the main article with no further mandatory mathematical revision. It closes R35.1 and R35.2 and independently reproduces the current native pair.

The publication source commit is `616904c0f151b51e75b8a09fb98c25b65209975a`. The only change among its 80 active TeX inputs is the date in `main.tex`. The evidence commit leaves the compiled inputs unchanged.

## Principal proof route retained

The experiment and resource definitions remain in `core/02_experiments.tex`, `text/operational_model.tex` and `risk_criteria.tex`. The fixed query menu, future-only tasks and per-report persistent labels have not been replaced by a different information resource.

`text/exact_information.tex` and `core/03_transversality.tex` retain the operational moment characterization, feasible binomial tangent, strict mixed-moment positivity, exact loss of one rank under normalization, and continuous-state realization. The tangent and normalization proofs were consulted in this preparation; their current objects agree with the pinned historical proof sources.

`text/analytic_inputs.tex`, `text/collision_flags.tex` and `text/collision_direct.tex` retain the positive/confluent pairing, actual acquisition-law minorization, whole-image cover and common raw-moment transducer arguments. Their source objects are unchanged. `text/main_classification.tex` continues to state the same joint calibration-and-budget theorem; its constants and quantifiers are unchanged. No claim of a new line-by-line reproof of every retained theorem is made by this ledger.

`text/collision_consequences.tex` retains the bit law, finite-power collision tree and two-parameter calculation. The v36 comparison in `v36/spectral_comparison.tex` retains both clarified statements verbatim: the common H = 16 and fixed L >= 8 in Corollary 13.3, and the comparison-envelope interpretation of the flat-path crossovers in Remark 13.4. The report's closure of these requests is recorded without adding new mathematical conditions.

## Realization theory and historical developments retained

`v35/moment_controllers.tex` remains the active finite-cell realization module. Its exact postprocessing region, conditional occupancy recursion, attained mean-risk representation, treatment of zero evidence and zero occupancy, atomless-command purification, scalarized polyhedral rules and fixed-budget collision transfer are unchanged.

`v33/finite_compatibility.tex`, `v33/precision.tex` and `v33/exact_instance.tex` remain active. Their labels and historical directory names are preserved for stable cross-references; they are not shortened summaries. All companion inputs are byte-identical. The complete structural, exact-kernel, uncertainty, effective, graph, occupation, regenerative and delayed-use developments retain their respective hypotheses and proofs.

The Git publication starts from the entire v36 native subtree `13d4e7441bd44c89dcc187e1d5343f01061c4af6`. Accordingly, inactive historical material is preserved as well as the active input closure. Old entrypoint, README and build-wrapper objects are additionally retained under `history/`. The downloadable native package is an execution package, not a replacement for the complete repository archive.

## Exact preservation result

The source-integrity verifier reconstructs the v36 manifest from the pinned v35 base and v36 delta, then verifies every active byte sequence. Replacing the new date by the old one recovers the original `main.tex` object exactly. No other TeX edit is permitted by that check.

The complete active closure contains 67 theorems, 54 lemmas, 40 propositions, 49 corollaries, nine definitions and three remarks: 222 theorem-like blocks. All 222 blocks and all 210 proof blocks match verbatim. This is a source-preservation result, not a correctness inference from a block count.

## Production evidence

`verification-v37/NATIVE_BUILD_V37.json` identifies six successful compiler invocations on the actual source commit, stable real cross-volume exports and 80-source recorder agreement. The native output has 42 main and 159 companion pages. The full receipt and stdout logs accompany the downloadable package.

`PDF_EQUIVALENCE.json` compares every PDF page with the supplied v36 output. The main title-page date is the sole text change; the other 200 pages have identical text and identical 72-dpi RGB pixels. `VISUAL_INSPECTION.json` separately records coarse inspection of all main pages and enlarged samples.

The four unchanged mathematical diagnostics retain their original provenance and match their v36 outputs in both Python modes. No new full-controller optimization, exhaustive originality search or formal proof-assistant result is claimed. The favorable report concerns the main article, not automatic endorsement of every companion result or all planned papers.
