# Historical derivation audit for A2 v16

## Source baseline

The active baseline is the full native A2 v15 tree `bb7df34e7d82fc449d523d6146928ac9416fe01b` at author commit `627c16b951994fa65acfbf3a621d0e4306131db3`. The latest independent report is pinned at `a108adf17c4b9360e340a2d5708b20a375d69283`. This is not a reconstruction from an earlier A1 discussion, an unpinned branch listing, or an abandoned A2 topic.

The historical chain used in preparing the revision is: physical suspension measure and first-hit normalization; exact quadratic stationary action and Schur complement; uniform endpoint localization; half-line action and relative determinant; mixed-derivative comparison and physical integration; higher contact jets and realized leading-data fibers; fixed and critical physical experiments; complete Volterra profiles; weighted Abel stability, smooth integrated-flux regularization and charged calibration; two independent contacts and the exact two-flight comparison; classical scalar density and its physical transport identity.

The written derivations, including the independent one-flight and two-flight benchmarks, are retained. The full-profile, finite-jet, exact analytic-family, smooth-envelope, and known-simple-pair experiments are not silently identified with one another.

## Preservation

The v16 Git tree is based on the entire v15 native tree. The new `main.tex` has all 52 previous direct inputs plus four new inputs. The only old proof module moved in the entry-point order is `v6/10_experiment_transfer.tex`, promoted from the auxiliary part to the main physical argument without changing its contents. The complete companion remains at `two_collision.tex` with blob `df44402b17031525c087d39dfedf8dac3ada611d`.

Original entry-point prose and submission metadata that are superseded by this revision are separately preserved under `history/v15/` by their existing Git blob identities. Existing `history/`, `sections/`, `v2/` through `v7/`, verification materials and tools are inherited. The older repository-level author and review directories are not modified or removed.

## What is added

The adaptive physical comparison is a consequence of the prior relative one-step law, proved for the complete stopped transcript rather than asserted from a fixed product estimate. The adaptive tangent overlap uses the common-history distribution and the correct product-weighted reverse erasure law. The rational strict-margin example is credited to the latest report. Classical coupling and scalar linearization are not relabelled as new general theories.

This audit does not assert that every historical diagnostic suite was rerun in this revision. Executed checks and actual native-build evidence are listed separately in `VERIFICATION.json`.
