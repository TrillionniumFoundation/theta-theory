# Historical derivation audit — A2 v58

## Frozen baseline and scope

The latest v57 report is frozen at `e14138660e716daf60471f3b98e8f1d30cb61a34`, reviewing source `e39315c1fbb79ce71d1da91186a0d7e99a5ad8d6` and manuscript subtree `1c11e890bdd5a96381b9a803ade7588eff5c04b9`. The full report and its audit ledger were read, including the expanded acquisition audit and its final disposition. The source-matched v57 Actions ZIP and nested source ZIP were verified against their pinned digests before use. The GitHub tree endpoint independently identifies the manuscript subtree.

This revision is based on the actual latest source, not on the earlier v54/v50 examples in the conversation. The v56/v57 principal-article architecture and explicit external acquisition dependency are retained. This audit is a record of selected proof reading and complete byte preservation, not a fresh certification of all 679 baseline files or all inherited theorems.

## Derivation interfaces used

| Historical source | Reading and role in v58 | Treatment |
|---|---|---|
| `journal/00_principal_introduction_v56.tex` and `rigidity.tex` | Complete headline statement, proof map, observation scope and dependency paragraph. | Strengthen the existing highest-degree clause; preserve all other headline conclusions. |
| `v4/10_boundary_layers.tex` | Complete half-line construction, two-end gluing, trace-norm compression, normalized determinant, fixed-offset law and associated consequences. | Byte-identical; no order-uniform derivative claim is substituted for its fixed-order estimates. |
| `article/23a_signed_endpoint_rigidity_v27.tex` | Leading parameters and degree conventions, actual-smooth remainder, homogeneous isolation, endpoint/interior multiplicities, explicit inverse and finite tangent argument. The new proof uses these exact identities. | Every existing statement/proof block unchanged; add only the new subsection input. |
| `article/23f_single_offset_law_inverse_v42.tex` | Complete positive-square/scalar-anchor inverse and finite-order stability/finite-flight interface through the jet-error proof. | Byte-identical; no total-variation-to-derivatives conclusion. |
| `article/23e_signature_stability_v25.tex` | Compact class and finite-signature hypotheses, finite embedding argument and boundary with continuation stability. | Byte-identical. The new diagonal norm bound does not estimate its compact inverse modulus. |
| `article/25b_augmented_global_reconstruction_v26.tex` | Finite separators, templates, direct capped estimator and the increasing-order budget construction; compared to the report's expanded acquisition reading. | Byte-identical; no re-proof of the entire earlier transcript-transfer machinery is claimed. |
| `article/23j_generic_finite_channel_rigidity_v45.tex`, current dependency ledger/map | The established R56-C1 statement/proof classification and geometric design inputs. | Corollary/application and its additional assumptions remain unchanged; refresh map locations only. |
| Remaining geometric, window, graph/support, statistical and companion inputs | Their published hypothesis boundaries are preserved; complete active-input identities are checked. | No claim that unchanged source hashes newly certify all these proofs. |

## New step in the original core

The route is

`positive curvature -> r_b = c/c_(1-b) < c -> r_b exp(-gamma) < (1+exp(-2 gamma))/2 < 1 -> explicit uniform row sums and identity decay -> actual fixed-lower-jet estimate and blockwise coefficient-space bounds`.

The first matrix refinement is credited to the v57 report, R57-M3. The coefficient-space interpretation and exact decomposition of the full finite derivative are included as consequences, not as a new unrestricted inverse theorem. The appended subsection is `article/23a1_uniform_blocks_v58.tex`, active in both complete entries.

The existing functional remainder lemma is still necessary: only after it factors actual actions through finite jets can equal lower jets cancel the nonlinear remainder exactly at the selected degree. The estimate controls the diagonal blocks, whereas `L_M = D_M + N_M` still has strictly lower-triangular couplings. The inverse finite sum for `I+D_M^(-1)N_M` isolates that distinction without inventing a defect in the old fixed-order result.

## Preservation invariant

Seven amended originals are archived exactly: `main.tex`, `rigidity.tex`, the principal introduction, the signed-contact input, the full-reference aliases and the two bibliographies. All 120 old paths remain active and every old label survives. The added proof raises the unique active-source count to 121: main 111, principal 42 and companion one, with overlaps. Of 572 old statement/proof blocks, 570 remain verbatim; the two amended blocks are the strengthened headline theorem and its proof map. One proposition/proof pair is added. No old block is removed. These source-path counts include shared display copies.

The original v57 response and maps remain historical records. The current `DEPENDENCY_LEDGER_V58.md` and `DEPENDENCY_MAP_V58.json` retain the substantive acquisition input and the distinction between semantic reading and syntactic regression. No A1, unrelated paper, default branch, historical report, collaborator permission or branch protection is changed.
