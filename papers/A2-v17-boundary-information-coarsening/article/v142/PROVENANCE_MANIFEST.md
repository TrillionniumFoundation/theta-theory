# Source provenance — A2 revision 139

The immutable immediate predecessor is native v138 at `cf4925b91608b806580bc0cd4fd8ce2441870c08`. The controlling v137 referee report is pinned at `ba24a0b2d062897ee2d7a6b02add81c504fe0e71` and retained verbatim in `review_inputs/`.

`PROVENANCE_MANIFEST.json` records the assembly commit, the report hash, all 310 inherited mathematical labels, unchanged TeX hashes and assembled input hashes. `NONDELETION_V139.json` records exact archives of every edited predecessor input. The new full proof is native TeX in `parts/20-finite-neighbourhoods.tex`.

The executed `evidence/BUILD_RECEIPT.json` binds fourteen finite scripts and three compiled PDFs to those inputs. It distinguishes local preflight from GitHub Actions execution. `revisions/a2-v139/` contains the checksummed transport overlay and the assembly/finalization programs; native output sources are also committed directly for normal referee access.

Source hashes and finite exact checks do not formally verify all-dimensional proofs or certify historical priority. The Ballico 1993 full-text comparison remains documentary-open. No predecessor manuscript or review branch is modified.
