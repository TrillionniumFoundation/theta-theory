# Logical dependency map — A2 v120

## What the current A2 core actually requires

The finite-algebra and conductor-boundary core is self-contained. Its hypotheses, classical inputs and proofs occur in the current manuscript. No theorem from a file labelled A1 is used as an unstated premise. This is a statement about the proof graph, not a claim that the project's historical naming has changed.

The mathematical chain is:

`unit normalization -> coefficient-ring codimension stabilization -> complete codimension-two Fitting presentation -> conductor incidence`.

The new boundary branch is:

`cube-zero augmentation -> quadratic multiplication tensor -> full ideal factorization -> complete Hilbert-function-(1,2,2) primary classification -> conductor-kernel strata`.

The projective interface is:

`regularity and kernel filling -> canonical global/finite multiplication cokernel -> the same Fitting scheme and its specified primary structure`.

The connected higher-contact family remains a separate application of the incidence and demonstrates unbounded nilpotent complexity. It is not discarded in favor of the bounded-length classification.

## Outputs available to A3 or later work

A later argument can invoke the canonical cokernel comparison, Fitting-ideal stabilization, exact-action conductor flags, and the classified boundary local rings, provided it verifies the generating, finite-local-freeness and degree/regularity hypotheses of the relevant theorem. These interfaces retain nilpotent structure. Replacing a full Fitting ideal by its radical is not the same input.

No specific current A3--D1 manuscript was verified here as already citing the v120 results. This note therefore identifies available logical interfaces, not invented downstream dependencies. Any program-wide integration should name the receiving theorem and verify its hypotheses explicitly.

## Retained historical material

The polar, quadratic, residual, wall, orientation, higher-product and unrestricted-hyperplane sections remain in the complete manuscript, followed by the contact-native, realization, component, structural and statistical appendices. They are not prerequisites of the new finite-algebra core. Their retention preserves the earlier theta-theory developments without forcing a false proof dependence.

The standalone geometry edition and the complete manuscript serve different reading needs. Neither is a replacement for an earlier branch; all original revision paths remain unchanged.
