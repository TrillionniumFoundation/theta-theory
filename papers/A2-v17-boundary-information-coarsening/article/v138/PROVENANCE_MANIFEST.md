# Source provenance — A2 revision 138

The immutable predecessor is the native v137 manuscript at review commit `ba24a0b2d062897ee2d7a6b02add81c504fe0e71`, which contains the controlling v137 referee report. The reviewed manuscript head is `d4f45624e91456ff135434a1236d8de2549ff0b9`.

`PROVENANCE_MANIFEST.json` records the assembly source commit, the controlling review, the reviewed source hashes, all 292 preserved mathematical labels, unchanged files, verbatim archived edits, and exact assembled file hashes. Remote assembly also preserves the controlling report in `review_inputs/`, with its recorded hash.

An executed `evidence/BUILD_RECEIPT.json` binds all thirteen finite scripts and all three PDFs to the source commit. It distinguishes local preflight from GitHub Actions execution. The readable overlays in `revisions/a2-v138/overlay/`, including this document, are applied after verified immutable-base reconstruction; the assembled manifest and executed receipt include the resulting source hashes.

Hashes and finite checks are not formal verification of the structural proofs or certification of historical priority. The Ballico 1993 full-text comparison remains documentary-open. The v137 manuscript directory and every review remain unchanged.
