# Revision 132 provenance

The full v131 article is the immutable base. All mathematical labels and inputs are retained; the revision adds packet descent and intrinsic generic web reconstruction. The JSON manifest binds the assembly commit and every executed source file by SHA-256. The build receipt records an actual build, not machine certification of structural proofs. Earlier response files are inherited history; the controlling response is RESPONSE_TO_REFEREE_V131.md.
