# Proof routes and publication units

## Paper II: independent of reconstruction

`lem:trace-lattice-v171` (explicit trace adjunction) + classical complete-intersection different + `lem:normal-complementary-v171` (height-one inverse differents) -> `thm:duality-conductor-v171` (actual conductor, arbitrary dimension and normal model).

That identity + trace Gram determinant -> `cor:defect-cycle-v171`.

That identity on two normal models + Jacobian chain rule + tame different transitivity -> `thm:conductor-tower-v171`.

Gotzmann embedding + explicit row count + all-power monomial integral closure -> `thm:chain-v171` (self-contained Hilbert chain and exhaustive cover).

General conductor identity + chain + irreducible residue polynomials and the DVR fundamental equality -> `thm:tangent-conductor-v171` (all a,r,s).

Three explicit normalizations + free sublattice conductor + scheme quotient calculation + ordinary endpoint blow-up induction -> `thm:square-tangency-v171` (all a, r=s=2).

Square-tangency charts + two explicit projective arcs -> `cor:tangent-lost-labels-v171` (same Hilbert limit, regular versus A1 normalization germs).

Exact semigroup and all facets + `lem:coordinate-ideal-v171` + cyclic invariant node ring + ordinary-power inequalities -> the complete fibre assertions of `thm:diagonal-v171`.

The general conductor identity gives its conductor assertions; `lem:conductor-depth-v171` separately expands the S2 extension argument, and `ex:unequal-cover-v171` gives an explicit conductor/fibre at a quotient crossing.

## Paper I: the inverse route is retained

Ungraded maximal-ideal filtration -> first relation space -> determinant rulings -> unequal coefficient-support ranks -> pencil Pluecker line -> scheme-level coefficient immersion -> sharp truncation and singular consequences.

No new Paper II theorem is an input. The appendix applications remain downstream and point to the complete technical supplement. The proofbody is retained byte-for-byte; that is preservation, not an external independent proof audit.

## Supplement and archive

`technical-supplement.tex` contains all 326 predecessor Paper II mathematical blocks. `geometry.tex` is the archival master. Its mathematical blocks are exactly the multiset union of Paper I, the new Paper II and the technical supplement. Predecessor front matters and disclosures are archived separately; they are not active publication front matter.
