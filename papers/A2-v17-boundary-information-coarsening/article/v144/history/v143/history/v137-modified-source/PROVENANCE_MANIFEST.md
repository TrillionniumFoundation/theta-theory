# Source provenance

The JSON manifest records the immutable v135 base, both controlling review identifiers, the assembled-source hashes, preserved predecessor labels and unchanged files. Its assembly commit distinguishes a local preflight from the source commit used in GitHub Actions. The executed receipt is in `evidence/BUILD_RECEIPT.json`; source hashes are not a proof certificate.
