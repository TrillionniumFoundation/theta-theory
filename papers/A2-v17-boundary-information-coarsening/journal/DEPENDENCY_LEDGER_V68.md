# A2 v68: mathematical dependencies and observation boundaries

The principal theorem retains label `thm:v66-main` for stable cross-references. It now includes smooth contact identification and finite-smoothness real-profile control. The title and abstract refer to revision 68; historical filenames are not revision identifiers.

| Conclusion | Proof dependencies | Scope retained |
|---|---|---|
| Relative physical law | `thm:v64-periodic-relative`, `thm:v64-physical-law`, finite-chain prerequisites and trace-norm localization | Supplied fixed clear nongrazing polygon; oriented returning periods; each derivative order fixed; correct physical clock and unconditioned area factor. |
| Complete action extraction | `prop:v65-two-offset` | Phase-resolved laws at two separated positive offsets, common amplitudes and known physical momenta; unknown separate normalizers cancel; positive residual floors. |
| Exact curvature and finite jets | `thm:v66-curvature`, `thm:v65-cyclic-jets`, actual smooth envelope and jet factorization | Positive curvature image, physical signed cycle, no candidate closeness; not arbitrary globally realizable data. |
| Uniform one-step collar | `lem:v68-real-visits`, positive Jacobi weighted implicit construction and uniqueness of shifted tails | Constant-one `a^j` contraction on one common real collar; finitely many marked phases; positive class, not a grazing/weak-hyperbolicity uniform limit. |
| Real functional separation | `prop:v68-weighted-separation`, actual envelope and terminal control | Same low jets; weighted `|u|^m` norm; `6a^m/(1-a^m)<1`; pair-dependent operator, not an observation-only algorithm. |
| Smooth germ rigidity | `thm:v68-smooth-rigidity`, exact curvature/jet inverse, real functional separation | Complete smooth contact germs; no analyticity or closeness; full action functions used again after jet matching. |
| Real profile stability | `lem:v68-finite-comparison`, `thm:v68-smooth-stability`, polynomial alignment and two-offset extraction | Fixed `C^(m+3)` graph bound, `C^m` law error, `C^0` profile output, small data error and positivity floors; realizable pairs only. |
| Flat physical information | `prop:v68-flat-family`, local envelope positivity and area first variation/implicit function theorem | Actual closed smooth equal-area obstacles; all graph/action jets fixed; complete laws differ; not identical normalized density jets or marked length spectra. |
| Exact whole obstacle images | `thm:v66-global-rigidity` plus actual analytic continuation | Connected closed analytic boundaries, fixed placement/lattice and labels, every obstacle visited. No extension to unvisited smooth arcs. |
| Local analytic norm inversion | `thm:v65-analytic-inverse`, `thm:v65-real-stability` | Original bounded-holomorphic inverse and conditional real-law theorem with their radii and analytic prior; independent of the new finite-smoothness estimate. |
| Finite-flight and finite-iteration jets | `prop:v66-finite-flight`, `eq:v67-residual-error`, `eq:v67-off-fixed-recursion`, `eq:v67-propagation-constants` | Original `C_M(tau^N+epsilon)+L_M E_m`; fixed order and class-dependent constants. |
| Nonlinear stable return | `cor:v64-transmission` and observed amplitudes | Physical endpoint normalization and marked quadratic reconstruction, not arbitrary detector changes. |
| Weaker-data and charged-experiment results | All earlier alternating-channel, unregistered, calibration, matching, analytic and statistical inputs | Each original observation model and prior retained. No new general-period sample budget or derivative inference from total variation. |

The new input adds a functional step to the central proof chain. Supporting propositions are not counted as separate principal breakthroughs. Mechanical checks retain all inherited active inputs and all labels in edited TeX files. Their scope is distinct from proof review.
