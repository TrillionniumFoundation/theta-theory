# Observation and theorem-dependency map — revision 72

The principal question is determination of actual smooth reflecting contact functions from rare endpoint records. Analytic whole-table continuation and intrinsic registration are retained consequences under other observations, not premises of the smooth theorem.

| Route | Supplied | Observed | Determined | Operative proof |
|---|---|---|---|---|
| General smooth theorem | Clear nongrazing polygon, frames, phase labels, two distinct positive offsets, exact gate/event | Two limiting conditional endpoint laws per phase | Complete smooth contact germs | v64 relative physical law → v65 coordinate/two-offset action separation → v66 global quadratic inverse and v65 signed blocks → v68 actual envelope |
| New oblique theorem | Same marks, nonzero marked momenta, positive offset range, exact gate/event; no numerical offset | One conditional law per phase at a fixed unreported offset | Offsets and complete smooth contact germs | v64/v65 actual physical law → v72 anchored quotient, clock and signed factor separation → v68 action-level smooth theorem |
| New finite oblique theorem | Bounded positive smoothness/clearance/area/obliquity class, independent resets, exact tags/gates, bounded post-acceptance readout error | `rB` preparations including failure symbols | Realizable table/offset estimator with complete-profile error and offset error | v72 uniform forward bounds + v69 derivative kernel lemma + realizable pair selection + v72 stability + charged binomial balance |
| Historical symmetric one-offset route | Same-type alternating channel, known positive offset, signed coordinates and its stated gap/registration inputs | One same-type law at each contact | Actions and the stated smooth jets / analytic and multichannel consequences | v42 fixed-anchor rank-one square factorization and the retained downstream modules |
| Local/exterior comparison | Local collars, clear trajectory tubes and specified record; equal area for charged equality | Exact finite local records versus separately defined complete exterior data | Exact locality/contact equivalence and actual distinct remote completions | v71 locality and two-bump completion, with corrected planar Noakes–Stoyanov theorem for the exterior distinction |

## New proof interfaces

`prop:v72-clock`: the scalar mixed derivative identifies the positive offset; all normalization and separate endpoint factors cancel.

`thm:v72-separation`: fixed signed nonzero anchors separate unequal actions. A `C^m` input, `m≥2`, gives `C^m` actions because derivatives are evaluated only at finitely many fixed points, not along a variable axis.

`thm:v72-rigidity`: the known momenta restore gauged actions. Global positive quadratic inversion, finite signed jets and the actual weighted envelope are inherited intact. Stability uses the already proved action norm estimate, with polynomial alignment before the weighted norm.

`lem:v72-obliquity`: deterministic first-hit geometry, not genericity, covers all polygons with at least three distinct physical contacts.

`thm:v72-sampling`: fixed nuisance offsets are included in the realizable law image. The forward bounds are checked uniformly, all success-count and bandwidth conditions remain explicit, and the charged rate is not called minimax.

## Content not used as a hidden premise

The later real-to-complex estimates, full analytic continuation, lattice registration, finite holonomy ambiguity, moving-family arguments, older coarsening/acquisition catalogue and companion proofs remain present, with their original hypotheses and attributions. Their preservation and compilation are not a new independent mathematical audit. No repeated formulation or page count is used as evidence of separate novelty.
