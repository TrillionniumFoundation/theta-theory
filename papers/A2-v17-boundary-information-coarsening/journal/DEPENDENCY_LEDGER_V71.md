# Revision 71 dependency and observation ledger

The complete central route in `DEPENDENCY_LEDGER_V70.md` is retained: v64 relative/physical law -> v65 graph-coordinate and two-offset extraction -> v66 global quadratic identification -> v65 signed finite jets -> v68 actual smooth inverse/stability -> v69 realizable finite-preparation estimate. No premise or bound in those five modules changes.

## Additional argument

| Claim | Inputs | What is actually proved |
|---|---|---|
| `prop:v71-locality` | Identical local single-edge length functions; v64 weighted bridge uniqueness; v64 exact physical measure; common free-segment/clearance margins | Exact area-ratio scaling of finite success subprobabilities, hence conditional equality; equal area gives identical charged failure/success records. |
| `cor:v71-local-completion`, local part | v65 two-offset extraction, v66 positive quadratic inverse, v65 signed finite jets and v68 actual smooth inverse; locality converse | Equivalence between two-offset law germs and marked contact equivalence. Common gates are small enough for both graphs; normalization is specified. |
| `cor:v71-local-completion`, realization | v64 three-obstacle table, two compactly supported disjoint remote normal bumps, nonzero area derivative and the implicit function theorem | Nonconstant equal-area actual closed convex completions; all local finite charged records agree. |
| Exterior comparison in the same corollary | The constructed compact obstacle unions; corrected Noakes–Stoyanov Theorem 1.1 | Their complete exterior travelling-time and scattering-length data are different. This implication is comparative, not an input to local recovery. |

The record consists only of a failure atom or successful endpoint mark after an independent reset. There is no rejected-ray path, no hidden complete exterior scattering relation and no claim about the unreset trajectory model. Adaptive choices with a fixed finite number of resets follow by equality of the conditional record kernels. A different recording/error kernel must not be silently substituted.

The locality proof is not counted as a new nonlinear inverse method. It explains why the existing main result is a local information theorem rather than a restatement of global exterior recovery. The classical roles of weighted contraction, kernel approximation, minimum-distance selection and binomial concentration remain distinguished from the actual billiard estimates that make them applicable.
