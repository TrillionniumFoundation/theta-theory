# Principal-article dependency ledger — revision 64

The v63 ledger and all inherited modules remain. Both `rigidity.tex` and `main.tex` actively include `article/10a_periodic_itinerary_relative_v64.tex` after `v4/10_boundary_layers.tex` and its `v5/15_differentiated_operators.tex` continuation; their introductions include the same `article/00e_periodic_mechanism_overview_v64.tex`.

## New proof chain

| Label | Inputs and full proof location | Output / scope |
|---|---|---|
| `lem:v64-geometric-jacobi` | Chord differentiation, specular reflection, positive obstacle curvature; new module, first subsection | Actual periodic Jacobi coefficients, uniform coercivity and persistence. No normal form assumed. |
| `lem:v64-reference` | Positive determinant-one transfer matrices, finite stationary Schur complement; second subsection | Exact reference twist and nonlinear cofactor identity. |
| `thm:v64-periodic-relative` | The two preceding lemmas; weighted walk expansion, local contraction, half-line sums, gluing, entrywise trace summability, determinant compression and trace series; third subsection | Relative finite-to-half-line law for a fixed even oriented period, a fixed common collar and separately fixed mixed derivative orders. |
| `cor:v64-transmission` | Exact double integration of the mixed derivative; stationary concatenation and exact reference-twist ratio; fourth subsection | Connected-action coefficient and normalized scalar stable/past linearizing densities. These are consequences of the relative theorem. |
| `thm:v64-physical-law` | Restored physical endpoint momenta, canonical flux, full-phase residual interval, normalization floor; fifth subsection | Charged probability and limiting conditional law for a selected positive endpoint window. Not the whole maximal-collision event. |
| `prop:v64-triangle` | Explicit disk and triangle geometry plus the proved persistence lemma; final subsection | Actual nonnormal three-obstacle realization and an open family outside the normal two-contact setting. |

All six proof bodies are printed in the principal article and in the full technical manuscript; no proof is replaced by an external-review citation. The original relative proof is retained as the detailed alternating case. The general-periodic module does not depend on downstream contact inversion or statistics.

## Unchanged downstream interfaces

The complete signed smooth inverse uses the alternating two-contact stationary-envelope and its triangular blocks. The analytic inverse uses the retained bounded-holomorphic function-space argument, not just determinant-one finite blocks. The real-observation theorem retains its prior, inverse neighborhood, anchor and radius loss. Finite-preparation results continue to use realizable-law fitting and the original full-phase channel mass. The entire v62 finite-experiment module, including its v63 correction, is unchanged.

Full-only prerequisites remain routed through the same source-matched external auxiliary files as in v63. The unchanged native builder verifies this actual producer/consumer relationship. No principal statement acquires an unprinted new prerequisite in the full manuscript through the v64 addition.

## Verification boundary

The new checker verifies retained source bytes and active inputs, exact finite recurrences and finite actual-geometric controls. The native build verifies source consumption, external auxiliaries, references and products. Neither operation is a formal mathematical proof or a decision about journal significance.
