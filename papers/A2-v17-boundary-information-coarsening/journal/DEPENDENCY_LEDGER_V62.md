# A2 v62 — proof dependency declaration

All inherited classifications in `DEPENDENCY_LEDGER_V60.md` and the v61 expository declaration remain in force. No existing proof module is edited or removed.

The new shared module is `article/23f2_finite_experiment_analytic_inverse_v62.tex`.

| New statement/application | Substantive mathematical inputs |
|---|---|
| `lem:v62-finite-densities` | Relative endpoint estimate `eq:v8-relative`, exact full-phase density and cofactor normalization, common positive-offset support/normalizer bounds. Uniformity on the analytic class uses Cauchy bounds on the smaller real collar and uniform geometric margins; no infinite-dimensional parameter differentiation. |
| `thm:v62-finite-contact` | The preceding lemma and the complete two-point conditional analytic inverse `thm:v60-real-observation-stability`. Cauchy's coefficient estimate yields simultaneous coefficient control. |
| `thm:v62-budget` | Finite-density/acceptance lemma, self-contained binomial and Bernoulli exponential estimates, cellwise real Lipschitz bound, countable dense-image selection, and the same analytic inverse. Independent normalized phase-volume preparation and exact calibrated fixed gap/frames are explicit experiment hypotheses. |
| `cor:v62-calibration` | Uniform offset/density/acceptance bounds, a cellwise boundary-tube computation, a supplied good observable pilot with deterministic cap, and the analytic inverse. Its conditional proof does not assume a density for the displaced observed law. |
| `cor:v62-total-budget` | **Substantively imports** full theorem `thm:v25-observable-calibration` and its cap `eq:v25-pilot-cap`, including compact marked-class, position-sensor, signed-design and preparation hypotheses. The older `cor:v47-physical-histograms` is cited as a complementary full-table conclusion, not as a replacement for the pilot. |

The principal article's `journal/full_reference_routes_v62.tex` imports the existing route unchanged, then explicitly aliases the full pilot theorem, pilot cap and full-table histogram corollary. Thus the new physical pilot application is not called self-contained or merely a comparison. The exact-calibration theorem does not depend on that pilot.

The inherited principal `cor:v45-physical-chart` still substantively imports full `thm:v25-global-physical-reconstruction` and all its original hypotheses. No reference is downgraded from prerequisite to background.

The new results concern complete local analytic contact germs on a smaller disc. The new rate is not an all-smooth bound, a global continuation or lattice rate, an unknown-origin recovery algorithm, a mixing theorem for one trajectory, or a global acquisition-rate theorem. The exactly calibrated power is distinct from the slower calibration-inclusive power, which substantively imports the retained pilot theorem. The complete older results concerning those distinct interfaces are retained. These distinctions are part of the theorem scope, not reasons to abandon the program.
