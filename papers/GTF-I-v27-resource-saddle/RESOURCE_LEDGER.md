# Resource ledger — revision 27

The unknown candidate parameter is fixed for the whole execution. Training-dependent data, calibration, random seeds, event selectors and any reusable time value must pass through the declared register. Runtime controllers do not read offline occupation arrays or adversarial priors.

| Result | Charged resource | Exact conclusion |
|---|---|---|
| Saddle realization | Every declared finite causal cut; buffers and shared rows explicit | Compatible normalized positive generators characterize exact saddle attainment |
| Marked decision converse | Complete post-training register; other cuts may be larger | Five states necessary and sufficient for exact U2 throughout gamma in [6/25,13/50] |
| Forward serial peak | All training, selection, candidate bit/mark, target bit/mark cuts; candidate read first | Twelve states necessary and sufficient for exact U2 |
| Construction | `(1,2,3,3,4,5,5,5,10,12,10,10,7,3)` | Five event labels; atomic stochastic selection; two training + two validation preparations |
| Global width-four converse | Whole peak at most four, any legal validation order | Positive ideal score loss; persists for sufficiently small target noise |
| Precision | Exact algebraic selection coin distinguished from finite fair-bit approximation | Inherited precision loss and origin/comparison/phase buffers retained |
| All-N localization | Unrestricted training memory; actual two-bit report pair estimated, fair mark known | Value error at most sqrt(3/N); not an N–peak frontier |
| Causal transport | Simulator state multiplied by controller state at each cut | No free calibration or internal selector |

The twelve-state minimum is **not** a theorem for all reverse-order or interleaved validations. The five-state decision lower bound is order-independent. The exact ideal width-four gap is positive by compactness but is not numerically evaluated; the allowed noise persistence interval is accordingly existential with an explicit physical modulus.

The earlier routing bottleneck K remains distinct from its m-state action buffer. The integer revelation decision width remains distinct from collision bit-level peak. All inherited resource coordinates and constructions remain at their original paths.
