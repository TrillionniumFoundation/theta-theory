\section{Provenance, preservation, and review navigation}\label{app:provenance}
\subsection{Exact current submission and controlling report}
The current report is on branch
\begin{quote}\small
\path{review/a1-english-v3-harsh-referee-2026-09-05}
\end{quote}
with the following pinned identities:
\begin{quote}\small
Review commit: \\ \texttt{574f2315a136d8b401644d8a3eeeb93c87887010}\\
Reviewed submission: \\ \texttt{025a9b3fdfd9ffa86e6ae2924f8b5e5b1b58ddd6}\\
Reviewed A1 tree: \\ \texttt{ec09fa385c5fdcc5f2aece59a6b7f0806bfbb8ea}.
\end{quote}
The complete report, its thirteen-entry claim audit, and its twenty-check
referee program are retained unchanged on the new branch. The report did
not certify the paper by those diagnostics and did not inspect its PDF.
This edition supplies an ordinary source directory rather than a workflow
that has to materialize an orphan source tree.

The preceding English-v2 review and its frozen source tree are, respectively,
\begin{quote}\small
\texttt{40be02de7479712d866a575c4b0b9ec930699282},\\
\texttt{900059b847980a27be4866d495b00eeb96562dc5}.
\end{quote} Its sixty-three-entry audit
and twelve diagnostics are historical evidence, not a new audit of every
inherited theorem. The v3 foundation tree
\texttt{833389dfccd571f989f6143ac44b7de0d0626e69} is preserved as the
\path{foundations/} companion without deletion of its mathematical sections.
The current principal Appendix~\ref{app:repairs} supplies the sharpened
reporting-space qualification and explicit outer-flux convention; it is
the controlling current statement when reading the inherited companion.

\subsection{Actual reuse rather than status inheritance}
For this revision the complete v3 principal mathematical source, the full
current report and claim audit, and the historical companion's hybrid
sensitivity and whole-preparation Lorentz sections were read. In particular,
\path{foundations/sections/13_hybrid.tex} supplies the distinction between
smooth terminal states and smooth reports; its preparation transports,
weight derivatives, and normalizer are retained in Appendix~\ref{app:repairs}.
\path{foundations/sections/16_lorentz_global.tex} supplies the whole-equilibrium
tube identity, its coverage argument, and its negative-Sobolev reporting
construction. Its ambient normalization is not conflated with a counted
placement attempt. These are focused source reuses, not a fresh audit of
all eleven papers or all historical branches.

The previous edition's historical derivation map records its consultation
of the round-twelve audit and the round-seventeen local primitive and pointer
source. The latter identity is retained here as an elementary mechanism:
\[
 B_j(q,p)=((q-s_{j-1})/w_j,s_{j-1}+w_jp),\qquad
 B_j^*(P\,dQ)-p\,dq=d\{s_{j-1}(q-s_{j-1})/w_j\}.
\]
Direct substitution gives this formula with no $dp$ term; it does not
establish a global symplectic quotient. The pointer calculation in
Section~\ref{sec:lab} uses the same local Hamiltonian principle. No global
quotient, conormal operator, spectral, renewal, or central-limit theorem is
imported from a historical status label. Nor is a many-collision Cauchy
hierarchy assumed by the new state or control results.

The corrected foundation companion retains the full inherited smooth-flow,
entropy, chronology, approximation, local Lorentz, and whole-equilibrium
arguments. The present main text is independently compilable and contains
all proofs needed for its new statements. Preserving the companion does not
turn an inherited execution receipt into a new build or proof certificate.

\subsection{What is preserved and what is added}
The current manuscript keeps every one of the thirteen original principal
proof-bearing labels. The laboratory, attainability, positive-filter,
and fixed-controller response source files are retained verbatim; the
coefficient-stability formulas and proofs are retained verbatim with an
explicit historical qualifier on its old verification discussion. The original control proof is changed only to correct
its intrinsic-dimension wording; its compactness, measurable representatives,
zero-evidence extension, and finite-menu proof remain present. The first
section keeps the operational theorem and adds a structural theorem. The
appendix retains the biased-entropy and transported-preparation mechanisms,
while correcting the readout qualification and making the two-trace outer
flux explicit.

Sections~\ref{sec:structural}--\ref{sec:advantage} add the Gram right inverse
and perturbation margin, the general intrinsic lower bound, optimal endpoint
policies, a two-arc last-step solution, quantitative endpoint lookup,
positive normalized physical surrogates, and a certified adaptive advantage
against the full nonadaptive class. The endpoint lemma and elementary
model-error suggestion are explicitly credited to the current referee;
neither is used as a substitute for the stronger class and performance
results. The matched polynomial degree and the controls' failure records
remain part of one instrument throughout.

\subsection{Reproducibility entrance}
The root \path{README.md}, \path{RESPONSE_TO_REFEREE.md},
\path{PROOF_LEDGER.md}, and \path{HISTORICAL_DERIVATION_MAP.md} in this
submission directory identify the current source and the point-by-point
response. New executed checks and their scripts are distinguished from
historical tests. The exact adaptive certificate gives every one of the
64 nonadaptive vertex values, all eight adaptive first-gate values, and
all continuation comparisons used in the proof. It is a finite arithmetic
certificate following an analytic all-Borel reduction, not a substitute for
that reduction or a proof-assistant verification of the entire manuscript.

The new branch preserves both prior manuscript and review trees. Current
validation receipts specify which tests and which PDF were actually built
and inspected. The companion's earlier build and referee receipts are
historical only. No independent rereview or journal acceptance is claimed
by this author revision; the next referee has a pinned source, proofs,
comparison class, and executable numerical inequalities to examine.
