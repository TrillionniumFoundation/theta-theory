# Foundation review priorities

Check the corrected bias objective, two density traces, and preparation product/normalizer terms at labels `prop:constraints`, `thm:shape`, and `thm:chamber`. Then review the new principal manuscript one directory above, especially its exact coefficient filter, matching continuous-state dimension bound, full acceptance-interval control theorem and common-support likelihood response. The unchanged review is the comparison source; current metadata must not be confused with inherited validation.
