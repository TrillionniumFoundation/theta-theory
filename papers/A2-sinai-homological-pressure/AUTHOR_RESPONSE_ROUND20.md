# A2 Author Response — Round Twenty

The active A2 source is now genuinely revised.  The impossible unweighted sum
over countably many return branches has been replaced by a theorem in which
every branch norm is multiplied by its inverse Jacobian and summed through the
uniform exponential return tail.

The parent-fold strong/weak Banach bundle is constructed in transported stable
curve charts, including branch births and compact embedding.  The periodic and
UNI obstruction is no longer delegated to a JSON summary: the manuscript
states the reflection equations, uses positivity of the length Hessian for
continuation, constructs four independent periodic differences, and writes
the common-suffix cancellation needed for the temporal derivative estimate.

The very-high-frequency proof has been replaced.  Two disjoint returned UNI
blocks provide two physical coarea coordinates; one integration is made in
each.  The derivative order is fixed and the weighted bad-family estimate is
summed by the growth lemma.  Thus no unknown constants `c_M` are used to choose
`M` after the frequency split.  This estimate is combined with the compact and
Dolgopyat ranges to give a globally integrable raw Fourier majorant.

The revision also proves absolute continuity of the roof coordinate, the
unsmoothed four-dimensional lattice--continuous LLT with `n^-2` scale, full
covariance nondegeneracy, and separately delimited local, central, and
saturated conditioning regimes.

No theorem has been downgraded or replaced by a no-go result.  The historical
certificate remains only a regression record; the active TeX source contains
the mathematical proof.
