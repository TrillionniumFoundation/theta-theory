# Author Response — Round Eight (A2)

## Disposition

All mathematical objections in the round-seven report are answered by a new controlling reconstruction; no theorem is replaced by a no-go statement and the paper remains active.

## Point-by-point reconstruction

1. The vanishing birth direction is renormalized by |R-R0|; no impossible ambient uniformly bounded inverse is claimed.
2. Compact-frequency periodic separation uses a finite chartwise family of five-word packets, while high-frequency UNI is proved separately from a geometric temporal-distance derivative.
3. The master LLT integrates the n^{-3/2} sum density over a roof interval of length nG_n and recovers the missing factor n.

## Controlling source

`revision/round8-referee-final/A2_RENORMALIZED_BUNDLE_UNI_LLT.tex` is byte-identical, after the generated header, to `ROUND8_POSITIVE_CLOSURE.tex`.

Compilation and repository-internal counterexample gates are necessary checks; independent external referee validation remains separate.
