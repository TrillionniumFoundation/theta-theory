# Historical material, not current delivery status

The active entry point is `../main.tex`. The v5-reviewed, v6-reviewed and v8-submission directories preserve earlier derivations and submission documents. Their delivery receipts, dates, numerical counts and proposed branches concern those versions only. They are not evidence of the status of v10.

The completed v9 source is pinned at `33ef794a398b23015651482e93be7667c08d6fad`. Its full lossless publication remains at the repository root under `.publication/a2-v9`; its restoration entry point remains in `papers/A2-v9-nonlinear-boundary-compatibility/restore_sources.py`. No existing source or review is overwritten by v10. The downloadable packet additionally retains an authenticated v9 reference snapshot.

The new active article retains all 168 v9 theorem, lemma, proposition, corollary and proof environments byte for byte, and all v9 active inputs in the same order. New manuscript material is identified in the current proof ledger. Retention is a textual invariant, not a proof certificate.

The Round 33 arithmetic/Fourier chapter remains here with its explicit hypotheses. It is not used as an unconditional transfer-operator premise of the near-onset billiard argument. The two-collision companion remains separately compiled because its fixed-window observation differs from the long-bridge experiment.

The historical inputs used in this revision are the v3 weighted stationary-action and full-phase construction, v4 half-line and relative-determinant arguments, v5 differentiated operators and contact jets, v6 experiment transfer and physical-family inverses, v7 acquisition and testing, v8 fixed-window and joint minimax comparisons, and the completed v9 profile and smooth-envelope arguments. This is a dependency-focused use of the repository history, not an assertion that every historical manuscript was re-refereed.
