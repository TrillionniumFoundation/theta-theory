# Historical derivation audit for A2 v14

## Frozen inputs

The author source is `0e54099f079232df233316ae6fe7986fc51b7ea1`, manuscript tree `ee946ef91770778839f15c8c35416d401e99ea1c`. The primary review is `b3f0ad5843782c221651c9189fc156d20865cab1`; the earlier same-v13 review is `f29d8f96e87bf46db2ffbe8217caa09563c5e536`. Current versions were identified from live GitHub branch/commit reads, not from a Library filename.

The previous complete reproducible source packet was materialized for local compilation because the container could not resolve the GitHub host. Its main source, preamble, companion and seven metadata files were matched to the live Git blob identities. The complete local `article` tree matched the live v13 article tree. The primary report and analytic benchmark were likewise matched to their live Git blob identities. The new native tree is based directly on the complete remote manuscript tree, not on an assumption that the local packet contains every inactive historical file.

## Direct mathematical reading for this revision

The primary report and its analytic normal-form companion were read in full, together with the earlier same-version report. The existing introduction, main source, historical audit, operator comparison and the relevant action, half-line, physical integration, energy-profile and two-flight passages were examined. The key dependencies are:

- `v3/10_geometry_action.tex`: physical generating length, effective Hessian, reference twist and finite stationary construction.
- `v3/20_integration.tex`: phase-volume normalization and fixed-domain residual integration.
- `v4/10_boundary_layers.tex`: half-line action, trace-class boundary amplitude and differentiated relative factorization.
- `article/15_operator_comparison.tex`: classical Jacobi/trace ingredients and smooth Morse transport.
- `article/20_boundary_compatibility.tex`: energy pushforward constants and complete-profile Volterra uniqueness.
- `article/29_two_flight_benchmark.tex`: scope of the exact germ and the constructed finite-family observation result.

The prior independent-contact, physical-image and acquisition conclusions are retained within their printed hypotheses, and their existing diagnostics were rerun. This note does not claim a fresh line-by-line proof audit of every historical appendix theorem.

## Mathematical use of the historical route

The v14 first-hit equation is the stationary equation for the tail of the v4 half-line action. Its derivative uses the original positive endpoint Hessian. The density cocycle follows from exact Schur concatenation and the existing normalized factorization; it does not replace the determinant proof with an absolute error estimate. The intrinsic-width normalization is checked against the v9 energy pushforward, not inferred from a formal Taylor series. The direct two-flight normalization note uses the original leading coefficient and keeps the sampling pilot separately charged.

## Preservation

The 52 existing active inputs remain active, and their 218 formal statement/proof blocks remain byte-identical. Section 8 adds six formal blocks. Changed main/introduction/normalization/bibliography text and metadata are retained under `history/v13-reviewed/` with their original contents. Prior manuscript directories, primary review files, inactive historical trees and verification tools remain in the repository. Only a new revision branch and its new manuscript directory, plus a non-destructive README index addition, are created.
