# A1 v36 — common normalization and crossover interpretation

**Attainable information and causal compression at exponent collisions**  
Qian Qi — 8 September 2026.

Revision branch: `revision/a1-english-v36-common-normalization-2026-09-08`.  
Manuscript directory: `papers/A1-english-v36-common-normalization/`.  
Controlling review: `review/a1-english-v35-harsh-independent-2026-09-08`, commit `30ea5daf9c1f5cf345061f940b55bd559e2e1686`.  
Reviewed manuscript: `02a19f2ddb83cf68bfbf8361c137613e2ffd3925`.

`main.tex` and `companions.tex` are the complete English native entrypoints. The current build has **42 main pages and 159 companion pages**. The two bounded clarifications are in Corollary 13.3, p. 38, and Remark 13.4, p. 39. The former fixes H = 16 and one L >= 8 across all three acquisition problems and verifies admissibility; the latter specifies that crossover orders concern the comparison envelope, not exact optimizer transitions. The main theorem and every existing risk conclusion are unchanged.

Read `RESPONSE_TO_REFEREE_V36.md` for the pointwise response and `PROOF_LEDGER_V36.md` for the historical dependencies and preservation map. `CHANGES_FROM_V35.diff` gives the entire substantive source diff. The `v35` theorem labels are intentionally retained.

The repository directory preserves the entire v35 native subtree, including inactive historical material and complete original modules. All 222 theorem-like blocks and 210 proof blocks remain present. Exactly two statements receive the clarifications and one proof gains the admissibility bound; no mathematical result or proof is deleted. All companion sources are unchanged. The standalone native review package contains the complete compilable source closure and current evidence, not a mirror of every inactive repository file.

## Build and checks

In this directory, with a complete TeX Live installation and Python 3.10 or later:

```sh
python build.py
python v36/verify_clarifications.py
python -O v36/verify_clarifications.py
python v35/verify_revision.py
python v35/author_verify_revision.py
python v35/independent_core_checks.py
```

The clarification checker uses only the standard library. The inherited spectral checker requires `sympy` and `mpmath`. `build.py` invokes the unchanged native builder at `v35/build_native.py`, supplying this directory and `--edition v36`; its location is historical, not the edition being built. It cleans generated entrypoint outputs, compiles both full volumes, exports actual labels, checks convergence and recorder input closure, and rejects unresolved references and overfull boxes.

`verification-v36/NATIVE_BUILD_V36.json` contains the current compact receipt with all source hashes reconstructible from the preserved v35 manifest. `BUILD_RECEIPT.json` and raw compiler stdout logs are included in the downloadable native package and are regenerated by the builder. `EXECUTION_RECORD.json`, `PRESERVATION.json` and `VISUAL_INSPECTION.json` distinguish execution, source preservation and sampled layout inspection. Earlier verification directories retain their historical meaning.

Four programs were executed in both ordinary and optimized Python with matching outputs. The current full build resolves references after three paired cycles and has no final overfull boxes. These checks accompany the analytic proofs; they do not establish an exact optimizer phase diagram, formal proof certification, exhaustive priority, or journal acceptance. No GitHub Actions execution is claimed.
