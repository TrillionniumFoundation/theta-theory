For positive sparse monomial experiments, we determine finite-memory
prediction uniformly through additive collisions of the exponents.
Maximal future determinant volumes, truncated by the dimensions actually
acquired, govern every integer label budget. The classification combines
attainable confluent flags, whole-image covering and one causal filter
whose updates contain no reciprocal collision gap.

We then characterize the nonregenerative common-filter optimum by finite
history--state compatibility equations. Finite command partitions
approximate its mean and worst-history risks with explicit errors uniform
in the label budget and through collisions, without continuity of the
coding rule or a persistent public seed. Exhaustive finite controller nets
give global certificates. The attained lower bound converts these into
relative certificates uniform across changes of dimension. A positive
two-checkpoint experiment has an exact unseeded two-label solution and an
unbounded risk ratio between two admissible acquisition schedules.

For independent edge experiments, the acquired profiles determine
adaptive graph-memory laws and reusable acquisition menus. We distinguish
fixed-calibration stabilization, collision-dependent menu penalties and
the separate hypotheses of graph-size-uniform readouts. Local small-ball
bounds yield occupation converses; under conditional regeneration an exact
statistical common-flow formula is realized by reset encoding. These
regenerative equalities retain their specified seed, descriptor and loss
conventions rather than replacing the compatibility constraints of a
shared posterior.
