# Historical and remote-source audit — v10

## Frozen source chain

The latest GTF-I report located for this revision is `e5373d129545b269ee4f5477a94542517dbfd0d1`, reviewing the v9 referee-ready head `71d5fa4c5d8b6d6f0f3d5c61aba07cedd342373a`. Its build receipt names mathematical source `6bcd0de01f7d164a8feab28f82d44f6149ba385f`. The existing v10-marked-compact head `04890dfca0b7dc9fca657049fe9700330828ad95` adds only a workflow to the report head. It contains no completed v10 manuscript. Our distinct marked-duality branch starts at that exact head and leaves the existing branch unchanged.

A source snapshot from GitHub Actions run `35778350206`, artifact `10717215463`, supplied the complete checkout at `04890df...`. The local source was reconstructed from that archive because the isolated container does not have direct GitHub transport. Remote reads and writes use the authorized GitHub connector. A branch creation is not represented as publication of a manuscript; the mathematical source and generated PDFs receive separate commits in this revision's release process.

## Materials used

The full controlling v9 report, the complete v9 central proof sources, its response/ledger/history/pipeline documents, the retained v8 spectrum and support framework, and the v7/v6/v5/v4 bodies and manifest closures were used to determine preservation and dependencies. The prior historical ledger inventories all eleven pipeline components and their retained review gates. Its exact source entries remain in the v10 contract.

The model-facing inspection focused on the historical A4 history-kernel/variation claim, the C1 transition-versus-observation indexing, the B4 nonlinear exponential generator and range condition, and the D1 one-policy phase/log-moment issue. The new paper supplies independent primitive-model proofs for these interfaces. It does not treat old labels such as “positive closure” as proof certificates, and does not claim to have independently re-proved every historical manuscript.

The preservation manifests include the v9 input closure and all additional source/document inputs. The complete-development TeX entry retains the earlier mathematical bodies and adds the v9 historical introduction. An exact-input audit and a compiled-label audit supplement, but do not replace, mathematical reading.

## Current A2 identity

A fresh branch search found revision 126. At `0ce25b1a492c230876930d45daf3f4d55a276358`, the root README still points to revision 82 and is not an authoritative current manuscript entry. We therefore read:

- `papers/A2-v17-boundary-information-coarsening/article/v126/geometry.tex`, Git blob `9d329d86f010f8c310b8943406325bc248c15b27`;
- `papers/A2-v17-boundary-information-coarsening/article/v126/frontmatter.tex`, Git blob `0e023cf2dd9d715ef0c64995707292e56c3310c8`.

The title and explicit imports identify the nilpotent-specialization/stratified-contact/polarized-reconstruction primary line. No theorem dependency on GTF is asserted from this entry inspection. The inherited finite A2 acquisition adapter keeps its own older pinned source and is not relabeled as the whole current primary paper. This update is a provenance and architecture audit, not an independent proof audit of v126.

## Preservation policy

No original source, old report or other branch is overwritten. The v9 report-only saturation source is copied locally and retyped explicitly; the stronger new marked theorem is supplied separately. The Gaussian source is copied with its proof intact and an added formal signature. All other v9 core sources are imported unchanged. The mathematical article is separated from response, status and pipeline governance files.

The branch graph and commits are frozen observations, not a promise that no new revision can appear elsewhere during or after the build. The new build receipt identifies the exact source that was compiled.
