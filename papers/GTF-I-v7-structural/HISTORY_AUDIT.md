# Whole-pipeline consultation — seventh structural revision

## Controlling objects

The current GTF review is the v6 Markov report at `ff34427b2a5539ad23e2aea5cd57ce9f88d2b627`, reviewing `f6f08bd529be23b7b97f5cc78829006c08e29e67`. Its complete numbered report, not an older response letter, controls the revision.

The fixed eleven-paper historical snapshot is `c04845b6613208406703695c9c184ae461f95805`. The consulted archive is GitHub Actions artifact `10678671849` from run `35691901705`, SHA-256 `9243458a9994c7cce192d583498c21c229f18f2239a5db7b0a3e318beaadfb13`. It contains 9,202 files. This number describes the archive, not a claim that every file was read. All eleven complete Round-20 reports and the native theorem/section inventories were freshly consulted; additional native proof reads are listed by range in `HISTORY_INPUT_MANIFEST.json`. `HISTORY_NATIVE_INVENTORY.json` records the exact heading lines and source hashes.

The master document's first-volume chain is positive experiments, future-test quotients, causal morphisms, information loss, attainable geometry and finite-memory implementation. The new canonical paper restores that sequence. Its G1–G4 goals are research interfaces, not an assertion that every physical platform already verifies them. The first-edition addendum is preserved with its historical A2-v112 cutoff.

## Component-by-component use

The fixed dependency chains are `A2 -> A3 -> A4 -> C2 -> D1` and `B2-GC -> B1 -> B2-MC -> B3 -> B4 -> C1/C2 -> D1`, with A1 separate. A modern paper reusing the A1 or A2 label is not silently substituted for a historical obligation.

| Historical component | Actual consultation and lesson | Relationship to the new proof |
|---|---|---|
| A1 exact benchmarks | Full Round-20 report and native theorem inventory; separate response/benchmark claims from general realization. | Bayes geometry is reproved from actual conditional means; no old CLT or response theorem is assumed. |
| A2 Sinai homological pressure | Full report and native inventory; raw vector/roof LLT and uniform Fourier estimates are model obligations. | The new count protocol is the modern A2 experiment, not a replacement proof of the Sinai LLT. |
| A3 empirical path LDP | Full report and native inventory; controlled stopped paths need exponential estimates and actual clock control. | A finite-horizon TV simulation is not promoted to a path LDP. |
| A4 history/memory pressure | Full report, inventory, and native argument lines 1–150. | Summable variations are not identified with exponential contraction. The new posterior contraction is independently verified on the actual instrument. |
| B1 microcanonical preparation | Full report and inventory; exact-number coefficients and preparation normalization remain physical data. | Preparation costs and failed outcomes remain in the causal experiment; they are not erased by conditioning. |
| B2 collision/cluster LDP | Full report and inventory; actual interaction/contact kernels cannot be supplied by an abstract quantizer. | No particle LDP is used by the tree or finite-experiment proof. |
| B3 Hamilton–Boltzmann cotangents | Full report and inventory; covariance, observability, process CLT and Mosco identification are distinct. | A2's Gaussian covariance is derived from its specified Poisson exposure, not an unproved LDP Hessian. |
| B4 nonlinear kinetic semigroups | Full report and inventory; common-core, m-dissipativity and semigroup generation require separate proofs. | The bounded probability-measure recursion is not called an unbounded kinetic closure. |
| C1 information/control | Full report, inventory, and native lines 1–175. | The new-state observation kernel is typed explicitly. A nonlinear measure update replaces unproved finite moment closure. |
| C2 cotangent rigidity | Full report and inventory; common domains and covariance scales do not follow from category language. | Resource transformers and error metrics are explicit; no cross-platform operator identity is asserted. |
| D1 theta phases | Full report and inventory; a phase label requires an actual stochastic state and posterior evolution. | Hidden shift coordinates and physical count observations are explicit; no phase theorem follows by relabeling them. |

These reports are historical reviewer assessments, not journal decisions or proofs of impossibility. The revision continues constructing positive mathematical results without treating historical claims as proven premises.

## Modern A1 and A2

The A1-v37 publication ref was rechecked at `90465076589f5e5c69227d624f278c47744f1c1d`. The predictable-network source was read for conditional regeneration, finite descriptor charging and causal flow implementation. The new checkpoint and resource-transport statements supply the corresponding foundational interface; they do not claim to reprove all of A1's multilevel geometry. Its exact source edition remains preserved.

The substantive A2-v118 mathematics is fixed at `44bfc648ead008896a6981a7302a6d5ab8b21bb8` (native source `84ea29f752ca223bbcda1eb8d155adc02082dc28`). Its higher-defect action/conductor stratification, nonreduced multigenerator/fat-point transport and statistical-experiment chapters were consulted in native form. Exact copies already preserved under the v6 revision are included in the inherited build inputs. The new JSON manifest gives full-file hashes, Git blob identities and read ranges.

The newer named v119 ref is `d4254d9b01405ad02c64d2ff591503d4cc7a6aa7`. Direct GitHub comparison against v118 showed exactly one added independent v118 review and no manuscript mathematics. Thus the latest named branch and the substantive mathematical endpoint are different facts, both recorded. No assertion about branches created after this discovery is implied.

The actual new A2 dependency is:

`declared known-mark exposure -> exact contact-score matrices -> v7 physical comparison theorem -> general Gaussian finite-output necessity theorem`.

The mean map has full row rank and the fixed score covariance is positive definite; a compact local neighborhood contains a mean ball. These verified statistical hypotheses give both sides of the label law. The algebraic conductor/action-rank strata do not determine noise without the declared exposure, and the theorem is not claimed uniform through covariance rank collapse. No A2 geometric source is modified by this revision.

## Preservation and scope of reading

All previous mathematical bodies remain in the complete-development companion and all original source editions remain at their original paths. Fresh reading, heading-only inventory and inherited consultation are separately marked. The archive and source manifests make the consultation reproducible, but neither source hashes nor retained labels certify mathematical validity. Independent referee evaluation of the new proofs is a separate task.
