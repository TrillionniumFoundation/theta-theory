# Final addendum: actual nonconjugate specular radial U3

This addendum is controlled by `maximal-strengthening/FINAL_SCOPE_CORRECTIONS.md`.

## Theorem P1-SINAI-RADIAL-U3-EXACT

For a finite-horizon periodic Sinai table in which one circular scatterer is radially inflated while centres remain fixed, assume a regular normal two-cycle persists.  Then:

1. the family is nonconjugate, because in Jacobi coordinates
   \[
   \operatorname{tr}M(a)
   =2+4d(a)(\kappa_1(a)+\kappa_2)
    +4d(a)^2\kappa_1(a)\kappa_2
   \]
   has strictly nonzero parameter derivative;
2. after pullback to normalized boundary coordinates, the invariant density
   \[
   \rho_a(i,x,\varphi)
   =\frac{\ell_i(a)\cos\varphi}{2\sum_j\ell_j(a)}
   \]
   is analytic in `a`;
3. the complete same-occurrence source at order `j<=3` is
   \[
   S_j=(I-L_0)\rho_0^{(j)},
   \]
   and hence
   \[
   R_0S_j=\rho_0^{(j)};
   \]
4. for every analytic constant-plus-coboundary twist
   \[
   \psi_a=c(a)+g_a-g_a\circ T_a,
   \]
   the twisted leading eigendata are explicit through
   \[
   L_{a,q}=e^{qc(a)}M_{e^{-qg_a}}L_aM_{e^{qg_a}}.
   \]

This is an actual source/projector U3 and an all-order cohomological twisted-eigendata theorem.  It is not a claim of arbitrary noncoboundary parameter differentiability of the full reduced resolvent.

## Export

```text
P1-SINAI-RADIAL-U3
```

has exactly the scope of the preceding theorem.
