# Theorem-level contribution and proof ledger — v19

All theorem labels below are resolved to source and PDF locations by the build. “Proof supplied” describes the manuscript, not an independent certification.

| Statement | Classical / inherited input | New statement and proof step | Consequence |
|---|---|---|---|
| `thm:v19-residual` | Classical residual equivalence; v18 compatible continuation covers | Exact stochastic implementation width; pairwise state-overlap sum gives total word-error lower bound | One intrinsic continuation count controls construction and obstruction |
| `thm:v19-binarywidth` | Classical likelihood testing; v18 binary reset value | Compute all majority continuation counts; weight cut errors by the least nonzero likelihood gap | Exact width profile attaining optimal reset value and strict deficit below it |
| `lem:v19-counter` | v17 private marked behavior law; empirical second moments | Four supported marked coordinates collapse to two report-pair counts; sign-regret and clipping proof | 400 training resets, 504,008 retained states with transcript and validation costs |
| `lem:v19-posterior` | Bayes and signed total variation | Clear the observation denominator before integration under either law | Global posterior L1/L2 estimate for the next proof |
| `thm:v19-global` | v18 common-preparation likelihood and entropic BSDE identities | Bounded terminal tilt identifies numerator log coefficient as a posterior signal; global H2 control and the BSDE integral identity | Global own-path integrand and backward-integral convergence, not only localization |
| `thm:v19-composition` | v18 exact gap, moving collision, Gaussian transport and effective presentation | Use the constrained private family for the small fixed audit; transport it and the global backward consumer with one physical modulus | Executable finite-register certificate persists through genuinely changing microscopic trajectories |
| `thm:v19-main` | All above | Organized statement of the classification and composition chain | Canonical entry point, not a separate unproved theorem |

The fixed-width auditor family is generally nonconvex. The new obstruction does not invoke a minimax exchange on it. An independent persistent selector is treated conditionally in the proof, not absorbed into a width-K state without charge. The binary lower bound concerns attaining the unrestricted reset value; its explicit deficit is not claimed sharp in every asymptotic regime. The nineteen-bit bound is sufficient and is not a minimality assertion.

The analytic transport hypotheses remain fixed positive noise, common preparation, bounded signals and the explicitly stated scalar bounded entropic terminal condition. Global reference H2 convergence has no new explicit rate claim. Coupled stochastic integrals are defined under their own marginal filtrations, not under an unjustified common Brownian filtration.

Inherited theorems retain their version credit in `PROOF_STATUS.json`; all new statements have `independent_review_pending`. The theorem-status graph records source identities and declared proof dependencies. It does not automatically verify their truth. The original Norberg proof-level priority comparison remains unverified.
