# Historical derivation map used in v21

The controlling report is the v20 independent report at
`078f34222b00797203cdf6dd421eab9f9f428c59`. The mathematical baseline is the
published v20 submission `6f103ad252d7c65f140720f4095585026f7bb1b9`, not an
older report or an unexecuted intermediate source transport.

The published source package was retrieved from the recorded v20 workflow
artifact. Its source manifest was matched to the Git blob returned for the
pinned published manuscript, and every one of its 729 listed source hashes
was checked. That checks the identity of the retrieved material; it does not
claim a new proof audit of every archived file.

## Derivations consulted for the revision

The operational model, mixed-moment positivity, binomial product tangent,
confluent prefix arguments, global thin-rectangle lemma, and full collision
checkpoint/streaming chain were used directly to isolate the transfer
hypotheses. In particular the complete actual-history mass argument retains
the report-word probability and integrates kernel coordinates. The raw
moment recurrence, not an inverse divided-difference update, is the source
of gap-free causality.

The v18 structural and affine developments locate the limits of a rank
classification and give the physical covariance law used for the moving-
kernel example. The v19 full-support moment-image and rectangular-attainment
arguments supply the dimension-free separation mechanism, while their
printed dimension ordering explains E20.1. The complete v20 quotient chart,
exact-kernel criterion, forced closure and balanced-block proofs were used
without reopening the already resolved v19 containment objection.

The inherited circular, operational inverse and ambiguity developments are
preserved in the full appendices. The original v17 inverse remains pinned
against its original source manifest, not merely compared with a mutable
copy in the new revision. Earlier confluence and affine-filtration routes
are compiled in the technical appendices rather than interleaved with the
minimal proof of the main theorem. The v19 and v20 historical maps and
source ledgers remain unchanged for deeper version provenance.

## Source-level disposition

The new `main.tex` and introduction replace the journal narrative, while the
old front matter remains in `history/v20-editorial/` and the complete v20
source remains in its original sibling directory and commit. Technical
sections are listed explicitly in the appendix input groups in main.tex.
Their heading commands are locally demoted where necessary; theorem, lemma,
proposition, corollary and proof environments are not silently removed.
The one formal correction is recorded as exact old/new text in
`REVISION_EDITS.json`.

No existing review branch, old manuscript version, CI workflow, main-branch
file, branch protection or member permission is changed by this revision.
Publication adds a new branch and its v21 materialization/validation workflow.
