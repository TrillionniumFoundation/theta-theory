# Round-Twelve historical derivation audit

## Fail-closed rule

The canonical recursive/CM2 corpus, the v83 control index, archived pre-governance sources, and the controlling packets from rounds three through eleven were searched before reconstruction.  A status word, compiler name, conditional interface, synthetic PASS, or orphan theorem was not accepted as mathematical credit.  A prior argument was reused only after its source/target spaces, physical incidence, estimates, and dependencies were restated in a round-twelve controlling proof.

## Findings

The previous corpus supplies useful local mechanisms: parent-branch moving-cut coordinates, Young/renewal coding, Green normal traces, real-trajectory cluster weights, source-dependent saddles, compressed resolvents, positive disintegrations, and phase-labelled contraction bookkeeping.  The round-eleven reports exhibit new counterexamples at precisely the missing model interfaces: source--target sheet topology, fold realization, full renewal residuals, past-kernel mixing, full-frequency hard-sphere pressure, physical Jacobi products, nonautonomous covariance control, exponential-jet comparison, positive slice evidence, signed cotangent duals, and microscopic phase localization.

Repository searches found no earlier document containing a complete graded flag-current complex, deterministic-clock controlled renewal LDP with residual kernel, polymer characteristic-pressure theorem, summed physical-Jacobi transversality modulus, positive evidence cone, common-domain memory form bundle, or canonical peripheral-band phase sheaf.  Those tools are new in round twelve.

## Reuse ledger

- A1 reuses exact affine branch generating functions and common symbolic path typing; the target-refinement tower and graded flag currents are new.
- A2 reuses finite-horizon geometry and returned triangular-gap branches; the parent-fold identity and multi-block very-high-frequency estimate are new.
- A3 reuses the inducing kernel and exponential return marks; the entropy-control deterministic-clock theorem with terminal residual is new.
- A4 reuses Doob eigen-data and compressed resolvent algebra; the Harris proof, quenched rough theorem, and integrable renewal-resolvent strip are new.
- B1 reuses the source-dependent saddle; the full pressure Fourier gap replacing impossible spatial blocks is new.
- B2 reuses the Green trace convention and absolute tree weights; the summed fixed-genealogy Jacobi modulus and continuous kinetic Hodge repair are new.
- B3 reuses exact pressure covariance normalization; the perturbative nonautonomous energy theorem and local-noise kernel characterization are new.
- B4 reuses the dynamic-only action; the resolvent core and primal verification comparison are new.
- C1 reuses coarea and evidence lifting; the positive slice/current separation and compact evidence-direction cone are new.
- C2 reuses strict weighted Radon duality; the signed invariant quotient and common-domain form bundle are new.
- D1 reuses finite-mixture bookkeeping; the canonical positive peripheral-band phase sheaf is new.

No paper is removed, no headline conclusion is replaced by a no-go statement, and no gap is hidden by an uninstantiated compiler.
