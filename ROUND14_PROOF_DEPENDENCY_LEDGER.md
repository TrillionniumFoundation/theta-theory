# Round-Fourteen proof-dependency ledger

**Locked reports:** `review/round13-gpt56-pro-harsh-11paper-2026-09-01@7bdc2ef6fd248fd18fa347e02dce3c40883bb173`

## A. Non-circular order

### Sinai chain

1. **A1** is independent.
2. **A2** constructs the parent-fold transfer bundle, full lattice rank, returned UNI, exhaustive Fourier estimates, and the four-coordinate LLT.
3. **A3** uses A2 only for the uncontrolled reference excursion kernel and proves a graph-completed deterministic-clock controlled renewal LDP.
4. **A4** uses A2's vertical bounds and A3's stopped renewal state to prove weighted coupling, rough convergence, and final-Schur memory.
5. **C2** uses the completed A4 form/resolvent interface.
6. **D1** may contract only the component laws already proved by A3/A4.

### Hard-sphere chain

1. **B2-GC** proves the compatible trace graph, fixed-horizon trajectory expansion, actual-contact Hamiltonian, and grand-canonical joint LDP.
2. **B1** extracts the exact particle-number coefficient from that grand-canonical source pressure and proves the source-dependent shell theorem.
3. **B2-MC** transfers the B2 joint LDP after B1.
4. **B3** constructs the joint density/contact covariance and process limit from B1/B2.
5. **B4** constructs the ensemble boundary generator and kinetic Nisio semigroup from B2/B3.
6. **C1/C2** use only the completed B1--B4 interfaces.
7. **D1** performs positive phase disintegration only after component LDPs and coefficients exist.

## B. Round-Fourteen innovations

- geometric mass, not refinement depth, controls A1 flags;
- four Fourier coordinates give the A2 factor \(n^{-2}\);
- A3 uses one stopped entropy-control action and charges the terminal excursion;
- A4 extracts descriptors from the final Schur complement after cancellation;
- B1 removes the empty-sector atom by exact-\(N\) coefficient extraction;
- B2 uses fixed-genealogy actual Jacobi maps and dominated summation;
- B3 noise acts on \(\Delta p+\psi\) and tightness is unconditional;
- B4 obtains the exponential collision term from the ensemble boundary Green identity;
- C1 keeps zero-evidence directions in an actual blow-up and uses growing Lipschitz coordinates;
- C2 separates strict Radon and strong distributional duals;
- D1 aggregates nonlinear phases by log-sum-exp.

## C. Fail-closed publication gates

1. All 11 Round-Thirteen reports are present and their exact SHA-256 values match the inventory.
2. Every numbered objection appears in a paper-local Round-Fourteen response.
3. Every registered source is byte-identical to the active `ROUND14_POSITIVE_CLOSURE.tex`.
4. Every theorem-like environment has a proof and every local reference resolves.
5. The declared dependency graph is acyclic.
6. Direct counterexamples from the reports are mechanically replayed.
7. All 11 papers clean-build with zero undefined references or citations.
8. The mathematical tree is frozen before certificate generation.
9. The pre-publication `main` is archived and `main` is moved only after all gates pass.
10. No downgrade, no no-go substitution, and no deletion of D1 are permitted.
