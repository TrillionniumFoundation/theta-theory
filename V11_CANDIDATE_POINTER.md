# v11 deterministic hard-sphere kinetic cotangent candidate

The controlling v11 source is stored under:

```text
review/v11-hard-sphere-kinetic/
```

The ordered base64 parts reconstruct a SHA-256-pinned source-only tarball. The inherited v9 `papers/` tree is not the controlling v11 manuscript source.

Platform: `HSBG-LIO-v1`. The candidate is an internal research draft in the short-time, bounded-regular-source Boltzmann–Grad scope. It is not a long-time dynamic LDP, a deterministic hydrodynamic Navier–Stokes LDP, or an external correctness certificate.
