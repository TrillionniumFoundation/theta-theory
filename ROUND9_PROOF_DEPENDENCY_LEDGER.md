# Round-Nine Proof-Dependency Ledger

## Non-circular order

### Sinai chain

1. **A1** is an independent exact physical/symbolic benchmark.
2. **A2** constructs the moving-billiard blow-up bundle, arithmetic packet,
   geometric UNI, high-frequency bounds, and density-level vector--roof LLT.
3. **A3** uses A2 finite-source estimates to prove the marked edge-flow and
   pointed physical/collision path LDPs.
4. **A4** uses the proved A2/A3 recurrent phase to construct the past kernel,
   quenched enhanced limit, and compressed-resolvent memory.
5. **C2/D1** use only the completed A2--A4 interfaces.

### Hard-sphere chain

1. **B2-GC**: compatible trace graph, all-contact cluster expansion, physical
   recollision estimate, and source-uniform grand-canonical pressure.
2. **B1**: source-dependent exact-saddle conditioning of B2-GC.
3. **B2-MC**: B1 transfers the B2 joint LDP and recovery laws to the shell.
4. **B3**: duality, constrained tangent curvature, covariance and process CLT.
5. **B4**: law-state nonlinear semigroup and viscosity limit.
6. **C1/C2**: controlled beliefs, contractions, optional projections and
   resolved memory.
7. **D1**: exact phase-labelled mixture and commutation theorem.

No B1 theorem is used in B2-GC.  B2-MC is declared only after B1.  No C/D
statement is used to establish an A/B parent theorem.

## Paper-level merge gates

| Paper | Required controlling gate |
|---|---|
| A1 | physical impact section distinct from symbolic extension; directional anisotropic response; conditional calibration |
| A2 | material birth connection; model-specific bundle; chartwise periodic certificate; independent UNI; density LLT with correct interval scaling |
| A3 | actual edge/profile state; finite projected LDP; common projective recovery; recession mixture; pointed clock completion |
| A4 | genuine past Feller kernel; quenched rough theorem; correct memory sign; instantaneous/residual split; full-Q realization |
| B1 | full connected source pressure; typical covariance; exact saddle; full mixed Fourier estimate; weighted shell coefficient |
| B2 | closed trace graph; positive renewal hierarchy; physical Jacobi integrated gain; scaled sewing; balance-preserving recovery; good joint LDP |
| B3 | regular-phase KKT coercivity; backward observability; covariance-first CLT; Mosco identification; process tightness |
| B4 | exact law state; resolvent graph core; finite-generator correctors; dynamic-only action; entropy-truncated comparison |
| C1 | stratified current tower; evidence lift; exact belief DPP; two-saddle coefficient; reachable chaotic reduction |
| C2 | closed linear nullspaces; weighted invariant separator; scale Kato transport; distinct resolved projection; correct memory derivative |
| D1 | exact labelled microscopic mixture; phase-specific recovery; projective topology upgrade; phase-specific shells and likelihoods |

## Fail-closed publication rules

1. Every paper loads exactly one `ROUND9_POSITIVE_CLOSURE.tex` copied
   byte-for-byte from the registered round-nine source.
2. Every theorem-like environment in a controlling source has a proof.
3. All local references resolve and all labels are globally unique.
4. Counterexample regressions check the explicit failures in the round-eight
   reports, not merely the presence of keywords.
5. All eleven papers clean-build with zero undefined references/citations.
6. The exact verified tree is committed before certificate generation.
7. The old `main` is archived and the verified final tree is published by an
   atomic, force-with-lease update; concurrent `main` changes are merged and
   all gates rerun.
