# A2 revision 135

**Intrinsic reconstruction of webs from nonreduced multiplication-failure schemes** — Qian Qi

Read `geometry.pdf` as the principal article. `supplement.pdf` contains the complete technical boundary and specialization results; `complete.pdf` combines both bodies for archival reading. All three have editable LaTeX drivers with corresponding names. The build generates cross-PDF mathematical references; run the full build rather than a single LaTeX pass.

The controlling review is the v134 report at `a08b157800c27c0f73f0c5c9a52155265ef4f395`. See `RESPONSE_TO_REFEREE_V134.md`, `ISSUE_MATRIX.json`, and `LITERATURE_AUDIT_V135.md` for the point-by-point response, its documentary limit, and exact primary-source comparisons. The complete Ballico 1993 comparison is **not** marked closed.

Run `bash build.sh` in this directory. It executes ten finite exact scripts and compiles all three PDFs. `evidence/BUILD_RECEIPT.json` records actual page counts, source/PDF hashes, dependencies, the controlling commits, preservation checks, and evidence scope. It is not a machine-certified mathematical proof or a referee decision.

`PROVENANCE_MANIFEST.json` binds the assembled sources to the immutable v134 baseline. All 241 inherited mathematical labels are retained in the complete manuscript; the accepted universal-readout and common-coordinate proof files are byte-identical. The original v134 article and reviews remain unchanged elsewhere in the repository. Historical v131–v134 audit files retained here are provenance, not the current response.
