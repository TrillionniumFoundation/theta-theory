# Reproducible A2 v132 source assembly

The authoritative referee-facing article is the expanded `papers/A2-v17-boundary-information-coarsening/article/v132` directory published by the isolated workflow. This staging directory is not a substitute for that article.

`assemble.py` copies the complete v131 source from review commit `f0ac12d5ce2084fffec3a6245e3cbe8c2e2b39ff`, applies three readable patches, and adds the two new mathematical sections from `overrides`. It preserves prior responses as history. Old PDFs, execution logs, and receipts are excluded from assembly and regenerated by the build. `expected-source-sha256.json` checks exact equality with the locally executed source before any CI build is accepted.

The assembly commit, immutable review commit, inherited hashes, current source hashes, and staging hashes are recorded separately. The later publication commit contains the resulting PDF and receipts. No force push or historical branch update is part of this workflow.

The new science is packet descent on the original base and generic intrinsic reconstruction of the web. The source and response delimit what is proved, what is checked by exact arithmetic, and what remains unclaimed.
