# A1 v36: frozen submission unit

Title: **Attainable information and causal compression at exponent collisions**.  
Author named in the source: Qian Qi.

The release reference `release/a1-v36-submission-2026-09-08` points to the original author commit `8f074b8027627a71a81a362b9d15f47975e1f3ae`. The branch is a publication pointer, not a technical immutability guarantee. Cite the commit for immutable source identity. No mathematical source was changed in making this reference.

The complete native entrypoints are `papers/A1-english-v36-common-normalization/main.tex` and `companions.tex` at that commit. The associated review is `reviews/a1-english-v36-harsh-independent-2026-09-08/REFEREE_REPORT.md` at `1e7af61f0812638d00ee0efad8fc9a68e2776c3c`. Its favorable recommendation concerns its stated main-article scope and is not a journal decision or a certification of all companion results.

## Source and production identity

The source closure is recorded by `verification-v36/NATIVE_BUILD_V36.json` and its explicitly identified base `verification-v35/NATIVE_BUILD_V35.json`. The v36 source changes select two clarified comparison modules and a new main entrypoint; the other active inputs retain their recorded blobs. There are 80 active TeX inputs in the recorded native closure. The current execution inspected these manifests and verified the remote release reference. It did **not** rebuild the 42+159-page v36 unit or re-certify its 80 inputs independently. The previously recorded author/reviewer builds remain separately dated evidence.

An exact checkout can be built using the existing `build.py` in the manuscript directory, with its declared TeX and Python dependencies. The main and companion cross-reference one another, so a main-only smoke build is not a substitute for that complete native build.

## Submission readiness and author decisions

Completed in this execution: dedicated release pointer; immutable source/review map; separation from dynamical A1; a draft cover letter; a companion role map; no added mandatory theorem.

Before an actual journal submission, the author must select the journal, verify current submission instructions, confirm authorship and affiliations, approve the final package and declarations, and establish exclusivity/related-publication statements. None of those declarations has been invented here. No submission, editor email, referee invitation, merge into main, or repository-permission change has occurred.

## Role of the companion

Proofs actually used by the main article must remain directly available. Independent extensions may be evaluated for later papers, but no automatic splitting or independent acceptance is implied. Historical alternative routes, diagnostic receipts and provenance remain repository material. Per-report, block-charged and public-descriptor protocols retain their different resource models.

## Separate workstreams

Statistical A1 studies additive exponent collisions and causal information retention. DYN-A1 studies response and fluctuations of a moving-cut conservative map. A2 concerns actual specular collision geometry; B2 concerns interacting hard-sphere histories. The term collision does not identify these mathematical objects with one another.
